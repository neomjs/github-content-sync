---
number: 412
title: >-
  feat(kb): the Knowledge Base ingests the github-content-sync corpus as its own
  tenant (#402)
author: neo-opus-vega
state: OPEN
createdAt: '2026-09-22T22:30:06Z'
updatedAt: '2026-09-22T22:33:27Z'
closedAt: null
mergedAt: null
head: vega/402-corpus-tenant-extractor
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/412'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #402

🌿 A conversation the Knowledge Base cites now names the repository it came from, and a ticket number alone can no longer say which conversation it means.

One built-in extractor, `ConversationCorpusSource`, replaces the three per-facet Sources that walked the Engine's frozen `resources/content` tree. It reads the org corpus that `neomjs/github-content-sync` publishes hourly — the root `_index.json` plus the `<origin>/{issues,pulls,discussions,archive}` trees — through the route-bound revision reader, so repository, revision and territory come from the tenant invocation and nothing is recovered from `AiConfig` or cwd. Every identity comes from the index: a file the index does not name yields nothing (the filename fallback is gone), an index row without an origin refuses the run, and two artifacts for one `(origin, facet, id)` refuse. The conversation's origin rides `customMeta` and the chunk name (`neo/issue-86#body` vs `neo-agent-brain/issue-86#body`); ownership stays the server's stamp for the corpus tenant, so the extractor never writes `repoSlug` or `tenantId`. The tenant is declared in `deploy/cloud/kb-config.yaml` with `disabled: true`; the flip and the plane-side receipts are #411, gated on #237. The default registry drops from ten Sources to seven; three specs and three `sourcePaths` keys leave with them.

Evidence: L2 (unit — the real profile runner over a real revision reader bound to a fixture Git mirror; no deployed plane in the sandbox) → L2 required (AC-1 to AC-4 are code and spec criteria). Residual: the deployed-plane activation and freshness receipts (the ticket's former AC-5 to AC-7), Residual-Owner: #411.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | CI-covered: `test/playwright/unit/ai/services/knowledge-base/source/ConversationCorpusSource.spec.mjs` runs one route (`_index.json` + origin roots) through `ExtractorCatalogue`; `extractorCatalogue.spec.mjs` lists the descriptor; `SourceRegistry.spec.mjs` asserts seven defaults with no conversation facet. Outside-CI: `grep -rn "resources/content" ai/services/knowledge-base/source/` → one hit, `ConceptSource.mjs:10` (JSDoc of a different facet, out of scope, restated on the ticket); `js-yaml` parses `deploy/cloud/kb-config.yaml` to 5 repos, the fifth carrying `disabled: true` and the `ConversationCorpusSource` profile. |
| AC-2 | CI-covered: spec arm 1 — two origins sharing `#86` yield 10 distinct chunks from one invocation, no chunk carries `repoSlug` or `tenantId`, `customMeta.origin` equals the path's first segment; arm 3 — a row without `repoSlug` rejects with `KB_CORPUS_INDEX_ROW_UNQUALIFIED` and zero writes; arm 4 (CONTROL) — a route whose territory omits `_index.json` rejects with `KB_REVISION_READER_PATH_OUTSIDE_SCOPE`. |
| AC-3 | CI-covered: spec arm "OWNERSHIP CONTROL" — `diffTenantManifest` with the corpus manifest under `github-content-sync` makes 0 engine rows actionable; the same manifest under `neo` makes `engine-source` actionable (negative control, not shipped). |
| AC-4 | CI-covered: descriptor arm (`1.0.0`, `deltaSafe: false`, `requiresHierarchy: false`) and the canonical `origins` option (`{origins: [' neo ', 'devindex', 'neo']}` → `['devindex', 'neo']`; empty or unknown keys throw); arm 1 pins the selected-file set — `yieldedSourcePaths` equals the five indexed conversation paths, `_index.json` is skipped as `index`, `neo/issues/chunk-1/issue-999.md` as `unindexed`. |

## Deltas from ticket
- AC-1, AC-4 restated on the ticket (2026-09-22): the shape is add-one-extractor / delete-three-Sources, so "no literal remains" became "no code path resolves it" and "bump the normalization version" became "the new descriptor is an identity input by construction".
- AC-5 to AC-7 (deployed-plane readiness, manifest and freshness) moved to #411, natively blocked by #237, so this leaf closes on its code and the activation is a one-line flip with its own receipts.
- `deploy/cloud/kb-config.yaml` gains the tenant entry with `disabled: true` — the ticket said "declared where tenants are declared" without naming the file.
- `ConversationCorpusSource.spec.mjs` is appended to `brain-unit.yml`'s executing list in the same commit that creates it.

## Test Evidence
- Mutation, two guards, one run: refusal of an unqualified row disabled **and** the filename fallback restored → 3 of 9 arms red (arm 1 minted `neo/issue-999`, arm 2 saw the extra chunk, arm 3 saw no refusal); reverted, 9/9 green.
- `DatabaseService.sync.spec.mjs` (only the dead `PullRequestSource` scope-out removed) is red on this checkout for an unrelated reason: `Class hierarchy coverage regressed below its interim floor: node_modules/neo.mjs/apps 92.9% (floor 93.0%); ai 0/176` — the checkout's hierarchy artifact, not this diff; the spec is not in `brain-unit.yml`'s executing list.
- `agent-preflight --no-fix` exit 1 on `check-ticket-archaeology`: three pre-existing refs in touched files (`configBase.mjs:103`, `:115` `ADR-0019`; `_export.mjs:87` `#11658`), zero introduced by this diff (two `D#17846` citations I had added were removed); not rewriting another author's load-bearing ADR references here.
- Corpus contract measured live at `github-content-sync` `dev@e5830396`: 19,441 index rows, 0 without `repoSlug`, 0 duplicate paths, `version: null` = active under `<origin>/<type>/`, `vX.Y.Z` = archived under `<origin>/archive/<type>/<version>/`.

## Post-Merge Validation
- [ ] The activation: flip `disabled: true` after #237 is dispositioned, record the corpus-owned manifest + revision-bound receipt, then `ask_knowledge_base` citing a same-day `neomjs/neo` conversation and a `neo-agent-brain` one.

Residual-Owner: #411

Authored by Vega (Fable 5.1, Claude Code) 🌿. Session fc04c361-0cae-4a80-9506-fa2ef4785d2b.
