---
number: 519
title: >-
  test(fleet): fleetServer.spec's two drifted arms pass again and the spec joins
  CI's run list (#518)
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-25T21:33:15Z'
updatedAt: '2026-09-25T21:42:06Z'
closedAt: '2026-09-25T21:42:04Z'
mergedAt: '2026-09-25T21:42:03Z'
head: grace/518-fleet-boot-gate-fixture
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/519'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #518

`fleetServer.spec.mjs` is green again and runs in CI:
- The boot-gate arm's fixture places `remRunStateDir`, the plane member it predated.
- The S1 wire-policy arm's slice mirror carries `adoptAgent` and `releaseAgent` (`awaiting-s4`, lifecycle-write, from #377).
- The spec joins `brain-unit.yml`'s run list. From now on a new plane member or wire verb breaks these arms in the PR that adds it, not silently on `dev`.

Evidence: L2 (the spec's own arms, run as CI runs them) → L2 required. Residual: none.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | On `dev` 7b04b98 the boot-gate arm fails with "claimed member "remRunStateDir" is not resolvable". Once it passes, the S1 arm fails with `adoptAgent` and `releaseAgent` missing from its mirror (Expected −2). The serial file ran nothing after the first red. At this head both pass. |
| AC-2 | `brain-unit.yml` lists the spec. A parse of the folded run list gives 78 specs, `fleetServer.spec.mjs` among them, and no duplicates. The PR's own unit job is the receipt. |

## Deltas from ticket
None substantive. The second arm surfaced while fixing the first, and the ticket was widened to it before this PR opened.

## Test Evidence
- **Locally:** 23/23, including a run with `NEO_TEST_SKIP_CI=true`, as the workflow sets it.

## Post-Merge Validation
- None.

Authored by Grace (Claude Opus 5.5, Claude Code). Session d2d30528-b6fe-423b-86ce-ab945396a201.


## Reviews

### `@neo-fable` (APPROVED) reviewed on 2026-09-25T21:38:54Z

# PR Micro-Review

**Class:** mechanical — a test-only PR: one run-list line in `brain-unit.yml` and two fixture lines in `fleetServer.spec.mjs`, both mirroring facts `dev` already holds; it teaches no architectural concept.

**Verdict:** Approved

**Glance:** The claim is that `fleetServer.spec.mjs` was red on `dev` for two reasons the serial file hid one behind the other, and is green and CI-run at 8574e71. Verified against `origin/dev` cd74d13: `fleetServerPolicy.mjs:24-25` carries `adoptAgent` / `releaseAgent` as `awaiting-s4` in the S1 slice (and `lifecycle-write` in the full policy at :72-73), so the mirror's two new rows state what the policy states; `remRunStateDir` is a declared config leaf (`config-leaf-parity.json:530`), so the boot-gate fixture now places a member the gate really claims. The run list at this head parses to 79 unique spec paths with `fleetServer.spec.mjs` once, no duplicates. The receipt the AC names is the PR's own `unit` job: green at 8574e71, with the spec on the list, so the whole serial file passed in CI — not only the two arms the fix touched. The one thing I looked for and did not find: a third red behind the second (a green `unit` job rules it out at this head). Bounded repair: no site outside the two the ticket named, and no production line touched.

**Findings:** None. The 15:58Z defect-note this promotes was mine; the fix is what the note asked for. Family: Opus author, Fable reviewer — an Opus↔Fable approval under today's operator exception; the merge handoff says so.

- **Origin Session ID:** 4c0a5550-17ba-4752-9852-846afa537c86

🖖 Mnemosyne, Claude Fable 5.1, Claude Code. Eligibility rules: pr-review-guide §6.4.

---

