---
number: 470
title: >-
  fix(deploy): the kb-, mc- and fleet-server caps cover the heap limit, native
  memory and the probe (#469)
author: neo-opus-vega
state: OPEN
createdAt: '2026-09-24T20:23:15Z'
updatedAt: '2026-09-25T10:06:27Z'
closedAt: null
mergedAt: null
head: vega/469-memory-caps-hold-heap-native-probe
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/470'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #469
🌿 *The heap-ceiling guard can no longer pass a memory cap that leaves no room for native memory or the probe.*

A cap must hold V8's heap limit, native memory and the healthcheck's own `node` at once. The probe counts because it runs in the same cgroup. If the cap is lower, a full heap ends in the kernel's silent SIGKILL instead of V8's `Reached heap limit`. This PR sizes three caps by that rule and makes the guard assert it:
- **Caps.** mc-server goes from `1g` to `2g`, kb-server from `1g` to `1536m` and fleet-server from `512m` to `768m`. Measured totals at the new caps are ≈ 1.21 GiB, ≈ 1.06 GiB and ≤ 600 MiB. The tables are in #469.
- **Guard.** `DeclaredHeapCeilings.spec.mjs` asserted only "strictly below", which admitted mc-server's 768 under the 1g it was killed at. It now adds each API server's measured non-heap memory, `NON_HEAP_MB`.
- **CI.** Brain Unit only listed the spec; now it runs it.
- **Compose notes.** The kb-server note states the rule, and the mc-server and fleet-server notes point to it.

Evidence: L1 (the spec, with a control run at `origin/dev`'s caps) + L2 (`docker compose config` renders; V8 heap limits measured under each new cap) → L2 required. Residual: none.

## AC Evidence
| AC | Coverage |
|---|---|
| AC-1 | Outside CI: `docker compose config` of the base file plus the local-agent-os overlay, with the three env vars unset, renders 2,147,483,648 / 1,610,612,736 / 805,306,368 bytes for mc / kb / fleet. The same render of `origin/dev` gives 1.0 / 1.0 / 0.5 GiB |
| AC-2 | CI: `DeclaredHeapCeilings.spec.mjs`, newly on the brain-unit list. At `95ec036` the hosted smoke ran 1,213 tests against `dev`'s 1,187; the 26 extra are the spec's. Outside CI: at `origin/dev`'s caps the three new tests fail: kb 768 + 316 > 1024, mc 768 + 473 > 1024, fleet 384 + 216 > 512 |
| AC-3 | Outside CI: `node --max-old-space-size=<flag>` in throwaway containers of the kb-server image, under each new cap, gives 864 / 864 / 432 MiB for mc / kb / fleet, as in the #469 Fix table |

## Deltas from ticket
None against the current body.
- **Scope grew after measuring.** The first version named only mc-server and kb-server and changed no guard. Measuring fleet-server and finding the spec's weak bound added both.
- **@neo-gpt's pre-review** changed three things:
  - The deployed AC was dropped, because its drain and ban-lift belong to #464's residual. That residual is met; #469 says how.
  - A Contract Ledger was added.
  - `NON_HEAP_MB` is now labelled as samples, not bounds.

## Test Evidence
Measured 2026-09-24 on `neo-local-canonical`:

| input | method | values |
|---|---|---|
| V8 heap limit | `node --max-old-space-size=<flag>` in a throwaway container of the kb-server image, under each cap | 768 → 816 MiB at 1g, 864 at 1536m and 2g · 384 → 387 MiB at 512m, 432 at 768m |
| probe | `process.resourceUsage().maxRSS` of each service's own healthcheck, 3 runs | mc 120.8 MiB · kb 120.2 MiB · fleet 72.5 MiB |
| native | RSS − heap total, from the heap observations | mc 256 MB · kb 99 MB · fleet has no observation, so its whole RSS (95 MiB) stands in |

The spec's `v8` entries are the allowances at the new caps: 96, 96 and 48.

## Post-Merge Validation
None. After merge the plane's recreate is routine operation. The plane's env file pins mc-server, so that recreate witnesses the pin, not the default.

## Commits
- `95ec036`: the three cap defaults, the spec's measured bound, and the brain-unit list entry.
- `8eb6647`: the kb-server heap note no longer sizes the ceiling below a `1g` limit, and `NON_HEAP_MB` is labelled as samples, not bounds.

Authored by Vega (Opus 5.5, Claude Code) 🌿 Session 9f7b8241-8b3c-4954-a9e5-2f9c1e41d669.
