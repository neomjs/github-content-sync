---
number: 524
title: >-
  ci: the Brain calls the shared PR baseline at its published release v0.1.19
  (#523)
author: neo-opus-grace
state: OPEN
createdAt: '2026-09-25T22:03:26Z'
updatedAt: '2026-09-25T22:15:00Z'
closedAt: null
mergedAt: null
head: grace/523-shared-pr-baseline
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/524'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #523

The Brain calls the shared PR baseline. `.github/workflows/shared-pr-baseline.yml` is the same caller Institution carries: read-only permissions, the live-read activity types, and `neomjs/neo-agent-skills/.github/workflows/reusable-pr-baseline.yml@v0.1.19`, the release tag `neo` pins. The team roster is read at the Brain's `dev`.

Two changes let it pass on its first run:
- **Secrets:** the job scans the whole tree, not the diff. On this repo's tree, the pinned checker (`check-secrets.mjs@v0.1.19`) found exactly two credential-shaped literals. Both are the fabricated `ghp_AAAABBBB…` token in the WriteAhead spec's masking arm, which now carries the checker's `secret-scan-ok:` marker.
- **Archaeology:** the job audits whole changed files, so touching that spec inherited three "@neo-gpt's RA-3" citations in its comments. The comments keep their reasons and drop the review provenance.

`agent-preflight.mjs` still names an `agent-pr-body-lint.yml` the Brain never had, and so do the stacked-PR guard's two `@see` lines. Out of scope, noted in a defect-note: touching that file inherits eleven refs in its own prose.

Evidence: L2 (the pinned checkers run against this tree, with seeded controls) → L2 required, plus this PR's own CI for AC-2 to AC-4. Residual: none.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | The caller pins `@v0.1.19`, a published release tag (neo pins the same). `#38`'s question was settled by `devindex#39`: a release, never a commit SHA. |
| AC-2 | This PR's checks list `Shared PR Baseline / Source comment archaeology`. Its first run is green on ed582d9: [job 108268073305](https://github.com/neomjs/neo-agent-brain/actions/runs/36194797037/job/108268073305). |
| AC-3 | c535413 added "Tracked in #523." to a durable comment, and the job went **red**: [job 108268413652](https://github.com/neomjs/neo-agent-brain/actions/runs/36194906051/job/108268413652). Its revert 5f12a23 is green: [job 108268681831](https://github.com/neomjs/neo-agent-brain/actions/runs/36194991416/job/108268681831). |
| AC-4 | All nine jobs are adopted by the one caller, with nothing deferred. Their dispositions on this tree: `pr-base` (targets `dev`), `skills-materialized` (the same `npm ci` + `--check` as `substrate-sync.yml`), `source-comment-archaeology`, `substrate-size` (N/A: no substrate entry points), `npm-overrides` (one override, still needed), `secrets` (clean after the marker), `commit-authorship` (roster at `dev`; a seeded `noreply` trailer was rejected locally), `pr-body`, `release-ref`. All nine are green on ed582d9's first run and again on 5f12a23. |
| AC-5 | The `(#68)` ref in `ai/configBase.mjs` is already gone on `dev` (`git grep "(#68)" origin/dev -- ai/configBase.mjs` is empty). |

## Deltas from ticket
- **The baseline grew from five jobs to nine** since the ticket was filed (09-03), and AC-4 covers all nine.
- **The ticket was transferred** from `neo-agent-skills#40`, because consumer leaves live in their consumer repo and the PR-body lint needs a same-repo close target. Its parent is still `neo-agent-skills#14`.
- **The first head also touched `agent-preflight.mjs`.** That was restored in 19014f4, so its inherited prose stays out of this change.
- **c535413 and 5f12a23 are AC-3's red arm and its revert.** They cancel out in the squash.

## Test Evidence
- **Pinned `check-secrets.mjs@v0.1.19 --all`:** 2 findings before the marker, 0 after (1,869 files).
- **Pinned `check-commit-authorship.mjs@v0.1.19`:** silent on this branch. On a seeded commit with a `noreply` trailer it printed the finding, so its silence here is a pass, not a mute.
- **Brain agent-preflight:** all gates, including archaeology on the changed files.

## Post-Merge Validation
- None.

Authored by Grace (Claude Opus 5.5, Claude Code). Session d2d30528-b6fe-423b-86ce-ab945396a201.
