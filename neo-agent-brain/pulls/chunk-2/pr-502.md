---
number: 502
title: >-
  fix(deploy): the REM run-state channel is shared between the orchestrator and
  mc-server (#500)
author: neo-opus-vega
state: OPEN
createdAt: '2026-09-25T16:48:09Z'
updatedAt: '2026-09-25T16:48:28Z'
closedAt: null
mergedAt: null
head: agent/500-rem-runs-channel
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/502'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #500

🌿 A REM cycle that ran and one that never ran read alike from the Memory Core, and now they cannot.

The compose file gives the REM run-state directory its own shared channel, `shared-rem-runs-data`, writable on the orchestrator and read-only on mc-server, the way the `handoff` channel already pairs the two. `configBase.mjs` declares `remRunStateDir` as a plane member that the dream child writes and both processes read. #425's per-service roots left it in the orchestrator's private root, so the Memory Core server read an empty directory of its own and `get_rem_pipeline_state` reported `recentCycles: []` on every compose plane while REM ran. On the local plane today: the 16:10Z cycle's receipt on disk beside 199 older ones, the tool empty.

The #425 guard gains a fourth arm pinning the channel: the orchestrator mounts it writable, mc-server read-only, no other service mounts it.

Evidence: L1 (a static guard over the committed compose file, red-first) → L1 required (AC-1, AC-2). Residual: the plane receipt after the recreate, under Post-Merge Validation.

agent-preflight: hosted, ran clean.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | `PlaneDataRootMount.spec.mjs` › "the orchestrator writes the REM run-state channel, mc-server reads it, and no other service mounts it": red against the untouched compose file (`Error: the orchestrator writes the channel`), red with only the orchestrator's mount (`Error: mc-server reads the channel`), green with both. |
| AC-2 | The three existing arms pass unchanged; the spec runs 6/6 at `84523a2`. |
| AC-3 | Post-merge on the local plane, under Post-Merge Validation. |

## Deltas from ticket
- None.

## Test Evidence
- `test/playwright/unit/ai/deploy/PlaneDataRootMount.spec.mjs` at `84523a2`, `NEO_TEST_SKIP_CI=true`: 6 passed, after the two red runs above.
- `docker compose … config` with the local overlay and the cut fragment merges cleanly: the channel appears on both services and `shared-rem-runs-data` in the volume set.

## Post-Merge Validation
- [ ] Local plane: the next recreate (Brain #492 + #498 + this) seeds `shared-rem-runs-data` from the orchestrator root's `rem-runs` with #425's F1 procedure; afterwards `get_rem_pipeline_state.recentCycles`, read from mc-server, lists the seeded runs and the first dream cycle after the recreate. Receipt on #84.

Residual-Owner: #84

Authored by Vega (Fable 5.1, Claude Code) 🌿. Session d19add67-d33c-489d-99aa-27ad2782ed5e.
