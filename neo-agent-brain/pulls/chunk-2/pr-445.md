---
number: 445
title: >-
  perf(memory-core): externally active sessions match memories against
  subscribers resolved once (#444)
author: neo-opus-vega
state: OPEN
createdAt: '2026-09-23T15:14:46Z'
updatedAt: '2026-09-23T15:22:16Z'
closedAt: null
mergedAt: null
head: vega/444-active-sessions-scan
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/445'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #444
🌿 *A drift sweep can no longer spend half an hour asking which twelve identities are subscribed.*

`getExternallyActiveSessionIds` now materializes the subscribed identities once, in a CTE with the same `WAKE_SUBSCRIPTION`, active-status and `harnessTarget` predicates, and matches memory rows against it with an uncorrelated `IN`. Before, a correlated `EXISTS` re-scanned all of `Nodes` for every memory row, in one synchronous statement under the heavy-maintenance lease. On the local plane's graph that was minutes of CPU per drift sweep with no log line. The returned set is unchanged.

Evidence: L3 (the statement planned and timed read-only against the local plane's `memory-core-graph.sqlite`; unit arms locally) → L4 required (AC-3: a deployed summary run). Residual: AC-3, Residual-Owner: #64.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | Outside-CI (local run; `QueryReRanker.spec.mjs` is CI-skipped, bucket C, and off Brain CI's list, #201): the four `#9959` externally-active arms pass unchanged — peer sessions excluded, parallel sessions of one identity protected, the named-session bypass, stale peers eligible. With the ChurnCooldown, SummarizePagination and wake-status-policy specs: 28 passed. |
| AC-2 | Outside-CI, measured on the plane's graph (230,740 nodes, 33,425 memory rows, 17 `WAKE_SUBSCRIPTION` nodes): `EXPLAIN QUERY PLAN` was `SCAN memory \| SCAN subscription EXISTS` and is now `SCAN memory \| LIST SUBQUERY \| MATERIALIZE subscribed \| SCAN subscription \| CREATE BLOOM FILTER`. The old shape took 2,793 ms for a 50-row sample, a lower bound of about 31 minutes for all rows; the new statement returns 30,776 rows in 891 ms. |
| AC-3 | `[L4-deferred — operator handoff needed]` — deployed plane, see Post-Merge Validation. |

## Deltas from ticket
None substantive. The ticket sketched two statements (identities, then `IN (…)` with parameters); one statement with a materialized CTE gets the same plan with no host-parameter limit and keeps the method's one-query shape.

## Test Evidence
- `env -u NEO_MCP_REMOTE_TOKEN -u NEO_TEST_SKIP_CI npx playwright test -c test/playwright/playwright.config.unit.mjs test/playwright/unit/ai/services/memory-core/{QueryReRanker,SessionService.ChurnCooldown,SessionService.SummarizePagination,wakeSubscriptionStatusPolicy}.spec.mjs --workers=1` → 28 passed. The four externally-active arms were run again by name (`--grep "active|subscription|wake"`) to confirm they executed rather than skipped.
- Plan and timings: read-only `better-sqlite3` on `/app/.neo-ai-data/sqlite/memory-core-graph.sqlite` inside the orchestrator (images `b99ea11`; `SessionService.mjs` and `wakeSubscriptionStatusPolicy.mjs` identical to `dev`), while a live summary run of the old shape sat at 85 % CPU, 45 minutes in, before `Found N sessions`.

## Post-Merge Validation
- [ ] AC-3: after deploy, a summary run's `Starting drift-detection` → `Found N sessions` gap (orchestrator log, then the `mc-server` file sink) is seconds, not tens of minutes.

Residual-Owner: #64

Related: #64 · #438 / PR #439 (the same hold's timeout half) · neomjs/neo#13637

Authored by Vega (Claude Opus 5.5, Claude Code) 🌿 Session 603e5af2-9d35-4bfc-9852-038c4cf38568.
