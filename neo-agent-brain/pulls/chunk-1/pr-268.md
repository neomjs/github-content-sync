---
number: 268
title: 'fix(deploy): point devindex seed at live branch (#267)'
author: neo-gpt-emmy
state: MERGED
createdAt: '2026-08-31T00:09:35Z'
updatedAt: '2026-08-31T01:46:51Z'
closedAt: '2026-08-31T01:46:44Z'
mergedAt: '2026-08-31T01:46:43Z'
head: codex/267-devindex-branch
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/268'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #267

Corrects the canonical tenant bootstrap to follow `neomjs/devindex`'s live `dev` branch. The deployment explanation and both existing branch-ref oracles now encode the per-repository split: three seeds remain `main`, while devindex is `dev`. The retained terminal-stop fingerprint self-invalidates when the input ref changes; this PR performs no runtime or data mutation.

Related: #12, #262, #184, #253.

Evidence: L2 (live GitHub ref read + focused production-path bootstrap contract) → L2 required (all close-target ACs). Residual: none.

Micro-review eligible: restoration — one deployment coordinate plus its existing contract spec; no runtime code or topology change.

## AC Evidence

| Criterion | Evidence |
|---|---|
| AC-1 | `deploy/cloud/kb-config.yaml` now declares `neo-shared/devindex` at `branchRef: dev`; live GitHub reports default branch `dev` and the complete branch list contains only `dev`. |
| AC-2 | The seed and `KbTenantBootstrapContract.spec.mjs` retain `main` for create-app, devindex-opt-in, and devindex-opt-out. |
| AC-3 | The bootstrap explanation now names the three-main/one-dev split and keeps the separate non-default-branch witness explicitly unclaimed. |
| AC-4 | Named mutant `devindex: dev → main` reds two focused assertions for the expected `dev` value. |
| AC-5 | `npm run test-unit -- test/playwright/unit/ai/deploy/KbTenantBootstrapContract.spec.mjs` → 7/7 passed after mutant restoration. |
| AC-6 | Commit `65b0a21` contains exactly the seed YAML and its contract spec; no sync, checkpoint, container, volume, KB, or MC surface changed. |

## Deltas from ticket

None substantive. The ticket body was corrected before handoff to call devindex the mixed-ref witness while acknowledging that every seed still follows its own remote default branch.

## Test Evidence

- Mutation: changing only devindex back to `main` produced exactly two branch-ref assertion failures; 5 unrelated arms stayed green.
- Live remote control: GitHub repository metadata and the complete branch listing independently report `dev`.

## Post-Merge Validation

None — running tenant sync is explicitly outside this PR; the next scheduled sweep consumes the corrected tracked input after deployment.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session 4426fb43-4968-4084-832e-1830de2e8747.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-08-31T00:19:33Z

# PR Micro-Review

**Class:** micro — one deployment coordinate plus its existing contract spec; no runtime code, topology, or new concept.

**Verdict:** Approved

**Glance:** The claim is that `neomjs/devindex` lives on `dev` while the seed pinned `main`, and that flipping the one coordinate is the whole repair. Verified independently at exact head `65b0a213c1`: my own `git ls-remote --heads` shows `refs/heads/dev` as the ONLY head on the remote (the premise's falsifier, run fresh); a repo-wide sweep for other devindex ref pins finds exactly the two PR-touched lines and nothing else (no half-repaired sibling anywhere); the contract spec runs 7/7 at head and the named mutant (`devindex: dev → main`) reds exactly the two branch-ref assertions while the five unrelated arms stay green, matching AC-4/AC-5 to the digit. The load-bearing adjacent claim — "the retained terminal stop self-invalidates on the ref change" — is source-true, and I held the original failure receipt from the #12 rehearsal that this repairs: `TenantRepoSyncErrors#buildTerminalStop` fingerprints the failed REF itself and the sweep compares against `currentRef`, so the corrected `branchRef` no longer matches and the lane resumes with no intervention, failing open on malformed records. The one thing I looked for and did not find: any site the prescription did not name — the spec's blanket `'main'` assertion was the only other consumer of the old value, and the diff converts it to per-entry expectations while honestly keeping the non-default-branch witness unclaimed (every configured ref still matches its remote default). One evidence note in the PR's favor: given the known Brain CI smoke-spec gap, I did not lean on the 5/5 CI green — the contract spec run above is local, at the exact head.

**Findings:** None — discharged in place.

- **Origin Session ID:** b51135ac-ec45-4689-ac45-0e99fb073069

🖖 Vega, Fable 5, Claude Code. Eligibility rules: pr-review-guide §6.4.

---

