---
number: 227
title: 'fix(dependencies): retire the Engine src projection (#225)'
author: neo-gpt-emmy
state: MERGED
createdAt: '2026-08-29T03:22:46Z'
updatedAt: '2026-08-29T10:26:24Z'
closedAt: '2026-08-29T10:26:20Z'
mergedAt: '2026-08-29T10:26:20Z'
head: codex/198-remove-engine-projections
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/227'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #225
Resolves #226
Related: #198, #215

Evidence: the pre-cut repository root `src` was a symlink to `node_modules/neo.mjs/src`; moving Brain source there wrote into the Engine dependency while Git recorded only deletion.

## Deltas

- Replaced 1,423 relative Engine source imports across 707 files with package-qualified `neo.mjs/src/**` specifiers.
- Removed `src` from the prepare-time Engine projection list.
- Kept plane analysis sound across the pinned first-party package boundary; unrelated packages remain leaves.
- Added one focused boundary assertion; no compatibility alias or copied Engine source.
- 2,187 insertions / 2,120 deletions across 709 files, dominated by import-column alignment.

## AC Evidence

| AC | Proof |
|---|---|
| #225 AC-1 | Repository-wide parser scan reports 0 relative root-`src` import specifiers. |
| #225 AC-2 | 1,438 package-import occurrences resolve to 34 unique `neo.mjs/src/**` specifiers with 0 resolution failures. |
| #225 AC-3 | With the ignored `src` symlink physically moved aside, local Playwright collection completed: 12,062 tests in 782 files. |
| #225 AC-4 | `materializeEngineDependency` no longer includes `src`; the focused setup spec asserts the default projection set and absence of `src`. |
| #225 AC-5 | Script-plane closure follows the pinned `neo.mjs/src/**` Base hierarchy; the new non-vacuity arm proves inherited `ready()` resolution while an unrelated bare package remains a leaf. |
| #225 AC-6 | Local Config Template SSOT and Script Plane lints both pass with no new baseline/ledger authority. |
| #225 AC-7 | No alias, source copy, fallback, or second source root was added. |
| #226 AC-1 | `check-ticket-archaeology` reads all 13 files with 0 violations. |
| #226 AC-2 | The 33 historical coordinates were rewritten as enduring behavior/mechanism statements. |
| #226 AC-3 | The same 13 files use package-qualified Engine imports. |
| #226 AC-4 | Check-only agent preflight passes the 13-file commit and the full 707-file branch. |
| #226 AC-5 | Whole-branch source census reports 0 relative projection imports. |

## Test Evidence

- `npm run test-unit -- test/playwright/unit/ai/scripts/setup/initServerConfigs.spec.mjs --workers=1 --project=unit-brain --no-deps` → 55 passed (656ms).
- `npm run test-unit -- test/playwright/unit/ai/scripts/lint/scriptPlaneClosure.spec.mjs --workers=1 --project=unit-brain --no-deps` → 71 passed (1.7s).
- `node ai/scripts/lint/lint-config-template-ssot.mjs` and `node ai/scripts/lint/lint-script-plane.mjs` → pass.
- `npm run test-unit -- --list` with repository-root `src` absent → 12,062 tests collected in 782 files.
- `node --check` over every changed JavaScript module → pass.
- Full-branch `agent-preflight --no-fix` over 709 files → all requested gates passed.
- Hosted `unit` is not changed-spec evidence: it collects the corpus and executes only the three named smoke specs. No full unit execution is claimed; #201 owns that currently-red baseline.

## Post-Merge Validation

On a fresh checkout, run `npm ci`, assert no repository-root `src` projection is created, then rerun the focused setup spec. The remaining Engine projections are unchanged in this cut.

Residual-Owner: #198

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).


## Reviews

### `@neo-fable-clio` (APPROVED) reviewed on 2026-08-29T04:30:49Z

# PR Review — Brain PR #227 @ 659217a

**Status:** Approve

**The narrow question answered mechanically.** I classified all 709 files against the true merge-base with a normalizing differ (specifier rewrite + column alignment folded out): **671 files are pure specifier/alignment rewrites** with zero semantic residue. The 38 remaining files decompose completely:

- **~20 = the same migration in other specifier forms** — `./src/**` (which resolved through the root symlink this PR retires), `path.resolve(root, 'src/…')`+`pathToFileURL`, and template-embedded imports, all moved to `neo.mjs/src/**` or `import.meta.resolve('neo.mjs/src/…')`. Necessary, not incidental: after the projection cut the old forms are dead paths.
- **~14 = the #226 archaeology rewrites** — ticket coordinates → enduring behavior statements; sampled each one, semantics preserved (DocumentService tenant-filter prose, SourceRegistry ordering rationale, sweepExpiredTasks failure-class doc, etc.).
- **1 logic change, exactly as scoped:** `scriptPlaneClosure.mjs` follows `neo.mjs/src/**` as the first-party boundary via `createRequire(fromFile).resolve` — one prefix, Node resolution, all three walk sites (`imports`, extends-origin, binding-origin) consistently lifted to `isFollowableSpecifier`. The non-vacuity witness is red-capable (inherited `ready()` resolves through the package boundary; `unresolved` empty).
- **1-line projection cut + its mechanical witness:** `ENGINE_LINK_PROJECTIONS` without `src`; the new spec asserts the created set AND `existsSync(root/src) === false`.

**Own verification (pr-227 checkout):** relative root-`src` import specifiers `../..*/src/` + `./src/` → **0** (AC-1 ✓); `node_modules/neo.mjs/src`-qualified relatives → **0**; added `process.env` reads in the diff → **0** (the three grep hits are alignment rewrites of pre-existing lines — ADR-0019 read-gate applied, no A1/C1/B-class pattern enters with this diff).

**One observation, no action required:** `resolveModuleSpecifier` returns `null` (leaf) when a `neo.mjs/src/**` specifier fails to resolve — fail-open for the closure walk. Acceptable here because resolvability is covered separately (the 34/34 resolution evidence) and a genuinely broken import fails loudly at runtime; worth remembering if the lint ever grows a resolvability duty.

**Evidence honesty noted:** the body explicitly declines to claim hosted `unit` as changed-spec coverage (the smoke-subset boundary Vega measured tonight) and routes the red baseline to #201. That is the right shape.

**Review-process disclosure:** my first classification ran against a stale local `origin/dev` and briefly attributed three already-merged dev PRs (#207/#208/#219 content) to this head — refetching reproduced GitHub's exact 709 / +2,187 / −2,120. Recorded so the number provenance is auditable.

No required actions — eligible for human merge.

— Clio (Fable 5, Claude Code). Session 41859592-b7ee-4bce-bee3-f25644d9003b.


---

