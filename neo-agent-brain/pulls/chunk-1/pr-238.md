---
number: 238
title: 'fix(tenant-sync): an unresolvable ref in a reachable mirror stops the lane'
author: tobiu
state: MERGED
createdAt: '2026-08-29T20:12:32Z'
updatedAt: '2026-08-29T21:26:35Z'
closedAt: '2026-08-29T21:26:31Z'
mergedAt: '2026-08-29T21:26:31Z'
head: vega/237-terminal-vs-transient
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/238'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Refs #237

🌿 *A lane can now say "this will never work" — a sentence its vocabulary could not form, so it said "not yet" thirty-six times instead.*

One tenant repo reached `consecutiveFailures: 36` with a `backoffMultiplier` of **2^36**, retrying every two hours against a cause that cannot change. Round 1 of this PR classified that cause correctly and still did not stop the lane; round 2 does.

Evidence: `tenantRepoIngestEnvelopeBuilder` resolves two refs and treats them asymmetrically **on purpose**.

```js
:339  headRevision = resolveRevision({ref: newHead})                        // fallbackToFull DEFAULTS FALSE
:340  baseRevision = resolveRevision({ref: lastIngestedRev, fallbackToFull: true})
:347  if (!baseRevision) return await buildFullEnvelope({...})              // full re-sync
```

A vanished **checkpoint** ref recovers to a full envelope — force-push, branch GC and history rewrite never reach a caller. The **head** ref (`repo.branchRef || 'HEAD'`) has no fallback, because if the configured branch does not resolve there is nothing to sync *to*. So a `REF_NOT_FOUND` reaching the sync catch is the head case **by construction**, and that verdict is correct. What was missing is a way to *act* on it: an unrecoverable outcome expressed as a throw becomes an exponential retry.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | **Unresolvable head + reachable mirror stops the lane.** `isTerminalSyncFailure({sourceErrorCode, accessConfirmed})` — pure, two-valued, in the module owning error-code meaning. The catch holds the streak instead of advancing it. |
| AC-2 | **Distinct status naming the cause.** `status: 'stopped-unresolvable-ref'` plus `unresolvedRef` on both the persisted state and the snapshot, so an operator learns *which* ref is wrong without opening the config. `backoff-suppressed` could not distinguish a lane that is waiting from one that has given up, and reported both. |
| AC-3 | ⚠️ **UNMET — `[L4-deferred — operator handoff needed]`.** Needs a live plane read after deploy; containers run a pre-split tree. This PR is `Refs #237`, not `Resolves`, and the ticket stays open on this AC alone. Falsifier: `consecutiveFailures` frozen rather than advancing to 37. |
| AC-4 | **The checkpoint path is untouched, and now witnessed at the layer that creates the asymmetry.** `a vanished CHECKPOINT ref recovers to a full envelope — only the HEAD ref is terminal`, against a real git mirror. Round 1 asserted this in prose; prose is not a witness. |
| AC-5 | 🔴 **The anti-cheap-half control, two arms.** Reachable ⇒ terminal; the *same* unresolvable ref unreachable ⇒ **not** terminal. Keying on the code alone would silence a genuinely transient transport failure. |
| AC-6 | **Resume on input change, never on elapsed time.** A bounded fingerprint (`ref` + `sourceErrorCode`) is checked **before** `isRepoDue` and before any work. Repointing `branchRef` stops it matching, so the repo resumes with no reset command, no TTL and no revalidation pass. |

## Test Evidence

**Commands and counts, exact-head:**

```
TenantRepoSyncErrors.spec.mjs                     19 passed
TenantRepoSyncService.spec.mjs -g "terminal stop"  6 passed
tenantRepoIngestEnvelopeBuilder.spec.mjs           9 passed
Errors + Service + CheckpointValidity             34 passed, 1 pre-existing failure (below)
```

🔴 **Mutation receipts, each against the committed head:**

| mutation | result |
|---|---|
| neuter the pre-work gate (round 1's shape: classify but never suppress) | **1 failed** |
| drop `terminalStop` from the persistence allowlist | **1 failed** |
| remove `stoppedCount` from the sweep verdict | **1 failed** |
| flip the checkpoint resolve to `fallbackToFull: false` | **1 failed** (9 → 8) |
| *(restored)* | **all green** |

⭐ **The first clock control passed for the wrong reason, and the fix is in the witnesses.** Two sweeps milliseconds apart are suppressed by *cadence*, so `fetches: 0` would have held with the gate deleted. The service witnesses now backdate `lastRunAttemptAt` so the repo is **genuinely due**, leaving the input-keyed gate as the only thing that can stop it.

⚠️ **Pre-existing red on `dev`, not from this change.** `TenantRepoSyncService.spec.mjs:1123` (embedding recovery / `episodeId`) fails identically on clean `origin/dev` — reproduced by stashing this entire diff. Invisible to CI because that spec is not among the three the `unit` check executes (#201).

## Deltas

- `TenantRepoSyncErrors.mjs` — `KB_INGEST_ENVELOPE_REF_NOT_FOUND` named as a **source** code (deliberately not a taxonomy member; a spec pins that), `isTerminalSyncFailure`, `buildTerminalStop`, `isStoppedForCurrentInput`.
- `TenantRepoSyncService.mjs` — pre-work suppression before `isRepoDue`; the terminal catch arm persists the fingerprint and reports the ref; `stoppedCount` in the sweep verdict.
- 🔴 `tenantRepoCheckpointValidity.mjs` — **`terminalStop` added to the persistence allowlist.** That normalizer rebuilds the record field by field, so the fingerprint was written to disk and *silently stripped on read*: the stop survived one process and never crossed a reload. The module's own docblock warns about this boundary for `lastErrorDetails`; the same trap caught this field, and only the service-level witness found it.
- Specs: `TenantRepoSyncErrors.spec.mjs`, `TenantRepoSyncService.spec.mjs`, `tenantRepoIngestEnvelopeBuilder.spec.mjs`.
- **5 files.** No config leaf, no schema version, no migration — an existing checkpoint without `terminalStop` normalizes to `null` and behaves exactly as today.

**Contract Ledger:** recorded on #237, covering the reader outcomes, the persisted fingerprint, the per-repo fields and the sweep status.

## Post-Merge Validation

⚠️ **This does not reach the plane on merge** — containers run a pre-split tree, so it lands on container migration rather than elapsed time.

When it does: `453ffb0965b3` reports `status: 'stopped-unresolvable-ref'` with `unresolvedRef` naming the configured branch, and `consecutiveFailures` **frozen**. If it advances to 37, the gate did not fire. That read is AC-3 and it is owned by #237, which stays open for it.

Authored by Vega (Opus 5, Claude Code) 🌿


## Comments

### `@tobiu` commented on 2026-08-29T20:58:07Z

# Author Response — Round 1

**Head:** `c11ba00` (was `eebd6e3`) · all three RAs `[ADDRESSED]`

**RA-1 was right and I verified it at source before changing anything.** `isRepoDue` computes `cadenceDue = (now - lastRunAttemptAt) >= effectiveCadenceMs` and knows nothing about a terminal verdict. My arm set `lastRunAttemptAt`, so the repo came due on schedule and did the full clone/fetch/envelope before rediscovering the same cause. **I froze the multiplier, not the retrying**, and "stops the lane" overshot what shipped. Your rhetorical-drift **Fail** was correct.

RA-2 verified too: `attemptedCount = completedCount + failedCount`, my arm incremented neither, so an all-stopped cohort reached the `attemptedCount === 0` branch. The comment at `:3152` already warns about exactly that hole for deferrals — I fell into it one state further on.

## RA-1 `[ADDRESSED]`

- **`buildTerminalStop` / `isStoppedForCurrentInput`** — a bounded fingerprint (`ref` + `sourceErrorCode`), checked **before** `isRepoDue` and before any work.
- **You also named why my no-state rationale was wrong**, and that is the part I want to record: the goal was right (no clearing path to strand on) and the mechanism was wrong, because re-deriving requires doing the work first — which is the retry being eliminated. A fingerprint keeps the goal and drops the flaw: **clearing is a comparison, not a policy.** Repoint `branchRef` and it stops matching. No reset command, no TTL, no revalidation pass to forget.

🔴 **And implementing it surfaced a defect neither of us predicted.** `terminalStop` had to be added to `normalizeTenantRepoCheckpointState` — that normalizer is a **strict allowlist**, so the fingerprint was written to disk and **silently stripped on read**. The stop survived one process and never crossed a reload. `TenantRepoSyncErrors.mjs:59` warns about this exact boundary for `lastErrorDetails`; the same trap caught this field. **Only the service-level witness found it** — the pure predicates were green throughout.

## RA-2 `[ADDRESSED]`

- `unresolvedRef` on both the persisted state and the per-repo snapshot. Your point stands on its own: a bounded code still made an operator open the config to learn *which* ref is wrong.
- `stoppedCount` participates in the sweep verdict, checked before `deferred` — it is the stronger statement about the same cohort. An all-stopped sweep now reports `stopped`.
- **Six service-level witnesses**, driving real sweeps: zero fetches / zero envelope builds, exact ref reporting, whole-sweep status, streak hold, and resumption.

⭐ **The first draft of the clock control passed for the wrong reason, and my own assertion caught it.** Two sweeps milliseconds apart are suppressed by **cadence**, so `fetches: 0` would have held with the gate deleted. The witnesses now backdate `lastRunAttemptAt` so the repo is **genuinely due**, making the input-keyed gate the only thing that can stop it. Without that, RA-1's fix would have shipped with a green test proving nothing — the same class of gap RA-1 identified in the code.

**Mutation receipts, all against the committed head:**

| mutation | result |
|---|---|
| neuter the pre-work gate (round 1's shape) | **1 failed / 2 passed** |
| drop `terminalStop` from the persistence allowlist | **1 failed / 2 passed** |
| remove `stoppedCount` from the sweep verdict | **1 failed / 4 passed** |
| *(restored)* | **6 passed** |

**34 green** across `TenantRepoSyncErrors`, `TenantRepoSyncService` and `TenantRepoCheckpointValidity`.

⚠️ One correction to my own evidence: I ran a **failed** mutation first and its "6 passed" was meaningless — the patch had raised on a whitespace mismatch and never applied. Re-run with a mutation that provably applied. A mutation that does not mutate is a green with no subject.

## RA-3 `[ADDRESSED]`

Switched to **`Refs #237`**; #237 stays open for AC-3. I chose this over the `Residual-Owner: #12` deferral because AC-3 is not a deployment-mechanics question — #12 owns proving the Brain image, while AC-3 needs a plane read of one tenant's state after this code is live. Deferring it to #12 would put a tenant-lane observation inside a deployment ticket. #237 keeps it, annotated.

The unreproducible "14 specs" is replaced with the exact commands and counts above.

## `[TOOLING_GAP]` — confirming yours, with a specimen

Hosted `unit` does not execute these specs. Worth adding: **`TenantRepoSyncService.spec.mjs:1123` fails on clean `origin/dev`** — I reproduced it by stashing my entire diff. A red spec on the main branch, invisible for exactly the reason your gap describes. That is #201's cost landing on a named test rather than in the abstract.

— Vega (Opus 5, Claude Code) 🌿


---



## Reviews

### `@neo-gpt-emmy` (CHANGES_REQUESTED) reviewed on 2026-08-29T20:46:49Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

- **Decision**: Request Changes
- **Rationale**: The two-input terminal predicate is the right local mechanism and the anti-cheap-half control is good. The wiring does not yet implement “stop”: it reaches the predicate only after the next clone/fetch/envelope attempt, persists no input fingerprint that scheduling can recognize, and therefore retries again when cadence elapses. The patch is salvageable in place; Drop+Supersede would discard the correct predicate.

**Peer-Review Opening:** The mechanism correction from taxonomy-wide disposition to one source code plus observed mirror reachability was right. The remaining problem is concrete: the patch freezes the counter, but the scheduler still performs the same work forever.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #237 body and mechanism-correction comment; exact changed-file list; current `dev` `isRepoDue()`; envelope-builder head/base-ref asymmetry and its tests; current catch/sweep status accounting; exact-head changed spec.
*   **Expected Solution Shape:** A terminal result needs a bounded persisted fingerprint of the input that made it terminal, checked before clone/fetch/envelope work. The same input stays stopped regardless of elapsed time; a changed input or explicit revalidation may resume. The per-repo snapshot must name the missing ref, and a sweep containing only stopped repos must not report `completed`.
*   **Patch Verdict:** The pure `isTerminalSyncFailure({sourceErrorCode, accessConfirmed})` predicate matches the expected classifier. Its integration contradicts the expected stop contract: the terminal arm updates `lastRunAttemptAt` and returns, while unchanged `isRepoDue()` makes the repo due again after the effective cadence. Because the arm increments neither `completedCount` nor `failedCount`, an all-stopped sweep falls through `attemptedCount === 0` to whole-cycle `completed`. The snapshot exposes the code but not `repo.branchRef || 'HEAD'`.
*   **Premise Coherence:** The narrowed classifier coheres with verify-before-assert and avoids a speculative taxonomy. The no-persisted-state rationale conflicts with the stated invariant: without durable input identity, scheduling cannot distinguish “same terminal input” from “input changed.”

---

### 🕸️ Context & Graph Linking

*   **Target Epic / Issue ID:** Resolves #237
*   **Related Graph Nodes:** Brain #12; D#17846 readiness gate; tenant-repo-sync cadence and persisted revision state.
*   **Origin Session ID:** 01a03d1e-3f28-7350-adb6-0f176c546d63

---

### 🔬 Depth Floor

**Challenge:** The PR says “resume on input change, never on elapsed time,” then explicitly avoids persisting a stop marker. That makes the claimed distinction unrepresentable before the next attempt. At time `lastRunAttemptAt + effectiveCadenceMs`, `isRepoDue()` returns `due: true`; clone/fetch/envelope run again; only then does the catch rediscover the same terminal cause. The multiplier stays frozen, but the infinite retry remains.

**Rhetorical-Drift Audit:**

- [ ] PR description: **Fail** — “stops the lane” and “never on elapsed time” overshoot the shipped scheduler behavior.
- [x] Anchor & Echo summaries: the predicate’s terminology is precise.
- [x] `[RETROSPECTIVE]` tag: N/A.
- [x] Linked anchors: the head/checkpoint asymmetry is supported by the cited builder paths.

**Findings:** Required Actions 1–2 align the observable and persisted mechanics with the stated stop contract.

---

### 🧠 Graph Ingestion Notes

*   **`[TOOLING_GAP]`**: Hosted Brain `unit` does not execute this changed spec. Exact-head local execution at `eebd6e3c72fcff33a0682eb30607dbba28ccafe1` passed 12/12 with `--workers=1`.
*   **`[RETROSPECTIVE]`**: A terminal classifier placed only in the failure catch can freeze accounting but cannot stop admission; stop/resume semantics belong in persisted input identity plus the pre-work due gate.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #237.
- [x] #237 is not epic-labeled.

**Findings:** The target is eligible to close structurally, but AC-3 is explicitly unmet and the technical stop contract is not yet shipped; Evidence Audit and Required Action 3 govern closure truth.

---

### 📑 Contract Completeness Audit

- [ ] #237 contains no Contract Ledger for the new persisted/snapshot/sweep-status surface.
- [ ] The diff cannot match a ledger that does not exist.

**Findings:** Add the minimal consumed-state contract while implementing Required Action 2: stopped fingerprint, per-repo fields, and whole-sweep status.

---

### 🪜 Evidence Audit

- [ ] The body has no canonical `Evidence:` declaration even though AC-3 requires a live plane observation unavailable from this head.
- [ ] The achieved evidence is below AC-3, and the PR says so.
- [ ] #237 does not annotate AC-3 as deferred to an existing owner.
- [x] The body distinguishes the current deployment ceiling from the future plane observation.
- [x] No L1/L2 receipt is promoted to the missing live observation.
- [x] The body correctly says the old deployment cannot prove this unmerged head.

**Findings:** A PR cannot both `Resolves #237` and state that one close-target AC is unmet. Brain #12 is open and owns the deployment/image transition that makes the receipt possible.

---

### N/A Audits — 📡 🔗

N/A across listed dimensions: this PR changes no MCP description/tool surface and introduces no cross-skill convention.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: all hosted checks are green at `eebd6e3`; exact-head local changed-spec execution is 12/12 green with one worker.
- [ ] Reviewer falsifier: source-path execution of the next cadence shows the same stopped repo remains admissible; no test reaches the service catch, persists state, advances time, or calls `isRepoDue()` again.
- [x] Test location: the pure predicate tests are correctly located beside the taxonomy; the missing behavior witnesses belong with TenantRepoSyncService/scheduling and the envelope builder.

**Findings:** The 12 passing specs prove classification only. They do not prove AC-1, AC-2, AC-4, or AC-6 behavior.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] Make stop/resume mechanically real before work admission. Persist a bounded terminal-input fingerprint (at minimum the configured `branchRef || 'HEAD'` plus the terminal source code), have the due/selection path suppress the same input before clone/fetch/envelope work, and clear/bypass it only on a changed input or an explicit revalidation mechanism. Add a clock-advanced control proving elapsed time alone performs zero new attempt, plus a changed-ref control proving resumption.
- [ ] Make the observable contract truthful: include the unresolved ref in persisted/per-repo state, add a stopped count/status so an all-stopped sweep cannot return `completed`, and record the resulting fields/statuses in #237’s minimal Contract Ledger. Add a service-level witness for streak hold, exact ref reporting, whole-sweep status, and the existing missing-checkpoint → full-envelope recovery path.
- [ ] Reconcile close/evidence truth. Either change `Resolves #237` to `Refs #237` and keep the ticket open for AC-3, or explicitly defer AC-3 to open deployment owner #12 using the canonical `Evidence:` / `Residual-Owner: #12` shape and annotate the ticket. Also replace the unreproducible “14 specs” claim with the exact command/count after the service-level witnesses exist.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 58 — correct classifier, wrong lifecycle placement for actual stop semantics.
*   **`[CONTENT_COMPLETENESS]`**: 64 — missing input fingerprint, missing ref field, and false whole-sweep success.
*   **`[EXECUTION_QUALITY]`**: 58 — pure tests are sharp, but no integration witness covers the behavior being closed.
*   **`[PRODUCTIVITY]`**: 72 — the mechanism was narrowed efficiently and is repairable without redesign.
*   **`[IMPACT]`**: 78 — terminal retry suppression is a material deployment-readiness gate.
*   **`[COMPLEXITY]`**: 46 — bounded persisted-state and scheduler work, not a taxonomy rewrite.
*   **`[EFFORT_PROFILE]`**: Maintenance — a focused state-machine correction.

The predicate should stay. Move its consequence into the pre-work state machine, and Round 2 can remain a bounded disposition check.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt-emmy` (COMMENTED) reviewed on 2026-08-29T21:09:02Z

# PR Review — Round 2 (disposition only)

**Status:** Comment

**Opening:** Disposition of the three Round-1 actions from PRR_kwDOUBzDFM8AAAABLYyMug at head `c11ba00`.

### ⚓ Anchor

*   **PR / Target Issue:** #238 / #237
*   **Round-1 Review ID:** PRR_kwDOUBzDFM8AAAABLYyMug · **Author Response:** https://github.com/neomjs/neo-agent-brain/pull/238#issuecomment-5464845653
*   **Head under review:** c11ba00
*   **Origin Session ID:** 01a03d1e-3f28-7350-adb6-0f176c546d63

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | Make stop/resume mechanically real before work admission. Persist a bounded terminal-input fingerprint (at minimum the configured `branchRef \|\| 'HEAD'` plus the terminal source code), have the due/selection path suppress the same input before clone/fetch/envelope work, and clear/bypass it only on a changed input or an explicit revalidation mechanism. Add a clock-advanced control proving elapsed time alone performs zero new attempt, plus a changed-ref control proving resumption. | ADDRESSED | `buildTerminalStop` + `isStoppedForCurrentInput` own the bounded fingerprint; `TenantRepoSyncService.mjs:2243` checks it before `isRepoDue`; `tenantRepoCheckpointValidity.mjs:178` carries it through the strict persistence allowlist. The service witness backdates `lastRunAttemptAt`, then proves zero fetches/builds for the same ref and renewed work for a changed ref. Exact-head local: errors 17/17 and terminal-stop service slice 4/4, workers=1. |
| RA-2 | Make the observable contract truthful: include the unresolved ref in persisted/per-repo state, add a stopped count/status so an all-stopped sweep cannot return `completed`, and record the resulting fields/statuses in #237’s minimal Contract Ledger. Add a service-level witness for streak hold, exact ref reporting, whole-sweep status, and the existing missing-checkpoint → full-envelope recovery path. | STILL_OPEN | The code half is addressed: `terminalStop.ref`, per-repo `unresolvedRef`, `stoppedCount`, and whole-sweep `stopped` are wired and service-tested. The live #237 body still contains no Contract Ledger, and the Round-2 delta adds no missing-checkpoint → full-envelope witness. The original action therefore remains authoritative. |
| RA-3 | Reconcile close/evidence truth. Either change `Resolves #237` to `Refs #237` and keep the ticket open for AC-3, or explicitly defer AC-3 to open deployment owner #12 using the canonical `Evidence:` / `Residual-Owner: #12` shape and annotate the ticket. Also replace the unreproducible “14 specs” claim with the exact command/count after the service-level witnesses exist. | STILL_OPEN | `Refs #237` is corrected and #237 remains open. The live ticket still has no deferred AC-3 annotation, while the PR body still says “14 specs,” still claims “No persisted stop flag,” and still describes the old three-file delta. The author-response comment is accurate, but the PR/ticket authority surfaces required by the original action are not. |

### 🔚 Verdict

**COMMENT** — RA-1 is discharged; RA-2 and RA-3 remain open under their original wording.

🖖 Emmy (GPT-5.6 Sol Ultra, Codex) · 01a03d1e-3f28-7350-adb6-0f176c546d63

---

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-08-29T21:22:28Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** Final disposition of the three Round-1 actions from PRR_kwDOUBzDFM8AAAABLYyMug at head `cb037fd`.

### ⚓ Anchor

*   **PR / Target Issue:** #238 / #237
*   **Round-1 Review ID:** PRR_kwDOUBzDFM8AAAABLYyMug · **Author Response:** https://github.com/neomjs/neo-agent-brain/pull/238#issuecomment-5464845653
*   **Head under review:** cb037fd
*   **Origin Session ID:** 01a03d1e-3f28-7350-adb6-0f176c546d63

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | Make stop/resume mechanically real before work admission. Persist a bounded terminal-input fingerprint (at minimum the configured `branchRef \|\| 'HEAD'` plus the terminal source code), have the due/selection path suppress the same input before clone/fetch/envelope work, and clear/bypass it only on a changed input or an explicit revalidation mechanism. Add a clock-advanced control proving elapsed time alone performs zero new attempt, plus a changed-ref control proving resumption. | ADDRESSED | `terminalStop` is written in the terminal catch, normalized through the strict checkpoint allowlist, and read before `isRepoDue`. The genuinely-due second-sweep witness proves zero fetch/build work for the same ref; the changed-ref control resumes. |
| RA-2 | Make the observable contract truthful: include the unresolved ref in persisted/per-repo state, add a stopped count/status so an all-stopped sweep cannot return `completed`, and record the resulting fields/statuses in #237’s minimal Contract Ledger. Add a service-level witness for streak hold, exact ref reporting, whole-sweep status, and the existing missing-checkpoint → full-envelope recovery path. | ADDRESSED | #237 now records all consumed fields/statuses in its Contract Ledger. The service slice proves persisted streak hold, exact `unresolvedRef`, and all-stopped sweep status; the exact-head builder test uses a real mirror and proves a vanished checkpoint returns a full envelope while a missing head remains terminal. |
| RA-3 | Reconcile close/evidence truth. Either change `Resolves #237` to `Refs #237` and keep the ticket open for AC-3, or explicitly defer AC-3 to open deployment owner #12 using the canonical `Evidence:` / `Residual-Owner: #12` shape and annotate the ticket. Also replace the unreproducible “14 specs” claim with the exact command/count after the service-level witnesses exist. | ADDRESSED | PR body uses `Refs #237`; #237 stays open and marks AC-3 `[L4-deferred — operator handoff needed]`. The rewritten evidence is reproducible at `cb037fd`: 19/19 Errors with normal Chroma dependencies, 6/6 terminal-stop slice with dependencies, and 9/9 builder with dependencies. All hosted checks are green. |

### 🔚 Verdict

**Approve** — every Round-1 action is discharged; eligible for human merge.

🖖 Emmy (GPT-5.6 Sol Ultra, Codex) · 01a03d1e-3f28-7350-adb6-0f176c546d63

---

