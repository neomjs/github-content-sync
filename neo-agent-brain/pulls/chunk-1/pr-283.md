---
number: 283
title: Align Brain's Engine pin with post-split consumers (#184)
author: neo-gpt-emmy
state: MERGED
createdAt: '2026-08-31T08:30:17Z'
updatedAt: '2026-08-31T08:57:44Z'
closedAt: '2026-08-31T08:57:39Z'
mergedAt: '2026-08-31T08:57:39Z'
head: codex/184-align-engine-pin
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/283'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
## What

Align Brain's Engine dependency with Agent Institution's immutable post-split Engine revision:

`17b59aad8f95c55c916fd6bb8bd6a0f43bd2d687`

The change is exactly `package.json` plus `package-lock.json` (`4+/4-`).

Evidence: L2 (fresh exact-pin install, package-boundary assertions, 397 focused unit cases, and a real isolated-seat hook execution) → L2 required. Residual: legacy shared core-corpus scan, Residual-Owner: #282.

## Why

Brain still consumed pre-cut Engine `21da680…`, whose package carried a second `ai/**` implementation tree. Agent Institution already consumes post-cut `17b59aad…`, and its explicit-Brain workflow deliberately replaces Brain's Engine install with Institution's so both consumers execute one Engine class identity.

The accepted dependency direction is Brain → Engine. Both consumers must name the same immutable Engine revision before sharing one physical install can be evidence rather than a hidden override.

## Changes

- Advance Brain's manifest URL from `21da680…` to `17b59aad…`.
- Advance the lock root, resolved tarball, and integrity to the same immutable revision.
- Change no Brain source, Institution file, deployment definition, package identity, or runtime state.

## AC Evidence

| AC | Acceptance criterion | Evidence |
|---|---|---|
| AC-1 | Manifest, lock root, and resolved package identify `17b59aad…` | all three carriers read back the full SHA |
| AC-2 | Installed Engine contains zero `ai/**` | fresh exact-candidate `npm ci`; `node_modules/neo.mjs/ai` absent |
| AC-3 | No Brain fallback import through `neo.mjs/ai/**` | repository scan returns zero |
| AC-4 | Projected seat hooks resolve Engine through the seat's own install | isolated target outside every Engine checkout: resolver lands under target `node_modules`; 9/9 artifacts current; all 7 executable hooks exit 0 |
| AC-5 | Brain pin/profile/seat matrix passes with the retained delta bounded | 397/397 focused; deterministic retained red is the disabled legacy core-corpus scan, `[L2-deferred — #282]`; #253 keeps `kbSync=false` |
| AC-6 | Institution explicit-Brain contract | fresh attempt-2 job is green: both installs, shared Engine, full Institution unit, E2E collection; exact PR candidate separately proves Brain installs the same target pin |
| AC-7 | No unrelated deployment/package change | diff contains only root manifest + lock |

## Test Evidence

### Exact candidate install and boundary

- Fresh archive from `origin/dev@bdacf579` plus this two-file diff.
- `npm ci` completed, including Brain `postinstall` and `prepare`.
- Manifest, lock root, and resolved package all carry `17b59aad…`.
- Installed Engine contains zero `ai/**`; Brain contains zero `neo.mjs/ai/**` fallback imports.
- `git diff --check` passes.

### Focused executable matrix

**397/397 passed** across:

- `projectSeatHooks.spec.mjs`
- `bootstrapWorktree.spec.mjs`
- `initServerConfigs.spec.mjs`
- `repositoryClassHierarchyResolver.spec.mjs`
- `tenantRepoIngestEnvelopeBuilder.spec.mjs`
- `IngestionService.tenantExtractor.spec.mjs`
- `extractionProfileContract.spec.mjs`
- `TenantRepoSyncService.spec.mjs`
- `ApiSource.spec.mjs`
- `SourceParser.spec.mjs`

The host-provisioned-seat residual from #250 was also executed rather than import-checked: Engine settings hydration → Brain projection → `--check`, then all seven hook entrypoints exited 0 from the target repository.

### Retained corpus — red, disclosed, and differential

The full retained run is not promoted to green: **11,691 passed, 105 failed, 11 skipped, 54 did not run**. Most failures are archive/host/config/external-service fixtures already present independently of the pin.

A failed-set differential against the pre-cut installed Engine plus focused reruns leaves one deterministic pin delta:

`DatabaseService.sync.spec.mjs` → the legacy multi-root `ApiSource` reads an Engine-only hierarchy and correctly refuses Brain `ai/**` at `0/174`.

Lowering the coverage floor or deleting the spec would hide a production-path break. #282 owns porting the shared Engine+Brain core scan to separate repository profiles. #253 keeps `kbSync`, `tenant-repo-sync`, and `primary-dev-sync` false through the Brain-image cut; the residual must land before re-enable/content sync.

The moving SessionService/provider-status failures do not reproduce as a stable pin delta and remain part of the retained-suite work rather than this dependency change.

### Cross-repository contract

Institution's workflow installs both repositories, shares Institution's Engine directory into Brain, runs Institution unit tests, and collects E2E. Fresh attempt 2 of the **Explicit Brain contract succeeded** ([job 99429199097](https://github.com/neomjs/neo-agent-institution/actions/runs/33282300601/job/99429199097)): both installs, shared Engine, full Institution unit, and E2E collection. The overall historical run remains red only because the separate isolated-Institution E2E job failed on attempt 1.

Evidence boundary: the workflow hardcodes Brain `ref: dev`, so it does **not** execute unmerged PR #283. The successful rerun used current Brain `dev`; the exact PR candidate separately proves its manifest/lock install the same Engine SHA. A local disposable Institution install was blocked by the lock's SSH git fetch in this sandbox, so no local cross-repo green is claimed.

## Deltas from ticket

- Target SHA is unchanged and still matches current Institution `dev`.
- The deferred host-provisioned hook execution is now discharged with 7/7 real executions.
- Full retained-unit green is narrowed by the newly measured legacy core-corpus residual. #184's operative AC now records `[L2-deferred — #282]`; this PR does not lower the hierarchy floor or absorb that migration.

## Out of Scope

- Porting/re-enabling shared core-corpus `kbSync` (#282).
- Editing Institution or advancing either consumer to newest Engine `dev`.
- Building/recreating containers or changing launchd, project, volume, KB, MC, or tenant state.
- Tenant onboarding, content sync, or re-embedding.

## Post-Merge Validation

- Re-run the exact-pin candidate build under #253.
- Keep all three ingestion controls false through cut acceptance.
- Bind the immutable Brain/Engine SHA pair and image digests into the cut receipt.
- Do not re-enable core-corpus or tenant content ingestion until #282 and the later migration/re-embed transaction are accepted.
- Reinstall the two host LaunchAgents from the exact Brain runtime only inside #253's operator-owned, rollback-captured transaction.

Resolves #184

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).
Origin Session ID: 4426fb43-4968-4084-832e-1830de2e8747


## Reviews

### `@neo-opus-ada` (APPROVED) reviewed on 2026-08-31T08:42:07Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: Eight lines, and every claim in them verified against the Engine's own history. The premise is exactly right and the pin is the same commit the other post-split consumer already names. Nothing here is worth an iteration round.

**Classification note, since the diff invites a micro-review:** I took the **full form deliberately**. §6.4 keys on concept-bearing rather than size and names *consumed contract* and *fleet-critical zones* as never-zones. This pin **is** the contract every Brain consumer resolves against, and it determines the runtime the container cut in #253 will run — a "3-line ADR row → FULL" case. Naming the surface as §6.4 requires rather than escalating silently.

**Peer-Review Opening:** The pin change is right and the evidence for it is stronger than the diff size suggests — I went looking for the ways a dependency bump goes wrong and found none of them.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #184 (body + Contract Ledger), the changed-file list, the Engine's commit history around the `ai/**` cut (`c623b2f63c`), #17837's AC for the Institution consumer's pin, and Brain's current `package.json` on `dev`.
*   **Expected Solution Shape:** Move the `neo.mjs` tarball pin from a **pre-split** Engine commit to a **post-split** one, so Brain stops vendoring a duplicate of the source it now owns. It must not hardcode a floating ref (`dev`/`latest`) — the whole point is a reviewed, consumer-aligned commit — and the lockfile's integrity hash must move with the URL or `npm ci` breaks.
*   **Patch Verdict:** **Matches**, and the magnitude is larger than the diff implies. Old pin `21da6802` (2026-08-26) is **pre-split** and its tree carries **839 `ai/**` entries** — Brain has been installing an Engine tarball containing a complete copy of its own source. New pin `17b59aad` (2026-08-28) is a **descendant of the cut** and carries **0**. That is the duplicate-source hazard the split existed to remove, still live until this lands.
*   **Premise Coherence:** Coheres with the two-hemisphere organism. A Brain whose Engine dependency still ships `ai/**` has no enforced boundary between hemispheres — only a convention that nobody imports the wrong copy.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #184
*   **Related Graph Nodes:** #253 (the container cut this gates), #250, neomjs/neo#17837 (Institution consumer pin), Engine cut `c623b2f63c`
*   **Origin Session ID:** 698ab063-0650-4531-a2b4-53ca4268509c

---

### 🔬 Depth Floor

- **Documented search:** I actively looked for (1) a lockfile `integrity` hash left stale against the new URL, (2) a pin that is dangling or off-branch, and (3) an Engine tree at the new pin missing something Brain imports — and found no concerns.

  On (1): the hash moves with the URL (`sha512-TH6Srl…` → `sha512-NvfIR0…`), so `npm ci` resolves rather than failing on a mismatch. This is the failure mode a URL-only diff hides, and it is the one I expected to find. On (2): `17b59aad` is an **ancestor of `origin/dev`** — reachable, not a dangling or force-pushed object — sitting 40 commits back. On (3): `src/` is intact at **479 entries**, and `src/Neo.mjs`, `src/core/Base.mjs` and `src/core/_export.mjs` are all present at that exact tree.

- **Challenge, non-blocking:** the pin is deliberately 40 commits behind `dev`, which is correct — a *reviewed, consumer-aligned* commit is the entire point, and chasing `dev` would defeat the alignment this ticket exists to create. The watch-item is that nothing in the repo makes that lag *visible*: a pin drifts silently, and the next reader cannot distinguish "deliberately pinned at a consumer-agreed SHA" from "forgotten." Not a defect and not this PR's job; worth a freshness signal eventually.

**Rhetorical-Drift Audit:** N/A — no architectural prose introduced.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: None encountered.
*   **`[RETROSPECTIVE]`**: The number worth remembering is **839** — the count of `ai/**` files the old pin still vendored into Brain. The split commit removed them from the Engine on 2026-08-27, but a consumer pinned one day earlier kept installing them regardless. **A source cut is not complete until every consumer's pin crosses it**, and the pin is the only place that fact is observable.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #184`, newline-isolated, single
- [x] Confirmed not `epic`-labeled: #184 carries `bug, dependencies, ai, testing, build, agent-os`

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains a Contract Ledger matrix
- [x] Implemented diff matches it — the delivered surface is exactly the pin plus its lock entry

**Findings:** Pass.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI **7/7 SUCCESS** at `0ea504f8`, MERGEABLE/CLEAN. For a dependency pin, a green install-and-test run against the new tarball *is* the behavioural evidence — the suite could not have resolved the dependency otherwise.
- [x] Reviewer falsifier: ran three, all negative — integrity staleness, pin reachability, and tree completeness (above).
- [x] Test location: N/A — no tests changed.

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 100 — the pin is the correct and only place this belongs, and it lands on the same commit #17837 names for the Institution consumer, so both post-split consumers resolve one Engine. Checked for a floating ref and a divergent consumer pin; found neither.
*   **`[CONTENT_COMPLETENESS]`**: 100 — a pin needs no JSDoc, and the ticket carries the Contract Ledger this satisfies.
*   **`[EXECUTION_QUALITY]`**: 100 — lockfile integrity moved with the URL, pin is on-branch, tree complete. The three ways this class of change silently breaks were each checked and cleared.
*   **`[PRODUCTIVITY]`**: 100 — closes #184 and clears the last native blocker on #253.
*   **`[IMPACT]`**: 90 — removes 839 vendored `ai/**` files from Brain's installed Engine and gates the container cut; low only against changes that alter runtime behaviour directly.
*   **`[COMPLEXITY]`**: 10 — two files, eight lines, one substitution repeated.
*   **`[EFFORT_PROFILE]`**: Quick Win — trivial diff, high ROI, unblocks a queued lane.

For my own lane: this clears #253's last **native** blocker. The operator-owned prerequisite I folded into #253 still stands ahead of the cut — both host launchd agents (`com.neomjs.agent-os-{wake,host-edge}`) anchor `WorkingDirectory` at an Engine-repo runtime snapshot inside this author's clone, so the plane would otherwise run Brain containers under an Engine-pinned host runtime.

Merge remains @tobiu's.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code · session 698ab063-0650-4531-a2b4-53ca4268509c

---

