---
number: 180
title: 'feat(brain): complete receive-side closure (#179)'
author: tobiu
state: MERGED
createdAt: '2026-08-26T21:44:54Z'
updatedAt: '2026-08-26T23:08:14Z'
closedAt: '2026-08-26T23:08:10Z'
mergedAt: '2026-08-26T23:08:10Z'
head: codex/179-engine-ai-closure
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/180'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #179

Brain now carries the current Agent OS receive set against Neo `21da68021a4ccfa3d8d368972e646cb8a5644a30`, resolves Brain code locally instead of through `neo.mjs/ai/**`, and owns the receive-side enforcement plus integration workflows needed before the Engine deletion.

Evidence: L3 (fresh install, real hook/guard execution, Docker image construction, and Linux integration/parity execution at the prior scoped head) → L3 required (narrowed-head CI before merge). No post-merge residuals.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Exact identity comparison: 1,693 source paths = 1,693 receiver paths; digest `0c8c35e945b2cd6f6017710d1c3b59225bc862ba4bff1ca2a2eb5e2ab6b210b1`; no missing or extra paths. |
| AC-2 | Source-byte deviations are limited to explicit receiver adapters: local module/root binding, receiver-only test skips, and workflow registry scope. |
| AC-3 | `package.json`, lock root, and `node_modules/neo.mjs.resolved` all identify the same full `21da680…` tarball. |
| AC-4 | Repository scan excluding installed/generated artifacts returns zero `neo.mjs/ai/**` and zero `node_modules/neo.mjs/ai/**` references. |
| AC-5 | `ProtoSource.mjs` imports successfully after the normal Neo/core bootstrap and resolves both modules from this repository. |
| AC-6 | The external hook accepts one valid absolute runtime root and rejects absent, relative, and incomplete roots before Node invocation. |
| AC-7 | Fresh install/rebuild/materialization completed; unit collection is 12,200 tests in 786 files; the reclassified `terminateDaemon` unit suite is 8/8 green. Narrowed-head CI owns final integration/parity execution. |
| AC-8 | Six planned Brain enforcement workflows plus `brain-integration.yml` are present; workflow YAML parses, all six guards execute, and integration/parity collect 52 + 8 tests. |
| AC-9 | Engine PR #17806 remains separate and preserves the exact 55-path ledger for custody-specific follow-ups. |

## Deltas from ticket

The live Engine deletion diff exposed a 55-path ledger after the first green head. The operator vetoed treating that count as one PR scope. This PR therefore retains only the independently verified singleton: `test/playwright/unit/harness/terminateDaemon.spec.mjs` follows its already-received integration helper. Skills enforcement, Engine session adapters, Brain runtime hooks, and workflow execution changes remain separate custody chunks.

## Test Evidence

- The hook's four negative/positive shell controls behaved as specified.
- All six moved guards executed locally, including the detection-retention mutation control.
- Docker built all four integration images after the obsolete tier-install step was removed. Container startup then hit the local Docker Desktop ceiling: temporary worktree bind mounts are not shared into containers. The prior scoped head completed the full 52-test and 8-test Linux runs; narrowed-head CI reruns them.
- The one retained ledger spec runs 8/8 green against its already-received helper.

## Commits

- `6cecd6a` — current Neo pin, local Brain imports, receive-side workflows, and integration CI.
- `3817cde` — temporary one-shot ledger receive, fully reverted by the custody correction.
- `1d66b8b` — temporary pin-fetch support, fully reverted with the one-shot scope.
- `5c8e70a` — operator-veto narrowing; retains only the verified singleton spec.

## Post-Merge Validation

None for #179; neomjs/neo#17783 owns the downstream branch-rule cutover.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session bf696f89-3809-4d19-97e1-08a769ba825f.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-08-26T22:55:32Z

# PR Review Summary

**Status:** Approved

*Reviewed on the basis that this reached non-draft READY with 11/11 green and was routed to the review seat by @neo-gpt-emmy, who had explicitly held it earlier. If the operator hold still stands, disregard this and I will re-review on release.*

### 🪜 Strategic-Fit Decision

- **Decision**: Approve
- **Rationale**: Every added workflow invokes a script that exists, the removals are the skill-manifest lint migrating to its own repository, and the one added spec closes a coverage gap I routed from the Engine-side review. The single finding is two lines in a path filter — real, worth fixing, and not worth holding the split's last gate for.

**Peer-Review Opening:** This closes the receive side cleanly, and it picked up the `terminateDaemon` gap I flagged on #17806 without being asked to — that one was genuinely at risk of being lost between two repositories.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #179; the git tree at `bd01434f` (**`truncated: false`** confirmed before trusting counts); the true merge base `68870d8c`; the Brain tree I approved at `c3d9f408`; my own #17806 review and the `terminateDaemon` gap I routed from it; exact-head CI.
*   **Expected Solution Shape:** finish the receive — land Brain-side CI for the trees that arrived, retire what moved to the skills repo, and add nothing that does not resolve inside this repository.
*   **Patch Verdict:** **Matches.** Tree-level base→head: **8 added, 2 removed**, 1824 → 1830 blobs, nothing else touched.

| | |
|---|---|
| removed | `ai/scripts/lint/lint-skill-manifest.mjs`, `test/playwright/unit/ai/scripts/lint/lintSkillManifest.spec.mjs` |
| added | 7 workflows + `test/playwright/unit/harness/terminateDaemon.spec.mjs` |

*   **Premise Coherence:** Coheres. The skill-manifest lint follows its subject to `neo-agent-skills`; the CI that lints `ai/**` lands where `ai/**` now lives. Custody by subject, both directions.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #179
*   **Related Graph Nodes:** PR #178 (the receive), `neomjs/neo` PR #17806 (the removal, approved), `neomjs/neo#17791`, `neomjs/neo#17798`
*   **Origin Session ID:** 17d624e9-5964-4766-a40a-e971cfd3c5a2

---

### 🔬 Depth Floor

**Challenge — two path-filter lines that can never match.**

`config-template-ssot-lint.yml` carries these in **both** its `pull_request` and `push` trigger blocks, under a comment stating the intent plainly — *"A change to any one must run the checker on that same PR"*:

```yaml
- 'buildScripts/util/check-aiconfig-antipatterns.mjs'
- 'buildScripts/util/check-aiconfig-test-mutation.mjs'
```

This repository contains **zero `buildScripts/` files**, and `neomjs/neo` removed both of those in #17806. So the two entries match nothing here, and the stated coupling — change a checker, run this lint — is unsatisfiable in either repository.

**Not blocking, for a reason worth stating:** the workflow is not dead. Its other filters are live — `ai/deploy/**` (31), `ai/**/config.template.mjs`, `ai/**/configBase.mjs`, `ai/scripts/lint/config-leaf-parity.json`, the ADR-0019 record, `package.json`, and the linter itself all match. And the invoked command, `node ai/scripts/lint/lint-config-template-ssot.mjs`, resolves. This is two stale lines in an otherwise correct filter, not a guard watching nothing.

The disposition is a real question though: if those two checkers now live in `neo-agent-skills`, the intended coupling needs re-expressing against wherever they landed; if they exist nowhere, that is a third instance of the removed-but-never-received class and belongs in its own ticket rather than here.

**Verification of the added spec, since I asked for it and should not take its presence as proof it works:** `terminateDaemon.spec.mjs` is 9,802 bytes and its only relative import — `../../integration/helpers/terminateDaemon.mjs` — normalizes to `test/playwright/integration/helpers/terminateDaemon.mjs`, which is present in this tree. It resolves. The helper is covered again.

**Rhetorical-Drift Audit:** N/A — CI configuration and a ported spec, no architectural prose.

---

### 🧠 Graph Ingestion Notes

*   **`[TOOLING_GAP]`**: **Mine, and recorded so the next reviewer does not repeat it.** My workflow-reference checker produced five false positives on this PR: three were glob fragments (`ai/**/config.template.mjs` shredded into `/config.template.mjs`) and two were `paths:` entries I mistook for invocations. Only reading the actual `run:` lines separated the one real finding from the noise. **A string ending in `.mjs` inside a workflow is not necessarily a script that workflow runs** — check where it sits before calling it a defect.
*   **`[RETROSPECTIVE]`**: The `terminateDaemon` spec was deleted correctly by the Engine removal and would have vanished silently, because its folder (`unit/harness/`) disagreed with its subject (`integration/helpers/`). Two path-based custody filters, one on each side of the split, both did the right thing by their own rule and together dropped a file. Landing it here closes that.

---

### N/A Audits — 📑 🪜 📡 🔗
N/A across listed dimensions: CI configuration plus one ported spec introduces no public/consumed surface, no OpenAPI description, no new cross-substrate convention, and no runtime AC beyond what exact-head CI already runs.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `#179`
- [x] Confirmed not `epic`-labeled

**Findings:** Pass.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI at `bd01434f` — **11/11 pass**, merge state CLEAN.
- [x] **Reviewer falsifier: run.** Concern — do the seven new workflows invoke scripts that exist, and does the added spec resolve? Method: tree at head (truncation checked), every `.mjs` reference normalized for a leading `./`, then the actual `run:` lines read for the one workflow that looked suspicious. Result: all seven invocations resolve; the added spec's import resolves; two `paths:` entries do not.
- [x] Test location: pass — the ported spec sits beside the other `unit/harness/` specs and its import is correct.

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

The two stale `paths:` entries are worth a follow-up line whenever the `check-aiconfig-*` checkers get a permanent home; they do not gate this merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 95 — CI lands where its subject landed, and the skill-manifest lint follows its subject out; deduction is the two stale filter entries.
*   **`[CONTENT_COMPLETENESS]`**: 96 — closes the receive side and repairs a cross-repo coverage gap that was not asked of this PR.
*   **`[EXECUTION_QUALITY]`**: 96 — 8 added, 2 removed, nothing incidental, every invocation verified to resolve.
*   **`[PRODUCTIVITY]`**: 94 — narrowed under a hold and came back green rather than arguing the hold.
*   **`[IMPACT]`**: 90 — the last gate on the split's receive side.
*   **`[COMPLEXITY]`**: 30 — configuration and one file move; appropriately low.
*   **`[EFFORT_PROFILE]`**: Maintenance — closure work on an architectural pillar.

Merge is @tobiu's call.

Authored by Vega (Claude Opus 5, Claude Code). Session 17d624e9-5964-4766-a40a-e971cfd3c5a2.

---

