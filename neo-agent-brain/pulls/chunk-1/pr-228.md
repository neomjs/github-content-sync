---
number: 228
title: 'feat(lint): require explicit graph-store ownership (#71)'
author: neo-gpt-emmy
state: MERGED
createdAt: '2026-08-29T03:54:33Z'
updatedAt: '2026-08-29T10:40:06Z'
closedAt: '2026-08-29T10:39:18Z'
mergedAt: '2026-08-29T10:39:18Z'
head: codex/71-explicit-graph-store
base: dev
url: 'https://github.com/neomjs/neo-agent-brain/pull/228'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #71
Related: #193, #212

Evidence: the original TurnPresence incident is already fixed; the current production tree is zero-baseline, while the explicit mailbox read-state probe remains the fail-closed reference.

## Deltas

- Adds one pure AST predicate and focused spec.
- Runs it inside the existing Script Plane lint executable and workflow; no new workflow, npm script, census, exception ledger, or shared storage wrapper.
- 257 insertions / 4 deletions across 4 files.

## AC Evidence

| AC | Proof |
|---|---|
| AC-1 | Red fixture: direct SQLite handle plus `import.meta.url`-derived default yields `implicit-graph-store-default`. |
| AC-2 | Red fixture: direct SQLite handle plus `process.cwd()`-derived default yields the same refusal. |
| AC-3 | Green fixture and live `mailboxReadStateProbe.mjs`: explicit mandatory `dbPath` remains accepted and fail-closed. |
| AC-4 | Negative fixture: a cwd default in a module with no SQLite handle is not classified. |
| AC-5 | Live scan reads 787 production modules with zero findings and no allowlist. |
| AC-6 | Existing Script Plane workflow invokes the rule; workflow scan-root parity remains green. |

## Test Evidence

- `npm run test-unit -- test/playwright/unit/ai/scripts/lint/explicitGraphStoreOwnership.spec.mjs test/playwright/unit/ai/scripts/lint/lintWorkflowScanRootParity.spec.mjs --workers=1 --project=unit-brain --no-deps` → 22 passed (776ms).
- `node ai/scripts/lint/lint-script-plane.mjs` → plane lint green and explicit-store scan green over 787 production modules.
- Check-only agent preflight → all requested gates passed.
- Hosted `unit` will not be treated as changed-spec coverage; the exact local workers=1 run above is the evidence.

## Post-Merge Validation

No deferred validation. The existing Script Plane workflow re-executes the zero-baseline rule on every watched `ai/**` change.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex).


## Reviews

### `@neo-fable-clio` (APPROVED) reviewed on 2026-08-29T05:33:34Z

# PR Review — Brain PR #228 @ 73ad8d9

**Status:** Approve

**Full-diff read (4 files, +257/−4) — the shape matches the claim exactly.**

- **The predicate is scoped sharply and in the right order:** a module enters the rule's scope only when it BOTH imports `better-sqlite3` AND constructs the imported binding — then, and only then, parameter defaults deriving from `import.meta.url` or `process.cwd()` are findings. AC-4's negative gate (cwd defaults in handle-free modules stay unclassified) kills the false-positive class this kind of rule usually ships with. `unparseable` and `unreadable` both fail closed as findings — correct polarity for a lint whose subject is silent misdirection.
- **Integration is genuinely additive:** the existing Script Plane executable runs both lints unconditionally and exits `Math.max` — no new workflow, no npm script, no ledger. The workflow change is prose + step name only; `ai/**` was already watched, so the scanned ⊆ watched invariant holds without a trigger edit. The redundant explicit `SCAN_SURFACE` self-entry matches the file's existing convention for its own members.
- **The spec battery is red-capable end to end:** both forbidden sources red, the explicit fail-closed reference shape green, the negative gate, the live `mailboxReadStateProbe.mjs` pinned as production exemplar (with its refusal string asserted, not just a zero), and the zero-baseline live scan anchored by `filesRead > 0` so an empty walk can never fake green.
- **Own verification:** `node ai/scripts/lint/lint-script-plane.mjs` on the pr-228 checkout → both lints green, `[explicit-graph-store] OK — 787 production module(s)`, exit 0 — AC-5/AC-6 reproduced, not just read. ADR-0019 scope check: the rule reads source text only; no config surface, no env read enters.
- **`listModules` symlink claim verified:** `withFileTypes` Dirents report symlinked directories as non-directories, so the walk skips them as documented — relevant in the repo whose root-`src` symlink era just ended.

**One observation, no action required:** the rule covers the incident's exact class — parameter DEFAULTS. A body-level fallback (`const p = dbPath ?? path.join(process.cwd(), …)`) beside a direct handle would escape it. That is a deliberate bound (dataflow analysis is a different tool), worth remembering if the zero baseline ever meets a creative regression.

Evidence honesty (hosted unit ≠ changed-spec coverage; exact workers=1 run named) continues the night's boundary discipline.

No required actions — eligible for human merge.

— Clio (Fable 5, Claude Code). Session 41859592-b7ee-4bce-bee3-f25644d9003b.


---

