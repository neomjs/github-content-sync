---
number: 251
title: >-
  feat(cockpit): perspectives switch from their drawer; the bar's preset buttons
  retire (#242)
author: neo-opus-ada
state: OPEN
createdAt: '2026-09-26T10:38:04Z'
updatedAt: '2026-09-26T12:19:58Z'
closedAt: null
mergedAt: null
head: ada/242-perspective-bar
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/251'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #242

The cockpit's top-left Overview · Focus · Review buttons are gone (the operator, 2026-09-26: "just moving the splitter … review opening the side view, which you also get when selecting an agent card … a waste of space"). The Perspectives drawer in the right rail is now the one place to apply or capture a layout. A refused switch used to show on the bar's refusal line; the drawer now names it on its meta line until a switch succeeds. The control bar keeps the state block and its actions.

- `cockpit/Container.mjs`: the bar declares no preset group and no refusal line. `presetError` is projected into `perspectives.applyNote` (`publishPerspectives`, re-published on every refresh), and `syncControlBar` keeps only the vessel-chrome sync.
- `perspectives/Container.mjs`: the meta line joins the refused switch and the capture verdict (`notesOf`).
- `CockpitPerspectives.buttons()` / `group()`, the controller's `onPresetSelect`, and the `.fm-preset-*` SCSS are deleted.

Evidence: L2 (unit arms, red on dev; the Neural Link journey over the real drawer) → L2 required for AC-1…AC-3; AC-4 is the goldens.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | `declaration.spec` "the drawer follows the engine's published name… no bar renders"; `FleetCockpitDockNL` "perspective presets switch the committed…" now reveals the drawer and applies Focus from its card (a real click) |
| AC-2 | The drawer lists Overview · Focus · Review (`declaration.spec`, `perspectiveCapture.spec`); card selection is untouched (`cockpit-review-1280.png` shows the inspector docked beside the roster, applied from the drawer) |
| AC-3 | `declaration.spec` "an unknown name is refused… the refusal lands in the drawer"; `projection.spec` "a standing refusal rides every drawer projection until a switch clears it"; `perspectives/container.spec` "a refused switch is named on the meta line…"; `FleetCockpitDockNL` reads the drawer's `applyNote` after `activatePerspective('Ghost')` |
| AC-4 | `cockpit-default-shell`, `cockpit-intermediate-720`, `cockpit-review-1280`, `cockpit-vessel-314` re-captured (before: the bar's three buttons top-left; after: the bar carries only the state block and its actions); stamp re-written |

## Deltas from ticket
- Scope narrowed at intake, recorded in the ticket body: the Review duty stays in the drawer. It is the only declared perspective that docks the inspector, and five arms pin the cold-inspector seating through it.
- **The goldens could not see this change.** Three of the four passed unchanged against the old images. Playwright's per-pixel threshold (0.2) counts dark buttons on the dark bar as equal, and only their label pixels count toward `maxDiffPixelRatio` 0.001. I re-captured them anyway so they show what renders, and noted the harness gap as a defect-note.

## Test Evidence
- Red first: with the source changes stashed, 4 of the new or changed arms fail on dev.
- At this head (`d3b256c`): unit 971/971 · visual 18/18 after re-capture · e2e `FleetCockpitDockNL` 2/2 + `FmReducedMotionCollapse` 2/2 (Brain runtime `7786f01`). The motion witness now reads the bar's Reconnect button (same `--motion-fast` tier) instead of a preset.
- Net diff: 46 lines in, 137 out.

## Post-Merge Validation
- [ ] In a team `.app` rebuilt from dev, the cockpit shows no perspective buttons, and the Perspectives drawer applies Focus and Review.

Authored by Ada (Claude Opus 5.5, Claude Code). Session 1b945fcf-1142-475f-8007-ac18d51c069a.

