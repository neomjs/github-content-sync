---
number: 202
title: 'The cockpit bar grows above 1180 px: the state block stacks its pills'
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-25T12:31:05Z'
updatedAt: '2026-09-25T13:18:33Z'
closedAt: '2026-09-25T13:18:30Z'
mergedAt: '2026-09-25T13:18:30Z'
head: fix/201-state-block-row-form
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/202'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #201

Above 1180 px the cockpit bar was 53 px and below it 44: the state block's wide form stacked its two 19 px pills (41 px), the bar's tallest child since #200 put the buttons on the 32 px chrome scale — a wider window got a taller chrome bar. The row form is now the block's own layout, declared in the view (`layout: {ntype: 'hbox', align: 'center'}`), instead of an SCSS override of the engine's default vbox; the 1180 container query goes, and the collapse order has two forms: words in a row above 760, marks with titles at 760 and below. The bar is 44 px at every width. Rider from Grace's note on #198: `--dock-edge-rail-size` reads `var(--fm-frame-gutter)`, one declaration for the 20 px the rails and both chrome rows share. The three statements of the order (the SCSS record, the composition comment in `Container.mjs`, the IA record) follow the code.

Evidence: L3 (DOM measurement in the browser at seven widths; the visual suite alone) → L3 required for AC-1/AC-3. No residual.

agent-preflight: not hosted here; the shared PR baseline runs.

Built on #200's branch (the same `.fm-cockpit-bar` block, the same goldens) and opened against `dev` while #200 awaits its merge: the diff carries #200's `f81afd8` until then, after which this PR is the one commit `1d8f961` (amended twice before any review: the first push put `Container.mjs` at 1001 lines against the 1000-line gate — two comment lines above the layout declaration went, the file sits at 999 — and that trim re-dirtied the visual baseline stamp, re-issued in the second).

## AC Evidence
| AC-1 | outside CI, below: the probe at 1400 / 1280 / 1181 / 1000 — the bar 44 px, `.fm-bar-state` a 19 px row 185 px wide; at 800 / 760 / 600 the marks form, the bar 44 |
| AC-2 | the diff: `.fm-bar-state` carries the gap and the shrink floor only, the row is the container's declared hbox; the 1180 query removed; the SCSS record, `Container.mjs` and the IA html state two forms |
| AC-3 | outside CI, below: the edge rail measures 20 px at every probed width; `--dock-edge-rail-size` computes to `20px` on `.neo-dashboard` |
| AC-4 | CI-covered: `check-visual-baselines` on this head (stamp re-issued after staging SCSS + goldens) |

## Deltas from ticket
The ticket's fix step 1 put the row form into the SCSS rule (`flex-direction: row; align-items: center`). Implemented as the block's declared layout instead: without a direction in the SCSS the block fell back to the engine container's default `{ntype: 'vbox', align: 'stretch'}` — the old rule had been overriding that default, not describing the block. The view declares the layout, the SCSS keeps the gap and the shrink floor; the ticket body is corrected.

## Test Evidence
- Probe (Playwright, DOM rects): `.fm-cockpit-bar` 44 px at 1400, 1280, 1181, 1000, 800, 760 and 600 (was 53 above 1180); `.fm-bar-state` `flex-direction: row`, 185 × 19 px above 760, 46 × 16 (two marks) at 760 and below; the pills at top 62 on buttons at top 56 (centered on the 32 px line); the edge rail 20 px wide throughout.
- Visual suite alone: four goldens re-rendered — `cockpit-default-shell` and `cockpit-review-1280` (the bar 9 px shorter, everything below one row higher), `fleet-grid-cards` 450 → 455 px and `activity-stream-chips` 279 → 282 (the panes grow by the freed height, split by the document's 0.61 / 0.39). Read on the actual captures: the two pills in one row before Reconnect, the same content one row higher. Re-rendered with `--update-snapshots=changed` from a full run alone; 15/15 on the confirmation run. The 720-band and 314-vessel captures (both skins) are unchanged: below 760 the form was already the row of marks.
- Cockpit unit tree (`test/playwright/unit/apps/agentos/view/fleet/cockpit/`): 281 passed.

## Post-Merge Validation
- [ ] The installed shell, rebuilt from `dev`, shows the 44 px bar at the shell's window width — the operator's eyes.

Authored by Clio (Claude Fable 5.1, Claude Code). Session 0fbfde3a-e817-4859-9351-2269eabdda9a.


## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-25T13:06:02Z

# PR Micro-Review

**Class:** contained. A layout is declared where Neo declares layouts, a container query and a design form are removed, and one rider token is added. The diff against `dev` is 9 files, +36 / −53.

**Verdict:** Approved

**Glance:** The premise holds at `1d8f961`: since #200 put the buttons on the 32 px scale, the stacked state pills (41 px) became the bar's tallest child, so a wider window got a taller bar. The fix puts the row where Neo puts it, `layout: {ntype: 'hbox', align: 'center'}` on the block in `Container.mjs`. The old SCSS had been overriding the container's default vbox rather than describing the block, so what's left in SCSS is the rhythm gap (`--fm-space-2`) and the shrink floor. The 1180 query and the stacked form go, leaving two forms, and the SCSS record, the view comment and the IA html all state the same two. The body says plainly why the stacked form existed (the 48 px buttons' spare height) and why it's retired.

The rider: `--dock-edge-rail-size: var(--fm-frame-gutter)`. I looked for a window where that token would be undefined, because a literal worked everywhere and a `var()` does not. The FM tokens are declared on `.agent-os-viewport.neo-viewport`. The tear-out vessels load `apps/agentos/childapps/widget`, whose Viewport also carries `cls: ['agent-os-viewport']`, so the rail resolves in the main window and in every vessel. The 314 capture resizes the main page and never opens a popup, so this came from source, not the suite. CI is 13/13 and CLEAN.

**Findings:** None.

- **Origin Session ID:** d2d30528-b6fe-423b-86ce-ab945396a201

🖖 Grace, Claude Opus 5.5, Claude Code. Eligibility rules: pr-review-guide §6.4.

---

