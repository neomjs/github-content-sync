---
number: 180
title: >-
  feat(activity): rows name their repository — bare #N at home, a short slug
  across origins (#179)
author: neo-fable-clio
state: OPEN
createdAt: '2026-09-23T01:24:51Z'
updatedAt: '2026-09-23T01:26:34Z'
closedAt: null
mergedAt: null
head: agent/179-activity-row-origin
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/180'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #179

The activity row's object cell now names the repository of every conversation the feed carries since Brain #416: bare `#N` inside the home repository (`neo`, or a row without the field), `<short>#N` across repositories from one declared map (`brain`, `institution`, `skills`, `devindex`; an unknown slug renders itself), and the full `neomjs/<repoSlug>#N` as the cell's `title` so hover answers what the short form compresses. Every existing row renders exactly as before; no golden moved.

Evidence: L2 (unit arms on the pure derivation and on a pooled row's object cell; the visual suite green with no golden moved) → L3 required (AC-4 ref-column fit at the default shell width and 820 px on a mixed-origin feed). Residual: AC-4's mixed-origin capture depends on a fleet server reading a corpus checkout root — see Test Evidence for what was rendered locally; Residual-Owner: #10.

## AC Evidence
| AC-1 a `neo` row or a row without `repoSlug` renders `#N` as today | `container.spec.mjs` › "object refs name their repository …" (home with and without the field); the existing row arm "row keeps actor once, named object …" unchanged and green |
| AC-2 a foreign-origin row renders `<short>#N`; an unknown slug renders itself | same arm: `brain#410`, `institution#178`, `skills#9` (stall subject), `neo-unknown-repo#3` |
| AC-3 every row's `title` carries `neomjs/<repoSlug>#N` | same arm (`getActivityObjectTitle`: home, foreign, stall subject, none without a number) + the pooled-row arm "a foreign-origin row carries the short ref in its object cell and the full slug in that cell's title" (`object.vdom.title`) |
| AC-4 the ref column fits at the default shell width and at 820 px | visual suite 15/15 alone, no golden moved (the fixture rows carry no `repoSlug`, so the goldens are the AC-1 proof); the mixed-origin capture is under Test Evidence |
| AC-5 unit arms for AC-1/2/3 red-first | with `RowContainer.mjs` stashed the spec fails to load on the new exports; green 17/17 with it |

## Deltas from ticket
The short-name map and the two helpers live in `RowContainer.mjs` beside their only consumer instead of a new `apps/agentos/util/` file — one declared map, exported for the spec, no new module (Accretion Defense). `lane-claim` rows use `issueNumber` and render the same way.

## Test Evidence
Unit isolated 840, Brain-contract 938 (`NEO_AGENTOS_RUNTIME_ROOT` = the Brain checkout at `1fc890c`), visual 15/15 run alone after a non-interactive theme build, no golden moved; `check-app-file-sizes` 0 over the bar; the visual input stamp regenerated after staging (`baseline-inputs.json`).

Live witness (host-local, a session script, not committed): the cockpit dev server (this head) against the host fleet server at Brain `dev@1fc890c` with `NEO_FLEET_CONTENT_ROOT` = a `github-content-sync` checkout root (five origins), dark skin, 0 console errors, 14 rows read:

```
[939px] #19058 · Dream-RSI and the Dream Pipeline we shipped in April — the re   title=neomjs/neo#19058
[960px] brain#410 · feat(fleet): the activity feed reads a declared content ro   title=neomjs/neo-agent-brain#410
[939px] brain#246 · Fleet content roots are hardwired checkout-relative — the    title=neomjs/neo-agent-brain#246
[951px] brain#253 · Cut the local Agent OS to Brain-built images without movin   title=neomjs/neo-agent-brain#253
```

At 1280×800 the three foreign rows sit in the same ink and column as the bare rows, `overflow=false` on the object cell. At 820×1100 the object cells measure 479–512 px; `brain#410` and `brain#253` fit whole (`scrollWidth = clientWidth`), `brain#246` ellipsizes at "the pr-lane dies …" exactly as the long home titles do at that band (`#19061` ellipsizes the same way) — the slug is the first token and never clips. Design read of the captures: the short form reads as one reference, not as a prefix and a number; nothing else on the row moved. One pre-existing observation, not this PR's: the object column's start x varies with the actor chip's width (a row without an actor starts ≈ 60 px further left).

## Commits
- fe163f4 — the origin display (row helpers, spec arms, stamp).
- 924fcae — the pooled-row layout JSDoc loses a pre-existing ticket reference: the source-comment archaeology gate reads every touched file whole and went red on it at fe163f4; the stamp regenerated with it.

## Post-Merge Validation
- [ ] A deployment whose `NEO_FLEET_CONTENT_ROOT` points at a corpus checkout root shows `brain#N` / `institution#N` rows beside bare `#N` rows in the live cockpit (the plane's compose value is Brain `#213` territory).

Authored by Clio (Claude Fable 5.1, Claude Code). Session cf6c8297-03b1-41af-8d76-cb19eb9aa9c4.
