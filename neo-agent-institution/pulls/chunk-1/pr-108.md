---
number: 108
title: >-
  fix(agentos): the scope control contains its label, the sub-narrow card wraps
  its verbs, the wordmark reads in both skins (#107)
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-04T17:52:01Z'
updatedAt: '2026-09-04T22:14:25Z'
closedAt: '2026-09-04T22:14:18Z'
mergedAt: '2026-09-04T22:14:18Z'
head: agent/107-shell-title-state-word
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/108'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #107

At the 314 px vessel the instance scope control now contains its own label (the 150 px label sits whole inside a 200 px box; it spilled 11 px over the wordmark and past the theme switch before), the wordmark yields to the scope at that band (the logo is the mark, the window title carries "Agent OS"), the roster's sub-narrow card wraps its verbs under the identity so the state word stays whole (identity column 53 → 129 px; "unobse" was a word cut by the verbs), and the wordmark binds `--fm-ink` — 13.5:1 on the dark band, 12.8:1 on the light one, where it read 1.04:1. Ledger items 1–3 of the 2026-09-01 design pass on #10; item 4 closed with PR #93.

Evidence: L3 (rendered locally on this Mac under the visual and e2e configs; computed receipts in the 314 arm and a both-skins contrast arm) → L3 required (AC-1…AC-5 are rendered-platform receipts; CI never runs the visual config). Residual: none.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | `FleetCockpitVisual.spec.mjs` 314 arm, computed: `labelInside` true, `labelOverTheme` 0, `titleWidth` 0. Probe before → after: label box x 115–265 inside a 126–254 button → x 75–225 inside 50–250 (150/150 px, no ellipsis). |
| AC-2 | same arm, per card: `wordPastLine ≤ 0` and `wordUnderVerbs` 0 (an intersection AREA — the verbs now sit on their own row); identity floor kept, 129 px (was 53). Probe: word right 201 vs line right 254 (was 201 vs 178, 15 px under the verbs). |
| AC-3 | new arm *the shell wordmark reads on its band in both skins*: dark 13.53:1, light 12.80:1 (before: 16.56 / **1.04**). |
| AC-4 | the decision and the before/after read of `cockpit-vessel-314.png` below; golden re-captured under the visual config, read by eye. |
| AC-5 | `AgentCardSynthesisRenderNL` both skins × six widths: 1/1 green, all twelve goldens byte-identical (the snapshot directory is clean after the run). The first run had the state-line wrap in every band and clipped a 294 px card by 19 px — the scoping in *Deltas* is that receipt. |
| AC-6 | unit **762/762** · visual **8/8** · components **3/3** · `check-visual-baselines` green on the head (stamp regenerated over the staged blobs — SCSS, the spec and the golden are inputs). |

## Deltas from ticket
- The state-line wrap is scoped to the sub-narrow band (≤240 px content), not every band: applied to the narrow band too, the `AgentCardSynthesisRenderNL` fixture clipped a 294 px card by 19 px — its card wall is height-constrained, and a second row overflows it. The narrow band was out of scope on the ticket; its goldens stay byte-identical. Recorded on #107's body.
- The verbs row rides `width: 100%` instead of a flex basis: the engine container writes `flex: 0 0 auto` inline, which outranks any stylesheet basis; a basis of `auto` follows the width.
- The vessel band's logo gutter drops from 15 px to the 8 px step (`--fm-space-2`): without it the scope label lost its last character (147/150 px).

## Design read — `cockpit-vessel-314.png`
Before: the pill's text ran over the "OS" of the wordmark and past the theme switch; the card's state line read "unobse". After: logo · pill (whole label, caret) · theme switch, one quiet row; the card keeps avatar + name + "◇" on the name line, "● unobserved" whole on the state line, and the stop/restart verbs on their own right-aligned row under the identity — one row taller, nothing cut. Every other surface of the frame is pixel-identical to the previous golden (the fleet head, the sort/filter rows, the activity list).

## Test Evidence
- Visual config (this Mac): the full suite 8/8 with only `cockpit-vessel-314.png` re-captured — the 1280 shell, grid, stream, 720, Review-1280 and Accounts goldens unchanged. One earlier full run, overlapped with the component suite, rendered the 314 frame in the fallback font (the webfont never arrived) and diffed the whole frame; alone on an idle machine it is green twice.
- e2e config, `AgentCardSynthesisRenderNL` (Neural Link fixture, both skins × 294/319/320/328/360/720): 1/1 green, goldens byte-identical.
- Probe receipts (untracked Playwright script, 314×900 and 1280×720, both skins) are the numbers quoted in the AC rows; the 1280 control is unchanged before/after apart from the wordmark's ink.

## Post-Merge Validation
- [ ] None — every receipt is a local render; nothing in this diff waits for a deployment.

## Commits
- `e06fcc3` — the three sheets + the label class + the two witnesses + the re-captured golden + the stamp (rebased onto `dev@0b0b33a` after PR #106).

Authored by Clio (Claude Fable 5.1, Claude Code). Session 46962d8b-08f3-49a3-8049-d74e2052af37.


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-04T22:10:56Z

# PR Micro-Review

**Class:** contained — three scoped presentation repairs, one view-class marker, and their visual witnesses; no new abstraction, state vocabulary, or control behavior.

**Verdict:** Approved

**Glance:** Reviewed `e06fcc34c46939ef472f13649e08f1de26834ffc` against #107, the existing shell/card width rules and TOKENS.md. The fix supplies the missing flex shrink permission, wraps controls only in the ≤240px card-content band, and binds the wordmark to `--fm-ink`; no site outside the prescribed surfaces changed. I compared the exact base/head 314px goldens: the scope label now clears the neighboring controls, and the state word remains whole above the relocated verbs. The synthesis snapshot directory has the identical Git tree at base and head (all twelve images), and the exact-head `checkVisualBaselines.mjs` passes. Token recomputation confirms 13.53:1 dark and 12.80:1 light, matching the author's computed browser receipts. Current-head CI is green; the author's 8/8 visual and synthesis receipts remain explicitly local, not attributed to CI. I looked for hidden state text, lost accessible scope labels, and leakage into the 240–289px card band: none was introduced. The switcher's full aria-label/title and the existing 44px narrow-band controls remain intact.

**Findings:** None. No required actions — eligible for human merge.

- **Origin Session ID:** 69fd9548-2fbe-45ce-892f-56767d752c5f

🖖 Emmy, GPT-6 Astra, Codex. 🪡

---

