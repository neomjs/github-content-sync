---
number: 51
title: >-
  refactor(agentos): extract the liveness read class — activity, roster, brain
  health (#50)
author: neo-fable-clio
state: CLOSED
createdAt: '2026-08-29T13:00:08Z'
updatedAt: '2026-08-29T16:04:18Z'
closedAt: '2026-08-29T16:04:16Z'
mergedAt: null
head: agent/50-liveness-reads
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/51'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #50
Related: #22 (parent epic) · #48 / PR #49 (whose util shape this PR RETIRES) · #42 (the state.Provider follow-up)

**Direction change, operator architecture ruling (recorded in #50):** *passing `owner` into a util file and triggering logic on it is a functional mixin, not a responsibility move — it keeps dodging view controllers.* This PR is the correction: **both cockpit read families now live on `FleetCockpitController`** — the named, lifecycle-bound home for view logic with first-class `this.component` access — and `AgentOS.util.CockpitSourceReads` is **deleted** (the util shape lasted one review cycle; it is not public history worth preserving, so the PR is one clean commit).

Evidence: L2 (full agentos unit tree **735 passed / 0 failed** — the pre-cut set lands identically through the migrated addresses plus the law witnesses; component battery 3/3 consecutive; visual 6/6 untouched goldens) → L2 required. Residual: none.

## What

**On the controller now:**
- the #48 fenced snapshot template (`readFencedSource` + `SOURCE_READS` — six pane feeds with descriptor-hook variance),
- the liveness class as NAMED methods (`loadActivity` · `loadRoster` · `loadBrainHealth` — structural variance: roster's sample-seed authority ladder, activity's four-way capability routing; deliberately not templated),
- the loss edges (`degradeWiredSurface` / `clearDegradedReason`) and identity pair (`loadOperatorIdentity` / `deriveOperatorIdentityPosture`),
- the file-local stateless helpers (`boundedRead`, `toSafeDegradedReason`).

**Contracts preserved:** every collaborator call is owner-virtual through `this.component` (the #49 RA-1 law — overrides keep seating, sentinel-witnessed); fences, typed fallbacks, WRITE-time pane resolution, in-flight accounting and the liveness laws move verbatim. **View state stays view-held in this cut** — the controller reads/writes through `this.component`; migrating projected state onto a `state.Provider` is the declared #42 follow-up, not a silent scope grab.

**The view keeps thin delegates** with unchanged public signatures for its existing callers (boot, liveness timer, vessel paths). **Specs migrated by address, not by meaning:** the prototype fakes now drive `FleetCockpitController.prototype` with `{component: fake}` — every fake shape and every assertion untouched; the law spec drives controller verbs directly.

## AC Evidence

- Both families controller-homed; util deleted; delegates unchanged — [L1] the diff; [L2] the full suite through the migrated addresses.
- Exact numbers at head (the #49 RA-2 law): `Container.mjs` **3,339 → 2,981 (−358)**; `Controller.mjs` 350 → 1,186; the dev-side util (+414 via PR #49) removed. **Epic total: FleetCockpit 3,640 → 2,981 across two cuts.**
- Identical pass set + witnesses — [L2] 735/0.
- Virtual-dispatch sentinels — [L2] the law spec's owner-override witnesses (roster fan-out seams, posture, degrade-in-catch).
- AC-7 — [L2] visual 6/6, stamp per commit.

## Deltas

One declared: the ticket's original shape (named statics on the util) was superseded mid-lane by the operator's ruling above; #50's body records the direction change and this PR implements it — including retiring the just-merged #49 util rather than stacking a second wrapper on it.

## Post-Merge Validation

None deferred. Next #22 cuts: the write verbs, vessel/tear-out, chrome-sync, the file-size guard; #42 owns the provider-state question this PR deliberately left in place.

Authored by Clio (Fable 5, Claude Code). Session 41859592-b7ee-4bce-bee3-f25644d9003b.


## Comments

### `@tobiu` commented on 2026-08-29T16:04:15Z

Closed per operator architecture review (rated 20/100 — the verdict and the violated-idiom checklist are recorded in #50): moving code between files is not decomposition. The rebuild lands as a fresh PR on the corrected foundation — declarative Container (static items with lazy module imports, own reactive configs, no anonymous imports, no additionalThemeFiles) + a Neo-pattern Controller (class = file name, logic directly in methods, no Impl/wrapper pairs, module consts camelCase and minimal) + a dedicated cockpit StateProvider class for the MULTI-consumer values (adapter states, degraded reasons — the operator's 'partial provider' ruling; per-pane snapshots stay controller state with direct pane writes). 1k is the hard limit and the rebuild targets it, not approaches it.

— Clio (Fable 5, Claude Code). Session 41859592-b7ee-4bce-bee3-f25644d9003b.

---

