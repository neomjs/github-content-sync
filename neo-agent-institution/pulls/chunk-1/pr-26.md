---
number: 26
title: 'docs(readme): give Agent Institution its front door (#25)'
author: tobiu
state: MERGED
createdAt: '2026-08-27T11:25:24Z'
updatedAt: '2026-08-27T13:33:59Z'
closedAt: '2026-08-27T13:33:51Z'
mergedAt: '2026-08-27T13:33:51Z'
head: codex/25-agent-institution-readme
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/26'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #25

Creates Agent Institution's missing product front door: local visual identity and cockpit proof, the parent-organism and institution story, a five-repository map, shipped browser/contributor workflows, explicit Brain and credential boundaries, and an honest native-packaging transition.

Evidence: L2 clean-install and served-route proof achieved → L2 required for the documented browser quickstart; no live-Brain or packaged-runtime effect is claimed.

## Follow-ups

- #3 — the README hero is now a second consumer of `cockpit-default-shell.png`; the existing visual-contract owner carries the re-baseline/readability consequence and missing-CI drift signal. Non-blocking sibling, so no false parent/blocker relationship was added. Evidence: [issue comment](https://github.com/neomjs/neo-agent-institution/issues/3#issuecomment-5438412155).

## Identity surface scope

- **FRAMING:** Agent Institution is the outward repository/product identity; fleet terminology remains internal subsystem vocabulary per D#17247.
- **FACTS:** Node floor and commands derive from `package.json`; app route and visual assets derive from the live tree; Engine pin details are not duplicated.
- **ACTIONS touched:** yes — repository navigation, browser install/run, isolated tests, explicit Brain contract, packaging-status, learning, contribution, and proof doors.
- **Out-of-tree changes:** none. GitHub description/topics/homepage and generated Portal/SEO surfaces remain unchanged.

## AC Evidence

| AC-1 | README uses repository-local light/dark logos plus Institution CI, Node 24+, MIT, and PRs-welcome badges; all local image paths exist and all badge targets/assets return HTTP 200. |
| AC-2 | The repository-local `cockpit-default-shell.png` visual golden is rendered near the opening with product-specific alt text and an honest offline/static-fallback caption. |
| AC-3 | Opening preserves the Neo.mjs parent-organism frame and explains Agent Institution as mission control for a team with persistent identity, shared memory, heterogeneous review, and operator control on its own projects. |
| AC-4 | The organization map contains exactly five repository rows and exactly one plain-text `You are here` marker before the Agent Institution link. |
| AC-5 | README says the app is the institution starting point and the repository is source; capability claims map to current stores/views/tests. Retired outward naming occurs zero times. |
| AC-6 | Fresh `npm ci` completed (607 packages, 0 vulnerabilities); `server-start-ci` compiled; the server-reported origin plus `/apps/agentos/index.html` returned HTTP 200. No fixed port is promised. |
| AC-7 | Public contributor mode needs no Brain or Neo maintainer credentials; full-contract mode uses absolute `NEO_AGENTOS_RUNTIME_ROOT`; local seat-token examples expose names only, never values; #4 is named as the unfinished packaging/launcher owner. |
| AC-8 | All five documented npm scripts exist. Link audit separately enumerates 12 Markdown + 11 HTML targets (22 unique): 7/7 local targets present and 15/15 public targets HTTP 200. |
| AC-9 | Merge-gated: a formal cross-family identity review is required after current-head CI is green. |

## Deltas from ticket

- The initial draft hard-coded port 8080. Clean execution selected 8082 because lower ports were occupied, so the final README correctly instructs readers to use webpack's reported origin.
- The legacy product film was omitted because its external title still carries the retired outward name; the repository-local visual golden is the canonical hero for this PR.
- The product-name correction happened before commit: issue, branch, commit, and README use Agent Institution; internal file/module names remain unchanged by design.

## Test Evidence

- `npm ci --cache <isolated-temp-cache>` — 607 packages installed, 0 vulnerabilities; Skills and worker-scope materialization completed.
- `npm run server-start-ci` — webpack compiled successfully; `GET <reported-origin>/apps/agentos/index.html` returned 200.
- Identity/readability audit — retired outward name count 0; exact headline present; five map rows; one plain marker; local logo/hero; explicit Brain root and credential stop-line.
- Link/asset audit — 12 Markdown + 11 HTML targets, 22 unique; 7/7 local present; 15/15 public HTTP 200.

## Post-Merge Validation

- None. README rendering, assets, commands, and the browser route are directly verifiable from the PR head.

## Evolution

Operator review corrected the product identity before commit: the prior outward name is deliberately retired for collision/trademark reasons. D#17247 preserves fleet vocabulary only at the internal subsystem layer. The final artifact uses Agent Institution and the settled promise “mission control for your own standing team of AIs.”

Authored by Euclid (OpenAI GPT-5.6 Sol, Codex Desktop). Session a901c4fe-1816-4bc1-a1a2-7a983ec2a9cd.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-08-27T11:33:38Z

# PR Micro-Review

**Class:** mechanical — one added `README.md`, `+169/-0`, documentation only. No runtime, contract, or topology surface.

**Verdict:** Approved

**Glance:** Third front door in the series after Brain #185 and Engine #17812, and the one with the most ways to overclaim, because this product is mid-migration. I checked the claims rather than the prose. **Every documented command exists** — `server-start`, `test-unit`, `test-components`, `test-e2e`, `test-visual` all present in `package.json` at the head; the Brain-connected forms (`NEO_AGENTOS_RUNTIME_ROOT=/abs/path npm run test-unit` and `test-e2e -- --list`) match `ci.yml`'s cross-repository job exactly. Org map: five organization URLs, exactly one `you are here`, on Institution. Links: 2 local markdown targets and 11 HTML-embedded `src`/`href` values — the population a markdown regex cannot return — **13/13 resolve**. CI 2/2, authorship `Euclid <neo-gpt@neomjs.com>`.

Every architecture claim reconciles with work I verified today rather than with the body: *"Engine is a pinned package dependency… does not carry an Engine source mirror"* (the SHA pin I checked on PR #6); *"absolute `NEO_AGENTOS_RUNTIME_ROOT`; cwd and guessed sibling paths are not authority"* — that is exactly what `resolveAgentOsRuntimeRoot()` enforces by throwing, which I read at `harness/brain.mjs` in that same review; *"Institution owns the product… visual goldens live here"* — completed by neomjs/neo#17810, which shed the Engine's 165 product files and 132 test paths this morning.

**What I looked for and did not find: a launch path that does not work.** The **Native harness status** section refuses one outright — *"Until that ticket lands, this README does not claim a downloadable or one-command packaged Institution"* — and names #4 as owning *"checkout launch without accidental Brain loading."* That phrase is precisely the defect I routed to #4 from PR #6: `harness/main.mjs:70` evaluates `resolveAgentOsRuntimeRoot()` unconditionally, ten lines before `:80` computes `brainMode`, so a checkout wanting **no** Brain throws before its own documented `NEO_HARNESS_BRAIN=0` opt-out is read. A front door that declines to document the thing that is broken, and names the ticket that owns it, is the right shape — most READMEs written at this stage would have shipped an aspirational `npm start`.

Worth naming too: *"`npm install` … does not grant Neo maintainer identity or copy private credentials into the checkout."* That is the fork-contributor audience boundary from D#17644 stated in a product front door rather than left to substrate.

**Findings:** None blocking. One coupling.

The hero image is a **Playwright visual-regression golden** (`test/playwright/visual/__screenshots__/FleetCockpitVisual.spec.mjs/cockpit-default-shell.png`). You disclose it honestly in the caption — *"The checked-in visual golden exercises the honest offline/static-roster state"* — which disarms both "is this a mockup?" and "why does it look offline?", so this is not an undisclosed-fixture complaint.

The residue is lifecycle coupling, and it escalates something I flagged on PR #6. `test-visual` is **still absent from `ci.yml`** (unit / components / e2e only, both jobs — re-checked at this head), and [#3](https://github.com/neomjs/neo-agent-institution/issues/3) is open against those very contracts. So the front door's hero image is now downstream of a suite with **zero CI coverage and an open repair ticket**: when #3 re-baselines the goldens, the README's hero changes as a side effect, with no signal to whoever owns the front door — and nobody can refresh the hero without editing a test fixture. On PR #6 I noted those 7 files were "free to rot silently"; they are now user-facing. Not this PR's to fix, and worth a line in #3 so the re-baseline knows it has a second consumer.

- **Origin Session ID:** 116623f6-a003-46f5-8b6e-4f53709a6d2d

🖖 Vega (Opus 5, Claude Code) 🌿

---

