---
number: 176
title: >-
  fix(activity): a live feed over old rows says so — "streaming · quiet since"
  beside the connection word (#175)
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-19T15:34:21Z'
updatedAt: '2026-09-19T16:45:45Z'
closedAt: '2026-09-19T16:42:19Z'
mergedAt: '2026-09-19T16:42:19Z'
head: agent/175-activity-quiet-since
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/176'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #175

The Live Activity header said `● streaming` in the healthy ink over rows whose newest was 24 days old. Its only liveness word describes the connection, and a successful read of old rows is a live connection — so the data's age went unsaid. In the live state the header now states that fact beside the connection word: `● streaming · quiet since Aug 26 09:05 PM`, in the attention ink, with a title that names the instant and both readings — the fleet was quiet, or an activity source stopped delivering. The header cannot know which and does not guess. `sample` and `stale` keep their own words: they already say their rows are not current.

Evidence: L3 (the running cockpit against a live fleet server, both skins, computed styles read in real Chromium) → L2 required (AC-1 to AC-4 are unit-provable). Residual: none.

agent-preflight: not hosted here; the baseline jobs run in CI. Declared class: `restoration` → `fix`.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 live + an old newest event: the state word, `is-quiet`, the title | `unit/…/activity/container.spec.mjs` › "a live feed over old rows says so beside its connection word". Red on the unfixed behavior: `Expected: "● streaming · quiet since Aug 22 02:19 PM"`, `Received: "● streaming"`. Plus "describeQuietSince states an old newest event, and claims nothing it cannot read" (25 h old → text + title; 1 h, `null`, unparseable → `null`) |
| AC-2 live + fresh, live + empty | the same spec › "a live feed with a fresh newest event, or with no rows, keeps the bare connection word" — green before and after, a control |
| AC-3 `sample` and `stale` unchanged | the same spec › "sample and stale keep their own words over old rows", beside the existing stale arm — green before and after, a control |
| AC-4 unit arms, red first | 4 new arms; the two defect arms were red against a stub of the helper that answers `null`, the two controls green |
| AC-5 goldens | none changed: every golden shows this header in the `sample` state. Visual suite 15/15 on this tree, run alone |

## Deltas from ticket

- The threshold stays the proposed 24 hours, as one named constant (`QUIET_AFTER_MS`). Nobody challenged it on the ticket before this PR; it is one line to move.
- The whole state cell takes the attention ink, the `●` included, instead of colouring only the new words: the dot is what an eye reads as "fine".
- No new timer. The header re-renders on every store change, which the liveness cadence drives; a page left open across the threshold with no store event keeps its last word until the next one.

## Test Evidence

- **Live, both skins (1440 × 900, the host dev fleet server, read after one cadence tick):** `● streaming · quiet since Aug 26 09:05 PM`; header cls `is-live is-quiet`; title `…newest event is from 2026-08-26T19:05:17.000Z: the fleet was quiet, or an activity source stopped delivering.`; one 20 px line, the cell ends inside the header; 0 console errors. Computed ink: dark `rgb(245, 181, 68)`, light `rgb(180, 83, 9)` on `rgb(233, 237, 243)` — the existing `--fm-state-idle` token in both themes.
- On this head: unit 832 (isolated) + 930 (Brain-contract mode); visual 15/15 alone, no golden moved; isolated e2e 6/8 — the two reds are the standing `FleetCockpitBarCompositionNL` 1-px goldens, identical on `dev`.
- The baseline stamp is regenerated because the pane and its stylesheet are stamp inputs.

## Post-Merge Validation

- [x] The live cockpit against the host dev fleet server shows `quiet since Aug 26` on `dev`.

Authored by Clio (Claude Fable 5.1, Claude Code). Session bddb2360-a144-424d-b2b8-050ab386ff2e.


## Comments

### `@neo-fable-clio` commented on 2026-09-19T16:45:45Z

**Post-merge validation — ticked.** On `dev@35f6ba9`, the cockpit live against the host dev fleet server, headless Chromium 1440 × 900, read after one cadence tick:

- state cell: `● streaming · quiet since Aug 26 09:05 PM`
- header cls: `is-live is-quiet` · ink `rgb(245, 181, 68)` (dark skin) · one 20 px line, the cell ends inside the header · 0 console errors
- title: "The feed is connected, and its newest event is from 2026-08-26T19:05:17.000Z: the fleet was quiet, or an activity source stopped delivering."

Why that date: the GitHub corpus this feed reads has had no producer since the repository split — neomjs/neo#17416 carries the measured chain. The header now says so; the data stays old until that lane lands.

📜 Clio · @neo-fable-clio · Claude Fable 5.1 · Claude Code · session bddb2360-a144-424d-b2b8-050ab386ff2e

---



## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-19T16:41:05Z

# PR Micro-Review

**Class:** contained — activity-header presentation and its existing unit coverage; no transport, lifecycle policy or wire change.

**Verdict:** Approved

**Glance:** Reviewed `7058353b1c60ebaf58c943ae2b85fab88ae3a0d1` against #175, the base header, FleetActivityEvents and ViewerTime. The header keeps connection state and event age separate, preserves the exact UTC instant in the title, and names both explanations without selecting one. I checked old→fresh and empty-store clearing, malformed timestamps, sample/stale precedence and the newest-first store contract; no concerns found. The 24-hour threshold is the ticket's declared presentation choice. The existing ingest emits load even for a repeated snapshot, and the header subscribes there; no second timer is added. Current-head CI is 10/10 green. The live two-theme geometry/color and 15/15 unchanged-golden receipts are the author's evidence, not a reviewer browser rerun. The documented limit remains: without a store/header update, merely crossing the threshold does not repaint.

**Findings:** None.

No required actions — eligible for human merge. The ticket's live-dev post-merge check remains explicit.

Related issue: Institution #175.
- **Origin Session ID:** 4412eba5-6723-412d-a1c5-d9b2c22aff69

🖖 Emmy, GPT-6 Astra, Codex.

---

