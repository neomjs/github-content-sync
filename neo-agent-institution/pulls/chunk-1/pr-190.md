---
number: 190
title: >-
  ci: call the shared PR baseline at its published release v0.1.17, not a commit
  SHA (#189)
author: neo-opus-grace
state: OPEN
createdAt: '2026-09-24T20:20:29Z'
updatedAt: '2026-09-24T20:29:31Z'
closedAt: null
mergedAt: null
head: grace/189-baseline-at-release-tag
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/190'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #189

The shared PR baseline is now called at its published release, not a commit SHA. The operator's rule, 2026-09-24: *"we want npm versions. published skill releases. NEVER EVER SHA values."*
- **The change:** `.github/workflows/shared-pr-baseline.yml:33` goes from `…/reusable-pr-baseline.yml@6b009521fac6…` to `…@v0.1.17`. The SHA was Skills **0.1.6** (2026-09-15), so this caller also gains every baseline job added since then; this PR's own run is that check.
- **Why the tag is safe:** `v0.1.17` is the first tag on `neo-agent-skills`, set at npm 0.1.17's `gitHead` (`f7b29e7`).
- **From here:** dependabot's `github-actions` ecosystem, already enabled daily in this repository, moves the tag on each release.
- **The comment above the line** defended SHA pinning, so it now describes the tag contract.

Evidence: L3 (this PR's own `PR baseline` run is the first caller run at a release tag) → L3 required (AC-2). Residual: none for this ticket; the dependabot read stays with neomjs/neo-agent-skills#114.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | `shared-pr-baseline.yml:33` reads `@v0.1.17`. `git grep -E "neo-agent-skills[^ ]*@[0-9a-f]{40}" -- .github/` returns nothing. |
| AC-2 | This PR's `PR baseline` checks run through the reusable workflow at the tag (CI below). |

## Deltas from ticket
- None.

## Test Evidence
- `git ls-remote https://github.com/neomjs/neo-agent-skills refs/tags/v0.1.17` peels to `f7b29e7`, which is npm's `gitHead` for 0.1.17.

## Post-Merge Validation
- [ ] The next `neo-agent-skills` release (0.1.18, neomjs/neo-agent-skills#115) produces a dependabot PR that moves `@v0.1.17`. That is neomjs/neo-agent-skills#114 AC-4.

## Commits
- `616d872`: the call moves to `@v0.1.17`, and its comment describes the tag contract.

Authored by Grace (Claude Opus 5.5, Claude Code). Session 1f7129c9-c0f7-42e0-ba47-7a42e5ac57c2.
