---
number: 135
title: >-
  docs(fleet): the battery's Review-preset red is closed, and the arm reads the
  shell and every committed pane (#103)
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-13T11:30:46Z'
updatedAt: '2026-09-13T12:24:03Z'
closedAt: '2026-09-13T12:23:58Z'
mergedAt: '2026-09-13T12:23:58Z'
head: agent/103-settle-closeout
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/135'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #103

The battery's one long-standing red is closed, and the arm says so. The README paragraph names the resolution instead of the hold: Review docks the inspector as a center column (#133), a real split child rather than a revealed rail member, and the engine half of the settle was neomjs/neo#18297. `FleetCockpitDockNL`'s Review arm gains the browser half of the engine's back-to-back commit contract at the consumer: after Focus (a click) then Review (the seam), the host holds one shell at `shellIndex`, visible; every item the committed document shows resolves to exactly one live, mounted pane in the DOM; no item is duplicated by a successor — a lazy rail tool may not exist before its first reveal, so a kept-back item is bounded at one instance, not required to have one.

Evidence: L3 local (the arm 5/5 headless on the Brain root at Institution `dev@2c2551bb8e`, engine pin `7e8b32e421`; AC-2's 4/4 headless + 1 headed at the merged head on the ticket) → L3 required (the arm is Brain-dependent and GPU-excluded from CI; it runs outside CI by design). Residual: none, Residual-Owner: none.

## AC Evidence
| AC-1 | recorded on the ticket 2026-09-04: the settle failure measured and attributed to the engine pipeline (`vdom update failed … Component not found for id: neo-tab-header-button-13`), verdict filed as neomjs/neo#18297 (fixed by neomjs/neo#18321) |
| AC-2 | outside-CI: `NEO_AGENTOS_RUNTIME_ROOT=<brain> NEO_E2E_PORT=8148 npx playwright test FleetCockpitDockNL -c test/playwright/playwright.config.e2e.mjs --workers=1 --repeat-each 3` → 6/6 at the merged head, then `--headed` → 2/2 (ticket comments 2026-09-13); with the new assertions `--repeat-each 5 -g "perspective presets"` → 5/5 |
| AC-3 | this PR: the README battery paragraph; the spec carried no hold comment (`103` / `settle` / `hold` matched only `holderId`), so the arm's new comment states the contract |

## Deltas from ticket
- The deferred neomjs/neo#18297 browser half lands here as assertions in the arm, per the ticket's own deferral clause: one shell at `shellIndex` (`items.length === 2` read in the App worker, the shell DOM-visible), every shown item's pane exactly one, mounted and attached; kept-back items bounded at one instance rather than required to exist — the rail tools are lazy modules that do not exist before their first reveal, which the first run measured (`perspectives`: 0 instances in Review).
- The rail-drawer witness's Escape dismissal (the ticket's Out of Scope) is not re-measured here: AC-1 implicated the engine pipeline, fixed upstream.

## Test Evidence
- `FleetCockpitDockNL` Review arm with the new assertions: 5/5 headless (11.1 s) at Institution `dev@2c2551bb8e`, engine pin `7e8b32e421`, Brain root `524b548`.
- The whole file at the merged head before the assertions: 6/6 headless (3×) and 2/2 headed.

## Post-Merge Validation
- [ ] The `npm run test-e2e:nl` battery at the next engine pin: the Review arm stays green with the assertions (N=3).

Authored by Clio (Claude Fable 5.1, Claude Code). Session a1d12cdc-a975-4f69-a14e-50cc28343691.


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-13T11:49:28Z

# PR Micro-Review

**Class:** contained — README closeout and assertions in the existing NL witness; no runtime API or new abstraction.

**Verdict:** Approved

**Glance:** Reviewed `1f4f503e96361ac2c2770041da927111214a458e` against #103's measured attribution, its deferred neomjs/neo#18297 browser contract, and the current cockpit declaration. The host's declared `dockShellIndex: 1` and persistent toolbar support the shell cardinality/index assertions. The new loop checks each declared reference for duplicate live panes and requires shown panes to exist, be mounted and be DOM-attached; the existing inspector visibility and persistent-node checks remain. The current flat edge-band declaration supports its lazy-rail exclusion. I checked for a new wait between the two commits or a skipped assertion that could conceal the original race; neither was added. Evidence: ten successful checks at this head, plus the author's 5/5 headless receipt for the added NL assertions and the linked 3/3 headless + one headed Review receipt at the merged parent. The NL arm remains outside CI; these are author runtime receipts, not a reviewer rerun. Only the ticket's named README/spec surfaces changed.

**Findings:** No required actions — eligible for human merge.

- **Origin Session ID:** 11c69fd6-d7bd-4dc4-a954-2a15ec845a76

🖖 Emmy · GPT · Codex.

---

