---
number: 186
title: >-
  fix(viewport): the instance manager's intents resolve the manager behind the
  firer id the engine stamps onto source (#183)
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-23T10:11:47Z'
updatedAt: '2026-09-23T10:39:11Z'
closedAt: '2026-09-23T10:39:08Z'
mergedAt: '2026-09-23T10:39:08Z'
head: agent/183-manager-intent-source
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/186'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #183

The instance manager's five intents (`saveinstance`, `retireinstance`, `probeinstance`, `connectinstance`, `connecttenant`) reach the ViewportController with `source` as the firer's id: the engine's `Observable#fire` stamps `source = me.id` onto every object-format event, and the handlers wrote their notices and cleared the editor on that string. One resolver, `resolveIntentSource`, turns the id into the instance (an instance handed in directly passes through, as the existing plane-admission arm already does), and each handler resolves first. Add now clears the editor and lands its notice; Connect + switch lands the switch — the credentialed way back to a live instance, which #181's witness could not reach.

Evidence: L3 achieved (cockpit dev server on this branch against a host fleet server, headless Chromium: Add → editor empty, notice `added — fleet-profile:v1:…`; bearer-less switch to a dead endpoint; manager → A's row → the dev server's handshake bearer → Connect + switch → notice `connected — custody verified, ingress retired`, the switcher back on the live endpoint, `FLEET · 9 AGENTS`, no console error) → L3 required (AC-3). Residual: none.

agent-preflight: not hosted here; baseline unit 845/845 isolated, visual 15/15 alone, `check-app-file-sizes` 0 over the bar, `neo-agent-skills-ticket-archaeology` 0 violations ran.

## AC Evidence
| AC-1 | `ViewportController.spec.mjs` "instance manager intents — `source` arrives as the firer's id": five arms (Add, Connect + switch, Retire, Probe, Plane admission) — red on dev with the live TypeErrors (`Cannot create property 'notice' on string`, `source.onClearClick is not a function`), green here |
| AC-2 | the plane-admission arm drives both forms — the id and a direct instance — and both notices land; the existing instance-form arm unchanged |
| AC-3 | live receipt below: Add clears the editor; Connect + switch with the handshake bearer switches the bound instance |

## Deltas from ticket
- The ticket asked for one convention across the manager's intents; there are five handlers, not three — Retire and Probe carried the same defect and take the same resolver.
- No change on the manager side: it keeps firing `source: me`; the engine's stamp is by design, so the handlers own the resolution (the cockpit controller's `onAgentLifecycleIntent` precedent).

## Test Evidence
Live (2026-09-23, cockpit dev server on this branch, host fleet server on the corpus checkout, headless Chromium 1440×900, `errors: none`): `[after Add] editor="" notice="added — fleet-profile:v1:http://127.0.0.1:8096/fleet"` · bearer-less switch → `switcher="127.0.0.1:8096/fleet"` · handshake `200`, `result.bearerToken` · A's row selected, bearer pasted, Connect + switch → `notice="connected — custody verified, ingress retired"`, `switcher="127.0.0.1:8095/fleet"`, `FLEET · 9 AGENTS`. On dev the same run logs the two TypeErrors and the switch never happens.

## Post-Merge Validation
- [ ] With #181 merged, the same run shows the seed roster on the dead instance and A's live roster again after Connect + switch (closes #181's switch-back line).

Authored by Clio (Claude Fable 5.1, Claude Code). Session f34cbeb6-fd44-4060-b31f-e05332e62aee.


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-23T10:32:14Z

# PR Micro-Review

**Class:** contained — resolve the existing event-source contract in the five instance-manager handlers.

**Verdict:** Approved

**Glance:** At `2a7fc8748db93b3edc460c2f42152af18c128c72`, the resolver uses `Neo.getComponent` for string IDs and preserves direct instances. I checked the pinned engine's `Observable.fire` source stamp, the registry lookup and the existing cockpit handler precedent. All five affected handlers resolve before using the source; credential custody and instance-switch behavior are unchanged. The regression arms use a registered component and restore their global overrides. All 10 head checks pass; the author additionally records the live Add and credentialed switch-back witness. An independent read-only review agreed; I found no actionable defect or weakened assertion.

**Findings:** No required actions — eligible for human merge.

- **Origin Session ID:** ef03b71d-0375-4160-8fde-ad4d19616eff

Emmy · GPT-6 Astra · Codex

---

