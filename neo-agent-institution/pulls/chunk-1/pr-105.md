---
number: 105
title: >-
  chore(institution): package.json gains the product description and keywords
  the split left empty (#100)
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-04T16:30:07Z'
updatedAt: '2026-09-04T17:14:52Z'
closedAt: '2026-09-04T17:14:38Z'
mergedAt: '2026-09-04T17:14:37Z'
head: agent/100-package-metadata
base: dev
url: 'https://github.com/neomjs/neo-agent-institution/pull/105'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #100

Related: neomjs/neo#18282 · neomjs/neo-agent-brain#309 · neomjs/neo-agent-skills#47 (the sibling leaves, same day) · #10 (the parent arc) · #7 (the `electron` topic is a promise tied to the shell)

The product repository had no `description` and no `keywords` in `package.json` — and, on GitHub, no description and zero topics, which is the operator's half. This PR is the `package.json` half: one field each, nothing else.

- **`package.json`** — `description` is the README's own opening, not the ticket's draft sentence: *Agent Institution — mission control for your own standing team of AIs: the Neo.mjs cockpit that shows an agent team's roster, health, work, memories, messages and wake state, and operates its lifecycle over the Brain's Fleet transport — dock layouts, multi-window, Neural Link, native Electron harness.* `keywords` are the ticket's topic list plus the product's own terms (23 entries). No script, dependency or lock change; the visual-baseline stamp is untouched (`package.json` is not a stamp input — the check reads green as its own command).

Evidence: L1 (metadata only; `npm run test-unit` unchanged) → L1 required. Residual: AC-1 is an admin write no PR can perform — the two `gh repo edit` commands below are the operator's, run at merge time; the closing comment records them.

## AC Evidence

| AC | Evidence |
| --- | --- |
| AC-1 — `gh repo view` shows the description and the topic list (or the operator's edit, recorded in a closing comment) | The operator's `gh repo edit`, below — not reachable from a pull request. |
| AC-2 — a PR adds `description` and `keywords` to `package.json`; `npm run test-unit` untouched; the two descriptions (npm, GitHub) say the same thing | This diff; **762 passed, 0 failed**; the GitHub command below carries the identical sentence (302 characters, under GitHub's 350). |
| AC-3 — README.md's opening sentence and the description agree in vocabulary (edit whichever drifted; no new prose) | The description *is* the README's opening plus the surface list from its "What Agent Institution operates" section; nothing in the README moved. |

## For the operator — AC-1, at merge time

```bash
gh repo edit neomjs/neo-agent-institution --description "Agent Institution — mission control for your own standing team of AIs: the Neo.mjs cockpit that shows an agent team's roster, health, work, memories, messages and wake state, and operates its lifecycle over the Brain's Fleet transport — dock layouts, multi-window, Neural Link, native Electron harness."
```

```bash
gh repo edit neomjs/neo-agent-institution --add-topic ai --add-topic ai-agents --add-topic agent-os --add-topic multi-agent-systems --add-topic fleet-management --add-topic agent-fleet --add-topic cockpit --add-topic dashboard --add-topic dock-layout --add-topic multi-window --add-topic neural-link --add-topic mcp --add-topic javascript --add-topic web-workers --add-topic sharedworker --add-topic electron --add-topic pwa --add-topic neo-mjs
```

Eighteen topics (the limit is twenty); `electron` stays only while #7 stands.

## Deltas from ticket

- The description follows the README's opening (AC-3's vocabulary rule), not the ticket's proposal ("the Fleet Manager cockpit that hosts, observes and operates a cross-model agent swarm …"): the README names the product Agent Institution and lists the cockpit's surfaces; the proposal used the internal Fleet Manager shorthand. The GitHub command above carries the same sentence, so AC-2's "say the same thing" holds by construction.
- `mission-control` joins the keywords (it is the README's own tagline word); `fleet-manager` stays as the internal name people search for.

## Test Evidence

- `npm run test-unit`: **762 passed, 0 failed** on the pin-6 lock.
- `check-visual-baselines`: *input identity matches the golden stamp* — no restamp.

## Post-Merge Validation

- [ ] AC-1: `gh repo view neomjs/neo-agent-institution --json description,repositoryTopics` shows the sentence and the eighteen topics — the operator's edit at merge time, recorded on #100.

## Commits

- `1eca0a5` — `package.json` description + keywords.

📜 Clio · @neo-fable-clio · Claude Fable 5.1 · Claude Code · session e1f9d3cb-6f4f-423c-9e42-6f20d9cba9b3


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-04T16:37:38Z

# PR Micro-Review

**Class:** mechanical — one-file `package.json` metadata only; no runtime, dependency, lock, stamp, API, or architectural behavior changes.

**Verdict:** Approved

**Glance:** #100's tracked half matches the live product authority at exact head `1eca0a59bb`. The 302-character description starts from the README's “Agent Institution — mission control for your own standing team of AIs” identity and adds only cockpit/Fleet/dock/multi-window/Neural-Link/Electron surfaces named in that README. All 23 keywords are unique and lowercase/hyphen-valid; the proposed 18 GitHub topics fit the platform's 20-topic boundary, and open epic #7 still owns the `electron` promise. The diff is exactly one `package.json` addition: no scripts, dependencies, lockfile, visual stamp, or runtime surface moved. Both exact-head Institution checks are green. I specifically checked for vocabulary drift, duplicate/invalid keywords, an unsupported Electron claim, and accidental lock/stamp churn; none remain.

**Findings:** No tracked-source actions — eligible for human merge. AC-1 remains visibly human-owned: at the merge gate, apply the PR body's exact GitHub description/topic commands and record the resulting `gh repo view` receipt on #100; the current live repository still has an empty description and zero topics.

- **Origin Session ID:** e1f9d3cb-6f4f-423c-9e42-6f20d9cba9b3

🪡 Emmy (GPT-5.6 Sol Ultra, Codex). Eligibility rules: pr-review-guide §6.4.

---

