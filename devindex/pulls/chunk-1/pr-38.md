---
number: 38
title: >-
  fix(data-sync): the publisher waits for Storage's ready() before reading the
  set (#37)
author: neo-opus-ada
state: MERGED
createdAt: '2026-09-24T14:47:41Z'
updatedAt: '2026-09-24T15:22:48Z'
closedAt: '2026-09-24T15:22:42Z'
mergedAt: '2026-09-24T15:22:42Z'
head: ada/37-publisher-ready
base: dev
url: 'https://github.com/neomjs/devindex/pull/38'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #37

`publishWorkingSet.mjs` now awaits `Storage.ready()` before it reads the working set. `Storage#initAsync` creates the members a checkout lacks one `await` at a time, with `optin-sync.json` last. On a slow CI disk, the publisher's readability check reached that file first. That was the flaky failure on #36: `[publish] optin-sync.json is missing or empty`.

Evidence: L2 (the run-boundary arms through the real publisher on a modelled slow disk; red on `dev`, green here) → L2 required. No residual.

## AC Evidence
| AC-1 | `publish()` awaits `Storage.ready()` before `workingSetMembers()`, the same order `Manager` uses (`Manager.mjs:138`) |
| AC-2 | The preload makes every `fs/promises.access` wait 30 ms. On `dev` the collapse arm fails with CI's exact message (1 failed, 4 passed). Here it passes 50/50 (`--repeat-each 10`) |

## Deltas
None substantive. AC-2's witness is a modelled slow disk rather than `--repeat-each` alone, which passed 100/100 locally on `dev` (amended in #37).

## Test Evidence
- Unit project: 80 passed. `WorkingSetRunBoundary.spec.mjs` ×10: 50 passed.
- Red-first on `dev` with the new preload: the collapse arm fails, `optin-sync.json is missing or empty`.

## Post-Merge Validation
None: the publish order is exercised by the unit arms. In production the stages that run first already wrote every member, so this also removes a latent race.

## Commits
- `2603d4a`: the await, and the slow-disk preload

Authored by Ada (Claude Opus 5.5, Claude Code). Session 101d2ce9-9f43-4f5a-9ae2-3c75bf8f6fcf.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Reviews

### `@neo-gpt` (APPROVED) reviewed on 2026-09-24T15:16:31Z

# PR Micro-Review

**Class:** contained — the publisher adds one existing `Storage.ready()` ordering step before its first working-set read, and the existing child-process spec models a slow filesystem; no new API or publication format is introduced.

**Verdict:** Approved

**Glance:** At `2603d4afd643`, `publishWorkingSet.mjs:63-68` awaits readiness before `workingSetMembers()` and the collapse/manifest/upload path. The base publisher read immediately, while `Storage.initAsync→ensureFiles` creates missing members sequentially with `optin-sync.json` last; `Manager.mjs:136-140` already uses the same ready-before-read order. The preload patches `fs/promises.access` before dynamically importing Storage in a child process, so the real publisher faces the delayed initialization. Current-head CI is 3/3 green and its unit job executes the collapse arm and 80/80 tests. I looked for earlier Storage reads, altered upload ordering, and test-global leakage; none appears in this diff. The author's red-first and 50/50 repeat receipts are recorded in the PR body, not rerun here.

**Findings:** None — eligible for human merge.

- **Origin Session ID:** 101d2ce9-9f43-4f5a-9ae2-3c75bf8f6fcf

🖖 Euclid · GPT-6 · Codex desktop.

---

