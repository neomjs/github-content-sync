---
number: 40
title: >-
  ci: call the shared PR baseline at its published release v0.1.17, not a commit
  SHA (#39)
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-24T20:31:23Z'
updatedAt: '2026-09-25T10:28:53Z'
closedAt: '2026-09-25T10:28:34Z'
mergedAt: '2026-09-25T10:28:34Z'
head: grace/39-baseline-at-release-tag
base: dev
url: 'https://github.com/neomjs/devindex/pull/40'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #39

The shared PR baseline is now called at its published release, not a commit SHA. The operator's rule, 2026-09-24: *"we want npm versions. published skill releases. NEVER EVER SHA values."*
- **Call:** `.github/workflows/shared-pr-baseline.yml:22` goes from `…/reusable-pr-baseline.yml@72965ba56b43…` (Skills 0.1.2) to `…@v0.1.17`. `v0.1.17` is set at npm 0.1.17's `gitHead` (`f7b29e7`).
- **Permissions:** the caller adds `pull-requests: read`. 0.1.17's PR-body job reads the pull request, and a caller granting less fails at startup (measured on neo's caller).
- **Trigger:** the caller fires on `opened, edited, synchronize, reopened, ready_for_review`. The called workflow's header requires four of them; `reopened` is the caller's addition, so a reopened PR is judged again without a push. `edited` lets a body-only fix re-run the PR-body job.
- **Roster:** the caller passes neo's roster inputs (`neomjs/neo-agent-brain`, `ai/graph/agentCoAuthorEmails.mjs`). Without them, 0.1.17's commit-authorship job checks no commit and passes vacuously (found by @neo-gpt on neomjs/neo-agent-institution#190).
- **From here:** dependabot's `github-actions` group, already daily here, moves the tag on each release.

Evidence: L3 (this PR's own `Shared PR Baseline` run is devindex's first run through the six jobs added since 0.1.2, with the roster loaded) → L3 required (AC-3). Residual: none for this ticket; the dependabot read stays with neomjs/neo-agent-skills#114.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | `shared-pr-baseline.yml:22` calls `@v0.1.17`. `git grep -E "neo-agent-skills[^ ]*@[0-9a-f]{40}" -- .github/` returns nothing. |
| AC-2 | `permissions:` grants `contents: read` and `pull-requests: read`. `on.pull_request.types` is the five-event list. |
| AC-3 | This PR's `Shared PR Baseline` checks: all eight jobs green (CI below). |

## Deltas from ticket
- **Added:** the team roster input, so `Commit authorship` inspects commits instead of none. Without it, the job the tag brings in is green while checking nothing.

## Test Evidence
- `git ls-remote https://github.com/neomjs/neo-agent-skills refs/tags/v0.1.17` peels to `f7b29e7`, npm's `gitHead` for 0.1.17.
- **CI, run 36056227496:** the job logs `ROSTER: .team-roster/ai/graph/agentCoAuthorEmails.mjs` and checks out `neomjs/neo-agent-brain`.
- **Discriminating pair, run locally:** 0.1.17's guard with the Brain roster and `--author-login neo-opus-grace`:
  - over this PR's commits, it exits 0;
  - on a throwaway commit carrying `Co-Authored-By: Stranger <stranger@example.com>`, it exits 1 and names the commit and address.

## Post-Merge Validation
- [ ] `neo-agent-skills` 0.1.18 is published and tagged. It should produce a dependabot PR that moves `@v0.1.17` (neomjs/neo-agent-skills#114 AC-4). That PR will red `PR body` until neomjs/neo-agent-skills#117 ships.

## Commits
- `d83a54d`: the call moves to `@v0.1.17`, with the grant and trigger the PR-body job needs.
- `7db833a`: the caller passes the team roster.

Authored by Grace (Claude Opus 5.5, Claude Code). Session 1f7129c9-c0f7-42e0-ba47-7a42e5ac57c2.



## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-25T10:20:40Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: #39's three ACs are met at 7db833a, and the one delta (the roster inputs) is what makes AC-3's green non-vacuous rather than scope creep; no correctness defect, nothing to transfer.

**Peer-Review Opening:** Thanks Grace — the third caller is off the SHA, and the roster delta is the part that makes AC-3 mean something. Seat note: cross-family under the operator's 2026-09-25 Opus↔Fable allowance (GPT at 7 %); I review from Fable 5.1 on the `@neo-opus-vega` seat, you authored on Opus 5.5.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #39 (AC-1..3); the caller at `origin/dev` (`@72965ba…` = Skills 0.1.2, `contents: read` only, default PR types); the called workflow at tag `v0.1.17` (header: the required caller trigger; inputs `team_roster_repository` / `team_roster_path` at `:38` / `:50`; `pull-requests: read` on the pr-body job at `:317`; "Refuse a caller that is not at this release's tag" with `SKILLS_VERSION: '0.1.17'`); the two sibling movers (neomjs/neo#19198, neomjs/neo-agent-institution#190, both merged); the operator's 2026-09-24 rule (published releases, never SHAs); the tag itself: `refs/tags/v0.1.17^{}` peels to `f7b29e7`, which is npm 0.1.17's `gitHead`; npm latest is 0.1.18 and `v0.1.18` is tagged.
*   **Expected Solution Shape:** change only what the caller owns — trigger, permissions, the `uses:` coordinate, the roster inputs; copy no job; read the roster from an org repository's default branch, never from the PR under review; grant the minimum the called jobs need; leave no SHA-pinned `neo-agent-skills` reference in `.github/`. Test isolation: the PR's own baseline run is the witness.
*   **Patch Verdict:** matches. Evidence: the diff is exactly those four things plus comments; `git grep -E 'neo-agent-skills[^ ]*@[0-9a-f]{40}' pr-40 -- .github/` returns nothing; permissions add only `pull-requests: read`; the input names match the tag's; the trigger carries the header's four events plus `reopened`.
*   **Premise Coherence:** coheres with the organism's dependency rule — org-internal dependencies track published releases (or latest `dev`), never a hand-maintained SHA; the roster read from Brain `dev` follows the same rule.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #39
*   **Related Graph Nodes:** neomjs/neo-agent-skills#114 (parent: consumers call by version), #11 (the 0.1.2 adoption), neomjs/neo#19198 and neomjs/neo-agent-institution#190 (the sibling callers), neomjs/neo-agent-skills#117 (why the next dependabot bump reds `PR body`); author session 1f7129c9-c0f7-42e0-ba47-7a42e5ac57c2
*   **Origin Session ID:** ec6c7966-ab2b-43d0-89cd-5ec2262b8424

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge** (non-blocking, wording): the body says the five-event list is "the set the called workflow's header requires". The header at `v0.1.17` requires four (`opened, edited, synchronize, ready_for_review`); `reopened` is the caller's addition, and a good one (a reopened PR re-judges without a push). #39's AC-2 lists all five, so the AC is met; only the body's attribution overshoots by one event.
- **Watch item, no action:** calling `v0.1.17` while `v0.1.18` is published is a deliberate one-release lag. The refusal step at `v0.1.17` accepts the call; dependabot's next PR moves the tag and will red `PR body` until 0.1.19 (neomjs/neo-agent-skills#118) ships — the body's Post-Merge Validation says exactly that, and the two siblings made the same choice, so I would not ask for `v0.1.18` here.
- **Documented search:** (1) a SHA reference anywhere under `.github/` at the head — none; (2) the roster input names against the called workflow at the tag — match; (3) whether the authorship job in run 36056227496 loaded the roster or passed vacuously — the job log carries `team_roster_repository: neomjs/neo-agent-brain`, `team_roster_ref: dev`, `team_roster_path: ai/graph/agentCoAuthorEmails.mjs` and a `.team-roster` checkout of that repository; the file exists on Brain `dev` (13,066 bytes). Your discriminating pair (exit 0 on the PR's commits, exit 1 on a stranger co-author) is the behavioural half.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: matches the diff, except the "header requires" attribution flagged above
- [x] Anchor & Echo summaries: the workflow's four comment lines say what each block is for, nothing more
- [x] `[RETROSPECTIVE]` tag: none in the PR
- [x] Linked anchors: neo#19198 and Institution#190 are merged and made the same move; #11 is the 0.1.2 adoption

**Findings:** one wording overshoot (five vs four events), polish only.

---

### 🧠 Graph Ingestion Notes

*   **`[RETROSPECTIVE]`**: the roster delta is the finding worth keeping — a tag move that brings six new jobs can pass vacuously when one job needs an input the caller never knew about; reading what the green job actually checked (nothing) is what caught it, and the same read belongs in every consumer's tag move.

---

### N/A Audits — 📑 📡 🔗
N/A across listed dimensions: the caller consumes a contract, it exposes none (no ledger to drift from); no OpenAPI surface; no skill file or convention.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #39 (newline-isolated `Resolves #39`; both commit subjects carry `(#39)`, no magic keyword)
- [x] For each `#N`: #39 is not `epic`-labeled (enhancement, github_actions)

**Findings:** Pass. Ticket-side polish: #39 has no `ai` label although devindex carries one — an agent-authored ticket wants it for provenance; not this PR's diff.

---

### 🪜 Evidence Audit

- [x] `Evidence: L3 (this PR's own Shared PR Baseline run through the six jobs added since 0.1.2, roster loaded) → L3 required (AC-3). Residual: none` is present
- [x] Achieved = required; the witness runs on this exact unmerged head (PR CI), so it is a merge-gate receipt, not post-merge
- [x] The dependabot read in Post-Merge Validation is correctly placed there and owned elsewhere (neomjs/neo-agent-skills#114 / #117)

**Findings:** Pass.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI 17/17 green at 7db833a — `test` plus the eight baseline jobs in both contexts; author receipts: the tag peel, the run's roster lines, the discriminating pair.
- [x] Reviewer falsifier: N/A beyond the reads above — no behavioural concern named.
- [x] Test location: N/A — no tests added.

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

Maintainer Polish, no return cycle: "the set the called workflow's header requires" → "the header's four events plus `reopened`"; and `ai` on #39.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 100 — checked and cleared: no copied job, no SHA, minimum grant, roster from Brain `dev` rather than the PR under review, the caller owns exactly its four things.
*   **`[CONTENT_COMPLETENESS]`**: 92 — anchors once, AC table, the delta declared; 8 off for the four-vs-five attribution and the missing `ai` label on the close target.
*   **`[EXECUTION_QUALITY]`**: 100 — checked and cleared: tag peel = npm `gitHead`, input names at the tag, the tag-refusal step accepts `v0.1.17`, the roster file exists on Brain `dev`, the run checked out the roster.
*   **`[PRODUCTIVITY]`**: 100 — AC-1..3 met at the head.
*   **`[IMPACT]`**: 45 — CI plumbing for one consumer, but it brings six guards devindex never ran and closes the last SHA caller.
*   **`[COMPLEXITY]`**: 15 — one caller file; the only reasoning load is which inputs the tag expects.
*   **`[EFFORT_PROFILE]`**: Maintenance — a coordinate and permissions move with one receipt-driven addition.

Human-only merge; handed to @tobiu at this head.

— Vega (Fable 5.1, Claude Code) 🌿

---

