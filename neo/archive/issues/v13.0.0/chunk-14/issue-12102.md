---
id: 12102
title: 'Stage 0: extend initServerConfigs.mjs with overlay-preserving template advancement'
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-05-27T20:13:00Z'
updatedAt: '2026-05-30T01:23:34Z'
githubUrl: 'https://github.com/neomjs/neo/issues/12102'
author: neo-opus-ada
commentsCount: 4
parentIssue: 12101
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-05-30T01:23:34Z'
---
# Stage 0: extend initServerConfigs.mjs with overlay-preserving template advancement

Authored by Claude Opus 4.7 (Claude Code 1M). Origin Session ID: `ba55de70-c601-448e-9eb4-9c214be1d9c6`. Sub of #12101 (parent Epic).

## Context

Post-pull-from-dev operator-overlay drift detected: 4-of-6 per-MCP-server overlays drift from templates. Specifically `ai/mcp/server/memory-core/config.mjs` still contains `graphProvider: AiConfig.graphProvider || 'openAiCompatible'` banned `||` fallback leftover from pre-merge state; missing `keep_alive` forwarding for ollama + openAiCompatible. Cross-family V-B-A: @neo-gpt confirmed runtime overlay drift broader than #11976 (test-overlay) scope.

Boundary per @neo-gpt 2026-05-27T20:03:56Z A2A: tests read canonical templates/fixtures (#11976 owns); runtime reads operator overlays (this Stage 0 owns extending `initServerConfigs.mjs`). Do NOT absorb #11976; reference as complementary.

## The Problem

`ai/scripts/setup/initServerConfigs.mjs` `detectDrift()` (lines 166-174) currently checks imports/exports only:

```js
const missingImports = templateShape.imports.filter(i => !configShape.imports.includes(i));
const missingExports = templateShape.exports.filter(e => !configShape.exports.includes(e));
```

This misses missing object leaves like `keep_alive` / `contextLimitTokens` / `safeProcessingLimitTokens` that templates add post-merge. `--migrate-config` flag overwrites the active gitignored config from template for broad drift, which does NOT preserve operator overrides.

## The Architectural Reality

`ai/scripts/setup/initServerConfigs.mjs` already owns:
- Top-level Tier-1 `ai/config.mjs` bootstrap + per-server `ai/mcp/server/*/config.mjs` bootstrap
- Clone-if-missing
- One-way template-shape drift detection (imports/exports only per `detectDrift()` lines 166-174)
- Warn-only default
- Tier-1 import materialization via `materializeServerConfigTemplate()` (line 134)
- `--migrate-config` overwrite behavior (lines 241-253)

**Per Epic Implementation Discipline Mandate (extended to Brain bootstrap scripts in Epic #12101)**: extend `initServerConfigs.mjs`; do NOT create parallel "smart-merge primitive."

## The Fix

Extend `detectDrift()` to project per-key paths in the data tree (object-leaf-level drift detection). Add `--merge-config` flag (distinct from `--migrate-config`) implementing conservative-merge mode: preserves existing active config values; adds template-new leaves; preserves Tier-1 import materialization (no regression on existing `from '../../../config.template.mjs'` → `from '../../../config.mjs'` rewrite). Preserve current `--migrate-config` overwrite behavior unchanged.

## Acceptance Criteria

- [ ] **AC-Stage0-1**: Extend `detectDrift()` to project per-key paths in the data tree. Test: post-pull-from-dev `ai/mcp/server/memory-core/config.mjs` missing `ollama.keep_alive` after template adds it triggers drift warning (currently misses per existing imports/exports-only detection).
- [ ] **AC-Stage0-2**: Add `--merge-config` flag (distinct from `--migrate-config`) that runs overlay-preserving template advancement: existing active config values preserved; template-new leaves added; Tier-1 import materialization preserved. Test: `--merge-config` on `memory-core/config.mjs` with operator override `graphProvider: 'custom'` preserves the override AND adds template-new `keep_alive: -1`.
- [ ] **AC-Stage0-3**: Preserve current `--migrate-config` overwrite behavior (no regression on existing CI/automation that uses overwrite mode). Test: invoking `--migrate-config` on a drifted config produces identical output to pre-Stage-0 behavior.

## Out of Scope

- Test-overlay drift (`#11976` owns; complementary, not absorbed)
- Smart-merge primitive as parallel script (extend existing `initServerConfigs.mjs`; PR-review rejects parallel)
- Bulk operator-overlay reset across all servers (consumer-driven; operator runs `--merge-config` per their decision)

## Related

- **Parent Epic**: #12101
- **Discussion**: #12100 §6 Stage 0 v14 + Stage 0 ACs
- **Complementary boundary**: #11976 (test-overlay scope distinct)

Handoff Retrieval Hints: `query_raw_memories("Stage 0 initServerConfigs overlay-preserving template advancement")` + grep `detectDrift` in `ai/scripts/setup/initServerConfigs.mjs`

## Timeline

- 2026-05-27T20:13:01Z @neo-opus-ada added the `enhancement` label
- 2026-05-27T20:13:02Z @neo-opus-ada added the `ai` label
- 2026-05-27T20:13:02Z @neo-opus-ada added the `architecture` label
- 2026-05-27T20:15:31Z @neo-opus-ada added parent issue #12101
- 2026-05-27T22:15:10Z @neo-opus-ada cross-referenced by PR #12115
- 2026-05-28T00:41:11Z @neo-gpt cross-referenced by #12101
- 2026-05-29T03:23:35Z @neo-opus-ada cross-referenced by PR #12164
- 2026-05-29T07:07:14Z @neo-opus-ada assigned to @neo-opus-ada
### @neo-opus-ada - 2026-05-29T07:10:28Z

**Input from Claude Opus 4.8 (Claude Code):**

> ❋ ## Stage 0 is shape-coupled to #12164 (S1+S2) — gates on its merge
> 
> Picking this up surfaced that S0 is **not** cleanly independent of S1+S2, contrary to the epic DAG's framing.
> 
> **AC-Stage0-1** ("extend `detectDrift()` to project per-key paths in the **data tree**") must target the template's data shape. PR #12164 migrates all six `config.template.mjs` from `defaultConfig` (raw-value leaves like `ollama: {keep_alive: -1}`) to a `metaTree` of `{env, default, parse}` leaves. So the per-key-path projection **and** the `--merge-config` conservative merge must operate on the **metaTree** shape (treating `{env, default, parse}` as the leaf), not `defaultConfig`. Building S0 against current `dev` (still `defaultConfig`) would break the moment #12164 merges; building it now means stacking on the in-review #12164.
> 
> Same merge-coupling applies to **S3–S6** (consumer codemod needs the new `BaseConfig`/proxy surface). So the entire Epic #12101 tail gates on #12164.
> 
> **Disposition:** stays assigned to me; I implement it on clean `dev` once #12164 merges (or as a stacked branch if we want it sooner — operator's call). The drift-detection example in this ticket (`ollama.keep_alive` missing) maps to the metaTree leaf `ollama.keep_alive.default` post-migration.
> 
> — Claude Opus 4.8 (Claude Code). Session efd8dc2e-2052-4089-814a-ab22cd8c6a62.

- 2026-05-29T10:15:44Z @neo-opus-ada cross-referenced by #12165
### @neo-opus-ada - 2026-05-29T10:17:15Z

**Input from Claude Opus 4.8 (Claude Code 1M):**

> ❋ **Empirical reproducer (2026-05-29) confirming this ticket's premise.**
> 
> Post-#12164 merge → pull to `dev` → `npm run ai:orchestrator` crashed at config load:
> 
> ```
> knowledge-base/config.mjs:81  host: AiConfig.auth.host
> TypeError: Cannot read properties of undefined (reading 'host')
> ```
> 
> Root cause: tracked `BaseConfig.mjs` migrated to the Provider/`metaTree` shape, but the gitignored `config.mjs` files (Tier-1 + 5 servers) were still the OLD `static defaultConfig` + `static envBindings` shape. The new `BaseConfig` reads only `metaTree` → ignores the legacy props → empty data tree → `AiConfig.auth` undefined → crash three hops downstream.
> 
> **`--migrate-config` no-op'd** — exactly the `detectDrift()` imports/exports-only blindness this ticket identifies (`initServerConfigs.mjs:166-174`). A `defaultConfig`→`metaTree` **body-shape** change registers zero import/export drift, so the migration silently skipped every file. Recovered by force-regenerating all 6 `config.mjs` from templates via the script's own exported `materializeServerConfigTemplate` transform (the server import-path rewrite is the canonical one).
> 
> **Proposed AC addition (complements the overlay-merge work):** a **`BaseConfig` legacy-shape guard** — when constructed with legacy `defaultConfig`/`envBindings` and no `metaTree`, throw an actionable `stale config.mjs — run <migration>` error rather than silently building empty data + a cryptic downstream `undefined` crash. This makes the *next* body-shape migration fail loud at the source instead of three hops away. (BaseConfig runtime substrate, complementary to this ticket's `initServerConfigs` tooling substrate.)
> 
> Origin Session ID: b124b83a-2cca-4a2b-a711-64868439ba1e

- 2026-05-29T10:55:15Z @neo-opus-ada cross-referenced by PR #12169
- 2026-05-29T18:57:03Z @neo-opus-ada cross-referenced by PR #12179
- 2026-05-29T21:45:07Z @neo-opus-ada cross-referenced by PR #12182
### @neo-opus-ada - 2026-05-29T22:46:12Z

**Input from Claude Opus 4.8 (Claude Code):**

> ❋ **[REFRAMED — source-merge prescription retired]**
> 
> Stage 0's premise (source-level drift detection + a `--merge-config` source-splice in `initServerConfigs.mjs`) is the wrong layer. Picking it up and re-reading `state.Provider` + the `BaseConfig` proxy showed aiConfig is **hierarchical nested data**: a mergeable data tree the Provider already deep-merges (`data_` `merge:'deep'`, `setData(obj)`, `load()`) and resolves up the parent chain — not text to diff and splice. Captured as a model doc in #12182 (`learn/agentos/AiConfigModel.md`).
> 
> So the original AC1/AC2/AC3 are retired. This ticket now tracks the **thin-child overlay implementation**: make `config.mjs` a thin child of its template — owning only operator deltas, inheriting the rest via the chain — instead of a materialized clone. That removes overlay staleness entirely (a template that grows a leaf is inherited, not merged).
> 
> **Deferred.** The active epic lane is the consumer codemods (#12105 → #12106). The thin-child overlay is high-blast; before implementation it needs a mechanism spike (`extends`-the-template + `merge:'deep'` vs a `getParent` overlay→template→Tier-1 chain).

- 2026-05-29T22:59:03Z @neo-opus-ada cross-referenced by #12105
### @neo-opus-ada - 2026-05-30T01:23:33Z

Closing as **superseded** — the prescribed approach (extend `detectDrift()` to project data-tree paths + a `--merge-config` overlay-preserving source-merge) is the wrong layer, and the underlying need has been reframed by AiConfigModel.md (#12182).

**Why the source-surgery approach was wrong-layer:** I initially built a `projectDataLeaves` source-parser + a source-splice merge for this (commit 5fec01e, reverted). Re-reading `state.Provider` + the config proxy (at the operator's prompt) showed the machinery already exists: `Provider#data_` is a `merge:'deep'` descriptor (a JSON overlay deep-merges via `setData`), and `BaseConfig.compileMetaLeaves` already extracts the leaf set from the meta-tree. Parsing/rewriting `config.mjs` source to reconcile a template + an overlay is exactly the anti-pattern AiConfigModel.md now warns against ("if you're parsing or rewriting config source to reconcile a template and an overlay, stop").

**The reframed model (AiConfigModel.md):** an operator overlay should be a *thin child* of its template — owning only operator overrides, inheriting template leaves up the parent chain — so advancing a config to a newer template is **inheritance, not a source-merge**. Under that model the drift this ticket targeted (a new template leaf missing from a generated `config.mjs`) largely dissolves: a thin child inherits the new leaf automatically.

**The real remaining need** (re-homed, not lost): make the *generated* `config.mjs` a thin child of its `config.template.mjs` rather than a full copy. That is a distinct effort with genuine design depth — the generation model in `initServerConfigs`/`bootstrapWorktree`, the runtime import graph, Tier-1-vs-template parent semantics — and warrants its own scoped ticket/Discussion referencing AiConfigModel.md, not this ticket's source-surgery framing.

This was Stage 0 (enabler tooling) of Epic #12101; the epic's core — meta-leaf tree + `BaseConfig extends Provider` + the consumer codemod + Env-internal retraction (Stages 1–6) — is delivered independently of it.


- 2026-06-08T14:15:05Z @neo-gpt cross-referenced by #12767

