---
id: 13248
title: 'NL undo Slice-1 Sub A1: capture set_instance_properties reverses onto the undo stack'
state: CLOSED
labels:
  - ai
  - architecture
assignees:
  - neo-opus-vega
createdAt: '2026-06-14T17:37:42Z'
updatedAt: '2026-06-14T18:54:15Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13248'
author: neo-opus-vega
commentsCount: 0
parentIssue: 13221
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-06-14T18:54:15Z'
---
# NL undo Slice-1 Sub A1: capture set_instance_properties reverses onto the undo stack

## Context

Sub A1 of neomjs/neo#13221 (Neural Link mutation undo, Slice-1), built on the merged neomjs/neo#13238 `(agentId, sessionId)` 2-tuple `TransactionService` core. The first wiring slice: capture the *reverse* of a `set_instance_properties` write so it becomes undoable. (The `undo` NL tool + two-agent e2e = Sub A2; `create_component` / `remove_component` capture = Sub B.)

## The Fix

- `Neo.ai.Client` owns a per-heap `TransactionService` (sibling to `WriteGuard`, same singleton shape).
- `Neo.ai.client.InstanceService.setInstanceProperties` captures the reverse-op (`set⁻¹ = set(oldValues)`) **post-enforcement-grant**, keyed on the live `{agentId, sessionId}` context, via a `begin`/`record`/`commit` per write. `buildSetReverse` captures the pre-set values + the audit `targetSubtreePath`; `recordUndo` commits the single-op tx best-effort.

## Acceptance Criteria

- [ ] `Client` owns a per-heap `TransactionService`.
- [ ] An enforcement-granted `set_instance_properties` agent write captures `set⁻¹` (the pre-set values) onto the writer's `(agentId, sessionId)` stack; multiple writes accumulate as separate committed transactions.
- [ ] A legacy write (no `context`) applies but is **not** captured (no writer identity ⇒ no per-writer stack).
- [ ] The capture is best-effort + fail-closed: a malformed / unserializable reverse, or an absent stack authority, is dropped (`abort`), never thrown into the forward write path.
- [ ] Unit-tested.

## Out of Scope

- The `undo` NL tool (6-site wiring) + the two-agent e2e — Sub A2.
- `create_component` / `remove_component` capture (needs a node-side marker, since they arrive app-side as generic `call_method add/destroy`) — Sub B.

## Related

- Parent: neomjs/neo#13221 (NL undo Slice-1). Core: neomjs/neo#13238 (2-tuple key) / neomjs/neo#13230. Epic: neomjs/neo-agent-brain#144 (Agent Harness Pillar-2) / neomjs/neo#9848.

Authored by @neo-opus-vega (Claude Opus 4.8, Claude Code). Origin Session ID: 4cc428e3-cf36-4324-8646-1b96cb23fa4a.

## Timeline

- 2026-06-14T17:37:43Z @neo-opus-vega added the `ai` label
- 2026-06-14T17:37:43Z @neo-opus-vega added the `architecture` label
- 2026-06-14T17:38:21Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-06-14T17:38:26Z @neo-opus-vega added parent issue #13221
- 2026-06-14T17:39:13Z @neo-opus-vega cross-referenced by PR #13249
- 2026-06-14T18:13:23Z @neo-opus-vega referenced in commit `3e0afef` - "test(ai): cover the AC4 capture-drop seam (stack rejection + absent authority) (#13248)

Per @neo-gpt's #13249 cross-family review: prove #13248 AC4 at the InstanceService seam — a capture-drop path neither breaks the forward write nor commits an undo record. Two negative tests: (1) a stack rejection (record → {ok:false}) is aborted, not committed, and not thrown — the set still applies; (2) an absent client.transactionService is a no-op — the set still applies, nothing thrown (capture is additive, never a write precondition). 3 → 5 unit tests green."
- 2026-06-14T18:54:15Z @tobiu referenced in commit `e02a25b` - "feat(ai): capture set_instance_properties reverses onto the per-session undo stack (#13248) (#13249)

* feat(ai): capture set_instance_properties reverses onto the per-session undo stack (#13248)

Sub A1 of #13221's NL-undo wiring, on the merged #13238 (agentId, sessionId) 2-tuple core. Client owns a per-heap TransactionService (sibling to WriteGuard). InstanceService.setInstanceProperties captures the reverse-op (set-inverse = set(oldValues)) POST-enforcement-grant, keyed on the live (agentId, sessionId) context, via a begin/record/commit per write.

buildSetReverse captures the pre-set values + the audit targetSubtreePath (deriveSubtreePath; audit-only, undo re-derives at undo-time). recordUndo commits the single-op tx best-effort: a legacy frame (no writer identity = no per-writer stack), an absent stack authority, or a stack rejection (malformed/unserializable reverse) is dropped (abort), never thrown into the forward write path.

Tests: 3 unit tests green — capture correctness (set-inverse + provenance + audit path), multi-write accumulation (most-recent undoable first), and the legacy-write skip.

* test(ai): cover the AC4 capture-drop seam (stack rejection + absent authority) (#13248)

Per @neo-gpt's #13249 cross-family review: prove #13248 AC4 at the InstanceService seam — a capture-drop path neither breaks the forward write nor commits an undo record. Two negative tests: (1) a stack rejection (record → {ok:false}) is aborted, not committed, and not thrown — the set still applies; (2) an absent client.transactionService is a no-op — the set still applies, nothing thrown (capture is additive, never a write precondition). 3 → 5 unit tests green."
- 2026-06-14T18:54:15Z @tobiu closed this issue
- 2026-06-14T19:14:23Z @neo-opus-vega cross-referenced by #13257
- 2026-06-15T00:32:56Z @neo-opus-vega cross-referenced by #13221

