---
id: 13282
title: Wire NL undo tx-sweep into agent-disconnect (lock/tx lifecycle coherence)
state: CLOSED
labels:
  - bug
  - ai
  - testing
assignees:
  - neo-opus-vega
createdAt: '2026-06-15T00:32:29Z'
updatedAt: '2026-06-15T00:50:33Z'
githubUrl: 'https://github.com/neomjs/neo/issues/13282'
author: neo-opus-vega
commentsCount: 0
parentIssue: 13221
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-06-15T00:50:33Z'
---
# Wire NL undo tx-sweep into agent-disconnect (lock/tx lifecycle coherence)

## Context

Found during a completeness audit of neomjs/neo#13221 (NL undo Slice-1) before considering it closeable. `Neo.ai.TransactionService.sweep` — the transaction-side teardown that "aborts any open transaction and retires the session's stack" — is **defined but never called anywhere** (`grep '\.sweep(' src/ ai/` → empty). Its own JSDoc states the contract: *"The transaction-side counterpart to `WriteGuard.releaseAgent` (the caller runs both on an `agent_disconnected` frame, so the lock + transaction lifecycles cannot diverge silently)."* But the only caller — `Neo.ai.Client.handleAgentDisconnected` — runs **only** `writeGuard.releaseAgent`.

## The Problem

On an `agent_disconnected` frame the agent's held write-locks are released, but its **open transaction + undo stack are not swept** — the exact lifecycle divergence the sweep exists to prevent. A disconnected (or worker-restarted) agent leaks its session entry in `TransactionService.sessions`; the lock and transaction lifecycles diverge silently. This is neomjs/neo#13221's **AC7** ("disconnect / `releaseAgent` / timeout sweeps the requester's open tx ... lifecycles can't diverge") — partially undelivered: the mechanism shipped (#13230), the wiring did not.

## The Architectural Reality

- `Neo.ai.Client.handleAgentDisconnected` (`src/ai/Client.mjs:284`) — validates the `(agentId, sessionId)` pair, then `return writeGuard.releaseAgent({agentId, sessionId})`. No `transactionService` call.
- `Neo.ai.TransactionService.sweep({id})` (`src/ai/TransactionService.mjs:332`) — deletes the `(agentId, sessionId)` session entry; fails closed on incomplete identity; already unit-tested in `TransactionService.spec`.
- `Neo.ai.Client` holds `this.transactionService` (`src/ai/Client.mjs:100`).

## The Fix

1. `handleAgentDisconnected` calls `this.transactionService.sweep({id: {agentId, sessionId}})` alongside `writeGuard.releaseAgent`, after the same identity validation; returns `{released, swept}`.
2. Extend `test/playwright/unit/ai/ClientAgentDisconnect.spec.mjs`: the sweep is invoked with the exact writer pair on a complete frame, and a half-stamped frame never sweeps (fail-closed symmetry with `releaseAgent`).

## Acceptance Criteria

- [ ] `handleAgentDisconnected` sweeps the disconnected writer's transaction stack via `TransactionService.sweep`, keyed on the same `(agentId, sessionId)` pair it releases locks for.
- [ ] Incomplete-identity frames neither release nor sweep (fail-closed).
- [ ] `ClientAgentDisconnect.spec` covers both.

## Out of Scope

- neomjs/neo#13221 AC10 (a live-tree e2e undo round-trip) — a separate, larger sub-lane, now enabled by the NL-drivable example (#13279).
- Lease/timeout-driven sweep (`TransactionService.timeout` wiring to a daemon) — distinct from the disconnect path.

## Related

- Completes neomjs/neo#13221 AC7 (NL undo Slice-1). Mechanism delivered by neomjs/neo#13230 (TransactionService core) + neomjs/neo#13236 (stack key).
- Epic: neomjs/neo-agent-brain#144 (Agent Harness, Pillar-2).
- Decision Record impact: none (wiring + test of shipped mechanisms; no ADR).

Release classification: boardless (post-release Pillar-2 coherence fix — not v13-release-blocking).

Retrieval Hint: "TransactionService sweep orphaned not wired agent_disconnected handleAgentDisconnected lock tx lifecycle divergence"

Origin Session ID: a0bc6b23-78c5-4bd7-b944-9db5e236f42d

## Timeline

- 2026-06-15T00:32:29Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-06-15T00:32:30Z @neo-opus-vega added the `bug` label
- 2026-06-15T00:32:30Z @neo-opus-vega added the `ai` label
- 2026-06-15T00:32:30Z @neo-opus-vega added the `testing` label
- 2026-06-15T00:32:43Z @neo-opus-vega added parent issue #13221
- 2026-06-15T00:32:56Z @neo-opus-vega cross-referenced by #13221
- 2026-06-15T00:40:28Z @neo-opus-vega cross-referenced by PR #13284
- 2026-06-15T00:50:33Z @tobiu referenced in commit `29b45ac` - "fix(ai): wire tx-sweep into agent-disconnect (lock/tx coherence) (#13282) (#13284)

Completes #13221 AC7. `TransactionService.sweep` (abort the open tx + retire the session stack) was
defined but never called (`grep '\.sweep(' src/ ai/` -> empty) — `Client.handleAgentDisconnected`
ran only `WriteGuard.releaseAgent`, so an `agent_disconnected` frame released the agent's locks but
LEAKED its open transaction: the exact lock/tx lifecycle divergence sweep's own JSDoc says it exists
to prevent.

Fix: `handleAgentDisconnected` now calls `transactionService.sweep({id:{agentId,sessionId}})`
alongside `releaseAgent`, after the same identity validation, and returns `{released, swept}`. Both
fail closed on the incomplete identity already rejected. The sole caller (`onSocketMessage`) ignores
the return; `WriteGuard.releaseAgent` is unchanged.

Tests: `ClientAgentDisconnect.spec` gains 2 wiring tests — sweep is invoked with the exact
`(agentId, sessionId)` writer pair on a complete frame; a half-stamped frame neither releases nor
sweeps (fail-closed symmetry). 24 green (ClientAgentDisconnect 6 + TransactionService 18); sweep's
own behavior stays covered by TransactionService.spec. Return-shape change blast radius verified
isolated (sole src consumer ignores it; WriteGuard/LockRegistry specs assert releaseAgent, unchanged)."
- 2026-06-15T00:50:33Z @tobiu closed this issue
- 2026-06-15T00:52:41Z @neo-opus-vega cross-referenced by #13286
- 2026-07-22T18:27:02Z @neo-gpt-emmy cross-referenced by #15681
- 2026-07-22T19:47:55Z @neo-kimi-phoebe cross-referenced by PR #15735

