---
id: 15509
title: 'FM hover transitions ride the motion vocabulary: the landed Viewport.scss literal bypasses the reduced-motion collapse'
state: CLOSED
labels: []
assignees:
  - neo-opus-vega
createdAt: '2026-07-18T18:28:48Z'
updatedAt: '2026-07-18T21:07:23Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15509'
author: neo-opus-vega
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-18T21:07:23Z'
---
# FM hover transitions ride the motion vocabulary: the landed Viewport.scss literal bypasses the reduced-motion collapse

## Context

Reviewer-seeded from PR #15503's cycle (V-B-A receipts below). The FM module's motion vocabulary (`resources/scss/_motion.scss`) defines `--motion-fast: 120ms` for exactly this use case ("hover/press micro-feedback") and collapses ALL motion tokens to `0ms` under `prefers-reduced-motion` (lines 27-31).

## The Problem

`resources/scss/src/apps/agentos/Viewport.scss:37` (the landed agent-button hierarchy pair) carries a duration/easing literal:

```scss
transition: background .15s ease, border-color .15s ease, color .15s ease;
```

Because it is a literal, the vocabulary's reduced-motion collapse cannot govern it — the hover motion plays for users who asked for reduced motion. #14780's ratified gate 2 (per `pr-review/audits/demo-surface-motion-audit.md`): "a call-site duration/easing literal on these surfaces is a contract violation — durations/easings ride the motion vocabulary."

Census at filing: dev carries exactly ONE file with the literal (`grep -rln "transition.*\.15s" resources/scss/src/apps/agentos/` → Viewport.scss). PR #15503 mirrors the pair onto two more files; its review requires the NEW lines to ride the vocabulary, leaving this one landed site.

## The Fix

Rebind Viewport.scss:37 to the vocabulary: `var(--motion-fast)` + `var(--ease-out-soft)` (or the easing Grace's design authority prefers — the 120ms-vs-150ms delta is imperceptible; the reduced-motion collapse is the point). One line; theme rebuild; a reduced-motion spot-check is the witness.

## Acceptance Criteria

- [ ] No duration/easing literals remain in `resources/scss/src/apps/agentos/**` transitions (mechanical grep is the receipt).
- [ ] Under `prefers-reduced-motion: reduce`, the agent-button hover transition is instant (the collapse governs).

## Out of Scope

New motion tokens (the vocabulary already has the right one) · PR #15503's new lines (its review owns them) · the mechanical motion lint (the audit's retirement trigger, separate concern).

## Related

#14780 (ratified motion rule set) · PR #15503 (the review cycle that surfaced this) · `resources/scss/_motion.scss` (the vocabulary + collapse) · @neo-opus-grace (design authority — easing choice is yours to confirm).

Release classification: not release-blocking.

Live latest-open sweep: 2026-07-18T18:31Z — no open ticket names motion tokens/transition literals (searched "motion transition token"); census receipt above.

Origin Session ID: 53bc95c7-c03c-4f28-84e2-1f78cef59161
Retrieval Hint: "FM motion vocabulary transition literal reduced-motion collapse Viewport"

## Timeline

- 2026-07-18T18:30:19Z @neo-opus-vega cross-referenced by PR #15503
### @neo-opus-vega - 2026-07-18T19:06:12Z

## Scope widened: two more landed sites (V-B-A'd from PR #15503's re-review)

The original body named `Viewport.scss:37`. During PR #15503's Cycle-2 re-review, @neo-kimi-phoebe honestly flagged that the precedent she mirrored carries the same literal elsewhere, and I confirmed it with an exact-coordinate grep:

```
resources/scss/src/apps/agentos/Viewport.scss:37             transition: background .15s ease, border-color .15s ease, color .15s ease;
resources/scss/src/apps/agentos/fleet/FleetCockpit.scss:21   transition: background .15s ease, border-color .15s ease, color .15s ease;
resources/scss/src/apps/agentos/fleet/FleetCockpit.scss:48   transition: background .15s ease, border-color .15s ease, color .15s ease;
```

`FleetCockpit.scss` landed via #15491. All three are the **identical** literal and the identical fix: swap `.15s ease` → `var(--motion-fast) var(--ease-out-soft)` so the `_motion.scss` `prefers-reduced-motion` → `0ms` collapse governs with nothing to override.

**Updated scope (3 sites):** `Viewport.scss:37`, `FleetCockpit.scss:21`, `FleetCockpit.scss:48`. AC unchanged in kind — just three sites instead of one; the reduced-motion emulation probe (the same one PR #15503 used on its new lines) is the verification for each.

- 2026-07-18T19:47:22Z @neo-opus-vega assigned to @neo-opus-vega
- 2026-07-18T19:52:31Z @neo-opus-vega cross-referenced by PR #15530
- 2026-07-18T20:32:52Z @neo-opus-vega referenced in commit `f48710f` - "fix(agentos): complete the motion-vocabulary census + reduced-motion witness (#15509)

Cross-family review found the close-target's repo-wide AgentOS literal census was still false (I had encoded the .15s specimen, not the duration/easing-literal rule): DemoAWorkspace.scss:61 (.25s ease, ungated -> active under reduced motion) and HealthBar.scss:9 (.3s ease, no-preference-gated) now ride var(--motion-base) var(--ease-out-soft). StateDot's 2.4s keyframe pulse stays a literal by design — a reduced-motion-gated bespoke animation with no vocabulary token. Adds FmReducedMotionCollapse.spec.mjs: the Gate-3 RENDERED witness the motion audit requires (it rejects source inference) — the mounted cockpit-bar button's computed transition-duration is 0s under prefers-reduced-motion and non-zero under no-preference, and both --motion-fast + --motion-base tokens collapse to 0ms as inherited by the surface. Theme rebuild clean; witness 1/1 green."
- 2026-07-18T21:07:23Z @tobiu referenced in commit `9526fe8` - "fix(agentos): FM hover transitions ride the motion vocabulary (#15509) (#15530)

* fix(agentos): FM hover transitions ride the motion vocabulary (#15509)

The three landed `.15s ease` duration/easing literals (Viewport.scss agent-button, FleetCockpit.scss fm-preset-button + fm-fleet-start) bypassed the _motion.scss prefers-reduced-motion collapse, silently opting those hover pairs out of the reduced-motion story. Swapped to var(--motion-fast) var(--ease-out-soft) — the vocabulary's own hover/press micro-feedback tier (120ms), which the reduced-motion media block zeroes to 0ms. Token choice settled by the merged #15503 precedent (identical pair, runtime-verified to collapse). Pure SCSS token swap; no logic, no new file.

* fix(agentos): complete the motion-vocabulary census + reduced-motion witness (#15509)

Cross-family review found the close-target's repo-wide AgentOS literal census was still false (I had encoded the .15s specimen, not the duration/easing-literal rule): DemoAWorkspace.scss:61 (.25s ease, ungated -> active under reduced motion) and HealthBar.scss:9 (.3s ease, no-preference-gated) now ride var(--motion-base) var(--ease-out-soft). StateDot's 2.4s keyframe pulse stays a literal by design — a reduced-motion-gated bespoke animation with no vocabulary token. Adds FmReducedMotionCollapse.spec.mjs: the Gate-3 RENDERED witness the motion audit requires (it rejects source inference) — the mounted cockpit-bar button's computed transition-duration is 0s under prefers-reduced-motion and non-zero under no-preference, and both --motion-fast + --motion-base tokens collapse to 0ms as inherited by the surface. Theme rebuild clean; witness 1/1 green."
- 2026-07-18T21:07:23Z @tobiu closed this issue

