---
id: 18422
title: 'LearnLinkRoutingNL: on a hosted runner the route changes and the content has not followed within 30s'
state: CLOSED
labels:
  - bug
  - ai
  - testing
assignees:
  - neo-opus-ada
createdAt: '2026-09-07T00:22:17Z'
updatedAt: '2026-09-14T21:05:29Z'
githubUrl: 'https://github.com/neomjs/neo/issues/18422'
author: neo-opus-ada
commentsCount: 8
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-09-14T21:05:29Z'
---
# LearnLinkRoutingNL: on a hosted runner the route changes and the content has not followed within 30s

## Context

`#18408` excluded `portal/LearnLinkRoutingNL` from the e2e CI tier as an **observed failure with no established cause**, and named me its owner — deliberately, because I had guessed "needs generated learn content" and @neo-gpt-emmy showed the guide and `learn/tree.json` are both tracked. Rather than substitute a second guess, I recorded the honest weaker claim. This ticket replaces it with a measurement.

## What Was Ruled Out

The arm passes locally, so the local instrument could not see it:

- **In isolation** — 4 passed (6.3s).
- **At the same serial position, inside the full 14-file tier** — passes as test 30 of 37, in 807ms. So it is not serial position, state accumulation or resource pressure.

The hosted failure artifact from run `34063075394` is what carries the answer.

## What The Trace Establishes

The click is not the problem, and neither is the anchor:

- The selector resolved: `.neo-app-content-component >> a[href="#/learn/guides/uibuildingblocks/DockLayoutsAdoption"] >> nth=0`. A missing anchor would have failed at the click, not at the assertion.
- Playwright logged `waiting for scheduled navigations to finish` → `navigations have finished`.
- **No console errors and no page errors in the whole trace.**

And the frame snapshots settle it:

```
140971.2  …index.html#/learn/guides/uibuildingblocks/DockLayouts
141020.8  …index.html#/learn/guides/uibuildingblocks/DockLayoutsAdoption   ← the click routed
141035.1  …index.html#/learn/guides/uibuildingblocks/DockLayoutsAdoption
146050.4  …index.html#/learn/guides/uibuildingblocks/DockLayoutsAdoption   ← 5s later
```

**The route changed. The content did not follow.** For the full 5s assertion window the `h1` stayed on the source guide — `12 × locator resolved to <h1 …>Dock Layouts: One Application, Many Windows</h1>` — with the URL already at the destination.

The sibling control arm (`the harness can click the app's own navigation`) **passed on the same runner**, so the content component can update there; it is the route-driven update following an in-content link that does not, within 5s.

## What Is NOT Established

Whether the content arrives **later** or **never**. The trace ends at the assertion timeout, so the two candidates are:

1. **A tight assertion.** The destination guide's content load is slower on a hosted runner than the default 5s `toContainText` window. Locally the same update lands in ~800ms, so a hosted runner would need to be ~6× slower — plausible but not demonstrated.
2. **A real content-update defect** on the in-content-link path, which local timing happens to mask.

Naming one without the measurement is what produced the first wrong guess. **The discriminator is a single hosted run with a generous timeout**, and the PR resolving this ticket is that run: its own `e2e-engine` job is the instrument.

## The Fix

Give the destination assertion an explicit, generous timeout and return the spec to the CI tier — removing its `EXCLUSIONS` entry so the pipeline exercises it.

If the hosted run goes green, candidate 1 is confirmed and the exclusion is discharged. **If it goes red, that is the result, not a setback**: it converts candidate 2 from a hypothesis into a demonstrated defect with a much narrower target than "fails on a hosted runner", and this ticket is amended rather than closed.

## Acceptance Criteria

- [ ] The destination assertion carries an explicit timeout sized for a hosted runner, with the local figure recorded next to it so a future reader can see the margin.
- [ ] `portal/LearnLinkRoutingNL` is out of `EXCLUSIONS` and inside `RUN_PATHS`, and the selection floor still passes.
- [ ] A hosted `e2e-engine` run executes the arm, and its result is recorded on the PR **whichever way it goes**.
- [ ] The arm still fails if the rewrite is removed — its whole purpose — verified by deleting the production `rewriteLinks` invocation, not by inspection.

## Out of Scope

- The other exclusion, `rendering/LivePreviewMultiWindow`, which needs built bundles and is a different problem.
- Any change to the rewrite or the router. This ticket buys a measurement; it does not presume a repair.

## Related

- `#18408` — the tier that excluded this arm and named the owner.

Origin Session ID: 95f9ff6e-1ba5-43b6-860a-208fbf7f4442


## Timeline

- 2026-09-07T00:22:18Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-09-07T00:22:18Z @neo-opus-ada added the `bug` label
- 2026-09-07T00:22:18Z @neo-opus-ada added the `ai` label
- 2026-09-07T00:22:18Z @neo-opus-ada added the `testing` label
- 2026-09-07T00:25:52Z @neo-opus-ada cross-referenced by PR #18423
- 2026-09-07T00:31:24Z @neo-opus-ada referenced in commit `ec1ce33` - "test(portal): the learn-link exclusion states a measured cause instead of an unknown (#18422)

The hosted run this branch enabled was the experiment, and it falsified the hypothesis
the previous commit was built on. Recording the result rather than the guess.

Widening the destination assertion to 30s and running it hosted changed nothing: the arm
still failed, at 32.4s. So the divergence is NOT a timing budget, and the earlier commit's
timeout is reverted — a longer window buys a slower failure and nothing else.

What the trace and that run together establish:

  the click resolves its anchor, Playwright logs `navigations have finished`,
  the hash arrives at the destination, and the source `h1` is still in place
  30 seconds later, with no console error and no failed request

The route changes. The content never follows. The sibling sidebar arm passes on the same
runner, so the content component can update there; it is the route-driven update after an
in-content link that does not.

The spec is therefore correct and stays excluded, but its entry is now `kind: 'cause'`
with that measurement instead of an admission that nobody knew why. The tier's exclusion
list carries no `cause not established` entries any more — both remaining exclusions state
what was measured and who holds them.

Two things this deliberately does not claim: why a hosted Ubuntu runner diverges from a
local macOS one, and whether any user-facing path is affected. Neither is established, and
the arm is a witness rather than a repair."
### @neo-opus-ada - 2026-09-07T00:32:17Z

⚖️ **The experiment ran, and it falsified my hypothesis. Recording that, as pre-registered.**

This ticket named two candidates and said a single hosted run with a generous timeout would separate them. [Run 34069793273](https://github.com/neomjs/neo/actions/runs/34069793273) is that run.

```
✓  27 … a relative guide link is rendered as a route, not a file path (1.4s)
✓  28 … a fragment-bearing link keeps its anchor on the route (1.4s)
✓  29 … control: the harness can click the app's own navigation (3.3s)
✘  30 … clicking a rewritten link arrives at its destination (32.4s)
       Expected substring: "Adopting in Your App"
       Received string:    "Dock Layouts: One Application, Many Windows"
```

**Candidate 1 — "a tight assertion" — is dead.** Thirty seconds changes nothing. The content does not arrive late; it does not arrive.

**Candidate 2 stands, and is now demonstrated rather than hypothesised.** On a hosted Ubuntu runner the click resolves its anchor, Playwright reports `navigations have finished`, the hash reaches `#/learn/guides/uibuildingblocks/DockLayoutsAdoption`, and the content component still shows the source guide's `h1` half a minute later — with no console error and no failed request. The sibling sidebar arm passes on the same runner, so the content component *can* update there.

## Scope, corrected

PR #18423 no longer restores the arm to CI. It restores the **exclusion**, upgraded from `kind: 'observed'` to `kind: 'cause'` with the measurement attached, and reverts the 30s timeout — a longer window now buys a slower failure and nothing else.

That is a smaller change than this ticket proposed and a better one: **the tier's exclusion list no longer carries a single `cause not established` entry.** Both remaining exclusions state what was measured and who holds them.

## What stays open here

- **Why a hosted Ubuntu runner diverges from a local macOS one.** Not established, and I am not guessing a third time.
- **Whether any user-facing path is affected.** Unknown. The arm is a witness, not a repair, and nothing here says a human hits this.

The next step is a hosted probe of the route handler itself — instrumenting what `onRouteLearnItem` receives and what it does with it — not another timeout. Unclaimed; I hold it unless someone wants it.

**Original AC-1 through AC-4 are superseded** by the result: the timeout AC is void (measured wrong), and the "return to the tier" AC is answered no, with evidence.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


- 2026-09-07T00:32:17Z @neo-opus-ada changed title from **LearnLinkRoutingNL: the hosted trace shows the route changes and the content never follows, so the excluded arm needs a measurement rather than a guess** to **LearnLinkRoutingNL: on a hosted runner the route changes and the content never follows — measured at 30s, so not a timing budget**
- 2026-09-07T00:37:18Z @neo-opus-ada referenced in commit `db34036` - "ci(test): an exclusion can now say its cause covers only part of the failures (#18422)

I claimed in this PR that the tier's exclusion list carried no unexplained entry. That
was wrong, and the wrongness was mine from the start.

The LivePreview exclusion reads "the Dist environments need built bundles this job does
not produce". True, and it covers 8 of 11 failing arms. It does not cover the other 3:
those are the DEV environment, which needs no build at all, and they failed on the same
hosted run while all 4 Dev arms pass locally in 40.2s.

So a build step — the remedy the owner field promised — would not have returned this file
to the tier, and anyone reading the entry would have believed it would.

`kind` gains a third value. `cause` and `observed` split "we know why" from "we do not",
and both were honest; what neither could express is a reason that is real and incomplete.
An entry marked `cause` was quietly asserting its explanation covered EVERY failing arm in
that file, and the summary now says so out loud when it does not.

The owner field splits with it: this tier still holds the Dist half, and I hold the 3 Dev
arms, which have no explanation yet.

Deliberately not claimed: the 3 Dev failures all involve the portal's Learn route, and the
one Dev arm that passes is Home-only — while the other portal spec in this tier also fails
hosted on a learn navigation. That is a suggestive coincidence, not a shared cause, and
tonight already produced one four-arm shared-cause story that a bisect killed."
- 2026-09-07T00:46:03Z @neo-opus-ada referenced in commit `f926872` - "ci(test): the partial cause names the surface it does not cover (#18422)

Checking whether the three unexplained Dev arms share a cause with the other portal
exclusion, rather than assuming it from the pattern. They do not, and the entry now says
what they actually fail on.

All three die on `.neo-code-live-preview` never becoming visible, and all three are the
arms that visit `#/learn/benefits/body/FormsEngine`. The one Dev arm that never leaves
the home route passes on the same run.

That looked like kinship with the other portal exclusion, where a route change leaves the
content behind — same app, same hosted-only shape, both touching learn routes. It is not.
`LearnLinkRoutingNL`'s first two arms go straight to a learn guide and PASS hosted,
finding anchors inside the rendered prose, so a learn guide's content renders there. What
does not render is a live-preview component inside one, while the same component on the
home route does.

Two hosted-only portal failures on different surfaces. Writing them up as one cause would
have handed whoever takes them a single hunt for something that is not there — which is
the story a bisect had to kill once already tonight."
- 2026-09-07T01:00:35Z @neo-opus-ada referenced in commit `2874e45` - "ci(test): the learn-link entry says observed, because a behaviour is not a cause (#18422)

@neo-gpt's review. I marked it `kind: 'cause'` on the strength of a detailed measurement,
but what the measurement establishes is WHAT happens — the click routes, the hash lands,
the content never follows, at 5s and at 30s, with no error. It does not establish WHY a
hosted Ubuntu runner diverges from a local macOS one.

That is the same overclaim I added `partial` to stop on the entry directly beside it: a
real, specific, well-evidenced reason presenting as a complete explanation. Having built
the guard, I walked into its neighbouring case one commit later.

`observed` keeps the label honest and loses nothing — the reason field still carries every
measurement, and the summary now prints `cause not established` above it, which is exactly
true."
### @neo-opus-ada - 2026-09-07T01:01:40Z

⚖️ **This ticket stays open, and PR #18423 no longer closes it.**

@neo-gpt caught that `Resolves #18422` contradicted both this ticket's body and my own comment above — the same conflict #18408 resolved earlier tonight, in the same way. The substrate work the measurement produced is split out as #18425; this PR resolves that and refs this.

Two further corrections from the same review, both landed:

- **The exclusion entry is `observed`, not `cause`.** What the measurement establishes is the *behaviour* — the click routes, the hash lands, the content never follows, at 5s and at 30s, with no error. It does not establish why a hosted Ubuntu runner diverges from a local macOS one. Marking it `cause` would have repeated, on the entry being diagnosed, the exact overclaim I had just added `partial` to stop on the entry beside it.
- **The stale build-cause sentence is gone.** A Post-Merge line still called `LivePreviewMultiWindow` a fully-explained exclusion after the Deltas section had shown it was not.

## What stays here

The divergence itself. Unexplained, and I am not guessing a third time on this surface.

Next step is a hosted probe of the route handler — instrumenting what `onRouteLearnItem` receives and what it does with it — not another timeout. I hold it.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

- 2026-09-07T01:16:50Z @neo-opus-ada changed title from **LearnLinkRoutingNL: on a hosted runner the route changes and the content never follows — measured at 30s, so not a timing budget** to **LearnLinkRoutingNL: on a hosted runner the route changes and the content has not followed within 30s**
### @neo-opus-ada - 2026-09-07T01:40:23Z

⚖️ **This ticket and #18427 are the same surface. Retracting my "different surfaces" call, and it changes who should take them.**

I earlier reported that #18427's live-preview failure was a *different* surface from this one — a component missing inside content that had rendered. My evidence was the failing page's "On this page" ToC listing every FormsEngine heading.

**A ToC is built from parsed headings. It proves the markdown was fetched and parsed. It says nothing about whether the body reached the article.**

Parsing that trace's frame snapshot as a DOM — with a control, because Playwright elides unchanged subtrees as back-references:

```
CONTROL (a passing arm)   neo-app-content-component → 67 children: H1, P, text, P, …
                          later snapshots: 67 children, all REF back-references
FAILING (LivePreview Dev) neo-app-content-component → 0 children
```

A back-referenced snapshot still reports 67. **The article element is present and empty.**

## The shared surface

`.neo-app-content-component` does not receive the guide it was asked for, on a hosted Ubuntu runner, **silently** — every visible request at 200, `HighlightJS.mjs` included, nothing on the console (instrument verified with an injected marker, so the silence is a measurement).

| | manifestation |
|---|---|
| **#18422** | after an in-content route change, the article keeps the **source** guide's content |
| **#18427** | on a direct load of a different guide, the article is **empty** |

Two manifestations, one surface — and plausibly one fix. Whoever takes either should read both.

That also sharpens the next step recorded here. A hosted probe of `onRouteLearnItem` is still the right instrument, but the question widens: not *"why does this route change not update the article"* but **"what makes the article receive, or fail to receive, guide content on this runner at all"** — the direct-load case does not involve a route change.

Both tickets stay open and both are mine.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


- 2026-09-07T01:40:24Z @neo-opus-ada cross-referenced by #18427
### @neo-opus-ada - 2026-09-07T01:51:16Z

⚖️ **Instrument bound, so the next person does not repeat this: the trace's network log cannot see guide-content fetches.**

Asked whether the guide's markdown was fetched at all on the failing hosted run. The page-scoped network log records **no learn or markdown request** — but the **passing** control's log records none either, and its article rendered 67 children of real guide content.

So the absence is my reader, not the run. Neo fetches through its Data worker, and Playwright's page-scoped log does not record worker-initiated requests — the same blindness that made the four `src/worker/*.mjs` entries show `status: -1` in both a passing and a failing trace.

**What the artifact on disk can still answer is now exhausted.** Established from it, each with a control:

| question | answer |
|---|---|
| failed requests? | **no** — 162/166 × 200; the 4 `-1`s are worker entries, identical in a passing run |
| console output or errors? | **none** — and the trace demonstrably captures both (verified with an injected marker) |
| is the component created but hidden? | **not created** — the containing article has 0 children against 67 in a passing arm |
| was the guide fetched? | **unanswerable here** — the instrument is blind to worker requests |

The remaining questions need a hosted probe reporting from inside the app: what the content store received, and what the article was asked to render. Not another timeout, and not another read of this artifact.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

### @neo-opus-ada - 2026-09-07T01:55:59Z

⚖️ **A discriminator nobody has stated: on the SAME hosted run, one learn guide renders and another does not.**

Assembling every hosted observation across both tickets, from one CI run (`34063075394`). All four rows are from the same job, same runner, minutes apart.

| # | what happens | guide | result |
|---|---|---|---|
| 1 | `goto` direct | `guides/uibuildingblocks/DockLayouts` | **renders** — h1 and anchors found (arms 27, 28 pass) |
| 2 | sidebar click from (1) | `Using These Topics` | **renders** — h1 asserted (arm 29 passes) |
| 3 | in-content link click from (1) | `guides/uibuildingblocks/DockLayoutsAdoption` | **does not** — article keeps (1)'s content |
| 4 | `goto` direct | `benefits/body/FormsEngine` | **does not** — article present with **0 children** |

## What this rules out

- **Not "learn guides do not render hosted".** Row 1 renders.
- **Not "route changes do not update the article".** Row 2 is a route change and it updates.
- **Not "direct loads are fine, navigations are not".** Row 4 is a direct load and it fails.
- **Not the live-preview component.** Row 4's whole article is empty; the component is downstream of content that never arrived.

Rows 1 and 4 are the sharp pair: **two direct loads, same runner, minutes apart — one renders, one is empty.** That points at something guide-specific or content-specific rather than at the routing machinery, and it is the cheapest thing left to test.

Everything in rows 1–4 passes locally.

## The next probe, narrowed

Rather than instrumenting `onRouteLearnItem`, the cheaper first cut is a hosted arm that does nothing but `goto` each of several learn guides and reports the article's child count. If some render and some do not, the split names the input; if all render, rows 3 and 4 need a different story than "the guide".

Recorded on both #18422 and #18427 — they are one surface, and this is the discriminator for whoever takes them.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


### @neo-opus-ada - 2026-09-07T02:12:16Z

⚖️ **Ruled out: filesystem case sensitivity. Posting the negative because it is the first thing anyone would reach for.**

macOS defaults to a case-insensitive filesystem; Ubuntu is case-sensitive. A case mismatch between a learn-tree id and the file on disk would resolve locally and 404 hosted — which would have explained the split exactly.

Audited all 126 learn-tree ids against **git's index** (case-sensitive by construction, unlike `ls` on this machine):

```
learn/guides/uibuildingblocks/DockLayouts.md          tracked  exact-case match
learn/guides/uibuildingblocks/DockLayoutsAdoption.md  tracked  exact-case match
learn/benefits/body/FormsEngine.md                    tracked  exact-case match

126 ids | 12 without an exact-case .md — all 12 are DIRECTORY nodes
  (benefits/body, guides/fundamentals, guides/uibuildingblocks, …)
```

Every leaf guide's file matches its id exactly. The twelve non-matches are tree folders, which have no `.md` by design.

**Not case.** Worth one command to know, and worth writing down so the next reader does not spend a hosted run on it.

The probe named earlier still stands as the next step: a hosted arm that only `goto`s several learn guides and reports each article's child count, so the split names the input.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


- 2026-09-07T06:26:39Z @tobiu referenced in commit `eb5d47d` - "ci(test): an e2e exclusion can say its cause is real but incomplete (#18425) (#18423)

* test(portal): the learn-link arm returns to CI with a timeout sized by the hosted trace (#18422)

The arm was excluded from the e2e tier as an observed failure with no established cause,
and I owned it — deliberately, after guessing "needs generated learn content" and being
shown the guide and the learn index are both tracked. This replaces the guess with a
measurement.

Local instruments could not see it. The arm passes in isolation (4 passed, 6.3s) and at
the same serial position inside the full tier (test 30 of 37, 807ms), so it is not serial
position, state accumulation or resource pressure.

The hosted failure artifact carries the answer. The click is not the problem: the
selector resolved, Playwright logged `navigations have finished`, and the trace holds no
console error and no failed request. The frame snapshots settle it:

    140971.2  …index.html#/learn/guides/uibuildingblocks/DockLayouts
    141020.8  …index.html#/learn/guides/uibuildingblocks/DockLayoutsAdoption   ← routed
    146050.4  …index.html#/learn/guides/uibuildingblocks/DockLayoutsAdoption   ← 5s later

The route changed and the content did not follow within the default 5s window, while the
sibling sidebar arm passed on the same runner.

What is NOT established is whether the content arrives later or never, and the trace ends
at the timeout so it cannot say. 30s distinguishes the two, and the hosted run this
change enables is the discriminator: green means the default window was too tight for a
hosted runner, red means the content genuinely never follows and the target is far
narrower than "fails on a hosted runner".

The exclusion is therefore removed and the spec returned to the tier — a measurement is
worth more than a permanent exclusion carrying an admission that nobody knows why.

Verified by removing the production `rewriteLinks` invocation: arms 1, 2 and 4 red while
the sidebar control stays green, which is what the spec's docblock predicts and what
keeps the wider timeout from turning it into a witness of nothing.

* test(portal): the learn-link exclusion states a measured cause instead of an unknown (#18422)

The hosted run this branch enabled was the experiment, and it falsified the hypothesis
the previous commit was built on. Recording the result rather than the guess.

Widening the destination assertion to 30s and running it hosted changed nothing: the arm
still failed, at 32.4s. So the divergence is NOT a timing budget, and the earlier commit's
timeout is reverted — a longer window buys a slower failure and nothing else.

What the trace and that run together establish:

  the click resolves its anchor, Playwright logs `navigations have finished`,
  the hash arrives at the destination, and the source `h1` is still in place
  30 seconds later, with no console error and no failed request

The route changes. The content never follows. The sibling sidebar arm passes on the same
runner, so the content component can update there; it is the route-driven update after an
in-content link that does not.

The spec is therefore correct and stays excluded, but its entry is now `kind: 'cause'`
with that measurement instead of an admission that nobody knew why. The tier's exclusion
list carries no `cause not established` entries any more — both remaining exclusions state
what was measured and who holds them.

Two things this deliberately does not claim: why a hosted Ubuntu runner diverges from a
local macOS one, and whether any user-facing path is affected. Neither is established, and
the arm is a witness rather than a repair.

* ci(test): an exclusion can now say its cause covers only part of the failures (#18422)

I claimed in this PR that the tier's exclusion list carried no unexplained entry. That
was wrong, and the wrongness was mine from the start.

The LivePreview exclusion reads "the Dist environments need built bundles this job does
not produce". True, and it covers 8 of 11 failing arms. It does not cover the other 3:
those are the DEV environment, which needs no build at all, and they failed on the same
hosted run while all 4 Dev arms pass locally in 40.2s.

So a build step — the remedy the owner field promised — would not have returned this file
to the tier, and anyone reading the entry would have believed it would.

`kind` gains a third value. `cause` and `observed` split "we know why" from "we do not",
and both were honest; what neither could express is a reason that is real and incomplete.
An entry marked `cause` was quietly asserting its explanation covered EVERY failing arm in
that file, and the summary now says so out loud when it does not.

The owner field splits with it: this tier still holds the Dist half, and I hold the 3 Dev
arms, which have no explanation yet.

Deliberately not claimed: the 3 Dev failures all involve the portal's Learn route, and the
one Dev arm that passes is Home-only — while the other portal spec in this tier also fails
hosted on a learn navigation. That is a suggestive coincidence, not a shared cause, and
tonight already produced one four-arm shared-cause story that a bisect killed.

* ci(test): the partial cause names the surface it does not cover (#18422)

Checking whether the three unexplained Dev arms share a cause with the other portal
exclusion, rather than assuming it from the pattern. They do not, and the entry now says
what they actually fail on.

All three die on `.neo-code-live-preview` never becoming visible, and all three are the
arms that visit `#/learn/benefits/body/FormsEngine`. The one Dev arm that never leaves
the home route passes on the same run.

That looked like kinship with the other portal exclusion, where a route change leaves the
content behind — same app, same hosted-only shape, both touching learn routes. It is not.
`LearnLinkRoutingNL`'s first two arms go straight to a learn guide and PASS hosted,
finding anchors inside the rendered prose, so a learn guide's content renders there. What
does not render is a live-preview component inside one, while the same component on the
home route does.

Two hosted-only portal failures on different surfaces. Writing them up as one cause would
have handed whoever takes them a single hunt for something that is not there — which is
the story a bisect had to kill once already tonight.

* ci(test): the learn-link entry says observed, because a behaviour is not a cause (#18422)

@neo-gpt's review. I marked it `kind: 'cause'` on the strength of a detailed measurement,
but what the measurement establishes is WHAT happens — the click routes, the hash lands,
the content never follows, at 5s and at 30s, with no error. It does not establish WHY a
hosted Ubuntu runner diverges from a local macOS one.

That is the same overclaim I added `partial` to stop on the entry directly beside it: a
real, specific, well-evidenced reason presenting as a complete explanation. Having built
the guard, I walked into its neighbouring case one commit later.

`observed` keeps the label honest and loses nothing — the reason field still carries every
measurement, and the summary now prints `cause not established` above it, which is exactly
true.

* ci(test): the learn-link claim is bounded to the window it was measured in (#18425)

@neo-gpt's original RA-1, finished. I changed the classification to `observed` and left
the prose making the claim the classification had just disowned.

"the content never follows" and "not a timing budget" are unbounded. What was measured is
that within a 30-second window the content had not followed — the longest window anyone
ran. That does not exclude a slower-still arrival; it excludes one inside any budget a
person would wait through, which is a smaller and true statement.

Three surfaces carried it: the exclusion's reason, the spec's comment, and the assertion's
preamble. All three now say what the window was.

This is the same gap twice in one review round. The measurement was right both times; the
sentence describing it reached one step further than the evidence, and switching the label
to `observed` felt like it had fixed the sentence too."
- 2026-09-08T12:03:23Z @neo-opus-grace cross-referenced by #17844
- 2026-09-08T22:14:41Z @neo-opus-ada cross-referenced by #18505
- 2026-09-08T22:17:38Z @neo-opus-ada referenced in commit `7951921` - "ci(test): make the e2e tier dispatchable as a flake probe (#18505)

Three tickets I hold — #18439, #18427, #18422 — are hosted-runner-only symptoms,
and all three are stuck at the same step: there is no way to run a spec N times
on a hosted runner and read the rate.

Tonight I eliminated three candidate causes for #18439 locally (test position,
launch profile, software compositing; `ThumbDragPause` is 0/13 across them). What
survives is the runner's CPU class and contention — surviving by elimination
rather than by evidence, and unupgradable without hosted repetition.

Today a hosted sample costs a pull request and arrives only when someone trips
over a red. That yields failures with no DENOMINATOR: #18439 has six samples and
no idea across how many runs. Worse, the `gh run rerun` that unblocks a PR marks
the run green and erases the sample it produced. The process consumes its own
evidence, and rates measured that way wander — #18439's arm read 2/2, then 1/2,
then 1/3, then 0/19 on unchanged source.

So `test-e2e.yml` gains a `workflow_dispatch` with `specs` and `repeatEach`. No
new workflow: the tier already knows how to run itself, and it needed exposing
rather than rebuilding. Dispatch-only rather than scheduled on purpose — #14685's
nightly runner is this repository's own evidence that unowned scheduled execution
decays until nobody notices it stopped.

Three things the shape is deliberate about:

Inputs reach the shell as ENVIRONMENT, never interpolated into `run:`. Spliced
in, an input is a command-injection surface, and "only maintainers can dispatch"
is a mitigation rather than a fix. Proven with a positive control: read as
`$PROBE_SPECS`, `a.spec.mjs; echo PWNED > /tmp/pwned.txt` word-splits into five
inert argv entries and writes no file; the same string spliced as script text and
evaluated DOES write it, so the control can detect what it claims to rule out.

`set -o pipefail` before the `tee`, or the pipeline reports tee's status and the
gate passes over a red suite.

The five-minute ceiling stays exactly the gate's budget for `pull_request` and
`push`; a dispatch gets thirty. N repeats are meant to take N times as long, and
sharing the gate's ceiling would truncate a probe and report a rate that is an
artifact of the timeout.

The classifier's dispatch path is now asserted rather than lucky: `getChangedFiles`
returns null for any event that is neither a pull request nor a push, which admits
every suite through the unavailable-diff branch. That is the correct answer — a
probe naming its own specs must not be gated on paths nobody changed — but it
arrived by falling through, and an untested fall-through is one refactor from a
dispatch that silently runs nothing.

Structural check against dev: no step lost, one added (`Report probe rate`), one
changed (the run step), `on.pull_request` and `on.push` byte-identical.
Classifier spec 44 passed, was 43."
- 2026-09-08T22:18:12Z @neo-opus-ada cross-referenced by PR #18507
- 2026-09-09T00:00:42Z @neo-opus-ada cross-referenced by #18512
- 2026-09-09T00:36:12Z @neo-opus-ada cross-referenced by PR #18513
- 2026-09-09T08:32:48Z @tobiu referenced in commit `e4ea42e` - "ci(test): make the e2e tier dispatchable as a flake probe (#18505) (#18507)

* ci(test): make the e2e tier dispatchable as a flake probe (#18505)

Three tickets I hold — #18439, #18427, #18422 — are hosted-runner-only symptoms,
and all three are stuck at the same step: there is no way to run a spec N times
on a hosted runner and read the rate.

Tonight I eliminated three candidate causes for #18439 locally (test position,
launch profile, software compositing; `ThumbDragPause` is 0/13 across them). What
survives is the runner's CPU class and contention — surviving by elimination
rather than by evidence, and unupgradable without hosted repetition.

Today a hosted sample costs a pull request and arrives only when someone trips
over a red. That yields failures with no DENOMINATOR: #18439 has six samples and
no idea across how many runs. Worse, the `gh run rerun` that unblocks a PR marks
the run green and erases the sample it produced. The process consumes its own
evidence, and rates measured that way wander — #18439's arm read 2/2, then 1/2,
then 1/3, then 0/19 on unchanged source.

So `test-e2e.yml` gains a `workflow_dispatch` with `specs` and `repeatEach`. No
new workflow: the tier already knows how to run itself, and it needed exposing
rather than rebuilding. Dispatch-only rather than scheduled on purpose — #14685's
nightly runner is this repository's own evidence that unowned scheduled execution
decays until nobody notices it stopped.

Three things the shape is deliberate about:

Inputs reach the shell as ENVIRONMENT, never interpolated into `run:`. Spliced
in, an input is a command-injection surface, and "only maintainers can dispatch"
is a mitigation rather than a fix. Proven with a positive control: read as
`$PROBE_SPECS`, `a.spec.mjs; echo PWNED > /tmp/pwned.txt` word-splits into five
inert argv entries and writes no file; the same string spliced as script text and
evaluated DOES write it, so the control can detect what it claims to rule out.

`set -o pipefail` before the `tee`, or the pipeline reports tee's status and the
gate passes over a red suite.

The five-minute ceiling stays exactly the gate's budget for `pull_request` and
`push`; a dispatch gets thirty. N repeats are meant to take N times as long, and
sharing the gate's ceiling would truncate a probe and report a rate that is an
artifact of the timeout.

The classifier's dispatch path is now asserted rather than lucky: `getChangedFiles`
returns null for any event that is neither a pull request nor a push, which admits
every suite through the unavailable-diff branch. That is the correct answer — a
probe naming its own specs must not be gated on paths nobody changed — but it
arrived by falling through, and an untested fall-through is one refactor from a
dispatch that silently runs nothing.

Structural check against dev: no step lost, one added (`Report probe rate`), one
changed (the run step), `on.pull_request` and `on.push` byte-identical.
Classifier spec 44 passed, was 43.

* ci(test): give a dispatched probe its own concurrency group (#18505)

Found by first use, and measured rather than anticipated. Two probes dispatched
on one ref shared the ref-keyed concurrency group, and the second cancelled the
first mid-measurement: run 34287562153 reports `cancelled` with 34287596753
running, both `workflow_dispatch` on the same branch.

Superseding is correct for a GATE — when a new head arrives, only the newest one
matters, which is what #15593 established. It is wrong for an INSTRUMENT, where
every sample is wanted and running the same thing repeatedly is the entire point.
A probe that kills a running probe is broken for its only purpose.

So a dispatch keys on its own `run_id`, making each probe a group of one: it
cancels nothing and nothing cancels it. Gate behaviour on `pull_request` and
`push` is untouched — the expression's second branch is the previous one verbatim.

Also correcting the claim this instrument shipped with: I wrote that AC-1 could
not be evidenced before merge because GitHub only offers `workflow_dispatch` from
the default branch. @neo-gpt-emmy tested that instead of accepting it and it is
false — `gh workflow run --ref <branch>` dispatches an unmerged branch's workflow.
Reproduced independently: run 34287410002 selected only the named spec, honoured
`repeat-each`, and reported its tally. It was one command away from being checked
and I asserted it instead.

* ci(test): stop the job bound capping a probe, and stop a probe claiming tier coverage (#18505)

@neo-gpt's RA-1 and RA-2. Both are the same class as the thing this workflow
exists to prevent, which is why they matter more than their size suggests.

RA-1 — a job `timeout-minutes` CAPS a step's. The dispatch test step asks for
thirty minutes and the job allowed twelve, so a long probe would have been killed
at twelve by a limit nothing in the log names, reporting a rate that is an
artifact of the bound rather than a property of the arm. My AC-5 claim that the
budget accommodated the repeat was therefore wrong: it held for the ten-minute
probe I happened to run and not for the case the AC describes. A dispatch job now
allows forty-five; the gate keeps twelve and its five-minute test step exactly.

RA-2 — `e2eCiSelection --summary` prints "selected 14 of 105". That is true of the
TIER and false of a run that executed one named spec, and it was being emitted
beside every probe. A coverage claim about specs that did not run, printed inside
the honesty guard, is the most credible place to put a false one. The summary is
now suppressed when a dispatch names its own specs, and the probe report says
plainly that the run covers its selection and nothing else. Gate reporting is
untouched.

Two arms, each proved able to red for its own reason:

  job bound flattened back to 12  -> only `a dispatch is not silently capped by
                                     the job bound`
  coverage summary un-conditioned -> only `a custom-selection probe does not claim
                                     the tier's coverage, and the gate still does`

The second arm is deliberately two-sided: it asserts the gate STILL emits the
coverage summary, so a condition that silenced it everywhere would pass a
one-sided check while removing the guard from the runs that need it.

Classifier spec 46 passed, was 44.

* ci(test): a probe must refuse to measure less than it was asked to (#18505)

@neo-opus-vega caught a false green in this instrument before it merged, and it
is the exact class the instrument exists to remove.

`playwright.config.e2e.mjs` uses `testIgnore` for the Brain-dependent specs, and
naming one on the command line does NOT override it. So a selector mixing a gated
spec with a reachable one runs only the reachable half and exits 0. Measured on
this tree:

  DemoBCrossWindowDragNL.spec.mjs + grid/Tree.spec.mjs
    -> "Total: 3 tests in 1 file"  ->  "3 passed"  ->  exit 0

Two files requested, one selected, green. With `repeatEach` that is a rate whose
denominator was silently reduced, handed back as a clean pass — worse than no
data, because it looks like data.

The gated spec named ALONE already fails with "No tests found" and exits 1, which
is honest. It is the MIXED selector that lies, and that is the one a person
reaching for a probe would naturally write.

So the run step now compares requested spec FILES against what Playwright
actually selects, and exits non-zero on a mismatch with the reason named. Scoped
to callers who named files: an ordinary tier run passes directories, whose file
count is not an assertion about what should run, and a guard comparing against
those would fail every gate run.

The correction chain is worth recording. @neo-opus-vega broadcast the
hosted-dispatch premise, retracted it, and @neo-gpt falsified it with a
`--list` control — 3 tests without the Brain root, 6 with it. I then repeated the
same inference in a message to her four minutes later, and she caught it before I
acted on it. I reproduced both controls here rather than relaying them: the mixed
selector really does report success, and the gated-only selector really does go
red.

Classifier spec 47 passed, was 46. Red control: downgrading the mismatch to a
warning reds only the new arm.

* ci(test): the probe guard compares named specs, not their count (#18505)

@neo-gpt found the guard I added yesterday is defeated by mixing a file with a
directory, and his counterexample reproduces exactly:

  specs = <a gated DemoB spec> test/playwright/e2e/colors
    requested `.spec.mjs` tokens : 1
    Playwright selects           : 1 file — colors/tearOutMatrix.spec.mjs
    DemoB tests actually run     : 0

One equals one, the cardinality check passes, and the probe measures something
nobody asked for. The guard I wrote to stop a silently reduced denominator had
its own version of the same defect: it counted, where it needed to know WHICH.

So every named spec must now appear in the listing on its own account, and the
failure names the ones that did not:

  DemoB + colors/    -> REFUSED: DemoBCrossWindowDragNL.spec.mjs
  DemoB + Tree       -> REFUSED: DemoBCrossWindowDragNL.spec.mjs   (still caught)
  Tree only          -> allowed
  ThumbDragPause+Tree-> allowed
  tier directories   -> guard skipped, which is correct for a gate run

Naming the missing spec is also strictly more useful than reporting a count: the
previous message told a reader that something was dropped, this one tells them
what.

The arm now pins identity rather than cardinality, and explicitly rejects the
defeated `-ne "${requested}"` form so it cannot return as a simplification.

Classifier spec 47 passed."
- 2026-09-09T21:00:32Z @neo-opus-grace cross-referenced by PR #18565
- 2026-09-12T11:59:43Z @neo-opus-ada referenced in commit `5081207` - "fix(container): drop tracking refs from durable comments (#18571)

`check-ticket-archaeology` flagged three ticket refs I put in JSDoc and spec docblocks — one in
`container/Viewport.mjs`, one in each new spec. Both the repo-local lint step and the published
baseline guard rejected them, and both were right: durable comments describe behaviour, and a
ticket ref rots when the ticket closes or is renamed.

The measurements stay, because they describe what the code does. Only the provenance moves — it
already lives in the commit subject and the PR body, which is where the guard says to put it.

Swept the whole diff rather than the three flagged lines: no tracking ref remains in any file this
branch touches. The two `#18422`/`#18427`/`#18568` refs in `e2eCiSelection.mjs` are pre-existing
EXCLUSIONS **data** — `owner` fields naming who gets an arm back, not comments — and both guards
accept them.

Refs #18571"
- 2026-09-12T12:42:01Z @neo-opus-ada referenced in commit `2bd175a` - "fix(container): drop tracking refs from durable comments (#18571)

`check-ticket-archaeology` flagged three ticket refs I put in JSDoc and spec docblocks — one in
`container/Viewport.mjs`, one in each new spec. Both the repo-local lint step and the published
baseline guard rejected them, and both were right: durable comments describe behaviour, and a
ticket ref rots when the ticket closes or is renamed.

The measurements stay, because they describe what the code does. Only the provenance moves — it
already lives in the commit subject and the PR body, which is where the guard says to put it.

Swept the whole diff rather than the three flagged lines: no tracking ref remains in any file this
branch touches. The two `#18422`/`#18427`/`#18568` refs in `e2eCiSelection.mjs` are pre-existing
EXCLUSIONS **data** — `owner` fields naming who gets an arm back, not comments — and both guards
accept them.

Refs #18571"
- 2026-09-12T13:14:24Z @tobiu referenced in commit `10f627c` - "fix(container): a viewport's theme reaches the body, so one surface cannot inherit another's colour (#18571) (#18599)

* fix(container): a viewport's theme reaches the body, so one surface cannot inherit another's colour (#18571)

`main.addon.Stylesheet#addGlobalCss` stamps `Neo.config.themes[0]` onto `document.body` once, on
the main thread, before any worker runs — and nothing in the theme path rewrote it afterwards.
`DomAccess#setBodyCls` had exactly one caller in the whole repository, in `apps/shareddialog`.

The measured consequence is not "the app renders dark", which is what #18571 inferred. With
`portalTheme: neo-theme-neo-light` stored, the preference DOES arrive — `div#neo-viewport-1` carries
`neo-theme-neo-light`. Only the body stays on the boot theme, and because `background-color` resolves
from the theme class on the viewport while `color` INHERITS from the body's, the page renders a light
background carrying the dark theme's text: rgb(255,255,255) behind rgb(240,242,240), a contrast ratio
of 1.13:1. The content is invisible.

`Neo.container.Viewport` already owns a body class — `applyBodyCls` puts `neo-body-viewport` there
in `onConstructed`. It owns this one too; the alternative is a second publisher per app, which is
what `apps/shareddialog` had to become. Every declared theme is removed rather than only `oldValue`,
because the class the body carries at boot came from `themes[0]` and `oldValue` is `undefined` on the
first change, so it cannot name what is actually there. A viewport with no `theme` config returns
early: the no-preference default must not start depending on this method.

The e2e arms assert the two surfaces together, per AC-2 — an arm reading only the viewport passes on
exactly this defect. The liveness poll before each is load-bearing rather than ceremony: a portal that
never boots still shows `themes[0]` on the body and still fails the light arm for an unrelated reason.
The contrast arm carries the same floor in the passing control, so it cannot be a floor nothing clears.

`e2e/portal` sits outside `RUN_PATHS` because both existing specs there are declared exclusions.
Neither cause reaches this arm — they fail hosted on live previews and content routing, and this one
boots the portal root and reads a class — so it is named individually in the run set, and the guard's
synthetic fixture gains the matching entry.

Resolves #18571

* test(container): a theme change reaches the live item and the one added after it (#18571)

#18571 AC-5: the stored-preference read is a promise, so the theme can land after the tree is built.
`ItemTheme.spec.mjs` covered precedence at CONSTRUCTION only — nothing asserted what an item added
later resolves.

Both directions are in one arm because they fail independently: `container.Base#afterSetTheme`
propagates to LIVE items, while `createItem` resolves precedence for a new one. An arm covering only
the second passes while a live item keeps a stale theme.

This is a regression lock, not red-first evidence: the spec's import graph is `container/Base.mjs`
and `component/Base.mjs`, it never reaches `container/Viewport.mjs`, and it passes unchanged on the
base commit. The engine already had this right; nothing asserted it.

Refs #18571

* fix(container): drop tracking refs from durable comments (#18571)

`check-ticket-archaeology` flagged three ticket refs I put in JSDoc and spec docblocks — one in
`container/Viewport.mjs`, one in each new spec. Both the repo-local lint step and the published
baseline guard rejected them, and both were right: durable comments describe behaviour, and a
ticket ref rots when the ticket closes or is renamed.

The measurements stay, because they describe what the code does. Only the provenance moves — it
already lives in the commit subject and the PR body, which is where the guard says to put it.

Swept the whole diff rather than the three flagged lines: no tracking ref remains in any file this
branch touches. The two `#18422`/`#18427`/`#18568` refs in `e2eCiSelection.mjs` are pre-existing
EXCLUSIONS **data** — `owner` fields naming who gets an arm back, not comments — and both guards
accept them.

Refs #18571

* fix(container): a viewport reset to inheritance republishes its effective theme (#18571)

@neo-gpt's R1. `theme_: null` is a supported state meaning "inherit", and the previous `!theme` early
return read it as "never had one". After light → null the body kept light while `getTheme()` had already
resolved the app default — the same two-surface disagreement this publisher exists to remove, arriving
through its own fix. His control settled the attribution: the base Viewport keeps the body dark across
that reset, so the mismatch was introduced here.

Two changes, and the split matters. `syncBodyTheme` now publishes `getTheme()` rather than the `theme`
config: the two differ exactly on a reset, where the config says nothing and `getTheme()` names the
inherited answer. The CALLERS decide whether to publish — `afterSetTheme` on `value || oldValue`, so a
reset publishes and a viewport that never had a theme stays silent, and `onConstructed` only when an
explicit theme is present. Untouched-default and `applyBodyCls: false` behaviour is unchanged, which
were the two boundaries the review named as not-to-move.

Red-proof against the unfixed Viewport: 1 failed / 4 passed, the failure being the reset arm, with the
recorder showing a single publication stranded on light. The other four arms stay green there, so the
new arm discriminates rather than failing wholesale.

The comment that justified the early return said an unconfigured viewport inherits the theme the body
already carries. True at untouched boot, false once this method has published something else — a
guarantee written for one state and applied to two.

Refs #18571"
### @neo-opus-ada - 2026-09-14T18:30:46Z

**The divergence reproduces on today's `dev`, deterministically — so there is now a hosted reproducer, which this ticket never had.**

[Run 34880690522](https://github.com/neomjs/neo/actions/runs/34880690522) — a `workflow_dispatch` of the e2e tier at `dev@f8425236af`, `specs=test/playwright/e2e/portal/LearnLinkRoutingNL.spec.mjs`, `repeat-each=3`. The run step executed (not skipped) and the dispatch's name guard found every requested spec in the listing, so this is not a vacuous selection.

| arm | result |
|---|---|
| `:26` a relative guide link is rendered as a route | 3/3 pass |
| `:40` a fragment-bearing link keeps its anchor | 3/3 pass |
| `:60` control: the harness can click the app's own navigation | 3/3 pass |
| `:77` **clicking a rewritten link arrives at its destination** | **3/3 fail** — 7.8s, 6.7s, 6.7s at the default window; `Received string: "Dock Layouts: One Application, Many Windows"` |

**What this rules out.** Everything I measured on 09-07 predates the mermaid changes that landed 09-09/09-10 — the ESM import and the bundle whose UMD wrappers cannot reach Monaco's loader. The CI job builds that bundle (`bundle-browser-deps` = parse5 + marked + mermaid), and the divergence survives it. `rewriteLinks` is unchanged since 09-06. So the failure is not an artifact of the old mermaid loading, and not a missing browser bundle on the runner.

**What it changes about the next step.** The previous comment ended on "a hosted probe reporting from inside the app", with no way to iterate it. 3/3 means a single dispatch — about a minute of test time — now answers whether any candidate change moves the arm, without a PR. The next step is a probe that localizes where the route-driven update is lost between the router, the controller and the content component, still not a guessed repair.

**One observation, stated as an observation and not as a cause.** This arm's source page is `DockLayouts`, the guide #18675 reports rendering its whole body twice. Two different symptoms on the same page and the same component path are worth ruling in or out together; I have not measured any link between them.

The `EXCLUSIONS` entry stays as written — it is still accurate.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code


- 2026-09-14T18:47:46Z @neo-opus-ada cross-referenced by #18704
### @neo-opus-ada - 2026-09-14T18:48:19Z

**Root cause filed as #18704.** `dist/highlight` is never built in CI, so the worker's highlight import fails. `DockLayoutsAdoption` has 14 code fences that survive extraction, and `Markdown#render()` awaits highlighting them before `set({html})`, so the article keeps the source guide. `DockLayouts` itself has none, which is why the href arms pass. This ticket closes with #18704's PR once arm `:77` goes green hosted.

⚖️ **Ada** · `@neo-opus-ada`


- 2026-09-14T19:11:16Z @neo-opus-ada cross-referenced by PR #18706
- 2026-09-14T21:05:29Z @tobiu closed this issue

