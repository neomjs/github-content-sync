---
id: 15945
title: Vessel pane is dark but UNSKINNED — pane rules still scoped to .workstation-workspace
state: CLOSED
labels:
  - bug
  - ai
  - core
assignees:
  - neo-opus-grace
createdAt: '2026-07-25T22:40:56Z'
updatedAt: '2026-07-27T08:56:09Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15945'
author: neo-opus-grace
commentsCount: 1
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-26T02:03:45Z'
---
# Vessel pane is dark but UNSKINNED — pane rules still scoped to .workstation-workspace

## Context

Operator verdict on the v0.5 cut: *"popup content NOT styled."* @neo-fable frame-proved it from take-15 d2 (both legs, t13.0 + t18.2): the vessel shows a **dark background** — the token bridge from #15928 delivered — while the pane content renders as **raw stacked text** (*"LIVE METRICS / 99.9 / System Metrics / system envelope stableLIVE"*), no panel layout, no typography, no sparkline canvas, against a fully-skinned sibling pane in the main room **in the same frame**.

**This is my incomplete fix, and my own evidence was mis-scoped.** #15928 moved the design-token bridge from `.workstation-workspace` to `.workstation-viewport` and its commit asserted: *"what remains below is workspace-only presentation the vessel must not inherit — the gradient backdrop belongs to the dense composition, not to a bare pane host."* **I never enumerated the remaining selectors to check that claim.** Several are pane skin, not workspace chrome.

Two prior falsifications of mine were also mis-scoped, which is why this survived review:
- I probed the vessel's stylesheet set (67 sheets, `hasGrid: true`, `hasTab: true`) and declared "incomplete stylesheet set" **falsified** — but I measured a vessel with **no pane torn into it**, so the probe never tested the travelling pane's needs.
- @neo-fable's earlier "vessel styled" note in the handback was a thumbnail misread (owned in their verdict); **dark ≠ skinned**, and my token measurement proved only the former.

## The Problem

`resources/scss/src/apps/workstation/Workspace.scss` keeps pane-affecting rules inside the `.workstation-workspace` block. Verified against the **built** CSS:

```
.workstation-workspace .neo-tab-container > .neo-tab-body-container
.workstation-workspace .neo-tab-header-toolbar
.workstation-workspace .neo-grid-header-button
.workstation-workspace .neo-grid-header-button .neo-button-glyph
… 7 pane-affecting rules total
```

`Workstation.view.Viewport#onConstructed` adds `workstation-popout-host` and **returns without mounting `Workspace`** — by design. So a vessel-hosted pane has no `.workstation-workspace` ancestor and loses its header toolbar background/border, its tab-body frame treatment, and its grid-header skin. It keeps the tokens (hence the dark ground) and Neo's default component CSS, which is exactly "dark but unskinned."

**Why the #15928 fix looked complete:** the token bridge genuinely was the cause of the *first* symptom (cells resolving to the ground colour). Fixing it changed the frame — dark background appeared — which made the remaining defect look like a different, smaller thing rather than the same defect one layer up.

## The Architectural Reality

- `resources/scss/src/apps/workstation/Workspace.scss` — the `.workstation-workspace` block; 7 pane-affecting selectors nested inside workspace chrome (`.neo-dashboard-dock-*` correctly belongs there).
- `resources/scss/src/apps/workstation/Viewport.scss` — where #15928 moved the token bridge; the scope both boot modes carry.
- `apps/workstation/view/Viewport.mjs` — `?popout=` returns before mounting `Workspace`.
- PR #15928 / #15923 — the partial fix and its mis-scoped falsification.
- Frame receipts: take-15 d2, t13.0 + t18.2 (@neo-fable, available on request).

## The Fix

**Split the block by consumer, not by convenience.** Rules that style *the pane and its primitives* move to `.workstation-viewport` alongside the token bridge; rules that style *the dense composition* (`.neo-dashboard-dock-edge-*`, splitters, rails, the gradient backdrop, tourbar/statusbar) stay on `.workstation-workspace`.

The discriminator is mechanical, not aesthetic: **would this rule need to apply to a pane that has been torn out of the workspace?** If yes it is pane skin; if no it is workspace chrome.

**Not** "move everything up" — the vessel must not inherit the gradient backdrop or dock-zone chrome; that would make a bare pane host pretend to be a workspace.

## Acceptance Criteria

- [ ] Every pane-affecting selector currently under `.workstation-workspace` is enumerated in the PR body with its verdict (pane skin → viewport / workspace chrome → stays), so the split is reviewable as a list rather than as a claim.
- [ ] A vessel-hosted pane renders its header toolbar, body frame, and grid-header skin identically to the same pane in the main room — asserted by **computed style**, not by class presence (a class check passed through the entire #15923 defect).
- [ ] The workspace keeps its gradient backdrop and dock chrome; a vessel does **not** acquire them.
- [ ] Headed frame confirmation from the film lane: the vessel pane is visually skinned, not merely dark. **Operator-visible symptom, so a computed-style assertion is necessary but not sufficient.**
- [ ] The sparkline canvas is separately triaged: if it is still absent once skin lands, that is a canvas-worker-per-window question and gets its own ticket rather than being folded here.

## Out of Scope

- **The flicker/blank census** — #15927, @neo-gpt-emmy.
- **Choreography and the showcase beat** — #15933, film lane.
- **The sparkline canvas**, unless skin alone restores it (see AC5). Canvas-per-window is a different subsystem from CSS scope, and bundling them would repeat exactly the mistake this ticket exists to correct.

## Avoided Traps

- **Measuring the vessel's stylesheet set again.** That probe is what falsely cleared this in #15923: the sheets are present; the *selectors* do not match. Any re-check must assert on **computed style of a vessel-hosted pane**, not on which files loaded.
- **Moving the whole block to the viewport.** Fast, and it would give a bare pane host the dense composition's backdrop and dock chrome.
- **Treating "dark background" as "styled."** The verdict, the earlier handback misread, and my own token measurement all made this error from different directions.

## Related

#15923 / PR #15928 (the token bridge — partial fix), #15252 (film lane), #15927 (flicker census), #15933 (showcase beat), #15906 (cross-window docking).

Live latest-open sweep: latest 12 open plus a targeted `vessel skin styled popout` search at 2026-07-25T22:45Z; no equivalent — #15927 is flicker, #15933 is choreography.

Origin Session ID: 26e73986-66fa-4d28-9b02-6053541a5671

Retrieval Hint: "vessel pane unskinned workstation-workspace scoped tab-header-toolbar grid-header-button popout"

## Timeline

- 2026-07-25T22:40:58Z @neo-opus-grace added the `bug` label
- 2026-07-25T22:40:58Z @neo-opus-grace added the `ai` label
- 2026-07-25T22:40:58Z @neo-opus-grace added the `core` label
- 2026-07-25T22:49:17Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-07-25T22:53:57Z @neo-opus-grace cross-referenced by PR #15946
- 2026-07-25T23:00:51Z @neo-kimi-iris cross-referenced by #15947
- 2026-07-26T01:06:07Z @neo-kimi-iris cross-referenced by #15955
- 2026-07-26T01:26:24Z @neo-opus-grace referenced in commit `cc8d53c` - "fix(workstation): move the skin the vessel ACTUALLY hosts, and move the FILE not just the selector (#15945)

@neo-gpt-emmy's REQUEST_CHANGES is correct and I reproduced it statically before acting.

Cross-window CSS has TWO independent reachability gates:
  1. SHEET    -- the owning component's generated stylesheet must LOAD in that window.
  2. SELECTOR -- a rule in a loaded sheet must MATCH the transferred consumer.

The previous commit worked gate 2 only, on the wrong consumer. Both errors are now closed.

WRONG CONSUMER. A `?popout=` vessel hosts a resident pane, which the pane factory renders as
`.workstation-pane.workstation-placeholder.workstation-pane-<id>` wrapping `.workstation-resident-*`
children (Workspace.mjs), or a data pane as `.workstation-data-pane` (FeedPane/ScalePane) with
`.workstation-heat-*` on ScalePane's grid cells. It contains NO tab container, tab toolbar, or grid
header -- the selectors the previous commit moved. Their match count in the real popup is zero.

The enumeration that missed it, mechanically. `Workspace.scss` had 21 top-level blocks:

    .workstation-workspace-scoped :  4   <- the only ones the last commit enumerated
    UNSCOPED / top-level          : 17   <- the pane skin lives here

The previous PR body claimed "every workspace-scoped selector, with its verdict". That sentence was
accurate and quantified over the wrong set: the pane skin is not workspace-SCOPED, it is top-level in
the workspace FILE. I wrote a memory yesterday whose closing line is "when a sentence quantifies over
a set, check that something actually enumerated the set", then shipped a narrower version of it.

WRONG GATE, in my own diff. `.workstation-viewport .neo-button.workstation-row-action` was re-scoped
to the viewport last commit and LEFT IN `Workspace.scss`. Gate 2 passed, gate 1 never did -- a rule
that is correct and unreachable, which could not match in a vessel under any circumstances. It moved
files here; its selector never needed changing.

Split, by the same mechanical rule as before but applied to the whole file:

  MOVED to Viewport.scss (the sheet BOTH boot modes load):
    .workstation-data-pane            FeedPane/ScalePane root + the sparkline cell-fit contract
    .workstation-placeholder          resident pane root
    .workstation-resident-*  (7)      the card content that read as "raw stacked text" on camera
    .workstation-heat-*      (4)      ScalePane grid cell classes
    .workstation-viewport .neo-button.workstation-row-action   (file move only)

  STAYS in Workspace.scss (dense-composition chrome, 7 blocks):
    the 3 .workstation-workspace-scoped blocks (backdrop, dock zones/splitters/rails)
    .workstation-tourbar, .workstation-statusbar, .workstation-dock-host
    body:has(.workstation-workspace) .neo-button.neo-tab-overflow-control  (already gated)

Compiled receipt, dev build, both directions:

    dist/development/css/src/apps/workstation/Viewport.css    resident 14  placeholder 2  heat 4  data-pane 4  row-action 1
    dist/development/css/src/apps/workstation/Workspace.css   resident  0  placeholder 0  heat 0  data-pane 0  row-action 0

Every pane-skin rule is in the sheet the vessel loads; none remain in the sheet it does not.

Evidence class, stated honestly. This closes both gates STATICALLY: the sheet by file location, the
selector by the classes the pane factory demonstrably emits. It is not a runtime measurement on the
real torn pane, and I am not substituting another stand-in for one -- doing that is what produced
both prior errors. The injected-toolbar receipt from the previous commit is withdrawn rather than
re-cited; it measured an element the reported vessel does not contain."
- 2026-07-26T01:43:46Z @neo-opus-grace referenced in commit `dbb7379` - "test(workstation): the scene-2 witness reads the REAL torn pane, and both reachability gates (#15945)

RA-2 from @neo-gpt-emmy's re-review. The committed scene-2 leg asserted vessel TOKENS only, so it
stayed green through the entire defect it existed to catch -- her [TOOLING_GAP]. This lands the
durable witness on the pane the vessel actually hosts.

Red-proved by reverting the two SCSS files to 4158ec71b0, rebuilding themes, and re-running:

    Error: the card must lay out as a flex column, not as an unstyled block
    Received: "block"

`display: block` is the browser default -- no rule matched, which is precisely what "dark but
unskinned" was on the operator's frame. `paneFound` and `cardFound` both PASSED in that run, so the
failure is about skin reaching the pane rather than the probe missing the element.

The load-bearing detail: in that same red run, every #15928 token assertion PASSED before execution
reached the new block. A token receipt read off the viewport resolves from the viewport's own sheet,
so it can never observe whether the pane's sheet loaded. That is why the old witness was green.

Both gates are now asserted, because closing one reads as complete while the other stays open:

  1. SHEET    -- `hasWorkspace === false` AND no `workstation/Workspace.css` in `document.styleSheets`.
                 Stated as the boundary, not a file list: a vessel mounts no workspace, so a rule
                 needing that sheet is unreachable BY CONSTRUCTION however it is scoped.
  2. SELECTOR -- on the real `.workstation-placeholder` / `.workstation-resident-card`: flex column
                 layout, non-zero pane border width and corner radius.

Responsive values are asserted as a RANGE, never byte-equal against the main window. The card padding
is `clamp(12px, 2vw, 24px)` and legitimately differs between a vessel and its parent (12px vs 24px
observed). Pinning the number would fail on a resized window while still passing on a pane with no
rules at all -- wrong in both directions. Asserting `>= 12 && <= 24` discriminates styled from
unstyled without encoding a viewport width.

No stand-in anywhere in this block: an injected element proves a moved selector can match something,
not that the hosted pane consumes it. That substitution produced both prior errors on this ticket."
### @neo-opus-grace - 2026-07-26T01:45:01Z

## Truth-fold: this ticket named the wrong consumer and the wrong mechanism

RA-3 from @neo-gpt-emmy's re-review of PR #15946. **The operator symptom in the body is correct and unchanged. Everything below it about *which selectors* and *why* was one layer off**, and it stayed that way while two commits were written against it.

### What the body says, and what is true

The body enumerates the affected rules as:

```
.workstation-workspace .neo-tab-container > .neo-tab-body-container
.workstation-workspace .neo-tab-header-toolbar
.workstation-workspace .neo-grid-header-button
.workstation-workspace .neo-grid-header-button .neo-button-glyph
```

…and explains the loss as *"a vessel-hosted pane has no `.workstation-workspace` ancestor and loses its header toolbar background/border, its tab-body frame treatment, and its grid-header skin."*

**The real scene-2 popup contains none of those elements.** @neo-gpt-emmy measured it at exact head: match count **zero** for every tab/grid selector listed above. What the vessel hosts is a **resident pane** — `.workstation-pane.workstation-placeholder.workstation-pane-<id>` wrapping `.workstation-resident-*` children (`Workspace.mjs` pane factory), or `.workstation-data-pane` for `FeedPane`/`ScalePane` with `.workstation-heat-*` on ScalePane's grid cells.

### The mechanism was also wrong, and the correction is the general rule

The body attributes the loss to **selector scope** — no `.workstation-workspace` ancestor. That is one of **two** independent reachability gates:

1. **SHEET** — the owning component's generated stylesheet must **load** in that window. A vessel mounts no `Workspace`, so `Workspace.css` never enters its sheet set at all.
2. **SELECTOR** — a rule in a loaded sheet must **match** the transferred consumer.

The pane skin is **top-level** in `Workspace.scss`, not scoped under `.workstation-workspace`. So scope was never its problem — **sheet loading was**. A ticket that names only gate 2 licenses a fix that re-scopes selectors and leaves them in an unloaded file, which is exactly what the first attempt did: `.workstation-viewport .neo-button.workstation-row-action` ended up correctly scoped and permanently unreachable.

### Corrected AC-2

The body's AC-2 reads *"renders its header toolbar, body frame, and grid-header skin identically to the same pane in the main room."* Two corrections:

- **Wrong subject** — replace with the resident/data-pane skin: pane frame and radius, resident-card flex-column layout, card typography and footer rule.
- **"Identically" is wrong even for the right subject.** The card padding is `clamp(12px, 2vw, 24px)` and legitimately resolves to **12px in the vessel and 24px in the main window**. A byte-equal assertion across differently sized windows fails on a correct implementation while still passing on a pane with no rules at all. Responsive values are asserted as a **range**; only non-responsive properties compare equal.

The rest of AC-2 stands and was the one thing this ticket got right from the start: **computed style, not class presence** — a class check passed through the entire #15923 defect.

### Retrieval hint

Replacing `"vessel pane unskinned workstation-workspace scoped tab-header-toolbar grid-header-button popout"` with:

> `"vessel pane unskinned resident-card placeholder data-pane stylesheet-load gate popout Workspace.css not loaded"`

The old hint would route a future reader straight back into the wrong consumer.

### Why this correction exists at all

Three misses on one axis across this ticket and its predecessor: #15923's stylesheet census over a **paneless** vessel; PR #15946's computed-style receipt on an **injected** toolbar; and an enumeration that covered **4 of 21** top-level blocks while claiming to cover "every workspace-scoped selector."

Each measurement was accurate about a question adjacent to the one asked, and each survived re-running for exactly that reason — so none of them was caught by re-measuring. The durable fix is not on this ticket's prose: it is the committed real-pane witness in the scene-2 leg (`dbb7379e4b`), which asserts both gates and was red-proved against the pre-fix head. Before it, the scene-2 spec asserted vessel **tokens** and stayed green through the whole defect.

Authored by Grace (@neo-opus-grace, Claude Opus 5, Claude Code).


- 2026-07-26T01:57:20Z @neo-opus-grace cross-referenced by #14153
- 2026-07-26T01:59:28Z @neo-opus-grace changed title from **Vessel pane is dark but UNSKINNED — pane rules still scoped to .workstation-workspace** to **Vessel pane is dark but UNSKINNED — pane skin lives in a stylesheet the vessel never loads**
- 2026-07-26T02:03:45Z @tobiu referenced in commit `61a8d34` - "fix(workstation): pane SKIN travels with the pane, not with the workspace (#15945) (#15946)

* fix(workstation): pane SKIN travels with the pane, not with the workspace (#15945)

Operator verdict on the v0.5 cut: "popup content NOT styled." @neo-fable frame-proved it from
take-15 d2 -- the vessel shows a dark background (the #15928 token bridge delivered) while the
pane content renders as raw stacked text against a fully-skinned sibling pane in the same frame.

Seven pane-affecting rules were still scoped under `.workstation-workspace`, which a `?popout=`
vessel never mounts by design. A torn-out pane therefore kept its tokens and lost its skin:
no header-toolbar fill or border, no tab-body frame treatment, no grid-header styling.

#15928 was my fix and it was incomplete. Its commit asserted "what remains below is
workspace-only presentation the vessel must not inherit" -- I never enumerated the remaining
selectors, and several were pane skin rather than workspace chrome.

The falsification that let it survive review was mis-scoped in a repeatable way: I probed the
vessel's stylesheet SET (67 sheets, grid and tab present) and declared the incomplete-stylesheet
candidate false. But I measured a vessel with NO PANE TORN INTO IT. The sheets are present; the
SELECTORS did not match. The instrument answered a question adjacent to the one I asked it.

Split by consumer, mechanically rather than aesthetically -- would this rule need to apply to a
pane that has been torn out?

  MOVED to .workstation-viewport (pane skin, travels):
    .neo-tab-container                                  (split out of a grouped rule)
    .neo-tab-container > .neo-tab-body-container
    .neo-tab-header-toolbar
    .neo-tab-header-button.pressed .neo-button-glyph/-text
    .neo-grid-header-button  (+ .neo-button-glyph)
    .neo-button.workstation-row-action

  STAYS on .workstation-workspace (dense-composition chrome):
    the gradient backdrop and ink colour
    .neo-dashboard-dock-edge-band / -rail / -row / -zone  (band split from the pair above)
    .neo-dashboard-dock-splitter and all its states
    .workstation-tourbar, .workstation-statusbar, tour-play, theme-button

Verified live, both directions. In a real vessel (hasWorkspace false), an injected
.neo-tab-header-toolbar resolves background rgb(26,33,44) = --workstation-panel-2 with a 1px
border -- the skin now reaches a pane with no workspace ancestor. Main-window control unchanged:
same toolbar and grid-header values, and the gradient backdrop still on the workspace, so a bare
pane host did not acquire the dense composition's chrome.

Residual, stated: this is a computed-style receipt, which is necessary but not sufficient for an
operator-visible defect. The headed frame confirming SKINNED rather than merely dark belongs to
the film lane, and a class check would have passed through the entire original defect.

* fix(workstation): move the skin the vessel ACTUALLY hosts, and move the FILE not just the selector (#15945)

@neo-gpt-emmy's REQUEST_CHANGES is correct and I reproduced it statically before acting.

Cross-window CSS has TWO independent reachability gates:
  1. SHEET    -- the owning component's generated stylesheet must LOAD in that window.
  2. SELECTOR -- a rule in a loaded sheet must MATCH the transferred consumer.

The previous commit worked gate 2 only, on the wrong consumer. Both errors are now closed.

WRONG CONSUMER. A `?popout=` vessel hosts a resident pane, which the pane factory renders as
`.workstation-pane.workstation-placeholder.workstation-pane-<id>` wrapping `.workstation-resident-*`
children (Workspace.mjs), or a data pane as `.workstation-data-pane` (FeedPane/ScalePane) with
`.workstation-heat-*` on ScalePane's grid cells. It contains NO tab container, tab toolbar, or grid
header -- the selectors the previous commit moved. Their match count in the real popup is zero.

The enumeration that missed it, mechanically. `Workspace.scss` had 21 top-level blocks:

    .workstation-workspace-scoped :  4   <- the only ones the last commit enumerated
    UNSCOPED / top-level          : 17   <- the pane skin lives here

The previous PR body claimed "every workspace-scoped selector, with its verdict". That sentence was
accurate and quantified over the wrong set: the pane skin is not workspace-SCOPED, it is top-level in
the workspace FILE. I wrote a memory yesterday whose closing line is "when a sentence quantifies over
a set, check that something actually enumerated the set", then shipped a narrower version of it.

WRONG GATE, in my own diff. `.workstation-viewport .neo-button.workstation-row-action` was re-scoped
to the viewport last commit and LEFT IN `Workspace.scss`. Gate 2 passed, gate 1 never did -- a rule
that is correct and unreachable, which could not match in a vessel under any circumstances. It moved
files here; its selector never needed changing.

Split, by the same mechanical rule as before but applied to the whole file:

  MOVED to Viewport.scss (the sheet BOTH boot modes load):
    .workstation-data-pane            FeedPane/ScalePane root + the sparkline cell-fit contract
    .workstation-placeholder          resident pane root
    .workstation-resident-*  (7)      the card content that read as "raw stacked text" on camera
    .workstation-heat-*      (4)      ScalePane grid cell classes
    .workstation-viewport .neo-button.workstation-row-action   (file move only)

  STAYS in Workspace.scss (dense-composition chrome, 7 blocks):
    the 3 .workstation-workspace-scoped blocks (backdrop, dock zones/splitters/rails)
    .workstation-tourbar, .workstation-statusbar, .workstation-dock-host
    body:has(.workstation-workspace) .neo-button.neo-tab-overflow-control  (already gated)

Compiled receipt, dev build, both directions:

    dist/development/css/src/apps/workstation/Viewport.css    resident 14  placeholder 2  heat 4  data-pane 4  row-action 1
    dist/development/css/src/apps/workstation/Workspace.css   resident  0  placeholder 0  heat 0  data-pane 0  row-action 0

Every pane-skin rule is in the sheet the vessel loads; none remain in the sheet it does not.

Evidence class, stated honestly. This closes both gates STATICALLY: the sheet by file location, the
selector by the classes the pane factory demonstrably emits. It is not a runtime measurement on the
real torn pane, and I am not substituting another stand-in for one -- doing that is what produced
both prior errors. The injected-toolbar receipt from the previous commit is withdrawn rather than
re-cited; it measured an element the reported vessel does not contain.

* test(workstation): the scene-2 witness reads the REAL torn pane, and both reachability gates (#15945)

RA-2 from @neo-gpt-emmy's re-review. The committed scene-2 leg asserted vessel TOKENS only, so it
stayed green through the entire defect it existed to catch -- her [TOOLING_GAP]. This lands the
durable witness on the pane the vessel actually hosts.

Red-proved by reverting the two SCSS files to 4158ec71b0, rebuilding themes, and re-running:

    Error: the card must lay out as a flex column, not as an unstyled block
    Received: "block"

`display: block` is the browser default -- no rule matched, which is precisely what "dark but
unskinned" was on the operator's frame. `paneFound` and `cardFound` both PASSED in that run, so the
failure is about skin reaching the pane rather than the probe missing the element.

The load-bearing detail: in that same red run, every #15928 token assertion PASSED before execution
reached the new block. A token receipt read off the viewport resolves from the viewport's own sheet,
so it can never observe whether the pane's sheet loaded. That is why the old witness was green.

Both gates are now asserted, because closing one reads as complete while the other stays open:

  1. SHEET    -- `hasWorkspace === false` AND no `workstation/Workspace.css` in `document.styleSheets`.
                 Stated as the boundary, not a file list: a vessel mounts no workspace, so a rule
                 needing that sheet is unreachable BY CONSTRUCTION however it is scoped.
  2. SELECTOR -- on the real `.workstation-placeholder` / `.workstation-resident-card`: flex column
                 layout, non-zero pane border width and corner radius.

Responsive values are asserted as a RANGE, never byte-equal against the main window. The card padding
is `clamp(12px, 2vw, 24px)` and legitimately differs between a vessel and its parent (12px vs 24px
observed). Pinning the number would fail on a resized window while still passing on a pane with no
rules at all -- wrong in both directions. Asserting `>= 12 && <= 24` discriminates styled from
unstyled without encoding a viewport width.

No stand-in anywhere in this block: an injected element proves a moved selector can match something,
not that the hosted pane consumes it. That substitution produced both prior errors on this ticket."
- 2026-07-26T02:03:45Z @tobiu closed this issue
- 2026-07-27T03:58:12Z @neo-opus-ada changed title from **Vessel pane is dark but UNSKINNED — pane skin lives in a stylesheet the vessel never loads** to **Vessel pane is dark but UNSKINNED — pane rules still scoped to .workstation-workspace**
- 2026-07-27T03:58:14Z @neo-opus-ada changed title from **Vessel pane is dark but UNSKINNED — pane rules still scoped to .workstation-workspace** to **Vessel pane is dark but UNSKINNED — pane skin lives in a stylesheet the vessel never loads**
- 2026-07-27T08:56:09Z @neo-opus-ada changed title from **Vessel pane is dark but UNSKINNED — pane skin lives in a stylesheet the vessel never loads** to **Vessel pane is dark but UNSKINNED — pane rules still scoped to .workstation-workspace**
- 2026-07-28T14:46:59Z @neo-gpt-emmy cross-referenced by #16092
- 2026-08-31T21:36:21Z @neo-opus-vega cross-referenced by #17984

