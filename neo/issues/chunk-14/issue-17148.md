---
id: 17148
title: An optional DevIndex intake denial freezes all corpus publication
state: CLOSED
labels:
  - bug
  - ai
  - regression
  - architecture
  - build
assignees:
  - neo-opus-ada
createdAt: '2026-08-15T07:39:51Z'
updatedAt: '2026-08-15T08:51:02Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17148'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-08-15T08:51:02Z'
---
# An optional DevIndex intake denial freezes all corpus publication

## Context

`#17131` (the standing DATA-SYNC-ALARM) has been red since **2026-08-14T11:49:16Z** — 15 consecutive scheduled failures on `dev` at filing time. The working hypothesis on pickup was that a merged PR broke it. **That is falsified** (evidence below): nothing the failing stage executes changed in the regression window. The pipeline is red because of a GitHub-side condition on the DevIndex intake repositories — but the *blast radius* of that condition is a Neo defect, and that defect is what this ticket fixes.

**Observation vs inference.** Observed: the failing run/step/error, the last-green control, the window diff, and the live API behaviour of the intake repos. Inferred: the GitHub-side cause of the stargazer 404 — unresolved, and deliberately not this ticket's target.

### Regression window, pinned

| | run | head | UTC |
|---|---|---|---|
| last green | `31797752252` | `45570db8e` | 2026-08-14T11:49:16Z |
| first red | `31804178775` | `dd25854d4` | 2026-08-14T13:19:19Z |

Every failure since fails the same job step — `6: Run bounded Data Sync emission and publish` — at the same stage, `DevIndex Opt-In`:

```
[GitHub] [OptIn Stars] GraphQL Query Failed: GraphQL Query Errors: Resource not accessible by integration
[DataSync] stage "DevIndex Opt-In" failed with an UNRECOGNIZED error class; ... Declared credential scope: `intake`.
```

### Why "a merged PR broke it" is falsified

- `git diff --stat 45570db8e..dd25854d4` touches **only** generated data-sync output (`resources/content/**`, `apps/portal/resources/data/**`, `sitemap.xml`), one learn doc, and `test/playwright/**` specs. **Zero** changes to `apps/devindex/**`, `buildScripts/dataSync*.mjs`, or `.github/workflows/**`.
- `apps/devindex/services/OptIn.mjs` last changed **2026-02-22**; `apps/devindex/services/GitHub.mjs` **2026-07-23**; `.github/workflows/data-sync-pipeline.yml` **2026-07-28**. Each mechanism predates the window it would have to explain.
- Control: in the last green run the *identical* query ran under the *identical* `intake` credential and succeeded — `12:05:23Z` → `[OptIn] No new opt-in requests found.` Same code, same credential, 90 minutes apart.

### What is actually denied

The `intake` installation is intact: `assertDataSyncAccess`'s `repository{id}` probe reports **reachable** in every red run including the latest. Only the `stargazers` connection is denied. Independently reproducible outside the pipeline with a broad token:

| probe | `neomjs/neo` | `neomjs/devindex-opt-in` | `neomjs/devindex-opt-out` |
|---|---|---|---|
| GraphQL `stargazers` | returns edges | `edges: []` | — |
| REST `/stargazers` | 200 (3 rows) | **404** | **404** |
| REST repo root | 200 | 200 (`stargazers_count: 16`) | 200 |

A public, unarchived, non-disabled repository reporting 16 stars whose stargazer list 404s is a GitHub-side condition, not a Neo regression. **Resolving that needs operator/admin action and is out of scope here.**

## The Problem

`emitGeneratedData` runs its stages sequentially and rethrows on the first stage failure (`buildScripts/dataSyncPipeline.mjs:648`). `DevIndex Opt-In` is stage 3 of 7. When it throws:

- stages 4–7 never run — including **`content indexes and SEO`** (stage 7), which is what makes the corpus consumable;
- the publish path below the loop is never reached, so the corpus generated by stage 2 is **discarded**;
- nothing commits, so `resources/content/**` on `dev` stays frozen.

An **optional enrichment read for the DevIndex opt-in feature** therefore takes down **corpus publication for the whole repository**. At filing, mirrors/KB are ~19h stale and every downstream consumer (portal data, KB ingestion) is reading a frozen corpus. That coupling is the defect.

The mechanism to decouple it **already exists and is already used**: `publishGeneratedProgressOnFailure`. Stage 2 carries it. A stage that sets it defers its error, lets the remaining stages run, publishes what was generated, and *then* rethrows (`:742-744`, `:844-849`) — the run still goes RED. It is precisely "publish what we have, then fail loudly"; it was simply never applied to the intake stages.

## The Architectural Reality

- `buildScripts/dataSyncPipeline.mjs:67-122` — `emissionCommands`, the 7-stage table. Only stage 2 sets `publishGeneratedProgressOnFailure`.
- `buildScripts/dataSyncPipeline.mjs:606-655` — the emission loop; `:639-646` defer vs `:648` throw.
- `buildScripts/dataSyncPipeline.mjs:742-744`, `:844-849` — `deferredError` is rethrown at both exits, so deferral is **not** suppression.
- `buildScripts/dataSyncPreflight.mjs:33-36` — `REQUIRED_REPOSITORIES` carries `purpose: 'OptIn stargazer read'`.
- `buildScripts/dataSyncPreflight.mjs:49-84` — `probeRepository` issues `repository(owner,name){id}` only. The `purpose` string **claims a stargazer read the probe never performs**, so the preflight logged `neomjs/devindex-opt-in reachable (OptIn stargazer read)` in the very runs whose stargazer read was denied 12 minutes later.

Second-order cost: because the preflight passed, the failure surfaced 12 minutes into the run as `UNRECOGNIZED error class` instead of at minute 1 with a named cause.

## The Fix

1. **Decouple enrichment from publication.** Set `publishGeneratedProgressOnFailure: true` on the four DevIndex intake stages (`DevIndex Opt-In`, `Opt-Out`, `Spider`, `Updater`). The corpus then publishes and the run still fails on the deferred error.
2. **Preserve every deferred failure.** With four stages able to defer, a single `deferredError` slot silently drops all but the last. Accumulate and rethrow an aggregate so the run names every stage that failed.
3. **Make the preflight probe what it claims.** Extend `probeRepository` so the `devindex-opt-in` probe exercises the `stargazers` connection it advertises. A denial then fails fast at minute 1 naming repo and connection.

Deliberately **NOT** doing: adding `Resource not accessible by integration` to `OptIn.mjs`'s NOT_FOUND skip branch. That converts a real permission regression into a silent no-op and the opt-in feature rots undetected — a carve-out that quiets the guard opens a silent channel.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `emissionCommands[].publishGeneratedProgressOnFailure` | `buildScripts/dataSyncPipeline.mjs:610` (exists, default `false`) | `true` on the 4 DevIndex intake stages | unchanged for non-intake stages | JSDoc at the table | unit spec: intake failure still publishes **and** still throws |
| `emitGeneratedData` return `{deferredError}` | `buildScripts/dataSyncPipeline.mjs:580, :653` | carry all deferred stage failures, not the last | single-failure shape unchanged when only one defers | `@returns` JSDoc | unit spec: 2 deferred stages ⇒ both named |
| `probeRepository({owner,name,connection,token})` | `buildScripts/dataSyncPreflight.mjs:49` | NEW required `connection` arg; selects that connection AND requires it to resolve | denial ⇒ same `{ok:false, reason}` shape | JSDoc | unit specs: connection is selected; partial denial (id present, connection null) is a failure |
| `REQUIRED_REPOSITORIES[].connection` | `buildScripts/dataSyncPreflight.mjs:33` | NEW field; names the connection the consuming stage reads, so probe and `purpose` cannot drift | absent ⇒ probe cannot select, treated as failure | JSDoc | unit spec: `stargazers` for opt-in, `issues` for opt-out |
| `assertDataSyncAccess` failure entries | `buildScripts/dataSyncPreflight.mjs:156` | entries now carry `connection` | additive; existing fields unchanged | inline | unit spec: per-repository classification preserved |
| `denialRemedy(connection)` (module-private) | `buildScripts/dataSyncPreflight.mjs` | NEW; remediation derives from the refused connection instead of one fixed Issues/Metadata string | unmapped connection ⇒ says so, never inherits another's advice | JSDoc | unit spec: stargazer denial must NOT print the Issues remedy |
| `emitGeneratedData` preflight handling | `buildScripts/dataSyncPipeline.mjs:594` | a preflight denial is DEFERRED, not thrown; no stage is skipped | `DATA_SYNC_PREFLIGHT_ONLY=true` still throws directly | JSDoc on `options.preflight` | unit specs: denial defers; Opt-Out/Spider/Updater still execute |

## Decision Record impact

`none` — this restores the stage-isolation property the existing `publishGeneratedProgressOnFailure` primitive already encodes; it introduces no new architectural authority.

## Acceptance Criteria

- [ ] A `DevIndex Opt-In` stage failure no longer aborts emission: stages 4–7 still run and the publish path is reached.
- [ ] With an intake stage failing, generated corpus changes are committed and pushed to `dev`.
- [ ] The run still exits non-zero on that deferred failure — `#17131` must not be silenced by this change.
- [ ] Two or more deferred stage failures are all named in the final error, not just the last.
- [ ] `probeRepository` exercises the `stargazers` connection for `devindex-opt-in`; a denial is detected before collection starts and names repo + connection. *(Corrected during review: originally said the denial "fails the preflight before collection starts". It is deferred, not fatal — rethrowing would re-couple publication to the intake identity one layer above the stage table this ticket decouples, freezing the corpus for the same reason, only faster.)*
- [ ] A denied capability does not stop stages that never consume it: an Opt-In stargazer denial still leaves Opt-Out, Spider, Updater and `content indexes and SEO` executing. *(Added from @neo-gpt's PR #17149 review: the first implementation skipped every `tokenScope: 'intake'` stage, but Spider queries search/community endpoints and Updater reads `users/:name/orgs` — neither touches a DevIndex repository.)*
- [ ] Remediation derives from the refused connection: a stargazer denial must not print the `Issues: Read and write` remedy that the workflow already satisfies.
- [ ] Unit coverage in `test/playwright/unit/ai/buildScripts/DataSyncPipeline.spec.mjs` for: intake-failure-still-publishes, still-throws, multi-deferred aggregation, preflight stargazer denial.
- [ ] No change to `OptIn.mjs` error classification (no new skip branch).

## Out of Scope

- The GitHub-side stargazer 404 on both intake repos — operator/admin domain.
- Widening the `intake` App's permissions (`.github/workflows/data-sync-pipeline.yml:96-97`) — human-owned credential settings.
- `#16532` (deprecated app-id in token minting) — adjacent, separate auth defect.
- The two unrelated isolated failures inside the same window (`31755201012`: `ladies-of-code` community-scan 404; `31775846068`: penalty-box path). Different causes, both self-recovered.

## Avoided Traps

- **Diagnosing from the newest failing run.** The newest run shows only the current top layer. Attribution came from the *first* red after the last green plus a step-sample across the window — which is also what separated the three distinct failure modes that all land on step 6.
- **Blaming a merged PR because the timing fits.** The window diff and file histories date every candidate mechanism against the window; nothing in it touches this path.
- **Silencing the denial to turn the pipeline green.** Green would then mean "opt-in silently dead". This unfreezes publication and keeps the run red.

## Related

- `#17131` — the standing alarm this unfreezes. It stays open until the GitHub-side stargazer condition is resolved; that is correct behaviour, not a gap.
- `#16532` — Data Sync mints tokens with a deprecated app-id.

Retrieval Hint: "DevIndex intake denial freezes corpus publication; stargazers 404 on both intake repos"
Retrieval Hint: Commit range `45570db8e..dd25854d4`



## Timeline

- 2026-08-15T07:39:51Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-08-15T07:39:52Z @neo-opus-ada added the `bug` label
- 2026-08-15T07:39:52Z @neo-opus-ada added the `ai` label
- 2026-08-15T07:39:52Z @neo-opus-ada added the `regression` label
- 2026-08-15T07:39:52Z @neo-opus-ada added the `architecture` label
- 2026-08-15T07:39:53Z @neo-opus-ada added the `build` label
- 2026-08-15T07:52:00Z @neo-opus-ada cross-referenced by PR #17149
- 2026-08-15T07:52:46Z @neo-opus-ada cross-referenced by #17131
- 2026-08-15T08:15:25Z @neo-opus-ada referenced in commit `0a6bcbf` - "fix(build): keep preflight fallout capability-local and derive its remedy (#17148)

Review finding from @neo-gpt on PR #17149: the preflight catch skipped every
stage declaring `tokenScope: 'intake'` whenever ANY probe failed, so one denied
`devindex-opt-in.stargazers` read stopped Opt-Out, Spider and Updater too.

Verified against the services: Spider queries search/community endpoints and
Updater reads `users/:name/orgs` -- neither touches a DevIndex repository at all.
A credential-scope declaration says which identity to INJECT, never which
capability a stage CONSUMES, and the skip silently made it mean both. It
re-coupled at the credential layer exactly what the stage table had decoupled,
stopping healthy work to save a few seconds of predicted failure.

Nothing is skipped now. Every stage runs and fails on its own merits, which is
simpler than a static capability map and strictly more accurate -- a map could
only drift from what the services actually call. The aggregate numbers each
cause, so a shared root reads as related failures rather than one survivor.

Second finding, same review: every denial printed "install the App with
`Issues: Read and write` and `Metadata: Read`" -- permissions the workflow
already requests at lines 96-97. The one message read while diagnosing a
stargazer denial sent the operator to confirm the state they were already in.
`denialRemedy` now derives from the refused connection: stargazers names the
admin/collaborator restriction and says plainly that widening App permissions may
not restore it; issues keeps the Issues guidance; an unmapped connection says so
rather than inheriting another connection's advice.

The remedy text stops at what is published and observed -- GitHub announced the
restriction and its admin/collaborator boundary but published no enforcement
schedule and says nothing about installation tokens, so it asserts no rollout
mechanism and no fix that has not been shown to work."
- 2026-08-15T08:32:32Z @neo-fable-clio cross-referenced by #17153
- 2026-08-15T08:51:02Z @tobiu referenced in commit `a9d86d5` - "fix(build): an optional DevIndex intake denial no longer freezes corpus publication (#17148) (#17149)

* fix(build): an optional DevIndex intake denial no longer freezes corpus publication (#17148)

GitHub restricted the stargazers endpoint to repository admins and collaborators
(announced 2026-06-30, no published enforcement date). The rollout reached this
pipeline's intake App between 12:05:23Z and 13:19:19Z on 2026-08-14: the OptIn
stargazer read began returning `Resource not accessible by integration` and the
scheduled run has failed every time since.

`DevIndex Opt-In` is stage 3 of 7 and threw straight out of the emission loop, so
stages 4-7 never ran -- including `content indexes and SEO`, which is what makes
the corpus consumable -- and the publish path below the loop was never reached.
The corpus generated one stage earlier was discarded, freezing
`resources/content/**` on `dev` for nineteen hours while portal data and KB
ingestion read the stale mirror. An optional enrichment read for the DevIndex
opt-in feature was deciding whether the whole repository published.

The four DevIndex intake stages now carry `publishGeneratedProgressOnFailure`,
the primitive the corpus stage already used for exactly this: defer, run the
remaining stages, publish, then rethrow. The run still exits non-zero, so the
standing alarm is not silenced -- the intake genuinely is denied, and a green
pipeline would assert otherwise.

Two supporting corrections:

- A single `deferredError` slot kept only the last failure, so one denial that
  fails all four intake stages reported one and dropped three, sizing the outage
  wrong. `aggregateDeferredFailures` folds them, spelling every cause into the
  message a CI log tail actually shows while keeping the originals reachable.

- The preflight logged `devindex-opt-in reachable (OptIn stargazer read)` in the
  very runs whose stargazer read was denied twelve minutes later: it probed
  `repository{id}` alone, which resolves from metadata. It now selects the
  connection its entry names, and requires that connection to resolve -- GitHub
  answers a partial denial with the id present and the connection null, so
  testing the id alone read the exact failing response as success. Its denial is
  deferred rather than thrown, since rethrowing would re-couple publication to
  the intake identity one layer above the stage table that just decoupled it.

* fix(build): keep preflight fallout capability-local and derive its remedy (#17148)

Review finding from @neo-gpt on PR #17149: the preflight catch skipped every
stage declaring `tokenScope: 'intake'` whenever ANY probe failed, so one denied
`devindex-opt-in.stargazers` read stopped Opt-Out, Spider and Updater too.

Verified against the services: Spider queries search/community endpoints and
Updater reads `users/:name/orgs` -- neither touches a DevIndex repository at all.
A credential-scope declaration says which identity to INJECT, never which
capability a stage CONSUMES, and the skip silently made it mean both. It
re-coupled at the credential layer exactly what the stage table had decoupled,
stopping healthy work to save a few seconds of predicted failure.

Nothing is skipped now. Every stage runs and fails on its own merits, which is
simpler than a static capability map and strictly more accurate -- a map could
only drift from what the services actually call. The aggregate numbers each
cause, so a shared root reads as related failures rather than one survivor.

Second finding, same review: every denial printed "install the App with
`Issues: Read and write` and `Metadata: Read`" -- permissions the workflow
already requests at lines 96-97. The one message read while diagnosing a
stargazer denial sent the operator to confirm the state they were already in.
`denialRemedy` now derives from the refused connection: stargazers names the
admin/collaborator restriction and says plainly that widening App permissions may
not restore it; issues keeps the Issues guidance; an unmapped connection says so
rather than inheriting another connection's advice.

The remedy text stops at what is published and observed -- GitHub announced the
restriction and its admin/collaborator boundary but published no enforcement
schedule and says nothing about installation tokens, so it asserts no rollout
mechanism and no fix that has not been shown to work."
- 2026-08-15T08:51:03Z @tobiu closed this issue
- 2026-08-15T09:04:40Z @neo-opus-ada cross-referenced by #17150
- 2026-08-15T09:18:10Z @neo-fable-clio cross-referenced by #17157
- 2026-08-15T09:26:39Z @neo-opus-ada cross-referenced by PR #17160
- 2026-08-15T10:17:37Z @neo-opus-ada referenced in commit `f5ce78f` - "docs(build): correct a falsified claim about create-github-app-token (#17150)

An earlier revision of this comment asserted that `create-github-app-token`
"fails the step outright when asked for a permission the installation lacks",
and used that to argue revoking `contents` would take the pipeline down before it
could emit a corpus.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- a permission `neomjs-devindex-intake` does not hold -- and the mint
step SUCCEEDED. The action does not validate requested permissions against the
installation.

Two consequences, both worse to leave standing than to correct:

- The operational warning was overstated. Revoking `contents` degrades the
  stargazer read (which #17148's deferral now absorbs), it does not kill the mint
  and freeze the corpus. The coupling between this line and the App grant is
  real; the failure mode is not.

- A successful mint proves NOTHING about what the token carries. #17150's probe
  matrix leaned on mint-success as conclusive evidence that `administration:
  read` was genuinely held. That inference is void. The matrix's CONCLUSIONS
  survive -- they rest on the capability probe (stargazers reachable / denied),
  which is a direct observation -- but the corroborating argument does not, and
  the ticket is corrected to say so."
- 2026-08-15T10:18:40Z @neo-opus-ada referenced in commit `4117b45` - "docs(build): correct the falsified mint-failure trigger (#17162)

An earlier revision of this comment claimed `create-github-app-token` fails
outright when asked for a permission the installation lacks, and used that to
argue a revoked App permission is a realistic trigger for the mint failure this
guard exists to absorb.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- not held by `neomjs-devindex-intake` -- and the mint SUCCEEDED. The
action does not validate requested permissions against the installation.

The ticket's premise survives on the triggers that do fail the mint for real: a
rotated or expired private key, or an installation removed from a named
repository. Those still kill the job before a corpus byte exists, which is the
defect. What changes is that a revoked PERMISSION degrades the capability -- and
#17148's deferral already absorbs that -- rather than freezing publication.

Also worth stating in-file because the inverse belief is actively misleading: if
you think the mint validates, a successful mint looks like proof the token
carries what was requested. It is not, and only a capability probe answers that."
- 2026-08-15T12:53:16Z @tobiu referenced in commit `c03f3a5` - "fix(devindex): restore the stargazer read and stop a dead phase killing the working one (#17150) (#17160)

* fix(devindex): a denied stargazer read no longer takes the issue path down with it (#17150)

Opt-in has two INDEPENDENT intake mechanisms reading the same repository over
different connections: `stargazers` and `issues`. GitHub restricted access at
connection granularity -- stargazer reads limited to repository admins and
collaborators (announced 2026-06-30) -- so exactly one of the two is currently
unavailable.

The stargazer loop ran first and threw straight out of `run()`, so
`processIssues()` was never reached and opt-in intake went to ZERO rather than
halving. The repository-not-found branch had the same defect for the same reason:
it `return`ed out of `run()`, and `processIssues` already handles NOT_FOUND for
itself by returning null, so pre-empting it bought nothing.

The stargazer phase moves into `collectStargazerOptIns`, which returns its own
result instead of mutating `run()` state -- that is what makes the isolation
expressible, since the caller can now lose the phase to a failure without
holding half-updated state. `run()` wraps it, continues to the issue path, and
rethrows LAST, after the issue work is persisted.

Deferred, never swallowed. A catch that quietly returned would leave a dead
intake mechanism reporting success indefinitely, which is the failure mode that
let this go unnoticed until the corpus froze.

Coverage: every test verified RED against the pre-fix source INDIVIDUALLY --
`describe.serial` skips after the first failure, so one suite run proved only the
first test and would have hidden three vacuous ones.

Also strips pre-existing trailing whitespace on 12 lines and applies
block-alignment; the commit hooks check whole staged files, and moving the
stargazer loop brought both into scope.

Does NOT decide whether stargazer access is restored; that is the open half of
#17150 and an operator call on a credential trade-off.

* feat(build): grant the intake token the stargazer read via contents:write (#17150)

GitHub limited the stargazers endpoint to repository admins and collaborators
(announced 2026-06-30) and infers that status from contents-write. `metadata:
read` used to carry the read and no longer does, so the DevIndex star-based
opt-in has been dead since the restriction reached this installation on 08-14.

Measured rather than argued -- four `preflight_only` probes, one variable each,
each mutating nothing:

  issues:write + metadata:read   -> DENIED    (run 31870344604)
  + administration:read          -> DENIED    (run 31874111382)
  + contents:WRITE               -> reachable (run 31875177082)
  + contents:READ                -> DENIED    (run 31875769230)

Read does not suffice; there is no lesser lever. `devindex-opt-out.issues` stayed
reachable across all four, which rules out a sick token and isolates the finding
to the connection.

The cost is stated in the workflow rather than buried: the old note claimed this
token "must never be able to write code anywhere", which is no longer true. What
it can now write is two 4 KB repositories holding a README, a `.gitignore` and
issue templates -- no code, no data. The DevIndex corpus lives in `neomjs/neo`,
which this App has no installation on, so the #15744 property that matters --
the intake identity cannot publish here -- is intact.

Operationally this line and the App grant are ONE unit: `create-github-app-token`
fails the step outright when asked for a permission the installation lacks, so a
future permission cleanup that drops `contents` must land in the same commit that
removes this line, or the pipeline dies before emitting a corpus.

* docs(build): correct a falsified claim about create-github-app-token (#17150)

An earlier revision of this comment asserted that `create-github-app-token`
"fails the step outright when asked for a permission the installation lacks",
and used that to argue revoking `contents` would take the pipeline down before it
could emit a corpus.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- a permission `neomjs-devindex-intake` does not hold -- and the mint
step SUCCEEDED. The action does not validate requested permissions against the
installation.

Two consequences, both worse to leave standing than to correct:

- The operational warning was overstated. Revoking `contents` degrades the
  stargazer read (which #17148's deferral now absorbs), it does not kill the mint
  and freeze the corpus. The coupling between this line and the App grant is
  real; the failure mode is not.

- A successful mint proves NOTHING about what the token carries. #17150's probe
  matrix leaned on mint-success as conclusive evidence that `administration:
  read` was genuinely held. That inference is void. The matrix's CONCLUSIONS
  survive -- they rest on the capability probe (stargazers reachable / denied),
  which is a direct observation -- but the corroborating argument does not, and
  the ticket is corrected to say so.

* docs(build): restore the true coupling claim -- my correction was the false one (#17150)

Reverting f5ce78fe4a's correction. It asserted `create-github-app-token` does not
validate requested permissions, citing run 31877595849 as a success. The raw log
of that run says otherwise:

  ##[error]The permissions requested are not granted to this installation.

The step FAILED. I read `conclusion` from the jobs API, which `continue-on-error`
rewrites to `success` on a failed step -- and #17162's guard was present on that
probe branch. So the guard rewrote the field I used to judge whether the guard
was needed.

Consequence for the operator warning, restored to the stronger form: revoking
`contents` on the App while this line remains does not merely cost the stargazer
read, it fails the mint outright."
- 2026-08-15T12:54:16Z @tobiu referenced in commit `6772ea0` - "fix(build): an intake credential fault no longer freezes corpus publication (#17162) (#17165)

* fix(build): an intake credential fault no longer freezes corpus publication (#17162)

The intake mint ran before checkout, Node and emission with nothing tolerating
its failure, so any intake credential fault killed the job before a single corpus
byte existed. That is #17148's blast radius one layer up: the corpus is emitted
under the READER identity and never consumes the intake token, yet an ENRICHMENT
credential decided whether the whole repository published.

`create-github-app-token` fails outright when asked for a permission the
installation lacks, so the realistic triggers are ordinary administration -- a
revoked permission, a rotated key, an installation removed from one repo -- and
the `contents` grant #17150 requires makes exactly that drift possible.

`continue-on-error: true` needs no new error path behind it. A failed mint leaves
`outputs.token` empty, the empty `DATA_SYNC_INTAKE_TOKEN` reaches
`assertDataSyncAccess` as "No intake token was provided", and `emitGeneratedData`
already defers a preflight failure: every stage runs, the corpus publishes, the
deferred error is rethrown and the run exits non-zero. The failure moves later
rather than disappearing -- a silently green run over a dead intake would be
strictly worse than the freeze it replaces.

The Publisher mint stays fatal. It is the publish credential; without it there is
nothing to salvage.

Adds the unit test pinning the runtime half, because the workflow change is
otherwise trusting an unasserted path: if the absent-token case ever threw
instead of deferring, a credential fault would resume killing publication while
the workflow guard still looked correct.

* docs(build): correct the falsified mint-failure trigger (#17162)

An earlier revision of this comment claimed `create-github-app-token` fails
outright when asked for a permission the installation lacks, and used that to
argue a revoked App permission is a realistic trigger for the mint failure this
guard exists to absorb.

Falsified by measurement. Run 31877595849 requested `permission-deployments:
write` -- not held by `neomjs-devindex-intake` -- and the mint SUCCEEDED. The
action does not validate requested permissions against the installation.

The ticket's premise survives on the triggers that do fail the mint for real: a
rotated or expired private key, or an installation removed from a named
repository. Those still kill the job before a corpus byte exists, which is the
defect. What changes is that a revoked PERMISSION degrades the capability -- and
#17148's deferral already absorbs that -- rather than freezing publication.

Also worth stating in-file because the inverse belief is actively misleading: if
you think the mint validates, a successful mint looks like proof the token
carries what was requested. It is not, and only a capability probe answers that.

* docs(build): my correction was the false one -- both mint triggers are real (#17162)

Reverting my own correction. @neo-gpt falsified it and the raw logs confirm him,
going further than he did: BOTH probes failed the mint, not one.

  run 31877595849  ##[error]The permissions requested are not granted to this installation.
  run 31879164402  ##[error]Not Found - .../get-a-repository-installation-for-the-authenticated-app

I read `conclusion` from `gh run view --json jobs` and saw `success` on both, and
concluded the mint cannot fail. But `continue-on-error: true` sets
`conclusion: success` on a FAILED step while `outcome` retains the failure --
so the field I read is the one THIS PR's own guard rewrites.

The guard masked the failures that justify it, and I read that as evidence the
guard was unnecessary. A mechanism that converts failure into success makes its
own necessity invisible to any status-based check.

So the original premise stands and this PR is better evidenced than when it was
opened: run 31879164402 is valid L4 proof of the continuation -- mint failed,
job proceeded through checkout and Node to the preflight, which rejected the
empty token and left the corpus path intact.

Restores the trigger list, records both measured failure modes with their runs,
and adds the instruction that would have prevented the error: read the LOG, not
the step status, because once this guard exists a failed mint renders green."

