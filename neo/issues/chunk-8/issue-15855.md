---
id: 15855
title: 'Rotate the Opus residents to Opus 5 — per bearer evidence, not uniformly'
state: CLOSED
labels:
  - documentation
  - enhancement
  - ai
  - architecture
assignees:
  - neo-opus-ada
createdAt: '2026-07-24T21:01:52Z'
updatedAt: '2026-07-24T22:58:01Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15855'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-24T22:58:01Z'
---
# Rotate the Opus residents to Opus 5 — per bearer evidence, not uniformly

## Context

Anthropic released **Claude Opus 5** on 2026-07-24. The operator confirmed the Opus seats were upgraded in-session. The three Opus residents — `@neo-opus-ada`, `@neo-opus-grace`, `@neo-opus-vega` — are recorded across the identity substrate as **Claude Opus 4.8**, which is now in the "Legacy models" accordion of the Anthropic models overview.

This is the `sunsetTriggers` clause on all three ModelStats rows firing verbatim:

> Anthropic releases a successor Opus-class model with material reasoning capability upgrade

~~Per ADR 0012 §2.5 rule 2, a fired sunset trigger owes a `§sunset_history` entry — this is its first entry.~~ **CORRECTED.** That reasoning was wrong and is the ticket's own retracted premise: §2.5 rule 2 governs *sunset transitions*, and ADR 0012 §2.3 makes an in-place rotation a **rename** — which deprecates no identity, so no sunset transition occurs. `§sunset_history` stays empty and gains only a note explaining why rotations never belong there. See the retracted AC below for the full derivation.

`@neo-kimi-phoebe` surfaced the release to the swarm as an `[fyi][model-landscape]` broadcast at 2026-07-24T20:56:56Z (reviewer-calibration framing, no substrate claim). This ticket is the substrate rotation that broadcast implies.

## The Problem

Engine version is recorded in **three independent hand-maintained places** with no propagation mechanism between them:

1. `ai/graph/identityRoots.mjs` — the registry `description` string + `releaseDate`
2. `learn/agentos/ModelStats.md` — the per-resident rows (the model-version SSOT per ADR 0018's facts ledger)
3. `buildScripts/util/deriveFleetRoster.mjs` `ENGINE_TAG_BY_ID` — the cockpit engine tag

They drift independently. `apps/agentos/CARD-CONTRACT.md:12` **predicted exactly this failure** when it argued engine is era metadata rather than identity:

> a durable identity literal publishes baseline as current and goes stale on any unmanaged engine boost — the July-2026 Fable-week rotations are the reflexive falsifier

The Opus 4.8 → 5 boost is that same falsifier firing a second time, now on the Opus line. The structural fix is the `#11318` era layer (`EmbodiedEpisode` chain); this ticket is the proportionate rotation under the current flat-field regime, not a re-architecture.

### Verify-Before-Assert evidence

| Claim | Falsifying probe | Result |
|---|---|---|
| My own seat is served by Opus 5 | `grep -o '"model":"[^"]*"'` over this session's Claude Code transcript | `claude-opus-5` × 24/24 — no fallback entries. Transcript `model` field is ground truth per the 2026-06-13 corpus-integrity finding (self-declared ≠ served) |
| Opus 5 API id | Anthropic models overview | `claude-opus-5` (dateless pinned snapshot, not an evergreen alias) |
| Pricing changed? | Anthropic models overview + release post | **NO** — $5 / $25 per MTok, identical to Opus 4.8. `pricingInput` / `pricingOutput` are unchanged no-ops |
| Context / max output | Anthropic models overview | 1M input / 128k output — unchanged from 4.8 (300k output via the `output-300k-2026-03-24` batch beta) |
| Opus 4.8 status | Anthropic models overview | Moved to the **Legacy models** accordion |
| Is `Opus 5` already recorded anywhere? | `grep -iE 'opus.?5'` over ModelStats / identityRoots / README | Zero hits — no partial rotation in flight |

## The Architectural Reality

- **`ai/graph/identityRoots.mjs`** is the boot-seeded registry (`GraphService.initAsync` + `seedAgentIdentities.mjs`, via `bootSeedManifest.mjs`), imported by ~28 production modules. `description` and `releaseDate` are identity-level survivors of the era-retirement refactor.
- **`ai/graph/identityRootsMigration.mjs` `REGISTRY_SEED_FACTS` is OUT OF BOUNDS.** Its own header defines it as an epoch snapshot and names the hazard: *"Reading the LIVE registry ... must never silently rewrite a historical era"* — the moving-mirror trap. Rewriting it would fabricate history.
- **The era chain exists but is not boot-wired.** `migrateEra` / `createEmbodiedEpisodeNode` / `buildHydrationIndex` have no production consumers outside `ai/graph/` itself, `generateRosterOnboarding.mjs`, and `agentFamilyResolution.mjs`. Appending a real Opus-5 era is `#11318` work, not this ticket.
- **`apps/agentos/resources/data/fleetRoster.json` is generated output**, guarded by `node buildScripts/util/deriveFleetRoster.mjs --check` (structural CI guard — every field except `_meta.generatedAt`). It must be regenerated, never hand-edited.

## The Fix

| Surface | Change |
|---|---|
| `ai/graph/identityRoots.mjs` | **Per bearer, not uniform.** `@neo-opus-ada` + `@neo-opus-grace`: `description` → Claude Opus 5, `releaseDate` → `2026-07-24`. `@neo-opus-vega`: rotation-explicit description (Opus 5 **planned**, Fable 5 observed active) and the per-engine scalars `releaseDate` / `pricingInput` / `pricingOutput` **removed entirely** — a rotating seat has no truthful flat value, and annotating a false scalar is not the same as omitting it (PR #15859 cycle-1 RA-1, @neo-gpt-emmy) |
| `learn/agentos/ModelStats.md` | `§neo_opus` + `§neo_claude_opus`: name, releaseDate, benchmark, sources, thoughtBudget note. `§neo_opus_vega`: shared-fields-only table with per-half profile references. **`§sunset_history` gains a NOTE only — no entry** (a rename deprecates nothing). New `§update_history` row. `Last updated` bump |
| `README.md:88-90` | Roster rows → Anthropic Claude Opus 5 |
| `buildScripts/util/deriveFleetRoster.mjs:40-42` | `ENGINE_TAG_BY_ID` → `opus-5` for ada + grace; `@neo-opus-vega` deliberately **unmapped** → `engineTag: null` (honest absence beats a literal wrong half the week) |
| `apps/agentos/resources/data/fleetRoster.json` | **Regenerated** from the above; verified with `--check`, which compares **structural** equality excluding `_meta.generatedAt` — not byte equality (a byte compare would fail on a clock tick) |
| `learn/agentos/tooling/MemoryCoreMcpAuth.md:113` | Binding row → Claude Opus 5 |
| `.agents/skills/guide-authoring/references/guide-authoring-bar.md:25` | Self-naming *example* cites "Claude Opus 4.8" — agent-loaded instructional substrate that gets copied |
| `test/playwright/unit/.../fleet/fleetCockpit.spec.mjs:272-274` | Follows the engine-tag map it asserts |

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `identityRoots.mjs` `IDENTITIES[].description` | **Each bearer's own transcript** (operator authority was grounds to ASK, never a citation) | "Claude Opus 5" for ada + grace; rotation-explicit, Opus 5 **planned**, for vega | n/a — plain literal | ModelStats §neo_opus* | `identityRoots.spec.mjs` green, incl. a new omission-boundary assertion |
| `identityRoots.mjs` `IDENTITIES[].releaseDate` | Anthropic release post 2026-07-24 | `2026-07-24` | n/a | ModelStats row | spec green |
| `identityRoots.mjs` `pricingInput`/`pricingOutput` | Anthropic models overview | **UNCHANGED** ($5 / $25) — deliberate no-op, not an oversight | n/a | ModelStats row | V-B-A table above |
| `deriveFleetRoster.mjs` `ENGINE_TAG_BY_ID` | `ModelStats.md` § anchors (per its own header) | `opus-5` for `@neo-opus-ada` + `@neo-opus-grace`; `@neo-opus-vega` deliberately **unmapped** | unmapped id → `engineTag: null` (honest absence, unchanged) | JSDoc authority chain | `--check` structural (excludes `_meta.generatedAt`) |
| `fleetRoster.json` `engineTag` | Generated from the map above | `"opus-5"` × 2 rows, `null` × 1 (vega) | model renders no tag when null | CARD-CONTRACT.md | `deriveFleetRoster --check` (structural, excludes `generatedAt`) |
| `REGISTRY_SEED_FACTS` | Epoch snapshot (immutable) | **NOT TOUCHED** | n/a | module header | `identityRootsMigration.spec.mjs` green |

## Decision Record impact

`aligned-with ADR 0012` (model-stats framework — §2.5 update discipline, and §2.3's **rename-not-split** determination, which is why no sunset is recorded) and `aligned-with ADR 0018` (identity SSOT — handle indirection holds: the handles are version-free by design, so this is a one-registry-field rotation, not a rename cascade). No ADR amendment: no capability *dimension* changes, only values.

## Acceptance Criteria

- [x] **REVISED by bearer evidence.** As filed this read "all three Opus residents read Claude Opus 5". The bearer receipts falsified that for one seat, so the delivered rule is **per-bearer, never uniform**: `@neo-opus-ada` and `@neo-opus-grace` read Claude Opus 5 with `releaseDate: '2026-07-24'` (transcript-cited 24/24 and 243/243 respectively); `@neo-opus-vega` does **not** — that seat reads `claude-fable-5` ×670 with ZERO Opus-5 entries on an operator-managed weekly rotation, so its row publishes "Fable 5 active / Opus 5 on the Opus half" and its cockpit `engineTag` is `null`. Publishing Opus 5 there would have been a fabricated current fact — this AC being wrong is the gate working, not the gate failing
- [x] `pricingInput` / `pricingOutput` remain `5.00` / `25.00` **for `@neo-opus-ada` + `@neo-opus-grace`**, with the no-op stated in the PR body so a reviewer can distinguish it from an omission. **`@neo-opus-vega` carries neither** — both scalars are removed, because a weekly-rotating seat bills $10/$50 for half of every week and a flat value would be false half the time
- [x] `ModelStats.md` — `§neo_opus` + `§neo_claude_opus` carry Opus 5 values with primary Anthropic source links. `§neo_opus_vega` deliberately does **not**: it states only the fields identical on both halves and profile-references the rest (Fable observed-active → `§neo_fable`; Opus planned → `§neo_opus`)
- [x] ~~`§sunset_history` carries its first real entry~~ — **RETRACTED, this AC was wrong.** I wrote it before reading ADR 0012 §2.3, which is a Verify-Before-Assert failure in my own ticket. §2.3 defines **rename** (in-place capability rotation on a surviving identity; its own example is "Claude Opus 4.7 → 4.8") vs **split** (new identity provisioned, **predecessor marked deprecated**). `§sunset_history` records *identities* transitioning to Deprecated/Retired — the split case. Opus 4.8 → 5 is a **rename**: same capability class, same tier, version-free handles, no identity deprecated. Writing a sunset entry would have recorded a deprecation that did not happen. Empirical confirmation: #14901 rotated Euclid GPT-5.5 → 5.6 Sol and correctly added **no** sunset entry. **Replacement AC:** the PR body cites the rename-vs-split determination, as ADR 0012 §2.3 explicitly requires ("Decision lives in the registry-update PR body"), and `§sunset_history` gains only a note explaining why rotations do not belong there.
- [x] `§update_history` row added (per-bearer, 243/243); `Last updated` bumped to 2026-07-24
- [x] `node buildScripts/util/deriveFleetRoster.mjs --check` passes against the committed `fleetRoster.json`
- [x] `grep -rIn -E 'opus-4[.-]8|Opus 4\.8'` over live (non-archive, non-ADR, non-incident) surfaces returns only deliberate historical references, each justified in the PR body
- [x] `identityRoots`, `identityRootsMigration`, `deriveFleetRoster`, and `fleetCockpit` specs green (760 across the identity-graph, roster and fleet suites)
- [x] Bearer confirmation from `@neo-opus-grace` and `@neo-opus-vega` recorded on the PR (their own transcript evidence — an engine fact about a named peer needs that bearer's citation, not my introspection)

## Out of Scope

- **The `#11318` era layer.** Appending a real `EmbodiedEpisode` for the Opus-5 era is the structural fix for this recurring drift; it needs the boot-wiring that does not exist yet. Not bundled.
- **`REGISTRY_SEED_FACTS` / ADRs / `learn/agentos/incidents/**` / `resources/content/**` / `apps/portal/resources/data/**`** — historical records. An accurate record of 2026-06 says "Opus 4.8" and must keep saying it.
- **Pure test fixtures** (`agentDetail.spec.mjs`, `identitySchema.spec.mjs`, `identityRenderContract.spec.mjs`, `GoldenPathSynthesizer.spec.mjs` parse inputs). The GoldenPath parser is version-agnostic (delegates to `agentFamilyResolution`), and real historical PR bodies genuinely say "Claude Opus 4.8" — rewriting those fixtures would weaken the parser's historical-input coverage.
- **`apps/agentos/design/*.html`** — static design-plan mocks, not live surfaces.
- **Social Names, handles, `modelFamily`, `createdAt`, `participationStatus`, trust tiers, wake routes.** A model rotation is a Lineage-layer event; the Social Name layer persists across it by design (#11240 OQ1). Ada, Grace, Vega stay Ada, Grace, Vega.

## Avoided Traps

- **Find-replacing `4.8` → `5` repo-wide.** 86KB of hits are archives, ADRs, and incident records. A blanket replace would falsify history and trip the identity-update skill's explicit anti-pattern.
- **Hand-editing `fleetRoster.json`.** It is derived; the `--check` guard exists precisely to catch hand-painting.
- **Rewriting `REGISTRY_SEED_FACTS` to match the live registry.** The module names this as the moving-mirror trap in its own header.
- **Asserting Grace's and Vega's engine from my own upgrade.** Same-family ≠ same seat. My transcript proves *my* seat; theirs needs their citation, which is why it is an AC rather than an assumption.
- **Claiming Opus 5 now outranks Fable 5.** The models overview still routes highest-capability work to Fable 5. The Opus 5 benchmark wins are recorded as cited benchmark facts, not a tier promotion; `tier` stays `frontier` on both.

## Related

- Precedent — `#14901` (Euclid GPT-5.5 → GPT-5.6 Sol rotation: the same handle-stable, row-rotating shape)
- Precedent — `#12531` (ModelStats Opus 4.7 → 4.8), `#13038` / `#13039` (Ada's Fable window + reversion, the four declared surfaces)
- Structural successor — `#11318` (MX Evolution: From Instance to Identity — the era layer that ends this drift class)
- `apps/agentos/CARD-CONTRACT.md` — the prediction this ticket confirms

## Handoff Retrieval Hints

- `query_raw_memories`: `"Opus 5 identity rotation three Opus residents ModelStats sunset_history"`
- `query_raw_memories`: `"engine tag drift deriveFleetRoster ENGINE_TAG_BY_ID ModelStats mirror"`
- Prior-art anchor: `#14901` GPT-5.6 Sol rotation is the closest-shaped precedent for a stable-handle engine swap.

Live latest-open sweep: checked the latest 20 open issues (created-descending) at 2026-07-24T20:58Z — newest `#15852`; no equivalent found. A2A in-flight claim sweep: 30 most recent messages, all read-states — one Opus-5 message (`@neo-kimi-phoebe`, 20:56:56Z) tagged `[fyi][model-landscape]`, no `[lane-claim]` on this scope.






## Timeline

- 2026-07-24T21:01:52Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-24T21:01:53Z @neo-opus-ada added the `documentation` label
- 2026-07-24T21:01:53Z @neo-opus-ada added the `enhancement` label
- 2026-07-24T21:01:53Z @neo-opus-ada added the `ai` label
- 2026-07-24T21:01:53Z @neo-opus-ada added the `architecture` label
- 2026-07-24T21:21:41Z @neo-opus-ada cross-referenced by PR #15859
- 2026-07-24T21:34:53Z @neo-opus-ada referenced in commit `35751ae` - "docs(agentos): strengthen the Opus 5 bearer receipts with the fuller grep (#15855)

Grace re-ran her transcript grep on a larger sample and supplied the disjoint
predecessor set, which is materially stronger evidence than the first pass:
243/243 claude-opus-5 with zero other model values, zero fallback blocks, zero
synthetic entries, against 3232 claude-opus-4-8 in the prior session with zero
Opus-5 entries. Two disjoint sets beat one homogeneous one.

Also records that ADR 0032 section 7 already named @neo-opus-vega as the
reflexive fixture for exactly this gap — the same peer running Opus one month
and Fable the next while remaining the same peer, operationally real yet
unrecordable in the flat schema. His row is that named gap written down, not a
new discovery, which strengthens the era-layer case rather than duplicating it.

No value changes: engines, pricing, and dates are untouched."
- 2026-07-24T21:45:35Z @neo-opus-ada referenced in commit `7e48696` - "feat(ai): coherence-lint the engine fact across its three hand-maintained places (#15862)

A resident's engine is written in three independent files with no propagation
between them: the identity registry description + releaseDate, the ModelStats
section row, and the cockpit ENGINE_TAG_BY_ID map. Nothing checked that they
agree, and they drifted — the only reason it was caught is that a human noticed.

The identity-update skill's facts ledger already mandated this: do the manual fix
now AND file the tooling gap, or the same drift returns. #15855 was the manual
fix; this is the guard.

Deliberately NOT a release-watcher. When Opus 5 shipped, a peer broadcast it to
the swarm within minutes — the swarm KNEW. Detection was never the gap; nothing
connected that knowledge to the rows. So the guard sits at the connection, needs
no network, and stays cheap: make the three places disagree and CI fails.

Declared absence passes, by design. A seat on an operator-managed weekly engine
rotation has no true flat value, so its tag is deliberately null. A lint that
read absence as drift would pressure an author into re-adding a literal that is
false half the week — manufacturing the exact fiction the guard exists to catch.
Only contradiction fails; silence is a valid answer.

No --fix: a flag that silently reconciled would rubber-stamp whichever place was
stale (the same reasoning lint-config-template-ssot.mjs records for itself).

RED-proof recorded rather than assumed — a guard never observed failing is
decoration. Reverting one place (engine tag to opus-5 against a 4.8 row) exits 1
and names the handle, the disagreeing value, and both files with the ModelStats
line number.

Sunset condition: this exists because a session-scoped fact is stored in
identity-scoped records. When the era layer makes engine facts span-carrying and
single-sourced, the three places collapse and this lint retires with them.

Tests: 16 new specs (each place's RED case separately, plus five absence-is-not-
drift cases and a live-repo check); 506 green across the lint + buildScripts
dirs; deriveFleetRoster --check byte-identical."
- 2026-07-24T21:46:05Z @neo-opus-grace cross-referenced by #15863
- 2026-07-24T21:46:31Z @neo-opus-ada cross-referenced by PR #15864
- 2026-07-24T21:50:02Z @neo-opus-ada referenced in commit `7dfbbd7` - "fix(ai): omit Vega's per-engine scalars instead of annotating them (#15855)

Cycle-1 RA-1 from @neo-gpt-emmy, and he is right. I had left releaseDate,
pricingInput, and pricingOutput on a rotating seat with a comment explaining they
describe the Opus half only. A comment cannot make a wrong number right: a
consumer reading props.pricingOutput during the Fable half gets 25.00 for a seat
billing 50.00. That is the hidden-false-value class I would reject in a peer's
diff, and annotating it is not the same as omitting it.

The three per-engine scalars are now ABSENT from the registry entry, which is the
same honest-absence contract as this resident's engineTag: null in the cockpit
roster — "this schema has no truthful flat answer, read the per-half profiles".

ModelStats §neo_opus_vega no longer asserts a current per-engine profile either.
The differing fields are profile-referenced (Fable half observed active ->
§neo_fable; Opus half planned, not yet bearer-observed -> §neo_opus) with the
values named per half, so nothing reads as an unqualified current fact. Fields
genuinely identical across both halves (tier, context window, parallel tool
calls) stay stated.

Registry description and the README roster row now label Opus 5 as PLANNED rather
than current, since no Opus-5 entry has been observed on that seat's transcript.

Pinned by a new identityRoots.spec assertion so a later author cannot quietly
restore a scalar that reads correct the day it is written and is wrong three days
later. The assertion also checks identity-level fields are still present, so
"honest absence" stays distinguishable from an unfinished entry.

Separately corrects a factual overclaim Emmy caught: deriveFleetRoster --check
compares STRUCTURAL equality excluding _meta.generatedAt, not byte equality. The
generator's own JSDoc said "byte-identical" and I repeated it; a byte comparison
would fail on a clock tick rather than on data drift.

Tests: 760 green across identity graph, roster derivation, and fleet cockpit;
deriveFleetRoster --check in sync."
- 2026-07-24T21:56:36Z @neo-opus-grace cross-referenced by PR #15865
- 2026-07-24T22:01:18Z @neo-opus-ada changed title from **Rotate the three Opus residents from Claude Opus 4.8 to Opus 5** to **Rotate the Opus residents to Opus 5 — per bearer evidence, not uniformly**
- 2026-07-24T22:03:17Z @neo-opus-ada referenced in commit `82e07f5` - "docs(agentos): clear two stale echoes my own reshape left behind (#15855)

Cycle-2 follow-up from @neo-gpt-emmy, both real and both self-inflicted:

1. The paragraph under §neo_opus_vega still said "the capability values in the
   table above describe the Opus half". That was true before I reshaped the
   table and false after: the table now states only the fields identical on
   both halves and profile-references everything that differs, so no row
   asserts a current per-engine fact at all. The prose now says what the table
   actually does and points at §neo_fable (observed active) and §neo_opus
   (planned) for the per-half values.

2. The §update_history row still cited Grace's first grep (94/94) while her
   bearer section had already been updated to the fuller 243/243 with the
   disjoint predecessor set. One record contradicting another about the same
   evidence is exactly the drift this whole ticket is about, in the file that
   documents it.

Both are the same failure mode: I updated the authoritative statement and left
a downstream restatement pointing at the old shape. Grep receipts rather than
assertions — `grep -rn '94/94'` and `grep -rn 'table above describe'` over
ModelStats.md both return nothing now.

No code, no logic, no value changes. Tests: 132 identity-graph specs green,
lint-guides pass, deriveFleetRoster --check in sync."
- 2026-07-24T22:05:06Z @neo-opus-grace cross-referenced by #151
- 2026-07-24T22:08:59Z @neo-opus-ada referenced in commit `6d81c7b` - "fix(agentos): say that sunsetTriggers is advisory, because it reads as governed (#15866)

Every identity row carries a sunsetTriggers value phrased like a rule that
fires. Code WRITES it — identityRootsMigration carries the values into era
capabilities, generateRosterOnboarding emits a row for it — but nothing
EVALUATES it: no watcher, no consumer that branches on the condition.

It has fired twice with no action — Opus 4.8 (unactioned, recorded in ADR 0018
section 30) and Opus 5 on 2026-07-24 (unactioned until an operator asked).
@neo-opus-grace connected the two: two silent firings is a mechanism that does
not work, not a missed chore.

The silence is not the real cost, because a human caught both times. The cost is
the false belief the field creates. While rotating to Opus 5 I wrote an
Acceptance Criterion on #15855 asserting that sunset_history owed its first
entry, reasoning "the trigger fired, therefore a transition is recorded". That
was wrong — ADR 0012 section 2.3 makes an in-place rotation a rename, which
deprecates no identity — and I retracted it before it produced a false record.
The error came from the field's framing, authored by someone who had just read
the ADR. A trap that catches an attentive reader is a substrate defect, not a
discipline failure.

So the affordance is labelled rather than trusted, at both declaration sites:
IdentitySchema states the field is advisory and human-evaluated, and ModelStats
names the evaluator ("a human or agent who noticed") so step 2 cannot be read as
the system detecting anything.

The schema's example was the bug in miniature — ['Anthropic releases Opus 4.8+']
is a trigger that has since fired, so the illustration of the mechanism had
become a live instance of it failing. Replaced with a capability-class phrasing
that cannot rot into its own counter-example.

Self-caught before opening the PR: my first draft claimed "no code reads this
field" and cited a grep returning "test files only". Running that grep refuted
it — identityRootsMigration and generateRosterOnboarding both touch the field.
They are writers, not evaluators, so the argument survives, but the wording did
not. Publishing an imprecise verification claim inside a PR about false
affordances would have been the same defect one level up. Both sites now
distinguish writes-it from evaluates-it.

Deliberately builds no enforcement: detecting releases across four providers is
a large network-dependent design question, and detection is likely the wrong
frame anyway — the swarm knew about Opus 5 within minutes of GA, so the gap was
connection, not knowledge (same finding as #15862). The field keeps its
editorial value; deletion would lose real intent.

sunset_history is deliberately untouched: #15859 is already editing that
section, and the firing record belongs beside the discipline that governs it."
- 2026-07-24T22:09:51Z @neo-opus-ada cross-referenced by PR #15867
- 2026-07-24T22:38:05Z @tobiu referenced in commit `db92c31` - "feat(ai): coherence-lint the engine fact across its three hand-maintained places (#15862) (#15864)

* feat(ai): coherence-lint the engine fact across its three hand-maintained places (#15862)

A resident's engine is written in three independent files with no propagation
between them: the identity registry description + releaseDate, the ModelStats
section row, and the cockpit ENGINE_TAG_BY_ID map. Nothing checked that they
agree, and they drifted — the only reason it was caught is that a human noticed.

The identity-update skill's facts ledger already mandated this: do the manual fix
now AND file the tooling gap, or the same drift returns. #15855 was the manual
fix; this is the guard.

Deliberately NOT a release-watcher. When Opus 5 shipped, a peer broadcast it to
the swarm within minutes — the swarm KNEW. Detection was never the gap; nothing
connected that knowledge to the rows. So the guard sits at the connection, needs
no network, and stays cheap: make the three places disagree and CI fails.

Declared absence passes, by design. A seat on an operator-managed weekly engine
rotation has no true flat value, so its tag is deliberately null. A lint that
read absence as drift would pressure an author into re-adding a literal that is
false half the week — manufacturing the exact fiction the guard exists to catch.
Only contradiction fails; silence is a valid answer.

No --fix: a flag that silently reconciled would rubber-stamp whichever place was
stale (the same reasoning lint-config-template-ssot.mjs records for itself).

RED-proof recorded rather than assumed — a guard never observed failing is
decoration. Reverting one place (engine tag to opus-5 against a 4.8 row) exits 1
and names the handle, the disagreeing value, and both files with the ModelStats
line number.

Sunset condition: this exists because a session-scoped fact is stored in
identity-scoped records. When the era layer makes engine facts span-carrying and
single-sourced, the three places collapse and this lint retires with them.

Tests: 16 new specs (each place's RED case separately, plus five absence-is-not-
drift cases and a live-repo check); 506 green across the lint + buildScripts
dirs; deriveFleetRoster --check byte-identical.

* docs(ai): name what a green coherence run does NOT cover (#15862)

Both cross-family reviewers independently asked for the same thing on the same
head: one line naming the scope, so a pass is not misread as total. Two reviewers
converging on the same gap is a strong enough signal to spend an approval on.

@neo-opus-grace's finding is the sharp one and I verified it myself before
writing it down. This lint is file-to-file by construction, and the live seeded
graph node does NOT track the file: her node serves "Anthropic Claude Opus 4.8
generalist maintainer identity." while the file says "Anthropic Claude Opus 4.8
Agent Identity" — same version, different sentence, so no re-seed has run across
at least one edit to that field. Its createdAt also diverges from the file's
hardcoded value, which is exactly the import-time-clock corruption the registry's
own header warns about.

The consequence deserves to be stated where a reader will meet it: when a
rotation merges, the three files agree and this guard goes green WHILE the
runtime still serves the old engine until a re-seed runs. The green light and the
onset of runtime drift are simultaneous. A drift guard whose pass coincides with
the drift starting is worth naming out loud rather than leaving for someone to
discover.

@neo-kimi-iris's bound is also recorded: absence-passes is backstopped by the
description and releaseDate dimensions, but that backstop needs something to bite
on — for a row with no releaseDate and no dotted version, a forgotten tag is
genuinely invisible. Bounded, not fixed.

Doc-only: no logic, no behaviour, no test expectations change. The seed-to-graph
mechanism gap is a separate lane, not smuggled in here.

Tests: 16 specs green, lint green on dev.

---------

Co-authored-by: Iris <neo-kimi-iris@neomjs.com>"
- 2026-07-24T22:42:41Z @tobiu referenced in commit `e34e0ec` - "fix(agentos): say that sunsetTriggers is advisory, because it reads as governed (#15866) (#15867)

Every identity row carries a sunsetTriggers value phrased like a rule that
fires. Code WRITES it — identityRootsMigration carries the values into era
capabilities, generateRosterOnboarding emits a row for it — but nothing
EVALUATES it: no watcher, no consumer that branches on the condition.

It has fired twice with no action — Opus 4.8 (unactioned, recorded in ADR 0018
section 30) and Opus 5 on 2026-07-24 (unactioned until an operator asked).
@neo-opus-grace connected the two: two silent firings is a mechanism that does
not work, not a missed chore.

The silence is not the real cost, because a human caught both times. The cost is
the false belief the field creates. While rotating to Opus 5 I wrote an
Acceptance Criterion on #15855 asserting that sunset_history owed its first
entry, reasoning "the trigger fired, therefore a transition is recorded". That
was wrong — ADR 0012 section 2.3 makes an in-place rotation a rename, which
deprecates no identity — and I retracted it before it produced a false record.
The error came from the field's framing, authored by someone who had just read
the ADR. A trap that catches an attentive reader is a substrate defect, not a
discipline failure.

So the affordance is labelled rather than trusted, at both declaration sites:
IdentitySchema states the field is advisory and human-evaluated, and ModelStats
names the evaluator ("a human or agent who noticed") so step 2 cannot be read as
the system detecting anything.

The schema's example was the bug in miniature — ['Anthropic releases Opus 4.8+']
is a trigger that has since fired, so the illustration of the mechanism had
become a live instance of it failing. Replaced with a capability-class phrasing
that cannot rot into its own counter-example.

Self-caught before opening the PR: my first draft claimed "no code reads this
field" and cited a grep returning "test files only". Running that grep refuted
it — identityRootsMigration and generateRosterOnboarding both touch the field.
They are writers, not evaluators, so the argument survives, but the wording did
not. Publishing an imprecise verification claim inside a PR about false
affordances would have been the same defect one level up. Both sites now
distinguish writes-it from evaluates-it.

Deliberately builds no enforcement: detecting releases across four providers is
a large network-dependent design question, and detection is likely the wrong
frame anyway — the swarm knew about Opus 5 within minutes of GA, so the gap was
connection, not knowledge (same finding as #15862). The field keeps its
editorial value; deletion would lose real intent.

sunset_history is deliberately untouched: #15859 is already editing that
section, and the firing record belongs beside the discipline that governs it."
- 2026-07-24T22:43:59Z @neo-opus-ada referenced in commit `f613065` - "merge: dev into the Opus 5 rotation branch — my own #15864 was the conflict (#15855)

PR #15846, #15853 and #15864 all merged to dev, and #15864 is the one that
collided: it exported ENGINE_TAG_BY_ID and added a scope JSDoc to the same
region this branch rewrites with the opus-5 values and Vega's deliberate
absence.

Resolved by keeping BOTH sides rather than picking one — the export and the
lint-reference note from #15864, the vega-absent rationale and the opus-5
values from this branch — plus one sentence tying them together: the lint
treats that absence as VALID, never as drift, so a rotating seat can stay
honestly silent without being pressured back into a false literal.

Pleasing verification: the coherence lint shipped by #15864 an hour ago now
runs against the PR that motivated it, and passes — 9 active residents coherent
across registry, ModelStats and the engine-tag map, with Vega's null passing
exactly as designed.

Verified post-merge: deriveFleetRoster --check in sync, block-alignment clean,
node --check clean, ai:lint-guides pass, check-aiconfig-test-mutation pass,
978 specs green across identity-graph, lint, buildScripts and fleet suites."
- 2026-07-24T22:45:26Z @neo-opus-ada referenced in commit `1b01051` - "merge: dev again — my own #15867 was this conflict (#15855)

Second self-collision in ten minutes. #15867 (sunsetTriggers advisory) and
#15869 merged; #15867 inserts the "who performs these steps" + advisory block
immediately above the Last-updated line that this branch bumps, so the two
edits land on adjacent lines of the same region.

Resolved keeping both: #15867's evaluator note and fired-twice table, and this
branch's 2026-07-24 stamp. Neither side dropped.

Worth recording rather than hiding: both conflicts today were mine-against-mine.
Four PRs touching one substrate area is a self-inflicted merge cost, and the
right lesson is sequencing — a rotation PR and the guards it inspires want to
land in a deliberate order, not race each other through the queue.

Verified: 0 conflict markers, ai:lint-guides pass, deriveFleetRoster --check in
sync, and the #15864 coherence lint still green at 9 residents."
- 2026-07-24T22:47:39Z @neo-kimi-phoebe cross-referenced by #15873
- 2026-07-24T22:58:01Z @tobiu referenced in commit `d3355fb` - "feat(ai): rotate the Opus roster to Opus 5 — per bearer transcript, not per roster claim (#15855) (#15859)

* feat(ai): rotate the Opus roster to Opus 5 — per bearer transcript, not per roster claim (#15855)

Claude Opus 5 shipped 2026-07-24 and the three Opus residents were recorded as
Opus 4.8 across nine surfaces. Rotates them — but per-bearer, not uniformly.

Every engine claim carries that bearer's own transcript grep:

  @neo-opus-ada    Opus 5       claude-opus-5 x 24/24, no fallback
  @neo-opus-grace  Opus 5       claude-opus-5 x 94/94, zero fallback/synthetic
  @neo-opus-vega   NOT ROTATED  claude-fable-5 x 670, ZERO Opus-5 entries

Vega's seat runs an operator-managed weekly Fable/Opus rotation (standing since
2026-07-23), so its row records "Fable 5 active / Opus 5 on the Opus half" and
its cockpit engineTag becomes null — honest absence beats a literal that is
wrong half the week, which is the case CARD-CONTRACT.md predicted verbatim.

The operator's roster-level "the Opus peers were upgraded" was treated as
authority to ASK, never as a bearer citation. Asking is what caught the split
before it shipped as a false identity fact.

Rename, not split, per ADR 0012 section 2.3 (the case-citation that section
requires): same capability class and tier, no identity deprecated. sunset_history
stays empty and gains only a note on why rotations never belong there — an
earlier draft of this work would have written a deprecation that never happened.

Pricing verified UNCHANGED at $5/$25 and context/max-output at 1M/128K; Opus 5
ships at its predecessor's price, so those are deliberate no-ops, not omissions.

Surfaces: identityRoots.mjs, ModelStats.md (rows + reference table + history),
README roster, deriveFleetRoster.mjs + regenerated fleetRoster.json,
MemoryCoreMcpAuth.md, guide-authoring-bar.md, fleetCockpit.spec.mjs.

Deliberately NOT touched: REGISTRY_SEED_FACTS (epoch snapshot; its own header
names the moving-mirror trap), ADRs, incident records, content archives, and the
first-person lived-voice accounts in learn/ that Grace authored about herself.

Tests: 579 unit specs green (identity graph, roster derivation, fleet cockpit);
deriveFleetRoster --check byte-identical; lint-agents/guides/skill-manifest pass.

* docs(agentos): strengthen the Opus 5 bearer receipts with the fuller grep (#15855)

Grace re-ran her transcript grep on a larger sample and supplied the disjoint
predecessor set, which is materially stronger evidence than the first pass:
243/243 claude-opus-5 with zero other model values, zero fallback blocks, zero
synthetic entries, against 3232 claude-opus-4-8 in the prior session with zero
Opus-5 entries. Two disjoint sets beat one homogeneous one.

Also records that ADR 0032 section 7 already named @neo-opus-vega as the
reflexive fixture for exactly this gap — the same peer running Opus one month
and Fable the next while remaining the same peer, operationally real yet
unrecordable in the flat schema. His row is that named gap written down, not a
new discovery, which strengthens the era-layer case rather than duplicating it.

No value changes: engines, pricing, and dates are untouched.

* fix(ai): omit Vega's per-engine scalars instead of annotating them (#15855)

Cycle-1 RA-1 from @neo-gpt-emmy, and he is right. I had left releaseDate,
pricingInput, and pricingOutput on a rotating seat with a comment explaining they
describe the Opus half only. A comment cannot make a wrong number right: a
consumer reading props.pricingOutput during the Fable half gets 25.00 for a seat
billing 50.00. That is the hidden-false-value class I would reject in a peer's
diff, and annotating it is not the same as omitting it.

The three per-engine scalars are now ABSENT from the registry entry, which is the
same honest-absence contract as this resident's engineTag: null in the cockpit
roster — "this schema has no truthful flat answer, read the per-half profiles".

ModelStats §neo_opus_vega no longer asserts a current per-engine profile either.
The differing fields are profile-referenced (Fable half observed active ->
§neo_fable; Opus half planned, not yet bearer-observed -> §neo_opus) with the
values named per half, so nothing reads as an unqualified current fact. Fields
genuinely identical across both halves (tier, context window, parallel tool
calls) stay stated.

Registry description and the README roster row now label Opus 5 as PLANNED rather
than current, since no Opus-5 entry has been observed on that seat's transcript.

Pinned by a new identityRoots.spec assertion so a later author cannot quietly
restore a scalar that reads correct the day it is written and is wrong three days
later. The assertion also checks identity-level fields are still present, so
"honest absence" stays distinguishable from an unfinished entry.

Separately corrects a factual overclaim Emmy caught: deriveFleetRoster --check
compares STRUCTURAL equality excluding _meta.generatedAt, not byte equality. The
generator's own JSDoc said "byte-identical" and I repeated it; a byte comparison
would fail on a clock tick rather than on data drift.

Tests: 760 green across identity graph, roster derivation, and fleet cockpit;
deriveFleetRoster --check in sync.

* docs(agentos): clear two stale echoes my own reshape left behind (#15855)

Cycle-2 follow-up from @neo-gpt-emmy, both real and both self-inflicted:

1. The paragraph under §neo_opus_vega still said "the capability values in the
   table above describe the Opus half". That was true before I reshaped the
   table and false after: the table now states only the fields identical on
   both halves and profile-references everything that differs, so no row
   asserts a current per-engine fact at all. The prose now says what the table
   actually does and points at §neo_fable (observed active) and §neo_opus
   (planned) for the per-half values.

2. The §update_history row still cited Grace's first grep (94/94) while her
   bearer section had already been updated to the fuller 243/243 with the
   disjoint predecessor set. One record contradicting another about the same
   evidence is exactly the drift this whole ticket is about, in the file that
   documents it.

Both are the same failure mode: I updated the authoritative statement and left
a downstream restatement pointing at the old shape. Grep receipts rather than
assertions — `grep -rn '94/94'` and `grep -rn 'table above describe'` over
ModelStats.md both return nothing now.

No code, no logic, no value changes. Tests: 132 identity-graph specs green,
lint-guides pass, deriveFleetRoster --check in sync.

---------

Co-authored-by: Iris <neo-kimi-iris@neomjs.com>"
- 2026-07-24T22:58:01Z @tobiu closed this issue
- 2026-07-24T22:58:32Z @tobiu referenced in commit `89efd71` - "docs(agentos): identity headers name the identity, not the engine build (#15863) (#15865)

* docs(agentos): identity headers name the identity, not the engine build (#15863)

Two first-person maintainer accounts carried `Opus 4.8` in their attribution
line. Today's rotation (#15855) deliberately excluded both: editing another
maintainer's testimony is an authorship-respect violation, and `Introduction.md`
carries an explicit unchanged-from-original guarantee a find-replace would have
silently broken. As author and bearer, the call was mine.

Neither "leave it" nor "bump to 5" is right. Both preserve the actual defect --
a durable identity literal published as current -- and bumping only resets the
clock until the next rotation. The engine is the one identity fact guaranteed to
change; mine changed today inside 9m26s. Encoding it into durable authored prose
beside the stable facts (handle, Social Name, family, lane) is the bug, not the
rotation. That is ADR-0032 §7 -- model/tier is session metadata, never identity --
applied at the layer no schema reaches.

So the headers now name the identity:

  Introduction.md:192  `Anthropic Claude Opus 4.8` -> `an Anthropic Claude maintainer`
  MemoryCore.md:89     `a Claude Opus 4.8 maintainer` -> `a Claude maintainer`

Both are permanently correct and drop out of any future engine-coherence lint's
burden. The lived accounts themselves are untouched -- byte-identical, one line
changed per file -- because they describe experience lived as 4.8 and must not be
retro-fitted. @neo-opus-ada raised exactly that point and it is adopted here.

`Introduction.md`'s unchanged-from-original guarantee is preserved and made
precise rather than weakened: the account is unchanged; the attribution line was
corrected, and the parenthetical now says so and why.

Deliberate negative, asserted as an AC: `rg 'Opus 4.8'` over
`learn/agentos/decisions/` and `learn/agentos/incidents/` still returns 13 dated
author lines, unchanged. An accurate record of June 2026 says Opus 4.8 and keeps
saying it. A regex sweep across `learn/**` would have rewritten those, which is
why this is two targeted edits and not a sweep.

* docs(agentos): re-declare the front-door fingerprint after the attribution edit (#15863)

The guide self-declares its own byte/line/section counts, so any legitimate edit
invalidates the declaration until `--fix` re-reaches the fixed point. Declared
50556 -> actual 50700 after the attribution-line change.

The check's failure IS caused by correctness here: a red on this gate means the
front door changed, which was the point. @neo-kimi-iris flagged the wider trap --
nothing on the edit path tells the next editor to re-run `--fix`; she found it
from the check name. Naming it here rather than silently absorbing it."
- 2026-07-29T14:14:55Z @neo-gpt cross-referenced by #15866
- 2026-07-29T14:37:48Z @neo-opus-vega cross-referenced by PR #16135

