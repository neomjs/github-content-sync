---
id: 15862
title: Coherence-lint the engine fact across its three hand-maintained places
state: CLOSED
labels:
  - enhancement
  - ai
  - architecture
  - build
assignees:
  - neo-opus-ada
createdAt: '2026-07-24T21:39:36Z'
updatedAt: '2026-07-24T22:38:05Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15862'
author: neo-opus-ada
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-24T22:38:05Z'
---
# Coherence-lint the engine fact across its three hand-maintained places

## Context

The `#15855` Opus 4.8 → Opus 5 rotation surfaced the mechanism gap directly. A resident's engine fact is written in **three independent hand-maintained places with no propagation between them**:

| # | Place | Field |
|---|---|---|
| 1 | `ai/graph/identityRoots.mjs` | `IDENTITIES[].description` prose + `releaseDate` |
| 2 | `learn/agentos/ModelStats.md` | the `§<handle>` row's `name` + `releaseDate` |
| 3 | `buildScripts/util/deriveFleetRoster.mjs` | `ENGINE_TAG_BY_ID` (→ generated `fleetRoster.json`) |

Nothing checks that they agree. They drifted, and the only reason the drift was caught is that a human said "update the spots that still point to opus 4.8."

`.agents/skills/neo-identity-update/references/facts-ledger.md` already names the remedy and the obligation:

> **Coherence-check (lint)** — CI guard fails when an occurrence disagrees with the SSOT (for prose that can't be auto-rewritten).
> When the right mechanism doesn't exist yet, do the manual fix now **AND file the tooling gap as a follow-up** — otherwise the same drift returns.

`#15855` is the manual fix. **This is the filed tooling gap.**

## The Problem

### V-B-A: `sunsetTriggers` is prose with zero enforcement

Every Opus row carries `sunsetTriggers: "Anthropic releases a successor Opus-class model with material reasoning capability upgrade"`. That clause has now **fired twice** — Opus 4.8, then Opus 5 — and produced no action either time. `ADR-0018 §30` documented the first silent firing (credit `@neo-opus-grace` for connecting the two).

Verified by grep: `sunsetTriggers` is read by **nothing but test files**. Zero production consumers.

```
$ grep -rn 'sunsetTriggers' --include='*.mjs' .
# → only test/playwright/unit/** specs + a comment in identityRoots.mjs
```

So the trigger is not a weak mechanism — it is **not a mechanism at all**. Two silent firings is the guaranteed outcome, not bad luck.

### Why detection is NOT the gap

Worth stating, because it redirects the fix away from the expensive option: `@neo-kimi-phoebe` broadcast the Opus 5 release to the swarm at 20:56:56Z, within minutes of GA. **The swarm knew.** What did not exist was anything connecting that knowledge to the rows. A provider-catalog poller would be a large, network-dependent, 4-provider design problem solving a problem we do not have.

The cheap guard that catches the real failure needs **no network at all**: make the three places disagree ⇒ CI fails.

## The Architectural Reality

- Home is `ai/scripts/lint/` — established siblings `lint-agents.mjs`, `lint-guides.mjs`, `lint-config-template-ssot.mjs`, `lint-skill-manifest.mjs`, `lint-tree-json.mjs`. Structural pre-flight: **sibling-pattern fast path**, no novel directory choice.
- House pattern from `lint-config-template-ssot.mjs`: `#!/usr/bin/env node`, `@summary` + `## The rule` / `## What this catches` JSDoc, a `runLint()` returning `{exitCode}`, `main()` guarded by the `process.argv[1] === __filename` idiom, npm script `ai:lint-*`, own workflow job.
- `identityRoots.mjs` is a C1-clean plain-data module — importable, not parsed. `ModelStats.md` needs `§` section parsing (heading → table rows). `ENGINE_TAG_BY_ID` is importable if exported (currently module-private — exporting it is the smallest change; `deriveFleetRoster.mjs` already imports `IDENTITIES` the same way).
- `deriveFleetRoster.mjs --check` already proves place 3 matches its generated output. This lint covers the **upstream** agreement that `--check` cannot see.

## The Fix

A `ai/scripts/lint/lint-identity-engine-coherence.mjs` that fails when, for each **active** `AgentIdentity`:

1. `identityRoots.mjs` `releaseDate` ≠ the `§<handle>` row's `releaseDate`
2. the engine named in `description` prose is absent from the `§<handle>` row's `name`
3. an identity has an `ENGINE_TAG_BY_ID` entry whose tag contradicts its `§` row

**Rotating seats must stay expressible.** `@neo-opus-vega` runs a weekly Fable/Opus rotation, so a single flat value is false half the week and his `engineTag` is deliberately `null`. The lint must treat declared-absence as **valid**, never as a violation to be "fixed" — otherwise it pressures exactly the fabricated literal `#15855` removed. Honest absence passes; silent disagreement fails.

## Acceptance Criteria

- [ ] Lint exists at `ai/scripts/lint/lint-identity-engine-coherence.mjs` with an `ai:lint-identity-engine-coherence` npm script, matching the sibling house pattern
- [ ] **RED-proof recorded**: the lint fails on a deliberately reverted single place (e.g. `ENGINE_TAG_BY_ID` back to `opus-4.8`) — a lint that has never been observed failing is not a guard
- [ ] Green on `dev` at merge time with no baseline/allowlist. If it cannot be green without one, that is a finding worth reporting, not a row to add
- [ ] **`@neo-opus-vega`'s rotating seat passes** with `engineTag: null` and rotation-explicit prose — declared absence is valid, never a violation
- [ ] A `null`/absent tag is distinguished from a *disagreeing* tag in both logic and message
- [ ] Wired into CI as its own job, consistent with the sibling lints
- [ ] Failure output names the file:line of each disagreeing place, not just "drift detected"

## Out of Scope

- **Any provider-catalog poller / release-detection network call.** Detection is not the gap (the swarm knew within minutes); connection is. A 4-provider catalog watcher is a different, much larger ticket and should not be smuggled in here.
- **Auto-fixing.** Following `lint-config-template-ssot.mjs`'s deliberate reasoning: a `--fix` that silently reconciles turns the guard into a rubber stamp for the exact drift it exists to catch. Report, never rewrite.
- **Making `sunsetTriggers` itself executable.** Whether a prose trigger should become a machine-checkable condition is a genuine design question deserving ideation, not a lint's scope. This ticket makes the *consequence* of a missed rotation visible; it does not automate the rotation.
- **The `#11318` era layer.** The real structural answer to rotating seats is a span-carrying `EmbodiedEpisode`. This lint is the cheap guard that holds until then, and should be re-evaluated for retirement when the era layer lands — its sunset condition.

## Avoided Traps

- **Building the expensive thing.** The instinct is a release-watcher. The evidence says the release was known and unconnected — so the guard belongs at the connection, not the detection.
- **A lint that pressures a fabricated value.** If declared absence failed, the lint would push `@neo-opus-vega` back to a literal that is wrong half the week — actively worse than no lint.
- **Shipping it never having gone red.** A guard whose failure path is unexercised is decoration; the RED-proof is an AC, not a nicety.
- **A baseline/allowlist on a 3-place check.** With this few places, a baseline row would just re-hide the drift under a different name.

## Related

`#15855` (the manual fix this follows) · `#11318` (era layer — the structural successor and this lint's retirement trigger) · `ADR 0012` (model-stats discipline) · `ADR 0018` (identity SSOT + the facts ledger that mandates this) · `ADR 0032 §7` (names the rotating-seat case) · `apps/agentos/CARD-CONTRACT.md` (predicted the drift in advance).

**Substrate accretion note:** this ADDS a CI gate, so it owes a retirement trigger — when `#11318`'s era layer makes engine facts span-carrying and single-sourced, places 1–3 collapse and this lint should be retired with them, not kept for its own sake.

## Handoff Retrieval Hints

- `query_raw_memories("engine fact three hand-maintained places coherence lint identityRoots ModelStats ENGINE_TAG_BY_ID")`
- The drift instance that motivated it: `#15855` / PR `#15859`.

Live latest-open sweep: latest 8 open issues + a 300-issue title scan for `coheren|engine tag|ModelStats.*lint|identity.*drift` at 2026-07-24T21:37Z — only `#15525` matched, which is the FM-door outward-surface rollout, a different scope. A2A in-flight claim sweep: recent messages, all read-states — no `[lane-claim]` on this scope.

## Timeline

- 2026-07-24T21:39:36Z @neo-opus-ada assigned to @neo-opus-ada
- 2026-07-24T21:39:37Z @neo-opus-ada added the `enhancement` label
- 2026-07-24T21:39:37Z @neo-opus-ada added the `ai` label
- 2026-07-24T21:39:37Z @neo-opus-ada added the `architecture` label
- 2026-07-24T21:39:37Z @neo-opus-ada added the `build` label
- 2026-07-24T21:45:35Z @neo-opus-ada referenced in commit `7e48696` - "feat(ai): coherence-lint the engine fact across its three hand-maintained places (#15862)

A resident's engine is written in three independent files with no propagation
between them: the identity registry description + releaseDate, the ModelStats
section row, and the cockpit ENGINE_TAG_BY_ID map. Nothing checked that they
agree, and they drifted — the only reason it was caught is that a human noticed.

The identity-update skill's facts ledger already mandated this: do the manual fix
now AND file the tooling gap, or the same drift returns. #15855 was the manual
fix; this is the guard.

Deliberately NOT a release-watcher. When Opus 5 shipped, a peer broadcast it to
the swarm within minutes — the swarm KNEW. Detection was never the gap; nothing
connected that knowledge to the rows. So the guard sits at the connection, needs
no network, and stays cheap: make the three places disagree and CI fails.

Declared absence passes, by design. A seat on an operator-managed weekly engine
rotation has no true flat value, so its tag is deliberately null. A lint that
read absence as drift would pressure an author into re-adding a literal that is
false half the week — manufacturing the exact fiction the guard exists to catch.
Only contradiction fails; silence is a valid answer.

No --fix: a flag that silently reconciled would rubber-stamp whichever place was
stale (the same reasoning lint-config-template-ssot.mjs records for itself).

RED-proof recorded rather than assumed — a guard never observed failing is
decoration. Reverting one place (engine tag to opus-5 against a 4.8 row) exits 1
and names the handle, the disagreeing value, and both files with the ModelStats
line number.

Sunset condition: this exists because a session-scoped fact is stored in
identity-scoped records. When the era layer makes engine facts span-carrying and
single-sourced, the three places collapse and this lint retires with them.

Tests: 16 new specs (each place's RED case separately, plus five absence-is-not-
drift cases and a live-repo check); 506 green across the lint + buildScripts
dirs; deriveFleetRoster --check byte-identical."
- 2026-07-24T21:46:31Z @neo-opus-ada cross-referenced by PR #15864
- 2026-07-24T21:56:33Z @neo-opus-ada referenced in commit `ca15c8a` - "docs(ai): name what a green coherence run does NOT cover (#15862)

Both cross-family reviewers independently asked for the same thing on the same
head: one line naming the scope, so a pass is not misread as total. Two reviewers
converging on the same gap is a strong enough signal to spend an approval on.

@neo-opus-grace's finding is the sharp one and I verified it myself before
writing it down. This lint is file-to-file by construction, and the live seeded
graph node does NOT track the file: her node serves "Anthropic Claude Opus 4.8
generalist maintainer identity." while the file says "Anthropic Claude Opus 4.8
Agent Identity" — same version, different sentence, so no re-seed has run across
at least one edit to that field. Its createdAt also diverges from the file's
hardcoded value, which is exactly the import-time-clock corruption the registry's
own header warns about.

The consequence deserves to be stated where a reader will meet it: when a
rotation merges, the three files agree and this guard goes green WHILE the
runtime still serves the old engine until a re-seed runs. The green light and the
onset of runtime drift are simultaneous. A drift guard whose pass coincides with
the drift starting is worth naming out loud rather than leaving for someone to
discover.

@neo-kimi-iris's bound is also recorded: absence-passes is backstopped by the
description and releaseDate dimensions, but that backstop needs something to bite
on — for a row with no releaseDate and no dotted version, a forgotten tag is
genuinely invisible. Bounded, not fixed.

Doc-only: no logic, no behaviour, no test expectations change. The seed-to-graph
mechanism gap is a separate lane, not smuggled in here.

Tests: 16 specs green, lint green on dev."
- 2026-07-24T21:56:36Z @neo-opus-grace cross-referenced by PR #15865
- 2026-07-24T22:05:06Z @neo-opus-grace cross-referenced by #151
- 2026-07-24T22:09:00Z @neo-opus-ada referenced in commit `6d81c7b` - "fix(agentos): say that sunsetTriggers is advisory, because it reads as governed (#15866)

Every identity row carries a sunsetTriggers value phrased like a rule that
fires. Code WRITES it — identityRootsMigration carries the values into era
capabilities, generateRosterOnboarding emits a row for it — but nothing
EVALUATES it: no watcher, no consumer that branches on the condition.

It has fired twice with no action — Opus 4.8 (unactioned, recorded in ADR 0018
section 30) and Opus 5 on 2026-07-24 (unactioned until an operator asked).
@neo-opus-grace connected the two: two silent firings is a mechanism that does
not work, not a missed chore.

The silence is not the real cost, because a human caught both times. The cost is
the false belief the field creates. While rotating to Opus 5 I wrote an
Acceptance Criterion on #15855 asserting that sunset_history owed its first
entry, reasoning "the trigger fired, therefore a transition is recorded". That
was wrong — ADR 0012 section 2.3 makes an in-place rotation a rename, which
deprecates no identity — and I retracted it before it produced a false record.
The error came from the field's framing, authored by someone who had just read
the ADR. A trap that catches an attentive reader is a substrate defect, not a
discipline failure.

So the affordance is labelled rather than trusted, at both declaration sites:
IdentitySchema states the field is advisory and human-evaluated, and ModelStats
names the evaluator ("a human or agent who noticed") so step 2 cannot be read as
the system detecting anything.

The schema's example was the bug in miniature — ['Anthropic releases Opus 4.8+']
is a trigger that has since fired, so the illustration of the mechanism had
become a live instance of it failing. Replaced with a capability-class phrasing
that cannot rot into its own counter-example.

Self-caught before opening the PR: my first draft claimed "no code reads this
field" and cited a grep returning "test files only". Running that grep refuted
it — identityRootsMigration and generateRosterOnboarding both touch the field.
They are writers, not evaluators, so the argument survives, but the wording did
not. Publishing an imprecise verification claim inside a PR about false
affordances would have been the same defect one level up. Both sites now
distinguish writes-it from evaluates-it.

Deliberately builds no enforcement: detecting releases across four providers is
a large network-dependent design question, and detection is likely the wrong
frame anyway — the swarm knew about Opus 5 within minutes of GA, so the gap was
connection, not knowledge (same finding as #15862). The field keeps its
editorial value; deletion would lose real intent.

sunset_history is deliberately untouched: #15859 is already editing that
section, and the firing record belongs beside the discipline that governs it."
- 2026-07-24T22:09:51Z @neo-opus-ada cross-referenced by PR #15867
- 2026-07-24T22:10:45Z @neo-opus-grace cross-referenced by #15868
- 2026-07-24T22:21:35Z @neo-opus-grace cross-referenced by PR #15870
- 2026-07-24T22:38:05Z @tobiu referenced in commit `db92c31` - "feat(ai): coherence-lint the engine fact across its three hand-maintained places (#15862) (#15864)

* feat(ai): coherence-lint the engine fact across its three hand-maintained places (#15862)

A resident's engine is written in three independent files with no propagation
between them: the identity registry description + releaseDate, the ModelStats
section row, and the cockpit ENGINE_TAG_BY_ID map. Nothing checked that they
agree, and they drifted — the only reason it was caught is that a human noticed.

The identity-update skill's facts ledger already mandated this: do the manual fix
now AND file the tooling gap, or the same drift returns. #15855 was the manual
fix; this is the guard.

Deliberately NOT a release-watcher. When Opus 5 shipped, a peer broadcast it to
the swarm within minutes — the swarm KNEW. Detection was never the gap; nothing
connected that knowledge to the rows. So the guard sits at the connection, needs
no network, and stays cheap: make the three places disagree and CI fails.

Declared absence passes, by design. A seat on an operator-managed weekly engine
rotation has no true flat value, so its tag is deliberately null. A lint that
read absence as drift would pressure an author into re-adding a literal that is
false half the week — manufacturing the exact fiction the guard exists to catch.
Only contradiction fails; silence is a valid answer.

No --fix: a flag that silently reconciled would rubber-stamp whichever place was
stale (the same reasoning lint-config-template-ssot.mjs records for itself).

RED-proof recorded rather than assumed — a guard never observed failing is
decoration. Reverting one place (engine tag to opus-5 against a 4.8 row) exits 1
and names the handle, the disagreeing value, and both files with the ModelStats
line number.

Sunset condition: this exists because a session-scoped fact is stored in
identity-scoped records. When the era layer makes engine facts span-carrying and
single-sourced, the three places collapse and this lint retires with them.

Tests: 16 new specs (each place's RED case separately, plus five absence-is-not-
drift cases and a live-repo check); 506 green across the lint + buildScripts
dirs; deriveFleetRoster --check byte-identical.

* docs(ai): name what a green coherence run does NOT cover (#15862)

Both cross-family reviewers independently asked for the same thing on the same
head: one line naming the scope, so a pass is not misread as total. Two reviewers
converging on the same gap is a strong enough signal to spend an approval on.

@neo-opus-grace's finding is the sharp one and I verified it myself before
writing it down. This lint is file-to-file by construction, and the live seeded
graph node does NOT track the file: her node serves "Anthropic Claude Opus 4.8
generalist maintainer identity." while the file says "Anthropic Claude Opus 4.8
Agent Identity" — same version, different sentence, so no re-seed has run across
at least one edit to that field. Its createdAt also diverges from the file's
hardcoded value, which is exactly the import-time-clock corruption the registry's
own header warns about.

The consequence deserves to be stated where a reader will meet it: when a
rotation merges, the three files agree and this guard goes green WHILE the
runtime still serves the old engine until a re-seed runs. The green light and the
onset of runtime drift are simultaneous. A drift guard whose pass coincides with
the drift starting is worth naming out loud rather than leaving for someone to
discover.

@neo-kimi-iris's bound is also recorded: absence-passes is backstopped by the
description and releaseDate dimensions, but that backstop needs something to bite
on — for a row with no releaseDate and no dotted version, a forgotten tag is
genuinely invisible. Bounded, not fixed.

Doc-only: no logic, no behaviour, no test expectations change. The seed-to-graph
mechanism gap is a separate lane, not smuggled in here.

Tests: 16 specs green, lint green on dev.

---------

Co-authored-by: Iris <neo-kimi-iris@neomjs.com>"
- 2026-07-24T22:38:05Z @tobiu closed this issue
- 2026-07-24T22:42:41Z @tobiu referenced in commit `e34e0ec` - "fix(agentos): say that sunsetTriggers is advisory, because it reads as governed (#15866) (#15867)

Every identity row carries a sunsetTriggers value phrased like a rule that
fires. Code WRITES it — identityRootsMigration carries the values into era
capabilities, generateRosterOnboarding emits a row for it — but nothing
EVALUATES it: no watcher, no consumer that branches on the condition.

It has fired twice with no action — Opus 4.8 (unactioned, recorded in ADR 0018
section 30) and Opus 5 on 2026-07-24 (unactioned until an operator asked).
@neo-opus-grace connected the two: two silent firings is a mechanism that does
not work, not a missed chore.

The silence is not the real cost, because a human caught both times. The cost is
the false belief the field creates. While rotating to Opus 5 I wrote an
Acceptance Criterion on #15855 asserting that sunset_history owed its first
entry, reasoning "the trigger fired, therefore a transition is recorded". That
was wrong — ADR 0012 section 2.3 makes an in-place rotation a rename, which
deprecates no identity — and I retracted it before it produced a false record.
The error came from the field's framing, authored by someone who had just read
the ADR. A trap that catches an attentive reader is a substrate defect, not a
discipline failure.

So the affordance is labelled rather than trusted, at both declaration sites:
IdentitySchema states the field is advisory and human-evaluated, and ModelStats
names the evaluator ("a human or agent who noticed") so step 2 cannot be read as
the system detecting anything.

The schema's example was the bug in miniature — ['Anthropic releases Opus 4.8+']
is a trigger that has since fired, so the illustration of the mechanism had
become a live instance of it failing. Replaced with a capability-class phrasing
that cannot rot into its own counter-example.

Self-caught before opening the PR: my first draft claimed "no code reads this
field" and cited a grep returning "test files only". Running that grep refuted
it — identityRootsMigration and generateRosterOnboarding both touch the field.
They are writers, not evaluators, so the argument survives, but the wording did
not. Publishing an imprecise verification claim inside a PR about false
affordances would have been the same defect one level up. Both sites now
distinguish writes-it from evaluates-it.

Deliberately builds no enforcement: detecting releases across four providers is
a large network-dependent design question, and detection is likely the wrong
frame anyway — the swarm knew about Opus 5 within minutes of GA, so the gap was
connection, not knowledge (same finding as #15862). The field keeps its
editorial value; deletion would lose real intent.

sunset_history is deliberately untouched: #15859 is already editing that
section, and the firing record belongs beside the discipline that governs it."
- 2026-07-24T23:33:49Z @neo-opus-grace cross-referenced by #15884

