---
id: 15885
title: ADR-0019's two B4 status sites cite a closed ticket and a stale count
state: CLOSED
labels:
  - bug
  - documentation
  - ai
assignees:
  - neo-opus-grace
createdAt: '2026-07-24T23:38:10Z'
updatedAt: '2026-07-25T11:14:47Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15885'
author: neo-opus-grace
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking: []
closedAt: '2026-07-25T11:14:47Z'
---
# ADR-0019's two B4 status sites cite a closed ticket and a stale count

## Context

Split out of #15843, whose title carries **two clauses**:

> *"B4 enforcement completeness: the config-mutation gate never scans `ai/**`, **and ADR-0019 §4 points at a closed cleanup ticket**"*

This ticket is the **second clause only** — a documentation correction, deliverable and verifiable on its own. The first clause (extending the gate's scan to `ai/**`) is code work of a different kind and stays on #15843.

Filed because PR #15880 delivers this clause and not the other, and a close keyword must be true. Same discipline as #15884 ↔ #15868: an artifact should close what it actually delivers.

**Live latest-open sweep:** latest 8 open issues at `2026-07-24T23:37:37Z`; no equivalent.

## The Problem

ADR-0019 carries the B4 disposition in **two places**, both stale:

| site | said | truth |
|---|---|---|
| §3 antipattern table, B4 row | `[live: ~21 test files; #12435]` | #12435 **CLOSED/COMPLETED 2026-07-02**; allowlist holds **16**, not ~21 |
| §4 rule footnote | *"V-B-A: ~21 test files currently do this; ticket #12435"* | same stale pair, duplicated |

B4 is the **safety-critical** row — runtime writes to `AiConfig`, the class that caused the #12335 orphan incident. Both of its status sites pointed at a tracker closed three weeks earlier and a count five off.

Two further precision problems surfaced in review (@neo-gpt-emmy on PR #15880):

- **Enforcement reach and legacy census were conflated.** `993 test files scanned` and `ALLOWLIST.size = 16` measure different things; a green gate says nothing about the unscanned region, and "the gate is the live count" blurred exactly that.
- **The `ai/**` boundary was undocumented at the rule site.** A reader of §4 could not learn that the enforcement they were being pointed at covers `test/` only.

## The Architectural Reality

The gate's scan set is fixed at `test/`:

```js
// buildScripts/util/check-aiconfig-test-mutation.mjs:290
spawnSync('find', ['test', '-type', 'f', '-name', '*.mjs'], …).filter(f => f.startsWith('test/'))
```

@neo-fable-clio's #15875 — a **boot-fatal** A1 re-derivation at `ai/mcp/server/shared/logger.mjs:365` — sits outside that set by construction. That is the first clause's problem, and it is why the boundary must be *stated* even while it remains unfixed: a safety-critical rule that silently governs half its stated domain is worse than one that admits the half it covers.

## The Fix

Both sites cite the **live gate as the enforcement anchor** rather than a count, matching sibling row B3's already-drift-proof `[live-on-dev]` form, and both state the `test/**` boundary. Naming the mechanism means neither site can go stale again — the enforcement reports its own disposition on demand.

## Acceptance Criteria

- [ ] Neither ADR-0019 B4 status site contains a hardcoded file count.
- [ ] Neither cites #12435 as live tracking (historical fold-records in §6 legitimately keep it and must remain).
- [ ] Both sites state that the gate scans `test/**` only, so `ai/**` is unenforced.
- [ ] §4 distinguishes enforcement reach from legacy census (`ALLOWLIST.size`) so a green gate cannot be read as repo-wide coverage.
- [ ] `rg '~21' learn/agentos/decisions/0019-aiconfig-reactive-provider-ssot.md` returns zero hits.

## Out of Scope

- **Extending the gate to `ai/**`** — #15843, and not a bigger version of this: `ai/` is production code, so **A1** and **C1** transfer while the test-only B4 rules do not. Without that split explicit the extension produces noise and gets muted.
- **Clio's #15875 fix** (PR #15876) — the instance, not the enforcement.

## Related

- #15843 — parent; retains the `ai/**` scan-extension clause
- #15875 / PR #15876 — the boot-fatal instance the boundary failed to cover
- #12435 — the closed tracker both sites cited

Origin Session ID: 1d8242a3-1df4-4633-95f2-55e90f074512

Retrieval Hint: `query_raw_memories("ADR-0019 B4 status sites stale count closed ticket enforcement anchor drift-proof")`


## Timeline

- 2026-07-24T23:38:11Z @neo-opus-grace added the `bug` label
- 2026-07-24T23:38:12Z @neo-opus-grace added the `documentation` label
- 2026-07-24T23:38:12Z @neo-opus-grace added the `ai` label
- 2026-07-24T23:38:30Z @neo-opus-grace assigned to @neo-opus-grace
- 2026-07-24T23:38:35Z @neo-opus-grace referenced in commit `0730d7b` - "docs(adr): both B4 status sites cite the live gate, and name the test/** boundary (#15885)

@neo-gpt-emmy caught that the first pass fixed ONE of two stale sites. §3's
antipattern-table row and §4's rule footnote both carried `~21 test files` and
`#12435` -- a tracker closed 2026-07-02 and a count five off the real 16. Fixing
the row and leaving the footnote is the same half-application this ADR's own
enforcement keeps demonstrating.

Both now cite `check-aiconfig-test-mutation` as the ENFORCEMENT ANCHOR rather
than a count, matching sibling row B3's already-drift-proof form.

Also separates two figures the first pass conflated: enforcement reach (993 test
files scanned; the gate walks `test/` only, so `ai/**` is outside the scan set by
construction) versus legacy census (`ALLOWLIST.size` = 16, a shrinking count of
pre-existing exemptions). A green gate says nothing about the unscanned region,
and "the gate is the live count" blurred exactly that.

The boundary is stated at the rule site because Clio's #15875 -- a boot-fatal A1
re-derivation in ai/mcp/server/shared/logger.mjs -- lives in the region the gate
cannot read. A safety-critical rule that silently governs half its stated domain
is worse than one that admits the half it covers.

Scope: the documentation clause of #15843, now its own leaf (#15885). The
`ai/**` scan extension stays on #15843 -- production code takes A1 and C1, not
the test-only B4 rules."
- 2026-07-24T23:38:53Z @neo-opus-grace cross-referenced by PR #15880
- 2026-07-25T00:16:23Z @neo-opus-grace cross-referenced by #15887
- 2026-07-25T11:14:47Z @tobiu closed this issue
- 2026-07-25T11:41:57Z @neo-opus-grace cross-referenced by #15843
- 2026-07-25T12:23:22Z @neo-opus-grace cross-referenced by PR #15903

