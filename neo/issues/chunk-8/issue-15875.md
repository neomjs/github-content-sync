---
id: 15875
title: 'Logger fallback re-derives the canonical logs path (A1, boot-fatal)'
state: CLOSED
labels:
  - bug
  - ai
  - architecture
assignees:
  - neo-fable-clio
createdAt: '2026-07-24T22:57:11Z'
updatedAt: '2026-07-25T00:17:22Z'
githubUrl: 'https://github.com/neomjs/neo/issues/15875'
author: neo-fable-clio
commentsCount: 0
parentIssue: null
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[x] 15803 Dev-compose completion against the elected plane placement'
closedAt: '2026-07-25T00:17:22Z'
---
# Logger fallback re-derives the canonical logs path (A1, boot-fatal)

## Context

Found by neomjs/neo#15803's AC5 first live run (receipts: neomjs/neo#15803 comment `IC_kwDODSospM8AAAABLoHMBQ`): the parity MC container crashed boot-fatal with `ENOENT: mkdir '/app/.neo-ai-data/logs'` — the CANONICAL logs path — despite the compose binding all 21 plane-member leaves to the parity root, including `NEO_MEMORY_LOG_PATH`.

## The Problem

`ai/mcp/server/shared/logger.mjs:365-366`:

```js
const logDir = loggerConfig.logPath || data.logPath ||
    path.resolve(data.neoRootDir || data.projectRoot || process.cwd(), '.neo-ai-data/logs');
```

The third branch is an **ADR-0019 §3 A1 antipattern** (module-level canonical-path re-derivation) combined with a **hidden default**: when a caller reaches `createLogger` with an empty/partial config (`createLogger(aiConfig = {})` — the default parameter invites it), the logger silently resolves the canonical plane's logs dir from rootDir/cwd, bypassing every declared leaf. The SSOT is nominally in scope (`data` IS the config read) — which makes the fallback exactly the class the catalog bans.

**Crash mechanism (the multiplier):** on partially-hydrated seats, `.neo-ai-data/logs` is a SYMLINK into the canonical checkout (verified on the authoring seat: `logs -> /Users/Shared/github/neomjs/neo/.neo-ai-data/logs`). In-container that symlink dangles (target outside the bind mount) → `fs.mkdirSync(logDir, {recursive: true})` at `:373` throws ENOENT → boot dies inside `initAsync` (`src/core/Base.mjs:315` anchor). The neomjs/neo#15803 dangle class, live-confirmed with the exact entry.

## The Architectural Reality

- ADR 0019 §3 A1 (re-derivation with the SSOT in scope) + the hidden-default class; §5 sanctioned form: read the resolved leaf at the use site, fail loud when it's absent.
- All three server config bases declare a `logPath` leaf (MC `:736` / KB `:267` / NL `:95`), each planeDataRoot-anchored and env-bound — the sanctioned source exists; the fallback shadows it.
- The parity compose (PR neomjs/neo#15871) is correct and was never consulted — which is the finding.

## The Fix

1. Remove the rootDir/cwd re-derivation branch. `logDir` resolves from `loggerConfig.logPath || data.logPath` and FAILS LOUD (named error) when neither is present — a logger with no resolved log path is a boot defect, not a silent canonical write (the same fail-loud posture as `seat-token`'s registry SETUP).
2. ~~Identify the empty/partial-config call chain~~ **Restated 2026-07-24 per PR neomjs/neo#15876 review (live authority delta):** the neomjs/neo#15803 integration rerun FALSIFIED the empty-config hypothesis — the measured mechanism is an **early write resolving the leaf's ANCHOR DEFAULT before the provider's env overlay applies** (the config is real; its `isReady` is false). The fix is a one-reality guard: while `aiConfig.isReady === false`, file-sink writes route to stderr — zero filesystem writes before the resolved overlay, on every seat shape.
3. Spec: fail-loud assertion (no-config → named error, never a canonical-path write) + a parity-shaped regression (bound `logPath` env wins over any default) + async stream-event containment + the pre-ready zero-write guard.

## Acceptance Criteria

- [ ] The rootDir/cwd fallback branch is gone; unresolved log path = named fail-loud error (spec).
- [ ] *(Restated per PR neomjs/neo#15876 review — mechanism falsified by the live rerun)* The pre-env-overlay write window is CLOSED: a not-ready provider produces ZERO file-system writes (stderr routing), and the measured mechanism is documented in place of the withdrawn empty-config chain hunt (spec: one-reality guard).
- [ ] Sink failures are contained synchronously AND asynchronously (WriteStream `error` events never escape as `uncaughtException`; spec with the directory-shaped-filename witness).
- [ ] Regression spec: bound `NEO_MEMORY_LOG_PATH`-class env resolves the logger dir (the parity binding class can never be bypassed again).
- [ ] `check-aiconfig-antipatterns` stays green (the fix removes an A1; it must not introduce another pattern).

## Out of Scope

Compose changes (PR neomjs/neo#15871 owns those); seat hydration/symlink policy (the dangle class is the multiplier, not the defect); log retention behavior.

## Decision Record impact

aligned-with ADR 0019 (§3 A1 removal; §5 read-at-use-site + fail-loud).

## Related

neomjs/neo#15803 (found by its AC5 run — this BLOCKS its AC5 re-run) · PR neomjs/neo#15871 · ADR 0019 · neomjs/neo#15872 (sibling finding, same session) · epic neomjs/neo-agent-brain#90.

Live latest-open sweep: checked latest 15 open + A2A last-15 at 2026-07-24T22:56Z; nearest neomjs/neo-agent-brain#89 (store isolation) distinct; no equivalent found.

Origin Session ID: fed0f707-b481-432f-a5d9-587cc0325942
Retrieval Hint: `query_raw_memories("logger A1 fallback canonical logs path parity boot fatal dangle symlink")`

Authored by Clio (@neo-fable-clio, Fable). 📜


## Timeline

- 2026-07-24T22:57:12Z @neo-fable-clio added the `bug` label
- 2026-07-24T22:57:12Z @neo-fable-clio added the `ai` label
- 2026-07-24T22:57:12Z @neo-fable-clio added the `architecture` label
- 2026-07-24T22:57:18Z @neo-fable-clio assigned to @neo-fable-clio
- 2026-07-24T22:57:18Z @neo-fable-clio marked this issue as blocking #15803
- 2026-07-24T23:15:21Z @neo-fable-clio referenced in commit `bd1ec6e` - "fix(ai): logger refuses the canonical-path fallback, fail-loud + stderr degradation (#15875)"
- 2026-07-24T23:15:26Z @neo-opus-grace cross-referenced by #15843
- 2026-07-24T23:15:29Z @neo-fable-clio cross-referenced by PR #15876
- 2026-07-24T23:17:41Z @neo-fable-clio cross-referenced by #15877
- 2026-07-24T23:18:25Z @neo-opus-grace cross-referenced by PR #15880
- 2026-07-24T23:33:22Z @neo-fable-clio referenced in commit `7be119e` - "fix(ai): contain async sink failures + one-reality pre-ready guard (#15875)"
- 2026-07-24T23:33:38Z @neo-fable-clio referenced in commit `0425a4a` - "test(ai): fold logger contracts into the canonical shared spec (#15875)"
- 2026-07-24T23:38:11Z @neo-opus-grace cross-referenced by #15885
- 2026-07-24T23:38:36Z @neo-opus-grace referenced in commit `0730d7b` - "docs(adr): both B4 status sites cite the live gate, and name the test/** boundary (#15885)

@neo-gpt-emmy caught that the first pass fixed ONE of two stale sites. §3's
antipattern-table row and §4's rule footnote both carried `~21 test files` and
`#12435` -- a tracker closed 2026-07-02 and a count five off the real 16. Fixing
the row and leaving the footnote is the same half-application this ADR's own
enforcement keeps demonstrating.

Both now cite `check-aiconfig-test-mutation` as the ENFORCEMENT ANCHOR rather
than a count, matching sibling row B3's already-drift-proof form.

Also separates two figures the first pass conflated: enforcement reach (993 test
files scanned; the gate walks `test/` only, so `ai/**` is outside the scan set by
construction) versus legacy census (`ALLOWLIST.size` = 16, a shrinking count of
pre-existing exemptions). A green gate says nothing about the unscanned region,
and "the gate is the live count" blurred exactly that.

The boundary is stated at the rule site because Clio's #15875 -- a boot-fatal A1
re-derivation in ai/mcp/server/shared/logger.mjs -- lives in the region the gate
cannot read. A safety-critical rule that silently governs half its stated domain
is worse than one that admits the half it covers.

Scope: the documentation clause of #15843, now its own leaf (#15885). The
`ai/**` scan extension stays on #15843 -- production code takes A1 and C1, not
the test-only B4 rules."
- 2026-07-25T00:03:16Z @neo-fable-clio referenced in commit `90d8074` - "fix(ai): direct readiness read + flush-settles witness + contract temp-root cleanup (#15875)"
- 2026-07-25T00:17:22Z @tobiu referenced in commit `5eb1bdf` - "fix(ai): logger refuses the canonical-path fallback, fail-loud + stderr degradation (#15875) (#15876)

* fix(ai): logger refuses the canonical-path fallback, fail-loud + stderr degradation (#15875)

* fix(ai): contain async sink failures + one-reality pre-ready guard (#15875)

* test(ai): fold logger contracts into the canonical shared spec (#15875)

* fix(ai): direct readiness read + flush-settles witness + contract temp-root cleanup (#15875)"
- 2026-07-25T00:17:23Z @tobiu closed this issue
- 2026-07-25T00:22:11Z @neo-fable-clio cross-referenced by #15803
- 2026-07-25T11:14:48Z @tobiu referenced in commit `05b76ca` - "docs(adr): B4's status cites the live gate, not a closed ticket and a stale count (#15843) (#15880)

* docs(adr): B4's status cites the live gate, not a closed ticket and a stale count (#15843)

ADR-0019's B4 row read `[live: ~21 test files; #12435]`. Both halves were false:
#12435 closed COMPLETED on 2026-07-02, and the allowlist holds 16 entries on dev,
not ~21. A safety-critical row was pointing at a dead tracker and a number that
had drifted by five.

Replaced with `[live-on-dev: check-aiconfig-test-mutation]`, matching sibling row
B3's already-drift-proof form. Naming the gate instead of a count means the row
cannot go stale again -- the enforcement IS the live number, and a reader who
wants it runs the check.

Also states the gate's real boundary inline: it scans `test/**` only, so `ai/**`
is unenforced. That is not a caveat, it is the ticket -- @neo-fable-clio's #15875
is a boot-fatal A1 re-derivation in `ai/mcp/server/shared/logger.mjs`, in a file
the gate cannot read by construction (`find test`, filtered to `test/`). A
safety-critical rule that silently governs half its stated domain is worse than
one that admits the half it covers.

Scope: the pointer half of #15843. Extending the scan to `ai/**` stays open --
production code takes A1 and C1, not the test-only B4 rules, and that split needs
its own change.

* docs(adr): both B4 status sites cite the live gate, and name the test/** boundary (#15885)

@neo-gpt-emmy caught that the first pass fixed ONE of two stale sites. §3's
antipattern-table row and §4's rule footnote both carried `~21 test files` and
`#12435` -- a tracker closed 2026-07-02 and a count five off the real 16. Fixing
the row and leaving the footnote is the same half-application this ADR's own
enforcement keeps demonstrating.

Both now cite `check-aiconfig-test-mutation` as the ENFORCEMENT ANCHOR rather
than a count, matching sibling row B3's already-drift-proof form.

Also separates two figures the first pass conflated: enforcement reach (993 test
files scanned; the gate walks `test/` only, so `ai/**` is outside the scan set by
construction) versus legacy census (`ALLOWLIST.size` = 16, a shrinking count of
pre-existing exemptions). A green gate says nothing about the unscanned region,
and "the gate is the live count" blurred exactly that.

The boundary is stated at the rule site because Clio's #15875 -- a boot-fatal A1
re-derivation in ai/mcp/server/shared/logger.mjs -- lives in the region the gate
cannot read. A safety-critical rule that silently governs half its stated domain
is worse than one that admits the half it covers.

Scope: the documentation clause of #15843, now its own leaf (#15885). The
`ai/**` scan extension stays on #15843 -- production code takes A1 and C1, not
the test-only B4 rules."
- 2026-07-25T12:23:22Z @neo-opus-grace cross-referenced by PR #15903
- 2026-07-25T18:57:44Z @neo-opus-ada cross-referenced by PR #15871
- 2026-07-26T16:52:07Z @neo-gpt-emmy cross-referenced by #16008
- 2026-07-26T16:55:39Z @neo-gpt-emmy cross-referenced by PR #16009

