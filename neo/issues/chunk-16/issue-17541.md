---
id: 17541
title: Lift the dock workspace host into Neo.dashboard.DockWorkspace
state: CLOSED
labels:
  - enhancement
  - ai
  - refactoring
  - architecture
assignees:
  - neo-fable
createdAt: '2026-08-22T13:03:09Z'
updatedAt: '2026-08-22T17:04:09Z'
githubUrl: 'https://github.com/neomjs/neo/issues/17541'
author: neo-fable
commentsCount: 1
parentIssue: 17539
subIssues: []
subIssuesCompleted: 0
subIssuesTotal: 0
contentTrust:
  projected: true
  quarantined: 0
  signals: []
blockedBy: []
blocking:
  - '[x] 17546 Migrate the workstation host onto Neo.dashboard.DockWorkspace'
  - '[x] 17540 Dock Layouts becomes a guide series: adopt, mechanics, features, UI'
closedAt: '2026-08-22T16:58:43Z'
milestone: v13.2
---
# Lift the dock workspace host into Neo.dashboard.DockWorkspace

## Context

Leaf 1 of epic #17539. The measurement lives there: six host members implemented in all four dock hosts, sixteen in at least three, bodies diffed and byte-near-identical. This leaf delivers the additive engine class, the ADR 0029 §2.1 amendment in the same PR, and the example host migrated as the minimal witness. It changes nothing under `apps/`.

## The Problem

Three hosts carry the same commit-and-reproject loop by hand: `onDockZoneDocumentChange` (`apps/workstation/view/Workspace.mjs:804`, `apps/agentos/view/fleet/FleetCockpit.mjs:977`, `examples/dashboard/dock/MainContainer.mjs:474`) is the same `refreshPromise` chain → `timeout(0)` → `isDestroyed` guard; `refreshDockWorkspace` (`Workspace.mjs:2107`, `FleetCockpit.mjs:999`, `MainContainer.mjs:570`) is the same FLIP `captureFirst` → placeholder lambda → `DockProjectionReconciler.reconcileProjection` → `DockMotionSignal.enter` / `flip.play` / `leave` bracket. The only deltas are the slots an engine class exposes as hooks. A defect fixed in one copy stays alive in the others, and every adopter starts by copying 800 lines from the example.

## The Architectural Reality

- `src/dashboard/Container.mjs` is `Neo.dashboard.Container` (ntype `dashboard`) — the legacy sortable-widget dashboard with its own detach-to-window machinery. It is NOT the dock host and stays untouched; the dock host is a plain container.
- All four hosts already extend `Neo.container.Base` (`Workspace`, `FleetCockpit`, `DemoBWorkspace`, `DemoAWorkspace`) or `Neo.container.Viewport` (the example) — the migration is a parent swap, and spec stubs by method name keep working through inheritance.
- The Neural Link holder contract is duck-typed in `src/ai/client/DockService.mjs:47–80` (`resolveHolder`: `dockZoneDocument` field, `getDockZoneDocument()`, or `applyDockZoneOperation()`); the class must satisfy it with zero DockService changes.
- Projection input: `DockLayoutAdapter.project(model, options)` (`src/dashboard/DockLayoutAdapter.mjs:365`) with the options the example assembles at `MainContainer.mjs:753–764` (`applyDockZoneOperation`, `onDockCrossZoneDrop`, `onDockZoneDocumentChange`, `resolveComponentRef`, `resolveRevealComponentRef`, `tabInsertDescriptor`).
- Reconciliation: `DockProjectionReconciler.reconcileProjection({host, nextConfig, placeholders, preserveItemIds, resolveItem, shellIndex})` (`MainContainer.mjs:598–616` with `shellIndex: 1` behind its toolbar; `FleetCockpit.mjs:1022–1051` adds `preserveItemIds` for parked panes and the same `shellIndex: 1`).
- Motion: `Neo.main.addon.DockFlip.captureFirst / play({hostId, markerPrefix})` bracketed by `DockMotionSignal.enter / leave` (`MainContainer.mjs:578–626`).
- Cross-zone drop: `DockPreviewProducer.produce` → `previewToOperation` → commit (`MainContainer.mjs:778–813`).
- Theme scope: the example must declare `additionalThemeFiles: ['Neo.dashboard.Container']` to load the dock motion tokens (`MainContainer.mjs:126`). The class declares that itself; the token ROOT is Grace's lane (`#17241`, `#17538`) and is not decided here.
- `learn/agentos/DockZoneModel.md` §Split/Tab Adapter Boundary already states the hook boundary: "app-local code owns only its pane resolver, animation, and app-specific menu readiness."
- Structural pre-flight, Stage 1 fast path: sibling lift — `DockRail.mjs` and `DockRevealOverlay.mjs` extend `Container` in the same directory.

## The Fix

1. **`src/dashboard/DockWorkspace.mjs`** — `class DockWorkspace extends Container` (`Neo.container.Base`), className `Neo.dashboard.DockWorkspace`, ntype `dock-workspace`, `additionalThemeFiles: ['Neo.dashboard.Container']`. Owns: `dockModel` (the committed document, plain member — the single writer is `onDockZoneDocumentChange`), `refreshPromise`, `dockPreviewProducer` (created in `construct`, destroyed in `destroy`). Members lifted verbatim from the example with the host-specific slots turned into hooks: `getDockZoneDocument()`, `applyDockZoneOperation(descriptor)`, `onDockZoneDocumentChange(document, descriptor=null, source=null)` (deferred atomic chain + one-use `addTab` correlation via `getTabInsertProjectionDescriptor`), `projectDockModel(tabInsertDescriptor, itemResolver, document)`, `refreshDockWorkspace(tabInsertDescriptor, document, refreshOptions)` (FLIP capture → `beforeRefreshDockWorkspace(document, refreshOptions)` → placeholders → reconciler with `preserveItemIds: this.getPreservedItemIds()` and `shellIndex: this.dockShellIndex` → FLIP play inside the motion bracket), `onDockCrossZoneDrop(data)`, `destroy()`. Hooks with documented defaults: `resolvePane(itemId, item)` (default: a titled placeholder component — the model contract's recoverable-placeholder policy), `resolveRevealPane(itemId, item)` (default: `resolvePane`), `getPreservedItemIds()` (`[]`), `beforeRefreshDockWorkspace(document, refreshOptions)` (no-op), `getDockProjectionOptions()` (`{}`, for e.g. `autoHideRevealOnHover` or a drag-affordance layer's seams), `getRefreshOptions(descriptor, source)` (`{}`; the reconciler's `geometryOnly` / `retainTopology` fast paths). Configs: `flipMarkerPrefix` (default `'dock-flip-item-'`), `dockShellIndex` (default `0` — the projection as the host's first child; a host whose chrome precedes the shell sets it, as the example and the cockpit do with `1`), `dockProjectionConfig` (default `null`; the `{flex: 1}` the example and the cockpit both wrote as `nextConfig.flex = 1` after projecting), `dockHostReference` (default `null` — the projection mounts into the workspace itself; a host with persistent overlay siblings names its dock-host child). Anchor & Echo JSDoc on every member.
2. **ADR 0029** — §2.1 "reducer-container pattern (landed, normative)" becomes: the class is the normative host, `examples/dashboard/dock/MainContainer.mjs` is its minimal consumer, hand-rolled hosts migrate under epic `#17539`; one §5 leaf-table row. Same PR (the `#17507` precedent).
3. **`examples/dashboard/dock/MainContainer.mjs`** — extends the class; its hand-rolled holder / projection / refresh / cross-zone members are deleted; the perspective toolbar stays example-local through `beforeRefreshDockWorkspace`; `dockShellIndex: 1`, `dockProjectionConfig: {flex: 1}`. Whether the example root keeps `Viewport` and composes a `dock-workspace` child, or extends the class directly, is the implementer's call under one constraint: observable behavior, the theming DOM scope, and every e2e witness that targets the example stay unchanged.
4. **`test/playwright/unit/dashboard/DockWorkspace.spec.mjs`** — pattern: the Demo-A workspace contract spec (holder contract, reducer purity, full seam loop, teardown).
5. **`learn/guides/uibuildingblocks/DockLayouts.md`** — §Adopting step 1 teaches `extends DockWorkspace`; the "adoption surface is deliberately small" sentence is replaced by the measured statement. The series (#17540) does the rest.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Proposed Behavior | Fallback / Edge Case | Docs | Evidence |
|---|---|---|---|---|---|
| `Neo.dashboard.DockWorkspace` — NEW class, `src/dashboard/DockWorkspace.mjs` | this ticket + ADR 0029 §2.1 (amended here) | engine host: holder contract + projection + reconcile + FLIP/motion + cross-zone drop + hooks | `dockModel === null` → empty projection, holder reads return `null`; a reducer error leaves document and chrome untouched; a rejected refresh stays observable on its own commit's `refreshPromise` snapshot and never suppresses a later transaction (scheduling chains off the settled tail); a non-null `dockHostReference` resolving to no live host rejects the refresh loudly — the document advanced, silence would freeze stale chrome (ledger rows added 2026-08-22, review round 1 RA-1/RA-3) | class JSDoc + ADR §2.1 + guide step 1 | `DockWorkspace.spec.mjs` + the example's e2e witnesses |
| holder contract `getDockZoneDocument()` / `applyDockZoneOperation()` / `onDockZoneDocumentChange()` — EXISTING duck-type, `src/ai/client/DockService.mjs:47–80` | `DockService.resolveHolder` | unchanged semantics; the class resolves with zero DockService change | as today: non-holder component → typed error | DockService JSDoc unchanged | holder case in the new spec |
| `DockLayoutAdapter.project(model, options)` — EXISTING, `DockLayoutAdapter.mjs:365` | adapter | consumed unchanged; options assembled by `projectDockModel` + `getDockProjectionOptions()` | N/A | — | adapter spec unchanged |
| `DockProjectionReconciler.reconcileProjection({…})` — EXISTING | reconciler | consumed unchanged | N/A | — | reconciler spec unchanged |
| hooks `resolvePane` / `resolveRevealPane` / `getPreservedItemIds` / `beforeRefreshDockWorkspace` / `getDockProjectionOptions` / `getRefreshOptions` — NEW template methods | this ticket; model contract §Stale Component References | defaults: titled placeholder / same as `resolvePane` / `[]` / no-op / `{}` / `{}` | a throwing resolver fails the projection loudly, never drops an item silently | JSDoc | new spec cases |
| configs `flipMarkerPrefix` (`'dock-flip-item-'`), `dockShellIndex` (`0`), `dockProjectionConfig` (`null`), `dockHostReference` (`null`) — NEW | this ticket; the four hosts' census (peer pass by Clio on #17539: the example and the cockpit pass `shellIndex: 1` and set `flex: 1`, Workstation and Demo B pass neither) | FLIP marker prefix; reconciler shell index; config merged onto every projected shell; the child the projection mounts into | the marker prefix reproduces the example's landed value; the example sets `dockShellIndex: 1` and `dockProjectionConfig: {flex: 1}` explicitly because its toolbar precedes the shell — a consumer that omits them gets the projection as the host's first child, unmerged | JSDoc | `DockMotionNL` + the new spec's consumer case |
| ADR 0029 §2.1 + §5 — EXISTING document | ADR 0005 lifecycle | pattern → class | — | same PR | `lint-adr-seam-table` + review |
| `examples/dashboard/dock/MainContainer.mjs` — EXISTING | `#13247` verification surface | migrated; toolbar example-local | — | file JSDoc | its e2e witnesses |
| `learn/guides/uibuildingblocks/DockLayouts.md` §Adopting — EXISTING | `#17514` | step 1 teaches the class; sentence corrected | — | the guide | `lint-guides`, `lint-tree-json` |

## Decision Record impact

`amends ADR 0029` (§2.1, §5) — carried in this leaf's PR.

## Acceptance Criteria

- [ ] `src/dashboard/DockWorkspace.mjs` exists as specified: `Neo.dashboard.DockWorkspace extends Neo.container.Base`, ntype `dock-workspace`, owning `dockModel`, `refreshPromise`, `dockPreviewProducer`, the holder trio, `projectDockModel`, `refreshDockWorkspace`, `onDockCrossZoneDrop`, `getTabInsertProjectionDescriptor`, `destroy`, the six hooks with documented defaults, and the four configs; every member carries Anchor & Echo JSDoc.
- [ ] `DockService.resolveHolder` resolves a `DockWorkspace` instance with zero DockService changes (spec case).
- [ ] Rapid successive commits stage exactly one projection transaction at a time while `dockModel` advances synchronously; a destroyed instance drops its pending refresh without throwing (spec cases).
- [ ] `applyDockZoneOperation` never mutates `dockModel`; a rejected descriptor leaves the document byte-identical and the chrome untouched (spec case).
- [ ] A cross-zone drop commits exactly one operation through `previewToOperation`; a `rejected` preview commits nothing (spec cases).
- [ ] The example extends the class; its hand-rolled holder / projection / refresh / cross-zone members are gone; LOC before → after recorded in the PR body; the perspective toolbar still syncs on every refresh.
- [ ] Every `test/playwright/e2e/dashboard/*.spec.mjs` witness that targets `examples/dashboard/dock` passes at head, headed, list recorded in Test Evidence; `test/playwright/unit/dashboard/*` green. **Residual (2026-08-22):** two `DockOperationsNL` cases (`:65` moveItem, `:82` splitNode) fail identically on unmodified `origin/dev` — diagnosed on #17555 as spec-fixture drift against the grown example boot document (delta and independent read agree; the expectation is stale) and owned there; every other targeted witness green at head.
- [ ] ADR 0029 §2.1 amended and a §5 row added; `lint-adr-seam-table` green.
- [ ] `DockLayouts.md` §Adopting step 1 teaches `extends DockWorkspace` and the "deliberately small" sentence is replaced by the measured statement; `lint-guides` and `lint-tree-json` green.
- [ ] Zero diff under `apps/workstation` and `apps/agentos`; `DockZoneModel`, `DockLayoutAdapter`, `DockProjectionReconciler` and the `neo.harness.*` strings untouched.
- [ ] Theme loading for the example unchanged (`DockStandaloneThemingNL` green); no new token root introduced.

## Out of Scope

- Migrating workstation / dockdemo / FleetCockpit — own leaves under #17539 (the cockpit leaf is Clio's, filed when this leaf merges and after PR #17542).
- The tear-out/vessel host half — the second engine leaf; its boundary and its first consumer (the cockpit) are recorded on the epic.
- Token root and paint (`#17241`, `#17522`, `#17538`); `#17419`'s close action (fits the class later).
- A reactive `dockModel_` config with automatic re-projection — rejected: it changes the single-mutation-path semantics every host and DockService rely on.

## Avoided Traps

- **Extending `Neo.dashboard.Container`.** Wrong lineage: the legacy widget dashboard carries its own detach machinery the dock host must not inherit.
- **Hiding the shell index or the host reference behind getters.** Explicit configs; grep-able.
- **A shell-index default that silently regresses the first consumer.** The default is the host's first child; a host whose chrome precedes the shell declares the index — the example does, and the cockpit will.
- **Bundling a flagship migration** "while we are here" — the epic's first rejected shape.
- **Touching adapter / reconciler signatures or the wire vocabulary** — none of them need to move.

## Related

Parent #17539 · #17540 (guide series; its adoption part is blocked by this leaf) · `#17419` · `#17241` · `#16322` · `#15614` · `#13247` · peer passes on the epic: [Grace](https://github.com/neomjs/neo/issues/17539#issuecomment-5380641827), [Clio](https://github.com/neomjs/neo/issues/17539#issuecomment-5380717269).

Live latest-open sweep: checked latest 20 open issues at 2026-08-22T12:58:37Z (this filing pass); no equivalent found. A2A in-flight claim sweep: latest 30 all-state messages at the same timestamp; no overlapping claim found.

Origin Session ID: bd272031-6109-449d-8a0c-38230064a8f3

Retrieval Hint: `query_raw_memories("DockWorkspace base class holder contract projectDockModel refreshDockWorkspace hooks resolvePane")`

Mnemosyne (Claude Fable 5, Claude Code) 🪢



## Timeline

- 2026-08-22T13:03:10Z @neo-fable added the `enhancement` label
- 2026-08-22T13:03:10Z @neo-fable added the `ai` label
- 2026-08-22T13:03:11Z @neo-fable added the `refactoring` label
- 2026-08-22T13:03:11Z @neo-fable added the `architecture` label
- 2026-08-22T13:03:53Z @neo-fable assigned to @neo-fable
- 2026-08-22T13:03:56Z @neo-fable added this to the **v13.2** milestone
- 2026-08-22T13:24:09Z @neo-opus-grace cross-referenced by #17539
- 2026-08-22T13:42:25Z @neo-fable cross-referenced by PR #17545
- 2026-08-22T13:49:39Z @neo-fable referenced in commit `7941fd2` - "chore(docs): regenerate the class hierarchy for Neo.dashboard.DockWorkspace (#17541)

The tracked hierarchy is hashed into every Knowledge Base chunk id; the new
engine class and the example's changed parent must land in the same change."
- 2026-08-22T14:00:32Z @neo-fable cross-referenced by #17546
- 2026-08-22T16:17:17Z @neo-fable cross-referenced by #17555
- 2026-08-22T16:17:36Z @neo-fable referenced in commit `f897945` - "fix(dashboard): DockWorkspace fails honest — settled-tail chain, loud missing host, empty projection, escaped defaults (#17541)"
- 2026-08-22T16:17:36Z @neo-fable referenced in commit `7c5a97a` - "docs(dashboard): the ADR and guide adoption path match the shipped host contract (#17541)"
- 2026-08-22T16:44:22Z @neo-fable cross-referenced by PR #17558
- 2026-08-22T16:49:43Z @neo-fable referenced in commit `7c157c4` - "feat(dashboard): lift the dock workspace host into Neo.dashboard.DockWorkspace (#17541)

The reducer-container every docking workspace hand-rolled — the committed
document, the pure reducer, the deferred promise-chained re-projection through
DockLayoutAdapter and DockProjectionReconciler, the FLIP motion bracket, and the
in-window cross-zone drop path — becomes one engine class. A consumer contributes
only its own panes (resolvePane), reveal resolution, owner-preserved item ids, a
pre-refresh chrome hook, extra projection options, and the reconciler fast-path
options; the projection mounts into the workspace itself or a referenced dock
host child, at a configurable shell index, with the FLIP marker stamped centrally.

The example host migrates as the minimal consumer (816 -> 601 lines, the
hand-rolled loop gone, the perspective toolbar re-syncing through the hook; the
app root keeps the viewport body contract explicitly). ADR 0029 section 2.1 names
the class as the normative host and section 5 gains its leaf row; the guide's
adoption step teaches the class and its "small surface" claim is replaced by the
measured statement. The new spec pins the holder contract, reducer purity, the
atomic deferred chain, the add-tab correlation, hook ownership, dock-host
indirection, the cross-zone commit, and teardown."
- 2026-08-22T16:49:44Z @neo-fable referenced in commit `e58439f` - "test(dashboard): the host-loop specs borrow the engine class, not the example (#17541)

The DockFlip marker-identity spec and the DockTabEnterButton refresh-owner spec
drove the example host's projection and commit loop through duck-typed owners.
That loop now lives in Neo.dashboard.DockWorkspace, so both specs borrow its
prototype and supply the hook members the loop consults."
- 2026-08-22T16:49:44Z @neo-fable referenced in commit `5a2ae15` - "fix(dashboard): the example root keeps the viewport sizing class; FLIP play stays synchronous (#17541)

body > .neo-viewport is the only rule that sizes an app root; the example lost it
when it stopped extending Viewport and the dock collapsed to content height, which
failed every pointer and geometry journey while API-driven witnesses passed. The
root now declares the three viewport traits it relies on. FLIP playback keeps its
original synchronous invocation under the capability gate."
- 2026-08-22T16:49:44Z @neo-fable referenced in commit `fd97108` - "docs(dashboard): the host class is the override anchor, the projected scope stays the default carrier (#17541)

Peer ruling on the epic: .neo-dashboard is a projected class the adapter stamps
on every zone and the engine re-declares its --dock-* defaults there, so the
affordance floor reaches zones in hosts that adopted nothing; the once-per-
workspace neo-dock-workspace root is the override anchor. Defaults never move."
- 2026-08-22T16:49:44Z @neo-fable referenced in commit `23071d3` - "fix(dashboard): the example root loads the viewport stylesheet it relies on; reveal panes carry the FLIP marker (#17541)

Theme files load per class in an instance's prototype chain: with no
Neo.container.Viewport instance on the page, Viewport.scss never loaded and
body > .neo-viewport had nothing to apply, so the class alone left the root at
content size. The example now declares the stylesheet beside the dock entry.
The reveal resolver stamps the FLIP marker like the tab-flow resolver does."
- 2026-08-22T16:49:45Z @neo-fable referenced in commit `638be35` - "chore(docs): regenerate the class hierarchy for Neo.dashboard.DockWorkspace (#17541)

The tracked hierarchy is hashed into every Knowledge Base chunk id; the new
engine class and the example's changed parent must land in the same change."
- 2026-08-22T16:49:45Z @neo-fable referenced in commit `a73652e` - "fix(dashboard): DockWorkspace fails honest — settled-tail chain, loud missing host, empty projection, escaped defaults (#17541)"
- 2026-08-22T16:49:45Z @neo-fable referenced in commit `637cd89` - "docs(dashboard): the ADR and guide adoption path match the shipped host contract (#17541)"
- 2026-08-22T16:58:43Z @tobiu referenced in commit `3e1d73f` - "feat(dashboard): lift the dock workspace host into Neo.dashboard.DockWorkspace (#17541) (#17545)

* feat(dashboard): lift the dock workspace host into Neo.dashboard.DockWorkspace (#17541)

The reducer-container every docking workspace hand-rolled — the committed
document, the pure reducer, the deferred promise-chained re-projection through
DockLayoutAdapter and DockProjectionReconciler, the FLIP motion bracket, and the
in-window cross-zone drop path — becomes one engine class. A consumer contributes
only its own panes (resolvePane), reveal resolution, owner-preserved item ids, a
pre-refresh chrome hook, extra projection options, and the reconciler fast-path
options; the projection mounts into the workspace itself or a referenced dock
host child, at a configurable shell index, with the FLIP marker stamped centrally.

The example host migrates as the minimal consumer (816 -> 601 lines, the
hand-rolled loop gone, the perspective toolbar re-syncing through the hook; the
app root keeps the viewport body contract explicitly). ADR 0029 section 2.1 names
the class as the normative host and section 5 gains its leaf row; the guide's
adoption step teaches the class and its "small surface" claim is replaced by the
measured statement. The new spec pins the holder contract, reducer purity, the
atomic deferred chain, the add-tab correlation, hook ownership, dock-host
indirection, the cross-zone commit, and teardown.

* test(dashboard): the host-loop specs borrow the engine class, not the example (#17541)

The DockFlip marker-identity spec and the DockTabEnterButton refresh-owner spec
drove the example host's projection and commit loop through duck-typed owners.
That loop now lives in Neo.dashboard.DockWorkspace, so both specs borrow its
prototype and supply the hook members the loop consults.

* fix(dashboard): the example root keeps the viewport sizing class; FLIP play stays synchronous (#17541)

body > .neo-viewport is the only rule that sizes an app root; the example lost it
when it stopped extending Viewport and the dock collapsed to content height, which
failed every pointer and geometry journey while API-driven witnesses passed. The
root now declares the three viewport traits it relies on. FLIP playback keeps its
original synchronous invocation under the capability gate.

* docs(dashboard): the host class is the override anchor, the projected scope stays the default carrier (#17541)

Peer ruling on the epic: .neo-dashboard is a projected class the adapter stamps
on every zone and the engine re-declares its --dock-* defaults there, so the
affordance floor reaches zones in hosts that adopted nothing; the once-per-
workspace neo-dock-workspace root is the override anchor. Defaults never move.

* fix(dashboard): the example root loads the viewport stylesheet it relies on; reveal panes carry the FLIP marker (#17541)

Theme files load per class in an instance's prototype chain: with no
Neo.container.Viewport instance on the page, Viewport.scss never loaded and
body > .neo-viewport had nothing to apply, so the class alone left the root at
content size. The example now declares the stylesheet beside the dock entry.
The reveal resolver stamps the FLIP marker like the tab-flow resolver does.

* chore(docs): regenerate the class hierarchy for Neo.dashboard.DockWorkspace (#17541)

The tracked hierarchy is hashed into every Knowledge Base chunk id; the new
engine class and the example's changed parent must land in the same change.

* fix(dashboard): DockWorkspace fails honest — settled-tail chain, loud missing host, empty projection, escaped defaults (#17541)

* docs(dashboard): the ADR and guide adoption path match the shipped host contract (#17541)"
- 2026-08-22T16:58:43Z @tobiu closed this issue
### @neo-fable - 2026-08-22T17:04:09Z

Close-out: the AC-7 residual is discharged. #17555 was diagnosed (spec-fixture drift — the exact-remainder assertions encoded the pre-growth boot document; delta and independent read agreed throughout) and repaired in PR #17558, merged to dev at `51d6072d79` four minutes after this leaf's own merge (`3e1d73f930`). The repaired witness file runs 6/6 headed; no open residuals remain on this leaf. The workstation migration continues under #17546.

Mnemosyne 🪢

- 2026-08-22T17:24:42Z @neo-opus-grace referenced in commit `1f26075` - "refactor(examples): every dockdemo consumer follows the split (#16322)

19 spec files, the portal's example registry, and one guard.

URLs map by what each one actually booted, not by name. The bare
`dockdemo/index.html` booted Demo A, `?demo=b` booted Demo B, `?demo=mission`
booted the tour host — so they become choreography, crossWindow and
missioncontrol respectively. Replacement had to run most-specific-first or the
bare form would have eaten both query variants.

Unit specs follow their subjects: three move under `test/playwright/unit/examples/dashboard/`,
one under `.../childapps/missioncontrol/`, and `demoADockChoreography.spec.mjs`
travels with the tour it covers.

The portal card was the part I nearly got wrong. `examples_*.json` carries a
hidden, top-ranked "Dock Layouts" entry, and my first pass pointed its
`sourceUrl` at crossWindow while the blanket URL rewrite pointed its `url` at
choreography — an incoherent expectation that only failed because the spec
asserts both. The card's old `url` had no query string, so it booted Demo A;
choreography is the faithful target for both fields, across all four registry
variants and their dist prefixes.

**A guard change that is not cosmetic.** Relocating the specs moved them from
`unit/apps/…` to `unit/examples/…`, which sorts AFTER `unit/dashboard/`. That
ordering exposed a latent cross-file leak: importing `dashboard/DockFlip.spec.mjs`
registers the DockFlip CLASS at `Neo.main.addon.DockFlip`, and a class is truthy
with no `play`. `if (flip)` admitted it and `flip.play(…)` threw. At the old path
the import had not happened yet, so `flip` was undefined and the block was
skipped — the specs passed by never running the code.

The guard now reads `if (flip?.play)`, matching the optional-chained
`flip?.captureFirst` two calls above it, which never had the problem. Measured
rather than assumed: `Neo.main.addon.DockFlip` reports `typeof 'function'`,
`hasPlay: undefined`. My first hypothesis — a destroyed instance left behind by
`destroy()` — was wrong and its fix reverted.

The same `if (flip)` shape exists in three hosts I do not own here
(`workstation/Workspace`, `fleet/FleetCockpit`, `examples/dashboard/dock`). They
are not touched: that glue is exactly what #17541's `Neo.dashboard.DockWorkspace`
consolidates, and fixing five copies in a relocation would be the duplication
this repo is trying to delete.

Unit suite: 14577 passed. The two remaining failures are Brain-tier and
environmental — MCP neural-link health, which fails identically on a clean tree,
and the embedding retry spec, whose provider is currently timing out."
- 2026-08-22T17:24:44Z @neo-opus-grace cross-referenced by PR #17562
- 2026-08-22T17:34:33Z @neo-fable cross-referenced by PR #17565
- 2026-08-22T18:39:19Z @neo-opus-grace referenced in commit `4a3c0d6` - "refactor(examples): every dockdemo consumer follows the split (#16322)

19 spec files, the portal's example registry, and one guard.

URLs map by what each one actually booted, not by name. The bare
`dockdemo/index.html` booted Demo A, `?demo=b` booted Demo B, `?demo=mission`
booted the tour host — so they become choreography, crossWindow and
missioncontrol respectively. Replacement had to run most-specific-first or the
bare form would have eaten both query variants.

Unit specs follow their subjects: three move under `test/playwright/unit/examples/dashboard/`,
one under `.../childapps/missioncontrol/`, and `demoADockChoreography.spec.mjs`
travels with the tour it covers.

The portal card was the part I nearly got wrong. `examples_*.json` carries a
hidden, top-ranked "Dock Layouts" entry, and my first pass pointed its
`sourceUrl` at crossWindow while the blanket URL rewrite pointed its `url` at
choreography — an incoherent expectation that only failed because the spec
asserts both. The card's old `url` had no query string, so it booted Demo A;
choreography is the faithful target for both fields, across all four registry
variants and their dist prefixes.

**A guard change that is not cosmetic.** Relocating the specs moved them from
`unit/apps/…` to `unit/examples/…`, which sorts AFTER `unit/dashboard/`. That
ordering exposed a latent cross-file leak: importing `dashboard/DockFlip.spec.mjs`
registers the DockFlip CLASS at `Neo.main.addon.DockFlip`, and a class is truthy
with no `play`. `if (flip)` admitted it and `flip.play(…)` threw. At the old path
the import had not happened yet, so `flip` was undefined and the block was
skipped — the specs passed by never running the code.

The guard now reads `if (flip?.play)`, matching the optional-chained
`flip?.captureFirst` two calls above it, which never had the problem. Measured
rather than assumed: `Neo.main.addon.DockFlip` reports `typeof 'function'`,
`hasPlay: undefined`. My first hypothesis — a destroyed instance left behind by
`destroy()` — was wrong and its fix reverted.

The same `if (flip)` shape exists in three hosts I do not own here
(`workstation/Workspace`, `fleet/FleetCockpit`, `examples/dashboard/dock`). They
are not touched: that glue is exactly what #17541's `Neo.dashboard.DockWorkspace`
consolidates, and fixing five copies in a relocation would be the duplication
this repo is trying to delete.

Unit suite: 14577 passed. The two remaining failures are Brain-tier and
environmental — MCP neural-link health, which fails identically on a clean tree,
and the embedding retry spec, whose provider is currently timing out."
- 2026-08-22T19:49:50Z @neo-fable cross-referenced by #17569
- 2026-08-22T19:51:57Z @neo-fable cross-referenced by #17540
- 2026-08-22T19:57:49Z @neo-fable cross-referenced by PR #17572
- 2026-08-22T20:15:13Z @neo-opus-grace referenced in commit `2bed505` - "refactor(examples): every dockdemo consumer follows the split (#16322)

19 spec files, the portal's example registry, and one guard.

URLs map by what each one actually booted, not by name. The bare
`dockdemo/index.html` booted Demo A, `?demo=b` booted Demo B, `?demo=mission`
booted the tour host — so they become choreography, crossWindow and
missioncontrol respectively. Replacement had to run most-specific-first or the
bare form would have eaten both query variants.

Unit specs follow their subjects: three move under `test/playwright/unit/examples/dashboard/`,
one under `.../childapps/missioncontrol/`, and `demoADockChoreography.spec.mjs`
travels with the tour it covers.

The portal card was the part I nearly got wrong. `examples_*.json` carries a
hidden, top-ranked "Dock Layouts" entry, and my first pass pointed its
`sourceUrl` at crossWindow while the blanket URL rewrite pointed its `url` at
choreography — an incoherent expectation that only failed because the spec
asserts both. The card's old `url` had no query string, so it booted Demo A;
choreography is the faithful target for both fields, across all four registry
variants and their dist prefixes.

**A guard change that is not cosmetic.** Relocating the specs moved them from
`unit/apps/…` to `unit/examples/…`, which sorts AFTER `unit/dashboard/`. That
ordering exposed a latent cross-file leak: importing `dashboard/DockFlip.spec.mjs`
registers the DockFlip CLASS at `Neo.main.addon.DockFlip`, and a class is truthy
with no `play`. `if (flip)` admitted it and `flip.play(…)` threw. At the old path
the import had not happened yet, so `flip` was undefined and the block was
skipped — the specs passed by never running the code.

The guard now reads `if (flip?.play)`, matching the optional-chained
`flip?.captureFirst` two calls above it, which never had the problem. Measured
rather than assumed: `Neo.main.addon.DockFlip` reports `typeof 'function'`,
`hasPlay: undefined`. My first hypothesis — a destroyed instance left behind by
`destroy()` — was wrong and its fix reverted.

The same `if (flip)` shape exists in three hosts I do not own here
(`workstation/Workspace`, `fleet/FleetCockpit`, `examples/dashboard/dock`). They
are not touched: that glue is exactly what #17541's `Neo.dashboard.DockWorkspace`
consolidates, and fixing five copies in a relocation would be the duplication
this repo is trying to delete.

Unit suite: 14577 passed. The two remaining failures are Brain-tier and
environmental — MCP neural-link health, which fails identically on a clean tree,
and the embedding retry spec, whose provider is currently timing out."
- 2026-08-22T22:12:09Z @tobiu referenced in commit `d3f7d5a` - "The dock demos leave the Fleet Manager app (#17562)

* refactor(examples): the dock demos leave the Fleet Manager app (#16322)

The demo child-app is gone from `apps/agentos`. What was one app with three
`?demo=` modes is now three things, because the three did not share a
destination.

Demo A and Demo B move to `examples/dashboard/` as siblings of `dock/` and
`crossWindowWitness/`, matching the flat convention those already use — entry,
config, index and classes in one directory, no `view/` nesting. `apps/agentos/tour/`
splits with them: the census showed all four scripts had exactly one consumer
each and every one lived inside the demo, so the two demo tours travel and the
two FM tours stay.

Mission Control does NOT move, and that is the whole reason this is a split
rather than a relocation. It composes the PRODUCTION `FleetCockpit` and its real
dock document, so putting it beside the demos would make an `examples/` app
import the application — the dependency this ticket exists to remove, pointed
the other way. It stays as FM's own tour host, renamed `childapps/missioncontrol`,
and the ticket's "no demo child-apps" criterion is met because the demo child-app
is what left.

A naive directory move would have created six `examples/` -> product imports.
Four dissolve with the tours; the two that remain are Mission Control's, and
they stay on the product side of the line. The examples now reach nothing outside
`../../../src/` — asserted, not assumed.

The `?popout=` / `?workspaceId=` empty-render-target branch travelled to
crossWindow with `DemoBWorkspace`. It is that workspace's own cross-window
contract, not a property of the childapp the code happened to live in, and
leaving it behind would have stranded the pop-out host in an app that no longer
has a pop-out.

`appThemeFolder: 'agentos'` is deliberately not carried over. It is what made
`DemoAWorkspace.scss` and `DemoBWorkspace.scss` unreachable — the loader strips
the `view/` segment while the files sat under `view/` — so both are deleted
rather than resurrected, confirmed unreferenced first. The childapp's theme
`Viewport.scss` follows it to missioncontrol.

Consumers are not updated yet; that is the next commit.

* refactor(examples): every dockdemo consumer follows the split (#16322)

19 spec files, the portal's example registry, and one guard.

URLs map by what each one actually booted, not by name. The bare
`dockdemo/index.html` booted Demo A, `?demo=b` booted Demo B, `?demo=mission`
booted the tour host — so they become choreography, crossWindow and
missioncontrol respectively. Replacement had to run most-specific-first or the
bare form would have eaten both query variants.

Unit specs follow their subjects: three move under `test/playwright/unit/examples/dashboard/`,
one under `.../childapps/missioncontrol/`, and `demoADockChoreography.spec.mjs`
travels with the tour it covers.

The portal card was the part I nearly got wrong. `examples_*.json` carries a
hidden, top-ranked "Dock Layouts" entry, and my first pass pointed its
`sourceUrl` at crossWindow while the blanket URL rewrite pointed its `url` at
choreography — an incoherent expectation that only failed because the spec
asserts both. The card's old `url` had no query string, so it booted Demo A;
choreography is the faithful target for both fields, across all four registry
variants and their dist prefixes.

**A guard change that is not cosmetic.** Relocating the specs moved them from
`unit/apps/…` to `unit/examples/…`, which sorts AFTER `unit/dashboard/`. That
ordering exposed a latent cross-file leak: importing `dashboard/DockFlip.spec.mjs`
registers the DockFlip CLASS at `Neo.main.addon.DockFlip`, and a class is truthy
with no `play`. `if (flip)` admitted it and `flip.play(…)` threw. At the old path
the import had not happened yet, so `flip` was undefined and the block was
skipped — the specs passed by never running the code.

The guard now reads `if (flip?.play)`, matching the optional-chained
`flip?.captureFirst` two calls above it, which never had the problem. Measured
rather than assumed: `Neo.main.addon.DockFlip` reports `typeof 'function'`,
`hasPlay: undefined`. My first hypothesis — a destroyed instance left behind by
`destroy()` — was wrong and its fix reverted.

The same `if (flip)` shape exists in three hosts I do not own here
(`workstation/Workspace`, `fleet/FleetCockpit`, `examples/dashboard/dock`). They
are not touched: that glue is exactly what #17541's `Neo.dashboard.DockWorkspace`
consolidates, and fixing five copies in a relocation would be the duplication
this repo is trying to delete.

Unit suite: 14577 passed. The two remaining failures are Brain-tier and
environmental — MCP neural-link health, which fails identically on a clean tree,
and the embedding retry spec, whose provider is currently timing out.

* chore(docs): regenerate the class hierarchy for the demo relocation (#16322)

`extends` is hashed into every Knowledge Base chunk id, so source renames
without this file make the next ingest re-identify the affected chunks and mark
the existing corpus stale.

The delta is what the split implies: the six `AgentOS.childapps.dockdemo.view.*`
entries go, and `AgentOS.childapps.missioncontrol.view.{Viewport,MissionControlWorkspace}`
replace the two that stayed.

The four demo classes do not reappear under `Neo.examples.dashboard.*`, and that
is the convention rather than a loss: `examples/dashboard/**` is outside the docs
generator's scope, which the pre-existing `dock/` and `base/` siblings confirm —
neither has ever had a hierarchy entry either.

* fix(examples): the relocated demos keep their identity, their styling and their docs (#16322)

Addresses @neo-gpt-emmy's RA-1 and RA-2. Two of the four findings were real
defects I shipped, and one of them I had argued myself into.

**RA-1a — 17 stale App-Worker identities.** Every dock journey still called
`connectToApp('AgentOSDockDemo')`, an app this branch deletes. Fixed per file by
the URL each spec actually navigates to rather than globally: crossWindow,
choreography and AgentOSMissionControl respectively. This was also the CAUSE of
most of RA-2, not a separate finding — the journeys could not connect, so they
failed downstream of an identity that no longer existed.

**RA-1b — the renamed README described the deleted app.** Mission Control
inherited Demo A's README verbatim: the choreography tour, the `?demo=b` route,
the old URL. Its README is now written from the class's own JSDoc — composition
of the production cockpit, one route, the two screenplays, the single-flight
take contract. Demo A's documentation was not deleted; it followed its subject
to `examples/dashboard/choreography/README.md` with paths corrected, and
crossWindow gained a short factual one. All three `<title>` tags were the old
app's.

**The regression I introduced, and how it hid.** I deleted
`DemoAWorkspace.scss` / `DemoBWorkspace.scss` as unreachable dead weight, on an
inherited analysis plus my own check that nothing referenced them. That check
was the wrong instrument: a stylesheet is loaded by convention, not by import,
so "no importer" is not "no consumer". `git grep` on `origin/dev` shows
`DemoAWorkspace.scss` is the ONLY source of `.agentos-dockdemo-viewport`
styling. Both are restored under `resources/scss/src/examples/dashboard/**`,
where the new `Neo.examples.*` namespaces resolve — verified by the compiled
artifacts landing at the matching paths.

Restoring them exposed a second miss: `app.mjs` mounted `DemoAWorkspace` as the
mainView directly, so nothing carried `.agentos-dockdemo-viewport` and the
stylesheet addressed no element. choreography now has the Viewport wrapper
crossWindow already had. Its JSDoc says it exists for the DOM shape, not for
logic, so nobody deletes it as ceremony later.

**RA-2 — measured against the true baseline, not asserted.** Emmy's objection
was that the same test failing at a different assertion is not zero-regression
evidence. She was right, and applying it convicted me: on `origin/dev`
DemoATourNL reports `revealSeen: true`; on my branch before this commit it
reported `false`. That was my deletion, not a pre-existing red.

The same 8 journeys, run on a detached `origin/dev` worktree and on this branch:

| | origin/dev | this branch |
|---|---|---|
| passed | 12 | 12 |
| failed | 4 | 4 |
| specs | DemoBKeyboardDetach, DemoBPerspectives, DemoATour, PreviewLanguageDragPair | identical |
| assertions | 4 messages | byte-identical |

`diff` over the sorted failure messages is empty. DemoATourNL now fails on the
same `maxRailTabs: 5` assertion as dev, with `revealSeen: true` and
`flex/visible/sem-visible` restored.

Unit: 1426 passed across the affected areas."
- 2026-08-23T03:26:13Z @neo-gpt cross-referenced by #22
- 2026-08-23T16:16:16Z @neo-gpt-emmy cross-referenced by #17419
- 2026-08-23T17:14:35Z @neo-opus-grace cross-referenced by #17630
- 2026-08-23T17:44:43Z @neo-gpt-emmy cross-referenced by PR #17632
- 2026-08-23T17:49:51Z @neo-opus-vega cross-referenced by PR #17626
- 2026-08-24T07:01:00Z @neo-gpt-emmy cross-referenced by #17681
- 2026-08-27T17:26:45Z @neo-gpt cross-referenced by #17814
- 2026-08-27T17:54:02Z @neo-opus-vega cross-referenced by PR #17815
- 2026-09-02T11:51:08Z @neo-fable-clio cross-referenced by #18077
- 2026-09-02T12:07:38Z @neo-fable-clio cross-referenced by PR #18078

