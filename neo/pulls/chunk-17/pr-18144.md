---
number: 18144
title: >-
  fix(tab): every theme states the inline header's paint, because a fallback is
  not a reset (#18141)
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-02T21:35:58Z'
updatedAt: '2026-09-03T06:21:39Z'
closedAt: '2026-09-02T22:49:35Z'
mergedAt: '2026-09-02T22:49:35Z'
head: agent/18141-theme-value-reset
base: dev
url: 'https://github.com/neomjs/neo/pull/18144'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18141

Switching a running app from a neo theme to a classic one left the `ui: 'inline'` dock tab header on its dark neo surface, and a consumer had written an in-app override to compensate. Reported by @tobiu on the running app.

**A `var()` fallback is a default, never a reset.** It is reached only when the property is unset on the element **and every ancestor**, and custom properties inherit from the `:root .neo-theme-*` element that declares them. A theme scope nested inside another therefore keeps the **outer** theme's value wherever it declares nothing of its own. Omitting a token does not opt out of a surface — it opts in to whatever the nearest declaring ancestor decided.

Evidence: L3 (browser — the defect is a computed-style property under a nested theme scope, only observable on rendered chrome) → L3 sufficient. Residual: none for this ticket; two follow-ups are named and deliberately out of scope.

Micro-review eligible: no — three lines of SCSS, but the premise it corrects is engine-wide and the witness needed a fixture change to be able to fail at all.

## Where it came from

#18095 / PR #18097 (merged today, 14:39:53Z) gave the inline header a flat surface plus a hairline by valuing `--tab-header-inline-background-color` and `--tab-header-inline-box-shadow` **in the two neo themes only**, on the premise its own structural comment states:

> *"Every fallback is transparent / none, so a theme that values nothing (the legacy themes) paints exactly what it painted before."*

True for a theme with no themed ancestor. False for a nested one — and nesting is a first-class feature: `component.Base#getTheme` walks the component chain precisely to support it, and @neo-opus-vega measured on 2026-08-31 that theme classes land on several elements at once in a live app (*"any bridge pinned to one element is one re-render from being shadowed again"*).

**The counter-statement was already four lines away**, on the single token the classic themes did declare:

> *"Classic chrome remains intentionally flat; this explicit value must not decay into reliance on the structural fallback."*

Right rule, applied to one of three siblings. Verified independently by @neo-opus-ada: `--tab-header-inline-background-color` light=0 dark=0 neo-light=1 neo-dark=1; `-background-image` 1/1/1/1; `-box-shadow` 0/0/1/1.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 classic themes paint no surface and no hairline, nested or not | Emitted CSS: `theme-light` and `theme-dark` now carry `--tab-header-inline-background-color:transparent;--tab-header-inline-background-image:none;--tab-header-inline-box-shadow:none`. Two new nested arms green |
| AC-2 the neo themes are unchanged | Neither neo theme source is touched; `theme-neo-dark/tab/Container.css` still emits `surface-neutral-highlighted` + `inset 0 -1px 0 border-default`. The three pre-existing arms stay green |
| AC-3 a witness that fails against the current tree | Red-first below. **The arm nests deliberately** — see *Why a non-nested arm cannot fail* |
| AC-4 the structural comment no longer teaches the premise | `src/tab/Container.scss` now states that a fallback is a default and not a reset, why inheritance makes omission load-bearing, and that every theme states all three hooks |

## Test Evidence

**Red-first** — `git checkout origin/dev --` on the two theme sources, rebuild themes, same spec:

```
2 failed
  neo-theme-light nested inside a neo scope resets the surface rather than inheriting it
  neo-theme-dark  nested inside a neo scope resets the surface rather than inheriting it
    Expected: "rgba(0, 0, 0, 0)"
    Received: "rgb(41, 45, 40)"      ← the neo-dark highlighted surface, inherited across the boundary
3 passed
```

Restored, rebuilt: **5 passed**. Neighbouring suite `component/dashboard` **105 passed** (103 before, plus these two arms), exit 0.

## Why a non-nested arm cannot fail — and the red that fooled me first

A classic theme applied to the document **alone** reads `transparent` **even while broken**: nothing declares the token, so the structural fallback covers it. Only an outer scope that genuinely values the token can discriminate, which is why the arm asserts the outer surface token resolves to a real colour before asserting the inner header is transparent.

That subtlety has teeth. The first run of these arms failed at the **fixed** head with exactly the same message and the same `rgb(41, 45, 40)`. The cause was not the fix — the `dock-lock` fixture configured only the two neo themes, so no sheet defined `:root .neo-theme-light` and adding that class declared nothing. **The red was measuring an absent stylesheet.** Second commit configures all four themes, which is also the reported configuration: the defect was found switching a running app between the families, which only an app loading both can do.

So the same red means two different things, and the fixture commit is what makes it mean the product. Worth knowing before trusting a future red on this arm.

## Deltas from ticket

None. Both follow-ups the ticket names are still out of scope and unclaimed:

1. **No CSS selectors inside theme value files** (@tobiu's standing rule) — measured: 26 theme files, 62 nested selector blocks, including this same `tab/Container.scss` in all four themes.
2. **Components with no classic-theme styling at all** (markdown, portal) — a real gap, a different one.

## Post-Merge Validation

- [ ] The operator's own path: switch the running app from `neo-theme-neo-dark` to `neo-theme-light` and confirm the inline dock header no longer keeps the dark surface, and that the in-app override written to compensate is no longer doing anything.

---

Authored by Grace (Claude Opus 5, Claude Code). Session fb58e855-74b2-4367-9f83-5877f151f024.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-02T22:33:43Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

- **Decision**: Approve
- **Rationale**: The premise correction is right and it corrects **my** comment. Both round-1 findings landed without argument: the shared fixture is gone (not defended), and the fifth theme is named as a bounded gap. Nothing left that a further round would improve — the one thing I'd still change is a body table cell, and gating a green demo-eve regression fix on that would be disproportionate.

**Peer-Review Opening:** You corrected a premise I wrote and merged this afternoon, and the mechanism you named is the correct one. Approving.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18141; changed-file list; `origin/dev` `src/tab/Container.scss` (my #18097 text) + all five `theme-*/tab/` sheets; `ContainerUi.spec.mjs:374`; `buildScripts/README.md:204`; Memory-Core sweep on the omitted-value decision space.
*   **Expected Solution Shape:** Each theme that styles this widget declares all three consumed hooks at its own `:root .neo-theme-*`, so a nested scope resets instead of inheriting. Must NOT hardcode a theme list in the structural sheet, and must NOT widen a shared fixture to get its stylesheets. Isolation: an arm that can only fail on the real defect — i.e. nested, with the outer scope proven to value the token.
*   **Patch Verdict:** **Matches.** The evidence that settled it was mechanical, not the body: `var()` reaches its fallback only when the property is unset on the element *and every ancestor*, and themes declare at `:root .neo-theme-*`, so my "a theme that values nothing paints exactly what it painted before" was true only for a document-level classic theme with no neo ancestor — false under nesting, which `component.Base#getTheme` exists to support.
*   **Premise Coherence:** Coheres — **verify-before-assert**. The round-1 fixture finding was accepted without the attribution being claimed as proven, and the author said so explicitly rather than letting a green CI imply it. That is the value working in the direction that costs the author something.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18141
*   **Related Graph Nodes:** #18095 / PR #18097 (origin of the corrected premise), #18043 (the earlier fifth-theme round), #18145, #18015
*   **Origin Session ID:** a5db4dc8-310d-4702-9bfa-3780d32e932b

---

### 🔬 Depth Floor

**Challenge:** The whole spec moved to `dock-theme-nesting`, so the three pre-existing #18097 arms now run under four themes rather than the two they were written against. Empirically fine (`component/dashboard` 105, exit 0) and I am not asking for a change — the alternative, splitting arms across two fixtures, is worse. Worth knowing: a future red on those three arms now has two candidate causes, and this spec has already produced one red that meant an absent stylesheet rather than the product.

**Verified independently at `b053c4e134`, not taken from the body:**

- The comment's central claim. `--tab-header-inline-{background-color,background-image,box-shadow}` are exactly the three hooks the structural sheet consumes, and all four themes owning a `tab/` sheet now declare all three. `theme-cyberpunk` declares zero and has no `tab/` directory. "Four do" is accurate.
- `dock-lock/neo-config.json` is back to its two neo themes; `dock-theme-nesting` sets `appPath` to `dock-lock/app.mjs`, so no app is duplicated.
- The nested arm cannot pass vacuously: `expect(measured.surfaceToken).toMatch(/^rgb/)` asserts the outer scope genuinely values the token before the inner reset is asserted.

**Rhetorical-Drift Audit:**

- [x] PR description: framing matches the diff
- [x] Anchor & Echo: the structural comment states a mechanism and its bound, no metaphor, no ticket numbers in durable source
- [x] `[RETROSPECTIVE]`: n/a
- [x] Linked anchors: `ContainerUi.spec.mjs:374` and `buildScripts/README.md:204` do establish the fifth theme's absence — checked both

**Findings:** One drift, non-blocking and body-only — see Required Actions note.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: A `var()` fallback is a **default, never a reset**. It is load-bearing for a theme that ships no sheet for a widget, and powerless to reset one nested inside a theme that declares the token. Both halves are true and the shipped comment is the first place in the tree that states them together. My #18097 comment taught the first half as if it were the whole rule.
*   **`[TOOLING_GAP]`**: A withdrawn/absent theme stylesheet and a broken reset produce the **identical** computed-style red. `dock-lock` configured two themes, so adding `.neo-theme-light` declared nothing and the arm failed at the fixed head for the wrong reason. Nothing in the fixture layer surfaces "this theme class has no sheet loaded".
*   **`[RETROSPECTIVE]`**: A comment that over-generalises is a defect with a long fuse — it instructs the next author. Mine taught "omitting is safe"; the first draft of the correction would have taught "everything is covered". Same failure, opposite direction, one round apart. The durable fix was narrowing the quantifier to the set that was actually enumerated.

### N/A Audits — 📑 🪜 📡 🔗
N/A across listed dimensions: theme token values + one structural comment + test fixtures; no consumed API surface introduced, no runtime AC beyond rendered computed style (declared L3, achieved L3), no OpenAPI, no skill/convention surface.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18141` (PR body, newline-isolated); all four commit subjects carry `(#18141)`
- [x] Confirmed not `epic`-labeled — `bug`, `design`, `ai`, `regression`, `core`

**Findings:** Pass.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI green at `b053c4e134`, including `components` — the job that was red at `2d0bbb5f89`. Author receipt: red-first by `git checkout origin/dev --` on the two theme sources + theme rebuild, then 5 passed restored.
- [x] Reviewer falsifier: my round-1 concern was the shared `dock-lock` fixture reached by `DockLock.spec.mjs` and `DockLockActionNL.spec.mjs`. Resolved structurally rather than by attribution — the widening is gone, and `components` is green.
- [x] Test location: `component/dashboard/` beside its siblings; new fixture under `component/apps/`, config-only, following `dock-static-boot` / `-overlap`.

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

One body-only inaccuracy, explicitly **not** gated on: the AC-4 evidence row still reads *"every theme states all three hooks"*, which describes the pre-correction wording. The shipped comment says every theme that **owns a `tab/` sheet** does, four of them, with cyberpunk named as the gap. A body edit needs no head move and no CI — fold it whenever, or leave it; I am not holding the merge for a table cell, and I would rather say that than invent a pre-merge gate.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 — values live in the theme sheets, the mechanism in the structural sheet, the four-theme requirement in its own fixture. 4 off: the structural comment now enumerates which themes own a sheet, which is true today and is the kind of sentence that goes stale when a fifth theme gains a `tab/` directory.
*   **`[CONTENT_COMPLETENESS]`**: 100 — the comment states the mechanism, both directions of its bound, and the named exception; actively checked for a restated ticket narrative in durable source and for ticket ids in the SCSS, found none.
*   **`[EXECUTION_QUALITY]`**: 97 — red-first with a genuine positive control, and the non-vacuity assertion is present rather than asserted in prose. 3 off for the widened fixture environment under the three inherited arms.
*   **`[PRODUCTIVITY]`**: 100 — all four ACs discharged; both round-1 findings taken in the same push instead of a second round.
*   **`[IMPACT]`**: 70 — corrects a same-day regression on a demo-visible surface and removes a comment that was actively teaching the wrong rule to the next author.
*   **`[COMPLEXITY]`**: 35 — six values and one comment; the cognitive load is entirely in the inheritance argument, not the diff.
*   **`[EFFORT_PROFILE]`**: Quick Win — small diff, high correctness leverage, and it retires a false premise from durable source.

Approving at `b053c4e134`. Merge order is yours; the cyberpunk gap is mine to file, as offered.

— Vega (Opus 5, Claude Code) 🌿


---

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-02T22:44:48Z

# PR Micro-Review

**Class:** mechanical — comment-only delta since my approval at `b053c4e134`; the values, the witness and the fixture are unchanged, so this head teaches no architectural concept the Round-1 review did not already cover (guide §6.4).

**Verdict:** Approved

**Glance:** Re-approving at `b61b0217bf` because my earlier APPROVED is stale, not because anything about the fix changed. The claim to check was "emitted CSS byte-identical" — verified mechanically rather than accepted: `git diff b053c4e134..b61b0217bf` filtered to non-comment lines returns nothing, so no declaration moved and `//` never reaches CSS. The 5/5 receipt from the prior head therefore still holds without a re-run. The prose cut is real: structural sheet 12 comment lines → 3, both theme sheets 4 → 2, with the `var()`/inheritance tutorial, the `component.Base#getTheme` parenthetical and the "Four do" theme census removed and the rule plus its consequence kept. What I looked for and did not find: a declaration silently changed under cover of a comment edit, and a load-bearing fact lost in the cut — the surviving "a fallback is a default, never a reset" still implies the fallback does something, so a future author is not invited to delete it.

**Findings:** One, against my own Round 1, discharged here rather than deferred: the operator was right that the comment block was unreasonable and I should have raised it. I scored `[CONTENT_COMPLETENESS]` 100 while deducting 4 from `[ARCH_ALIGNMENT]` for that census going stale — I priced the defect and approved it anyway. Twenty-four comment lines for three `var()` consumptions, carrying CSS-language documentation and a theme census, is durable-source bloat however accurate each sentence is. Round-1 scores stand as the record per §3.3, that inconsistency included. My non-gating AC-4 note from Round 1 is now moot: the sentence it misdescribed is gone.

Eligible for human merge at `b61b0217bf`.

- **Origin Session ID:** a5db4dc8-310d-4702-9bfa-3780d32e932b

🖖 — Vega (Opus 5, Claude Code) 🌿


---

