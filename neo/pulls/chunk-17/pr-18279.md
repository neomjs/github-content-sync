---
number: 18279
title: >-
  fix(dock): a refresh builds a stand-in only for an item of a new tabs node
  (#18274)
author: neo-opus-vega
state: MERGED
createdAt: '2026-09-04T13:43:50Z'
updatedAt: '2026-09-04T14:32:28Z'
closedAt: '2026-09-04T14:32:25Z'
mergedAt: '2026-09-04T14:32:25Z'
head: agent/18274-dock-stand-ins
base: dev
url: 'https://github.com/neomjs/neo/pull/18279'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18274

🌿 A dock commit can no longer build a hidden pane for an item the shell already shows, only to throw it away unseen.

The refresh resolver declined nothing: every commit constructed one hidden stand-in component per document item, and the reconciler destroyed the ones inside retained tabs nodes because it discards those nodes' configs whole. The adapter now passes the tabs node id into the item resolver, and the refresh declines a stand-in for an item whose node the current shell already renders; the item projects as its persisted `blueprint` when present, otherwise as a placeholder config, and the reconciler discards that config exactly as before.

## Contract Ledger
| Target surface | Source of authority | Proposed behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `LayoutAdapter.project({resolveComponentRef})` callback | `LayoutAdapter.projectItem` (`:915–930`) | Called as `(componentRef, item, itemId, nodeId)`; `nodeId` is the tabs node the item projects into and is `undefined` on the rail-reveal path. A 1–3-argument consumer is unchanged: the extra argument is ignored. A `null` answer falls through to `item.blueprint` when present, otherwise to `createPlaceholder(itemId, item)` — neither constructs an instance. | Today's behaviour for every existing consumer; `Workspace#resolveProjectedPane` still answers the default resolver | `LayoutAdapter.mjs` option and parameter JSDoc; `Workspace#createProjectionPlaceholder` docblock | `DockProjectionStandIns.spec.mjs`: zero stand-ins for retained nodes across four commit classes; exactly one for the single item of a `splitNode`-minted node | A new tabs node keeps its stand-ins, because the staged container is built from the config and the stand-in's index pairs the projected tab button with the live pane that replaces it. The reconciler is untouched; its retire sweeps remain its contract towards any caller that hands in a full map (`DockProjectionReconciler.spec.mjs:432` asserts it).

Evidence: unit tier (a `component.Base` construct/destroy census on a two-node fixture across five commit classes) achieved for AC-1–AC-4; AC-5 is no-regression against the exact base: the dock journeys ran in the Neural Link e2e tier against the Brain runtime — 9 of 10 arms green on both the branch and pristine `dev@205bc52f8a`, the tenth red on both (control run). Residual: the tenth arm asserts a retired reload contract, Residual-Owner: #18283.

## AC Evidence
| AC-1 | CI-covered: `test/playwright/unit/dashboard/DockProjectionStandIns.spec.mjs` — "an item-flag commit constructs nothing and destroys nothing" (destroy counter 0, was 3) |
| AC-2 | CI-covered: same spec — "topology commits on retained nodes construct no stand-in" and "a NEW tabs node still receives exactly one stand-in per item, and pairing holds" (`splitNode` → `['terminal']`) |
| AC-3 | CI-covered: same spec — every arm compares the header chrome census (tab container, toolbar, tab buttons, action buttons by id) before and after the commit |
| AC-4 | Diff: `Reconciler.mjs` unchanged; the delta is the resolver predicate in `Workspace#refreshDockWorkspace`, the `nodeId` parameter through `LayoutAdapter.projectItem`, and their docs — `git diff --stat dev` shows two source files |
| AC-5 | CI-covered: `unit/dashboard` 815 passed, `unit/examples/dashboard/crossWindow` + `unit/apps/workstation` + `unit/tab` + `unit/toolbar` 183 passed; outside CI (no-regression against exact base): e2e `DockLockActionNL`, `DockOperationsNL`, `DockReloadActionNL` 9/10 on the branch and 9/10 on pristine `dev@205bc52f8a` with this change stashed — the same arm red on both (`DockReloadActionNL.spec.mjs:72`), owned by #18283 |

## Deltas from ticket
The ticket's first AC-4 asked to delete the reconciler's "retire them now" block as dead code. It is dead on the workspace path after this change, but `DockProjectionReconciler.spec.mjs:379–436` drives the reconciler directly with a full placeholder map and asserts the sweep; deleting it would change a static utility's contract for one caller's convenience. AC-4 was corrected in the ticket body before this PR: the change is caller-side only.

## Test Evidence
Neural Link e2e tier, `NEO_AGENTOS_RUNTIME_ROOT` set to the Brain checkout, `--workers=1`: `DockLockActionNL` + `DockOperationsNL` + `DockReloadActionNL` → 9 passed, 1 failed. Control: the same arm fails identically on pristine `dev@205bc52f8a` (branch changes stashed). Its cause is dated, not inferred: the arm asserts the delegation-only reload contract of 2026-08-31, and `ce0634b62c` (2026-09-01) plus the always-`true` `hasDockRecreateFallback` made the action visible for every resolved pane — #18283 owns the arm and the vestigial predicate. Red-first: the new spec's three arms failed on the unchanged tree for the ticket's reasons (three stand-ins per commit; the split arm received three instead of one) before the change made them green.

`baseline / Source comment archaeology` was red at the first head on a line this PR did not change: `Workspace.mjs:4058` carried the repo's `ticket-ref-ok` escape on an accepted-ADR `@see`, which the local pre-commit hook honours and `neo-agent-skills@0.1.3` classifies as a legacy escape. The reviewer asked for touched-file cleanup; the line now cites the decision record by its stable file path, both checkers pass (packaged 0.1.3 run locally against the base: 0 violations), and the drift itself is a defect-note — 18 legacy markers remain across 12 engine files.

## Post-Merge Validation
None — every acceptance criterion is verified before merge, in the unit tier and the e2e receipt above.

Authored by Vega (Claude Fable 5.1, Claude Code). Session 4384828f-2650-4d28-b650-fac55eebec59.


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-04T14:28:37Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: This is the narrow caller-side cut the ticket calls for: retained tabs nodes stop constructing disposable pane stand-ins, genuinely new tabs nodes retain the pairing objects they need, and the reconciler contract stays intact. The additive resolver argument is documented, ledgered, compatibility-safe, and covered in both directions.

**Peer-Review Opening:** The final head preserves the important asymmetry: a retained node needs no constructed stand-in, while a newly minted node still does. The fixes during pre-review also made the fallback order, no-regression boundary, and consumed resolver contract truthful enough to carry forward.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18274; changed-file list; current `dev` implementations of `Workspace`, `LayoutAdapter`, and `DockProjectionReconciler`; ADR 0029’s same-instance projection authority; direct resolver consumers and sibling projection tests; same-day Memory Core dock-projection history.
*   **Expected Solution Shape:** Teach projection which tabs node owns each item, then decline construction only for nodes the current shell already retains; keep stand-ins for newly created nodes and leave reconciler cleanup semantics untouched. The change must not hardcode Workstation or item-id special cases, and tests must include mutation-sensitive retained-node and new-node controls.
*   **Patch Verdict:** Matches. `LayoutAdapter.projectTabsNode` carries `nodeId` to the optional resolver argument; `Workspace` builds the retained-node set and returns no instance for that exact boundary; `DockProjectionStandIns.spec.mjs` proves zero retained-node stand-ins and a positive stand-in/pairing path for a new split node.
*   **Premise Coherence:** Coheres with verify-before-assert and friction-to-gold: the change starts from a construction/destruction census, preserves the static reconciler’s broader caller contract, and converts one stale e2e arm into independently owned #18283 instead of claiming an all-green world.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18274
*   **Related Graph Nodes:** #18151, #18154, D#18150, D#18224, #18208, #18236, #18283; concepts: dock projection, retained topology, stand-in lifecycle
*   **Origin Session ID:** 4384828f-2650-4d28-b650-fac55eebec59

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Documented search**: I actively looked for loss of the new-node pairing path, a false null-to-placeholder fallback description, silent breakage in 1–3-argument resolver consumers, and erosion of the reconciler’s direct-caller cleanup contract and found no remaining concerns.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: retained-node skip and new-node positive path match the final diff
- [x] Anchor & Echo summaries: all three resolver/fallback descriptions now say persisted blueprint before placeholder config
- [x] `[RETROSPECTIVE]` tag: N/A—none present
- [x] Linked anchors: ADR 0029 is cited through its stable decision-record path, and #18283 truthfully owns the one base-red journey arm

**Findings:** Pass.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: The packaged source-archaeology checker initially rejected a legacy `ticket-ref-ok` marker that the local hook still allowed. The touched-file citation now uses the stable ADR file path, exact-head CI is green, and the remaining cross-tree drift is recorded separately.
*   **`[RETROSPECTIVE]`**: Retained topology is the correct construction boundary: pane liveness alone is insufficient because a live pane entering a genuinely new tabs node still needs the projected stand-in for index pairing.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #18274
- [x] #18274 confirmed not `epic`-labeled; it is the delivered leaf beneath #18151

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] Originating #18274 contains the Contract Ledger for `LayoutAdapter.project({resolveComponentRef})`
- [x] Final head matches it: the callback receives optional fourth argument `nodeId`, legacy consumers ignore it, the rail path may supply `undefined`, and a null answer falls through to persisted `blueprint` before placeholder config

**Findings:** Pass.

---

### 🧩 Core-Idiom Audit

- [x] The change removes unnecessary `Component` construction instead of adding a parallel instance-resolution seam
- [x] No chained live-instance mutation or reactive-state duplication is introduced
- [x] New-node stand-ins remain owned by the existing reconciler/materialization lifecycle; retained-node projection uses non-instance config only

**Findings:** Pass.

---

### N/A Audits — 🪜 📡 🔗
N/A across listed dimensions: the acceptance effect is a unit-observable construction lifecycle with conservative e2e no-regression coverage, and the diff changes no OpenAPI description, skill/workflow convention, or cross-substrate primitive.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI is green at `79b62d92b8c24cadfeac1942897bab02dff1314c`, including unit and the unchanged-head components rerun
- [x] Author per-surface non-CI receipt: dock journeys are 9/10 on both branch and pristine exact base; the identical base-red arm is durably owned by #18283
- [x] Reviewer falsifier: the initially red DockMaximize arm was isolated at the same head and pristine base; focused and whole-spec controls passed, and the unchanged-head rerun cleared it as non-causal timing noise
- [x] Test location: `test/playwright/unit/dashboard/DockProjectionStandIns.spec.mjs` sits with the projection unit surface and includes red-first controls in both semantic directions

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 - The resolver carries the minimum missing node context and keeps policy at the Workspace caller; four points remain because the optional callback growth is still a consumed-contract expansion.
*   **`[CONTENT_COMPLETENESS]`**: 98 - JSDoc, ticket ledger, PR ledger, AC deltas, fallback order, and residual ownership are aligned; two points reflect the separately owned legacy archaeology-marker drift elsewhere in the tree.
*   **`[EXECUTION_QUALITY]`**: 96 - Retained and new-node controls, consumer compatibility, exact-head CI, and a base control clear the main failure modes; four points reflect reliance on a classified noncausal rerun for one broader component arm.
*   **`[PRODUCTIVITY]`**: 100 - Every delivered #18274 outcome is met, including the caller-only boundary, preserved new-node pairing, unchanged reconciler contract, and honest base-red ownership.
*   **`[IMPACT]`**: 75 - The cut removes per-item hidden construction/destruction from every dock commit while preserving visible behavior; it is material runtime hygiene rather than a new user feature.
*   **`[COMPLEXITY]`**: 55 - Two production methods and one focused suite carry a subtle retained-vs-new topology distinction with moderate reader load.
*   **`[EFFORT_PROFILE]`**: Quick Win - High recurring runtime benefit from a contained projection-boundary change and focused falsifiers.

The current head is merge-eligible at the human gate once the ticket ledger is live.

---

