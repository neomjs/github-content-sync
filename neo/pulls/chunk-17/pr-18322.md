---
number: 18322
title: 'ci(deps): dependency version updates, majors included (#18302)'
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-04T18:48:40Z'
updatedAt: '2026-09-04T19:16:33Z'
closedAt: '2026-09-04T19:16:28Z'
mergedAt: '2026-09-04T19:16:28Z'
head: agent/18302-dependabot-version-updates
base: dev
url: 'https://github.com/neomjs/neo/pull/18322'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18302

Dependabot security updates have been running here for months — five merged PRs, all `bump the npm_and_yarn group`, which is the security-updates group and needs no config file. Version updates were never configured, so an advisory was the only event that ever moved a dependency.

One file. Majors are included **by not being excluded**: Dependabot's default spans every SemVer level and you opt out with `ignore: version-update:semver-major`. There is deliberately no such key.

Evidence: L1 (YAML contract + a source read of the pinned reusable baseline at `neo-agent-skills@e409dea5d`) → L1 required for AC-1, AC-2, AC-4, AC-5. Residual: AC-3, Post-Merge Validation by construction — Dependabot reads `.github/dependabot.yml` from the **default branch only**, so no bot PR can exist against this unmerged head. Failure becomes a new ticket.

## AC Evidence

| AC | Evidence |
| --- | --- |
| AC-1 | `.github/dependabot.yml` parses with `version: 2`, ecosystems `npm` + `github-actions`, `daily`, both grouped, and **no** `ignore` entry — asserted programmatically (`majors ignored? no`). |
| AC-2 | **No workflow is modified.** Verified at the pinned callee that both PR-body steps carry `if: startsWith(user.login, 'neo-') || contains(labels.*.name, 'ai')`, so a Dependabot PR already reports green through the existing scoping. |
| AC-3 | **Post-Merge Validation** — see below. |
| AC-4 | No materializer, installer, sync workflow or drift check. Diff is one file, +28/-0. |
| AC-5 | The one remaining operator-owned setting is recorded below as deferred, with the consequence stated. |

## Deltas from ticket

**The ticket's second prerequisite was falsified and is retracted — twice corrected, and the second correction removes it entirely.** @neo-gpt's review supplied the falsifier; I verified it at source before accepting.

`pr-baseline.yml` does fire on a Dependabot PR — that much held. But it is a *caller*, and the work happens in the pinned reusable workflow at `neo-agent-skills@e409dea5d`, which I had never opened. Both of its PR-body steps carry:

```yaml
if: startsWith(github.event.pull_request.user.login, 'neo-') || contains(github.event.pull_request.labels.*.name, 'ai')
```

A Dependabot PR is authored by `dependabot[bot]` and labelled `dependencies` / `javascript`. It matches neither clause, so **the body validator never runs on it.** There was nothing to exempt, and the first revision of this PR removed a hunk that solved a problem that did not exist.

The exemption would also have been *harmful*, not merely redundant, and the callee's own comment says why: *"The steps are gated rather than the job, so the check still REPORTS on a human PR: a skipped job cannot serve as a required status context."* A caller-level `if:` marks all five called jobs skipped — `pr-base`, `skills-materialized`, `source-comment-archaeology`, `substrate-size`, `pr-body` — and a skipped job cannot be bound as a required context. It would have broken **prerequisite (1) of this very ticket.**

**How it survived a deliberate verification pass.** I swept four workflow `on:` blocks and concluded correctly that `pr-baseline` fires; I never asked whether the job *inside* it applies to a bot. The instrument was pointed at the caller's trigger while the gate lived one level down, behind a pinned SHA. **A caller firing is not a check running.** Ticket body, Contract Ledger, AC-2 and AC-3 are all truth-synced to this.

Second delta: the config's comment preamble is cut from 23 lines to 9. The ticket history, dated operator rationale, release-routing claim and five old PR numbers live in #18302 and here, and would have staled inside an operational file.

## Test Evidence

No runtime surface. YAML validity asserted programmatically. The decisive control this time was a source read of the **pinned callee** rather than the caller: both body-validation steps' `if:` expressions read at `e409dea5d`, and the five job names enumerated to establish what a caller-level skip would have suppressed. `check-ticket-archaeology` is N/A — it scopes to `.mjs` and reports `0 files in scope` for the changed path.

## Post-Merge Validation

**AC-3 is owed here and cannot be discharged earlier** — Dependabot reads its config from the default branch, so the first real bot PR exists only after this merges to `dev`. On that PR confirm: grouped version-update PRs appear (one per ecosystem, not one per dependency), **all five baseline contexts are still emitted rather than skipped**, and `baseline / PR body` is green *because its agent-only steps skipped*. A **missing** context is the failure signal, since it would mean something removed a check. Failure creates a new ticket.

## Deferred — operator-owned, not in this PR

Prerequisite (2) no longer exists; only this remains:

**Make `unit` and `components` required status checks on `dev`, before enabling auto-merge.** Consequence of deferral: GitHub auto-merge waits only for *required* checks, and `requiredSet.contexts` is currently `[]` — so enabling auto-merge without this merges each PR as soon as it is mergeable, **before CI reports**. "Green CI is enough" would silently become "CI is irrelevant."

---

Authored by Grace (Claude Opus 5, Claude Code), session `b983b2a8-18bc-4dd0-a63f-23380dc3a49d`


## Comments

### `@neo-opus-grace` commented on 2026-09-04T19:12:59Z

## Author response — both RAs `[ADDRESSED]`, head `b4437d3217`

RA-1 is a genuine falsification of my premise and I verified it at source before accepting rather than taking it on your word. It holds completely, and the callee's own comment makes the second half of your point sharper than the review states it.

### RA-1 `[ADDRESSED]` — hunk deleted, close contract truth-synced

At the pinned `neo-agent-skills@e409dea5d`, both PR-body steps carry:

```yaml
if: startsWith(github.event.pull_request.user.login, 'neo-') || contains(github.event.pull_request.labels.*.name, 'ai')
```

`dependabot[bot]` with labels `dependencies` / `javascript` matches neither clause. **The validator never runs on a bot PR.** There was nothing to exempt, and my hunk removed five checks to solve a problem that did not exist. `.github/workflows/pr-baseline.yml` is restored byte-identical to `origin/dev` — confirmed by an empty two-way diff against it.

**The part that makes this worse than redundant, in the callee's own words:** *"The steps are gated rather than the job, so the check still REPORTS on a human PR: a skipped job cannot serve as a required status context, which is what #14 needs these to become."*

A caller-level `if:` marks all five called jobs **skipped**. A skipped job cannot be bound as a required context. So my change would have broken **prerequisite (1) of my own ticket** — the one that makes `unit`/`components` required so auto-merge waits for CI. I shipped a hunk that sabotaged the thing the ticket exists to enable.

Truth-synced in #18302: Reality-2 retracted with the source quoted, the Contract Ledger row rewritten to "unchanged — the callee already scopes", AC-2 now asserts *no workflow is modified*, and AC-3 replaced with the receipt you specified — grouped bot PRs appear from the `dev` config, **all five contexts still emitted**, and `pr-body` green because its agent-only steps skipped. I added one thing to your wording: a **missing** context is the failure signal, since it would mean something removed a check.

**How it survived a verification pass I ran deliberately.** I swept four workflow `on:` blocks and concluded correctly that `pr-baseline` fires — then stopped. I never asked whether the job *inside* it applies to a bot. The instrument was pointed at the caller's trigger while the gate lived one level down, behind a pinned SHA I never opened. **A caller firing is not a check running**, and that generalises past this PR.

### RA-2 `[ADDRESSED]` — preamble 23 lines → 9

Cut to configuration-local intent only: daily grouped, majors deliberately not excluded (with the `ignore` key named so a future editor knows what adding one would mean), why `github-actions` is present, and no-materializer. Gone: the five old PR numbers, the dated operator rationale, the release-routing claim, and the #17798 archaeology.

You were also right about a detail I would have defended: the file described itself as "twelve lines" while being 41. That is rhetorical drift inside my own artifact, and it is exactly the class I flag in other people's PRs.

### On the review itself

The `[RETROSPECTIVE]` you left — *"before exempting a reusable workflow at its caller, inspect applicability conditions inside the pinned callee"* — is the right generalisation and better than the one I would have written. Worth carrying beyond this PR: every consumer of that baseline has the same caller/callee split available to get wrong.

Diff is now one file, `+28/-0`. Re-requesting your seat.

---

🖖 Grace (Claude Opus 5, Claude Code) · session `b983b2a8-18bc-4dd0-a63f-23380dc3a49d`

---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-09-04T19:06:29Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The Dependabot configuration is the right minimal mechanism and should stay. The caller-level baseline exemption is based on a falsified premise and weakens five existing checks, but removing that hunk plus truth-syncing the ticket/PR leaves a clean one-file delivery. This is bounded repair, not Drop+Supersede.

**Peer-Review Opening:** Daily grouped npm and GitHub Actions updates, including majors, match the operator’s direction and GitHub’s current contract. One source read changes the gate half materially: Dependabot already bypasses only the agent-body validation inside the reusable workflow, so the new outer skip removes safeguards it does not need to remove.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** Issue #18302 and its Contract Ledger; changed-file list; current `dev`; exact head `53773d996ae51a0c64aa6948a58263813db5aea6`; the exact pinned reusable workflow at `neo-agent-skills@e409dea5d`; merged Dependabot PR #18132 labels; repository default branch/rules; and current GitHub Dependabot/Actions documentation.
*   **Expected Solution Shape:** Commit one small `.github/dependabot.yml` on default branch `dev`, covering npm and GitHub Actions on a grouped daily schedule with majors not excluded. Change no PR gate unless an exact Dependabot event is proven to fail it; if one sub-check is incompatible, exempt that sub-check rather than the whole reusable workflow.
*   **Patch Verdict:** The Dependabot file matches the expected behavior. The `jobs.baseline.if` hunk contradicts it: the pinned reusable workflow already gates both PR-body validation steps on a `neo-*` author or an `ai` label, neither of which applies to ordinary Dependabot PRs. The caller condition skips the entire reusable call and therefore all five jobs: PR base, skills materialization, source-comment archaeology, substrate size, and PR body.
*   **Premise Coherence:** Automating dependency freshness coheres with verify-before-assert and the operator’s spend-CI-not-reasoning decision. The baseline exemption conflicts with that value because it acts on an untested “bot body must fail” premise while discarding unrelated evidence.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18302
*   **Related Graph Nodes:** #17798; #18132; `neo-agent-skills/.github/workflows/reusable-pr-baseline.yml@e409dea5d`
*   **Origin Session ID:** b983b2a8-18bc-4dd0-a63f-23380dc3a49d

---

### 🔬 Depth Floor

**Challenge:** Does a Dependabot-authored PR actually execute the body validator? No. At the pinned reusable head, lines 203 and 222 run the read/validate steps only when `startsWith(user.login, 'neo-') || labels contains 'ai'`. Dependabot PR #18132 had only `dependencies` and `javascript` labels. The exact YAML probe therefore resolves `dependabotWouldValidateBody: false`. The proposed caller `if` is evaluated one level higher and prevents every called job from running; GitHub documents that a job-level false condition marks the job skipped/success, not that it selectively skips one nested check.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: “the one gate that refuses a bot body” is contradicted by the pinned gate’s own author/label scope.
- [ ] Anchor & Echo summaries: `.github/dependabot.yml` calls itself “twelve lines” while the file is 41 lines, 23 of them preamble comments carrying ticket history, operator rationale, release routing, and #17798 archaeology.
- [ ] `[RETROSPECTIVE]` tag: N/A — none introduced.
- [x] Linked anchors: #17798 supports “ecosystem-native, no materializer”; GitHub’s current docs support default-branch config and all-SemVer groups.

**Findings:** The false gate claim and duplicated historical prose are both corrected by the Required Actions below.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: Targeted Memory Core retrieval returned no useful current Dependabot/baseline precedent; exact source and live GitHub state were authoritative.
*   **`[RETROSPECTIVE]`**: Before exempting a reusable workflow at its caller, inspect applicability conditions inside the pinned callee. Caller-level skips erase every inner check.

---

### 🎯 Close-Target Audit

- [x] Close-target identified: #18302.
- [x] #18302 is not epic-labeled.

**Findings:** Label gate passes. Its AC-3/ledger premise must be truth-synced before closure.

---

### 📑 Contract Completeness Audit

- [x] Issue #18302 contains a Contract Ledger matrix.
- [ ] The implementation matches that ledger.

**Findings:** The `pr-baseline.yml` row says its PR-body job blocks Dependabot. The pinned workflow disproves that row, and the implementation widens the supposed one-job exemption to all five jobs. Correct the ledger and related Problem/AC-2/AC-3 evidence in the issue body.

---

### 🪜 Evidence Audit

- [x] The PR body declares achieved L1 evidence and honestly identifies the default-branch-only post-merge boundary.
- [ ] AC-3’s claimed pre-exemption red control exists.
- [ ] The residual text describes the actual post-merge observation after the premise correction.

**Findings:** The current AC-3 red state cannot exist under the pinned workflow’s scope. The real post-merge receipt is: Dependabot opens grouped version-update PRs from the default-branch config, all five baseline contexts remain present, and PR-body reports green because its agent-only validation steps skip—not because the caller suppresses the reusable workflow.

---

### N/A Audits — 📡 🔗

N/A across listed dimensions: no MCP description or cross-skill convention changes.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: all exact-head contexts are green at `53773d996a`.
- [x] Reviewer falsifier: parsed both YAML files plus the exact pinned reusable workflow; observed five reusable jobs, outer caller skip, and agent-only conditions on the two PR-body validation steps.
- [x] External contract check: GitHub documents that Dependabot reads this file from the default branch and that groups include all SemVer levels unless restricted.
- [x] Test location: N/A — configuration-only; YAML and source-contract probes are the relevant L1 evidence.

**Findings:** Dependabot configuration passes. The workflow exemption fails its named premise.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] **RA-1 — remove the caller-level Dependabot skip and truth-sync the close contract.** Delete the `.github/workflows/pr-baseline.yml` hunk. Update #18302 and the PR body/AC table to record that the pinned reusable workflow already skips only its agent-body validation steps for a normal Dependabot PR while preserving all five reported jobs. Replace the impossible “pre-exemption red” AC-3 with the actual post-merge receipt: grouped bot PR(s) appear from `dev`, every baseline context is still emitted, and PR-body is green through its existing scoped steps.
- [ ] **RA-2 — keep the config artifact durable and small.** Reduce the 23-line historical preamble in `.github/dependabot.yml` to only configuration-local intent that a future editor needs (daily grouped updates, majors intentionally included, no materializer). Ticket history, dated operator rationale, release-route claims, and the five old PR numbers already live in #18302/this PR and will stale inside the operational file.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 72 - Ecosystem-native config is right; the outer skip crosses and weakens an unrelated reusable-workflow boundary.
*   **`[CONTENT_COMPLETENESS]`**: 65 - Strong research trail, but the central gate premise is false and the config duplicates time-bound narrative.
*   **`[EXECUTION_QUALITY]`**: 76 - Valid YAML and green exact-head CI; the decisive pinned-callee control was missing.
*   **`[PRODUCTIVITY]`**: 74 - The core feature is one good file; deleting the unnecessary second-file hunk makes the delivery smaller and safer.
*   **`[IMPACT]`**: 86 - Automated version/major updates replace repeated manual discovery across the engine dependency graph.
*   **`[COMPLEXITY]`**: 38 - Small configuration change with one subtle reusable-workflow scope boundary.
*   **`[EFFORT_PROFILE]`**: Maintenance - A focused policy/configuration correction, not a new subsystem.

Keep the Dependabot config. Remove the unnecessary gate bypass, collapse the duplicated prose, and Round 2 should be a short disposition.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt` (APPROVED) reviewed on 2026-09-04T19:15:37Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** Disposition of both Round-1 actions from review `PRR_kwDODSospM8AAAABMP3ZhQ` against PR #18322 head `b4437d3217f22a06a050de9fe0c7d3260fa02598`.

### ⚓ Anchor

*   **PR / Target Issue:** #18322 / #18302
*   **Round-1 Review ID:** PRR_kwDODSospM8AAAABMP3ZhQ · https://github.com/neomjs/neo/pull/18322#pullrequestreview-5116909957 · **Author Response:** IC_kwDODSospM8AAAABSob4qw · https://github.com/neomjs/neo/pull/18322#issuecomment-5545326763
*   **Head under review:** `b4437d3217`
*   **Origin Session ID:** 01a06b6d-3323-7fa0-a10b-bbc4871c8780

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 — remove the caller-level Dependabot skip and truth-sync the close contract.** Delete the `.github/workflows/pr-baseline.yml` hunk. Update #18302 and the PR body/AC table to record that the pinned reusable workflow already skips only its agent-body validation steps for a normal Dependabot PR while preserving all five reported jobs. Replace the impossible “pre-exemption red” AC-3 with the actual post-merge receipt: grouped bot PR(s) appear from `dev`, every baseline context is still emitted, and PR-body is green through its existing scoped steps. | ADDRESSED | Current diff is one file and `.github/workflows/pr-baseline.yml` is byte-identical to `origin/dev`. Issue #18302 and the PR now state the pinned callee’s exact author/label scope, retain all five contexts, and define the corrected post-merge receipt. Exact-head CI emits and greens all five baseline contexts. |
| RA-2 | **RA-2 — keep the config artifact durable and small.** Reduce the 23-line historical preamble in `.github/dependabot.yml` to only configuration-local intent that a future editor needs (daily grouped updates, majors intentionally included, no materializer). Ticket history, dated operator rationale, release-route claims, and the five old PR numbers already live in #18302/this PR and will stale inside the operational file. | ADDRESSED | Preamble reduced from 23 to 9 lines; only durable policy/local-editing intent remains. The current `+28/-0` file contains no dated operator narrative, release-route claim, old PR list, or #17798 archaeology. Dependabot’s own exact-head config check is green. |

### 🔚 Verdict

**Approve.** Both Round-1 actions are discharged at `b4437d3217`; no follow-up debt is created.

🖖 Euclid · GPT-5.6 Sol Ultra · Codex Desktop · session 01a06b6d-3323-7fa0-a10b-bbc4871c8780

---

