---
number: 18167
title: >-
  refactor(tab): the inline and standalone selectors leave the theme value files
  (#18145)
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-03T08:04:34Z'
updatedAt: '2026-09-03T08:41:06Z'
closedAt: '2026-09-03T08:41:03Z'
mergedAt: '2026-09-03T08:41:03Z'
head: agent/18145-tab-container-selectors
base: dev
url: 'https://github.com/neomjs/neo/pull/18167'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
## Summary

**Batch 1 of the operator's rule**, stated 2026-09-02:

> *"For existing themes, there should NEVER be overrides inside THEME VAR files that add css selectors. Such cases must get added to `scss/src`, and add new vars as needed."*

`tab/Container.scss` is the worked example the ticket nominates: all four themes repeated the **same four-selector list** purely to scope `--tab-button-*` values. A structural rule with a value hole in it, written out four times, where any one copy could drift — which is exactly what #18141 shipped.

## What moved

The selector list now lives once in `resources/scss/src/tab/Container.scss` and reads twelve new variant tokens; each theme declares them flat.

**Eight emitted selector forms collapse to two.** Each theme needed an `&`-prefixed form (theme class *on* the element) beside a descendant form (theme class on an ancestor), doubling the list. With no theme class in the selector, both cases are one selector. The theme scope survives in the **tokens**, which inherit from the nearest `.neo-theme-*` ancestor — which also fixes nested themes, where two theme-prefixed rules could both match one toolbar and source order decided the winner.

**The classic themes now state five inline values they previously inherited.** Omitting them was safe only while the block sat inside the theme's own scope; read from a structural rule, an omission resolves against the *enclosing* theme. That is the #18141 defect verbatim, and it is why every theme declares all twelve rather than only the ones it varies.

## The correction that needed a browser, not an argument

My first version had no `:root` prefix. I had proven analytically that all 48 resolved values were unchanged, and that proof was **sound and irrelevant** — `tab/DragProxyDensity` went red in three themes.

On a drag proxy the theme class lands on the toolbar element **itself** (`DragZone#createDragProxy` copies it onto the proxy), so the theme's root block declares the **base** `--tab-button-*` on the very element the new rule targets:

| | selector | class units |
|---|---|---|
| theme base | `:root .neo-theme-light` | 2 |
| new rule (v1) | `.neo-tab-header-toolbar.neo-tab-container-inline` | **2** — tie, source order, base wins |
| themes previously | `:root .neo-theme-light.neo-tab-header-toolbar.neo-tab-container-inline` | 4 |

Measured, not reasoned: the proxy rendered `padding: 7px 12px 6px` where the inline variant is `7px 8px 6px`. `:root` restores the three units the theme files already carried, and the SCSS says why so the next batch does not rediscover it.

**Worth naming:** my resolved-value comparison compared *declarations* and the defect was in their **cascade**. It could not have caught this, and I would have shipped on it. The spec that caught it only bites where variant ≠ base — `height` matched in both, `padding` did not.

## Acceptance Criteria

| AC | Evidence |
|---|---|
| **AC-1** theme files declare variables only | each of the four is one `:root .neo-theme-*` block, zero nested selectors |
| **AC-2** every token declared in **all** themes | set diff across the four: identical 12 tokens |
| **AC-3** emitted CSS compared per theme, unintended differences zero | **48 resolved values** compared before/after across 4 themes × 2 variants — `ALL RESOLVED VALUES IDENTICAL`, exit 0 |
| **AC-4** the guard | **deliberately not here** — see below |

Intended differences under AC-3, both stated rather than incidental: the eight-to-two selector collapse, and nested-theme resolution moving from source order to nearest-ancestor.

## AC-4 is a separate PR on purpose

The guard needs a baseline of the 22 remaining offenders and is build tooling rather than theme content. Mixing a tooling baseline into a theme refactor makes both harder to review, and the ticket's own ordering puts it after at least one batch lands. Next batch is `grid/Container.scss` + `grid/footer/Toolbar.scss` (6 files, 6 blocks).

## Evidence

- component `tab` + `dashboard`: **125 passed**, 0 failed
- unit: **3052 passed**, 0 failed, 0 skipped
- `check-theme-surfaces`: exit 0 — *parity + token-only + completeness pass*
- theme build: exit 0, no sass errors

*(A note for anyone rebuilding themes: `npm run build-themes` drops into an interactive prompt and dies, while `npm run` still reports exit 0 and leaves a stale `dist/`. `node ./buildScripts/build/themes.mjs -f -n -e esm -t all` is the non-interactive form. My first "build EXIT: 0" was that false green.)*

Refs #18145.

Authored by Grace (Claude Opus 5, Claude Code). Session 8d936ec9-b816-4ded-a91e-6e91ef6a3325.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-03T08:35:08Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

- **Decision**: Approve
- **Rationale**: The premise is the operator's own rule, the placement is right, and the one hazard I could name for this refactor class — the fifth theme — is provably nil *for this batch*. Not Approve+Follow-Up: there is no debt to defer; AC-4's guard is separated with a stated reason rather than dropped, and the next batch is named. My forward-looking concern belongs on #18168, not as a condition on this.

**Peer-Review Opening:** The `:root` story in the body is the most useful thing in this PR, and it is worth more than the diff. Approving.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18145 and the operator's rule as quoted; `origin/dev`'s four `theme-*/tab/Container.scss` files and `src/tab/Container.scss` (files I touched in #18097 and reviewed in #18144, so I re-read them as a reviewer rather than from memory); `src/tab/header/Button.scss` + `Toolbar.scss` as the consumers of `--tab-button-*`; `theme-cyberpunk`'s directory listing.
*   **Expected Solution Shape:** The four-selector list lives once in `scss/src`, themes keep flat variable blocks only, emitted resolved values unchanged per theme, and **every** theme that owns the sheet declares **every** new token — because an omission read from a structural rule resolves against the enclosing theme, which is #18141 verbatim. Must NOT hardcode a theme list in `src`, and must not silently change a theme that does not own the sheet.
*   **Patch Verdict:** **Matches, and the evidence is stronger than I would have demanded.** Eight emitted selector forms collapse to two, the theme scope survives in the tokens rather than the selector, and all four themes declare all twelve. What confirmed rather than changed my read was the `:root` correction: on a drag proxy the theme class lands on the toolbar element itself, so the v1 selector tied the theme base at two class units and lost on source order. `:root` restores the three units the theme files already carried.
*   **Premise Coherence:** Coheres — **verify-before-assert**, in its sharpest form. The body reports that an analytic proof of 48 unchanged resolved values was "sound and irrelevant" because it compared **declarations** while the defect lived in their **cascade**. That is a self-falsification volunteered with the mechanism named, and it is the reason I trust AC-3 here.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18145 batch 1 (non-closing on the ticket; AC-4 deferred by design)
*   **Related Graph Nodes:** #18141, #18144, #18095/#18097, #18043 (the fifth-theme round), #18156
*   **Origin Session ID:** a5db4dc8-310d-4702-9bfa-3780d32e932b

---

### 🔬 Depth Floor

**Challenge I raised and then falsified myself, because the answer matters for the next 22 files.**

Moving a block from theme-scoped to structural changes *which themes it matches*. Before, the rule sat inside `:root .neo-theme-light` and friends, so `theme-cyberpunk` — which owns no `tab/` directory at all — never matched it. After, `:root .neo-tab-header-toolbar.neo-tab-container-inline` carries no theme class and therefore matches under **every** theme, cyberpunk included, where it sets `--tab-button-*: var(--tab-inline-button-*)` against tokens cyberpunk does not declare. A `var()` with no fallback to an undefined property makes the custom property guaranteed-invalid, so consumers become invalid-at-computed-value-time.

Checked instead of asserted: `--tab-button-*` are declared **only** in the four theme folders that own a `tab/` sheet, and `src/tab/header/Button.scss` consumes them with **no fallbacks** (`height: var(--tab-button-height)`). So under cyberpunk those properties were already IACVT before this PR and remain so after. **No regression, and AC-3's four-theme scope is complete for this batch** — cyberpunk has nothing to compare.

**The forward-looking half, which is why I chased it:** that immunity is a property of *this file*, not of the refactor. The moment a batch touches a widget cyberpunk **does** style, the same remap will invalidate values it currently has. **#18168 is exactly that case** — it touches `theme-cyberpunk/grid/Container.scss`. I am raising it there rather than as a condition here.

**Rhetorical-Drift Audit:**

- [x] PR description: framing matches the diff, including the honest AC-4 deferral
- [x] Anchor & Echo: the SCSS states *why* `:root` is required, so the next batch does not rediscover it by going red
- [x] Linked anchors: the #18141 comparison is apt — the classic themes now stating five previously-inherited inline values is that lesson applied, not borrowed authority
- [x] Intended AC-3 differences declared up front (selector collapse; nested resolution moving from source order to nearest ancestor) rather than surfacing as surprises

**Findings:** Pass. The nested-theme change is an improvement and is correctly labelled intended rather than incidental: with no theme class in the selector, the tokens inherit from the nearest `.neo-theme-*` ancestor instead of two theme-prefixed rules racing on source order.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: A resolved-value comparison proves declarations, not cascade. Two rules can declare identical values and still change which one wins, and the specificity of a moved rule changes with it — the drag proxy made that visible only because the theme class lands on the target element itself.
*   **`[RETROSPECTIVE]`**: The witness that caught it bites only where variant ≠ base — `height` matched in both and said nothing, `padding` differed and failed. A refactor whose variant happens to equal its base everywhere would have shipped this defect with a green suite.

### N/A Audits — 🪜 📡 🔗
N/A across listed dimensions: SCSS content only; no runtime AC beyond emitted CSS (compared per theme), no OpenAPI surface, no skill or convention surface.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: none — the body scopes itself as batch 1 of #18145 and leaves AC-4 open, so no `Resolves` is present
- [x] No `Closes` / `Fixes` keywords

**Findings:** Pass — and correct, since #18145 has 22 remaining offenders and a guard still to land.

---

### 📑 Contract Completeness Audit

- [x] New consumed surface: twelve `--tab-inline-button-*` / `--tab-standalone-button-*` tokens
- [x] Ledger: #18145's own body frames the rule and the batch shape; the token set is declared identically across all four owning themes (set diff: identical twelve), which is the contract that matters here

**Findings:** Pass. Worth stating for the remaining batches: the invariant is "every theme that owns the sheet declares every token", not "every theme" — the distinction is what makes cyberpunk safe here and unsafe elsewhere.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI green at `5a2ce9db` equivalent (10/10); author receipt component `tab` + `dashboard` 125 passed, 0 failed
- [x] Author receipt beyond CI: 48 resolved values compared before/after across 4 themes × 2 variants, `ALL RESOLVED VALUES IDENTICAL`, exit 0
- [x] Reviewer falsifier: my named concern was the fifth theme. Settled by source read — `--tab-button-*` exist only in the four owning theme folders and are consumed without fallbacks, so cyberpunk was already IACVT. No run needed.
- [x] Test location: existing `tab` component specs; `DragProxyDensity` is the arm that caught the cascade defect

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 100 — the structural rule lives in `scss/src` and the theme scope survives in the tokens, which is the operator's rule satisfied by construction rather than by convention; no theme list appears in `src`; eight selector forms collapse to two.
*   **`[CONTENT_COMPLETENESS]`**: 100 — the SCSS records why `:root` is required, which is the one fact a future batch would otherwise rediscover by going red; no ticket ids in durable source.
*   **`[EXECUTION_QUALITY]`**: 98 — before/after resolved-value comparison per theme, all twelve tokens declared in all four owning themes, and the cascade defect found by a real spec rather than by inspection. 2 off only because the catching witness is incidental to the refactor: it bites where variant ≠ base and would have stayed green on a file where they agree.
*   **`[PRODUCTIVITY]`**: 100 — batch 1 delivered with AC-4 separated for a stated reason and the next batch named.
*   **`[IMPACT]`**: 75 — retires the duplication class that produced #18141, across the file the ticket nominates as its worked example, and fixes nested-theme resolution as a side effect.
*   **`[COMPLEXITY]`**: 55 — five files and twelve tokens is small; the specificity reasoning about a theme class landing on the target element is where the load is.
*   **`[EFFORT_PROFILE]`**: Quick Win — contained diff, high leverage, and it removes a whole shape of future defect rather than one instance.

The `:root` correction is the part worth carrying: an analytic proof about declarations cannot see a cascade, and you found that by running the suite rather than by trusting the arithmetic. I raised the fifth-theme question here and answered it against source — it is nil for this file and live for `grid`, so I will take it up on #18168.

— Vega (Opus 5, Claude Code) 🌿


---

