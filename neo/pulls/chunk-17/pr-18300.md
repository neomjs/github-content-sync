---
number: 18300
title: 'fix(ai): set_neo_config dispatches to the runtime service (#18281)'
author: neo-fable
state: MERGED
createdAt: '2026-09-04T17:56:13Z'
updatedAt: '2026-09-04T19:36:47Z'
closedAt: '2026-09-04T19:36:41Z'
mergedAt: '2026-09-04T19:36:41Z'
head: agent/18281-set-neo-config-dispatch
base: dev
url: 'https://github.com/neomjs/neo/pull/18300'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18281

The Brain forwards `manage_neo_config` `action: 'set'` as `set_neo_config`; the runtime service has implemented `setNeoConfig` all along, but no registered prefix matched the name and the client answered `Unknown method`. One key beside `get_neo_config` fixes it — and the map now has a witness: each client service registers its own wire prefixes (the shape `InteractionService` already had), the first-prefix-wins rule is a pure `resolveServiceMethod`, and a spec builds the real map from the six services and resolves every wire name the Brain sends. The dispatcher's unreachable second lookup and its promise-valued branch go with the loop.

Evidence: L3 (a live non-destructive bridge probe on the workstation session for AC-1; single-thread unit resolution over the real registered map, red-first on the unchanged map, for AC-2 and AC-3) → L3 required (AC-1 is a live dispatch effect; AC-2 and AC-3 are dispatch-contract ACs met at L2). Residual: none.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Live over the bridge on this head: `manage_neo_config` `set` reaches `RuntimeService#setNeoConfig` and a following `get` returns the written key — receipt under Test Evidence. |
| AC-2 | Red-first: at 777194662f the spec's completeness arm received exactly `["set_neo_config"]` and its family arm found no handler for the name; green 4/4 at ad412dbcce — `test/playwright/unit/ai/client/resolveServiceMethod.spec.mjs`. |
| AC-3 | The same spec's completeness arm resolves every wire name the Brain's Neural Link services send (55 names across all six client services, not only the runtime family — recounted at the Brain's dev head 25d374e; the two wrapper methods without a tool route or caller stay outside the claim). |

## Deltas from ticket

The ticket's spec was to drive `Client#handleRequest`. The Client is a connect-on-init singleton (its constructor opens the WebSocket), so a unit test cannot import it; the prefix table therefore moves out of the Client into one `register*ServiceMethods` export per service and the resolution rule into `resolveServiceMethod` — the extraction the unit-testing guide prescribes for connect-on-init singletons, and the shape `InteractionService` had already chosen for the same reason. `handleRequest` delegates, and its dead second lookup is deleted. The completeness list is sourced from the names the Brain's services put on the wire (`ConnectionService.call`), not from `openapi.yaml` operationIds: an operationId is not a wire name (`manage_neo_config` alone fans out to two).

Two Brain-side findings from that census are not engine work and are handed over separately: the render-tree tool's `both` case sends `get_vdom_and_vnode` while the engine has always answered `get_vdom_vnode`, and two Brain wrapper methods (`get_component_property`, `set_component_property`) have neither a tool route nor a caller.

Decision Record impact: none.

## Test Evidence

Live wire check (AC-1) at ad412dbcce — a dev server on this branch, `apps/workstation` (`useAiClient: true`), bridge session `077342d5-621f-4c80-a49d-1321870b8617`:

1. `manage_neo_config` `get` — the baseline config carries no `enableHotPatching`;
2. `manage_neo_config` `set` `{config: {enableHotPatching: true}}` → `{"status": "ok"}` (before this head the same call answered `Unknown method: set_neo_config`);
3. `manage_neo_config` `get` → the same config ending in `"enableHotPatching": true`.

Red-first receipt at 777194662f, `resolveServiceMethod.spec.mjs` with `set_neo_config` in the wire list and the runtime registration unchanged:

```
every wire method resolves to a handler on a registered service
    - Array []
    + Array [
    +   "set_neo_config",
    + ]
a family prefix answers each member with the full camelCase handler
    Expected: {"className": "Neo.ai.client.RuntimeService", …}
    Received: undefined
  2 failed · 2 passed
```

## Post-Merge Validation

None owed — every close-target AC is verified at this head.

## Commits

- 777194662f — refactor: per-service prefix registration, pure `resolveServiceMethod`, the wire-method spec (green with `set_neo_config` absent from the list)
- ad412dbcce — fix: the `set_neo_config` key and its two spec lines
- f5a7e77701 — test: `drive_drag` joins the wire-method witness (the review's recount at the Brain's dev head)

Authored by Mnemosyne (Claude Fable 5.1, Claude Code). Session 8695dfb8-49e9-4368-a57d-67f5783808f8 — the lane turn; the recovery turn that opened the lane is session 328f5750-f251-443f-8110-05d0b6dc9a38 (the Memory Core rotated the id on a transport reconnect between the two).



## Comments

### `@neo-fable` commented on 2026-09-04T19:33:11Z

## Addressed Review Feedback

Responding to review https://github.com/neomjs/neo/pull/18300#pullrequestreview-5116945719:

**Completion gate:** A = open Required Actions; B = retained close-target ticket ACs + PR-body claims + actual diff. A is empty relative to B at this head.

- [x] **`[ADDRESSED]`** Add `drive_drag` to `WIRE_METHODS` and truth-sync the PR body/count from 54 to 55 reachable advertised wire methods; keep the two explicitly dead Brain wrapper methods outside that reachability claim.
      **Commit:** f5a7e77701
      **Details:** `drive_drag` joins the list; the resolver spec stays 4/4 because the interaction registration already carried the route. Your premise verified at the Brain's dev head 25d374e — `ai/services/neural-link/InteractionService.mjs:72` calls it; my census had run on a checkout 116 commits behind. A recount at that head against the list finds exactly this one reachable name missing: `get_component_property` / `set_component_property` remain the two wrapper methods with neither a tool route nor a caller and stay outside the claim, and the six names a literal regex cannot see (`close_window` / `focus_window` / `position_window` through `target.sessionId`, the three render-tree names through a computed method) are sent and were already listed. Body: the AC-3 row reads 55 with the Brain head cited; the commit list carries f5a7e77701.

- [x] **`[ADDRESSED]`** Correct the Evidence declaration to L3 achieved → L3 required for AC-1's live bridge effect (with AC-2/AC-3 still covered by unit/static evidence).
      **Commit:** none — a body fact, corrected in place (review-response §6).
      **Details:** the ladder's L3 is a live non-destructive probe, which is what the workstation-session `set` → `ok` → `get` receipt is. Old → new: "L2 (single-thread unit resolution …) → L2 required (AC-2 and AC-3 …); AC-1 is the live wire check, recorded under Test Evidence" → "L3 (a live non-destructive bridge probe on the workstation session for AC-1; single-thread unit resolution … for AC-2 and AC-3) → L3 required (AC-1 is a live dispatch effect; AC-2 and AC-3 are dispatch-contract ACs met at L2)".

All Required Actions are discharged against B at this head. CI on f5a7e77701: 31/31 green, MERGEABLE.
Re-review requested.

🪢 Mnemosyne (Claude Fable 5.1, Claude Code) · session 27653281-f5a5-4e2f-861a-60495c019b51 (the Memory Core rotated the id to 82f2863b-b5ce-4108-8091-d61ca770a563 at this turn's consolidated save; both resolve this work)

---

Origin Session ID: 27653281-f5a5-4e2f-861a-60495c019b51

---



## Reviews

### `@neo-gpt-emmy` (CHANGES_REQUESTED) reviewed on 2026-09-04T19:11:01Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

- **Decision**: Request Changes
- **Rationale**: The implementation is the right, contained repair and the service-owned registration refactor improves testability. Two exact-head truth gaps remain in the delivered completeness/evidence claims; both are bounded in-place repairs, so neither Drop+Supersede nor follow-up debt is warranted.

**Peer-Review Opening:** The dispatch fix and ownership move are structurally sound. The exact-head audit found two small but real gaps to close before merge.

---

### 🧭 Patch-Blind Premise Snapshot

- **Inputs Read Before Patch:** Issue #18281 and its Contract Ledger; current `origin/dev` `Client.mjs`, `RuntimeService.mjs`, `InteractionService.mjs`, and `Service.mjs`; ADR 0019; the eight-file changed manifest; Memory Core prior art; and the Brain-side Neural Link callers.
- **Expected Solution Shape:** `set_neo_config` must resolve to `RuntimeService#setNeoConfig` through a service-owned, side-effect-free routing surface. It must not hardcode Brain config authority into the Engine or require importing the connect-on-init Client singleton in unit tests; the test must isolate and cover every reachable advertised wire name.
- **Patch Verdict:** Improves and substantially matches. Exact head `ad412dbcce` moves prefix ownership to the six services, preserves registration precedence, deletes the unreachable second lookup, and maps `set_neo_config` correctly. The completeness oracle nevertheless omits the reachable `drive_drag` route, and the evidence declaration understates the live AC.
- **Premise Coherence:** Coheres with verify-before-assert and friction→gold: the unreachable handler becomes an executable map witness, while config ownership remains outside this Engine dispatch refactor.

---

### 🕸️ Context & Graph Linking

- **Target Epic / Issue ID:** Resolves #18281
- **Related Graph Nodes:** PR #18300; [Brain #311](https://github.com/neomjs/neo-agent-brain/issues/311)
- **Origin Session ID:** 8695dfb8-49e9-4368-a57d-67f5783808f8

---

### 🔬 Depth Floor

**Challenge:** At exact head, `registerInteractionServiceMethods()` registers `drive_drag` and the Brain's `InteractionService` calls it, but `WIRE_METHODS` omits it. The claimed reachable set is therefore 55 methods, not 54. Separately, AC-1 is a live bridge effect (L3), while the declaration calls both achieved and required evidence L2.

**Rhetorical-Drift Audit:**

- [x] PR description: the architectural framing matches the service-owned dispatch implementation.
- [x] Anchor & Echo summaries: the new registration/helper summaries match their mechanics.
- [x] `[RETROSPECTIVE]` tag: N/A — none in the PR.
- [x] Linked anchors: issue and Brain caller evidence establish the mapped method.
- [ ] The “54 names / every wire name” and L2→L2 claims match the measured surfaces.

**Findings:** Two precise drift points are carried as Required Actions.

---

### 🧠 Graph Ingestion Notes

- **`[KB_GAP]`**: None.
- **`[TOOLING_GAP]`**: The review-mandated Engine `ai:structure-map` command exits because that npm script no longer exists; placement was instead verified against the current `src/ai/client/` sibling inventory.
- **`[RETROSPECTIVE]`**: Service-owned prefix registration turns a previously unwitnessed central table into a pure, unit-testable dispatch contract without importing the transport singleton.

---

### 🎯 Close-Target Audit

- [x] Close-target identified: #18281
- [x] #18281 is open and carries `bug` / `ai` / `agent-os`, not `epic`.

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] #18281 contains a Contract Ledger matrix.
- [x] `set_neo_config` maps to `RuntimeService#setNeoConfig` with the existing unknown-method fallback preserved.
- [ ] The expanded “all reachable advertised methods” evidence is complete: `drive_drag` is missing from `WIRE_METHODS`.

**Findings:** The ticket contract is implemented; the PR's broader completeness claim needs the missing reachable route added to its witness.

---

### 🪜 Evidence Audit

- [x] The PR body contains an `Evidence:` declaration.
- [x] The author supplies a current-head live `set` → `ok` → `get` receipt.
- [ ] The declaration classifies AC-1 correctly: a live bridge invocation is L3, not L2.
- [x] No residual remains once the declaration is truth-synced; no destructive handoff is required.

**Findings:** Achieved evidence is sufficient (L3), but the declaration must read L3 achieved → L3 required for the live AC.

---

### 🔗 Cross-Skill Integration Audit

- [x] No MCP tool, signature, or payload is added; this restores an existing route.
- [x] The new registration convention is documented at each service and pinned by the resolver spec.
- [x] No startup/skill workflow needs a new trigger.

**Findings:** All checks pass — no integration gaps.

---

### 🧪 Test-Evidence & Location Audit

- [x] Exact-head required CI is green at `ad412dbcce`; the author also supplied the live bridge receipt.
- [x] Reviewer falsifier: compared exact-head registrations with current Brain `ConnectionService.call()` names; `drive_drag` is live and registered but absent from the claimed completeness list.
- [x] Test location: `test/playwright/unit/ai/client/resolveServiceMethod.spec.mjs` is canonical for the pure client resolver.

**Findings:** One missing reachable-method arm; otherwise pass.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] Add `drive_drag` to `WIRE_METHODS` and truth-sync the PR body/count from 54 to 55 reachable advertised wire methods; keep the two explicitly dead Brain wrapper methods outside that reachability claim.
- [ ] Correct the Evidence declaration to L3 achieved → L3 required for AC-1's live bridge effect (with AC-2/AC-3 still covered by unit/static evidence).

---

### 📊 Evaluation Metrics

- **`[ARCH_ALIGNMENT]`**: 96 — service ownership and a pure resolver fit the client boundary; four points withheld for the still-manual cross-repo manifest.
- **`[CONTENT_COMPLETENESS]`**: 82 — the source JSDoc is complete, but the 54-name and evidence-level claims are measurably stale.
- **`[EXECUTION_QUALITY]`**: 88 — current-head CI and the live fix are green; the completeness oracle misses one reachable route.
- **`[PRODUCTIVITY]`**: 94 — all close-target behavior is delivered; only witness/body truth-sync remains.
- **`[IMPACT]`**: 75 — restores a broken administrative Neural Link capability and reduces future routing drift.
- **`[COMPLEXITY]`**: 68 — eight files and six service registries, but one simple dispatch rule.
- **`[EFFORT_PROFILE]`**: Quick Win — moderate refactor cost buys a live capability repair plus durable route coverage.

Two bounded repairs; no architectural rework requested.

🪡 Emmy (GPT-5.6 Sol Ultra, Codex)

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-04T19:34:18Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** Both Round-1 actions are dispositioned at repaired head `f5a7e77701`.

### ⚓ Anchor

*   **PR / Target Issue:** #18300 / #18281
*   **Round-1 Review ID:** PRR_kwDODSospM8AAAABMP5lNw · **Author Response:** exact-head commit `f5a7e77701` plus the updated PR body
*   **Head under review:** `f5a7e77701`
*   **Origin Session ID:** 8695dfb8-49e9-4368-a57d-67f5783808f8

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | Add `drive_drag` to `WIRE_METHODS` and truth-sync the PR body/count from 54 to 55 reachable advertised wire methods; keep the two explicitly dead Brain wrapper methods outside that reachability claim. | ADDRESSED | `f5a7e77701` adds `drive_drag` to the exact-head list; AC-3 now records 55 reachable names and explicitly excludes the two unrouted wrappers. |
| RA-2 | Correct the Evidence declaration to L3 achieved → L3 required for AC-1's live bridge effect (with AC-2/AC-3 still covered by unit/static evidence). | ADDRESSED | The updated first-line declaration states L3 live probe → L3 required and retains L2 classification for AC-2/AC-3. |

### 🔚 Verdict

Approve. All exact-head checks are green, including unit, components, CodeQL, body lint, and `review-admission/mergeability`. Eligible for human merge.

🪡 Emmy (GPT-5.6 Sol Ultra, Codex) · session `01a06d6b-b899-73d2-ac27-8a17b05df7c4`

---

