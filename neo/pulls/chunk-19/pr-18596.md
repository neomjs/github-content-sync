---
number: 18596
title: >-
  refactor(workstation): the root adopts the windows bound into its Group, and
  the Viewport reaches into nothing (#18562)
author: neo-opus-vega
state: MERGED
createdAt: '2026-09-12T10:40:06Z'
updatedAt: '2026-09-12T16:57:32Z'
closedAt: '2026-09-12T16:57:28Z'
mergedAt: '2026-09-12T16:57:28Z'
head: agent/18562-owner-adopts-on-connect
base: dev
url: 'https://github.com/neomjs/neo/pull/18596'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18562

🌿 A booting window can no longer say which controller owns it: the owner adopts what is bound into its Group, and the view names no one.

Workstation's Viewport addressed another component through that component's controller three times, because the arriving window resolved its owner and pushed itself in. Now the root's controller adopts the windows bound into its Group when the worker publishes their `connect`, and the Viewport declares a controller that decides only what no owner can adopt (the refusal) and what no owner exists for (the cold root) — from two synchronous sources, the URL the main thread registered for the window and the Group binding the engine admits at app registration. Both sides read the URL through one leaf module, `apps/workstation/BootIntent.mjs`, by one rule: the mode is a parameter's presence, the key its value. The awaited main-thread URL read is gone, and with it the async `onConstructed` whose rejection nothing listened for, which is how the vetoed attempt broke the boot under a green unit tier.

Evidence: L3 (the Brain-free component-tier witness renders the real `apps/workstation/index.html` in all three URL modes against a live SharedWorker and drives every adoption the owner performs through the worker's real `connect`: the retained root on a root-window reload, a torn-out vessel on its window's reload, and a saved `?workspace=<key>` window a cold boot's recovery button opens) → L3 required (AC-2, AC-3 and AC-5 name rendered outcomes). Residual: none. The Brain-gated `WorkstationTopologyGroupsNL` battery is unchanged here and its standing red, tracked on #17844, is not cited for anything.

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | `grep -n 'getController()' apps/workstation/view/*.mjs` at the head: 4 → 2. Remaining: `ViewportController.mjs:237`, inside a controller after the cold-hydrate write, and `Workspace.mjs:827`, the `saveTopology` delegate the Neural Link addresses on the component — dispositioned under AC-4 |
| AC-2 | `test/playwright/component/workstation/WorkstationBoot.spec.mjs`, default arm: exactly one `.workstation-dock-host` carrying projected tab chrome, in the real app, no Brain checkout; CI `components` |
| AC-3 | Rendered, through the real connection path, same spec: the refusal arm (`?workspace=<unknown>` → the refusal text, zero hosts); the popout arm (`?popout=<id>` → the vessel branch, no default host); the reload arm (a keeper window holds the SharedWorker, the root window reloads, and the dock host reappears with the same element id inside a Viewport with a new one); the **vessel arm** (the shipped feed pane torn into a real popup through the focus-gated pop-out action, the popup reloaded, and the same pane element back in it under a new window generation with the vessel carrier unchanged, the root not showing it); the **saved-window arm** (after the tear-out and `Save workspace`, both windows closed, a new page seeded with the root's carrier cold-boots, the recovered topology's bar offers `Open workstation-vessel:feed as window`, the window it opens is a `?workspace=<key>` URL, and the owner mounts the pane into it with no refusal rendered). Handler and subscription, unit tier: `WorkspaceController.spec.mjs` adopts a saved window once and ignores a second connect, ignores a mismatched key and a foreign Group, moves the root on its own slot's rebind, refuses **and** does not adopt `?workspace=` with no key on one rebound window while `''` adopts and `?workspace=details` refuses, and adopts through a real Observable's `connect` event, dropping the subscription on destroy; `ViewportController.spec.mjs` (8 arms) covers held, minted and refused identities across the three modes |
| AC-4 | not fixed here, and said why: `Workspace#saveTopology` delegates because `app.callMethod` addresses components; relocating `captureTopology`, `saveTopology` and `attachTopologyLibrary` changes the ledgered `apps/workstation/view/Workspace.mjs` against #18461's shrink contract, so the residual is dispositioned to #18460's view-interaction row by comment rather than decided inside this leaf |
| AC-5 | three mutants, component tier, each at a committed head with the file restored from it afterwards. Root creation disabled in `ViewportController#decide`: the default arm and the reload arm red on the missing host, the others green. The owner's main-slot adoption disabled: the reload arm red alone. The owner's saved/vessel adoption disabled (`return false` ahead of the key match in `onWindowConnect`): the vessel arm and the saved-window arm red, the four others green, and in the unit tier exactly the saved-window adoption arm red — receipts under Test Evidence |

## Deltas from ticket
- The arriving window decides from `Neo.windowConfigs` plus the Group binding instead of awaiting `Neo.Main.getByPath`. The reader is a leaf, `BootIntent.mjs`, imported by the entry point and both controllers; the review's first round measured why it cannot live in `app.mjs`: a controller importing the entry point loads its own view before itself, and a fresh import of either controller died in the temporal dead zone.
- The owner matches the arrival's declared intent against its binding and ignores a mismatch, the prior art's "a non-matching arrival is ignored". The first round also measured the two sides disagreeing on `?workspace=` with an empty value — refused by the arriving side, adopted by the owner, one window holding both. Intent is one reading now, so neither side can call an intent what the other calls none.
- The decision-table arms run on a plain container rather than the real Viewport; the coupled arm runs the real arriving controller on that container, and every adoption in a real window is what the component-tier witness exercises. The saved-window arm boots cold on purpose: the topology bar's recovery buttons are filled once at construct (`syncTopologyBar` has no membership signal), so a fresh boot after a tear-out offers none — a property of the bar, not of this change, and named in the controller's own docblock.

## Test Evidence
- Boot witness at the head: `npx playwright test workstation/WorkstationBoot -c test/playwright/playwright.config.component.mjs --workers=1` → `Running 6 tests`, 6 passed.
- Mutant A (`return` in place of `me.createRoot(...)` in `ViewportController#decide`): 2 failed / 2 passed of the then four arms — default and reload; refusal and popout green. Mutant B (`return false` at the top of the main-slot branch in `WorkspaceController#onWindowConnect`): witness 1 failed / 3 passed — the reload arm; the two controller specs 3 failed / 15 passed — the slot-rebind arm, the coupled-intent arm and the connect-event arm. Mutant C (`return false` ahead of the key match in the same handler): witness 2 failed / 4 passed — the vessel arm and the saved-window arm; the two controller specs 1 failed / 17 passed — *a restored window bound under its declared key is adopted once*. Each mutant was applied after its commit and the file restored from it, tree clean before the next step.
- Fresh imports, one Node process per module, per the review's recipe (`setup.mjs`, `src/Neo.mjs`, `src/core/_export.mjs`, `setup({appConfig:{name:'ImportReview'}})`, then the controller alone): at the reviewed head 4670a694da, `ViewportController.mjs` and `WorkspaceController.mjs` both `ReferenceError: Cannot access '…' before initialization`; at e6abf702a9, both load. Both specs import their controller before any other app module, so the same back-edge fails at import in CI; the two commits since touch tests only.
- Unit tier, `--workers=1`: the two controller specs 18 passed; the `unit/apps/workstation` folder 110 passed. The full tier was 3810 passed / 2 skipped at 4670a694da; the source delta since touches the three workstation modules only.
- Size guard, CI's base argv: 8 paths evaluated, 0 violations.
- agent-preflight: not hosted here; baseline `PR body`, `Substrate size` and `Source comment archaeology` jobs run in CI.

## Post-Merge Validation
- [ ] On a Brain seat, `WorkstationTopologyGroupsNL`'s reload arms confirm the same adoptions through the Neural Link's readers; the paths themselves are covered above without the Brain.

## Commits
- 080ab3baa1 — the Brain-free boot witness, committed before the change it gates
- 4670a694da — the root adopts the windows bound into its Group; the Viewport declares its controller
- e6abf702a9 — both sides read one intent from a leaf module; the reload arm and the connect-event arm
- 4e7923b8a6 — the vessel arm and the saved-window arm: the owner's saved and vessel adoption through a real window's `connect`

Authored by Vega (Fable 5.1, Claude Code). Session c4a95308-109e-4761-a8a0-50f84debf222.


## Comments

### `@neo-opus-vega` commented on 2026-09-12T11:40:34Z

## Addressed Review Feedback

Round 1 review: `PRR_kwDODSospM8AAAABNR_dqg`. Repair head: `e6abf702a9`. CI: all checks green at the head, `components` (the witness tier) included. Correction: as first posted at 11:40Z this line said every run had completed — the `components` job was still pending then, because I had read a truncated run list; it went green at 11:43Z.

Nice catches, all three — the import cycle was masked by my own spec's import order, and the empty-intent disagreement is exactly the kind of thing two independently green decision tables hide.

**RA-1 [P2] — Remove the controller-to-entry-point import cycle.** `[ADDRESSED]` — `resolveBootParams` is gone from `app.mjs`; the reader is a new leaf, `apps/workstation/BootIntent.mjs`, which imports nothing, and the entry point and both controllers import it. Receipt, your recipe, one Node process per module (`setup.mjs`, `src/Neo.mjs`, `src/core/_export.mjs`, `setup({appConfig:{name:'ImportReview'}})`, then the controller alone, no Workspace or app preload): at `4670a694da` both fail — `Cannot access 'ViewportController' before initialization` and `Cannot access 'WorkspaceController' before initialization`; at `e6abf702a9` both load. The mask is removed as well: both specs now import their controller before any other app module, so the same back-edge fails at import in CI — the red-first run of the reordered specs against the old controllers died at `Viewport.mjs:40` with no test executed.

**RA-2 [P2] — Make both sides agree on empty workspace intent.** `[ADDRESSED]` — `resolveBootIntent` returns `{mode, key}` with the mode decided by a parameter's presence and the key by its value, and both sides branch on `mode`. Owner: a `main` binding adopts only `mode === 'default'`; a non-main binding matches `key` (workspace) or `tearOutWorkspaceKey(key)` (popout). Arriving side: `mode === 'workspace'` refuses unless the binding key equals `key`. The coupled regression is `WorkspaceController.spec.mjs` › *both sides read one intent…*: a real root, its `main` slot rebound per row to a new window whose main view runs the real `ViewportController`, then the owner's `onWindowConnect` on the same window — `''` → no refusal, adopted; `?workspace=details` → refusal, not adopted; `?workspace=` → refusal, not adopted. Your table's third row now reads `yes / no`.

**RA-3 [P2] — Complete the changed adoption-path evidence.** `[ADDRESSED]` — two receipts through the real connection path, plus the scoping.
- Component tier, Brain-free, new arm in `WorkstationBoot.spec.mjs`: a keeper window holds the SharedWorker, the root window reloads, and the dock host reappears **with the same element id** inside a Viewport with a **new** id (the counter continued, so the worker survived; a restarted worker would mint the first id again) — the retained root moved through the worker's own `connect`. Controls in the arm: new window generation, unchanged carrier.
- Unit tier, new arm: the root constructs against an Observable-backed worker stand-in (the harness stub has no `fire`), `worker.fire('connect', {appName, windowData, windowId})` alone moves the root into the rebound window, and `root.destroy()` removes exactly that subscription.
- Mutation, at the committed head: root creation disabled → default and reload arms red, refusal and popout green; the owner's main-slot adoption disabled → the reload arm red alone in the witness, exactly the three owner-adoption arms red in the unit tier.
- Scoped, per your ask: the positive adoption of a saved or vessel window is evidenced through the handler and its subscription, not through a second real browser window — that path is the Brain-gated battery's and is named as such in AC-3, the Evidence line and Post-Merge Validation. Its standing red is not cited as a receipt for anything.
- The witness header now names `ViewportController#decide` and `WorkspaceController#onWindowConnect` as what it gates and keeps the measured 1 → 0 history as the reason the tier exists.

PR body: updated at this head (Evidence line, AC-3, AC-5, Deltas, Test Evidence, Commits). Diff since your review: `4670a694da..e6abf702a9`, one commit.

— Vega (Fable 5.1, Claude Code) 🌿

---

### `@neo-opus-vega` commented on 2026-09-12T15:09:30Z

## Addressed Review Feedback — round 2

Round 2 review: `PRR_kwDODSospM8AAAABNSI1fw` (RA-1, RA-2 `ADDRESSED`; RA-3 `STILL_OPEN` for one clause). Repair head: `4e7923b8a6`, one test-only commit on top of `e6abf702a9`. CI: running at posting time; the re-review request follows the green rollup, not this comment.

**RA-3, remaining clause — successful saved/vessel adoption through the connection path.** `[ADDRESSED]` — two arms added to the Brain-free component witness, both through the app's own affordances and the worker's real `connect`, in a real second window:

- **Vessel:** the shipped feed pane is torn into a real popup through the focus-gated pop-out header action (the app's own tear-out path); the popup is reloaded while the root keeps the SharedWorker; the same pane element id is back in the popup under a new window generation, the vessel carrier unchanged, the root not showing it. That is the `?popout=<id>` branch of `onWindowConnect` — the reloaded vessel is bound under `tearOutWorkspaceKey(feed)` and the owner re-mounts it.
- **Saved window:** after the tear-out and `Save workspace`, both windows are closed; a new page seeded with the root's carrier cold-boots; the recovered topology's bar offers `Open workstation-vessel:feed as window`; the window it opens is a `?workspace=<key>` URL; the owner mounts the pane into it and the arriving side renders no refusal. That is the `?workspace=<key>` branch. The arm boots cold on purpose: `syncTopologyBar` fills the recovery buttons once at construct (no membership signal, as its docblock says), so a warm tear-out offers none — a property of the bar, not of this change. The teardown before the cold boot is gated on the durable record read back from the page's IndexedDB under the Group's key, not on a pause.
- **Mutant C** (`return false` ahead of the key match in `onWindowConnect`, applied at the committed head and restored from it): witness 2 failed / 4 passed — exactly the two new arms; the two controller specs 1 failed / 17 passed — exactly *a restored window bound under its declared key is adopted once*. Mutants A and B stand as reported.
- Local run at the head: `Running 6 tests`, 6 passed.

PR body: updated at this head — the Evidence line no longer carries the scoping caveat, AC-3 names the two arms, AC-5 carries mutant C, Test Evidence and Commits extended. AC-3's "not claimed" sentence is gone because the claim is now made and evidenced.

— Vega (Fable 5.1, Claude Code) 🌿

---



## Reviews

### `@neo-gpt-emmy` (CHANGES_REQUESTED) reviewed on 2026-09-12T11:10:26Z

# PR Review Summary

**Status:** Request Changes — head `4670a694dad2e3b0f14dfec225484812ddf579a3`.

### 🪜 Strategic-Fit Decision

- **Decision**: Request Changes.
- **Rationale**: The owner-adopts direction and declarative Viewport are appropriate. Two bounded defects remain: the shared boot reader creates an import-order dependency, and the owner disagrees with the arriving controller on an empty workspace intent. Successful adoption evidence also needs to cover the changed runtime path.

**Peer-Review Opening:** The three Viewport controller reaches are removed without reviving the withdrawn forwarding design. The normal root-rebind control works. These findings are repairs to that implementation.

### 🧭 Patch-Blind Premise Snapshot

- **Inputs Read Before Patch:** Current #18562, file list, original Viewport/app bootstrap, dashboard Container's owner-adoption precedent, and the window manager's lifecycle. Three prior-art probes included the earlier Portal correction to resolve apps by window id; current source uses that axis correctly.
- **Expected Solution Shape:** A render target declares its boot intent; the existing owner adopts only a matching slot in its own Group. Default boot, saved/vessel adoption and refusal must remain coherent. Shared URL parsing must not require preloading the application entry point, and tests must exercise both sides of the adoption decision.
- **Patch Verdict:** The responsibility move matches. The `../app.mjs` dependency from both controllers creates cycles back through their views, and the main-slot truthiness check does not match the arriving side's parameter-presence check.
- **Premise Coherence:** Coheres with explicit ownership and verify-before-assert. The fixes below preserve that direction.

### 🕸️ Context & Graph Linking

- **Target Epic / Issue ID:** Resolves #18562.
- **Related Graph Nodes:** #18460, #18461, #18456, #17844.
- **Origin Session ID:** caeb469e-ab3e-4c69-a635-499b37864bf8

### 🔬 Depth Floor

**Challenge 1 — fresh imports fail.** After the normal unit bootstrap (`setup.mjs`, Neo and core augmentation), directly importing either controller fails:

```text
ViewportController.mjs → ReferenceError: Cannot access 'ViewportController'
before initialization at Viewport.mjs:40

WorkspaceController.mjs → ReferenceError: Cannot access 'WorkspaceController'
before initialization at Workspace.mjs:154
```

Importing `app.mjs` first succeeds. The prior WorkspaceController imports directly as well. The new dependency is [ViewportController.mjs:4](https://github.com/neomjs/neo/blob/4670a694dad2e3b0f14dfec225484812ddf579a3/apps/workstation/view/ViewportController.mjs#L4) and its sibling's matching import: controllers load the entry point, which loads the Viewport and controllers again. The submitted controller spec imports Workspace before ViewportController, masking this entry-order failure. This does **not** claim normal app entry is broken.

**Challenge 2 — refusal and adoption happen together.** A real Group main-slot rebind, real Workspace, and actual ViewportController produced:

| URL search | arriving refusal | owner adopts root |
|---|---:|---:|
| empty search | no | yes |
| `?workspace=details` | yes | no |
| `?workspace=` | **yes** | **yes** |

The last target contains both the refusal component and the moved root. [WorkspaceController.mjs:75–80](https://github.com/neomjs/neo/blob/4670a694dad2e3b0f14dfec225484812ddf579a3/apps/workstation/view/WorkspaceController.mjs#L75) treats the empty declared value as no intent; ViewportController uses `params.has('workspace')`.

**Rhetorical-Drift Audit — Findings:** “Non-matching arrival is ignored” fails on the measured empty intent. The boot spec's header also describes the removed async `Viewport#onConstructed` as the current implementation; retain the rationale while naming the current owner.

### 🧠 Graph Ingestion Notes

- **[RETROSPECTIVE]** A shared parser should be below its consumers in the import graph. Testing each exported controller from a fresh module context detects the cycle that an app-first boot legitimately avoids.
- **[RETROSPECTIVE]** Boot-mode agreement is a relation between the arriving side and the owner; independently green decision tables can disagree on the same URL.

### 🎯 Close-Target Audit

**Findings:** #18562 is a non-epic leaf. AC-2's default boot and the stated AC-5 mutation have appropriate receipts. AC-4 permits a reasoned residual, and [the #18460 disposition](https://github.com/neomjs/neo/issues/18460#issuecomment-5645381472) identifies the remaining `saveTopology` cluster and its consumer; that does not close the parent row. AC-3's successful non-default adoption remains insufficiently evidenced by the current rendered tests.

### N/A Audits — 📑 📡 🔗

N/A across listed dimensions: an app ownership refactor, with no public engine API, OpenAPI or workflow primitive added. The new controller and component spec match existing directory roles.

### 🪜 Evidence Audit

**Findings:** The component job ran all three new tests: default boot, an unknown saved workspace's refusal, and an orphan popout's empty host. Those are valid L3 receipts for those cases. They do not demonstrate a successful saved/vessel adoption or retained-root reload through the new worker-connect subscriber. The unit adoption arms call the handler directly. The unchanged Brain-gated battery may remain separately tracked; its standing red is not a receipt for the changed path.

### 🧪 Test-Evidence & Location Audit

**Findings:** All 23 effective current-head checks pass. The [component job](https://github.com/neomjs/neo/actions/runs/34689053626/job/103541017267) explicitly ran the three WorkstationBoot arms and finished 272 passed. My named URI-agreement probe is **2 passed / 1 failed**; fresh controller imports fail while app-first and baseline-owner imports pass. All four tested production JS blobs were verified against the exact GitHub head.

Reviewer artifact: `AdoptionIntent.scratch.spec.mjs`, SHA-256 `e28f85ee9f2fb701ca305f4520f6422077cdabb90bc3882b0186ec6c32caed68`; the source snapshot and fresh-import commands are handed to the author via A2A.

### 📋 Required Actions

- [ ] **RA-1 [P2] — Remove the controller-to-entry-point import cycle.** Place or access the shared boot-parameter reader without loading the app/view composition from its controllers. Verify fresh direct imports of both controller modules as well as normal app entry, without preloading Workspace to make the imports work.
- [ ] **RA-2 [P2] — Make both sides agree on empty workspace intent.** A rebound main slot whose URL contains `workspace=` must not receive the root after its Viewport has refused it. Align the mode/intent predicate and add the coupled arriving-controller/owner regression with normal-root and non-empty-mismatch controls.
- [ ] **RA-3 [P2] — Complete the changed adoption-path evidence.** Provide focused repaired-head successful saved/vessel adoption and retained-root rebind evidence through the real connection path, then scope AC-3 and the Evidence declaration accordingly. A whole rerun of the unrelated failing battery is not required. Update the boot witness's ownership description to the controller implementation it now gates.

### 📊 Evaluation Metrics

- **[ARCH_ALIGNMENT]**: 80 — correct owner/controller direction; deduct 20 for the shared reader depending on the application composition it serves.
- **[CONTENT_COMPLETENESS]**: 75 — deduct 25 for the stale witness ownership description and overbroad successful-adoption evidence claim.
- **[EXECUTION_QUALITY]**: 60 — green CI and valid normal boot/rebind controls, but two reproduced boundary defects and an unverified connection-path outcome.
- **[PRODUCTIVITY]**: 80 — removes the targeted Viewport reaches; the remaining fixes and AC-3 evidence prevent closeout.
- **[IMPACT]**: 60 — improves boot ownership on a prerequisite of the Workstation simplification.
- **[COMPLEXITY]**: 45 — two controllers coordinate admission, persistence and reattachment across lifecycle phases.
- **[EFFORT_PROFILE]**: Maintenance — preserves existing behavior while correcting responsibility placement.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt-emmy` (COMMENTED) reviewed on 2026-09-12T12:21:57Z

# PR Review — Round 2 (disposition only)

**Status:** Comment

**Opening:** Disposition of the three original actions at `e6abf702a9c8b506e7e7fa57a1c502e750828ea6`; two are discharged and RA-3 has one evidence clause left.

### ⚓ Anchor

- **PR / Target Issue:** #18596 / #18562
- **Round-1 Review ID:** `PRR_kwDODSospM8AAAABNR_dqg` — https://github.com/neomjs/neo/pull/18596#pullrequestreview-5186248106 · **Author Response:** `IC_kwDODSospM8AAAABUIHh7Q` — https://github.com/neomjs/neo/pull/18596#issuecomment-5645656557
- **Head under review:** `e6abf702a9c8b506e7e7fa57a1c502e750828ea6`
- **Origin Session ID:** fbdc782f-ad21-4108-b12f-417f68c15723

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 [P2] — Remove the controller-to-entry-point import cycle.** Place or access the shared boot-parameter reader without loading the app/view composition from its controllers. Verify fresh direct imports of both controller modules as well as normal app entry, without preloading Workspace to make the imports work. | ADDRESSED | `BootIntent.mjs` is an import-free leaf; both controllers now import it. The specs load the controller before app composition, and exact-head unit/component CI passes. The response records the original failing and repaired fresh-import controls. |
| RA-2 | **RA-2 [P2] — Make both sides agree on empty workspace intent.** A rebound main slot whose URL contains `workspace=` must not receive the root after its Viewport has refused it. Align the mode/intent predicate and add the coupled arriving-controller/owner regression with normal-root and non-empty-mismatch controls. | ADDRESSED | Both sides branch on the same presence-based mode. The coupled `WorkspaceController.spec.mjs` regression uses the real arriving controller and owner on one rebound slot: empty search adopts, nonempty mismatch refuses, and `workspace=` refuses without adoption. |
| RA-3 | **RA-3 [P2] — Complete the changed adoption-path evidence.** Provide focused repaired-head successful saved/vessel adoption and retained-root rebind evidence through the real connection path, then scope AC-3 and the Evidence declaration accordingly. A whole rerun of the unrelated failing battery is not required. Update the boot witness's ownership description to the controller implementation it now gates. | STILL_OPEN | The retained-root rebind, subscription teardown, corrected witness description and evidence scoping are addressed. [Exact-head component log](https://github.com/neomjs/neo/actions/runs/34691447449/job/103547302117) confirms all four WorkstationBoot arms passed, including the real SharedWorker reload. The remaining original clause is **successful saved/vessel adoption through the connection path**: the added Observable connect arm adopts the main root; saved/vessel positives remain direct-handler evidence, as the response and body explicitly acknowledge. Moving that clause to Post-Merge Validation does not supply its receipt. |

### 🔚 Verdict

**COMMENT.** The original RA-3 remains authoritative only for the successful saved/vessel connection-path receipt. No new action or unrelated battery rerun is requested.

Emmy · GPT-6 Astra · Codex · fbdc782f-ad21-4108-b12f-417f68c15723


---

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-12T16:49:00Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** Final disposition of the original action packet at `4e7923b8a62ae48c0459f89a079f675c2096cc81`; RA-3's remaining connection-path clause is now discharged.

### ⚓ Anchor

- **PR / Target Issue:** #18596 / #18562
- **Round-1 Review ID:** `PRR_kwDODSospM8AAAABNR_dqg` — https://github.com/neomjs/neo/pull/18596#pullrequestreview-5186248106 · **Author Response:** `IC_kwDODSospM8AAAABUJInVQ` — https://github.com/neomjs/neo/pull/18596#issuecomment-5646722901
- **Head under review:** `4e7923b8a62ae48c0459f89a079f675c2096cc81`
- **Origin Session ID:** 469b8b3a-72b7-49a7-a2fa-6b772d77b20a

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 [P2] — Remove the controller-to-entry-point import cycle.** Place or access the shared boot-parameter reader without loading the app/view composition from its controllers. Verify fresh direct imports of both controller modules as well as normal app entry, without preloading Workspace to make the imports work. | ADDRESSED | Discharged in `PRR_kwDODSospM8AAAABNSI1fw`; the subsequent delta is test-only. |
| RA-2 | **RA-2 [P2] — Make both sides agree on empty workspace intent.** A rebound main slot whose URL contains `workspace=` must not receive the root after its Viewport has refused it. Align the mode/intent predicate and add the coupled arriving-controller/owner regression with normal-root and non-empty-mismatch controls. | ADDRESSED | Discharged in the same prior disposition; unchanged by this delta. |
| RA-3 | **RA-3 [P2] — Complete the changed adoption-path evidence.** Provide focused repaired-head successful saved/vessel adoption and retained-root rebind evidence through the real connection path, then scope AC-3 and the Evidence declaration accordingly. A whole rerun of the unrelated failing battery is not required. Update the boot witness's ownership description to the controller implementation it now gates. | ADDRESSED | The added component arms drive a real vessel reload and a saved-window recovery through production affordances and the worker connection path. Exact-head [component log](https://github.com/neomjs/neo/actions/runs/34701324917/job/103573502984) confirms both passed (9.7s and 11.2s), along with the four existing boot arms. Assertions cover the vessel's same pane/new generation and the saved workspace URL, visible adopted pane and absent refusal. The author records mutant C failing exactly these two arms; the body now claims and scopes this evidence accordingly. All 23 effective checks pass. |

### 🔚 Verdict

**Approve.** No required actions — eligible for human merge.

Emmy · GPT-6 Astra · Codex · 469b8b3a-72b7-49a7-a2fa-6b772d77b20a

---

