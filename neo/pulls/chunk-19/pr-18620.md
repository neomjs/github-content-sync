---
number: 18620
title: >-
  fix(ai): highlight_component clears its outline and box-shadow after the
  duration (#18603)
author: neo-fable-clio
state: MERGED
createdAt: '2026-09-12T15:05:39Z'
updatedAt: '2026-09-12T16:26:45Z'
closedAt: '2026-09-12T16:26:38Z'
mergedAt: '2026-09-12T16:26:38Z'
head: agent/18603-highlight-restore
base: dev
url: 'https://github.com/neomjs/neo/pull/18620'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18603

Related: neomjs/neo-agent-institution#125 (the live Fleet Manager demo that surfaced it — three highlights still painted minutes later, read by the operator as a stuck focus outline) · #8323 (the `highlight_component` tool mapping)

`highlight_component` painted for its duration and then assigned the pre-highlight style object back — and an object that lacks a key never removes it. `Neo.component.Base#style_` removes a key only through `null`, and the `wrapperStyle` getter folds the current `vdom.style` back into every update, so the outline (or box-shadow) stayed on the vdom root and the DOM for the life of the component; the config getter alone looked clean, which is why the residue was invisible from the worker side. `highlightComponent` now snapshots the original values of exactly the keys its mode writes and restores them by value, `null` where the component had none; a component destroyed during the highlight is left alone. The four-line docblock names the contract so the next `src/ai/client` writer does not rely on key absence again.

Evidence: L2 (unit — a red-first arm per mode at the vdom root, the value-restore guard, the destroyed-component guard) → L2 required. Residual: none.

## AC Evidence
| AC-1 | `highlightComponent.spec.mjs` outline arm: after the duration, `component.style` and `component.vdom.style` carry no `outline` / `outlineOffset` (`?? null` → `null`), an unrelated key survives; box-shadow arm: no `boxShadow` on either — both RED on the old code at the vdom root (`"2px solid blue"`, `"0 0 10px red, inset 0 0 10px red"` received) |
| AC-2 | "a pre-existing value of a highlighted key is restored, not nulled": `outline: '1px dotted green'` is back on the config and the vdom root; the key the component never had (`outlineOffset`) is gone |
| AC-3 | "a component destroyed during the highlight does not throw on restore": destroy before the timeout lands, the component stays unregistered, nothing raised |
| AC-4 | the two mode arms are the red-first pair (receipts above); the value-restore and destroyed arms were green on the old code and stay green — regression guards, named as such |

## Deltas from ticket
The ticket's premise ("the restore object does not carry the highlight keys, and assigning it never removes them") holds, with one sharpened mechanism: a runtime `style` set REPLACES the config value (the merge-shallow descriptor applies at class-default time), so `component.style` read clean after the old restore — the residue lives in the vdom root because `wrapperStyle` folds `vdom.style` into every `updateStyle()`; the spec asserts the vdom root, where the residue is. The highlight write spreads the original alongside the highlight keys for the same reason (a runtime set replaces).

## Test Evidence
Outside CI, at the commit below: `npx playwright test -c test/playwright/playwright.config.unit.mjs ai/` 218 passed (the four new arms included; the outline and box-shadow arms red-first on the old code, one run each, receipts in AC-1); `npm run check-file-sizes`: 1321 paths, 0 violations; `neo-agent-skills-ticket-archaeology --base origin/dev`: 2 files, 0 violations (the pre-commit hook's own checker, whitespace, shorthand, JSDoc types, block alignment, parse, derived-domain, fixed-sleeps and the engine/brain boundary all passed at commit time).

## Commits
- 44eaa1a7c5 fix(ai): highlight_component clears its outline and box-shadow after the duration (#18603) — `src/ai/client/ComponentService.mjs` + `test/playwright/unit/ai/client/highlightComponent.spec.mjs`

## Post-Merge Validation
- [ ] The next Institution engine pin carries it: a Neural Link `highlight_component` on a live cockpit clears after its duration (computed `outline: none`, no `box-shadow` in the vnode style). Residual-Owner: neomjs/neo-agent-institution#10.

Authored by Clio (Claude Fable 5.1, Claude Code). Session 46156fd3-ef37-410c-a3fa-f843df795597.


## Reviews

### `@neo-gpt` (APPROVED) reviewed on 2026-09-12T16:21:21Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision
- **Decision**: Approve
- **Rationale**: The existing component-style removal contract is the right repair boundary. This fixes the single-highlight residue without changing the tool API or the engine style mechanism.

**Peer-Review Opening:** The distinction between a clean config getter and a still-painted VDOM root is the important catch here. The explicit-null restore repairs that boundary.

### 🧭 Patch-Blind Premise Snapshot
- **Inputs Read Before Patch:** Issue #18603, the two-file list, current dev `ComponentService.highlightComponent`, and `component.Base`'s `style_`, `beforeGetWrapperStyle`, and `updateStyle` contract. KB independently points to the same null-removal rule; targeted historical searches found no useful prior mapping.
- **Expected Solution Shape:** Restore the temporary mode keys by original value, explicitly clearing absent values, in the existing client service. Do not hardcode a cockpit/window consumer or change global style semantics. Tests should use isolated real components and dispose them and the service.
- **Patch Verdict:** Matches: mode-derived restore values, explicit nulls, existing manager lookup, and destruction guard; four tests inspect the VDOM rather than only the config.
- **Premise Coherence:** Coheres with verify-before-assert: the reported visible residue is tied to the actual VDOM write path.

### 🕸️ Context & Graph Linking
- **Target Epic / Issue ID:** Resolves #18603
- **Related Graph Nodes:** `highlight_component`, `Neo.component.Base#style_`
- **Origin Session ID:** 92f5d790-2865-4f69-b25e-150175745a6d

### 🔬 Depth Floor
**Challenge / bounded search:** I checked both modes, a pre-existing outline, destruction, and an unrelated style update during the highlight. A deterministic callback probe against the real component showed base leaves outline/box-shadow residue while head clears it; head preserves the original outline. Both base and head roll an intervening unrelated color update back to the original snapshot. That inherited concurrency behavior is outside this single-highlight repair; this approval does not certify concurrent style-write arbitration.

**Rhetorical-Drift Audit — Findings:** The PR's mechanism correction is supported: runtime config replacement can look clean while wrapperStyle retains VDOM values. “Exactly the keys” describes the restore map, not a guarantee that unrelated concurrent writes survive the full snapshot assignment. No expanded concurrency guarantee is accepted here.

### 🧠 Graph Ingestion Notes
- **[RETROSPECTIVE]** Read the rendered-state input loop, not just the public getter, when checking style removal.
- **[TOOLING_GAP]** The first temporary diagnostic called setup before Neo initialization; corrected import order before using its results. No product failure inferred from that bootstrap error.

### 🎯 Close-Target Audit
**Findings:** One newline-isolated leaf target, #18603 (bug/ai, not epic); commit bodies contain no additional magic close targets. The four delivered criteria have current-head unit coverage.

### N/A Audits — 📑 🪜 📡 🔗
N/A across listed dimensions: existing API/schema unchanged; close-target behavior is unit-observable; no new tools, workflow conventions, or cross-substrate primitive.

### 🧪 Test-Evidence & Location Audit
- **Execution evidence:** All 24 emitted checks green at `44eaa1a7c5d48c320b735ec8ae7bdcffc0bbb69f`, including unit, components, engine E2E and review-admission/mergeability; targets dev.
- **Reviewer falsifier:** Real Component + ComponentService with a controlled timeout callback; base/head arms for both modes, original value, destruction, and intervening color update. This isolates restore semantics, not native rendering or timer scheduling.
- **Test location:** Correct existing `unit/ai/client` family; setup, fresh instances and teardown checked. Routine green CI not rerun.
- **Placement:** Existing Engine-side client service remains the owner. Brain structure map ran successfully; no Brain/config placement change.

### 📋 Required Actions
No required actions — eligible for human merge.

### 📊 Evaluation Metrics
- **[ARCH_ALIGNMENT]**: 100 — existing service, manager lookup and explicit-null contract checked; no new abstraction or consumer coupling.
- **[CONTENT_COMPLETENESS]**: 90 — evidence anchors are complete; the test preamble still compresses config replacement and VDOM persistence into “merges shallow.”
- **[EXECUTION_QUALITY]**: 90 — exact-head CI and direct restore controls pass; inherited concurrent-style rollback remains outside the repaired contract.
- **[PRODUCTIVITY]**: 100 — both residue modes, original-value restoration and destroyed-component handling checked against the leaf criteria.
- **[IMPACT]**: 40 — removes a visible diagnostic artifact, not a broad rendering subsystem defect.
- **[COMPLEXITY]**: 20 — one existing method and four focused test arms.
- **[EFFORT_PROFILE]**: Quick Win — a small local repair with directly discriminating evidence.


---

