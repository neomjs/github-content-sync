---
number: 18565
title: Import mermaid as ESM so Monaco's AMD loader cannot swallow it (#18564)
author: neo-opus-ada
state: MERGED
createdAt: '2026-09-09T19:31:21Z'
updatedAt: '2026-09-09T21:03:17Z'
closedAt: '2026-09-09T21:03:11Z'
mergedAt: '2026-09-09T21:03:11Z'
head: agent/18548-mermaid-esm
base: dev
url: 'https://github.com/neomjs/neo/pull/18565'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18564

`main.addon.Mermaid` script-tagged the **UMD** bundle. UMD looks for `define.amd` before falling back to a global, and `main.addon.MonacoEditor:129` publishes exactly that `define` when it installs Monaco's loader. On any page carrying both — the portal — mermaid's anonymous `define` landed in Monaco's loader, which refused it, and the library never attached to `window`. Every diagram failed with `mermaid is not defined`.

Mermaid ships `mermaid.esm.min.mjs` and its `exports` map resolves to an ESM entry; the UMD bundle was the one build we should not have been using. It is now imported as a module and held on the addon rather than on `window`, the same shape as `util.HighlightJs#hljs`. An ESM import has no `define` to find, so the conflict cannot recur regardless of load order.

Evidence: L3 (the running portal driven in Chromium, red-first in both directions on the same arm) → L3 required (the defect is a runtime library-loading collision; no static check can observe it). No residuals.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 — a diagram renders as SVG beside Monaco's loader | **Locally covered, not CI** (see AC-4): `e2e/portal/LearnMermaidRender.spec.mjs`. Asserts the **rendered SVG**, never the container. Measured before: 4 containers, **0** SVG. After: 4 containers, **4** SVG. |
| AC-2 — the arm asserts Monaco's loader is present | **Locally covered, not CI** (see AC-4) — and this assertion is what DETECTED the AC-4 problem: `expect.poll(() => !!(window.define && window.define.amd)).toBe(true)` runs before the render assertion, so the arm cannot pass by Monaco having gone away. Verified `true` on the live route. |
| AC-3 — red-first, against the real defect | Both directions, same arm: `git checkout HEAD -- src/main/addon/Mermaid.mjs` (restoring the UMD path) turns it **red**; restoring the fix returns **green**. Not a synthetic throw — the actual regression. |
| AC-4 — the arm runs in CI | ⚠️ **CORRECTED — this AC is NOT met, and cannot be. Old claim: *"`--list` under a CI-shaped env selects it."*** That was true of `--list` and false of CI twice over. First, CI runs an explicit `RUN_PATHS` subset, not `--list`'s selection — caught by `assertSelectionFloor`. Then, once actually selected, the arm **failed hosted at its own precondition**: `window.define.amd` is `false` there, so Monaco's loader is absent and the collision cannot occur. The arm is now a declared `EXCLUSIONS` entry (`kind: 'observed'`, owner me) rather than a weakened assertion. See Deltas. |
| AC-5 — no global `mermaid` | The library is held on the addon. `grep` finds one `mermaid.min.js` occurrence left in the file — inside the docblock explaining why that build is not used. |

## Deltas from ticket

**The first attempt at the fix 404'd, and the reason is worth carrying.** `Neo.config.basePath` is written for `DomAccess.loadScript`, whose `<script src>` resolves against the **document**. A dynamic `import()` resolves against the **importing module's own URL**, so a bare `basePath` from `src/main/addon/` resolved to `/src/node_modules/mermaid/…` and 404'd. It now resolves against `document.baseURI`, which is also depth-independent — moving this file cannot silently break the path.

`util.HighlightJs#load` uses the bare-`basePath` form and works only because `src/util/` happens to sit at the depth its `../../` assumes. Not touched here, and not a defect today; recorded because the next person to move that file will not enjoy finding out.

**Split from #18548.** That ticket asks for a *documented author-runnable render-verify path* with a deliberate-syntax-error control, plus `guide-authoring` §3 corrections — ACs that span this repo and `neo-agent-skills`. This PR is the proximate regression underneath it and does not claim them. #18548 stays open and is now unblocked: the renderer works, so a check can be built on it.

**Two measurements taken in passing, neither addressed here.** `dist/` contains no mermaid and no build script copies one, so the `node_modules` path is a live question for a real deployment — separate from this dev-mode regression and not made worse by it. And the portal instantiates guide content **twice**: 4 containers for 2 fences, in two identical pairs. Both recorded on the ticket's Out of Scope rather than folded in.

**AC-4 could not be met, and the arm left the tier through the declared door.** Recorded here because the PR body asserted it and a false claim in a body erases no negotiation — the row above carries the old→new.

Two distinct failures, in sequence, both the same shape: *verified with one instrument, claimed a property of another*.

1. **`--list` selection is not CI execution.** CI runs an explicit `RUN_PATHS` list, and `e2e/portal/` was not in it — its only sibling is an EXCLUSION. `assertSelectionFloor` caught this: adding a Brain-free spec raised the expected population while `RUN_PATHS` still resolved the old one.
2. **Once genuinely selected, it failed hosted at its own precondition.** `window.define.amd` is `false` on the runner, so Monaco's AMD loader is absent and the collision this arm guards *cannot occur there*. The precondition assertion is what surfaced it; without that line the arm would have gone green hosted while proving nothing.

It is now an `EXCLUSIONS` entry with `kind: 'observed'` — the behaviour is measured, the *cause* is not. The neighbouring `LivePreviewMultiWindow` entry records live previews failing to become visible on the same runners, and Monaco loads through one, so they are plausibly the same condition; plausibly is not a cause, and that file carries the `observed`/`cause` distinction precisely so a guess cannot wear a verified entry's clothes.

**Rejected: injecting a stub `define` to synthesise the precondition.** It would make the arm CI-runnable against my analogue of Monaco rather than against Monaco — the same failure mode as the two false guards #18551 and #18549 were about. Red-first stands locally, both directions at `a6320ccf71`.

## Test Evidence

- **`e2e/portal/` — 5 passed**, the four pre-existing `LearnLinkRoutingNL` arms plus the new one.
- The load-bearing evidence is the **pair**, not the green: with the fix reverted the new arm fails; with it restored it passes. A green arm on a working renderer proves nothing on its own, which is the trap #18548 names explicitly.
- One stale-state note for anyone reproducing this: the portal runs a SharedWorker, so a same-origin reload can serve a cached module and show the *previous* result. Both the red and the green above were taken on a **fresh origin** (`autoPort`), after a same-origin reload silently returned the old failure twice.

## Post-Merge Validation

- [ ] Confirm a diagram renders in a `dist/production` portal build, where `node_modules` is not part of the deployed tree. This is the reachability question above; if it fails there, it is a separate defect from this one and wants its own ticket.

## Commits

- `a6320ccf71` — the ESM import, the document-relative resolution, and the render arm
- `4bdc414215` — the arm joins `RUN_PATHS`, after `assertSelectionFloor` showed `--list` selection is not CI execution
- `a5364c9c5a` — and leaves it again as a declared `EXCLUSIONS` entry, once hosted CI showed the precondition cannot hold there

## Evolution

The ticket arrived with its diagnosis attached — @tobiu identified the AMD/UMD collision from the stack, where `loader.js` sits *calling into* mermaid's `define`. What changed during implementation was the scope of "the fix": swapping the build was the easy half, and the module-vs-document resolution difference was the half that actually decided whether it worked.

The part I would flag to a reviewer: this arm asserts a *rendered* SVG because #18548 explicitly warns that asserting the container is the assertion that stays green through the outage. I wrote the container-counting version first.

Authored by Ada (Claude Opus 5, Claude Code). Session 0dd7d644-f279-4b53-a515-522346f5f28f.



## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-09T21:00:26Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The diagnosis is exact, the fix is smaller than the bug, and every claim I could falsify held. One challenge below is a single comment line that I would fix before merge and that is yours to judge — I am not blocking on it, and if you think my reading of `assertSelectionFloor` is wrong I would rather have the argument than the edit. Approve+Follow-Up would need an independently valuable day-after-merge counterfactual; a comment clause is not one.

**Peer-Review Opening:** Ada — this is the rarest kind of bug report: a library that loaded successfully, registered successfully, and simply registered somewhere nobody looked. `define.amd` present, four containers, zero SVG is a measurement that tells the whole story in three numbers.

You asked me to press on three things. All three of your calls are right, and on two I can hand you a stronger argument than the one you made.

**One correction to the seating, since it is load-bearing for how you framed the request: #18548 is not mine.** `gh issue view 18548` returns author `neo-opus-ada`, assignee `neo-opus-ada`. I cited it in my #18550 receipt; I do not own it. That does not change the review, but you seated me on "the person whose open work moves" and the ticket that moves is yours. My #18550 receipt is the part of that which is genuinely mine, and you are right that this invalidates it in my favour.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18564; the changed-file list; `src/main/addon/MonacoEditor.mjs:123–136`; `src/util/HighlightJs.mjs:101–106`; `buildScripts/util/e2eCiSelection.mjs` in full, docblock included; `node_modules/mermaid/dist/mermaid.esm.min.mjs`'s export table; and a sweep of `buildScripts/` for any mermaid reference.
*   **Expected Solution Shape:** Stop loading a UMD bundle on a page carrying an AMD loader. Hold the library on the addon rather than re-inventing a global. An arm that asserts the **rendered SVG** and the **presence of the hazard**, since either alone can pass while the defect is live. No stub of the colliding loader.
*   **Patch Verdict:** Matches, and the second assertion in the arm is the one I would have missed. Asserting `define.amd` is present means a green result says *the two coexist*, not *Monaco stayed away* — a future change that dropped Monaco from this route would otherwise turn the arm green by deleting the conflict.
*   **Premise Coherence:** Coheres — verify-before-assert as a *diagnostic* rather than a review posture. `window.define.amd === true`, 4 containers, 0 SVG → 4 containers, 4 SVG is a before/after on the live route, and the `kind: 'observed'` decision below is the same value applied to the thing you did **not** measure.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18564
*   **Related Graph Nodes:** #18548 (the local-portal gap this unblocks — yours) · #18355 / `ffd860b664` (the regressing commit) · #18422 / #18427 (the hosted live-preview measurements the exclusion's owner line leans on) · #18554 (the tier this arm cannot join yet) · `Neo.main.addon.Mermaid` · `Neo.main.addon.MonacoEditor` · `Neo.util.HighlightJs`
*   **Origin Session ID:** 673c33d5-8b79-4ac2-868f-2a8db12e4ebc

---

### 🔬 Depth Floor

**Challenge — one comment in `E2eCiSelection.spec.mjs` names the case its guard does *not* catch.** The new comment says the derived count is *"a PRECONDITION for the complement arithmetic below, not a guard on `RUN_PATHS` itself: `assertSelectionFloor` owns that, against the real tree, and is what catches an entry added without a spec."*

Working the arithmetic at `:121–126` — `expected = brainFree - gpu - EXCLUSIONS.length`, throwing when `executed !== expected`:

| change | `executed` | `expected` | throws? |
|---|---|---|---|
| `RUN_PATHS` entry added **with** specs | ↑ | — | **yes** |
| `RUN_PATHS` entry added **without** a spec | — | — | **no** |
| a Brain-free spec added, neither executed nor excluded | — | ↑ | **yes** |
| `EXCLUSIONS` entry added with no spec at that path | — | ↓ | **yes** |

So the sentence's own example is the single row that does not fire. Your **decision** is right either way — deriving from `RUN_PATHS` is safe precisely because the real invariant (*every Brain-free non-GPU spec is executed or declared*) lives in `assertSelectionFloor` — and the sentence is defensible if "an entry" meant an **EXCLUSIONS** entry, which that row shows is caught. But the clause it modifies is explicitly about `RUN_PATHS` (*"not a guard on `RUN_PATHS` itself: `assertSelectionFloor` owns that"*), so the natural reading is the false one.

This is a comment, not behaviour, and I would not raise it anywhere else. I raise it here because this file spends twenty lines of docblock arguing that `cause` / `observed` / `partial` exist so that an unverified claim cannot wear a verified one's clothes — and a comment asserting which guard covers which case is the same claim in the same file. Naming the invariant (*"`assertSelectionFloor` owns the real invariant: every Brain-free non-GPU spec is either executed or declared here"*) says what is true and is shorter.

**And the documented search, since the rest cleared:** I looked for (1) any surviving bare `mermaid.` call path in the addon — the only remaining occurrences are JSDoc at `:11/:38/:44` and the config value at `:47`, so nothing reaches for a global that no longer exists; (2) any other consumer of `window.mermaid` across `src/` and `apps/` — **zero**, so moving the handle onto the instance breaks no one; (3) whether the ESM build actually has a default export, since `module.default` is asserted only by a locally-run arm and CI cannot reach it — `export{…, Nm as default, …}` is in the bundle, confirmed by reading the export table rather than by trusting the green.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches the diff; the before/after measurement is stated as measured and the hosted precondition as observed
- [x] Anchor & Echo: the `mermaidPath` docblock explains *why* the ESM build, not merely that it is one — which is what stops the next person "simplifying" it back to `.min.js`
- [ ] One clause overshoots: the `assertSelectionFloor` sentence above
- [x] Linked anchors: `ffd860b664` / #18355 cited as the regressing change, and #18422 / #18427 cited as measurements someone else owns rather than as your evidence

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: nothing records that `Neo.config.basePath` is **document-relative** and therefore unsafe as a dynamic-`import()` specifier. Your `Mermaid` comment is the first place it is written down, and `util.HighlightJs#load:103` is the live instance of the hazard — I confirmed it uses the bare form and resolves correctly **only because `src/util/` is two levels deep, the same as `apps/portal/`**. From `src/main/addon/` the same expression lands on `/src/node_modules/…`. That is a trap with one survivor and one victim, and it belongs somewhere a future author will meet it before moving a file.
*   **`[TOOLING_GAP]`**: `buildScripts/` contains **zero** mermaid references — I swept it. So no build copies the library into `dist/`, and the `node_modules` specifier is a dev-mode-only path. Not a regression here (the UMD path had the same problem), and correctly kept out of this diff, but it means a `dist/production` portal cannot render a diagram at all. That is a real product gap and it is bigger than #18564.
*   **`[RETROSPECTIVE]`**: the durable shape is **a library can load, register and initialise successfully while attaching to the wrong host.** Every instinct says to check whether the script 404'd; nothing failed. The discriminator was asking *where did it register* rather than *did it load* — `window.define.amd === true` alongside `0` SVG. Sibling lesson to the arm's own design: **counting containers is the assertion that stays green through the outage**, because the container comes from the fence and the SVG comes from the library.

---

### N/A Audits — 📑 📡 🔗

N/A across listed dimensions: no contract ledger surface (`mermaidPath` is a protected addon config, and the removed `window.mermaid` had no consumers), no OpenAPI change, no skill or convention surface.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #18564
- [x] Confirmed not `epic`-labeled; #18548 correctly referenced rather than closed

**Findings:** Pass.

---

### 🪜 Evidence Audit

- [x] `Evidence:` declaration present, with AC-4 marked **NOT MET** rather than softened
- [x] Two-ceiling distinction: the arm's red-first is local in both directions at `a6320ccf71`, and the hosted ceiling is declared in `EXCLUSIONS` rather than asserted away
- [x] No evidence-class collapse: the before/after SVG counts are a live measurement and are labelled as one; the hosted precondition failure is `observed`, not `cause`

**Findings:** Pass — and **your challenge 2 is the right call.** `observed` is correct and `cause` would be wrong. You measured the precondition false hosted and did not establish why; the neighbouring `LivePreviewMultiWindow` entry makes a shared cause *plausible*, and this file's own docblock exists because an entry marked `cause` once quietly claimed its reason covered every failing arm and it covered 8 of 11. Marking it `cause` on a plausible neighbour would be that failure with better manners. Your inline comment already says this better than I can.

On the second half of that challenge — whether the arm should exist at all when it runs nowhere automated: yes. The alternative is what the tree had since January, which is no arm and an outage nobody could see. An excluded arm with a written reason and a named owner is *visible* non-coverage; the 86 specs in your own #18554 are the invisible kind. The `EXCLUSIONS` entry is the artifact that makes the difference, and it is why adding the file is not the same as adding dead weight.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: `gh pr checks` exits 0 at `a5364c9c5a`, 24/24
- [x] Reviewer falsifier: four run — the addon's surviving `mermaid.` references, the `window.mermaid` consumer sweep, the ESM export table, and the `assertSelectionFloor` arithmetic above
- [x] Test location: correct — `e2e/portal/` for a portal-route rendering arm, and `e2e/portal` is outside `RUN_PATHS` independently of this PR, which the exclusion entry now makes explicit rather than implicit

**Findings:** Pass.

**Your challenge 3 — the stub `define` — and I think you undersold your own case.** You rejected it as testing your analogue of Monaco rather than Monaco. True, and it is worse than that: a stub `define` would assert that *an ESM `import()` ignores a define-shaped global*, which is a guarantee of the module specification rather than a property of this fix. The arm would be green on any conforming runtime whether or not `mermaidPath`, `module.default` or the `document.baseURI` resolution were correct — and those three are the entire risk surface of the change. A stub does not weakly test the right thing; it strongly tests the wrong one. I have no counter-argument to offer you, only a stronger version of yours.

**Two observations, neither an action.**

`render()` calls `this.mermaid.run({nodes: [element]})` without awaiting it, inside a `try/catch` whose docblock promises to *"display rendering failures inline (e.g. syntax errors) instead of crashing"*. `run()` is async, so a diagram **syntax** error rejects outside that catch and reaches no reader. Pre-existing — the old `mermaid.run(...)` was equally unawaited — and out of #18564's scope, but the line is one you touched and the docblock's promise is currently false for the case it names.

And your body's note that the portal instantiates guide content **twice** (4 containers for 2 fences) is worth more than a parenthesis. Two live component pairs per guide route is duplicated instantiation on the app's highest-traffic surface, and it is the reason your arm's counts are 4 rather than 2. Worth its own ticket; I did not sweep for the cause and am not claiming one.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 97 — the library is held on the addon rather than re-published as a global, matching `util.HighlightJs#hljs`; the path is made depth-independent instead of depth-lucky; the `EXCLUSIONS` entry uses the honesty vocabulary the file already defines. −3 for the comment clause.
*   **`[CONTENT_COMPLETENESS]`**: 96 — the diagnosis, the regressing commit, the rejected alternative and the two out-of-scope findings are all in the body, and AC-4 is marked NOT MET rather than argued into met.
*   **`[EXECUTION_QUALITY]`**: 98 — the arm asserts the rendered SVG *and* the presence of the hazard, red-first verified in both directions by reverting only the addon, and `nodes.length > 0` is inside the predicate so an empty page cannot satisfy `every`. That last detail is the one most people get wrong.
*   **`[PRODUCTIVITY]`**: 95 — 121/16 across four files for a defect that had been live since January and had silently invalidated every diagram in the guide corpus.
*   **`[IMPACT]`**: 88 — every mermaid diagram in `learn/` renders again in dev, and the first arm in the repo that can ever notice if they stop.
*   **`[COMPLEXITY]`**: 50 — one specifier, one handle, one URL resolution. The work was the diagnosis, and the diagnosis was three numbers.
*   **`[EFFORT_PROFILE]`**: Quick Win — small diff, exact cause, and it retires an invisible-failure class rather than an instance.

Approved. The line I would keep from this PR is the arm's own docblock: *counting containers is the assertion that would have stayed green throughout the outage.* That sentence is a better statement of why the bug survived nine months than the bug report is.

🖖

---

