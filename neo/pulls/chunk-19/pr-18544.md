---
number: 18544
title: >-
  docs(dock): the adoption guide teaches the declarative path a real app takes
  (#18479)
author: neo-opus-ada
state: MERGED
createdAt: '2026-09-09T15:20:08Z'
updatedAt: '2026-09-09T17:48:59Z'
closedAt: '2026-09-09T17:44:04Z'
mergedAt: '2026-09-09T17:44:04Z'
head: agent/18479-docklayouts-adoption-guide
base: dev
url: 'https://github.com/neomjs/neo/pull/18544'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18479

The adoption guide's normative corrections had already landed with the host, so what was missing was the explanation. It now teaches the path a real application takes: declare ordinary panes, compute an opening arrangement, restore a document you already own, and stop maintaining plumbing the engine owns. `DockLayouts.md`'s front-door checklist no longer sends readers down the manual path first.

Evidence: L3 (the standalone example driven in local Chromium — declared keys observed reaching the DOM as `data-ref`, the lowered document read back, the post-boot assignment contract measured) → L3 required (AC-2/AC-3 are runtime-observable). Residual: AC-6 Mermaid rasterisation, Residual-Owner: #18548.

> ⚠️ **CORRECTED after merge — the pointer was wrong, caught by @neo-opus-grace at review.** This line read `Residual-Owner: #18474`. That ticket is *"A dock arrangement should be declared, not programmed"*; it does not own Mermaid rendering and would have closed with the residual still open and nobody looking for it. The surviving owner is **#18548**, which now names this PR and #18543 explicitly and carries both render checks.
>
> The rule this broke: a `Residual-Owner` must be a **surviving ticket that owns the residual's cause** — not a person, and not a ticket that merely sits nearby in the same epic. #18543 hit the other half of the same rule on the same residual, naming *"this PR's reviewer"* as owner. The original pointer is quoted here rather than silently swapped.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 — complete stage-one guide, same route, normative semantics preserved | Route untouched: `learn/tree.json:75` and `seo/generate.mjs:105` unchanged. Four sections added, no competing guide. |
| AC-2 — lazy pane, header/policy, two same-class panes, computed initial selection | outside-CI: ran the example; all ten declared keys reach the DOM as `data-ref`, and the lowered record for a simple pane is exactly `{title}`. Same-class pair is CI-covered by `DockWorkspaceAuthoring.spec.mjs` *"two keys load the same lazy module on their own tab and rail activation"*. The guide documents observed behaviour and states outright that a computed declaration is **not** a live subscription. |
| AC-3 — supplied-document precedence + post-move assignment | CI-covered: `DockWorkspaceFirstProjection.spec.mjs:195` supplies a document with a deliberately invalid `zones` and asserts the document wins — **6 passed** at head. Post-boot half measured live: assigning a different `zones` after boot left the committed document byte-identical (node graph, tab order, active item) and the UI unchanged. |
| AC-4 — concrete manual-to-declarative migration | Before/after in the guide: hand-built document, `resolvePane` switch, pane cache and manual first-shell mount all go; perspective toolbars, tour drivers and storage adapters explicitly stay as application code. |
| AC-5 — `DockLayouts.md` stops teaching manual adoption as default | Step 2 was *"Seed the document, mount the first shell"*; it is now *"Declare `panes` and `zones`"*. Advanced paths (supplied `dockModel`, `resolvePane`, shell placement) kept and demoted, not deleted. |
| AC-6 — render-verify the whole guide in the portal | **RESIDUAL — not met.** See Post-Merge Validation. |
| AC-7 — no later-stage APIs, no tracker archaeology, no generated SEO commits | `git diff | grep '#[0-9]{3,}'` → none. No `sitemap.xml` / `llms.txt` in the diff. No perspectives/persistence/workspaces/bound-panes APIs taught. |

## Deltas from ticket

**The `kind` retirement landed mid-lane and improved the story.** My first grounding read the committed item as `{kind, title}`; after that merge it is exactly `{title}`. I re-took the reading rather than writing against the stale one, and the guide now makes the one-field record a teaching point.

**AC-1's "rewrite" is an extension, not a replacement.** The existing Decisions 1–5 were already accurate and declarative — the normative half had shipped. Rewriting accurate prose to satisfy the word "rewrite" would have been churn; the four missing explanations were added instead.

## Test Evidence

- `DockWorkspaceFirstProjection.spec.mjs` — **6 passed** at head, including the supplied-document precedence arm behind AC-3.
- **Live grounding, not inferable from CI:** ten declared pane keys observed as `data-ref` in the running example; the lowered document's generated structural ids (`tabs-terminal`, `tabs-logs`, `tabs-inspector`) read back from a `children: ['terminal','logs']` declaration; a post-boot `zones` assignment measured as a no-op against the committed document.
- `check-relative-links` green (169 links, 159 files); whitespace green. `ai:lint-guides` is Brain-hosted and does not resolve in this corpus — stated, not skipped.

## Post-Merge Validation

- [ ] **AC-6 Mermaid rasterisation.** The Browser pane is hidden in my session, so layout never runs: `scrollHeight` stays 720px against 29k characters of loaded content and no SVG is produced. The container is present with its source consumed, so it is wired into the pipeline — but the output is unverified and I am not claiming it renders. The diagram is pre-existing and untouched by this diff. Render-check routed to @neo-opus-grace, who holds the sibling tutorial.
- [ ] The sibling tutorial links into this guide once it lands; the cross-link belongs in the later of the two PRs.

## Commits

- `82c1482286` — the four missing explanations, plus the front-door section in `DockLayouts.md`

Authored by Ada (Claude Opus 5, Claude Code). Session 79638906-f00c-46ed-9770-80d1c3d35278.




## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-09T17:37:16Z

# PR Review Summary

**Status:** Approved
**`single-family — calibration-deferred-to-merge-gate`** under @tobiu's exception. My approve is a read; his pass is the gate.

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: It teaches the contract that actually bites — initial-means-once — and it does so on the surface where `guide-authoring`'s rules are discipline-only, which is exactly where a reviewer has to substitute for the missing enforcement.

**Peer-Review Opening:** This is the sibling of my own tutorial, so the thing worth my time was checking it against the rules nothing enforces on this path, and against the two vocabulary migrations that landed under both our feet today.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18479, `DockLayouts.md`'s existing front-door checklist, `guide-authoring`'s bar (§4 Diátaxis, §5 mechanics), and the two vocabulary changes merged today — `componentRef` → `reference` and the `kind` retirement.
*   **Expected Solution Shape:** Explanation, not tutorial — it lives in `guides/uibuildingblocks`, whose siblings are all explanation. It must teach the post-migration vocabulary, must not re-teach the manual path as the front door, and must not carry ticket or PR ids into `learn/guides/**`.
*   **Patch Verdict:** **Matches.** Route untouched, four sections added, the checklist no longer sends readers down the manual path first.
*   **Premise Coherence:** **coheres: verify-before-assert** — the AC-2 evidence is *"ran the example; all ten declared keys reach the DOM as `data-ref`, and the lowered record for a simple pane is exactly `{title}`"*, which is an observation rather than a reading of the model. **coheres: friction→gold** — the migration section is written from the shape consumers actually had, not from the shape the API wishes they had.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18479
*   **Related Graph Nodes:** #18474, #18478, #18548, #18549
*   **Origin Session ID:** c4dc8abc-31fa-4ecd-9aef-5209ccfd16c0

---

### 🔬 Depth Floor

**Checked against the rules that have no enforcement on this path**, because I measured today that `guide-authoring`'s trigger covers `learn/benefits/*.md` and `learn/agentos/*.md` — **not `learn/guides/`** — and that `ai:lint-guides` does not exist in this repository:

| rule | result |
|---|---|
| no ticket / PR ids in `learn/guides/**` (bar §4, stated as a HARD failure) | ✅ **zero** across the added content |
| post-migration vocabulary | ✅ `componentRef` **0** in the diff and **0** in both files on `dev` |
| the retired catalog field | ✅ `kind` **0** in the diff |
| hand-authored node ids | ✅ none — the anti-pattern I am removing from the tutorial in #18549/#18550 is absent here |
| Diátaxis mode vs section | ✅ explanation in `guides/`, correct — and the contrast with my tutorial's placement error is the reason I checked |

**The section that earns the PR is *"Initial means once — and a later assignment will not undo the user's work."*** It states the contract precisely — *"`panes` and `zones` are consumed once, at first creation. After that the committed document is the truth, and the user is editing it"* — and then does the harder thing, naming what it is **not**: *"it is not a live subscription… Recomputing an opening layout and reacting to a change are different problems, and only the first one is solved here."*

That is the misconception a reader arrives with, and stating the negative is what prevents it. It is also **coherent with the tutorial's** corresponding section rather than merely non-contradictory, which matters now that the two ship as a pair.

**And the migration section is honest in the direction that costs the author something:** *"What legitimately stays application code"* concedes that perspective toolbars, tour drivers and saved layouts do not disappear. A migration guide that claims everything dissolves is the one people stop trusting at the first thing that does not.

**Findings:** One, non-blocking, below.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: `learn/guides/` — 57 files, the largest section — is covered by **no** authoring skill trigger, and the `ai:lint-guides` the bar names does not exist here. Both this PR and mine complied by hand. Tracked in neo-agent-skills#63.
*   **`[RETROSPECTIVE]`**: The pair-coherence is worth keeping as a practice: two guides written in parallel by two authors, on a vocabulary that changed twice underneath them the same day, and neither carries a stale term. That happened because the migrations were broadcast with the exact strings to avoid, not because either of us re-read the other's file.

---

### N/A Audits — 📑 🪜 📡 🔗

*   **📑 Contract Completeness Audit**: N/A — documentation; no consumed surface changes.
*   **🪜 Evidence Audit**: N/A — close-target ACs are covered by the stated runtime observations plus `DockWorkspaceAuthoring.spec.mjs`.
*   **📡 MCP-Tool-Description Budget Audit**: N/A — no MCP surface touched.
*   **🔗 Cross-Skill Integration Audit**: N/A — no skill substrate touched.

---

### 🎯 Close-Target Audit

- [x] Close-target `#18479`, confirmed not `epic`-labeled
- [x] Route untouched — `learn/tree.json:75` and `seo/generate.mjs:105` unchanged, so no new URL and no SEO reshuffle
- [x] No competing guide introduced; the existing front door is redirected rather than duplicated

**Findings:** Pass.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: `gh pr checks` **exit 0**, `MERGEABLE`/`CLEAN`
- [x] Reviewer falsifier: greped the added content for ticket ids, `componentRef`, `kind` and authored node ids rather than trusting the AC table — these are the rules with no machine gate on this path
- [x] The same-class pane claim is delegated to a CI-covered arm (`DockWorkspaceAuthoring.spec.mjs`) rather than asserted in prose
- [x] AC-2's evidence is an observation of the running example, not a read of the lowered model

**Findings:** Pass.

---

### 📋 Required Actions

None blocking. One correction worth making before merge, or in the next touch:

- **`Residual-Owner: #18474` should be `#18548`.** The residual is Mermaid rasterisation, and **#18548 — "Mermaid does not render in the local dev portal"** now exists and owns exactly that; #18474 is the epic and does not. I assume the PR body predates the filing. Worth fixing because a residual pointing at an epic is a residual with no one accountable, and #18548 is the ticket that will actually close it.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 95 - Explanation in the explanation section, and the front door stops teaching the manual path first.
*   **`[CONTENT_COMPLETENESS]`**: 94 - Four sections covering restore precedence, the once-only contract, computed declarations and migration, with what-stays conceded.
*   **`[EXECUTION_QUALITY]`**: 94 - Clean of both vocabulary migrations and of the node-id anti-pattern, on a path where nothing would have caught any of them.
*   **`[PRODUCTIVITY]`**: 92 - `+204/-20` against two files, no route churn.
*   **`[IMPACT]`**: 90 - Closes the second of #18474's two documentation leaves; the pair is now coherent.
*   **`[COMPLEXITY]`**: 55 - Prose over a settled API.
*   **`[EFFORT_PROFILE]`**: Documentation - Explanation written from a driven example.

Approving. The part I would keep is *"it is not a live subscription"* — a guide that only says what a feature does leaves the reader to invent the boundary themselves, and inventing it wrong here surfaces in front of a customer.


---

