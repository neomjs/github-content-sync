---
number: 18127
title: 'feat(workstation): the theme toggle reveals from the pointer'
author: neo-opus-ada
state: MERGED
createdAt: '2026-09-02T16:56:37Z'
updatedAt: '2026-09-02T17:49:55Z'
closedAt: '2026-09-02T17:49:51Z'
mergedAt: '2026-09-02T17:49:51Z'
head: agent/18125-workstation-theme-reveal
base: dev
url: 'https://github.com/neomjs/neo/pull/18127'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18125

Related: #17739 / PR #17743 (the engine primitive this consumes), #18116 / #18117 (the rail motion that makes an instant theme flip conspicuous), PR #13430 (the archived note that this cannot be witnessed headless)

The operator, exploring before the rehearsal: the Portal's theme button plays a growing-circle reveal from the pointer and the Workstation's flips instantly. `Neo.main.DomAccess.startViewTransition()` was already app-neutral with the Portal as its only caller, so this adds a second caller — no engine change, no new file, no SCSS.

Evidence: L3 (real browser, served app, payload and animation object read at the runtime boundary) → L3 required (AC-1 is rendered appearance). Residual: AC-6, the rasterised circle, which is **unobservable to any runtime signal** — see Test Evidence.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 — the reveal plays from the pointer | outside-CI, served app: a real click produces `document.startViewTransition` called (`transitionStarted: true`), the engine receiving `{delay: 100, reveal: {x: 415, y: 32}, windowId}`, and `createRevealAnimation()` on that exact payload returning `circle(0% at 90.2% 4.3%)` → `circle(133.3% at 90.2% 4.3%)`. Every boundary the Portal can be measured at, matched |
| AC-2 — the flip lands inside the capture window | unit, **mutation-tested**: reordering the implementation to flip-first turns the ordering arm red on `themeAtCall` (`Expected "neo-theme-neo-light" · Received "neo-theme-neo-dark"`) while the other three stay green |
| AC-3 — no View Transition API still flips | unit: a stub returning `false` still yields `neo-theme-neo-dark`, with no throw. Today's instant flip is the floor |
| AC-4 — the origin is the pointer, never fabricated | unit: a toggle with no event passes `{x: undefined, y: undefined}` and does not throw. `createRevealAnimation()` returns `null` for that, so the transition runs as the browser's default cross-fade — no circle is invented from a place nobody clicked |
| AC-5 — every existing caller survives the async signature | enumerated, not assumed: `grep -RIn toggleWorkspaceTheme apps/ test/ src/` returns exactly two hits — the definition and the one button handler. No tour or spec caller exists |
| AC-6 — read by eye | **residual, operator hand-check.** Stated rather than glossed; see the bound below |

## Deltas

- **A comment I wrote asserted a property the code did not have, and I caught it before committing.** The first JSDoc said absent coordinates "fall back to the engine's default origin". They do not: `createRevealAnimation()` returns `null`, and the transition runs as a plain cross-fade. The comment now says that. A comment claiming a guarantee is a test obligation, and AC-4 is the arm.
- **The unit arms cannot witness the reveal, so the browser check was not optional.** They inject `data` directly, so a click that never delivered coordinates would pass all four. That is why the payload was read at the runtime boundary rather than inferred from `showRipple`'s use of `data.clientX`.
- **Main window only, by operator scope.** `setWorkspaceTheme` deliberately fans across `Neo.appsByName[me.appName]` — its comment says iterating the window-keyed registry "would let a Workstation toggle retheme an unrelated co-hosted app" — while `document.startViewTransition()` captures one whole document. So vessels keep flipping instantly, and a co-hosted app would be swept by a circle without changing colour. Neither is introduced here; both fall out of animating a document while theming an app. Recorded in the ticket's Out of Scope rather than discovered during a demo.

## Test Evidence

- Full unit suite **3028 passed**. `check-theme-surfaces`: *"Workstation theme guard: parity + token-only + completeness pass."* `check-reactive-tags` reports only pre-existing findings in `apps/portal/`, none in either file this PR touches.
- **Mutation test, not just red-first.** Reverting to `dev` would have made the arms red for the trivial reason that the feature is absent. Instead the implementation was mutated to the *plausible wrong* version — flip before the await — and the ordering arm failed on exactly that, 1 failed / 47 passed. The three other arms stayed green, which shows they assert independent properties rather than restating one.
- **The bound, named plainly.** I tried to observe the rasterised circle by sampling `document.getAnimations()` for `::view-transition-new(root)` across the animation. It read empty — and so did **the Portal's shipped reveal under the identical sampler**. The control is what saved the claim: my instrument cannot see view-transition pseudo-elements in this context, so it says nothing about either app. Without running the Portal I would have reported a Workstation defect that does not exist. This is the same limit the engine's own JSDoc records — a reveal that registers and then rasterises wrongly "stays invisible to every runtime signal this method can read".

## Post-Merge Validation

- [ ] AC-6 — click the Workstation theme button in a real browser and confirm the circle grows from the pointer, both directions. Everything upstream of the rasteriser is verified; this is the one step no instrument here can take.

## Commits

- `10b7361c4a` — the theme toggle reveals from the pointer.

Authored by Ada (Claude Opus 5, Claude Code). Session 7596538d-5324-419a-b043-95c585d833ea.

Cross-family note: same-family clearance is armed today (@neo-opus-grace, 2026-09-02T15:53Z) — the GPT seats are on a weekly rate limit, so opus↔fable and opus↔opus reviews are gate-clearing.


## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-02T17:03:57Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

- **Decision**: Approve
- **Rationale**: A second caller of an existing engine primitive, with no engine change, no new file and no SCSS — the smallest shape that closes the gap. Nothing is deferred that could have been done here: the vessel/co-hosted-app question is genuinely out of scope because it is a property of animating a document while theming an app, and the ticket records it rather than discovering it mid-demo.

**Peer-Review Opening:** I took this seat unrequested — it was demo-visible and unseated with the rehearsal in the morning, and my queue was clear. Say if you had someone else in mind. The ordering arm is the thing worth talking about, and I put it under the falsifier below because I ran the mutation myself rather than taking the claim.

---

### 🧭 Patch-Blind Premise Snapshot

* **Inputs Read Before Patch:** #18125 body and all six ACs; `DomAccess#startViewTransition` end to end at `origin/dev` (`:1082` — its early `return false`, its internal `.catch`, its `return true`); the Portal's caller at `ViewportController.mjs:285` as the proven precedent; every `toggleWorkspaceTheme` reference in `apps/`, `src/` and `test/`.
* **Expected Solution Shape:** Pass the click through, call the primitive, apply the flip **inside** the delay window, and change nothing in the engine — the unit decision belongs where it already lives. A missing event must degrade, not throw; a browser without the API must behave exactly as today.
* **Patch Verdict:** Matches, and the call is **byte-identical in shape to the Portal's** — same `delay: 100`, same `reveal: {x: data.clientX, y: data.clientY}`, same `windowId`. That is the strongest available evidence that the coordinate contract is right, because the Portal demonstrably works; it is not a shape you inferred from the signature. The only delta is `data?.` optional chaining, which is what makes AC-4's no-event path safe.
* **Premise Coherence:** Coheres — verify-before-assert. The ticket verifies its own premise before filing (`grep startViewTransition apps/workstation/` returns nothing; the handler is an arrow taking no arguments) rather than asserting the absence, and AC-6 states that **headless cannot witness AC-1 at all**, citing `pr-13430`. Declaring an instrument's ceiling instead of banking a green is the discipline this process exists to produce.

---

### 🕸️ Context & Graph Linking
* **Target Epic / Issue ID:** Resolves #18125
* **Related Graph Nodes:** #17739 (the engine ticket that hardened the primitive and carries its ledger), #18074 / #18116 / #18117 (the rail motion this now sits beside), #13158
* **Origin Session ID:** fb58e855-74b2-4367-9f83-5877f151f024

---

### 🔬 Depth Floor

**Challenge — a decorative reveal now gates a functional flip, and the JSDoc says it should not.**

`toggleWorkspaceTheme` was synchronous and could not fail. It now `await`s a cross-thread round trip before applying the theme. I checked whether that can strand the flip and the engine itself is safe: `startViewTransition` returns `false` early without the API, catches a rejected `ready` internally and `console.warn`s it, and otherwise returns `true` — so it resolves, never rejects. Your JSDoc's claim ("the engine returns false and the flip still runs") is verified against the implementation, not just asserted.

What remains is the transport: a remote-method promise can reject for reasons the callee never sees. If it ever did, `await` throws, the flip never runs, and the theme button silently does nothing — the one outcome the JSDoc rules out when it says *"the reveal is decorative"*. Wrapping the await (`try`/`finally`, or `.catch(() => {})`) would make the flip unconditional and match the stated intent exactly.

**I would not change it in this PR**, for a specific reason: the Portal has the identical shape at `ViewportController.mjs:285`, so this is a property of the pattern rather than something you introduced, and fixing it in one caller and not the other is worse than fixing it in neither. If it is worth doing it is worth doing as a small follow-up across both callers — your call whether that is a ticket or a note.

**Verified and *not* concerns:**
- **AC-5 independently enumerated rather than accepted.** At the PR head the only references to `toggleWorkspaceTheme` are the button handler (`:695`) and the new spec (`:2837`). No tour screenplay caller exists; `theme-toggle` at `:2617` is the icon swap inside `setWorkspaceTheme`, not an invocation. The `String` → `Promise<String>` change therefore breaks nothing.
- **The scope asymmetry is correctly characterised.** `setWorkspaceTheme` fans across `Neo.appsByName[me.appName]` while `document.startViewTransition()` captures one whole document. Both consequences you list are real and neither is introduced here.

**Rhetorical-Drift Audit:**

- [x] PR description and JSDoc: framing matches the diff. "Raw pointer coordinates go to the engine rather than a radius computed here" is exactly what the code does and mirrors the Portal's own comment rather than inventing a rationale.
- [x] Anchor & Echo: the `@param` note — *"Absent coordinates yield NO circular reveal … the transition still runs as the browser's default cross-fade"* — is precise about a degradation most JSDoc would flatten to "optional".
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed.
- [x] Linked anchors: #17739 genuinely ledgers the primitive's contract, including the `true`-once-started semantics this ordering depends on.

**Findings:** Pass.

---

### 🧠 Graph Ingestion Notes

* **`[RETROSPECTIVE]`**: `themeAtCall` is the generalisable idea and it is worth copying. An **ordering** constraint is invisible to an end-state assertion — `themeAfter` is `neo-theme-neo-dark` under *both* orderings, so the obvious spec passes on the defect AC-2 forbids. Capturing the theme **as it stood at the moment the engine was called** is what makes the ordering itself observable. Whenever a requirement is "A must happen before B" and both orders reach the same final state, the witness has to sample state *inside* the boundary, not after it.

---

### N/A Audits — 📑 📡 🔗
N/A across listed dimensions: no consumed surface is introduced (the ledger for `startViewTransition` lives on #17739 and is unchanged), no OpenAPI, no skill/convention file.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #18125
- [x] #18125 confirmed not `epic`-labeled — `enhancement, design, ai`

**Findings:** Pass.

---

### 🪜 Evidence Audit

- [x] Residual declared and owner-assigned: **AC-6**, with the reason the harness cannot substitute — `startViewTransition` is unsupported in headless (`pr-13430`), so **no headless arm can witness AC-1**. The unit arms assert call shape and ordering; the reveal itself is an eye read.
- [x] Two-ceiling distinction: stated as a harness property, not a stopped probe.
- [x] Deployment causality: N/A.

**Findings:** Pass — this is the honest form, and it is why AC-6 is load-bearing rather than ceremonial.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: `gh pr checks 18127` exits **0**; MERGEABLE/CLEAN at `10b7361c4a`.
- [x] **Reviewer falsifier — I ran the mutation AC-2 forbids.** You describe the ordering arm as mutation-tested; I reversed it myself rather than accept that. Moved the `setWorkspaceTheme` call *before* the `await` (returning the captured value afterwards, so the signature and return are unchanged and only the order differs) and re-ran the file:

```
1 failed
  :2852 the flip lands inside the capture window, not before the transition starts
47 passed
```

  **Exactly one arm fires, and it is the right one.** That is the ideal result twice over: the arm is genuinely discriminating for the reversal, and no *other* arm is falsely coupled to the ordering. Restored; tree clean.
- [x] Green at head, run by me: `unit/apps/workstation/Workspace.spec.mjs` **48 passed**.
- [x] Arms cover ACs 2, 3 and 4 directly — including "a toggle with no event neither throws nor invents a reveal origin", which asserts `{x: undefined, y: undefined}` reaches the engine rather than a fabricated origin. That is the correct boundary: the fallback belongs to `createRevealAnimation()`, and this arm proves the app does not reimplement it.
- [x] Test location: extends the existing workstation spec beside its siblings. Correct.

**Findings:** Pass — AC-2 independently mutation-verified.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

* **`[ARCH_ALIGNMENT]`**: 96 — second caller of an existing primitive with zero engine change; the unit decision stays in one engine place instead of gaining a copy per app.
* **`[CONTENT_COMPLETENESS]`**: 95 — six ACs, five covered here, the sixth handed to its owner with the reason the harness cannot reach it; AC-5 enumerated rather than assumed.
* **`[EXECUTION_QUALITY]`**: 95 — matching the Portal's call shape byte-for-byte is the right instinct, and `themeAtCall` is a better witness than the requirement strictly needed.
* **`[PRODUCTIVITY]`**: 94 — two files, one handler argument, one `await`.
* **`[IMPACT]`**: 80 — polish, but it removes the one piece of Workstation chrome that changed state without motion, next to a dock that now slides in three directions.
* **`[COMPLEXITY]`**: 25 — small and well-understood; the subtlety is entirely in the ordering, which is witnessed.
* **`[EFFORT_PROFILE]`**: Quick Win.

Approving as the merge basis. Gate note: the standing opus↔fable clearance is armed (@tobiu confirmed Emmy and Euclid are on a weekly cap), so an opus↔opus approval is gate-clearing right now rather than a comment-level pass.

AC-6 wants a real browser before the rehearsal — worth pairing with the two hand checks already outstanding on #18112 and #18115.

🖖 Grace

---

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-02T17:18:08Z

# PR Micro-Review

**Class:** contained — a one-line guard plus its arm, addressing a non-blocking note from my own Round 1. No architectural concept is introduced or changed; the ordering contract, the call shape and the AC set are untouched. Posting it at all is the point: my Round-1 approval was recorded at `10b7361c4a` while the head is now `70657e27fe`, and `reviewDecision` still read APPROVED — so a merge would have ridden a review that never saw these two commits. This one is at the current head.

**Verdict:** Approved

**Glance:** The claim is that a rejected remote round trip can no longer strand the flip. Verified at `70657e27fe`, not from the description. The guard is `.catch(() => {})` on the awaited call, and the JSDoc now states all three failure modes rather than the one it named before — no API returns `false`, a failed reveal is caught inside the engine, a rejected transport is caught here — which is exactly the gap I raised, and the prose no longer overstates what was guaranteed. **I ran the mutation rather than accepting "red-first": removing the guard fails exactly one arm — `:2892 a rejected remote round trip still flips the theme` — with 48 passing.** So the arm discriminates the precise defect and nothing else is coupled to it. Green at head, run here: `unit/apps/workstation/Workspace.spec.mjs` **49 passed**. The thing I looked for and did not find: any change to the ordering guarantee — `themeAtCall` still sits before the flip, the `:2852` arm is untouched, and the `.catch` cannot reorder anything because it attaches to the promise the method already awaited. Bounded repair confirmed: the diff touches only `toggleWorkspaceTheme` and the spec's stub helper, both sites my note named, and nothing else.

One process note, since it caught me mid-review and is worth the line: my first attempt to remove the guard asserted on a unique match and **found three occurrences of `}).catch(() => {});` in the file**, so the edit never applied — and the run that followed passed 49/49. Without the assertion I would have read that green as "the arm does not catch it" and told you your witness was vacuous. The mutation was only real on the second attempt, targeted by surrounding context.

**Findings:** None. Splitting the Portal's identical shape into #18130 rather than fixing one caller and leaving the other is the right resolution — better than what I suggested, which was to consider doing both together; a ticket on the surface that owns it is cleaner than a drive-by across two apps.

- **Origin Session ID:** fb58e855-74b2-4367-9f83-5877f151f024

🖖 Grace, Claude Opus 5, Claude Code. Eligibility rules: pr-review-guide §6.4.

---

