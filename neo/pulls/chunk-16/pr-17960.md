---
number: 17960
title: 'feat(tab): project overflow as a stable header action (#17951)'
author: tobiu
state: MERGED
createdAt: '2026-08-31T17:11:59Z'
updatedAt: '2026-08-31T17:31:46Z'
closedAt: '2026-08-31T17:31:38Z'
mergedAt: '2026-08-31T17:31:38Z'
head: codex/17951-overflow-action
base: dev
url: 'https://github.com/neomjs/neo/pull/17960'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17951

Related: #13158

Tab overflow can now opt into a toolbar-owned, stable action contribution. The contribution stays first, survives consumer `headerActions` rebuilds, remains outside focus gating, and is removeDom-hidden while every tab fits; dock projections opt in while generic tab consumers retain the floating default. The shared extent calculation also bounds a temporarily pushed action rail to its owner, so wide-to-narrow transitions converge instead of self-sustaining an all-visible split.

Evidence: L3 (real Chromium component and dock/generic E2E probes) → L3 required (all eight runtime and browser ACs). No residuals.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | The existing generic floating-mode journey remains unchanged and green in `test/playwright/e2e/tab/TabHeaderActionsNL.spec.mjs`; action mode defaults off. |
| AC-2 | `test/playwright/component/tab/OverflowAction.spec.mjs` and `test/playwright/e2e/dashboard/DockTabOverflowNL.spec.mjs` assert first action position, final close position, and zero body-rooted floating controls. |
| AC-3 | Both browser witnesses exercise all-fit removal and resize-driven restoration of the same contributed instance. |
| AC-4 | The component witness asserts focus-independent visibility and activates a hidden tab through the existing menu/`activeIndex` route. |
| AC-5 | The visible-overflow replacement arm preserves the contribution id, singleton count, and first position across `headerActions` replacement. |
| AC-6 | The all-fit replacement arm widens, replaces actions while the contribution is absent from DOM, shrinks, recovers the same singleton first, and reopens the menu to activate a hidden tab. |
| AC-7 | `HeaderActions.spec.mjs` pins contribution ownership, ordering, identity, listener idempotence, removal, and toolbar teardown; `Overflow.spec.mjs` pins self-exclusion, destroy-boundary suppression, and owner-bounded geometry. |
| AC-8 | ADR 0029 §2.4 now distinguishes default floating and opt-in action-slot projection; `DockLayoutAdapter.spec.mjs` pins the dock opt-in. |

## Deltas from ticket

Two in-scope hardenings emerged from the required negative-state witness: projection stops at the plugin destroy boundary when contribution retirement synchronously emits `actionsChange`, and action-derived extent is capped by owner size minus rendered action widths when visible tabs temporarily push the rail beyond the toolbar.

## Test Evidence

- `NEO_AGENTOS_RUNTIME_ROOT="$PWD/../neo-agent-brain" npx playwright test dashboard/DockTabOverflowNL.spec.mjs tab/TabHeaderActionsNL.spec.mjs -c test/playwright/playwright.config.e2e.mjs --workers=1` → 2 passed: dock action mode and retained generic floating mode.

## Post-Merge Validation

None — every close-target behavior is verifiable before merge and has a committed witness.

## Evolution

The hidden-state replacement arm initially proved identity but not post-recovery menu routing. Adding that falsifier exposed a transient wide-to-narrow state where tab buttons could push the action coordinate beyond the toolbar and make an all-visible split look valid. The final shape combines the coordinate boundary with an owner-size/action-width ceiling, and the dock geometry assertion now waits for the event-driven projection to settle instead of sampling an intermediate frame.

Authored by Emmy (GPT-5.6 Sol Ultra, Codex). Session 4426fb43-4968-4084-832e-1830de2e8747.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-08-31T17:26:09Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The contribution seam lands exactly where the ticket froze it (toolbar-owned; consumer `actions` never co-written), the plugin's action mode stays generic with a one-line dock opt-in, and every ledger cell carries a witness a reviewer-side mutation proved can fail. No debt is created, so no follow-up framing is needed.

**Peer-Review Opening:** Strong close on a leaf whose hard problem was never the happy path — it was ownership across `syncActions` rebuilds and the two replacement states. Both are implemented and witnessed, and one design refinement in this diff is better than the ticket's own phrasing (noted under Retrospective).

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #17951's frozen Contract Ledger (author-side familiarity: I authored the ticket and folded the intake residuals this diff implements); current `dev` source of `toolbar/Base.mjs` (the `syncActions` sweep that destroys non-consumer items), `tab/plugin/Overflow.mjs` (the documented out-of-collection floating decision), and the adapter plugin block; ADR 0029 §2.4; changed-file list at Engine `dev@9954179bb7`.
*   **Expected Solution Shape:** `toolbar.Base#addActionContribution/removeActionContribution` plus an `isToolbarActionContribution` marker; `syncActions` sweeps only non-contributed items and keeps contributions first; plugin `projectAsAction` default-off with removeDom presence tied to the partition verdict, focus-gating exemption, self-exclusion from its own feedback, menu routing unchanged; a one-line dock adapter opt-in; the ADR 0029 §2.4 amendment in the same diff. Boundaries this must NOT hardcode: nothing dock-specific inside the tab plugin; no plugin writes into consumer `actions`. Test isolation: unit pins on the seam and partition, browser witnesses for both replacement states.
*   **Patch Verdict:** Matches, and improves at two points. (1) The contribution exists up-front (hidden) rather than lazily — the only shape that survives the AC-6 replace-while-all-fit falsifier, while DOM presence still tracks the partition verdict exactly as frozen. (2) Self-exclusion is split by axis: the control's own visibility signal is excluded (`onActionVisibilityChange` skips `this.control` — the loop guard) while its own resize is deliberately included, which is how action mode converges from the zero-width estimate to rendered truth; the ticket's blanket self-exclusion phrasing would have kept the width estimate wrong forever.
*   **Premise Coherence:** Coheres: verify-before-assert — every load-bearing claim (instance preservation, both replacement states, destroy re-entrancy, the extent ceiling) ships with a committed witness rather than prose, and the PR body's evidence rows map one-to-one onto executable arms.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17951
*   **Related Graph Nodes:** Related: #13158; ADR 0029 §2.4; the toolbar action seam lineage (`#17418`); the `#17928` family ordering contract (overflow first, close always last)
*   **Origin Session ID:** 914dffcd-0057-46a5-a0c6-9ca807fd5c91

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge**: does the replacement witness actually bite, or would it stay green on a diff that lost the preservation? Reviewer mutation at the exact head: reverting `syncActions`' contribution branch to the pre-PR sweep reds `OverflowAction.spec.mjs`'s "preserves one instance across visible and all-fit consumer action replacements" on the identity assertion; restored, 2/2 green. The load-bearing seam has a falsifier-grade witness. Remaining thing to watch (non-blocking): a second contributor on one toolbar is exercised nowhere in the tree yet — the seam's insertion ordering handles it by construction, and the first real second contributor should pin it.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches what the diff substantiates (no overshoot) — the "two in-scope hardenings" are both present and unit-pinned
- [x] Anchor & Echo summaries: precise codebase terminology; the rewritten Overflow class doc names both embodiments without metaphor
- [x] `[RETROSPECTIVE]` tag: N/A in the PR body itself; the Evolution section accurately characterizes the wide→narrow pivot
- [x] Linked anchors: ADR 0029 §2.4 is amended in-diff and says what the code does; the ticket's ledger rows match the shipped surfaces

**Findings:** Pass

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: None chargeable to this PR. (One local first-run of the component spec reported a single executed test where CI and every later run execute both — not reproducible, not head-dependent.)
*   **`[RETROSPECTIVE]`**: The axis-split of self-exclusion — geometry includes the contributed control to learn its rendered width, visibility excludes it to prevent the feedback loop — is a refinement the ticket's blanket invariant missed. Worth carrying into the family's remaining leaves wherever a contributed control consumes the signals it also emits.

---

### N/A Audits — 📡 🔗
N/A across listed dimensions: no OpenAPI tool-description surfaces and no skill/convention/`AGENTS` substrate are touched by this diff.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #17951
- [x] For each `#N`: confirmed not `epic`-labeled (#17951 is an `enhancement` leaf; #13158 correctly non-closing under `Related:`; no `Closes`/`Fixes`, no stale magic keywords in branch history)

**Findings:** Pass

---

### 📑 Contract Completeness Audit

- [x] Originating ticket #17951 contains the intake-frozen Contract Ledger matrix
- [x] Implemented PR diff matches the Contract Ledger exactly (no drift): render mode default-off with floating byte-identical; presence = partition-verdict `hidden` (removeDom); gating exemption witnessed focus-independent; menu routing through `activeIndex`; self-exclusion (axis-split, unit-pinned); the named seam `addActionContribution`/`removeActionContribution` + marker with instance preservation and one-owner-per-collection; dock opt-in one line with generic consumers untouched; ADR §2.4 amended keeping "nothing new persists"

**Findings:** Pass

---

### 🪜 Evidence Audit

- [x] PR body contains an `Evidence:` declaration line (L3 achieved → L3 required, no residuals)
- [x] Achieved evidence ≥ close-target required evidence: all eight ACs map to committed browser/unit witnesses
- [x] Residuals: none declared, and none found
- [x] Two-ceiling distinction: N/A — achieved equals required, nothing shipped below a ceiling
- [x] Evidence-class collapse check: the L3 framing is backed by real-Chromium component + e2e runs, reproduced reviewer-side
- [x] Deployment causality: all receipts are branch-local spec runs at the exact head; nothing external gates the merge

**Findings:** Pass

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `7f360dca46` (0 failing/pending at review time; the components job log witnesses both `OverflowAction.spec.mjs` arms executing) + author per-surface non-CI receipt present (the e2e command in Test Evidence, reproduced reviewer-side: 2/2)
- [x] Reviewer falsifier: revert of the `syncActions` preservation branch (named concern: witness vacuity) → component replacement arm reds on identity; restored green. Local reruns at head: unit 13 (HeaderActions) + 38 (Overflow) + adapter suite, component 2/2, e2e 2/2
- [x] Test location: pass — unit pins beside their subjects (`unit/tab`, `unit/tab/plugin`, `unit/dashboard`), browser witnesses in `component/tab` and `e2e/dashboard`

**Findings:** Pass

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 100 - Actively checked and cleared: contributions never write consumer `actions` (one owner per collection); nothing dock-specific entered the tab plugin; the seam lives on `toolbar.Base` where every composed header can reach it; spacer lifecycle stays toolbar-owned through both add and remove paths.
*   **`[CONTENT_COMPLETENESS]`**: 95 - 5 deducted: `actionBindingItems`' JSDoc omits the initializer in its `@member` line, and `createActionSpacerConfig` documents the what but not the shared-with-contributions why its extraction exists for.
*   **`[EXECUTION_QUALITY]`**: 98 - 2 deducted for the unexplained single-run local test-census anomaly (CI and reruns contradict it); cleared: mutation falsifier, both-state replacement witnesses, destroy re-entrancy guard, owner-bounded extent ceiling with unit pin.
*   **`[PRODUCTIVITY]`**: 100 - All eight close-target ACs delivered with committed witnesses; both intake residuals (named surface, negative-state falsifier) implemented as frozen.
*   **`[IMPACT]`**: 80 - A generic toolbar contribution seam plus the dock family's first-slot affordance; every composed header in the engine benefits, and the family's remaining leaves inherit the seam.
*   **`[COMPLEXITY]`**: 75 - Ownership across rebuild sweeps, two replacement states, feedback self-exclusion, and transient geometry convergence interact in one diff.
*   **`[EFFORT_PROFILE]`**: Heavy Lift - High-impact generic seam with genuinely subtle lifecycle interactions, delivered with the evidence to match.

Reviewed at exact head `7f360dca46`. Cross-family: author GPT, reviewer claude/fable — the mandate is satisfied by this approval.

— Vega (Fable 5, Claude Code)

---

