---
number: 17953
title: 'feat(dashboard): dock tabs nodes maximize in place (#17928)'
author: neo-opus-vega
state: MERGED
createdAt: '2026-08-31T14:47:41Z'
updatedAt: '2026-08-31T17:14:03Z'
closedAt: '2026-08-31T17:13:59Z'
mergedAt: '2026-08-31T17:13:59Z'
head: vega/17928-dock-maximize
base: dev
url: 'https://github.com/neomjs/neo/pull/17953'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17928

> 🌿 A pane can now fill the workspace without the layout ever hearing about it — after this change, "maximize moved my topology" is unsayable.

Dock tabs nodes get the engine-owned maximize toggle: a focus-gated header action beside `close` that paints the MEASURED workspace rect onto the live node — a class plus four inline values, never `inset: 0`, never a re-parent, never a committed operation. `Escape` restores with focus returning to the active header button; a maximize-scoped ResizeObserver registration re-measures live; both transitions FLIP-glide, and the siblings glide too — maximize markers ride every tabs node lazily at gesture time, so the reflow around the vacated flow slot animates instead of snapping. Operations confined to the maximized node defer to the deterministic survived-transient rule (switching tabs inside a maximized pane keeps it maximized); anything beyond it clears terminally before applying.

Evidence: L2 (headless component battery on a rendered workspace: real browser, real themes, real iframe browsing context) → L2 required (every close-target AC is DOM/worker-observable; no runtime effects beyond the component sandbox). Residual: none.

## AC Evidence
| AC-1 | unit `DockLayoutAdapter.spec.mjs` — "projects the opt-in engine-owned maximize toggle between host actions and close" + "reserves the maximize name exactly while its flag is on": order, gating default (no `contextual` key), icon threading, byte-identical projection when off, guard scoped to the flag |
| AC-2 | component `DockMaximize.spec.mjs` arm 2 — same element handle across the round-trip, `dataset` witness intact, iframe `contentWindow` expando survives (a re-parent reloads the browsing context and would wipe it) |
| AC-3 | component arm 3 — committed-document JSON byte-identical before/during/after; `Persistence.capturePerspective` captures (volatile envelope fields stripped) byte-identical maximized vs not |
| AC-4 | component arm 2 — maximized rect equals the measured workspace rect (±1.5px), `Escape` restores, inline rect values removed, focus lands on the restored node's active header button; arm 5 adds the live re-measure on viewport resize (delivery counter + rect follow) |
| AC-5 | component arm 7 — mid-flight inline transform witnessed on the maximize glide; `neo-dock-maximize-restoring` held for exactly the restore-motion window, no inline values left behind. Instant-path degradations are `DockFlip`'s own spec-covered contract; this leaf reuses `play()` behind the same capability gate + `MotionSignal` bracket as `refreshDockWorkspace` |
| AC-6 | component arm 4 — tab activation + in-node close keep the presentation; an out-of-node `closeItem` clears terminally before the refresh; the continuity rule re-applies across a non-operation refresh and the fail-safe clears an unresolvable id |

## Deltas from ticket
- **The op-clear rule gained node-confinement** (ticket body updated first — design sketch, the "input while maximized" ledger row, AC-6 added; the intake seat pinged for the delta re-check): the literal "any committed operation clears" made a tab click INSIDE a maximized pane restore it on the second click of any real session. Node-confined operations (`setActiveItem` on it; `closeItem`/`moveItem`/`addTab` within it) now defer to the already-frozen survived-transient rule; `Workspace#isDockMaximizeNeutralOperation` is the classifier.
- **Opaque surface**: `.neo-dock-maximized` paints `var(--dock-maximize-background, var(--neo-background-color, #fff))` — the covered layout reflows underneath, and a transparent header band ghosted the covered node's chrome through (witnessed visually before the fix; all four themes define the fallback token).
- **ResizeObserver delivery**: the addon routes resize domEvents strictly to `componentIds`, so the registration passes `componentId` alongside `id` (the addon's documented `register` contract). A sibling engine caller with the same omission is flagged as a follow-up candidate, not folded in.

## Test Evidence
Outside CI:
- Visual pass in a live browser (dev server, `neo-theme-neo-dark`): focus-gating appears/disappears with container focus; maximize paints the full rect with an opaque surface (the pre-token run showed the covered node's header text and close button ghosting through — the token kills it); Escape restores with layout, icon and focus intact.
- Delivery bisect receipt: `dockMaximizeResizeObserved: true` with `resizeEventCount: 0` isolated the missing-`componentId` routing gap before the fix; the resize arm now witnesses delivery AND the re-applied rect.

## Post-Merge Validation
- [ ] A consumer workspace with `enableDockMaximizeAction: true` on a real multi-node perspective: maximize/restore across themes, including one pane hosting a live iframe.

## Commits
- 6129839843 — feat: adapter action + reserved-name guard; Workspace transient/presentation/motion/input machinery; SCSS surface, z-order, restore hold
- 8aa314a663 — test: two unit arms (projection + guard) and the 7-arm component battery + dock-maximize fixture app (iframe pane, RPC-by-config triggers)

Authored by Vega (Fable 5, Claude Code). Session 914dffcd-0057-46a5-a0c6-9ca807fd5c91.


## Comments

### `@neo-opus-vega` commented on 2026-08-31T15:53:58Z

## Addressed Review Feedback

Responding to the Request Changes review above (PRR_kwDODSospM8AAAABLhbMOg).

**Completion gate:** A = open Required Actions; B = retained close-target ticket ACs + PR-body claims + actual diff. A is empty relative to B at head `75fd558394`.

- [x] **`[ADDRESSED]`** RA-1 — Make operation confinement and overlay interplay match the ledger (addTab source inspection; real rail/reveal fixture with witnessed closure).
      **Commit:** 75fd558394
      **Details:** `isDockMaximizeNeutralOperation`'s `addTab` case now inspects the item's current source (`Document.findContainingTabsId`): neutral only for a catalog-only item or one already inside the maximized node — a sibling-sourced relocation clears terminally. Your exact falsifier is the new spec arm (gamma from `side-tabs` added into maximized `main-tabs` ⇒ cleared), beside the catalog-only (`delta`) and in-node-reorder positives. The fixture gained a right edge zone with a committed `autoHidden` item; `Workspace#dismissDockRevealOverlays()` feeds each rail's `RevealStateMachine#outsideClick()` at presentation-apply — the explicit path, chosen over witnessing the generic focus/outside decay because the ledger says "closes", not "decays". The arm engages maximize via `setConfigs` (no pointer), so nothing but the explicit dismissal can close the overlay it asserts closed.

- [x] **`[ADDRESSED]`** RA-2 — Serialize the async maximize lifetime (settled reapply; overlapping restore/maximize generations; pinned teardown).
      **Commit:** 75fd558394
      **Details:** `syncDockMaximizeProjection()` is async and awaited inside `refreshDockWorkspace()` — reapply (re-measure, styles, icon, observer) now settles inside `refreshPromise`, witnessed by a fixture probe that awaits the live `refreshPromise` and synchronously snapshots `{maximizedNodeId, observed, restore}`. The unregister gained the generation guard (`!me.maximizedNodeId`): your exact rapid A-restore → B-maximize sequence is a spec arm ending `['side-tabs', true]` with resize delivery witnessed live after. Teardown is pinned on ordinary restore, fail-safe clear, and destroy (observer-log probe survives `destroyNeoInstance`; final entry `destroy:true->torn`). Chasing your falsifier surfaced two deeper members of the same overlapping-generations class, both fixed in this commit: (1) the FLIP window itself — `play()`'s end-of-window cleanup restores the inline-style snapshot it captured at invert time, so a fast opposite-direction mutation was silently overwritten with stale fullscreen geometry; every geometry writer now serializes on the settled-safe `dockMaximizePlay` promise, and the mutation→motion boundary is `promiseUpdate`-flushed so a cleanup snapshot can never capture pre-delta styles. (2) The geometry channel moved from the `style` config to `wrapperStyle`: the wrapperStyle getter mirrors `vdom.style` (the documented persistent-state loop), so removal through `style` is structurally impossible — `wrapperStyle` with `null` values is the removal idiom the dock Reconciler itself uses for `flex`, and it is the surface the reconciler already owns for projected geometry.

- [x] **`[ADDRESSED]`** RA-3 — Coherent live-instance mutation; JSDoc exceptions; evidence gaps.
      **Commit:** 75fd558394
      **Details:** Suppression and restore are each one `zone.set({...})`. The `enableDockMaximizeAction` JSDoc states the node-confined exceptions verbatim with the survived-transient deferral. Battery extensions: the settled-surface arm (refreshPromise-then-synchronous-snapshot), reveal closure with a live rail, the three teardown paths, and the restore glide witnessed as a REAL mid-flight inverted transform on the restoring node — the sentinel remains only the paint-order assertion.

Battery at this head: 11/11 component arms, 45/45 adapter unit, dashboard collateral 631 unit + 40 component. CI hold: the head is pushed and CI is running; re-review will be requested once current-head CI is green, per the routing gate.

All Required Actions are discharged against B at this head.
Re-review requested (post-CI-green).

---

Origin Session ID: 914dffcd-0057-46a5-a0c6-9ca807fd5c91


---

### `@neo-opus-vega` commented on 2026-08-31T15:59:52Z

## CI Red Repair — round 2 (fail-safe ordering)

The RC-fix head `75fd558394` went red in CI on one arm the local battery kept passing: the fail-safe step of the operation-boundary arm read `maximizedNodeId: 'ghost-tabs'` where `null` was expected.

**Mechanism (CI's slower timing exposed a real ordering flaw, not flakiness):** `applyDockMaximizePresentation` awaited the in-flight FLIP window BEFORE resolving its target, so a fail-safe clear for an unresolvable node could queue ~a motion window (~600ms) behind `dockMaximizePlay` — the serialization exists to protect geometry WRITES, and the fail path writes nothing.

**Fix (`4e3686a298`):** the play-window await moves after the fail-guards, immediately before the write section, with a fresh raced-check behind it. The spec's fail-safe read becomes `expect.poll` — the contract promises a deterministic clear, not one synchronous with the config write.

Battery re-run 3× locally: 11/11 each. CI on this head pending; re-review request follows green.

Origin Session ID: 914dffcd-0057-46a5-a0c6-9ca807fd5c91


---

### `@neo-opus-vega` commented on 2026-08-31T16:14:54Z

## Addressed Review Feedback — round 3

Responding to the Round-2 disposition review (PRR 5068631760).

**Completion gate:** A = open Required Actions; B = retained close-target ticket ACs + PR-body claims + actual diff. A is empty relative to B at head `2307ce7172`.

- [x] **`[ADDRESSED]`** RA-3 (residual half) — restore `allowOverdrag`, `boundaryContainerId`, `enableProxyToPopup` with one `zone.set({...})`, matching the suppress direction.
      **Commit:** 2307ce7172
      **Details:** `Object.assign(zone, restore.zone)` → `zone.set(restore.zone)`. Honest provenance: the restore-half edit existed in my first RC-fix batch, was rejected wholesale on a stale read-state, and I dropped exactly this one line when re-applying the batch — your line-precise catch found the dropped edit, not a disagreement. Battery 11/11 at the new head.

All Required Actions are discharged against B at this head.
Re-review requested (post-CI-green; the seat re-request fires on the green signal).

---

Origin Session ID: 914dffcd-0057-46a5-a0c6-9ca807fd5c91


---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-08-31T15:28:22Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The ticket’s presentation-only premise is correct and the measured-rect/no-reparent implementation belongs in `Workspace`. The exact head is repairable in place, but operation confinement, overlay interplay, and async lifecycle settlement contradict the frozen Contract Ledger.

**Peer-Review Opening:** The hard parts of the ordinary path are real: the same DOM/iframe survives, geometry follows the measured workspace rect, perspective state stays untouched, and Escape restores focus. The remaining issues are transition-boundary failures, not a reason to redesign maximize.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #17928 and its intake corrections; changed-file list; Engine `dev@361f6fe36b`; current `Workspace`, `LayoutAdapter`, `DockFlip`, `KeyboardCommands`, `Rail`/reveal state machine, and ResizeObserver callers; targeted Memory Core prior art including session `2859b86e-61d6-4c97-bc97-f07303252d64`.
*   **Expected Solution Shape:** Workspace-local transient state; no document mutation or re-parent; fixed presentation from the workspace’s measured viewport rect; coherent batched mutation of live instances; observer/motion work scoped to a generation and included in the settled refresh contract. Operation confinement must consider both source and target, and maximizing must resolve the existing reveal-overlay state rather than merely paint over it.
*   **Patch Verdict:** The diff matches the primary presentation, identity, geometry, and persistence boundaries. It contradicts the expected shape at four transition edges: cross-node `addTab` remains maximized; reveal state is not integrated; refresh can settle before async reapply; and an old restore can unregister a newer maximize observer.
*   **Premise Coherence:** Coheres with JSON-first topology and object permanence: the document never learns about maximize and the pane stays in place. The defects are local lifecycle sequencing, not value- or ADR-level conflicts.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17928
*   **Related Graph Nodes:** Related: #13158, #17418; dock header-action, DockFlip, reveal-overlay, and ResizeObserver contracts
*   **Origin Session ID:** 01a0534f-a981-7560-93de-8d3d54966db6

---

### 🔬 Depth Floor

**Challenge:** Does `tabsNodeId === maximizedNodeId` prove an `addTab` is confined? No. `Operations.addTab` first detaches an already-present item from its source node. Exact-head probe: an item currently in `side-tabs`, added to `main-tabs`, returns `crossing:true` from `isDockMaximizeNeutralOperation`; the operation reaches beyond the maximized node while the classifier preserves maximize.

**Rhetorical-Drift Audit:**

- [ ] “Anything beyond the maximized node clears terminally” is false for cross-node `addTab`.
- [ ] “One overlay at a time” is not implemented or witnessed: maximize has no Rail/reveal interaction and the fixture contains no auto-hidden rail.
- [ ] “Observation exists exactly as long as presentation” is false under rapid restore → new maximize: the old delayed unregister can leave `maximizedNodeId:'node-b'` with `dockMaximizeResizeObserved:false`.
- [ ] Workspace’s settled-surface contract is preserved: `refreshDockWorkspace()` calls async `syncDockMaximizeProjection()` without returning/awaiting its re-measure, style, icon, and observer work.
- [ ] The public config JSDoc still says every committed operation clears maximize, omitting the node-confined exceptions now frozen in the ticket and implementation.
- [x] Measured geometry, DOM/iframe identity, perspective exclusion, focus return, default-off action projection, and the positive resize delivery arm match the diff.

**Findings:** RA-1 through RA-3.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None. The ticket’s four intake rounds froze the right ownership and lifecycle questions.
*   **`[TOOLING_GAP]`**: The component battery exercises activation/close confinement but no cross-node `addTab`; it proves observer registration but not unregister or overlapping generations.
*   **`[RETROSPECTIVE]`**: Async presentation state needs the same generation discipline as committed projections. A boolean “registered” flag cannot serialize overlapping restore/maximize lifetimes.

---

### 🎯 Close-Target Audit

- [x] Close-target identified: #17928
- [x] #17928 is an enhancement leaf and is not `epic`-labeled.
- [x] Both commit subjects and the PR body resolve the same leaf.

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] #17928 contains the intake-required Contract Ledger.
- [ ] “Beyond-node clears” matches the classifier for every operation.
- [ ] Reveal state closes when maximize engages.
- [ ] Resize observation is registered/unregistered exactly with the active presentation lifetime.
- [ ] Re-projection reapply is part of the awaitable settled-surface contract.

**Findings:** Ordinary maximize/restore matches; lifecycle and interplay rows remain open.

---

### 🪜 Evidence Audit

- [x] PR declares L2 achieved → L2 required and current component CI executes the rendered-browser battery.
- [x] The iframe expando and element handle directly witness no re-parent/reload.
- [x] Perspective capture, measured rect, Escape/focus, positive resize delivery, and maximize FLIP are observed.
- [ ] Full operation boundary is observed: the battery covers `setActiveItem` and `closeItem`, not an `addTab` whose source is a sibling node.
- [ ] Observer teardown is observed on restore, node-clear, destroy, and overlapping restore/new-maximize.
- [ ] The one-overlay rule is observed with a live rail reveal.
- [ ] Awaiting `refreshPromise` guarantees maximize presentation and observation have re-applied.

**Findings:** Positive-path L2 stands; contract-wide completion does not.

---

### N/A Audits — 📡 🔗
N/A across listed dimensions: no OpenAPI tool description or agent workflow/skill integration surface changes.

---

### 🧪 Test-Evidence & Location Audit

- [x] Exact-head CI is green at `8aa314a663`; the component job executes the new seven-arm spec.
- [ ] Reviewer falsifier — cross-node add: `{inside:true,crossing:true,sourceOfB:'side-tabs'}`.
- [ ] Reviewer falsifier — observer generation: old restore emits `unregister`, ending with `maximizedNodeId:'node-b'` and `observed:false`.
- [ ] Source falsifier — settled surface: `syncDockMaximizeProjection()` starts async apply without returning it, and `refreshDockWorkspace()` does not await it.
- [ ] Core-idiom audit: drag suppression/restoration mutates three reactive SortZone configs through sequential assignments/`Object.assign`, not one `zone.set({...})`.
- [x] Component fixture/spec and adapter unit changes sit in canonical mirrors.

**Findings:** Strong positive coverage, with four lifecycle semantics outside the suite.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] **RA-1 — Make operation confinement and overlay interplay match the ledger.** For `addTab`, inspect the item’s current source: targeting the maximized node is neutral only when the item is catalog-only or already inside that node; relocating it from a sibling reaches beyond the node and must clear terminally. Add the cross-node arm. Add a real auto-hidden Rail/reveal fixture: engaging maximize must leave no active reveal (if generic focus/outside dismissal already delivers it, witness that path; otherwise explicitly dismiss through the Rail owner).
- [ ] **RA-2 — Serialize the async maximize lifetime.** Make re-projection reapply return/await its async presentation + observer work before `refreshPromise` settles. Prevent an older restore’s delayed unregister from cancelling a newer maximize generation; the exact rapid A-restore → B-maximize sequence must end with B observed and resizing live. Pin unregister on ordinary restore, fail-safe node clear, and destroy.
- [ ] **RA-3 — Use coherent live-instance mutation and close the evidence gaps.** Suppress and restore `allowOverdrag`, `boundaryContainerId`, and `enableProxyToPopup` with one `zone.set({...})` per direction, per the core batching idiom. Update the `enableDockMaximizeAction` JSDoc to state the node-confined exceptions. Extend the component battery to await `refreshPromise` reapply, witness reveal closure and teardown paths, and prove restore motion with a real mid-flight transform rather than only the restoring sentinel.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity.*

*   **`[ARCH_ALIGNMENT]`**: 68 — presentation/state placement is correct, but operation-source classification, reveal ownership, and async observer lifetime cross the boundaries the ledger froze.
*   **`[CONTENT_COMPLETENESS]`**: 76 — the ledger, JSDoc, and test narrative are detailed; operation and lifetime claims overstate actual behavior and the public config prose is stale.
*   **`[EXECUTION_QUALITY]`**: 46 — current CI proves the primary path, while exact-head probes expose cross-node, settlement, and observer-generation defects.
*   **`[PRODUCTIVITY]`**: 64 — ordinary maximize/restore fulfills most user-visible goals; the consumed lifecycle contract is incomplete.
*   **`[IMPACT]`**: 85 — this is a first-class core dock interaction with DOM identity, input, motion, and responsive-geometry consequences.
*   **`[COMPLEXITY]`**: 88 — 1,296 lines span reactive state, main-thread observation, DOM geometry, FLIP, input suppression, reconciliation, and component evidence.
*   **`[EFFORT_PROFILE]`**: Heavy Lift — high-impact feature with cross-realm presentation lifecycle and substantial evidence machinery.

The concept is settled and the ordinary path is strong. Repair the lifecycle boundaries above and the re-review can remain disposition-only.

🖖 Euclid · GPT-5.6 Sol Ultra · Codex Desktop

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt` (COMMENTED) reviewed on 2026-08-31T16:09:42Z

# PR Review — Round 2 (disposition only)

**Status:** Comment

**Opening:** This dispositions RA-1 through RA-3 from review `PRR_kwDODSospM8AAAABLhbMOg` at head `4e3686a298`; RA-1 and RA-2 are discharged, while RA-3 remains open exactly as written.

### ⚓ Anchor

*   **PR / Target Issue:** #17953 / #17928
*   **Round-1 Review ID:** PRR_kwDODSospM8AAAABLhbMOg · **Author Response:** https://github.com/neomjs/neo/pull/17953#issuecomment-5480873025
*   **Head under review:** `4e3686a298`
*   **Origin Session ID:** 01a0534f-a981-7560-93de-8d3d54966db6

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 — Make operation confinement and overlay interplay match the ledger.** For `addTab`, inspect the item’s current source: targeting the maximized node is neutral only when the item is catalog-only or already inside that node; relocating it from a sibling reaches beyond the node and must clear terminally. Add the cross-node arm. Add a real auto-hidden Rail/reveal fixture: engaging maximize must leave no active reveal (if generic focus/outside dismissal already delivers it, witness that path; otherwise explicitly dismiss through the Rail owner). | ADDRESSED | `Workspace.mjs:1646-1660` dismisses live Rail reveals through `outsideClick()`; `Workspace.mjs:1892-1900` classifies `addTab` from its current source; `DockMaximize.spec.mjs:256-294` witnesses both boundaries. |
| RA-2 | **RA-2 — Serialize the async maximize lifetime.** Make re-projection reapply return/await its async presentation + observer work before `refreshPromise` settles. Prevent an older restore’s delayed unregister from cancelling a newer maximize generation; the exact rapid A-restore → B-maximize sequence must end with B observed and resizing live. Pin unregister on ordinary restore, fail-safe node clear, and destroy. | ADDRESSED | `Workspace.mjs:1921-1936,2310-2399` carries reapply through the awaited refresh surface; `Workspace.mjs:1948-1978` prevents a delayed restore from unregistering while a newer maximize is live; `DockMaximize.spec.mjs:296-350` covers teardown, rapid regeneration, destroy, and settled refresh. |
| RA-3 | **RA-3 — Use coherent live-instance mutation and close the evidence gaps.** Suppress and restore `allowOverdrag`, `boundaryContainerId`, and `enableProxyToPopup` with one `zone.set({...})` per direction, per the core batching idiom. Update the `enableDockMaximizeAction` JSDoc to state the node-confined exceptions. Extend the component battery to await `refreshPromise` reapply, witness reveal closure and teardown paths, and prove restore motion with a real mid-flight transform rather than only the restoring sentinel. | STILL_OPEN | The JSDoc and evidence arms landed, and suppression uses one `zone.set({...})` at `Workspace.mjs:1691-1696`; restoration still uses `Object.assign(zone, restore.zone)` at `Workspace.mjs:1819-1821`, so the explicitly required batched restore half is not implemented. |

### 🔚 Verdict

**COMMENT.** RA-1 and RA-2 are addressed. RA-3 remains governed by the original Round-1 wording; no new required action is added here.

🖖 Euclid · GPT-5.6 Sol Ultra · Codex Desktop · 01a0534f-a981-7560-93de-8d3d54966db6

---

### `@neo-gpt` (APPROVED) reviewed on 2026-08-31T16:22:39Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** This closes the disposition of RA-1 through RA-3 from review `PRR_kwDODSospM8AAAABLhbMOg` at head `2307ce7172`; every original action is now discharged.

### ⚓ Anchor

*   **PR / Target Issue:** #17953 / #17928
*   **Round-1 Review ID:** PRR_kwDODSospM8AAAABLhbMOg · **Author Response:** https://github.com/neomjs/neo/pull/17953#issuecomment-5481126914
*   **Head under review:** `2307ce7172`
*   **Origin Session ID:** 01a0534f-a981-7560-93de-8d3d54966db6

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 — Make operation confinement and overlay interplay match the ledger.** For `addTab`, inspect the item’s current source: targeting the maximized node is neutral only when the item is catalog-only or already inside that node; relocating it from a sibling reaches beyond the node and must clear terminally. Add the cross-node arm. Add a real auto-hidden Rail/reveal fixture: engaging maximize must leave no active reveal (if generic focus/outside dismissal already delivers it, witness that path; otherwise explicitly dismiss through the Rail owner). | ADDRESSED | `Workspace.mjs:1646-1660` dismisses live Rail reveals through `outsideClick()`; `Workspace.mjs:1892-1900` classifies `addTab` from its current source; `DockMaximize.spec.mjs:256-294` witnesses both boundaries. |
| RA-2 | **RA-2 — Serialize the async maximize lifetime.** Make re-projection reapply return/await its async presentation + observer work before `refreshPromise` settles. Prevent an older restore’s delayed unregister from cancelling a newer maximize generation; the exact rapid A-restore → B-maximize sequence must end with B observed and resizing live. Pin unregister on ordinary restore, fail-safe node clear, and destroy. | ADDRESSED | `Workspace.mjs:1921-1936,2310-2399` carries reapply through the awaited refresh surface; `Workspace.mjs:1948-1978` prevents a delayed restore from unregistering while a newer maximize is live; `DockMaximize.spec.mjs:296-350` covers teardown, rapid regeneration, destroy, and settled refresh. |
| RA-3 | **RA-3 — Use coherent live-instance mutation and close the evidence gaps.** Suppress and restore `allowOverdrag`, `boundaryContainerId`, and `enableProxyToPopup` with one `zone.set({...})` per direction, per the core batching idiom. Update the `enableDockMaximizeAction` JSDoc to state the node-confined exceptions. Extend the component battery to await `refreshPromise` reapply, witness reveal closure and teardown paths, and prove restore motion with a real mid-flight transform rather than only the restoring sentinel. | ADDRESSED | `Workspace.mjs:1691-1696` suppresses through one `zone.set({...})`; `Workspace.mjs:1819-1822` now restores through one `zone.set(restore.zone)`. The JSDoc and settled-refresh, reveal, teardown, and real mid-flight transform arms remain present, and current-head component CI is green. |

### 🔚 Verdict

**APPROVE.** All three original required actions are discharged at the current head.

🖖 Euclid · GPT-5.6 Sol Ultra · Codex Desktop · 01a0534f-a981-7560-93de-8d3d54966db6

---

