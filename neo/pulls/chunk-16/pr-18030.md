---
number: 18030
title: 'fix(data): TreeStore resolves child membership by key, not identity (…'
author: vansh2408
state: MERGED
createdAt: '2026-09-01T17:42:25Z'
updatedAt: '2026-09-01T19:01:53Z'
closedAt: '2026-09-01T19:01:53Z'
mergedAt: '2026-09-01T19:01:53Z'
head: fix/17855-treestore-child-key-dedup
base: dev
url: 'https://github.com/neomjs/neo/pull/18030'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
**Does this PR resolve an issue?** (Required)

Closes #17855

**What kind of change does this PR introduce?** (check at least one)

- [x] Bugfix
- [ ] Feature
- [ ] Code style update
- [ ] Refactor
- [ ] Build-related changes
- [ ] Other, please describe:

**Does this PR introduce a breaking change?** (check one)

- [ ] Yes
- [x] No

`add()` of an already-present key previously produced a duplicate child entry. Code relying on that duplication was relying on a defect; nothing relies on it in-tree.

**The PR fulfills this requirement:**

- [x] It's submitted to the `dev` branch, _not_ the `main` branch

**Other information:**

### The defect

`#allRecordsMap` is keyed, so re-adding a node overwrote its entry. `#childrenMap` holds arrays and tested membership with `Array#includes` — object identity, which an equal-but-distinct literal never satisfies. The literal was appended beside the object it had just replaced, so the child array held a stale node and a live one under the same key:

```javascript
store.add({id: 'Group', parentId: 'root', name: 'Group', isLeaf: false});
store.add({id: 'Group', parentId: 'root', name: 'Group', isLeaf: false});

store.getCount();                // 1  — correct
store.getChildren('root');       // ['Group', 'Group']  — WRONG
store.get('Group').siblingCount; // 2  — WRONG
```

`updateSiblingStats()` derives `siblingCount` from that array's length, so a screen reader was told "1 of 2" for a level holding one node — view-invisible, since the tree renders correctly and only reports itself incorrectly.

### The fix

Child-array membership now resolves by key, in a single `#addChild()` helper used by **both** identity tests in the "Process Additions & Moves" block — the main ingestion path and the auto-heal branch that re-parents an orphan to `'root'`. Fixing only the first leaves a re-added orphan duplicating; there is a spec for that specifically.

An existing key is replaced in place, so both maps end up pointing at the same record: what `getChildren()` returns is what `get()` returns. The same-reference fast path still no-ops, and `affectedParents` grows only when the array actually changed, so `updateSiblingStats()` runs for a replacement and not for a no-op.

`splice()` also now assigns `depth: 0` when it re-parents a node to `'root'`. `depth` was resolved against the declared parent — the one that turned out to be absent — so the value it produced described a level the node does not end up on.

### Performance

The insert takes the previous record from `#allRecordsMap` (captured before the overwrite) rather than scanning for the key. A key the Structural Layer has never held cannot be in any child array, so ingesting a genuinely new node performs no scan at all — where the old code ran an `includes()` scan per node.

| Ingesting 20k flat siblings | Time |
|---|---|
| `dev` | 526 ms |
| naive key scan (rejected) | 11,627 ms |
| this PR | 453–571 ms |

The middle row is why the helper takes `existing` as a parameter: a per-node key scan makes bulk ingestion O(N²) with a record getter on every comparison, in the one class whose premise is 50k-record ingestion.

### Tests

- Six new specs in `test/playwright/unit/data/TreeStore.spec.mjs` covering each AC: single child entry after a re-add, unchanged `siblingCount` / `childCount`, the re-added values winning with the stale object unreachable, the same-reference no-op, `depth: 0` on auto-heal, and the re-added orphan going through the second membership test.
- The CONTROL at `TreeStorePathMaterialization.spec.mjs:140` is **inverted** with its comment rewritten. It asserted the defect deliberately; left as-is it would have been a false green.
- `learn/guides/datahandling/TreeStore.md` gains an "Adding a node that already exists" section documenting the keyed semantics, that a re-add is a replace and not a merge, and the auto-heal `depth`.

Verified with `npm run test-unit` (the repo's explicit config, not bare `npx playwright test`): **2668 passed**. One failure, `apps/workstation/Workspace.spec.mjs` › "remote main previews render all four edges…", **reproduces identically on clean `dev` with this branch stashed** — pre-existing and unrelated. `npm run test-components` shows two flaky `dashboard/Dock*` specs that also fail on clean `dev` and alternate between runs.

### Out of scope — two findings for follow-up

**1. A re-add that changes `parentId` leaves a stale entry under the old parent.** `add({id: 'X', parentId: 'B'})` for an `X` currently under `A` leaves `X` reachable through `getChildren('A')` as the stale object, with `A.childCount` still 1. This is the same trap the ticket names, reached via a different route. It behaves identically on `dev` — this PR neither fixes nor worsens it — and the ACs are all same-parent, so I left it alone. Happy to open a ticket if you want it scoped.

**2. The Projection Layer keeps the stale object after a re-add.** `store.getAt(0)` still returns the replaced record while `store.get(…)` returns the new one, because `Neo.collection.Base#splice` skips an existing key on add and explicitly refuses same-key replacement in its remove branch. That is a Collection-wide contract question affecting every store, not a `TreeStore` one.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-01T18:47:41Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: Correct at mechanism, scoped exactly to the ticket, and every claim it makes is checkable — I checked them. Both fixes carry independent mutation controls, and the JSDoc pre-empts the one architectural objection this class attracts instead of waiting for a reviewer to raise it.

**Peer-Review Opening:** Welcome, and this is a strong first contribution — stronger than a lot of what lands here. Two things stand out. You wrote a **CONTROL** test (*"CONTROL: a raw `add()` of the same node does not duplicate it either"*), which is the discipline that stops an arm from passing for the wrong reason, and most first PRs do not have it. And you anticipated the O(N²) objection with a measurement rather than an argument. I went looking for reasons to push back and found the reasoning already there.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #17855's body and ACs, current `TreeStore.mjs` on `dev`, and a Memory Core sweep of the TreeStore decision space — which surfaced the prior art that shaped this review: `#allRecordsMap` is the keyed authority that `get()` falls back to; `siblingCount`/`siblingIndex` are model fields deliberately maintained at write time to buy O(1) reads during 60–120fps virtual scroll; and a previous proposal to rebuild maps during `filter()`/`doSort()` was **rejected outright** for large-dataset cost.
*   **Expected Solution Shape:** membership resolved by key rather than reference, with `#allRecordsMap` staying the single keyed authority — and, given that prior art, **without** adding a per-node scan to bulk ingestion. A re-add should replace in place so both maps point at one record, and derived stats should recalculate for a real replacement but not for a no-op.
*   **Patch Verdict:** **Matches, and answers the performance constraint before I could raise it.** `#addChild()` takes the pre-overwrite `existing` from the caller, so a genuinely new node — the common ingestion path — never scans a level at all; only a real re-add pays a lookup. The identity check runs first with a key scan as the drift fallback, and `affectedParents` grows only when the array actually changed.
*   **Premise Coherence:** coheres: **verify-before-assert** — the JSDoc states a measured figure (20k flat siblings, 0.5s versus 11.6s) rather than asserting the design is fast.

---

### 🕸️ Context & Graph Linking

*   **Target Epic / Issue ID:** Closes #17855
*   **Related Graph Nodes:** #9411, #9430, #9435 (the TreeStore projection/mutation lineage)
*   **Origin Session ID:** a4fa24e6-b983-4ecd-81a2-8b98baef9cfc

---

### 🔬 Depth Floor

**Documented search.** *"I actively checked the ticket's stated scope against the diff, revert exposure against current `dev`, both fixes under independent mutation, and the re-parenting case the diff does not cover, before reaching this verdict."*

**The depth change is in scope, not scope creep.** My first instinct was that `data.depth = 0` in the auto-heal branch was a second fix riding along. It is not: #17855 raises it in the body (*"an auto-healed root node keeps `depth: 1`... cheap to fix alongside"*) and carries it as its own AC. It also has its own test and its own mutation control. Good — a bundled fix without a ticket line is exactly what I would have asked you to split out.

**Non-blocking observation, and explicitly out of scope.** Re-adding a node with a *different* `parentId` inserts it under the new parent without removing it from the old one, so it becomes reachable under both. That is **pre-existing** — the code you replaced (`if (!includes(data)) push`) did not remove either — so this PR neither introduces nor worsens it, and #17855 scopes the re-parenting behaviour out by name (*"only its `depth` inconsistency is in scope here"*). Raising it only so it is written down somewhere; do not widen this PR for it.

---

### 🧠 Graph Ingestion Notes

*   **`[RETROSPECTIVE]`**: the generalisable defect here is a **keyed map and an array-of-objects disagreeing about identity**. One overwrote by key while the other scanned by reference, so a re-read literal satisfied neither — and the symptom surfaced in the least obvious place: `updateSiblingStats()` publishing the array length as `siblingCount`, so a one-node level announced "1 of 2" to a screen reader while the rendered tree looked perfect. Worth remembering that a duplicate in a derived structure can present first as an accessibility lie.
*   **`[KB_GAP]`**: nothing states that `#allRecordsMap` and `#childrenMap` must agree on identity. This PR makes them agree; the invariant is still only implied.

---

### N/A Audits — 📑 📡 🔗

N/A: no contract-ledger surface, no MCP/OpenAPI tool descriptions, no cross-skill or substrate convention changes.

---

### 🎯 Close-Target Audit

`Closes #17855` is correct, and the PR body declares no open or pre-merge work. I read the ticket's ACs against the diff cell by cell rather than trusting the close keyword — including the auto-heal `depth` AC, which is the one a reader would most easily miss.

---

### 🪜 Evidence Audit

Exact-head CI green (16 SUCCESS, 1 rollup row), `mergeStateStatus` CLEAN. Run by me at `634b81535c`:

```
unit/data/TreeStore*                    62 passed
```

**Revert exposure: none.** The branch base is `68efc42bb2` while `dev` is `dfcbda2959`, so it is behind — but the overlap between what `dev` changed since the base and what this PR touches is **empty**, and nothing on `dev` has touched `src/data/TreeStore.mjs` or its specs. So being behind costs nothing here; a rebase is optional rather than required.

**One claim I did not reproduce:** the 0.5s versus 11.6s figure over 20k siblings. I accepted it as a design rationale, and the design is sound independently of the exact numbers, since the common path provably does not scan.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: 62 passed at the exact head, run by me.
- [x] Reviewer falsifier — **both fixes mutation-controlled independently**, run by me:

```
mutation — key-membership resolution disabled     6 failed · 56 passed
mutation — `data.depth = 0` removed               1 failed · 61 passed
```

  Neither fix is decorative, and neither is carried by the other's tests.
- [x] Test location: specs sit beside the existing `unit/data/TreeStore` suite, split across the behaviour file and the path-materialization file that each already owns its surface.
- [x] Non-vacuity: your own CONTROL arm covers it.

**Findings:** none.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 93 — keeps `#allRecordsMap` as the single keyed authority and refuses to put a scan on the bulk-ingest path, which is the constraint this class exists to honour.
*   **`[CONTENT_COMPLETENESS]`**: 95 — the JSDoc explains the defect, the symptom, the fix, *and* why the parameter shape is what it is; the guide is updated alongside. This is above the bar, not at it.
*   **`[EXECUTION_QUALITY]`**: 92 — identity-first with a key-scan fallback for drifted entries, a no-op short circuit, and `affectedParents` gated on real change. Seven tests including a deliberate control.
*   **`[PRODUCTIVITY]`**: 90 — closes the ticket including its secondary AC, in one pass.
*   **`[IMPACT]`**: 82 — every TreeStore consumer; the former failure mode published a wrong `siblingCount` to assistive tech.
*   **`[COMPLEXITY]`**: 40 — one new private method plus two call-site replacements.
*   **`[EFFORT_PROFILE]`**: Quick Win — nothing between this and human merge.

Merge is @tobiu's call. Thanks for picking a ticket with a subtle invariant rather than an easy one — and for the control arm.

🖖 Vega (Opus 5, Claude Code) 🌿

---

