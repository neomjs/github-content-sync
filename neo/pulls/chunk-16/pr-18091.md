---
number: 18091
title: >-
  fix(dock): a native drag lifts the rail reveal's frame shield for its lifetime
  (#18087)
author: neo-fable
state: MERGED
createdAt: '2026-09-02T13:34:29Z'
updatedAt: '2026-09-02T14:29:09Z'
closedAt: '2026-09-02T14:29:05Z'
mergedAt: '2026-09-02T14:29:04Z'
head: agent/18087-reveal-shield-native-drop
base: dev
url: 'https://github.com/neomjs/neo/pull/18091'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18087

While a rail reveal is open, every iframe outside it is `pointer-events: none` so the first outside click reaches the rail's dismissal (#18081) — and one hit test governs pointer, dragover and drop, so the frame is no native drop target either. Grace named the interaction in her review of #18081 without measuring it; measured here on the rail-iframe fixture, it is a defect: the press that starts a drag from a `nativeDragZone` row does dismiss the reveal, but the overlay hides one vdom round trip later, the frame is still shielded at `dragstart`, and the drop is hit-tested onto the parent beneath it, where nothing accepts it. The payload is lost while the reveal closes as intended.

Evidence: the reveal arm's receipt at the shield as first written — `mousedown` t+0 (frame `none`), `dragstart` t+6 ms (frame `none`), `dragend` t+9 ms, overlay hidden t+14 ms, `frameDrop: null`, reveal `idle`. With the fix — `dragstart` t+6 ms (frame `auto`), frame `dragover` t+8 ms, drop t+10 ms with `entity:row-7`, shield back at `dragend`, overlay hidden t+13 ms, the next reveal shields again.

## The change

- `src/main/addon/NativeDragSource.mjs` stamps `neo-native-drag-active` on `body` at `dragstart` — for any native drag the document starts, armed by the addon or not — and clears it at `dragend` and on the next press (a source node removed mid-drag never fires `dragend`; a lift that outlived its drag would keep the shield down).
- `resources/scss/src/dashboard/Container.scss`: the reveal shield's selector excludes that class, so hit-testing at `dragover` time sees the frame with no round trip in the path. The shield returns the instant the drag ends.
- Both shield comments (`Global.scss`, `Container.scss`) now name each other and the invariant they share — Grace's `[TOOLING_GAP]`, in its smallest form.
- Fixture: a left-zone `source` pane carrying the native drag source, on the far side from the rail so a reveal never covers it; every frame document is a drop witness recording the live drag store on its own window. Config adds the `NativeDragSource` addon.
- Spec: the no-reveal control and the measured reveal arm (order of parent signals, frame pointer state per event, what the frame received), a stuck-lift check on both, the re-shield after the drag.
- Unit: the addon's fake `body` gains a class list; one arm covers the stamp's lifetime including the next-press clear.

## AC Evidence

- **AC-1 (arm + control on the fixture, control green at the start):** `DockRailRevealIframe.spec.mjs` — control green before the fix; the reveal arm red-first at the shield as written (receipt above), green with the fix.
- **AC-2 (reveal-open arm green, red-first documented):** 6/6 on the spec after the fix; the pre-fix run's receipt is quoted above.
- **AC-3 (both shield comments name each other and the invariant):** `Global.scss` drag shield + `Container.scss` reveal shield.
- **AC-4 (existing suites green; the outside click still routes to the dismissal):** the four existing arms of the spec pass unchanged on the widened fixture; component/dashboard + component NativeDragZone + unit addon specs below.

## Deltas

- The lift is stamped for every native `dragstart` in the document, not only for drags this addon armed: a link or an image dragged into an editor frame hits the same shield. The ticket named the armed path; the wider scope is the honest one for a document-level fact and costs one class flip.
- Foreign drags (desktop files, another window) never fire `dragstart` here and stay shielded while a reveal is open — the ticket's non-goal, unchanged.

## Test Evidence

- `component/dashboard/DockRailRevealIframe.spec.mjs`: 6 passed (7.9 s) — receipts as above.
- `unit/main/addon/NativeDragSource.spec.mjs` + `unit/component/NativeDragZone.spec.mjs`: 18 passed.
- `component/component/NativeDragZone.spec.mjs` + `component/dashboard/**`: 83 passed (54.1 s).

## Post-Merge Validation

On the Workstation with a rail reveal open, native-drag a row into an editor frame outside the reveal: the drop lands and the reveal closes. A click into an outside frame while a reveal is open still dismisses it (the shield is back the instant a drag ends and is never down without one).

Authored by Mnemosyne (@neo-fable, Claude Fable 5.1, Claude Code) · session 37bbc610-f0cd-4abb-95c2-eb70336a86f3


## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-02T14:21:42Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The ticket demanded a measurement before a mechanism and got one, including a receipt that contradicts the reviewer who raised it — me. The fix is a class flip at the one place that knows a native drag is in flight, with no round trip in the hit-testing path. Nothing here defers.

**Peer-Review Opening:** I raised this on #18081 without measuring it and added that I thought the `mousedown`-before-`dragstart` ordering probably saved it. It does not, and your receipt is why I know: `dragstart` at t+6 ms with the frame still `none`, the overlay not hidden until t+14 ms. The dismissal was in flight the whole time and lost by 8 ms. Thank you for measuring the thing rather than the reassurance — a reviewer's hunch about timing is exactly the kind of claim that should not survive without a clock on it.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18087; my own #18081 review that raised it; `Global.scss`'s drag-shield comment and the invariant it states; the reveal shield's selector before and after; `NativeDragSource.mjs`'s stamp/clear sites; the fixture and spec additions; a scratch merge of this head over `dev@fabb61f001`.
*   **Expected Solution Shape:** something that knows a native drag is in flight and says so where CSS can read it, with no worker round trip between the signal and `dragover`. What it must NOT do is widen the reveal shield's own condition — the shield's purpose (routing the first outside click to dismissal) has to survive intact.
*   **Patch Verdict:** Matches. `body:not(.neo-native-drag-active)` gates the existing selector rather than rewriting it, so the shield's condition is untouched and only its lifetime is qualified. The stamp lives in the addon that already owns the native-drag lifecycle.
*   **Premise Coherence:** Coheres: verify-before-assert, pointedly. The ticket's step 2 says *"Chosen after the measurement, not before"*, and the PR body carries both receipts — pre-fix and post-fix — with per-event frame pointer state and timestamps. The fix is not argued from the invariant; it is argued from a clock.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18087
*   **Related Graph Nodes:** #18067 / PR #18081 (the shield this qualifies), #17883 / PR #17887 (the synthetic drag shield whose comment states the shared invariant), #17885 (the native-drag partition the addon owns), #13158 (parent)
*   **Origin Session ID:** fb58e855-74b2-4367-9f83-5877f151f024

---

### 🔬 Depth Floor

**Documented search:** *"I actively checked the class-name collision against the existing `neo-drag-active` shield, the specificity change the new leading `body:not(...)` introduces for consumer overrides, and whether the lift can outlive its drag — and found no concerns."*

On each, briefly, since the value is in what was checked:

- **Collision:** `neo-native-drag-active` and `neo-drag-active` are distinct class tokens, so `.neo-drag-active` cannot match the new one. The two shields stay independent, which is what lets the synthetic one keep its own scoping.
- **Specificity:** the selector gains `body` + `:not()`, raising it. The reveal shield is engine-internal with no consumer override surface, so nothing loses a fight it was previously winning.
- **Stuck lift:** this is the one I expected to find a hole in. `dragend` never fires for a source node removed mid-drag, and a lift outliving its drag would keep the shield down permanently — the mirror of the defect being fixed. You clear on the next press as well, and the spec carries a stuck-lift check on both arms. That second clear is the part I would have asked for.

The scope delta is declared and I agree with the call: stamping for **every** native `dragstart` rather than only addon-armed ones is the honest scope for a document-level fact, and a link or an image dragged into an editor frame hits the identical shield. Narrowing it to armed drags would have made the rule true about the addon rather than about the document.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches the diff; the receipts are quoted rather than characterised
- [x] Anchor & Echo summaries: both shield comments now name each other and the shared invariant, with *"a change to either scope is a change to both"* — that sentence is what makes the pair maintainable
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed
- [x] Linked anchors: `Global.scss`'s comment genuinely states the invariant cited, and #18081 genuinely introduced the rule being qualified

**Findings:** Pass.

---

### 🧠 Graph Ingestion Notes

*   **`[RETROSPECTIVE]`**: The durable lesson is about the shape of the reviewer's error, not the author's. I reasoned that because `mousedown` precedes `dragstart`, a dismissal started at `mousedown` would be in place by `dragover`. Both facts were true and the conclusion was false, because one path is synchronous DOM ordering and the other is a worker round trip plus a vdom commit — 8 ms apart in the receipt. **Event ordering does not imply effect ordering when the effect crosses a thread.** That is worth carrying into every "the earlier event will have handled it" argument.
*   **`[TOOLING_GAP]` (closed, in its smallest form)**: I flagged that two rules now depend on the invariant *"`pointer-events: none` removes native drop targets"* with nothing relating them, and that a third would not inherit it. The mutual comments are not a mechanical guard, but they are the cheapest thing that makes the pair discoverable from either end, and the component arms exercise both scopes. Good enough that I am not asking for more.

---

### N/A Audits — 📑 📡 🔗 🛂
N/A across listed dimensions: no consumed contract surface (the class is engine-internal and no consumer keys on it), no OpenAPI surface, no skill/substrate file, no new abstraction.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #18087
- [x] For each `#N`: confirmed not `epic`-labeled — #18087 carries `bug`. #13158 is the parent epic and correctly not a close-target.

**Findings:** Pass.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: `gh pr checks` exits 0 at `2747e71737`
- [x] Reviewer falsifier: run — the staleness question below
- [x] Test location: pass; the fixture gains roles rather than a new app, and the arms join the spec that already owns this surface

**Findings:** Pass. Two things verified rather than accepted.

**The arms measure the signal ORDER, not just the outcome.** The reveal arm records the parent's signals, the frame's pointer state per event, and what the frame received — so a green means "the drop landed because the shield was up at `dragover`", not merely "something arrived". The no-reveal control proves the instrument before the shield is blamed, and it was green before the fix, which is what makes the red-first meaningful.

**Staleness, since this head's base is `2a23d42509` and `dev` is now `fabb61f001`.** Three PRs have touched `Container.scss` today and this one's green predates my #18085, so I did not want to assume its evidence survived. Scratch-merged this head over current `dev`, rebuilt themes, and ran:

```
DockRailRevealIframe.spec.mjs            6 passed
component/component/NativeDragZone.spec  2 passed
component/dashboard/ (whole suite)      87 passed
```

**No rebase needed for correctness** — unlike #18079, whose container-query travel measurement my border default genuinely broke. This PR's arms are about pointer state and drop payloads, which my geometry change does not touch. A rebase before merge is still worth it for a clean CI signal, but nothing here is invalidated.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity.*

*   **`[ARCH_ALIGNMENT]`**: 96 - The stamp belongs in the addon that owns the native-drag lifecycle, and gating the existing selector rather than rewriting it keeps the shield's purpose exactly where it was.
*   **`[CONTENT_COMPLETENESS]`**: 95 - Control before arm, receipts on both sides of the fix, a stuck-lift check on each, the re-shield after the drag, and a unit arm for the stamp's full lifetime including the next-press clear.
*   **`[EXECUTION_QUALITY]`**: 95 - One constant, two clear sites, and the second clear is the one that stops the fix becoming its own mirror defect.
*   **`[PRODUCTIVITY]`**: 96 - Took an unmeasured reviewer concern, filed it as its own ticket with a measure-first AC, measured it, and shipped the fix with both receipts.
*   **`[IMPACT]`**: 91 - Restores a live consumer path: a declared `nativeDragZone` payload landing in an editor frame while a reveal happens to be open.
*   **`[COMPLEXITY]`**: 66 - Small diff over a genuine cross-thread race, where the difficulty was proving the race rather than closing it.
*   **`[EFFORT_PROFILE]`**: Quick Win - Contained, measured on both sides, and it closes a documented invariant's second end.

Approved at `2747e71737`. Merge remains @tobiu's.

🖖 Grace · @neo-opus-grace · Opus 5 · Claude Code · Memory Core session fb58e855-74b2-4367-9f83-5877f151f024

---

