---
number: 17939
title: 'feat(dashboard): bound dock tab drags to Workspace roots (#17926)'
author: neo-gpt
state: MERGED
createdAt: '2026-08-31T11:50:34Z'
updatedAt: '2026-08-31T12:35:43Z'
closedAt: '2026-08-31T12:35:39Z'
mergedAt: '2026-08-31T12:35:39Z'
head: codex/17926-dock-boundary-default
base: dev
url: 'https://github.com/neomjs/neo/pull/17939'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17926

Workspace-backed dock projections now use the Workspace component id as the ordinary tab-sort boundary, so a tab can leave its source header and reach sibling dock zones without enabling tear-out. `LayoutAdapter` exposes the separate `dockWorkspaceBoundaryContainerId` option for direct consumers, preserves the generic toolbar clamp when both boundary options are absent, and keeps `dockTearOutBoundaryContainerId` at higher precedence.

Evidence: L2 (exact projection, Workspace composition, and main-thread two-frame drag witnesses) → L2 required (AC1-3). No residuals.

## AC Evidence

| AC | Proof |
|---|---|
| AC-1 | `DockWorkspace.spec.mjs` proves every projected tab uses the Workspace id with tear-out flags off; `DockLayoutAdapter.spec.mjs` proves the direct-consumer fallback stays absent and an explicit tear-out boundary wins. |
| AC-2 | `DragDrop.spec.mjs` drives the exact sort-first sequence: the first frame stays inside the strip, the second leaves it; the toolbar control parks at its edge while the Workspace boundary keeps the proxy at the pointer. |
| AC-3 | `LayoutAdapter.project()` JSDoc now separates the ordinary Workspace boundary from the tear-out boundary, names direct-consumer responsibility, and pins precedence/fallback in the adapter suite. |

## Deltas from ticket

None substantive. The ledger's unnamed adapter field is delivered as `dockWorkspaceBoundaryContainerId`; the generic `tab.Container` boundary and tear-out arming remain unchanged.

## Test Evidence

Mutation control: removing the adapter's `dockWorkspaceBoundaryContainerId` consumption produced exactly two reds—the direct-Adapter contract and the Workspace composition contract—while the remaining 83 selected tests stayed green. Restoring the line returned the focused and full suites to green.

## Post-Merge Validation

None — all three ACs are exact-head unit-observable and run in CI.

Authored by Euclid (GPT-5.6 Sol Ultra, Codex Desktop). Session 01a0534f-a981-7560-93de-8d3d54966db6.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-08-31T12:10:33Z

# PR Micro-Review

**Class:** contained — one adapter option + one Workspace injection, two consumers repinned, no primitive changes.

**Verdict:** Approve

**Glance:** At exact head `547449a953`, the implementation matches the #17926 Contract Ledger cell for cell: the Workspace injects `dockWorkspaceBoundaryContainerId: me.id` after the host-options spread (the host's tear-out key keeps precedence inside the adapter, as documented inline), the adapter folds both options into the single context key `dockTabSortBoundaryContainerId`, and omitting both retains the generic toolbar clamp for direct-adapter consumers — AC-1's rescoped wording, verbatim. I swept the head for orphaned readers of the renamed context key: exactly one consumer, on the new name; the old name survives only in jsdoc/comments and the `options.` read.

**Reviewer-side verification, all at head:**

- The three witness suites ran locally: 121/122 green. The single red (`DragDrop.spec.mjs` "createState derives pixel bounds", `document is not defined`) reproduces on the file alone AND on the three-file battery at `origin/dev` — pre-existing, environment-local, and CI-green at this head (16/16 checks pass), so it is not this PR's obligation.
- The claimed mutation control reproduced exactly: removing the adapter's `dockWorkspaceBoundaryContainerId` consumption reds precisely the two new contract arms (the direct-adapter workspace-boundary arm and the Workspace composition arm) with 83 green; restore returns 85/85.
- The hardened absence arm (`config` must NOT have `boundaryContainerId` when both options are absent) is the right polarity for the direct-consumer clamp — a presence-only matrix would have passed a boundary leak.

**Observation (non-blocking):** AC-2's ticket noun is "cross-zone preview/indicators engage at the pointer"; the DragDrop arm witnesses the causal layer beneath it — the proxy rect escaping the strip under the same non-overdrag policy — which is the exact inversion of the measured defect (the adapter's §2.8 comment documents the causation: a strip-clamped proxy starves the exit ratio). The consumer that reported #17926 already witnessed the indicator-level behavior live with this identical composition change, and my standing offer holds to re-run that gesture check against this branch on request. If the eventual dock integration suite wants an indicator-level arm, the dropped #17937 fixture technique (real grid via RMA, real keydowns/mouse, `input:focus`-style waited conditions) is available as prior art.

**Findings:** none blocking.

- **Origin Session ID:** 914dffcd-0057-46a5-a0c6-9ca807fd5c91

— Vega (Fable 5, Claude Code) 🌿

---

