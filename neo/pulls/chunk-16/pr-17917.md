---
number: 17917
title: Restore the dependency-free half of the PR-body gate (#17916)
author: neo-opus-grace
state: CLOSED
createdAt: '2026-08-31T06:43:34Z'
updatedAt: '2026-08-31T08:34:16Z'
closedAt: '2026-08-31T08:34:16Z'
mergedAt: null
head: fix/restore-pr-body-lint-gate-17916
base: dev
url: 'https://github.com/neomjs/neo/pull/17917'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17916

Restores the PR-body anchor gate that has been absent from every repository since 2026-08-26. **One file, +205 lines, no source or test surface touched.**

Evidence: L3 (the gate is exercised live by this PR — its own run is the positive control, and the negative control below drives it red and back to green) → L3 required. No residuals.

## What was actually wrong

`pull-request-workflow.md` §9 line 337 tells every seat that `agent-pr-body-lint.yml` enforces six anchors as **unconditional**. That file was deleted in `91ae3604b4` (`fix(engine): remove received Brain substrate`, #17791) and exists in no repository — not here, not the Brain, not Skills.

It went out as collateral, not as a decision. The workflow ran **three** steps; two invoked `ai/scripts/*` that moved with the split, so the file could not run and was swept out with the mover set:

| step | implementation | Brain-dependent? |
|---|---|---|
| `Validate PR body with the owning implementation` | `node ai/scripts/agent-preflight.mjs` — the AC-Evidence **content** check | yes |
| `Validate PR Body` | inline `actions/github-script@v9` — anchor **presence** | **no — none at all** |
| `Stacked-PR guard` | `node ./ai/scripts/lint/lint-pr-stacking.mjs` | yes |

The third step needs no checkout, no `npm ci`, no Brain, and no cross-repository caller decision. It is the entire content of this PR.

## Why this was worth doing tonight rather than filing and moving on

It has a live victim, and the victim is not hypothetical. PR #17902 — APPROVED cross-family at Round 2, CLEAN, checks exit 0 — carried a body missing `## AC Evidence` and `## Test Evidence`, and **merged into `dev` at 06:31:27Z** carrying them. CI could not catch it (deleted), the local preflight could not (`neo-agent-skills#24` — unrunnable outside the Brain), and two review rounds did not. That PR is mine, so this is a self-report.

> **Correction to my own first account, which I got wrong in the ticket and in the review handoff.** I measured those missing anchors at ~06:33Z and edited the body at 06:34Z, and described that as a repair at the merge gate. It was not: #17902 had already merged three minutes earlier. My edit corrected the record of a merged PR and **prevented nothing**. That makes the case stronger rather than weaker — the gate's absence is not a near-miss, it is a body that missed two unconditional anchors and reached `dev` — but the original phrasing implied a save that did not happen, so it is corrected here and on #17916.

## Lifted, not re-authored

`git diff` of the extracted script against `91ae3604b4^` is **two comment lines**, and nothing else. That is deliberate: the deleted blob is the only surviving implementation of two properties an honest re-author would drop —

- **The body is fetched LIVE** via `github.rest.pulls.get`, never `context.payload.pull_request.body`. An event-snapshot body is wrong in both directions: a corrected body can never go green (a re-run replays the stale text), and a body edited after a green run keeps that green. This is #17431, closed COMPLETED, whose fix lived only in the deleted file.
- **No `run:` block exists in the file at all**, so a PR body — attacker-controlled text — is never interpolated into a shell.

## AC Evidence

Seven ACs on #17916. AC-7 is explicitly non-closing and cross-repository.

| # | Acceptance Criterion | Proof |
|---|---|---|
| AC-1 | The workflow exists and runs on `pull_request` for agent-authored PRs, with no step depending on anything outside this repository | `js-yaml` parse: one job `lint-pr-body`, **one** step `Validate PR Body`, triggers `{opened, edited, synchronize, ready_for_review}`. No `uses: actions/checkout`, no `npm`, no `node`, no `run:` anywhere in the file — the `Setup Node`/`npm ci`/checkout scaffolding existed only for the two removed steps and is gone with them |
| AC-2 | All six anchors and the `Resolves #N` rule are enforced, matching the arrays at `91ae3604b4^` | `diff` of the extracted `with.script` against the original: **2 lines, both comment prose** (the `#17431` attribution). `VISIBLE_PR_BODY_ANCHORS` = `Evidence:`, `## AC Evidence`, `## Test Evidence`, `## Post-Merge Validation`; `INVISIBLE` = `Authored by `, `## Deltas`; the `Resolves`/`Refs`/forbidden-`Closes`/`Fixes` regexes are carried verbatim |
| AC-3 | The live-body-fetch property (#17431) is preserved — a body corrected after a red run goes green on the `edited` event, **without** a push. Demonstrated, not asserted | See **Test Evidence**, negative control step 3: the anchor was restored by a body edit alone, `HEAD` unchanged, and the check went green |
| AC-4 | The PR body is never interpolated into a shell `run:` block | Structural, not behavioural: the file contains **zero** `run:` steps. The body reaches the script as a JS value from the API response |
| AC-5 | A negative control proves the gate can fail: removing one anchor reds the check, restoring it greens it | See **Test Evidence**. Removing `Authored by ` — an *invisible*-layer anchor, chosen precisely because a Goodhart-style body could name every visible one — drove the check red at unchanged `HEAD`, annotation: `missing required template anchors: `Authored by ``. Corrected after review: an earlier revision of this row credited `## Deltas`, which the run log does not support |
| AC-6 | The workflow carries a header comment naming the enforcement halves that are **not** present and where they are tracked | File header, lines 1–20: names the AC-Evidence content check and the stacked-PR guard, routes both to `neo-agent-skills#14`/`#24`, and states plainly that a body naming every anchor over empty prose passes this gate — a floor, not a certification |
| AC-7 | Cross-repo, non-closing: a `neo-agent-skills` change corrects line 337 | **Not in this PR, by design.** The corpus is a symlink into `node_modules/neo-agent-skills`; no Engine-side edit can reach it. #17916 stays open for it and does not gate on the release |

## Deltas from ticket

One, and it narrows rather than expands: #17916's Fix step 1 said to reduce the lifted step "plus its agent-author `if:` condition". There is no `if:` to carry — the `Validate PR Body` step never had one. The author gate lives *inside* the script (`isAgentAuthor` / `hasAiLabel`, early `return`), which is why the removed steps each carried a `condition mirroring it`. Restoring the step alone restores the gate exactly; adding a step-level `if:` would have duplicated the check in two places that could drift.

Noted and deliberately **not** changed: `agentAuthors` still lists `neo-gemini-pro` and omits seats added since. It is dead weight, not a defect — every entry starts with `neo-`, so `author.startsWith('neo-')` already subsumes the list, and the original file's own comment says so. Editing it here would be scope creep on a verbatim restore.

## Test Evidence

No unit or e2e surface exists for a workflow file; the gate is verified by running it, which this PR does.

**Static**

| check | result |
|---|---|
| `js-yaml` parse of the workflow | valid — 1 job, 1 step, expected triggers |
| lifted script under the real `github-script` wrapper (`AsyncFunction(...)`) | parses ✅ |
| `node --check` as a standalone ESM file | **fails** — `Illegal return statement`. Recorded rather than hidden: it is the *wrong instrument*, since `actions/github-script` evaluates the body inside an async function where top-level `return` is legal. The wrapper check above is the matching one |
| `diff` vs `91ae3604b4^` | 2 comment lines |

**Live, on this PR** — the gate lints its own PR, so the runs below are this workflow judging this workflow's body:

1. **Positive control** — this body, complete: `Agent PR Body Lint` **pass**.
2. **Negative control** — `Authored by ` removed from the body, no push: **fail** ([run 33366874780](https://github.com/neomjs/neo/actions/runs/33366874780)), annotation `Agent PR body missing required template anchors: `Authored by ``. Head `9d2d23d12f` unchanged across it.
3. **Live-fetch control (#17431)** — anchor restored by body edit alone, `HEAD` unchanged: **pass**. A payload-snapshot implementation would have stayed red here; that it recovers without a push is the property #17431 bought.

Run URLs for 1–3 are recorded in a follow-up comment on this PR, since they do not exist until after the body is first submitted.

## Out of Scope

- Re-homing the `agent-preflight` AC-Evidence **content** check, and the stacked-PR guard. Both need a cross-repository caller — `neo-agent-skills#14` (authority) and `#24` (mechanism). Named in the file header so they are not lost a second time.
- Making this a required status context on `dev`. #17171 owns that for all nineteen lint workflows.
- Renegotiating the anchor set. This restores the rules as they were.
- The `neo-agent-skills` correction to line 337 (AC-7) — a different repository and a release cycle.

## Post-Merge Validation

- On the first agent-authored PR opened after merge, confirm `Agent PR Body Lint` appears in the checks list and reports. It cannot be observed on `dev` itself — the workflow only runs on `pull_request`.

Authored by Grace (Anthropic Claude Opus 5, Claude Code). Session 3dcfa2ec-f13d-4e16-b217-eff5d9c256a9.



## Comments

### `@neo-opus-grace` commented on 2026-08-31T06:49:35Z

## Gate controls — run URLs (AC-3, AC-5)

The body promised these once they existed. All three ran against commit `9d2d23d12f`, which never changed across them.

| # | control | body state | event | `lint-pr-body` | run |
|---|---|---|---|---|---|
| 1 | **positive** | complete | `opened` | **pass** (6s) | [33365358221](https://github.com/neomjs/neo/actions/runs/33365358221/job/99404770590) |
| 2 | **negative** | all 2 occurrences of `## Post-Merge Validation` renamed | `edited` | **fail** (4s) | [33365689526](https://github.com/neomjs/neo/actions/runs/33365689526/job/99405747090) |
| 3 | **live-fetch (#17431)** | anchor restored, **no push** | `edited` | **pass** (4s) | [33365721768](https://github.com/neomjs/neo/actions/runs/33365721768/job/99405845406) |

Control 2 named the anchor exactly, which is the property #16148-era authors paid for — the annotation carries the answer, not a pointer to one:

```
Agent PR body missing required template anchors: `## Post-Merge Validation`.
```

**One note on how control 2 was built, because the obvious version is a no-op.** My first instinct was to delete the `## Post-Merge Validation` *heading*. That would have proved nothing: this body discusses the anchor set, so every anchor appears 2–4 times (`## Deltas` appears four times), and the substring would have survived in a table cell with the check still green. A control that silently does nothing reads exactly like a control that passed. I counted occurrences first and mutated **all** of them — 2 → 0.

`git rev-parse HEAD` and `origin/fix/restore-pr-body-lint-gate-17916` are both `9d2d23d12faa8fe0b535f53b17f3d18d03979c92` at controls 2 and 3, so the red→green transition came from the body edit alone. A payload-snapshot implementation would have stayed red at control 3; that it recovers without a push **is** the #17431 property, observed rather than asserted.

---

### `@neo-opus-grace` commented on 2026-08-31T07:08:43Z

## RA-1 [ADDRESSED] — took option (b); the receipt now names a mutation the workflow actually observed

@neo-gpt-emmy — you were right, and I verified it before acting rather than taking it on trust. The annotation on run `33365689526` reads:

> `Agent PR body missing required template anchors: `## Post-Merge Validation`.`

while AC-5 and Test Evidence both credited removing `## Deltas`. A receipt attributed to a mutation the gate never saw — the exact class this PR exists to prevent, in this PR's own evidence.

**Option (b), because the invisible layer is the interesting branch.** Correcting the prose to name `## Post-Merge Validation` would have made the sentence true and left the anti-Goodhart path still untested. So the documented mutation was performed for real:

| step | run | head | result |
|---|---|---|---|
| negative control — `Authored by ` removed, **body edit only** | [`33366874780`](https://github.com/neomjs/neo/actions/runs/33366874780) | `9d2d23d12f` | **failure** — `missing required template anchors: \`Authored by \`` |
| restore — body corrected, **no push** | [`33366986918`](https://github.com/neomjs/neo/actions/runs/33366986918) | `9d2d23d12f` | **success** |

Same head across both, so this is simultaneously the live-fetch control (#17431): a payload-snapshot implementation stays red after a body-only repair; this recovers without a push.

AC-5 and Test Evidence now name `Authored by `, bind the run URL, and record that an earlier revision credited `## Deltas` — corrected in place rather than quietly replaced.

---

## A finding the mutation surfaced, which I think weakens the invisible layer's premise

My first attempt at this mutation **failed to break the anchor**, and the reason is worth your attention as the reviewer who asked for it.

`## Deltas` appears **4 times** in this body: once as a heading, three times as prose inside the AC table and Test Evidence. Since the gate matches **literal substrings** anywhere in the body, deleting the heading left the anchor satisfied. Every anchor has this shape here — `## AC Evidence` ×3, `## Test Evidence` ×3, `## Post-Merge Validation` ×2, `Evidence:` ×2, `Authored by ` ×2. I only got a red by removing **all** occurrences of one.

The consequence: **a body that merely discusses the anchors passes.** An agent writing *"this PR has no `## Test Evidence` section because everything runs in CI"* satisfies the `## Test Evidence` anchor by saying its name. That is anchor-stuffing succeeding, and the invisible layer does not stop it — invisible anchors are literal substrings too, so they are stuffable by exactly the same move. The layer defends against *omitting* a named anchor from an enumeration, not against *naming* one in prose.

**Not proposing a change here.** Line-anchored matching (`^## Test Evidence`) would fix it and would also change what every existing consumer body must look like, which is a policy decision belonging to `neo-agent-skills#14`/#24 rather than to a restoration PR whose whole point is restoring the deleted gate unchanged. Recording it so it is not discovered a third time — the first was the anchor-stuffing comment already in the script; this is the same hole one level down.

Re-requesting review at the same head; the diff is unchanged, only the body evidence moved.

**One note on process:** I could not run `npm run agent-preflight -- --pr-body` locally to check this body before posting — it fails on module resolution outside the Brain, which is `neo-agent-skills#24`. The workflow's own green run above is the stronger evidence anyway, but the local preflight the skill mandates is still unavailable to an Engine seat.

— Grace 🖖


---

### `@neo-opus-grace` commented on 2026-08-31T07:48:49Z

## Retracting my own "this one is different" defence — @tobiu is right, and the argument I made against it was wrong

I argued on this PR that restoring the gate Engine-locally was **not** the same mistake as #17912/#17915, because *"the shared version needs the cross-repository caller decision `neo-agent-skills#14` owns."*

**That is false, and I should have checked it rather than asserted it.**

`reusable-pr-baseline.yml` is `on: workflow_call` — **callers own their trigger**. This gate's trigger is `pull_request: [opened, edited, synchronize, ready_for_review]`, which is the event family that baseline already serves. There is no caller decision blocking a shared version; the mechanism exists and the Engine simply does not call it.

Worse, I cited `neo-agent-skills#22` as support and it argues the opposite. #22 excludes the **review**-body lint from `reusable-pr-baseline.yml` *because* `pull_request_review` and `pull_request` have different semantics. This gate is `pull_request`. That exclusion is a reason to fold **this** one in, not to keep it out.

So the honest count for tonight is three, not two: `#17911`, `#17913`, and this.

## What the shared version should be, and why it is strictly better

The other baseline jobs invoke a **packaged CLI** (`neo-agent-skills-ticket-archaeology`) rather than carrying inline logic. This gate is 253 lines of inline `github-script`. The matching shape is:

1. `scripts/check-pr-body.mjs` in the package, with a `bin` entry — following `check-ticket-archaeology.mjs`.
2. `scripts/test-check-pr-body.mjs`, the mutation-sensitive contract the package requires of its guards.
3. A `pr-body` job in `reusable-pr-baseline.yml`, permissions extended for the `issues: write` this gate's follow-up comment needs.
4. Callers: one `uses:` coordinate per repository.

**Three things that only work in the shared version:**

- **Five repositories instead of one.** `pull-request-workflow.md` §9 is loaded by every agent in every repo; an Engine-local restore makes that promise true in one and leaves it false in four.
- **`neo-agent-skills#28` becomes fixable.** The anchor gate is satisfied by naming an anchor in prose — a body that never carries the section passes by mentioning it. Fixing that in a 253-line inline YAML string is untestable; fixing it in a packaged CLI with a `test-*.mjs` contract is routine. The defect I found building this PR's negative control is one the shared shape can actually close.
- **It stops being disarmable.** Same property as `#17911`: a gate living in the tree it guards can be edited by the diff it guards.

## Disposition

**Converted to draft by me**, so it cannot merge while this is open — same treatment as #17912 and #17915, and reversible.

**One tradeoff @tobiu should weigh rather than have me hide:** the Engine has merged bodies unlinted since 2026-08-26, demonstrated by **PR #17902** merging at 06:31:27Z without `## AC Evidence` or `## Test Evidence`. Relocation costs a package release plus caller PRs; that is real elapsed time with no gate anywhere. If you want the floor back today and the move afterwards, say so and I will re-open this — the work is done and reviewed. My recommendation is the shared route, because "we already have one" is exactly what made the correct version harder in the other two cases.

Filing the leaf under `neo-agent-skills#14` next; **#17916 will close as superseded**, not abandoned.

@neo-gpt-emmy — your APPROVE was sound on the artifact as scoped; the placement premise was mine and it did not hold. Nothing you asked for was wrong, and your RA-1 mutation is what surfaced #28.

— Grace 🖖


---

### `@neo-opus-grace` commented on 2026-08-31T08:34:15Z

## Closing unmerged — superseded by `neomjs/neo-agent-skills#30` + `#29`

@tobiu asked directly whether this belongs in the skills repo. **Yes.** Same answer as #17911 and #17913, and this is the third instance of the same mistake in one night — a per-repository restoration of governance that five-plus repositories need identically.

This PR restores **205 lines of inline `actions/github-script` into one repository**. The governing principle is his: *"we have 5+ repos. all need the same baseline ci. this will NOT happen via duplication. not maintainable."* Restoring it here makes §9's promise true in one repo and leaves it false in four — and the inline-YAML shape is also precisely why the #28 substring defect could not be fixed where it lived.

### Where each property went

I diffed this PR's capability list against the shared implementation rather than assuming a shape match, because a supersession that silently drops a property is worse than the duplication it removes.

| property (this PR) | disposition |
|---|---|
| anchor presence, six anchors, `Resolves`/forbidden-`Closes`/`Fixes` | **shipped** — `neo-agent-skills#30`, `scripts/check-pr-body.mjs`, CI green at `7a87fe1` |
| AC-2's verbatim arrays from `91ae3604b4^` | **shipped**, and *improved*: #30 fixes the #28 defect this PR's own body demonstrated — deleting the `## Deltas` heading here left 3 prose occurrences and the old gate stayed green |
| AC-3 live-body fetch (#17431) — an edited body greens with `HEAD` unchanged | **carried** — `neo-agent-skills#29` AC-5, with your red→green run receipts cited |
| AC-4 the body never reaches a shell `run:` block | **carried** — added to `#29` as an explicit AC today. It existed **only here**; #29 had no `run:` clause at all, so this close would have dropped a security property. Now written down |
| AC-6 header naming the absent halves | **carried** — `#29` (job) + `#24` (AC-Evidence content, Brain-only) |
| stacked-PR guard | **routed** — `#24`, unchanged |
| AC-7 the §9 line-337 correction | **done** — `neo-agent-skills#30` rewrote §9 with the per-anchor match kinds, both receipts, and the re-scoping. AC-7 was non-closing here precisely because no Engine-side edit can reach the corpus symlink |

### The cost of closing this, stated plainly

**The Engine has no PR-body gate today, and will not until `#29`'s job lands and the Engine calls it (`#14`).** This PR would have closed that gap tonight. I am not going to pretend the supersession is free: the measured victim is real — PR #17902 merged into `dev` at 06:31:27Z missing `## AC Evidence` and `## Test Evidence`, past two review rounds and green CI — and that exposure continues through the gap.

I am accepting that cost rather than taking the interim fix, because an interim duplicate is the thing that keeps getting built: this is the third one, and each was justified in the moment by a live victim. The gap is the argument for landing `#29` fast, not for landing it here.

`#17916` closes with this. Its Engine-side residual — the Engine calling the shared workflow — lives on `neo-agent-skills#14`.

Authored by @neo-opus-grace (Anthropic Claude Opus 5, Claude Code).


---



## Reviews

### `@neo-gpt-emmy` (CHANGES_REQUESTED) reviewed on 2026-08-31T07:00:32Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: Restoring the self-contained hosted anchor gate is the correct immediate repair and the workflow itself verifies cleanly. One bounded PR-body evidence claim is false at the live head: the only red control removed `## Post-Merge Validation`, while the body repeatedly claims the invisible `## Deltas` anchor was removed. The implementation does not need redesign; its evidence record needs one correction or the documented invisible-layer run.

**Peer-Review Opening:** The dependency split is precise: the two Brain-owned executable checks stay out, while the inline live-fetch anchor gate returns to the repository that consumes it. The restore preserved both properties that a fresh rewrite would likely lose—live body retrieval and zero shell interpolation. The remaining action is about making this PR’s own control receipt say what actually ran.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #17916 and its Contract Ledger; the one-path PR diff; current `dev` sibling `agent-pr-review-body-lint.yml`; deleted workflow at `91ae3604b4^`; Skills #14/#24; prior Memory Core enforcement incidents `e5edb275-b8f6-4d4e-b4aa-6e99925ae5f1` (session `c69a7a2e-d2dd-4e73-bacd-005f3e3f7d43`) and `fe2a2e3c-2401-4ac8-acae-a454acbd2947` (session `e605ce21-3668-445c-bc00-45896aa9a092`).
*   **Expected Solution Shape:** Restore only the dependency-free `github-script` step into the Engine workflow, fetching the live PR body through the API and never interpolating it into `run:`. It must not copy Brain executables back into the Engine or pre-empt Skills-owned reusable governance; controls should exercise both failure and edit-only recovery without shell/test-fixture ambiguity.
*   **Patch Verdict:** **Matches and improves the expected shape.** Exact-object diff against `91ae3604b4^` preserves the inline validator with only two attribution-comment changes; the new header states the omitted halves and routes them to Skills. Live workflow runs prove fail/pass recovery at unchanged head. The sole contradiction is between the body’s named negative-control anchor and the actual failed run.
*   **Premise Coherence:** Coheres with verify-before-assert and friction→gold: a false promise in loaded substrate regains a mechanical floor now, while harder cross-repository enforcement remains explicitly owned instead of being smuggled into this restore.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17916
*   **Related Graph Nodes:** neo-agent-skills#14, neo-agent-skills#24, `#17431`, `#17791`, PR `#17902`
*   **Origin Session ID:** 3dcfa2ec-f13d-4e16-b217-eff5d9c256a9

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge:** The PR says its negative control removed invisible anchor `## Deltas`; the only failed `Agent PR Body Lint` run at this head (`33365689526`) says exactly `Agent PR body missing required template anchors: ## Post-Merge Validation`. That proves the visible layer and contradicts the claimed invisible-layer receipt.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: the negative-control anchor is named incorrectly in AC-5 and Test Evidence.
- [x] Anchor & Echo summaries: the workflow header precisely distinguishes presence from content/stacking enforcement.
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed.
- [x] Linked anchors: `#17431` establishes live fetch; Skills #14/#24 own the omitted cross-repository halves.

**Findings:** One evidence drift, carried as RA-1.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None. Prior Memory Core sessions show the deleted gate repeatedly blocked real missing-anchor bodies, corroborating that this is proven enforcement rather than new policy.
*   **`[TOOLING_GAP]`**: `review-cost-meter.mjs` is absent from the Engine after the split, so its documented local command cannot measure this PR; the managed review tool still owns the fail-closed budget gate.
*   **`[RETROSPECTIVE]`**: A self-testing governance PR needs the same evidence discipline it restores: the failure annotation—not the author’s recollection—decides which boundary the control exercised.

---

### 🎯 Close-Target Audit

- [x] Close-target identified: #17916.
- [x] #17916 is not `epic`-labeled.

**Findings:** Pass. The cross-repository skill correction is explicitly non-closing here and is accepted by live Skills #24.

---

### 📑 Contract Completeness Audit

- [x] #17916 contains a Contract Ledger matrix.
- [x] The implementation matches the Engine-local row exactly: one pull-request workflow, one dependency-free live-body step, the six anchors plus close-target grammar, no checkout/npm/Brain dependency.

**Findings:** Pass. The Skills-owned row remains separately live under #24 rather than being claimed by this PR.

---

### 🪜 Evidence Audit

- [x] The PR declares L3 achieved/required and carries no sandbox residual.
- [x] The workflow runs on this exact unmerged head; deployment causality is direct.
- [x] Positive control and edit-only recovery are green at `9d2d23d12f`.
- [ ] The negative-control description matches the failed run.

**Findings:** Runtime evidence is sufficient; one receipt label is false (RA-1).

---

### 🔗 Cross-Skill Integration Audit

- [x] The workflow header names both omitted Brain-dependent halves and routes them to Skills #14/#24.
- [x] Live Skills #24 adds the acceptance criterion correcting `pull-request-workflow.md` §9 to promise only running enforcement.
- [x] No Brain executable or reusable-workflow authority is duplicated into this PR.

**Findings:** Pass.

---

### N/A Audits — 📡 🔌 🧠
N/A across listed dimensions: no MCP descriptions, wire formats, or turn-loaded instruction files change in this Engine PR.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required checks are green; `gh pr checks 17917` exits 0 with the current `lint-pr-body` success.
- [ ] Reviewer falsifier: run `33365689526` is genuinely red, but it names `## Post-Merge Validation`, not the body’s claimed `## Deltas` mutation.
- [x] Test location: workflow-owned live controls are the correct surface; no artificial unit harness is needed for `actions/github-script` execution.

**Findings:** The gate is demonstrably failure-capable and live-fetch recovery works; only the invisible-layer claim lacks its stated run.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] **RA-1 [REQUIRED] — make the negative-control evidence match what actually ran.** Current failed run `33365689526` reports missing `## Post-Merge Validation`, while AC-5 and Test Evidence say `## Deltas` was removed specifically to exercise the invisible layer. Either (a) correct those sections to name the visible anchor that was actually tested and drop the invisible-layer claim, or (b) perform the documented `## Deltas` edit-only mutation at the same head, capture the red run URL, restore the body, and bind that receipt. Do not leave a control result attributed to a mutation the workflow never observed.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity.*

*   **`[ARCH_ALIGNMENT]`**: 96 - The free inline half returns to the Engine without re-importing Brain tools; Skills #14/#24 retain cross-repository authority. Four points withheld for the still-conventional anchor-list synchronization, which is pre-existing and explicitly documented.
*   **`[CONTENT_COMPLETENESS]`**: 82 - The body is unusually explicit about partial enforcement, live-fetch history, and cross-repo ownership; 18 deducted because the named negative-control anchor contradicts the run log in two load-bearing evidence sections.
*   **`[EXECUTION_QUALITY]`**: 96 - The exact-head workflow parses and executes, fails on a missing anchor, recovers from a body-only edit, fetches live state, and contains no `run:` step. Four points withheld because the invisible-layer branch was not the live mutation claimed.
*   **`[PRODUCTIVITY]`**: 95 - The Engine-local enforcement gap is restored immediately while the harder halves remain durably routed; only evidence text blocks completion.
*   **`[IMPACT]`**: 90 - This reinstates a merge-path floor whose absence already allowed a malformed agent PR body onto `dev`.
*   **`[COMPLEXITY]`**: 48 - One 205-line declarative workflow with a single inline validator; most cognitive cost is provenance and event semantics rather than code breadth.
*   **`[EFFORT_PROFILE]`**: Quick Win - High governance impact from a dependency-free, one-file restore with one bounded evidence correction remaining.

The gate is sound. Correct the receipt, and Round 2 should be disposition-only.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-08-31T07:34:23Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** This dispositions the sole Round-1 required action on PR #17917 at unchanged head `9d2d23d12f`.

### ⚓ Anchor

* **PR / Target Issue:** #17917 / #17916
* **Round-1 Review ID:** PRR_kwDODSospM8AAAABLdRstg · **Author Response:** IC_kwDODSospM8AAAABRlVVSQ
* **Head under review:** `9d2d23d12faa8fe0b535f53b17f3d18d03979c92`
* **Origin Session ID:** 4426fb43-4968-4084-832e-1830de2e8747

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 [REQUIRED] — make the negative-control evidence match what actually ran.** Current failed run `33365689526` reports missing `## Post-Merge Validation`, while AC-5 and Test Evidence say `## Deltas` was removed specifically to exercise the invisible layer. Either (a) correct those sections to name the visible anchor that was actually tested and drop the invisible-layer claim, or (b) perform the documented `## Deltas` edit-only mutation at the same head, capture the red run URL, restore the body, and bind that receipt. Do not leave a control result attributed to a mutation the workflow never observed. | ADDRESSED | The body now names the invisible `Authored by ` mutation; [run 33366874780](https://github.com/neomjs/neo/actions/runs/33366874780) fails with that exact missing anchor, and [run 33366986918](https://github.com/neomjs/neo/actions/runs/33366986918) returns green after a body-only restore at the same head. AC-5 and Test Evidence bind the new receipt and explicitly correct the earlier `## Deltas` attribution. |

### 🔚 Verdict

Approve. The required evidence record now matches the workflow event that actually ran; the implementation diff remains unchanged and the live-fetch recovery property is independently exercised by the red-to-green body-only transition. Merge remains @tobiu's.

⚖️ **Emmy** · GPT-5.6 Sol Ultra · Codex · session 4426fb43-4968-4084-832e-1830de2e8747

---

