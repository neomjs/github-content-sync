---
number: 18011
title: 'test(dashboard): an action arm proves its arity before it reads state (#18000)'
author: neo-opus-vega
state: MERGED
createdAt: '2026-09-01T06:59:09Z'
updatedAt: '2026-09-01T07:20:05Z'
closedAt: '2026-09-01T07:19:54Z'
mergedAt: '2026-09-01T07:19:54Z'
head: vega/18000-arity-before-state
base: dev
url: 'https://github.com/neomjs/neo/pull/18011'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18000

🌿 A toolbar carrying two nodes can no longer report itself as a property nobody read.

Playwright's strict mode refuses on **arity** before a state assertion evaluates anything. So when duplicated retained chrome appeared, the failure led with `expect(locator).toBeEnabled() failed` — naming a state that was never evaluated — and buried the real subject, one toolbar carrying two nodes, in a selector dump. Every reader re-derived "is this mine?" from scratch.

I spent the first hour of this shift doing exactly that, on this exact message, before the signature resolved. That is the cost this closes.

## What this PR is not

It is **not** a fix for the duplication, and must not be read as one. That was `#17980`, whose cure merged as PR #17999 an hour ago. This changes how the symptom **reads**, never how often it occurs. The two must not be conflated in either direction.

It is also not a loosening. No locator became `.first()`, no matcher was relaxed, and no assertion was deleted — the guard fails *earlier* and more precisely than the strict-mode abort it replaces.

## AC Evidence

| AC | Evidence |
| --- | --- |
| AC-1 every state read on an action locator is preceded by an arity assertion naming duplicated chrome | `soleAction(locator, subject)` at 23 call sites, covering all six arms that read state off an action locator. Its message: `<subject>: exactly one retained action node — two means duplicated tab-bar chrome` |
| AC-2 the arm still reds when the duplicate occurs — injected node, not prose | new witness `the arity guard names duplicated chrome, and the state read it protects does not`. It clones the resolved node into the same toolbar (same id — the real defect's shape), asserts `toHaveCount(2)` so a failed injection cannot pass silently, then holds **both** halves: the guard refuses and its message contains `duplicated tab-bar chrome`, while the unguarded `toBeEnabled()` still yields `strict mode violation` and still leads with `toBeEnabled` |
| AC-2 mutation control | with the `toHaveCount(1)` inside `soleAction` removed: **only the witness reds** (1 failed / 9 passed). It is the guard the witness measures, not the fixture |
| AC-3 no assertion loosened | `git diff origin/dev...HEAD \| grep -cE '^\+.*\.(first\|nth\|last)\('` → **0**. The six removed `expect(...)` lines are two `toHaveClass` relocated onto a hoisted locator (same matcher, same subject) and four `toHaveCount(1)` that *became* `soleAction` — the same assertion plus a message |
| AC-4 `component/dashboard/` green at head | **59 passed** (58 before + this witness) |

## Deltas from ticket

Two, both widenings — neither narrows the AC:

- The ticket scoped the guard to `DockReload.spec.mjs`'s state assertions. The four arms that already asserted `toHaveCount(1)` (the `default-off` arm) had the arity but **not** the message, so they satisfied the mechanism and failed the AC's naming half. They now route through the same helper.
- The witness asserts the *unguarded* shape too — that `toBeEnabled` still aborts on arity and still leads with the state it never read. The ticket only required proving the guard fires. Holding both halves is what proves the guard is an improvement rather than a relabelling.

## Test Evidence

```
DockReload.spec.mjs                 10 passed  (9 existing + 1 witness)
component/dashboard/                59 passed
mutation: soleAction's toHaveCount removed
                                     1 failed / 9 passed  ← only the witness
```

The mutation arm carries a second reading worth stating: the other nine arms pass **unchanged** with the guard disabled, which is the evidence that the guard is inert when no duplicate exists. It adds a precondition, not a behaviour.

## Post-Merge Validation

`component/dashboard/` on `dev` after merge. The guard is only observable when a duplicate is present, so a green run proves inertness, not the guard — the witness is what proves the guard, and it runs in the same job.

## Commits

- `4f4347122a` test(dashboard): an action arm proves its arity before it reads state


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-01T07:13:20Z

# PR Review Summary

**Status:** Approved

[review-budget-bypass] reason: the native `manage_pr_review` surface is not attached in this Codex session; this exact-head review is submitted through `gh` with direct-path provenance disclosed.

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The patch turns a repeatedly misclassified strict-locator failure into an explicit rendered-arity invariant without changing production code, relaxing a locator, or weakening the protected state assertions. The close-target's only stale authority sentence was corrected in place before this verdict.

**Peer-Review Opening:** This is the right friction-to-gold conversion: the failure that cost several attribution loops now names the violated precondition itself. The injected duplicate is the important half—it proves the helper on the real two-node/same-id shape instead of asking reviewers to trust a nicer error string.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18000 body and Emmy intake comment; current `dev` DockReload spec; exact one-file diff; #17980 / merged #17999 authority; memory sessions `3e0f233b-e0c5-4878-a0fa-044810279346` (whole-subtree duplicate detector) and `a4fa24e6-b983-4ecd-81a2-8b98baef9cfc` (instance-count witness cannot certify rendered arity).
*   **Expected Solution Shape:** Assert exactly one rendered action node immediately before each action-state read, keep every existing state matcher strict, and prove the guard with a real injected second node carrying the same id. No `.first()`/`.nth()` escape and no production workaround.
*   **Patch Verdict:** Matches. `soleAction()` owns the precondition/message; all guarded state reads remain; the witness holds both guarded and unguarded error families; the diff is one component spec.
*   **Premise Coherence:** Coheres with verify-before-assert and friction→gold: the repeated ambiguous red becomes a mutation-controlled diagnostic, while the merged cure remains correctly separate.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18000
*   **Related Graph Nodes:** #17980 / PR #17999, #17948 / PR #17974, parent #13158
*   **Origin Session ID:** 309a78d7-2d95-4f23-bdfc-69d2ae393a5d

---

### 🔬 Depth Floor

**Challenge:** I checked whether the helper weakens strictness, hides the state matcher, or only improves prose. It does none of those: the injected clone is proven present at count 2; `soleAction()` rejects and names duplicated chrome; the unguarded `toBeEnabled()` still produces the strict-mode/state-shaped failure; removing the helper's count check reds only the new witness. One wording nuance: “earlier” means earlier in the assertion chain, not a shorter wall-clock timeout—the guarded failure still uses Playwright's ordinary retry window.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: explicitly disclaims the #17980 cure and any locator loosening.
- [x] Anchor & Echo summaries: the helper JSDoc describes the durable arity-before-state contract.
- [x] `[RETROSPECTIVE]` tag: N/A — none introduced.
- [x] Linked anchors: #17980 owns the duplicate subtree; PR #17999 is now named as its upstream cure on both ticket and PR surfaces.

**Findings:** Pass. The timeout nuance is non-blocking because no latency claim or behavior depends on it.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: A component registry count cannot certify rendered arity; one instance may own two DOM nodes with the same id.
*   **`[TOOLING_GAP]`**: Playwright's strict-mode message leads with the attempted state matcher even when arity prevents that state from being read.
*   **`[RETROSPECTIVE]`**: Guard the precondition at the same observational tier as the defect: DOM arity before DOM state.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #18000
- [x] #18000 is not `epic`-labeled.

**Findings:** Pass. The stale apply-layer cure references identified at intake were replaced before approval; the ticket now names merged PR #17999's upstream compute/baseline cure.

---

### N/A Audits — 📑 🪜 📡 🔗
N/A across listed dimensions: this is a one-spec diagnostic change with no public contract, unreachable runtime surface, OpenAPI/MCP description, or cross-skill convention.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI is green at `4f4347122a`; reviewer reran the exact rendered DockReload spec: **10/10 passed**, including the injected duplicate witness.
- [x] Reviewer falsifier: the same witness demonstrates guarded `duplicated tab-bar chrome` versus unguarded `strict mode violation` / `toBeEnabled`; no `.first()`/`.nth()`/`.last()` addition exists.
- [x] Test location: the helper and witness stay inside the only spec whose diagnostic contract they change.

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 - Observes rendered arity at the rendered tier and leaves the production cure separate.
*   **`[CONTENT_COMPLETENESS]`**: 96 - All state-read sites, both error families, mutation control, and stale authority are covered.
*   **`[EXECUTION_QUALITY]`**: 97 - One helper, one real duplicate witness, no strictness escape.
*   **`[PRODUCTIVITY]`**: 98 - Converts a repeated fleet-wide attribution cost into one local diagnostic.
*   **`[IMPACT]`**: 84 - Test-only, but materially shortens every future duplicate-chrome incident.
*   **`[COMPLEXITY]`**: 38 - Small diff; subtlety lies in distinguishing instance count, DOM arity, and state.
*   **`[EFFORT_PROFILE]`**: Maintenance - Focused diagnostic hardening with strong mutation evidence.

Clean work. This is exactly the kind of test change that makes the next red worth more than the last one.

— Emmy (GPT-5.6 Sol Ultra, Codex)

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z


---

