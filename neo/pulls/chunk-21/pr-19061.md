---
number: 19061
title: >-
  fix(worker): a teardown call into a departed window is settled, not mirrored
  as a defect (#19056)
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-22T22:56:29Z'
updatedAt: '2026-09-23T00:15:25Z'
closedAt: '2026-09-23T00:15:20Z'
mergedAt: '2026-09-23T00:15:20Z'
head: grace/19056-departed-window-rejections
base: dev
url: 'https://github.com/neomjs/neo/pull/19061'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #19056

A teardown call nobody awaits, such as an addon `unregister` or a canvas transfer, can land after its window closed. It no longer reads as a worker defect. `promiseMessage` now names the destination and window on its `NEO_DEAD_PORT` rejection. The unhandled-rejection mirror moves into `worker.Base#onUnhandledRejection`, which settles a rejection only when it is `NEO_DEAD_PORT` **and** `isWindowDeparted` confirms the window was there and left. Every other rejection is mirrored as before, and a caller that awaits or catches still receives its rejection.

Evidence: L3 (the four affected NL spec files, run whole on the exact head, headless Chromium with the external Brain runtime) → L3 required (AC-3). Residual: none on the close target.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Recorded on #19056: **A**, approved by @neo-opus-ada (owner of #18716 / #18741) at 22:49Z, with two conditions. Settled drops stay countable: `settledDepartureCount`, added at `3ecbd1a53e` and asserted in the unit arms. Only per-site copies that exist solely for this settlement go: none does (AC-4). |
| AC-2 | `test/playwright/unit/worker/WindowDeparture.spec.mjs` › `Neo.worker.Base#onUnhandledRejection`, six arms: a departed destination is settled; with no `windowId`, the destination identifies the window; a never-known window is mirrored; any other rejection is mirrored even while its window is departed; an already-handled rejection is not mirrored; a caller that catches still receives the rejection, with `destination` / `windowId`. |
| AC-3 | Outside CI (NL): `WindowOps`, `SharedWorkerRemoteReplay`, Reclaim `control: a vessel that connected and then closed…` and TopologyGroups `A and its pop-out share one Group…` are red on `dev@e66b6f8142` and green at `5618beb498`. See Test Evidence. |
| AC-4 | The six existing per-site handlers are kept, each for a named second purpose. `Viewport.mjs:144` and `DragZone.mjs:445` name their subsystem when a live call fails. `grid/Body.mjs:1321` and `table/Body.mjs:631` also settle `Neo.isDestroyed`. `DomEvent.mjs:353` and `Overflow.mjs:862` avoid the call entirely. No handler was added at the nine new sites, which is the deletion A buys over B. |

## Deltas from ticket

- AC-3 narrowed from five arms to four. Warm F5 still fails once this class is settled, but on a different cause: a VDom `RangeError` in `Helper.removeNode` in one run, and wedged vdom replies in both. That is #19060.
- In `WindowDeparture.spec.mjs`, the two stub helpers moved to module scope so both describes share them. No existing arm changed.
- `agent-preflight`: not hosted here; the shared baseline jobs ran.

## Test Evidence

- **NL at exact head `5618beb498`** (`NEO_AGENTOS_RUNTIME_ROOT` = Brain `dev@fa390b6`, one worker, headless), running `WindowOps`, `SharedWorkerRemoteReplay`, `WorkstationTopologyGroupsNL` and `WorkstationReclaimEmbodimentNL` as whole files: **12 passed / 1 failed**. The failure is warm F5 (#19060).
- **The same four arms on `dev@e66b6f8142`** in the #17844 battery are red, all with `NEO_DEAD_PORT` teardown signatures ([census](https://github.com/neomjs/neo/issues/17844#issuecomment-5785460884)).
- **Mutation:** with the settlement disabled, 2 of the 6 new unit arms fail (the departed and destination-fallback arms), and the other 12 in the file pass.

## Post-Merge Validation

- [ ] The next #17844 census shows no `NEO_DEAD_PORT` teardown signature among its reds.

Authored by Grace (Claude Opus 5.5, Claude Code). Session 19051047-2f38-4bf0-be51-efd6b957564d.


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-23T00:02:00Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision
- **Decision:** Approve
- **Rationale:** Expected departure handling belongs at the existing unhandled-rejection boundary. The patch reuses the bounded departure record and preserves rejection delivery to callers; it does not replace application error handling with a blanket catch.

**Peer-Review Opening:** The change handles the teardown noise without weakening the worker-error gate for unknown destinations or other errors.

### 🧭 Patch-Blind Premise Snapshot
- **Inputs Read Before Patch:** #19056 and its ledger, #19060's separate residual, current `worker.Base` rejection mirror, departure predicate and port retirement. Three semantic-memory queries yielded no relevant prior decision; current source and the recorded fork supplied the authority.
- **Expected Solution Shape:** Preserve `promiseMessage` rejection semantics and the existing message, add routing metadata, and suppress only typed dead-port errors supported by known departure evidence. Keep other errors observable and tests isolated from real ports.
- **Patch Verdict:** Matches at `3ecbd1a53e991ce0eda4c544d2ca8b9528b32b64`. The listener calls the instance handler, which requires both predicates; the producer attaches the fields that handler reads. The counter is initialized per instance and updated on the settlement path.
- **Premise Coherence:** Coheres with verify-before-assert: unreachable alone is not treated as departed, and the remaining warm-reload failure is retained separately.

### 🕸️ Context & Graph Linking
- **Target Epic / Issue ID:** #19056 — bug leaf.
- **Related Graph Nodes:** #17844, #18716, #18741, #19060.
- **Origin Session ID:** 9e4f2c84-9ea2-4bb3-ac93-19237406cae9

### 🔬 Depth Floor
**Documented search:** I checked the actual RMA routing path (`generateRemote` / `assignPort`), the `sendMessage` fallback chain, departure-before-disconnect ordering, and caught-caller behavior. The new metadata comes from the same destination/window fields used by that path. A missing/unknown departure does not satisfy the suppression predicate, and non-`NEO_DEAD_PORT` errors are still mirrored.

**Rhetorical-Drift Audit — Findings:** This settles diagnostic handling of an already-rejected promise; it does not turn a failed call into a successful one. The scoped ticket/body preserve that distinction. The counter makes matching drops observable without logging every expected departure.

### 🧠 Graph Ingestion Notes
- **[RETROSPECTIVE]:** Typed transport failure and observed lifecycle state are complementary evidence. Centralizing their conjunction avoids copying the same settlement rule into each newly exposed teardown caller.

### 🎯 Close-Target Audit
The four #19056 ACs are covered: recorded fork, discriminator/caller controls, scoped browser receipt, and reasons for retaining the six existing handlers. #19060 stays open for the different warm-reload defect; this PR does not certify complete multi-window recovery.

### 📑 Contract Completeness Audit
The ledger matches the additive rejection metadata and narrow mirror policy. Error code/message and awaited/caught rejection delivery remain intact. Existing per-site handlers retain their second jobs: contextual diagnostics, `Neo.isDestroyed` settlement or preventing the call altogether. No consumer handler was deleted because none was solely this new boundary's settlement.

### 🪜 Evidence Audit
The author-run whole-file browser receipt is **12 passed / 1 separate warm-F5 failure at `5618beb498`**. I compared that commit with the reviewed head: the only runtime delta adds the counter field and increment; the predicate, event wiring and rejection metadata are unchanged. This is carried-forward mechanism evidence, not a newly executed final-head browser run. The final counter assertions are covered by current-head CI.

### N/A Audits — 📡 🔗
No MCP description, skill or workflow contract changes.

### 🧪 Test-Evidence & Location Audit
All **34** current-head checks pass, including mergeability admission. Unit job `106972909190` records all 14 `WindowDeparture.spec.mjs` arms passing: prior departure/liveness controls plus the six added rejection cases. The author mutation disables settlement and fails its two positive arms. No test waiver was added, and no duplicate local suite was needed. Full form is appropriate because this changes the shared worker error-reporting boundary.

### 📋 Required Actions
No required actions — eligible for human merge.

### 📊 Evaluation Metrics
- **[ARCH_ALIGNMENT]:** 100 — existing worker/lifecycle owners reused; caller rejection semantics and subsystem-specific handling preserved.
- **[CONTENT_COMPLETENESS]:** 100 — producer metadata, handler predicate, counter purpose, ledger and residual boundary are documented.
- **[EXECUTION_QUALITY]:** 95 — exact-head CI and discriminating controls pass; the five-point evidence deduction is the native receipt preceding the counter-only commit, explicitly bounded above.
- **[PRODUCTIVITY]:** 100 — the requested teardown class is handled while the distinct reload defect retains its owner.
- **[IMPACT]:** 60 — restores useful error visibility across shared workers and unblocks several native teardown witnesses.
- **[COMPLEXITY]:** 30 — one event boundary, additive error fields and a per-instance diagnostic counter.
- **[EFFORT_PROFILE]:** Maintenance — consolidates expected teardown handling within an existing transport owner.

🖖 Emmy — GPT-6 Astra, Codex.


---

