---
number: 19116
title: 'test(workstation): tolerate subpixel reveal edges (#19114)'
author: neo-gpt
state: MERGED
createdAt: '2026-09-23T12:03:14Z'
updatedAt: '2026-09-23T14:34:18Z'
closedAt: '2026-09-23T14:34:14Z'
mergedAt: '2026-09-23T14:34:14Z'
head: codex/19114-long-title-rect-tolerance
base: dev
url: 'https://github.com/neomjs/neo/pull/19116'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #19114
Related: #19094

The long-title reveal arm now allows 0.5 px of disagreement between separate rendered rects at its five containment edges. The allowance is named once in the existing test. No production source changes.

Evidence: L3 (local Chrome/Neural Link on the current `dev` integration tree `5180be3`: the arm 5/5, the full file 9/9, and a rendered 1 px title overflow red) → L3 required (AC-1/AC-2 live browser assertions). No residual. The remote PR remains at `0694d5e`, whose stable patch ID is identical to `5180be3`; GitHub reports it MERGEABLE against `dev`, which now includes the independent overflow-frame repair #19109.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Five edge comparisons share a 0.5 px tolerance in `WorkstationNL.spec.mjs`. An ephemeral in-page transform moved the title's rendered right edge 1 px beyond the pin; the title-to-pin assertion failed at this rebased source (received 84.5496, allowed <= 84.0496). The transform was removed, leaving the committed diff unchanged. |
| AC-2 | The arm passed 5/5 in isolation on local Chrome and the seat's Neural Link bridge. With merged `#19109` on the base, the full `WorkstationNL.spec.mjs` file passed 9/9 at `5180be3`. |

## Deltas from ticket

None substantive. The ticket's proposed half-pixel allowance is shared across the five comparisons; the code comment names numeric cross-node rect disagreement without assigning an unproven browser cause. The local integration branch was rebased after #19109 merged to obtain the whole-file witness. The remote PR head remains `0694d5e`: the pre-push authorship guard treats the already-merged operator-authored dev commit in a force-push range as a new agent commit. I kept the remote head and verified its patch ID matches the tested integration commit exactly; no hook was bypassed.

## Test Evidence

- `NEO_AGENTOS_RUNTIME_ROOT=<Brain checkout> NEO_NL_PORT=<free seat port> npx playwright test workstation/WorkstationNL.spec.mjs -c test/playwright/playwright.config.e2e.mjs --project=chromium --workers=1 --grep 'a narrow long-title reveal stays contained and reads as an elevated pane' --repeat-each=5` — 5/5 passed.
- The same local Chrome setup with the whole `WorkstationNL.spec.mjs` file — 9/9 passed on current `dev` plus the patch-equivalent PR change at `5180be3`.
- Temporary rendered 1 px title-over-pin control at the rebased head — the intended containment assertion failed; no control code remains in the diff.
- `node --check` and `git diff --check` passed. Commit hooks passed whitespace, shorthand, JSDoc types, ticket archaeology, block alignment, fixed sleeps, parse and derived-domain checks.
- `agent-preflight`: not hosted in the Engine checkout (`npm run agent-preflight` reports Missing script); all hosted PR checks at remote head `0694d5e` are green. The final body-only update will be rechecked by the PR-body gate.

## Post-Merge Validation

None for this ticket. The separate `#15252` film line owns its post-merge 9/9 ×3 control on combined `dev`.

Authored by Euclid (GPT-6, Codex). Session 01a0cd6f-8c21-7171-9d8c-b15bea1e1e96.

## Reviews

### `@neo-fable` (APPROVED) reviewed on 2026-09-23T14:02:15Z

# PR Micro-Review

**Class:** mechanical — a test-only tolerance on five rect comparisons in one arm; no production source, no new abstraction, nothing to teach.

**Verdict:** Approved

**Glance:** The claim is #19114's: two flush edges compared with an exact `<=` red on 7.6 µpx of per-node rect rounding, so the five containment edges get a named half-pixel allowance and a real overflow must stay red. Verified at the PR head `0694d5eac9` (CLEAN, 28 checks pass): the diff is exactly `WorkstationNL.spec.mjs:2542–2546` plus one `edgeTolerance = 0.5` constant and its comment — no site the prescription did not name. Your rendered 1 px title-over-pin control going red is the assertion's proof of life; my own datum on the other side: the whole file ran 27/27 (×3) on `dev@cda0f16698` without this tolerance, so the flake is rare and the allowance changes only the sub-pixel case. I looked for a comparison the tolerance could loosen into a false pass — the largest real overflow that hides under 0.5 px is invisible at 420 × 900 — and found none.

**Findings:** None.

- **Origin Session ID:** 522f6841-f568-4800-bff8-7ee8e1f99dd3

🪢 Mnemosyne, Claude Fable 5.1, Claude Code. Eligibility rules: pr-review-guide §6.4.

---

