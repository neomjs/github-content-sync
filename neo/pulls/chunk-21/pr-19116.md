---
number: 19116
title: 'test(workstation): tolerate subpixel reveal edges (#19114)'
author: neo-gpt
state: OPEN
createdAt: '2026-09-23T12:03:14Z'
updatedAt: '2026-09-23T12:03:14Z'
closedAt: null
mergedAt: null
head: codex/19114-long-title-rect-tolerance
base: dev
url: 'https://github.com/neomjs/neo/pull/19116'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Refs #19114
Related: #19094

The long-title reveal arm now allows 0.5 px of disagreement between separate rendered rects at its five containment edges. The allowance is named once in the existing test. No production source changes.

Evidence: L3 (local Chrome/Neural Link: the arm 5/5, a rendered 1 px title overflow red, whole file 8/9) → L3 required (AC-2: whole-file 9/9 at this PR head). Residual: AC-2, Residual-Owner: #19094. This PR remains draft until the separate overflow-frame repair in PR `#19109` lands and the full file is green on a refreshed head.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Five edge comparisons share a 0.5 px tolerance in `WorkstationNL.spec.mjs`. An ephemeral in-page transform moved the title's rendered right edge 1 px beyond the pin; the title-to-pin assertion failed (received 84.6652, allowed <= 84.1652). The transform was removed before commit. |
| AC-2 | The arm passed 5/5 in isolation on local Chrome and the seat's Neural Link bridge. The whole file ran 8/9: only the duplicate overflow-control frame at line 2033 failed, the subject of open `#19094` / PR `#19109`. A 9/9 run is still owed before ready-for-review. |

## Deltas from ticket

None substantive. The ticket's proposed half-pixel allowance is shared across the five comparisons; the code comment names numeric cross-node rect disagreement without assigning an unproven browser cause.

## Test Evidence

- `NEO_AGENTOS_RUNTIME_ROOT=<Brain checkout> NEO_NL_PORT=<free seat port> npx playwright test workstation/WorkstationNL.spec.mjs -c test/playwright/playwright.config.e2e.mjs --project=chromium --workers=1 --grep 'a narrow long-title reveal stays contained and reads as an elevated pane' --repeat-each=5` — 5/5 passed.
- The same local Chrome setup with the whole `WorkstationNL.spec.mjs` file — 8/9; the sole red is the independently owned overflow-frame monitor in `#19094`.
- Temporary rendered 1 px title-over-pin control — the intended containment assertion failed; no control code remains in the diff.
- `node --check` and `git diff --check` passed. Commit hooks passed whitespace, shorthand, JSDoc types, ticket archaeology, block alignment, fixed sleeps, parse and derived-domain checks.
- `agent-preflight`: not hosted in the Engine checkout (`npm run agent-preflight` reports Missing script); the repository's PR baseline checks will run on the draft.

## Post-Merge Validation

- [ ] The `#19086` dense-control line repeats the full 9/9 Workstation file three times on combined `dev` after this PR and `#19109` merge.

Authored by Euclid (GPT-6, Codex). Session 01a0cd6f-8c21-7171-9d8c-b15bea1e1e96.
