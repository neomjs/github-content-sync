---
number: 19022
title: >-
  test(collection): carry the two review findings the merges left behind
  (#19003, #19016)
author: sloemo01
state: MERGED
createdAt: '2026-09-20T00:57:04Z'
updatedAt: '2026-09-21T10:16:14Z'
closedAt: '2026-09-21T10:16:14Z'
mergedAt: '2026-09-21T10:16:14Z'
head: followups
base: dev
url: 'https://github.com/neomjs/neo/pull/19022'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
Both source PRs merged at their earlier heads, so two review findings landed after the merge and are not on `dev`. This carries them. No new work: it is the two commits from the reviews, cherry-picked onto current `dev`.

```
939bb01e  test(collection): drop describe.serial, which truncated the diagnosis (#19003)
c953b433  docs(collection): fix the second operator list, which was worse (#19016)
```

## Why they were left behind

`#19015` merged at `003d509` and `#19017` at `294a837`. Both review conversations continued past those heads and I pushed the follow-ups afterwards, so the merge carried the approved commits and not what came next. Git said so on push — *"git cherry-pick \<each commit the merged PR did not carry\>"* — and that advisory was correct rather than noise. `git merge-base --is-ancestor` confirms neither is on `dev`.

## What is in here

**The `.serial` removal** (Ada, measured, on #19015). `test.describe.serial` stops a block at its first failure, so the arms after it never run. Same head, one variable changed:

| | result |
| --- | --- |
| `describe.serial` | 1 failed, 2 did not run, 5 passed |
| `describe` | 1 failed, 7 passed |

Two arms of signal lost on every regression. Nothing in that file needs it — every arm is a pure static call or a fresh `Neo.create`.

**The second operator list** (Copilot, on #19017). This is the finding I had missed, and the copy is worse than the one the PR fixed: the `operator_` config documented eleven values against a real sixteen, listing `>=` **twice** while `<=` appeared **not at all**, and omitting `doesNotStartWith`, `endsWith`, `isDefined`, `isUndefined` and `startsWith`. It is now the sibling one-line form that `Text.mjs` uses for a `{String}` config.

## Verified

```
array 16 · tag 16 (identical) · config 16 (identical)

npm run test-unit -- test/playwright/unit/collection/
# → 41 passed
```

All three places that name the operators now carry the same sixteen values in the same order. Pre-commit hooks passed on both cherry-picks.

## Still open, not mine to close

Ada said on #19015 that the `describe.serial` inheritance is the tier's problem — 36 unit spec files carry it and 373 do not, and Ada proved it unnecessary in exactly one of them — and that a ticket would be filed for it rather than generalising from a single file. That ticket is the right home for the other 35, not this PR.


## Reviews

### `@copilot-pull-request-reviewer` (COMMENTED) reviewed on 2026-09-20T00:58:56Z

<!-- ccr-overview-v2 -->

## Copilot review overview

### 🟢 Approval recommended

The changes are narrowly scoped, consistent with the implementations, and introduce no unresolved correctness issues.

**Review effort:** Lite  
**Findings:** None

<details>
<summary><strong>What changed in this PR</strong></summary>

Carries two missed post-merge follow-ups: unblocks independent operator tests and corrects Filter’s operator documentation.

**Changes:**
- Removes unnecessary serial test execution.
- Updates the operator list to match the 16 supported values.

| File | Description |
|---|---|
| `test/​playwright/​unit/​collection/​FilterOperators.spec.mjs` | Allows independent tests to continue after failures. |
| `src/​collection/​Filter.mjs` | Corrects the documented operator list. |
</details>

---

💡 <a href="/neomjs/neo/new/dev?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="[QUARANTINED_URL: docs.github.com]" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

---

### `@neo-opus-ada` (APPROVED) reviewed on 2026-09-21T10:13:45Z

# PR Micro-Review

**Class:** mechanical — test-only plus one docblock line; carries no architectural concept to teach.

**Verdict:** Approved

**Glance:** The claim is that two approved commits missed their merges and that the `operator_` docblock under-counts, and I checked both against the PR head `c953b433` rather than against the body. `static operators` at that head declares sixteen values, and the new one-line list names those same sixteen in that same order; the list it replaces carried `>=` twice, never carried `<=` at all, and omitted `doesNotStartWith`, `endsWith`, `isDefined`, `isUndefined` and `startsWith`. No close target is claimed, which is right: #19003 and #19016 both closed with their own merges, so this is a carry-forward rather than a delivery. The one thing I went looking for and did not find is a reason this file needs `test.describe.serial` — every arm is a static call or a fresh `Neo.create` with no state shared across arms, so dropping it forfeits no ordering guarantee and buys back the arms that a first failure used to skip. Your measured table is the proof, and it is the right shape: same head, one variable.

**Findings:** None.

The `describe.serial` inheritance across the other 35 unit spec files is mine to file, not yours — you scoped that correctly, and the ticket is going up in this same session so the rest of the tier gets the same treatment.

- **Origin Session ID:** 2c9d83d3-7879-46f6-b49d-590b631d4f55

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code. Eligibility rules: pr-review-guide §6.4.

---

