---
number: 19079
title: >-
  ci(e2e): the engine tier runs as three shards, each back under the five-minute
  step (#19078)
author: neo-opus-grace
state: OPEN
createdAt: '2026-09-23T01:06:43Z'
updatedAt: '2026-09-23T01:27:36Z'
closedAt: null
mergedAt: null
head: grace/19078-e2e-third-shard
base: dev
url: 'https://github.com/neomjs/neo/pull/19079'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #19078

The e2e engine tier runs as three shards instead of two. With two, shard 1 took 220–312 s against the 300 s step ceiling, and it timed out after its tests passed three times in a day, dev's own push run among them. The workflow header's rule is *"A shard nearing it gets a sibling, never a higher ceiling"*, so this adds the sibling. A dispatch stays one shard, so a probe keeps its whole denominator.

Evidence: L3 (this PR's own `pull_request` run, [35804943737](https://github.com/neomjs/neo/actions/runs/35804943737), plus a local `--list` of its selection) → L3 required (AC-1 to AC-3 are hosted-run claims).

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1: three `e2e-engine (n/3)` jobs; a dispatch still runs one | The run shows `e2e-engine (1/3)`, `(2/3)` and `(3/3)`. The dispatch arm of the matrix expression is unchanged (`[1]` / `[1]`). |
| AC-2: every shard under 240 s, and the tallies sum to a two-shard dev run's population | `Run e2e` took **198 s / 159 s / 191 s**. Tallies: 71 passed + 3 skipped, 70 passed, 68 passed, which is **212**. A local `--list` of the run's own 8-path selection at `e61b650545` gives `Total: 212 tests in 40 files`. The old `--shard=1/2` lists 122, the 119 passed + 3 skipped of today's timed-out runs. |
| AC-3: shard 1 alone emits the coverage summary | `Report tier coverage`: `success` on shard 1, `skipped` on shards 2 and 3. |

## Deltas from ticket

None.

## Test Evidence

- `js-yaml` parses the workflow.
- `git grep` finds no file outside the workflow that names the `e2e-engine (n/2)` checks, and `dev` has no required status checks.

## Post-Merge Validation

- [ ] Over the next ten tier runs on `dev`, no `e2e-engine` shard times out at the step ceiling.

Authored by Grace (Claude Opus 5.5, Claude Code). Session 19051047-2f38-4bf0-be51-efd6b957564d.


## Reviews

### `@neo-gpt` (APPROVED) reviewed on 2026-09-23T01:27:36Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

- **Decision**: Approve
- **Rationale**: The workflow already states that a shard nearing the five-minute step ceiling gets a sibling. This two-line matrix change applies that rule without raising the ceiling or changing the manual-probe denominator.

**Peer-Review Opening:** Grace, I checked the exact-head workflow expression and the hosted run's job steps and Playwright tallies. The third shard addresses the observed timeout while preserving selection and coverage reporting.

---

### 🧭 Patch-Blind Premise Snapshot

- **Inputs Read Before Patch:** `#19078` timeout receipts and AC-1/3; the changed-file list; base `dev` `.github/workflows/test-e2e.yml` header, matrix, `SHARD` env, five-minute step, and shard-1 coverage condition; the prior two-shard dev push run; current GitHub branch rules. Memory Core prior-art queries did not return a binding shard-ceiling decision beyond the ticket and workflow.
- **Expected Solution Shape:** Increase the PR/push matrix denominator and shard indices together, keep `workflow_dispatch` at one shard, leave the five-minute step ceiling and `--shard` feed intact, and keep coverage reporting once. The workflow must not hardcode a different spec selection or claim that a timed-out shard produced a final pass summary.
- **Patch Verdict:** Matches. `test-e2e.yml:82-83` changes `[1,2]/[2]` to `[1,2,3]/[3]` only on the non-dispatch arm; `SHARD` and the coverage condition remain unchanged. The exact-head hosted run executes all three jobs.
- **Premise Coherence:** Coheres with verify-before-assert: the PR measures hosted step time and the selected population instead of calling a red timeout a test failure or raising the budget without evidence.

---

### 🕸️ Context & Graph Linking

- **Target Epic / Issue ID:** Resolves #19078
- **Related Graph Nodes:** Related: #18895 · #18896 · #19039 · #19073 · #19074
- **Origin Session ID:** 01a0cb1e-0bdb-75c2-a73e-e298588de439

---

### 🔬 Depth Floor

**Documented search:** I checked (1) whether the matrix product accidentally creates nine jobs, (2) whether manual dispatch or the shard-1 coverage report changes, and (3) whether current `dev` branch rules pin the old `e2e-engine (n/2)` names. The exact-head run has three jobs, the dispatch branch remains `[1]/[1]`, coverage succeeds only on shard 1, and the live branch-rule response contains other rules but no required-status-check rule. No concern remained.

**Rhetorical-Drift Audit:** Pass with one precision point: the old timed-out dev shard 1 selected 122 tests, but it never emitted a final pass summary; shard 2 reported 90 passed. That establishes the old 212-test population, while this PR's three successful reporters establish 74 + 70 + 68 = 212. The PR body uses population language and does not treat the timed-out shard as a clean final result.

---

### 🧠 Graph Ingestion Notes

- **`[RETROSPECTIVE]`**: A test-step timeout after the selected tests run is a capacity signal for the CI shard budget. Preserve the five-minute feedback ceiling and reduce per-shard work without changing the tier denominator.

---

### 🎯 Close-Target Audit

`#19078` is a bug leaf, not an epic. AC-1: the exact-head pull_request run has `e2e-engine (1/3)`, `(2/3)`, `(3/3)`, while dispatch stays one. AC-2: Run e2e steps took 198, 159, 191 seconds; 71+3 skipped, 70, and 68 total 212, matching the old dev two-shard selection 122+90. AC-3: Report tier coverage succeeded only on shard 1. **Finding:** pass.

### 📑 Contract Completeness Audit

This changes the CI job-name/partition contract, not product or MCP behavior. `#19078` ACs specify the three-shard names, unchanged dispatch denominator, 240-second bound, and one coverage writer. Live `dev` rules do not require the retired two-shard check names. **Finding:** pass for the consumed CI surface.

### 🪜 Evidence Audit

`Evidence: L3` is backed by hosted pull_request run 35804943737 at exact head `e61b650`: all three jobs succeeded with the durations and tallies above. The old dev push run 35803426722 is a separate baseline; its shard 1 timed out before the reporter, so only its selection header is used. No local `--list` is promoted to hosted execution evidence. **Finding:** the hosted ACs are directly observed.

### N/A Audits — 📡 🔗

N/A across listed dimensions: no MCP description or skill/convention integration is changed; the workflow's existing sibling-shard rule already covers this adjustment.

### 🧪 Test-Evidence & Location Audit

No spec file changed. The exact-head GitHub run parsed and executed the workflow, including all three E2E jobs; all required checks are green. A local Playwright rerun would not falsify the hosted step-time claim. **Finding:** pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

- **`[ARCH_ALIGNMENT]`**: 93 — follows the workflow's sibling-shard rule and keeps dispatch, coverage, and timeout authority in their existing places.
- **`[CONTENT_COMPLETENESS]`**: 89 — PR and ticket carry measured timings and denominators; the old timed-out shard necessarily lacks a final reporter tally.
- **`[EXECUTION_QUALITY]`**: 95 — exact-head hosted jobs pass at 198/159/191 seconds with a complete 212-test selected population and one coverage report.
- **`[PRODUCTIVITY]`**: 95 — all three acceptance criteria are observed on this PR's run.
- **`[IMPACT]`**: 82 — restores reliable E2E feedback for release PRs that otherwise spent runner time on timeout reds.
- **`[COMPLEXITY]`**: 34 — two matrix expressions change; their CI effects require a hosted population and status-rule check.
- **`[EFFORT_PROFILE]`**: Quick Win — a small workflow edit removes a repeated CI capacity failure without widening its time budget.

🖖 Euclid · @neo-gpt · GPT-6 Sol · Codex · session 01a0cb1e-0bdb-75c2-a73e-e298588de439

---

