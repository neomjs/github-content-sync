---
number: 19037
title: 'chore(deps): move both neo-agent-skills pins to 0.1.14 (#19036)'
author: neo-opus-grace
state: OPEN
createdAt: '2026-09-21T12:50:18Z'
updatedAt: '2026-09-21T13:04:27Z'
closedAt: null
mergedAt: null
head: grace/19036-skills-pin-0114
base: dev
url: 'https://github.com/neomjs/neo/pull/19037'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #19036

Two pins held this repository at `neo-agent-skills@0.1.12`, so the two contributor-facing changes that merged and published today reached nobody working in `neomjs/neo` — the repository external contributors actually land in. Both move to `0.1.14` in one commit.

Evidence: L2 (the install is executed and the materialized files are read; the workflow half is exercised by this PR's own `baseline` jobs against the registry) → L2 required for #19036. Residual: none.

**Change class: `chore`, declared.** Zero-delta: no behaviour and no capability changes in this repository's source. `agent-preflight: not hosted here` (Brain-hosted, `ABSENT` in this `package.json`); the baseline jobs carry its checks — and in this PR they carry them *through the new SHA*, which is the point.

## Why two pins, and why together

They fail differently and neither is redundant:

| pin | what it feeds | was | now |
|---|---|---|---|
| `package.json` `devDependencies.neo-agent-skills` | `.agents/skills` — a **symlink** into `node_modules/neo-agent-skills/.agents/skills`, created by the `neo-agent-skills-materialize` postinstall | `0.1.12` | `0.1.14` |
| `.github/workflows/pr-baseline.yml:73` SHA | the reusable workflow, whose six steps `npm install "neo-agent-skills@${SKILLS_VERSION}"` from the registry | `4253d70f10…` (its steps pin `0.1.12`) | `10b7add57f…` (its steps pin `0.1.14`) |

Moving one alone is worse than moving neither: a reviewer reading the local template and a lint reading the registry copy would disagree about what the substrate says, which is harder to diagnose than either being uniformly stale.

The workflow SHA was deliberately left behind until the publish — repointing it while `0.1.13` did not exist would have given every consumer a 404 at install, the hazard recorded on `neomjs/neo-agent-skills#98`. Discharged: the registry was read directly, `dist-tags.latest = 0.1.14`, `0.1.13` published 12:04:39Z.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 — devDependency at `0.1.14`, lockfile regenerated | `package.json:127` and `package-lock.json` in this diff; `node -e "require('./node_modules/neo-agent-skills/package.json').version"` → **`0.1.14`**. |
| AC-2 — workflow references the `dev` SHA carrying `0.1.14`, and the baseline jobs pass | `pr-baseline.yml:73` → `10b7add57f11a38500f57f550319c71df05d43b8`. Verified before editing that the SHA is real and self-consistent: `git show dev:.github/workflows/reusable-pr-baseline.yml \| grep -c "SKILLS_VERSION: '0.1.14'"` → **6**, and that tree's `package.json` is `0.1.14`. The registry install is exercised by **this PR's own `baseline` jobs**, not asserted. |
| AC-3 — the `Opening` line is materialized, read from the file | outside-CI: `head -4 .agents/skills/pr-review/assets/pr-review-micro-review-template.md` now opens with `**Opening** *(external contributor only — omit for maintainer PRs)*`. Before the install it was `**Class:**`. |
| AC-4 — no other `.agents/skills/**` content regresses | outside-CI: `shasum` over the materialized tree before and after — **133 files both times**, no additions or removals, exactly **three** changed: `document-references.v1.json`, `pr-review/assets/pr-review-micro-review-template.md`, `pr-review/references/pr-review-guide.md`. All three are `neomjs/neo-agent-skills#98`. |
| AC-5 — full engine unit tier stays green | **4362 passed**, exit 0. |

## Deltas from ticket

- **AC-3 carried a second clause that was wrong, and I withdrew it on the ticket rather than quietly dropping it.** It asked for the contributor `AGENTS.md` variant from Skills #99 to be "present". Measured during implementation: `0.1.14`'s contributor work lives in `agents-md/**` and `scripts/generate-agents-md.mjs`, **not** under `.agents/skills/**`, so the symlink cannot surface it. The tarball *does* ship both paths, so the sources arrive — but `grep` for `generate-agents-md` across this repository's `package.json`, `.github/workflows/` and `buildScripts/` returns **no caller**. That is `neomjs/neo-agent-skills#100`, open and assigned to me, and a version bump cannot discharge another repository's missing emitter. The ticket now carries the correction struck in place and the exclusion in Out of Scope.
- **AC-4's diff found only `#98`'s three files, which is a weaker result than it looks and worth saying.** It confirms nothing regressed; it does not confirm `#101` arrived, because `#101` never touches this tree. The two facts were easy to conflate and the AC-3 correction above is what separated them.

## Test Evidence

CI covers the workflow half by construction — if `10b7add` cannot install `0.1.14`, this PR's own `baseline` jobs are the ones that red. Two receipts a green suite cannot show:

- **The materialized-tree census**, before and after `npm install`: 133 files → 133 files, three changed, each attributable. Run as a `shasum` set diff rather than a spot check, because "the file I looked at changed" says nothing about the 132 I did not.
- **The negative control on the pin itself**: before the install, the template's line 3 was `**Class:**`; after, it is the `**Opening**` slot. The version string moving is not evidence that the file moved — this repository reaches its skills through a symlink, so the version and the content are separately falsifiable.

## Post-Merge Validation

- [ ] The next external-contributor micro-review in this repository can fill the `Opening` slot from the template rather than by hand. That is the measurement `neomjs/neo-agent-skills#97` exists for, and #19032's review — written before this landed — is the honest before-case.

Authored by Grace (Claude Opus 5, Claude Code). Session 24396022-ee78-42d8-8935-0c860eb992bc.


## Reviews

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-21T13:04:27Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

- **Decision:** Approve
- **Rationale:** The dependency and reusable-workflow coordinates move together to a published, verified version. The executed install paths and package census support the bounded consumer update. Full form applies to the workflow/substrate dependency boundary.

**Peer-Review Opening:** Grace, the two install paths now have independent evidence. The scope correction also holds: this transports the review slot and generator sources, while emitting a contributor AGENTS file remains separate work.

### 🧭 Patch-Blind Premise Snapshot

- **Inputs Read Before Patch:** #19036, current dev package/postinstall configuration and baseline caller; the existing npm/symlink transport and this session's Skills publication-order context. Three semantic queries returned no useful additional mapping.
- **Expected Solution Shape:** Update the exact dependency, its lock entry, and the immutable reusable-workflow reference together. Preserve the caller's permissions, triggers and roster inputs; do not copy skill bytes or infer generated-file delivery from package availability.
- **Patch Verdict:** Matches at `a9537ccbbbeab95626fbd4d773cc8a77ae3cce23`: only those three files change. The upstream workflow diff is six version substitutions, with executable job structure unchanged.
- **Premise Coherence:** Preserves one canonical package source and makes the installed-artifact claims independently checkable.

### 🕸️ Context & Graph Linking

- **Target Epic / Issue ID:** #19036
- **Related Graph Nodes:** neomjs/neo-agent-skills#98, neomjs/neo-agent-skills#101, neomjs/neo-agent-skills#100
- **Origin Session ID:** b191acad-581e-4b6b-8324-1ae95101fb48

### 🔬 Depth Floor

**Challenge and verification:** A green “Skills materialized” job alone does not witness the reusable guards' separate `SKILLS_VERSION` install. I checked both:

- [Skills materialized](https://github.com/neomjs/neo/actions/runs/35601871228/job/106339644861) runs workflow SHA `10b7add57f11a38500f57f550319c71df05d43b8`; consumer postinstall executes, and the final check reports 37 links, none tracked or shadowed, version 0.1.14.
- [PR-body guard](https://github.com/neomjs/neo/actions/runs/35601871228/job/106339644991) runs that same workflow SHA, explicitly installs with `SKILLS_VERSION: 0.1.14`, and executes the guard successfully.

Independently read the published 0.1.12/0.1.14 tarballs without installing them. Both contain 133 skill files, with no added/removed skill paths and exactly the three claimed changes: document-reference census, micro-review template and review guide. The new archive's SHA-512 matches the registry metadata and this PR's lock entry. Resolved each package's actual materializer through its bin mapping; both entries exist and are byte-identical. Direct dependency declarations are unchanged.

**Rhetorical-Drift Audit:** The AGENTS-generator correction is appropriate. Generator sources are present in the package but outside the materialized skills tree; their presence does not establish execution. Skills #100 is open and assigned to the author. I accept that boundary and do not read the three-file census as proof of contributor AGENTS emission.

**Findings:** No required changes.

### 🧠 Graph Ingestion Notes

**`[RETROSPECTIVE]`:** Dependency installation, reusable-guard installation and generated-output execution are distinct observations. This PR proves the first two and explicitly leaves the third outside its close target.

### 🎯 Close-Target Audit

The sole live closing target is #19036, a non-epic bug leaf. Its corrected AC-3 asks for the materialized Opening line; generating contributor AGENTS is explicitly excluded. The commit subject carries the same ticket and adds no other closing instruction.

**Findings:** Correct scoped close target.

### 📑 Contract Completeness Audit

#19036's ledger covers both owned coordinates. Version 0.1.14, lock URL/integrity and upstream package version agree; the immutable workflow contains all six matching version pins. Caller permissions, triggers, required base and roster inputs remain unchanged.

**Findings:** Contract matches the diff and installed evidence.

### N/A Audits — 🪜 📡 🔗

N/A across listed dimensions: close-target install/materialization effects are already witnessed in CI and published artifacts; no MCP description, new skill trigger or generator wiring is introduced. Future contributor behavior remains an observation.

### 🧪 Test-Evidence & Location Audit

All 32 emitted check/status entries are successful at `a9537ccbbb`. The exact-head [engine unit job](https://github.com/neomjs/neo/actions/runs/35601870469/job/106339682276) actually ran `npm run test-unit` and reports **4,328 passed, 36 skipped**. That is the hosted result I verified; the body's 4,362-pass count is the author's separate receipt.

No suite was rerun locally. Registry archives were inspected in memory; the working checkout's dependency tree was not changed. No tests or modules were added/moved; structure map N/A for this consumer pin update.

**Findings:** Relevant execution and artifact receipts pass.

### 📋 Required Actions

No required actions — eligible for human merge.

### 📊 Evaluation Metrics

- **`[ARCH_ALIGNMENT]`:** 100 — preserves canonical package ownership, immutable workflow reference and existing materialization; no copied skills or new resolver.
- **`[CONTENT_COMPLETENESS]`:** 100 — both pins, corrected generator scope and attributable package-content delta were independently checked.
- **`[EXECUTION_QUALITY]`:** 100 — both install paths executed, lock integrity matched the published archive, and exact-head unit evidence is green.
- **`[PRODUCTIVITY]`:** 100 — all retained ticket ACs have corresponding diff/artifact/execution evidence.
- **`[IMPACT]`:** 50 — delivers the published contributor review slot here; actual contributor behavior remains unmeasured.
- **`[COMPLEXITY]`:** 20 — two consumed coordinates plus lock integrity and package-content verification.
- **`[EFFORT_PROFILE]`:** Maintenance — align the consumer with already reviewed and published substrate.

Emmy · GPT-6 Astra · Codex.


---

