---
number: 19017
title: 'docs(collection): move the operator list into the member tag (#19016)'
author: sloemo01
state: MERGED
createdAt: '2026-09-20T00:06:43Z'
updatedAt: '2026-09-20T00:20:39Z'
closedAt: '2026-09-20T00:20:39Z'
mergedAt: '2026-09-20T00:20:39Z'
head: fix/filter-operator-doc-comment
base: dev
url: 'https://github.com/neomjs/neo/pull/19017'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
Closes #19016

A two-line doc change. The operator list sat on its own line above the `@member` tag, where nothing ties it to the declaration it describes, and it had drifted: the array shipped sixteen operators and the prose listed fifteen, so `doesNotStartWith` was accepted and working while a reader trusting the comment concluded it did not exist.

## The change

```diff
-     * Valid values for the operator config:<br>
-     * ['==','===','!=','!==','<','<=','>','>=','endsWith','excluded','included','isDefined','isUndefined','like','startsWith']
-     * @member {String[]} operators
+     * Valid values for the operator config
+     * @member {String[]} operators=['==','===','!=','!==','<','<=','>','>=','doesNotStartWith','endsWith','excluded','included','isDefined','isUndefined','like','startsWith']
```

The list moved into the tag rather than gaining a word where it was. That is the form every sibling uses:

```
src/form/field/Text.mjs:16   @member {String[]} autoCapitalizeValues=['characters','none','on','off','sentences','words']
src/form/field/Text.mjs:23   @member {String[]} labelPositions=['bottom','inline','left','right','top']
src/form/field/Base.mjs:14   @member {String[]} isTouchedEvents=['focusEnter','focusLeave']
```

So this removes the free-floating copy instead of correcting it. A doc comment that restates a list in its own paragraph is a copy nothing checks; one inside the tag is at least visible as part of the member it belongs to. A scan of every `src/**/*.mjs` finds no other file restating a list that way, so `Filter.mjs` was the last instance of the pattern rather than one of several.

`static operators` is untouched. It is the enforced vocabulary through `beforeSetEnumValue`, and nothing about it was wrong.

## Verified

The doc tag and the array are now identical, in order, with no operator named twice and every entry implemented:

```
doc tag: 16   array: 16   impls: 16
doc == array exactly, in order? True
array-only entries: []   doc-only entries: []
every array entry implemented? True
```

```bash
npm run test-unit -- test/playwright/unit/collection/
# → 41 passed
```

That includes `FilterOperators.spec.mjs` from #19015, which asserts against the array rather than the comment, so it holds either way. I re-ran it after the commit because the repo's `check-block-alignment` hook runs with `--fix`.

## Not changed

`Neo.collection.Sorter` does **not** repeat the free-floating pattern, so there is nothing to report there. #19007 remains its own ticket for the coverage gap.

## AI assistance

The change is mine; no draft was needed for two lines. I ran the sibling scan and the doc-versus-array comparison myself rather than reading them off, and the pre-commit hooks (whitespace, shorthand, JSDoc types, block alignment, parse, class module scope, engine/brain boundary) all passed on the commit.


## Comments

### `@neo-opus-grace` commented on 2026-09-20T00:09:29Z

## Substance verified; holding the formal verdict until CI lands — and one merge-order fact you should know

Verified at `294a837364` while CI runs:

```
doc tag: 16 · array: 16 · IDENTICAL IN ORDER: True
FilterOperators.spec.mjs → 8 passed
```

So both of your self-claims hold: the tag and the array agree entry-for-entry and in order, and the spec from #19015 stays green without modification — which is what it was built to do.

**The route you took is better than either of the two your own ticket proposed.** #19016 offered "add the missing operator to the prose" or "stop duplicating"; you took a third — move the list into the `@member` tag's default-value position. That is the repo's own JSDoc convention (`@member {Type} name=default`), so the list now sits where the declaration and the doc generator both read it, rather than floating above as free prose. It does not fully dissolve the duplication — the tag still restates the array — but it removes the part that actually drifted: a list with no structural tie to the thing it describes.

### The merge-order fact

**#19017 stacks on #19015.** Its history is `294a837364` → `003d5098a1` → `626051de3f`, so this PR currently contains your spec commit as well, which is why GitHub shows it as +199/-3 rather than +2/-3.

That is fine and nothing needs changing, but it means order matters:

- **#19015 merges first** → this PR collapses to the two-line doc change on its own. Cleanest, and it is the order I would expect since #19015 is already approved.
- **#19017 merges first** → #19015 becomes empty and should be closed rather than merged.

I have flagged it for @tobiu, who does the merges. No action from you.

### One note for whenever you next rebase

Both branches are based on `626051de3f`; `dev` has since moved to `d06dea4296`. No conflict risk for these two files, so there is no need to rebase for this pair — mentioning it only so a stale-base warning later does not read as a problem.

Formal review once `gh pr checks` goes green; I do not sign off on a pending run.

🖖 Grace

---

### `@neo-opus-ada` commented on 2026-09-20T00:13:28Z

⚠️ **Merge order, @tobiu — #19015 must go first.** This branch is stacked on it, not on `dev`:

```
$ git merge-base --is-ancestor <19015 head> <19017 head>
YES

$ git log --oneline origin/dev..pr-19017
294a837364 docs(collection): move the operator list into the member tag (#19016)
003d5098a1 test(collection): pin the Filter operator matrix (#19003)   ← #19015
```

So this PR's diff is `+199/−3` rather than the two doc lines its body describes: it carries #19015's 197-line spec as well. Squash-merging this one first lands that spec under *this* commit message, leaves #19015 empty, and **#19003 never gets closed** — the ticket silently outlives the work that resolved it.

Merge #19015, then this. Nothing needs changing on either branch.

@sloemo01 — this is not a mistake on your part, it is what branching off your own in-review work correctly looks like, and you described the dependency in the body. The only thing it changes is the order someone merges them in, which is ours to get right. Worth knowing for next time: branching off `dev` instead means the two can merge in any order, at the cost of not being able to run your new spec against your doc change locally — which is exactly what you wanted to do here, so the tradeoff you took was the right one.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

---



## Reviews

### `@copilot-pull-request-reviewer` (COMMENTED) reviewed on 2026-09-20T00:09:41Z

<!-- ccr-overview-v2 -->

## Copilot review overview

### 🔵 Needs a closer look

A moderate coverage gap and several stale or unclear documentation and scope details remain.

**Review effort:** Lite  
**Findings:** None

<details>
<summary><strong>What changed in this PR</strong></summary>

Updates `Neo.collection.Filter` documentation and includes operator behavior coverage.

**Changes:**
- Moves the complete operator list into the `@member` declaration.
- Adds coverage for operator behavior and edge cases.

| File | Summary | Review findings |
|---|---|---|
| `src/​collection/​Filter.mjs` | Updates the documented operator list. | **Nit, 1 vote:** another stale `operator_` JSDoc list remains, with omissions and a duplicated `>=`. |
| `test/​playwright/​unit/​collection/​FilterOperators.spec.mjs` | Provides operator-matrix and edge-case tests. | **Moderate, 1 vote:** fixtures are not verified against `Filter.operators`. **Nits, 1 vote each:** stale “unpinned” and “undocumented” claims remain, and the dependency on open PR #19015 is unclear. |
</details>

---

💡 <a href="/neomjs/neo/new/dev?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="[QUARANTINED_URL: docs.github.com]" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

---

### `@neo-opus-ada` (APPROVED) reviewed on 2026-09-20T00:18:42Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve (after #19015 — see the merge-order comment above)
- **Rationale**: You did not correct the stale copy, you **removed the thing that could go stale**. That is the difference between fixing a defect and retiring its category, and it is the harder of the two to choose when the one-word fix is sitting right there.

**Peer-Review Opening:** Thank you — and specifically for this sentence: *"this removes the free-floating copy instead of correcting it."* Adding `doesNotStartWith` to the prose would have closed the ticket, passed review, and left the same trap armed for the next operator. You went and looked at what the siblings do first. Three artifacts in one evening and this is the one that shows the most judgement.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** `#19016`, `src/collection/Filter.mjs` on `dev`, the three siblings you cite, and `buildScripts/docs/docletPipeline/memberDefaults.mjs` — the stage that decides what a `@member` default actually publishes.
*   **Expected Solution Shape:** Either add the missing operator to the prose, or move the list into the tag. I expected the first and hoped for the second.
*   **Patch Verdict:** The second, with the sibling precedent cited rather than asserted. Verified: **165** files in `src/` use the in-tag `@member {String[]} name=[…]` form, so this is the house pattern and not a local invention.
*   **Premise Coherence:** Holds. Your framing is that a reader trusting the comment concludes `doesNotStartWith` does not exist — correct, and I checked it is a **source** reader rather than a docs-app reader: `Neo.collection.Filter#operators` is not published as a member doclet at all (only the class doclet is), while 54 other protected-static array members are. That does not weaken the ticket; it locates it.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Closes #19016
*   **Related Graph Nodes:** #19015 (this branch's parent commit), #19003, `publishedMemberDefault`
*   **Origin Session ID:** 1246b3b3-6cd1-4073-9e20-7bf593f2a22d

---

### 🔬 Depth Floor

**Challenge**: None against the diff. One finding *adjacent* to it, which your change makes safer rather than riskier.

**Your new tag is 168 characters — 42% longer than the longest one the docs pipeline has ever been handed** (`Resizable.mjs` `validDirections`, 118). So I went and read `publishedMemberDefault`, which re-derives an array default from the raw comment text:

```js
let defaultValue = item.comment.substr(item.comment.indexOf('=') + 1);
return defaultValue.substr(0, defaultValue.indexOf('\n'))
```

**The first `=` in the entire comment block wins.** Your prose line is `Valid values for the operator config` — no `=` — so the slice starts at your tag's own `=` and the full list survives. Length is a non-issue; there is no truncation anywhere in that function.

And here is the part in your favour: **the line you deleted contained `'=='`.** Had `operators` been a published member doclet, the old comment's first `=` would have been inside that prose list, and the docs would have shown a fragment of your own operator list as the default. Your change removed a live instance of a real hazard, not just a stale sentence.

That hazard **is** firing elsewhere. Nineteen published member defaults in the docs output are prose fragments rather than values, because a `=>` or `==` sits above the tag:

```
Resizable.mjs:36    validDirections  => "> resize"
MapboxGL.mjs:37     layers           => "> map id, value => {Array} layers"
ServiceBase.mjs:122 networkFirstPaths=> "= SW v2`) and command a reload."
```

Not yours to fix and explicitly not this PR's scope — I am filing it, and your change is what led me to look. The module's own docblock enumerates three silent edges of that function; this is a fourth it does not name.

**Rhetorical-Drift Audit:** Pass. One note on method, offered because it is a good habit rather than a correction: *"A scan of every `src/**/*.mjs` finds no other file restating a list that way"* is an **absence claim**, which is the kind that most often rests on a broken instrument. I verified it and it is **true** — exactly one hit on `dev`, `Filter.mjs:18`, zero at your head. But my first instrument returned **zero on `dev` too**, which would have "confirmed" you for the wrong reason: BSD `grep -E` on macOS mishandles the `[^]]` character class and the pattern silently never matches. When you make an absence claim, it is worth running the instrument once against a case you *know* is there.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: `publishedMemberDefault` keys on the comment's first `=` rather than the `@member` tag's, corrupting 19 published defaults. Filing separately; not caused by this PR.
*   **`[RETROSPECTIVE]`**: The reusable judgement here is *retire the category, do not patch the instance*. A restated list is a copy nothing checks; the fix was to stop having one, and it took reading three siblings to know that was available.

---

### N/A Audits — 📑 🪜 📡 🔗
N/A across listed dimensions: two doc lines, no behaviour, no public or OpenAPI surface, no convention substrate beyond adopting the existing one.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #19016
- [x] Confirmed not `epic`-labeled

**Findings:** Pass. `Closes #19016` only; #19003 is left to #19015, which is correct — and is also why the merge order matters.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: 30 checks green at `294a837`
- [x] Reviewer falsifier: the tag-versus-array comparison re-run independently, plus the docs pipeline exercised end to end
- [x] Test location: no new test; the existing `FilterOperators.spec.mjs` asserts the array, which is the right thing for it to assert

**Findings:** Pass. What I ran at your head:

1. **`static operators` is byte-identical to `dev`** — `diff` of the array block, no output. Your claim.
2. **Tag versus array, parsed rather than eyeballed:** `tag: 16  array: 16`, `identical, in order? true`, `tag-only: []`, `array-only: []`, no duplicates. Your numbers reproduce.
3. **The whole diff is the two doc lines** — `git diff origin/dev...` on `Filter.mjs` shows `−3/+2` and nothing else. The `+199` in the PR summary is #19015's spec riding along on the parent commit.
4. **`npm run generate-docs-json` at your head: clean, 4.47s**, and I read the emitted `all.json` rather than trusting the exit code. No truncation, no malformed doclet, no new suspect default.

---

### 📋 Required Actions

No required actions. One sequencing constraint that is not yours: **merge #19015 first**, per the comment above.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 - Adopts the form 165 files already use, cites three of them, and leaves the enforced vocabulary alone.
*   **`[CONTENT_COMPLETENESS]`**: 92 - Diff, precedent, verification, and the two negative claims (`Sorter` unaffected, array untouched) all stated and all true.
*   **`[EXECUTION_QUALITY]`**: 94 - Minimal, and minimal in the right direction: one fewer copy rather than one more correct copy.
*   **`[PRODUCTIVITY]`**: 90 - Filed the ticket, then closed it, having found it while doing something else.
*   **`[IMPACT]`**: 60 - Two lines. Its value is the copy that no longer exists.
*   **`[COMPLEXITY]`**: 20 - Trivial to write, and the judgement was the whole job.
*   **`[EFFORT_PROFILE]`**: Quick Win

---

**Some orientation, since you are three artifacts in and the repo does not advertise any of this.**

- **Tiers.** `test/playwright/` has separate configs: `unit` (Node, no browser — where `Filter` belongs), `component`, `e2e`, `matrix`, `visual`. Run them by name: `npm run test-unit`, `test-components`, `test-e2e`. A bare `npx playwright test` silently picks up the wrong config and passes; you already avoided it, and your PR body saying so is why I trusted the rest of your numbers.
- **Pick the tier by what the thing is.** Pure comparators like `Filter`'s go in `unit`. Anything **visual** belongs in `component` or `e2e`, because a unit assertion on a rendered thing can usually only read back the value you just set. This is worth knowing before you look at #19006 or #19010, where the answer is not `unit`.
- **Why `setup()` comes before the imports.** App code runs in a **Web Worker**, not on the main thread. `setup()` stands the worker environment up before the modules that expect it are evaluated. Most of the genuinely subtle bugs in this codebase live at that boundary.
- **Architecture decisions live in `learn/agentos/decisions/`.** When something looks arbitrary, there is usually an ADR saying why.
- **See what you have been testing:** `npm run build-all`, then open `apps/workstation` — and drag a tab *outside the browser window*. The `Filter` you pinned in #19015 is what keeps those grids in sync once they are in separate windows. `learn/benefits/Introduction.md` is the short version of why any of this is built the way it is.
- **Ask.** Questions on a ticket are welcome and cost us nothing; guessing costs you an evening. *"Still on it, stuck on X"* is a perfectly good comment.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

---

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-20T00:20:00Z

# PR Micro-Review

**Class:** mechanical — a two-line doc-comment change; no operator, no validation, no behaviour.

**Verdict:** Approved

**Glance:** The claim is that the doc tag and the array describe the same set and that #19015's spec stays green without modification. Verified at `294a837364`, both hold:

```
doc tag: 16 · array: 16 · IDENTICAL IN ORDER: True
FilterOperators.spec.mjs → 8 passed
```

`gh pr checks` exit 0, 13/13. `static operators` is untouched, which is right — it was never the thing that was wrong.

**The route is better than either your own ticket proposed.** #19016 offered "add the missing operator to the prose" or "stop duplicating"; you took a third — moving the list into the `@member {String[]} operators=[…]` default-value position. That is this repo's JSDoc convention, so the list now lives where the declaration and the doc generator both read it instead of floating above as untethered prose. It does not fully dissolve the duplication, and you did not claim it did; it removes the part that actually drifted.

**On the stacking, now resolved:** #19015 was **squash-merged** at `00:16:34Z` (`1f64482fc8`), so your branch's original spec commit is not an ancestor of `dev` and GitHub still renders this as +199/-3. It is cosmetic — the spec blob is byte-identical on both sides (`925fc0cbd3` on `dev` and on your branch), so the merge is a no-op for that file and the net change reaching `dev` is the two doc lines. Nothing for you to rebase.

**Findings:** None.

- **Origin Session ID:** eb5c78bf-b451-4ed0-ae82-2e9c60e8cbff

---

Three PRs and a ticket in one night, and the ticket was the best artifact of the four — it verified its own premise, named the validation mechanism, scoped itself to documentation, and told the next reader how to interpret a failure. Then you answered it yourself with a better option than the two you had written down.

If you want a next one: **#19007** (Sorter coverage) is the natural sequel — `Neo.collection.Sorter` has the same shape as `Filter`, and your own #19016 asked whether it repeats the doc/array duplication. You would be the right person to find out.

🖖 Grace, Opus 5, Claude Code. Eligibility rules: pr-review-guide §6.4.


---

