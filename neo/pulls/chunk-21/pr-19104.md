---
number: 19104
title: 'fix(worker): bind onerror on the worker object for a SharedWorker (#19092)'
author: sloemo01
state: CLOSED
createdAt: '2026-09-23T09:46:43Z'
updatedAt: '2026-09-24T10:52:25Z'
closedAt: '2026-09-24T10:52:25Z'
mergedAt: null
head: fix/19092-sharedworker-onerror
base: dev
url: 'https://github.com/neomjs/neo/pull/19104'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
## What this is

`Neo.worker.Manager#createWorker` bound its error handler through the same ternary as `onmessage`:

```js
(isShared ? worker.port : worker).onerror = me.onWorkerError.bind(me);
```

For a dedicated `Worker` that is right. For a `SharedWorker` it is not: a load or parse failure fires `error` on the `SharedWorker` object, and `worker.port` is a `MessagePort` with no `error` event, so the assignment created a property nothing calls. `onWorkerError` never ran for a SharedWorker, whose own comment calls it the only signal for one that fails to load or parse.

The binding now lives on the worker object in both modes, with `onmessage` staying on the port.

## Verification

Both arms were run on `dev@9c5b5f9b7c` without the fix, and again on `b3867db547` with it.

| arm | without the fix | with it |
|---|---|---|
| SharedWorker | `onWorkerErrorCalls: 0` | `1` |
| dedicated worker (control) | `1` | `1` |

The harness stubs `Worker` and `SharedWorker` with a class whose `port` is a prototype-less object carrying only a `start` method, so an assigned `port.onerror` cannot read as bound the way it would on a literal. It fires a load error the way a browser does, on the worker object, and counts calls into a replaced `onWorkerError`.

```
npm install
npm run bundle-browser-deps
npm run test-unit -- test/playwright/unit/worker/ManagerWorkerError.spec.mjs
```

`2 passed`. The full worker unit directory is `72 passed`, so nothing neighbouring moved. Reverting only `src/worker/Manager.mjs` turns the SharedWorker arm red again at `Expected: 1, Received: 0` while the control stays green, which is the two-direction check the ticket asks for.

## Scope

`onWorkerError`'s body is untouched. What it logs, errors thrown inside a SharedWorker after it loads, and telling the app worker that a canvas worker failed to start are all out of scope per the ticket.

## Disclosure

Assisted by Hermes Agent (deepseek-v4.1-flash). The ticket's harness was reproduced before the fix and the four assertions were read against their output; the port stub was changed to `Object.create(null)` after the first run showed a message handler landing on the port while the error handler read as absent, which is the distinction the spec exists to make.

Fixes #19092


## Comments

### `@neo-opus-ada` commented on 2026-09-23T09:53:46Z

@sloemo01, thank you: 42 minutes from filing to a two-arm fix. I'll post the formal review once CI finishes on this head.

Some context, since you're now one of our most regular contributors, and we should have given you this earlier:

- **Where this fix fits.** #19092 is the prerequisite of #19103. When two main windows of one app share a canvas worker in Chromium, the second window kills the first window's canvases. That crash gates the next engine release, and #19103's design needs a load-failure signal that, until your fix, never arrived.
- **What we're working on.** The engine's next release, 13.2, is its first since the project split into several repositories. [`ROADMAP.md`](https://github.com/neomjs/neo/blob/dev/ROADMAP.md) says what ships and why, and the [v13.2 milestone](https://github.com/neomjs/neo/milestone/9) holds the open work. The repositories release independently: this engine on npm as `neo.mjs`, the Brain as container images, the agent skills as `neo-agent-skills` on npm. #19106 adds exactly this to `CONTRIBUTING.md`, because it wasn't there.
- **Your next step.** We're reserving good first issues for first-time contributors through Hacktoberfest (October), so there's always a way in for the next person. For your next PR, look at `help wanted` or an unassigned issue in the v13.2 milestone, and comment to claim it the same way. Right now `help wanted` is thin. I'm refreshing it with current, self-contained roadmap work over the next day, and I'll ping you here when it's there.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5.5 · Claude Code

---

### `@neo-opus-ada` commented on 2026-09-23T11:50:19Z

## `[MERGE_HOLD]`

I am withdrawing my approval. The fault is in my ticket #19092, not in this PR.

@sloemo01, you built exactly what #19092 asked for, and the two-direction receipt was careful work. The ticket's premise was wrong, and I did not test it before filing. #19092 claimed a SharedWorker that fails to load "reaches nobody", taking that from `onWorkerError`'s own comment ("the ONLY signal for a SharedWorker that fails to load or parse"). Checked today in Chrome 152:

| probe | `SharedWorker.onerror` | `port.onerror` | page console |
|---|---|---|---|
| SharedWorker, script 404 | 1 | 0 | `Failed to fetch a worker script.` |
| SharedWorker, module parse error | 1 | 0 | `Failed to fetch a worker script.` |
| dedicated Worker, script 404 | 1 | n/a | `Failed to fetch a worker script.` |

`'onerror' in MessagePort.prototype` is `false`, so the old port binding is dead code, as the ticket said. But the browser already reports the failure in the page console for both kinds of worker. Moving the binding would only add a duplicate `console.error` line. Nothing in the code consumes it, so this PR has no effect worth merging.

I'm closing #19092 as not planned. The programmatic start-failure signal the crash slice needs moves into #19103, which will bind its own listener. Thank you, and sorry for the wasted effort. If you'd like another good first issue, I'm glad to point you to one I've verified properly this time.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5.5 · Claude Code


---

### `@neo-opus-ada` commented on 2026-09-24T10:52:24Z

Closing this: as my `[MERGE_HOLD]` above says, it has no effect worth merging, and #19092 is closed as not planned. What it was for carried over: #19125 (for #19103) binds its own `error` listener on the canvas worker object, which is where your receipt showed the event fires. Thank you again, @sloemo01. Your #19150 has a review requested now.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5.5 · Claude Code

---



## Reviews

### `@copilot-pull-request-reviewer` (COMMENTED) reviewed on 2026-09-23T09:49:16Z

<!-- ccr-overview-v2 -->

## Copilot review overview

### 🟢 Approval recommended

The fix is focused and regression-tested for both worker types.

**Review effort:** Lite  
**Findings:** None

<details>
<summary><strong>What changed in this PR</strong></summary>

Fixes `SharedWorker` load-error handling by binding `onerror` to the worker object while preserving port-based message handling.

**Changes:**
- Corrects error-handler binding in `Manager#createWorker`.
- Adds regression coverage for SharedWorker and dedicated Worker behavior.

| File | Summary |
|---|---|
| `test/​playwright/​unit/​worker/​ManagerWorkerError.spec.mjs` | Verifies error handling in both worker modes. |
| `src/​worker/​Manager.mjs` | Binds worker errors on the worker object. |
</details>

---

💡 <a href="/neomjs/neo/new/dev?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="[QUARANTINED_URL: docs.github.com]" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

---

### `@neo-opus-ada` (DISMISSED) reviewed on 2026-09-23T11:34:48Z

# PR Micro-Review

**Class:** contained. One handler binding moves to where the browser fires the event, and the PR adds the spec #19092 prescribed. No new abstraction, contract or config.

**Verdict:** Approved

**Glance:** Thank you, @sloemo01, and sorry for the wait after CI went green. This is exactly the fix and the harness the ticket asked for, and your two-direction receipt is the evidence #19092 needed. The claim: a SharedWorker's load or parse failure fires `error` on the `SharedWorker` object, and `worker.port` is a `MessagePort` with no `error` event. So `onWorkerError` has to be bound on `worker` in both modes, while `onmessage` stays on the port. Checked at exact head `b3867db547`: the diff changes only that binding in `createWorker` and adds the new spec. It merges cleanly onto today's `dev` (`6bfb02fc16`), and no commit has touched `src/worker/Manager.mjs` since the PR opened. CI's `unit` job ran both new tests (✓, 4,389 passed). I looked for another binding in `src/` that still assumes the port and found none; `worker/Base.mjs` sets `globalThis.onerror` inside the worker, which is a different signal. Nothing outside the prescription changed, and `onWorkerError`'s body is untouched.

**Findings:** None. No required actions; eligible for human merge.

Optional, no action needed: the stub comment says a prototype-less `port` keeps the probe honest. But `'onerror' in {start(){}}` is already `false`, and it becomes `true` once assigned, exactly like the `Object.create(null)` version. Your call-count and `boundOnPort` assertions already discriminate, so the comment can stay or go.

- **Origin Session ID:** 3be453e4-8b04-4865-be62-4cff34f4e0c6

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5.5 · Claude Code

---

