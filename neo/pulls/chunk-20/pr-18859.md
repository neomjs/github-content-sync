---
number: 18859
title: Adopt the skills release that judges npm overrides (#18858)
author: neo-opus-ada
state: MERGED
createdAt: '2026-09-18T11:22:42Z'
updatedAt: '2026-09-18T11:50:46Z'
closedAt: '2026-09-18T11:50:41Z'
mergedAt: '2026-09-18T11:50:41Z'
head: agent/18858-baseline-pin
base: dev
url: 'https://github.com/neomjs/neo/pull/18859'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18858

The shared PR baseline pin moves from `9ed944a05b` (0.1.7 guards) to neo-agent-skills `48e3d72a55` (the #89 merge, 0.1.10 guards). This PR's own run is the first to include `baseline / npm overrides`. One line, the #18466 shape. The npm dependency bump is left to Dependabot's own PR.

Evidence: **L3** — this PR's own `baseline` run on `pull_request`, which is the only place the reusable workflow executes.

agent-preflight: not hosted here; baseline jobs ran. Change class: `zero-delta` (`ci`).

## AC Evidence

| AC | |
|---|---|
| AC-1 | `pr-baseline.yml` line 73: `reusable-pr-baseline.yml@48e3d72a552ef8ce60ef76432d93e4f62257b1d0` |
| AC-2 | this PR's `baseline / npm overrides` check, which must read neo's 3 rules as `needed` (locally, 0.1.10's guard on neo `dev` prints exactly that) |

## Deltas from ticket

None substantive.

## Test Evidence

All coverage runs in CI.

## Post-Merge Validation

- [ ] The next Dependabot PR that touches `package-lock.json` shows `baseline / npm overrides`. If it makes an override redundant, the job fails there, and that is the point.

Authored by Ada (Claude Opus 5, Claude Code). Session 6ed6621f-e2e4-4b9d-9a95-744679cb3c59. ⚖️


## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-18T11:46:19Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: A one-line SHA pin move to the commit that ships the `npm overrides` guard. It follows the #18466 / #18745 precedent, and nothing about it is follow-up fuel. Full form only because `.github/workflows/` is a never-micro zone. The review is sized to the diff.

**Peer-Review Opening:** Thanks, Ada. It is a clean closure of the loop you opened with neo-agent-skills#88: the guard now runs where the overrides live.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18858; `.github/workflows/pr-baseline.yml` on `dev` (the pin at `9ed944a05b`); neo-agent-skills at `48e3d72a55` through the GitHub API (commit, `package.json`, and its position against skills `dev`).
*   **Expected Solution Shape:** exactly one `uses:` ref changes, to a full 40-character SHA on skills `dev` whose guards include the `npm overrides` job. It must not hardcode a tag (the repo has none, neo-agent-skills#80) or a branch ref, which would let the baseline drift unreviewed. Isolation: the PR's own baseline run is the only execution surface.
*   **Patch Verdict:** Matches. `+1/−1` on line 73, a full SHA. `48e3d72a55` is "Merge pull request #89 from neomjs/agent/88-npm-overrides", **identical** to skills `dev` (ahead 0 / behind 0), with `package.json` at `0.1.10`.
*   **Premise Coherence:** Coheres with verify-before-assert. The guard judges overrides from the lock, where Dependabot never looks, and the PR's own run is the receipt.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18858
*   **Related Graph Nodes:** neo-agent-skills#88 · neo-agent-skills#89 · neo-agent-skills#80 · neo-agent-skills#84 · #18466 · #18745 · #18809 · author session 6ed6621f-e2e4-4b9d-9a95-744679cb3c59
*   **Origin Session ID:** 0a997d38-ccdb-4352-ac21-566606a6ab2d

---

### 🔬 Depth Floor

**Challenge (non-blocking, already disclosed):** the pin also brings 0.1.9's tighter archaeology escape binding (neo-agent-skills#84). It judges only the files a PR changes, so nothing merged re-reds. But the first PR to touch a file carrying a loose escape will meet it cold. #18809 records the local mirror and the published guard disagreeing, and the ticket names this. The residual is that no one is named to tighten the local mirror (`buildScripts/util/check-ticket-archaeology.mjs`) to 0.1.9's binding, so the pre-commit hook and CI can disagree in the permissive direction until #18809 lands.

**Documented search:** I actively looked for three things and found no concern:
- a second caller of `reusable-pr-baseline.yml` in neo that would still sit on the old pin (none: `git grep` on `dev` finds only `pr-baseline.yml:73`)
- a new required input or permission at `48e3d72a55` that this caller does not pass (none: the whole baseline family runs green on this head)
- a non-SHA ref (it is the full SHA)

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: "0.1.7 guards → 0.1.10 guards" and "the #89 merge" both match the API reads above
- [x] Anchor & Echo summaries: N/A (workflow YAML, no JSDoc)
- [x] `[RETROSPECTIVE]` tag: none
- [x] Linked anchors: #18466 is the one-line pin-bump precedent it cites

**Findings:** Pass

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: N/A
*   **`[TOOLING_GAP]`**: A SHA-pinned reusable workflow in a tagless repository is invisible to Dependabot. Every guard release needs a hand-moved pin in each caller, and this is the third such bump (#18466, #18745, now this). neo-agent-skills#80 tracks tags.
*   **`[RETROSPECTIVE]`**: A guard that judges configuration Dependabot never touches only protects the repo once the pin that runs it moves, so the release and the adoption are two separate acts.

---

### N/A Audits — 📑 📡 🔗
N/A across listed dimensions: no consumed API, no OpenAPI surface, and no skill or convention change in this repository. The guard's contract was reviewed in neo-agent-skills#89.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #18858, a standalone `Resolves`
- [x] #18858 is not `epic`-labeled

**Findings:** Pass

---

### 🪜 Evidence Audit

- [x] `Evidence:` line present: L3, this PR's own `baseline` run on `pull_request`
- [x] The residual is a Post-Merge Validation item: the next Dependabot lock PR shows the job
- [x] No evidence-class collapse: the claim is exactly the run it cites

**Findings:** Pass

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI green at `617c39c23a` (`gh pr checks 18859`, every check pass, including the new `baseline / npm overrides`)
- [x] Reviewer falsifier: I read that job's log for AC-2. All three of neo's rules read `needed`: `gray-matter > js-yaml ^3.15.2`, `lodash-es ^4.18.0` (chevrotain ×3 + `dagre-d3-es` below the floor), and `monaco-editor > dompurify ^3.4.13` (`monaco-editor@0.56.0` declares 3.4.8).
- [x] Test location: N/A

**Findings:** Pass

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity. These are importance-to-verdict weights, not effort budgets.*

*   **`[ARCH_ALIGNMENT]`**: 100 - Checked that the pin is a full SHA on skills `dev`, the only caller, and the unchanged caller interface. All clear.
*   **`[CONTENT_COMPLETENESS]`**: 95 - The body and ticket state the side effect (#84's binding) and the Dependabot split. 5 deducted because the local-mirror follow-through for #84 has no named owner.
*   **`[EXECUTION_QUALITY]`**: 100 - A one-line change. Every baseline job is green at the head, and AC-2 is read from the log rather than inferred from the check colour.
*   **`[PRODUCTIVITY]`**: 100 - Both ACs are met at the head.
*   **`[IMPACT]`**: 45 - It turns on a guard for a failure class Dependabot cannot see: an override that silently stopped holding a floor.
*   **`[COMPLEXITY]`**: 5 - One ref.
*   **`[EFFORT_PROFILE]`**: Maintenance - A routine pin adoption.

The merge handoff is yours. It is same-family under the operator's GPT-dark exception of 2026-09-18.

🖖 Grace, Claude Opus 5, Claude Code.

---

