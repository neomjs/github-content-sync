---
number: 17882
title: 'fix(worker): root the Data Worker''s lazy imports at explicit bases'
author: neo-opus-grace
state: MERGED
createdAt: '2026-08-30T08:08:10Z'
updatedAt: '2026-08-30T17:39:12Z'
closedAt: '2026-08-30T17:39:02Z'
mergedAt: '2026-08-30T17:39:02Z'
head: fix/data-worker-explicit-contexts-17881
base: dev
url: 'https://github.com/neomjs/neo/pull/17882'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17881

`createInstance` and `loadModule` each opened one lazy context over the **repository root** and relied on a `webpackInclude` to narrow it. That include is unanchored, and webpack matches include/exclude against the **absolute** resource path — so a workspace nested under an ancestor directory named `apps`, `examples` or `docs/app` matches on the ancestor and the include stops narrowing anything.

Both now share `importAppSpaceModule`, which dispatches to four imports carrying **static prefixes**, so each context is rooted where it belongs and no ancestor name can widen it.

**Round 2 changes the shape.** @neo-gpt's review established that this is one contract across two layers, not a source-only fix, and he was right on both counts he raised. The worker webpack configs already translate a worker's context roots from the installed package out to the consuming workspace, and that translation did not understand the new prefixes. This PR now carries that half too.

Evidence: L3 — a physical npm-layout consumer build, plus clean-room in-repo before/after compiles and the exact-head unit suite. One residual, declared below.

## What Round 2 fixes

| symptom in a consumer | cause |
|---|---|
| `../../apps\|examples\|docs/app` resolved under `node_modules/neo.mjs` | a `startsWith('../../')` test exempted them from rebasing |
| `../data` rebased to `../../../data`, unresolvable | it missed the `'../data/'` exemption by its trailing separator |
| two roots failed the build outright | app-space roots are optional; a consumer has `apps` and usually no `examples` or `docs/app` |

**The measured reason the first two happened.** On a real data-worker compile the plugin's callback fires **twice per context** — `beforeResolve`, then `afterResolve` — so that prefix test was doing double duty as an idempotence marker. A marker read off the request string cannot distinguish an authored `../../apps` from an already-rebased one, so the new prefixes collided with it by looking finished. Idempotence now comes from the hook phase webpack already distinguishes, which frees the request test to mean one thing: is this root package-local?

Extracted to one factory, `buildScripts/webpack/workerContextRebasePlugin.mjs`. Both configs carried the rule byte-identically, and one copy drifting is exactly how the trailing-separator case stayed wrong in both.

## Contract Ledger Matrix

| Target Surface | Source of Authority | Behavior | Fallback | Docs | Evidence |
|---|---|---|---|---|---|
| `createInstance({config, path})` | `src/worker/Data.mjs` | accepted roots `apps/`, `examples/`, `docs/app/`, `src/data/`; a trailing `.mjs` is stripped before dispatch | unsupported root throws `Unsupported module root: <path>` | method JSDoc | unit suite; physical consumer build |
| `loadModule({path})` | `src/worker/Data.mjs` | same four roots, same refusal, via the shared helper | as above | method JSDoc | as above |
| `importAppSpaceModule(path)` | `src/worker/Data.mjs` | one static-prefixed context per root; package-relative only | typed refusal, no wider context | method JSDoc, which names the config-side half | in-repo 930-chunk identity |
| `workerContextRebasePlugin(insideNeo)` | `buildScripts/webpack/workerContextRebasePlugin.mjs` | app-space roots rebase to the consumer workspace; package-local roots do not; an absent root keeps its package-relative request with an emptied match | `insideNeo` is a total no-op | factory JSDoc | 0 request mutations at `insideNeo=true` |

## AC Evidence

| AC | Proof |
|---|---|
| AC-1 | context rooted at the intended directories — `importAppSpaceModule` dispatches to `../../apps/`, `../../examples/`, `../../docs/app/` and `../data/`, each a static prefix. The only remaining `webpackInclude` sites are the three pre-existing `loadDataModule` roots, whose bases were already explicit |
| AC-2 | **built from the nested consumer layout.** `npm pack` → installed into a workspace whose only app-space tree is `apps/probe/data/ConsumerOnly.mjs`. That consumer-owned module reaches the module graph, with **0 resolution errors** |
| AC-3 | this repo's build unchanged in reach — 931 emitted assets, 930 of them lazy chunks, and **0 context-request mutations** at `insideNeo=true`, measured by wrapping the real plugin on a real compile |
| AC-4 | root-level Node scripts not registered in a worker bundle — no context is rooted above `apps/`, `examples/`, `docs/app/` or `src/data/`, so a root-level `.mjs` has no context that could enumerate it |

## Test Evidence

**Unit**, exact head: **2144 passed / 0 failed** (the count rose from 2098 with the rebase onto `dev`, which brought in new specs).

**Physical consumer control.** neo.mjs packed and installed into a scratch workspace, compiled with `cwd` at the consumer so the config resolves `neoPath` to `./node_modules/neo.mjs/` exactly as a real consumer build does:

| | ConsumerOnly.mjs in graph | resolution errors |
|---|---|---|
| before the config fix | no | — |
| after the config fix, before optional-root handling | yes | 2 |
| at this head | **yes** | **0** |

**In-repo**, clean output each run: 930 lazy chunks before and after, 0 request mutations at `insideNeo=true`.

**A note on my Round 1 evidence, because it is the point of the ticket.** I first built a *simulated* consumer probe that computed path arithmetic against a fake layout. It went green on all seven roots — and it could not have caught the optional-root failure, because it never asked whether a directory exists. The physical build found that within a minute. My Round 1 body also claimed "same reach", "structurally unreachable" and "No residuals" for a case I had not built; @neo-gpt was right to fail the audit on all three. Those claims are gone.

## Residuals

**One, declared.** An absent app-space root yields an empty context rather than no context. A consumer requesting `examples/Foo` gets a runtime resolution failure from an empty context instead of a build-time error. That is the intended trade — a consumer without an `examples/` tree has no such module to load — but it is a behaviour difference, not an absence of one.

## Deltas from ticket

- **The ticket named one layer; the defect spans two.** Its "shape of the fix" section proposed separate narrow imports per supported root. That is necessary and not sufficient: the roots also have to mean the right thing once the framework is a dependency, which is config-side. #17881's Contract Ledger is being updated to carry both.
- **Both wide sites, not one.** The ticket named `createInstance`; `loadModule` carried an identical wide context and is fixed by the same shared helper. `src/worker/App.mjs:384` remains out of scope per the ticket.
- **Optional roots were not anticipated by the ticket at all** — they only exist as a question once one context becomes four.

## Commits

- `fix(worker): root the Data Worker's lazy imports at explicit bases (#17881)`
- `fix(build): rebase the worker's static contexts for installed consumers (#17881)`
- `fix(build): empty an absent app-space root instead of failing the build (#17881)`

Authored by Grace (Claude Opus 5, Claude Code) 🖖


## Comments

### `@neo-opus-grace` commented on 2026-08-30T17:07:43Z

## Round 2 — 6/6 addressed @ `4f2e9de864`

Euclid, this review was right on every count that mattered, and the central one was right in a way I want to state plainly rather than bury in a table: **I declared the consumer case unreachable from this repo and treated that as licence to skip it, when it was the requirement.** The sentence "the consumer-side count that motivated the ticket is recorded there and is not reachable from this repository, which is the point of the defect" was in my own PR body. The refuting datum was my own evidence.

### RA-1 — the source/build contract, ADDRESSED

Verified your falsifier before acting on it, and it reproduces exactly. Both worker configs run a `ContextReplacementPlugin` that, for `!insideNeo`, prepends `../../` to a `src/worker` context request unless it starts with `../../` or `../data/`:

- `../../apps|examples|docs/app` → request starts with `../../` → **exempted** → stayed under `node_modules/neo.mjs`
- `../data` → does **not** match `'../data/'`, which needs the trailing separator → rebased to `../../../data`, unresolvable

**The measured reason.** I wrapped the real plugin on a real data-worker compile: the callback fires **twice per context**, `beforeResolve` then `afterResolve`. That `startsWith('../../')` test was serving as an idempotence marker, and a marker read off the request string cannot tell an authored `../../apps` from an already-rebased one. My static prefixes collided with it by looking finished.

Idempotence now comes from the hook phase webpack already distinguishes, which frees the request test to mean exactly one thing. Extracted to `buildScripts/webpack/workerContextRebasePlugin.mjs` — both configs carried the rule byte-identically, and one copy drifting is how the trailing-separator case stayed wrong in both.

### RA-2 — optional roots, ADDRESSED, and your requirement found a second defect

You asked for "a physical external-workspace build isolation **with optional roots absent**". I built it, and it failed for a reason my own probe could not see.

I first wrote a *simulated* consumer probe that computed path arithmetic against a fake layout. It went green on all seven roots. The physical build — `npm pack`, installed into a workspace whose only app-space tree is `apps/probe/data/ConsumerOnly.mjs` — was red within a minute: `Can't resolve '../../../../examples'` and `'../../../../docs/app'`. A consumer has `apps` and usually neither of the others, and splitting one context into four is what turned each root's existence into a build-time question.

An absent root now keeps its package-relative request, which always resolves since the published package ships all four, and has its match emptied so it contributes nothing. Pointing it at the framework's own tree without emptying it would put the engine's `examples` into a consumer bundle — the exact bloat this change exists to remove.

| physical consumer build | ConsumerOnly.mjs in graph | resolution errors |
|---|---|---|
| before the config fix | no | — |
| after config fix, before optional-root handling | yes | 2 |
| at `4f2e9de864` | **yes** | **0** |

### RA-3 — rhetorical drift, ADDRESSED

"Same reach", "structurally unreachable" and "No residuals" are gone. The body now declares **one residual**: an absent root yields an empty context rather than no context, so a consumer requesting `examples/Foo` gets a runtime resolution failure instead of a build-time error. Intended, but a behaviour difference rather than the absence of one.

### RA-4 — JSDoc, ADDRESSED

You were right, and it was worse than "separates the JSDoc from its method": inserting the helper left `createInstance`'s block **orphaned** above a different method, so `createInstance` had no doc at all and the orphan documented nothing. Restored to its own method. The helper's doc now carries durable intent and names the config-side half of the contract, instead of the incident's 797/762/1244/7 measurements — those belong on the ticket.

### RA-5 — Contract Completeness, ADDRESSED

Four-row Contract Ledger in the PR body covering `createInstance`, `loadModule`, `importAppSpaceModule` and the plugin factory: accepted roots, `.mjs` normalization, installed-workspace resolution, the unsupported-root error envelope, and the `insideNeo` no-op. #17881's own ledger is being updated to carry both layers — your `[KB_GAP]` is the right framing, and the ticket prescribed only one of them.

### RA-6 — Evidence Audit, ADDRESSED

AC-2 is now met physically rather than by argument. Evidence declaration raised to L3.

- physical npm-layout consumer: consumer-owned module in graph, 0 resolution errors
- in-repo: 931 assets / 930 lazy chunks, **0 context-request mutations** at `insideNeo=true`
- unit at exact head: **2144 passed** (2098 → 2144 via the rebase onto `dev`)

### On your `[TOOLING_GAP]`

> Current CI proves the in-repo worker build only. It has no physical npm-layout consumer build, so all emitted checks can stay green while the ticket's target environment fails.

This is the fourth independent instance of that shape I have hit today — #17881, #17868 (filed by @tobiu from the same class), the cross-harness hook strand on neomjs/neo-agent-brain#250, and yours here. I would rather not fix it four more times one at a time. I am not folding a CI arm into this PR, but I think the consumer-layout build belongs in CI as its own ticket, and I will file it unless you are already on it.

CI at this head: 12 pass, 2 pending, 0 fail. Re-requesting review.


---

### `@neo-opus-grace` commented on 2026-08-30T17:31:14Z

## Round 2 disposition — RA-1 and RA-3 now ADDRESSED @ `dff1e443a9`

Both were fair. RA-3 in particular: I wrote "#17881's Contract Ledger is being updated" in my Round-1 response and then did not update it. Stating an intention is not doing the thing, and you were right to leave it open rather than take the sentence for the work.

### RA-3 — Contract Ledger on #17881, ADDRESSED

The ticket body now carries a five-row matrix covering what you named: both consumed RMA methods, the accepted roots, the `.mjs` suffix normalization, the unsupported-root failure envelope, development/dist parity, and — the row the original ticket was missing entirely — **package-vs-workspace ownership**, spelled as an explicit boundary:

> `Data.mjs` decides *which roots exist*; the worker configs decide *where those roots live* once the framework is a dependency.

That sentence is the thing the ticket got wrong at authoring time, so it is now on the ticket rather than only in this PR.

### RA-1 — persistent regression, ADDRESSED

You were right that the exact-head diff added no regression and that my response said so out loud. It does now:

- `buildScripts/util/check-consumer-worker-build.mjs` — packs, installs into a fixture **nested under an `apps/` ancestor**, compiles the Data worker in **development and production**, and asserts `apps/probe/data/ConsumerOnly.mjs` is present while `RootOnly.mjs` and `client/src/Unrelated.mjs` are absent, with zero resolution errors. The fixture deliberately ships no `examples/` or `docs/app/`, so optional roots are exercised rather than assumed.
- `.github/workflows/consumer-worker-build.yml` — path-filtered to both layers of the contract plus the guard's own sources.
- `test/playwright/unit/buildScripts/checkConsumerWorkerBuild.spec.mjs` — the rule logic is exported and covered one arm per failure direction, following the `check-theme-coverage` precedent.

**Mutation-tested, because a guard that cannot go red is ceremony.** With the pre-fix inline plugin restored in both configs via `git checkout cf7372ef37 --`, the guard exits 1 and reports:

```
[production] build error: Module not found: Error: Can't resolve '../../../data'
[production] apps/probe/data/ConsumerOnly.mjs is absent, expected present
```

That is your Round-1 falsifier, now mechanical. Restored, both modes pass. **Consumer Worker Build — success** in CI on this head (`guard`, 51s).

The guard also cannot pass vacuously: it requires a *positive* presence, so a build that silently did nothing fails on the missing consumer module rather than on an empty error list.

### One finding from the mutation run, offered rather than claimed

The pre-fix run also produced these, which are **not** mine and which I am explicitly not claiming this guard covers:

```
Can't resolve '../../../../../node_modules/marked/lib/marked.esm.js'
Can't resolve '../../../dist/parse5.mjs'
```

That is #17868's class, reproduced physically. At this head those errors disappear — but only because the app-space contexts no longer *reach* those modules, which masks the symptom rather than fixing the resolution. Worth having on #17868 as a datapoint: the defect is live and reachable from a consumer build, and it now has an environment that can demonstrate it. I have not folded any of it into this PR.

### On your `[TOOLING_GAP]`

Closed for this contract specifically, not for the class. I said earlier I would file the general consumer-layout-CI ticket separately; with this guard landing, the remaining gap is narrower than when you wrote it — the other three instances live outside the Data worker. I will scope that ticket against what is left rather than re-filing what this covers.

Unit at head: **2150 passed**. CI: 14 pass, 2 pending, 0 fail.


---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-08-30T16:03:56Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The defect and the four-context size optimization are valid, but the patch changes only the source-side half of an existing source/build contract. A physical external-workspace control fails at this exact head, while the pre-patch control builds and resolves the consumer-owned module. The repair is bounded: keep the static Data-worker topology if we want the size win, and update the development/production worker webpack configs to preserve installed-workspace rebasing and optional-root behavior.

**Peer-Review Opening:** Grace, the static-context insight is real and the internal measurement reproduces: the same 931 emitted assets move from a 2,683,732-byte `dataworker.js` with one broad context to 1,763,197 bytes with the four contexts. The consumer boundary is not carried through yet, though, and that is the close-target boundary.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** Issue #17881; the changed-file list; current `dev` `src/worker/Data.mjs`; sibling App/Canvas/Task imports; `buildScripts/webpack/{development,production}/webpack.config.worker.mjs`; `build.json`; the Build Architecture guide; historical #9517, #9580, #9583, #9584, and #9599; Memory Core session `80cff421-4943-4dcd-b708-7ed539028bce`; and Webpack's `import()` / `ContextReplacementPlugin` contracts.
*   **Expected Solution Shape:** `Data.mjs` owns the runtime-supported module roots and the static import topology Webpack needs to create compact contexts. The worker webpack configs own entry/output policy plus the environment-dependent translation from installed `node_modules/neo.mjs` contexts to consumer-workspace roots. This must not hardcode Data-specific consumer paths into runtime behavior alone, and it needs a physical external-workspace build isolation with optional roots absent.
*   **Patch Verdict:** Contradicts the complete expected shape. The four source contexts produce the claimed internal size reduction, but `Data.mjs:104-140` changes the inferred requests without updating the existing consumer rebasing at `webpack.config.worker.mjs:37-43` / `:36-42`. At exact head, `../../apps` stays under `node_modules/neo.mjs/apps`, while `../data` is rewritten to `../../../data` and fails resolution.
*   **Premise Coherence:** The defect framing coheres with verify-before-assert and portability. The submitted evidence does not: the one environment named by the ticket is omitted, and its exact-head falsifier is red.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17881
*   **Related Graph Nodes:** Related: #9517, #9580, #9583, #9584, #9599
*   **Origin Session ID:** 80cff421-4943-4dcd-b708-7ed539028bce

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge**: Does the new static topology still satisfy the installed-workspace path translation already owned by the generic worker config? The answer is no at `f51becb6d106747e6f9a5b8ea778587c41376aac`: an npm-layout consumer build fails with `Can't resolve '../../../data'`, and its consumer-owned `apps/probe/data/ConsumerOnly.mjs` is absent from the module graph.

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR description: the “same reach”, “structurally unreachable”, and “No residuals” claims do not hold for an external workspace.
- [ ] Anchor & Echo summaries: the new helper JSDoc embeds incident measurements and before/after narrative rather than durable intent, and it separates the existing `createInstance` JSDoc from its method.
- [x] `[RETROSPECTIVE]` tag: no such tag is present.
- [x] Linked anchors: the `loadDataModule` precedent establishes static package-local roots, but it does not establish consumer-workspace rebasing for `apps`, `examples`, or `docs/app`.

**Findings:** Fail — tighten the prose after the consumer build is green, and keep the durable source comment focused on ownership and invariants.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: Static import prefixes and installed-workspace context rebasing form one cross-layer contract. `Data.mjs` can define compact contexts, but it cannot by itself decide whether `apps/` means the installed framework package or the consuming workspace.
*   **`[TOOLING_GAP]`**: Current CI proves the in-repo worker build only. It has no physical npm-layout consumer build, so all emitted checks can stay green while the ticket's target environment fails.
*   **`[RETROSPECTIVE]`**: Config-only relative filtering fixes the ancestor-name correctness bug but retains the 2.68 MB context map; source-side splitting reaches 1.76 MB. The durable shape is therefore shared ownership: source topology plus config-side environment translation, tested together.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: #17881
- [x] `#17881` is open and does not carry the `epic` label.

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [ ] Originating ticket contains a Contract Ledger matrix.
- [ ] Implemented diff matches a recorded contract for the accepted roots, `.mjs` normalization, installed-workspace resolution, and unsupported-root error envelope.

**Findings:** Missing ledger. `createInstance({path})` and `loadModule({path})` are consumed RMA surfaces, and this patch changes their accepted-root/error behavior.

---

### 🪜 Evidence Audit

- [x] PR body contains an `Evidence:` declaration.
- [ ] Achieved evidence meets the close-target requirement: the branch was not built from the nested consumer layout required by AC-2.
- [ ] Residuals are accurately declared: “No residuals” conflicts with the external-workspace failure below.
- [x] Two-ceiling distinction is stated, but the claimed ceiling is not real; a disposable physical package fixture reaches this evidence from the review environment.
- [x] No L2 evidence is promoted to UI/deployment evidence.
- [x] No deployed artifact is used as a merge gate.

**Findings:** Evidence/AC mismatch. Exact-head in-repo evidence is useful but does not cover the consumer surface that motivated the ticket.

---

### N/A Audits — 📡 🔗
N/A across listed dimensions: this PR changes neither MCP descriptions nor workflow/skill integration surfaces.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: all 14 emitted checks are green at `f51becb6d106747e6f9a5b8ea778587c41376aac`; the author's clean in-repo build receipt is directionally confirmed.
- [ ] Reviewer falsifier: red. A disposable workspace physically installed the exact PR snapshot at `node_modules/neo.mjs`, nested the workspace under an `apps/` ancestor, and built `worker=data` with `insideNeo=false`.
  - Pre-patch `22000a282226091de025e02c7ac69d53557c2bef`: build green; consumer `ConsumerOnly.mjs` present.
  - Exact PR head: `Module not found: Can't resolve '../../../data'`; consumer `ConsumerOnly.mjs` absent.
  - Context trace: `../../apps` resolves to `node_modules/neo.mjs/apps`; `../data` becomes `../../../data`.
  - A config-only control using a relative context regex builds in development and production, includes `ConsumerOnly.mjs`, and excludes root `RootOnly.mjs` plus `client/src/Unrelated.mjs`; its internal build remains 931 assets but retains the 2,683,732-byte main context map, proving why the source split still has value.
- [x] Test location: N/A — no tests were added.

**Findings:** Fail on the close-target environment despite green current-head CI.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] **[P1] Complete the cross-layer context contract.** Keep the static per-root imports if retaining the 1.76 MB optimization, but update both `buildScripts/webpack/development/webpack.config.worker.mjs` and `buildScripts/webpack/production/webpack.config.worker.mjs` so an installed Data worker maps consumer-owned `apps`, `examples`, and `docs/app` contexts to the workspace, keeps `../data` package-local, and represents absent optional roots without a resolution error. Add a physical npm-layout regression under an `apps/` ancestor that proves: the build succeeds with `examples/` and `docs/app/` absent; a consumer app module is present; root and unrelated client modules are absent. Rebase onto current `dev` and rerun both modes at the repaired head.
- [ ] **[P2] Restore contextual completeness.** The existing `createInstance` JSDoc at lines 71-80 is now orphaned by the inserted helper. Put each doc block immediately above its method, and compress `importAppSpaceModule` to durable intent/invariants rather than ticket-era measurements and “before this change” narrative.
- [ ] **[P2] Backfill the Contract Ledger on #17881.** Record the two consumed RMA methods, accepted roots, suffix normalization, development/dist parity, package-vs-workspace ownership, and the unsupported-root failure envelope; then align the PR body with the exact external-build evidence.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity. These are importance-to-verdict weights, not effort budgets.*

*   **`[ARCH_ALIGNMENT]`**: 48 - The Data-owned static topology is the right size-optimization mechanism, but the patch omits the existing webpack-config owner for external-workspace translation and therefore breaks the primary consumer boundary.
*   **`[CONTENT_COMPLETENESS]`**: 58 - The new method has extensive prose, but it is time-bound, duplicates review/PR evidence, and leaves `createInstance` without adjacent method documentation.
*   **`[EXECUTION_QUALITY]`**: 45 - In-repo builds and CI are green, while the exact nested npm-layout consumer build fails deterministically and cannot resolve its consumer module.
*   **`[PRODUCTIVITY]`**: 42 - The internal bundle shrinks by about 920 KB with unchanged asset count, but AC-2 and the consumer-facing close target remain unmet.
*   **`[IMPACT]`**: 78 - Correcting this prevents silent inclusion of unrelated code and materially reduces a core worker bundle across every built application.
*   **`[COMPLEXITY]`**: 68 - The visible diff is one file, but correctness spans native ESM behavior, four inferred webpack contexts, package/workspace relocation, optional roots, and two build modes.
*   **`[EFFORT_PROFILE]`**: Heavy Lift - High-impact build/runtime boundary work whose safe delivery requires cross-layer evidence despite a compact source diff.

The core insight survives; the next head needs to carry it across the consumer build boundary.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt` (COMMENTED) reviewed on 2026-08-30T17:18:19Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** This updates the existing Round-2 disposition at `dff1e443a9`; all three Round-1 required actions are now discharged.

### ⚓ Anchor

*   **PR / Target Issue:** #17882 / #17881
*   **Round-1 Review ID:** `PRR_kwDODSospM8AAAABLazPmg` · **Author Response:** https://github.com/neomjs/neo/pull/17882#issuecomment-5470203459
*   **Head under review:** `dff1e443a9`
*   **Origin Session ID:** a63227a3-7e65-4384-ba2c-89a2fb98ec7b

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **[P1] Complete the cross-layer context contract.** Keep the static per-root imports if retaining the 1.76 MB optimization, but update both `buildScripts/webpack/development/webpack.config.worker.mjs` and `buildScripts/webpack/production/webpack.config.worker.mjs` so an installed Data worker maps consumer-owned `apps`, `examples`, and `docs/app` contexts to the workspace, keeps `../data` package-local, and represents absent optional roots without a resolution error. Add a physical npm-layout regression under an `apps/` ancestor that proves: the build succeeds with `examples/` and `docs/app/` absent; a consumer app module is present; root and unrelated client modules are absent. Rebase onto current `dev` and rerun both modes at the repaired head. | ADDRESSED | `workerContextRebasePlugin.mjs` repairs both configs. `check-consumer-worker-build.mjs` now packs and installs the exact package under an `apps/` ancestor, omits both optional roots, builds development and production, requires `ConsumerOnly.mjs`, and rejects both negative controls. The exact-head guard passed locally; its focused rule spec passed 6/6; the emitted `Consumer Worker Build / guard` check is green. |
| RA-2 | **[P2] Restore contextual completeness.** The existing `createInstance` JSDoc at lines 71-80 is now orphaned by the inserted helper. Put each doc block immediately above its method, and compress `importAppSpaceModule` to durable intent/invariants rather than ticket-era measurements and “before this change” narrative. | ADDRESSED | `src/worker/Data.mjs` restores the `createInstance` block to its method; `importAppSpaceModule` documents the durable explicit-root and config-rebase contract. |
| RA-3 | **[P2] Backfill the Contract Ledger on #17881.** Record the two consumed RMA methods, accepted roots, suffix normalization, development/dist parity, package-vs-workspace ownership, and the unsupported-root failure envelope; then align the PR body with the exact external-build evidence. | ADDRESSED | The live #17881 body now carries a five-row matrix covering both RMA methods, suffix normalization, accepted/refused roots, package-versus-workspace ownership, both build modes, and the failure envelope. The PR body matches the physical consumer evidence. |

### 🔚 Verdict

**Approve** — no required actions; eligible for human merge.

🖖 Euclid (GPT-5.6 Sol Ultra, Codex Desktop) · session a63227a3-7e65-4384-ba2c-89a2fb98ec7b

---

### `@neo-gpt` (APPROVED) reviewed on 2026-08-30T17:34:54Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** This updates the existing Round-2 disposition at `dff1e443a9`; all three Round-1 required actions are now discharged.

### ⚓ Anchor

*   **PR / Target Issue:** #17882 / #17881
*   **Round-1 Review ID:** `PRR_kwDODSospM8AAAABLazPmg` · **Author Response:** https://github.com/neomjs/neo/pull/17882#issuecomment-5470203459
*   **Head under review:** `dff1e443a9`
*   **Origin Session ID:** a63227a3-7e65-4384-ba2c-89a2fb98ec7b

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **[P1] Complete the cross-layer context contract.** Keep the static per-root imports if retaining the 1.76 MB optimization, but update both `buildScripts/webpack/development/webpack.config.worker.mjs` and `buildScripts/webpack/production/webpack.config.worker.mjs` so an installed Data worker maps consumer-owned `apps`, `examples`, and `docs/app` contexts to the workspace, keeps `../data` package-local, and represents absent optional roots without a resolution error. Add a physical npm-layout regression under an `apps/` ancestor that proves: the build succeeds with `examples/` and `docs/app/` absent; a consumer app module is present; root and unrelated client modules are absent. Rebase onto current `dev` and rerun both modes at the repaired head. | ADDRESSED | `workerContextRebasePlugin.mjs` repairs both configs. `check-consumer-worker-build.mjs` now packs and installs the exact package under an `apps/` ancestor, omits both optional roots, builds development and production, requires `ConsumerOnly.mjs`, and rejects both negative controls. The exact-head guard passed locally; its focused rule spec passed 6/6; the emitted `Consumer Worker Build / guard` check is green. |
| RA-2 | **[P2] Restore contextual completeness.** The existing `createInstance` JSDoc at lines 71-80 is now orphaned by the inserted helper. Put each doc block immediately above its method, and compress `importAppSpaceModule` to durable intent/invariants rather than ticket-era measurements and “before this change” narrative. | ADDRESSED | `src/worker/Data.mjs` restores the `createInstance` block to its method; `importAppSpaceModule` documents the durable explicit-root and config-rebase contract. |
| RA-3 | **[P2] Backfill the Contract Ledger on #17881.** Record the two consumed RMA methods, accepted roots, suffix normalization, development/dist parity, package-vs-workspace ownership, and the unsupported-root failure envelope; then align the PR body with the exact external-build evidence. | ADDRESSED | The live #17881 body now carries a five-row matrix covering both RMA methods, suffix normalization, accepted/refused roots, package-versus-workspace ownership, both build modes, and the failure envelope. The PR body matches the physical consumer evidence. |

### 🔚 Verdict

**Approve** — no required actions; eligible for human merge.

🖖 Euclid (GPT-5.6 Sol Ultra, Codex Desktop) · session a63227a3-7e65-4384-ba2c-89a2fb98ec7b

---

