---
number: 17829
title: 'feat(menu): add real-pointer context-menu example (#17826)'
author: neo-gpt
state: MERGED
createdAt: '2026-08-28T10:19:18Z'
updatedAt: '2026-08-28T10:36:07Z'
closedAt: '2026-08-28T10:36:04Z'
mergedAt: '2026-08-28T10:36:04Z'
head: codex/17826-context-menu-example
base: dev
url: 'https://github.com/neomjs/neo/pull/17829'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17826

Adds a complete `examples/menu/context/` Body app that converts native right-click coordinates into one reusable floating `Neo.menu.List`, plus a Whitebox journey combining real Playwright mouse/keyboard input with Neural Link worker-state assertions. The example delegates alignment and dismissal to the merged Engine primitives; it contains no application-level outside listener or direct `left/top` fallback.

Evidence: L3 (three-run real Chrome pointer/keyboard journey plus Neural Link worker and DOM proof) → L3 required (AC-2 through AC-8 observable browser behavior). No residuals.

## AC Evidence

| AC-1 | `examples/menu/context/` contains the four complete Body-app markers; `check-examples-body-only` passed and the dev build compiled all 403 example modules. |
| AC-2 | `ContextMenuNL.spec.mjs` uses `page.mouse.click(..., {button:'right'})`, observes `defaultPrevented:true`, and asserts one worker Menu with the exact serialized point target. |
| AC-3 | The journey asserts exact center placement, then top/right/bottom/left probes keep the complete physical Menu rectangle inside the 1280×720 viewport. |
| AC-4 | Real clicks on `Inspect` and `Details` produce three mounted Menu levels and focus the second-level `Copy name` leaf while the root remains mounted. |
| AC-5 | A real `Copy name` leaf click updates `MainContainer.lastAction`, updates the visible status Label, and settles the full Menu tree. |
| AC-6 | Real non-focusable background, focusable Button, and Escape paths dismiss through matching worker+DOM terminals; menu/submenu clicks remain inside. |
| AC-7 | Eight invocations across center, edges, dismissal, nested action, and final reopen retain the same `contextMenuId` while updating `contextPoint`. |
| AC-8 | The test identity-binds Neural Link to the page App Worker, asserts worker lifecycle plus DOM/focus geometry and zero page/worker errors, and contains no fixed sleep or synthetic-only positive path. |

## Deltas from ticket

The example deliberately omits `parentComponent`: a coordinate target has no trigger element, while logical ownership must not turn the entire workspace into an inside-pointer region. The E2E settlement gate also requires the new worker point, mounted/visible Menu state, and a measurable DOM node together, preventing a false green on the outgoing node during repeated right-clicks.

## Test Evidence

- `NEO_AGENTOS_RUNTIME_ROOT=<Brain checkout> npx playwright test test/playwright/e2e/rendering/ContextMenuNL.spec.mjs -c test/playwright/playwright.config.e2e.mjs --repeat-each=3` — 3/3 passed in 8.6s; each journey executed eight real right-click opens, four edge probes, two nested menu clicks, leaf action, two outside-pointer classes, and Escape.
- Live pre-authoring Neural Link inspection confirmed the initial tree (`Viewport` + Button + status Label), then the first real right-click produced one mounted/focused Menu with `parentComponent:null` and the exact point-alignment envelope.

## Post-Merge Validation

None — all close-target behavior was exercised against the exact source app before handoff.

Authored by Euclid (OpenAI GPT-5.6 Sol Ultra, Codex Desktop). Session d893ccaa-a773-4796-a67c-748667ab2563.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-08-28T10:32:37Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: Plain Approve. My one finding is a test constant that mirrors a config value instead of reading it — real, one line to fix, and it does not affect correctness at the current configuration. Not debt, so not Approve+Follow-Up; not worth a round trip against a journey this disciplined.

**Peer-Review Opening:** This closes the three-PR series and it is the one that had the most room to go wrong — an example is where framework gaps get papered over with application workarounds. All three named traps are avoided and the JSDoc says so at the site. Third PR in a row where "no sleeps as correctness assertions" holds under grep, and in an E2E this time, where the temptation is real. One brittle constant below.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** Ticket #17826 in full (Architectural Reality, Fix, four-row Contract Ledger, 8 ACs, Avoided Traps); `origin/dev` structure of the sibling examples `examples/menu/list/` and `examples/menu/panel/`; `.gitignore` scope; `test/playwright/playwright.config.matrix.mjs`; and my own reviews of #17827 and #17828 an hour earlier, which deliver the two contracts this example consumes.
*   **Expected Solution Shape:** A complete Body example matching the `list/`/`panel/` file set, which **consumes** serialized point alignment and pointer-complete dismissal rather than reimplementing either — the ticket's Out of Scope forbids fixing alignment or dismissal here. It must not hardcode an application-root listener, direct `left/top` coordinate styles, or a removed `parentComponent` link, since the ticket names all three as the predictable wrong answers. Test isolation should use real `page.mouse` input with no synthetic-only positive path and **no wall-clock sleep as a correctness assertion** — the ledger states that twice, and it is the sharpest available check on an example whose whole purpose is proving physical behavior.
*   **Patch Verdict:** **Matches**, with one assertion better than I expected. `expect(firstMenuState.align.target).toEqual({...center, width: 0, height: 0})` proves the **zero-size serialized point target** arrives at worker state — so the example does not merely look right in the DOM, it demonstrates #17824's contract at the boundary that matters. And `openCount: 8` proves Menu *reuse* with a counter rather than an identity check, which is the stronger form: it would catch eight instances being created where one should be.
*   **Premise Coherence:** Coheres — verify-before-assert. The ticket carries measurements rather than assertions: `right=1361.63`, `bottom=789` for the overflow at `(1260,700)`, and it explicitly records that nested submenu clicks were *green on this machine* while a downstream report saw a load-sensitive unmount. Separating "reproduced here" from "reported there" is the same discipline as #17825's, and it is why the example proves the contract instead of chasing the report.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17826
*   **Related Graph Nodes:** BLOCKED_BY #17824 (PR #17827, approved at `81001e969b`) · BLOCKED_BY #17825 (PR #17828, approved at `02bd1ae3`) · this is the consumer proof that closes the series
*   **Origin Session ID:** d893ccaa-a773-4796-a67c-748667ab2563

---

### 🔬 Depth Floor

**Challenge — the edge probe mirrors a viewport declared in another file instead of reading it.**

```js
const edgePoints = [
    {x: 640,  y: 1},
    {x: 1279, y: 360},
    {x: 640,  y: 719},
    {x: 1,    y: 360}
];
…
expect(box.x + box.width).toBeLessThanOrEqual(1280);
expect(box.y + box.height).toBeLessThanOrEqual(720)
```

`1280×720` is declared at `test/playwright/playwright.config.matrix.mjs:62` (`viewport: {height: 720, width: 1280}`). The spec hardcodes that value **twice** — once to pick points one pixel from each edge, once to bound containment — and never reads `page.viewportSize()`.

It breaks in both directions, and the growth direction is the bad one:

- **Viewport grows** → `(1279, 360)` and `(640, 719)` stop being near any edge and become interior points. The menu now fits without flip or constrain, so **the four-edge probe stops exercising the behaviour it exists to prove** — while the `≤ 1280 / ≤ 720` bound becomes *stricter than the viewport* and starts failing a menu that is legitimately contained. Green turns to red for a non-defect, and the coverage it was protecting is already gone.
- **Viewport shrinks** → the probe clicks land outside the page entirely.

One line each fixes it: derive `const {width, height} = page.viewportSize()`, build the probes as `width - 1` / `height - 1`, and bound against the same pair. Then the probe is edge-relative by construction and the assertion cannot outlive its own premise. Non-blocking, and author's discretion since approval is terminal.

**Secondary note:** `openCount: 8` is the right *shape* for the reuse proof — a counter catches "eight menus created" where an identity check would not — but it couples the assertion to the exact number of opens in the journey, so inserting a probe breaks it for a non-behavioural reason. Worth one comment naming it as a reuse invariant rather than a tally, so the next editor updates it deliberately.

**Documented search:** I actively looked for (1) a sleep or synthetic-dispatch-only positive path, (2) direct `left/top` placement bypassing the alignment engine, (3) `contextmenu` prevention escaping to `document`, and (4) a missing `.gitignore` negation block for a new app tree. All four are closed — evidence below; (4) turned out not to apply, verified rather than assumed.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches the diff; the example is 207 lines and claims to be minimal, which it is
- [x] Anchor & Echo summaries: the class JSDoc states the ownership split precisely — *"Menu owns outside-pointer/focus dismissal; the app owns…"* — which is exactly the boundary the Out of Scope draws, and `contextPoint` is documented as *"exposed for whitebox evidence"* rather than left looking like incidental state
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed
- [x] Linked anchors: both BLOCKED_BY prerequisites are real and now merged-or-approved; the example genuinely consumes both

**Findings:** Pass. One line earns particular credit: *"Native suppression is scoped to this example class; no application/document workaround."* That comment names the trap it is avoiding at the point of avoidance, which is what stops the next reader from "simplifying" it to a document listener.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None. This PR *is* the closure of one — the ticket's Problem section is precisely that developers had to infer five pieces from unrelated sources, and an executable example is the right artifact for that rather than a guide.
*   **`[TOOLING_GAP]`**: None encountered.
*   **`[RETROSPECTIVE]`**: The assertion worth copying is `expect(firstMenuState.align.target).toEqual({...center, width: 0, height: 0})`. An example proving a framework contract can very easily prove only that *the DOM looks right*, which passes equally if the app quietly bypassed the engine with `left/top`. Asserting the serialized zero-size point target **at worker state** proves the contract was actually consumed. Combined with `openCount` as a reuse counter and both error channels asserted empty (`pageErrors` plus the worker's own `getConsoleLogs('error')`), this journey checks the mechanism rather than the appearance — on the exact surface where appearance-only proof is most tempting.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #17826`, newline-isolated, single leaf
- [x] For each `#N`: #17826 is not `epic`-labeled; #17824/#17825 appear as `BLOCKED_BY`, not close-targets

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains a Contract Ledger matrix — four rows
- [x] Implemented PR diff matches the Contract Ledger

**Findings:** Pass, verified at `f91993c885`:

| Ledger row | Verified |
|---|---|
| New `examples/menu/context/` — no application-root listener or direct `left/top` fallback | File set is `MainContainer.mjs · app.mjs · index.html · neo-config.json`, **identical to `examples/menu/panel/`**. `me.addDomListeners({contextmenu: me.onContextMenu, scope: me})` — scoped to the component, never `document`. No `style.left` / `style.top` write anywhere in the example |
| Pointer placement consuming #17824; reopen updates the same Menu | `createMenuAlign(point)` builds `{axisLock: true, constrainTo: 'document.body', edgeAlign: 't0-b0'}` with the point as target; `onContextMenu` sets `menu.align` then `await menu.alignTo()`. Reuse proven by `openCount: 8` on one `contextMenuId` |
| Menu lifecycle consuming #17825; no wall-clock sleeps | Nested levels asserted by `.neo-menu-list` count `2` → `3`; leaf action → `Last action: Copy name` then count `0`; Escape; focusable outside; non-focusable outside → `{hidden: true, mounted: false}`. **Zero** `waitForTimeout` / `setTimeout` / `sleep(` in the spec |
| Real-input evidence, synthetic not accepted as the only path | `page.mouse.click(..., {button: 'right'})` in the open helper, `page.mouse.click(640, 100)` for outside, `page.keyboard.press('Escape')`. **Zero** `dispatchEvent` / `new MouseEvent` |

---

### 🪜 Evidence Audit

- [x] PR body carries its evidence declaration and the AC-by-AC table
- [x] Achieved ≥ required: this is the L3 tier the series was building toward — real browser pointer input plus Neural Link worker-state inspection. Correct level for a consumer proof, where the two prerequisites shipped at L2 deterministic-contract level
- [x] Residuals: none claimed
- [x] Two-ceiling distinction: not engaged
- [x] Evidence-class collapse check: I am not promoting the example's existence to proof of the underlying contracts; #17827 and #17828 carry those, and this PR is cited as the consumer demonstration
- [x] Deployment causality: N/A

**Findings:** Pass.

---

### N/A Audits — 📡 🔗
N/A across listed dimensions: no `ai/mcp/server/*/openapi.yaml` surface, and no skill / convention / `AGENTS*.md` / architectural-primitive files in the diff.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head CI green at `f91993c885bfd7edc97441b180b9bb1a18500700` — including `unit`, `components`, `substrate`, `check`, `check-freshness`, CodeQL and `lint`; `mergeStateStatus` CLEAN. Commit `author.name` is `Euclid`
- [x] Reviewer falsifier: **the `.gitignore` check, which did not fire.** `apps/**` html/mjs/neo-config.json are ignored repo-wide and every real app carries an explicit negation block, so a new tree can stage silently into the void. I checked whether `examples/` has the same trap: `.gitignore` lines 3–5 scope it to `/apps/**` only, with no `examples/` pattern, so no negation block is owed here. Verified rather than fired
- [x] Test location: pass. `test/playwright/e2e/rendering/ContextMenuNL.spec.mjs` sits beside `ButtonBaseNL.spec.mjs` exactly as the ticket's Architectural Reality specifies

**Findings:** Pass, with the viewport-constant caveat from the Depth Floor.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 — the file set matches `examples/menu/panel/` exactly, the example consumes both prerequisite contracts rather than reimplementing either, and all three named traps are avoided at the source: `contextmenu` is scoped to the component, placement goes through the alignment engine, and `parentComponent` is untouched. 4 deducted only because a test constant reaches outside its own file for a value it does not read.
*   **`[CONTENT_COMPLETENESS]`**: 94 — the class JSDoc states the app/Menu ownership split, `contextPoint` is documented as deliberately exposed for whitebox evidence rather than looking incidental, and the native-suppression comment names the trap at the point of avoidance. 6 deducted: the hardcoded `1280`/`720` constants carry no note of their coupling to `playwright.config.matrix.mjs`.
*   **`[EXECUTION_QUALITY]`**: 95 — real pointer input only, zero sleeps, and assertions spanning worker state, DOM counts for both submenu levels, focus, geometry, a reuse counter, and both error channels. Scored from source reads plus exact-head CI. 5 deducted for the config-mirroring bound, which can go green-then-red without a defect and silently stop exercising flip/constrain.
*   **`[PRODUCTIVITY]`**: 100 — all eight ACs traced to source or a named assertion, including the two hardest: viewport containment across four edges, and one reused instance across the whole journey.
*   **`[IMPACT]`**: 70 — canonical executable documentation for a common pattern, and the consumer proof that closes a three-PR series. Bounded because it adds no `src/` behaviour.
*   **`[COMPLEXITY]`**: 45 — one small example app and one journey spec; the difficulty here was discipline rather than structure, and the structure is deliberately minimal.
*   **`[EFFORT_PROFILE]`**: Quick Win — small, contained, high leverage as the pattern other applications will copy.

**Closing remarks:** Approving at `f91993c885`. Across #17827, #17828 and this one, the property I want on the record is that each PR proved the thing it was chosen *for* rather than a nearby proxy — the frozen terminal tuple, the deleted timer proven without a sleep, and here the serialized zero-size point target asserted at worker state instead of inferred from a correct-looking DOM. The viewport constants are the one place a future config change can quietly retire a probe; derive them from `page.viewportSize()` and the series is closed clean.

— Vega (Opus 5, Claude Code) 🌿

---

