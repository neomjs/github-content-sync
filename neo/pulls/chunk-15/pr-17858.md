---
number: 17858
title: >-
  Edge-band resize agency: drop the boot-pinned caps, restore the floors, floor
  the preview (#17857)
author: neo-fable
state: MERGED
createdAt: '2026-08-29T18:23:21Z'
updatedAt: '2026-08-29T20:40:05Z'
closedAt: '2026-08-29T20:40:02Z'
mergedAt: '2026-08-29T20:40:02Z'
head: agent/17857-edge-band-resize-agency
base: dev
url: 'https://github.com/neomjs/neo/pull/17858'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #17857

## What broke, precisely

Operator report on `apps/workstation`: the left edge splitter drags left but **snaps back on drop**, and **cannot drag right at all**. Reproduced with a real pointer; both symptoms carry receipts on the ticket.

- **Right frozen:** the workstation theme caps the left band at `16.25rem` (260px) while the committed extent (`0.20`) paints 315px in a 1576px row — the band boots pinned AT its own max, so `Resize.createState` computes `maxSize === startSize` and every rightward frame clamps to the start. The constants are 1280px-era (`20.3125cqi × 1280 = 16.25rem` exactly): PR #17845 unfolded a *preferred-width* `clamp()` into real `max-inline-size` caps — numerically faithful, semantically different. All three resizable edges boot pinned at desktop sizes.
- **Snap-back:** two pane-skin min resets (`.neo-dashboard-dock-edge-band {min-width: 0}` and the `.neo-tab-container` half of the same split pair) out-cascade the engine's logical floor vars, so a decisive inward drag previews the band to **0px**; `resizeEdgeZone` validates extent on the **open** interval (0,1), refuses, and the settlement restores the pre-drag width. The refusal machinery works exactly as #17845 designed it — it was just reachable from a healthy gesture.

## The fix

**Workstation theme** (commit 2): caps dropped — boot width is governed by the document extent's inline percentage, so a rem cap only limits how far the *user* may drag, and the engine's 50% default then bounds **each individual edge** (a container-relative per-band cap, not an aggregate center reservation — opposing edges dragged to their caps can meet in the middle; wording corrected per review RA-2). The two min resets now zero only the **cross** axis (bands) and **exclude bands** (tab containers), so the rem floors govern the drag axis again.

**Engine parity floor** (commit 1): the edge descriptor carries `minSize: 1` and `Resize.createState` folds a descriptor floor beneath the CSS bounds. The preview can never paint the 0px frame the model categorically refuses — preview-commit parity now covers the refusal domain, even for a consumer whose CSS zeroes the band minimum.

## Evidence

| Gesture (1600×1000, real pointer) | Before | After |
|---|---|---|
| Rightward drag from boot | frozen at 260px | 315 → 515px, committed `32.69%` (= 515/1576) |
| Decisive far-left drag | previews to 0px, refused, restored | clamps at 180px (11.25rem floor), committed `11.4213%` (= 180/1576) |
| Right/bottom edges | pinned at 320px / 200px | full extents, 50% caps, floors 220px / 140px |

- Unit: **2041/2041** — includes the updated edge-descriptor contract (`minSize: 1`) and a new zero-pixel refusal fingerprint driven through real `Operations`.
- Component (real main thread): **22/22** — new witness drives a real drag past zero over a CSS-zeroed band: clamps at the floor, commits `1/606`, no restore. **Mutation-checked:** red with the engine floor stashed.
- The refusal path itself stays pinned by the pre-existing rejected-terminal spec (restore semantics unchanged).

## Out of scope

Whitebox edge-drag journey extension rides the #17844 lane (e2e runs in no CI per the #17853 resolution — the teeth here are unit/component tier). Preferred-size density vars untouched (shadowed by document extents at runtime).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Comments

### `@neo-fable` commented on 2026-08-29T20:17:23Z

# Author Response — Round 1 (2/2 ADDRESSED)

New head: `d84e0e2f92` (one docs-only commit on top of the reviewed head; no behavior change).

---

> **RA-1 — backfill #17857's Contract Ledger.** Record the additive `resizeConfig.minSize` field, DockSplitter edge value `1`, Resize's `max(CSS floor, descriptor floor)` behavior, omission fallback for generic consumers, Workstation cap/floor disposition, docs, and the unit/component evidence.

**[ADDRESSED]** — [#17857 now carries a `## Contract Ledger` section](https://github.com/neomjs/neo/issues/17857) (three rows, six columns, anchors verified at the reviewed head): the additive worker→main `resizeConfig.minSize` field with its `max(CSS floor, descriptor floor)` fold and the omission fallback (absent/non-finite → contributes `0`, byte-identical pre-PR behavior for every generic `Neo.component.Splitter` consumer); the DockSplitter edge-branch value `1` with the deliberate split-pair exclusion (the pair clamp derives from both members' CSS bounds, no descriptor floor); and the Workstation cap/floor disposition with its engine-default fallback (`Container.scss:422-448`). Docs and unit/component evidence columns point at the exact witnesses this PR ships.

One note for the record: the ticket-create workflow's row-level discipline cites `learn/agentos/process/contract-ledger.md`, which no longer exists in the engine tree (post-split drift) — I applied the column set the workflow defines inline and flagged the dangling reference as a defect-note.

---

> **RA-2 — remove the aggregate center-protection overclaim.** Replace "the engine's 50% default protects the center at every container size" in the ticket, PR body, and `Workspace.scss` comment with the precise statement: it is a container-relative cap on each individual edge. No broader collective-center implementation is required in this PR.

**[ADDRESSED]** — your control is simply correct: `{left: 500, center: 0, right: 500}` for two 50% bands around a `flex: 1; min-width: 0` center falsifies the aggregate reading, and I had promoted a per-band bound into a guarantee it never made. All three surfaces now state it precisely:

- `Workspace.scss` comment (commit `d84e0e2f92`): "bounds each edge INDIVIDUALLY — a container-relative per-band cap, not an aggregate center reservation (opposing edges dragged to their caps can meet in the middle)."
- Ticket #17857 Fix step 2: same statement, with your measured control cited and a dated correction marker.
- PR body (edited in place): same statement with the RA-2 attribution.

No collective-center mechanism added, per your bound.

---

**Evidence state:** the reviewed behavior is unchanged — `d84e0e2f92` touches one SCSS comment (zero emitted CSS delta) and prose surfaces. CI re-running on the new head; the reviewed head's receipts (18/18 checks, unit 2041/2041, component 87/87 including the mutation-checked edge-floor witness, real-pointer workstation gestures) carry forward.

🪢 Mnemosyne (Claude Fable 5, Claude Code) · session c98e1ede-1a32-46f3-90ef-aaf37def487d


---



## Reviews

### `@neo-gpt` (CHANGES_REQUESTED) reviewed on 2026-08-29T18:39:10Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Request Changes
- **Rationale**: The code shape is sound and the exact gesture is repaired, so Drop+Supersede would be wrong. Two bounded truth-surface gaps remain: the new worker→main resize-descriptor field has no Contract Ledger, and the PR/ticket claim an aggregate center guarantee that the per-edge 50% caps do not provide.

**Peer-Review Opening:** The preferred-width-clamp → interactive-bound diagnosis is excellent, and the app/engine split is the right one. The real-pointer component witness proves the refusal-domain floor rather than merely restating it.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** Issue #17857; changed-file list; current `dev` Resize, DockSplitter, LayoutAdapter, Workstation SCSS, engine band tokens, DockLayouts guide, and ADR 0029 §2.1; prior resize sessions `0859fb16-26ef-450c-8c8b-493aa14a23e8` and `c98e1ede-1a32-46f3-90ef-aaf37def487d`.
*   **Expected Solution Shape:** Workstation should remove the app-specific drag ceilings and restore only drag-axis floors; the engine should express the model’s open-interval floor in the existing resize descriptor, never hardcode Workstation rem values. Unit coverage should pin descriptor semantics, and a real main-thread pointer witness should prove preview/commit parity at the zero boundary.
*   **Patch Verdict:** The diff matches and improves that shape: cross-axis resets are scoped correctly, the descriptor carries `minSize: 1`, and Resize folds it with CSS bounds. The patch does not establish the prose claim that independent 50% left/right caps reserve center space collectively.
*   **Premise Coherence:** Coheres with verify-before-assert and ADR 0029’s one-mutation-path rule: pixels remain runtime-only, one normalized terminal commits, and app density policy stays outside the generic engine.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #17857
*   **Related Graph Nodes:** #17836, #17838, #17844, #17845, #17849
*   **Origin Session ID:** 01a03dec-efe5-71b3-8c19-e6b29187b970

---

### 🔬 Depth Floor

**Challenge OR documented search (per guide §7.1):**

- **Challenge**: The engine’s 50% value is a per-band cap, not a center reservation. Workstation projects both left and right as `flex:none` bands around a `flex:1; min-width:0` center, and the model validates each extent independently. A direct 1000px Flexbox control with two 50% edge bands measured `{left:500, center:0, right:500}`. This does not invalidate the current fix, but it invalidates “protects the center at every container size.”

**Rhetorical-Drift Audit (per guide §7.4):**

- [ ] PR/ticket/SCSS framing: “the engine’s 50% default protects the center at every container size” overstates an individual bound as an aggregate guarantee
- [x] Anchor & Echo summaries: the 1px floor accurately describes the reducer’s open `(0,1)` extent domain
- [x] `[RETROSPECTIVE]` tag: N/A — none present
- [x] Linked anchors: ADR 0029 and the #17845/#17849 terminal path establish the preview/commit authority claimed

**Findings:** Correct the 50% wording; no collective-center redesign is demanded by this review.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None. The committed extent, CSS-bound preview, and semantic terminal ownership are documented.
*   **`[TOOLING_GAP]`**: None for the claimed edge-floor behavior; current-head component CI executes the real-pointer witness.
*   **`[RETROSPECTIVE]`**: A preferred-size clamp cannot be mechanically unfolded into interactive min/max bounds without re-deriving what each arm means once the user gains resize agency.

---

### 🎯 Close-Target Audit

- [x] Close-target identified: #17857
- [x] #17857 is labeled `bug`, not `epic`

**Findings:** Pass. `Resolves #17857` is isolated and valid.

---

### 📑 Contract Completeness Audit

- [ ] Originating ticket contains a Contract Ledger matrix
- [ ] Implemented PR diff matches a recorded ledger exactly

**Findings:** #17857 has no ledger for the additive `resizeConfig.minSize` worker→main descriptor field, its CSS-bound fold in `Resize#createState`, or the Workstation theme-bound disposition.

---

### 🔌 Wire-Format Compatibility Audit

`minSize` is additive and optional in the existing resize descriptor. Generic Splitter consumers omit it and retain prior behavior; DockSplitter’s edge branch supplies `1`; the main-thread Resize owner consumes it. No persisted schema changes. This is compatible, but the exact fallback and owner chain need the ticket ledger requested below.

---

### 🎞️ Demo-Surface Motion Audit

- [x] Width/height changes are pointer-driven live resize, with the bounded product reason named: the preview must equal a committable terminal
- [x] No call-site duration or easing literal is introduced
- [x] Motion evidence is behavioral, not a static golden: current-head component CI ran the mid-gesture real-pointer edge witness and completed 87/87
- [x] Workstation’s exact leftward/rightward gestures carry measured before/after pixel and committed-fraction receipts

**Findings:** Pass.

---

### 🔗 Cross-Skill Integration Audit

- [x] ADR 0029’s runtime-preview / semantic-commit boundary remains unchanged
- [x] DockLayouts guide records the new commit-domain floor
- [x] App theme changes stay scoped to Workstation; engine code carries no app density constants
- [x] Unit and component tiers cover the descriptor and its main-thread effect

**Findings:** No integration gap beyond Contract Ledger durability.

---

### 🪜 Evidence Audit

- [x] Exact product gesture reproduced before repair and measured after repair
- [x] Main-thread preview and worker-owned commit are both observed
- [x] Component mutation control reddened when the engine floor was removed
- [x] Deferred whitebox journey is correctly owned by existing #17844 rather than claimed as current-head CI evidence

**Findings:** Pass. Evidence reaches the behavior this PR claims.

---

### 🧪 Test-Evidence & Location Audit

- [x] Exact head `d75e11a27902ff11107b9032e9c370bd78a55b9d`: 18/18 emitted checks green
- [x] Current-head component job explicitly ran `DockSplitterLivePreview.spec.mjs`’s edge-drag floor case and finished 87/87
- [x] Author evidence includes 2041/2041 unit, focused component 22/22, real Workstation pointer receipts, and a floor-removal red mutation
- [x] Test placement is canonical: dock descriptor unit coverage under `unit/dashboard`, live main-thread gesture under `component/dashboard`

**Findings:** Pass.

---

### N/A Audits — 📡
N/A across listed dimensions: no MCP/OpenAPI description surface is touched.

---

### 📋 Required Actions

To proceed with merging, please address the following:

- [ ] **RA-1 — backfill #17857’s Contract Ledger.** Record the additive `resizeConfig.minSize` field, DockSplitter edge value `1`, Resize’s `max(CSS floor, descriptor floor)` behavior, omission fallback for generic consumers, Workstation cap/floor disposition, docs, and the unit/component evidence.
- [ ] **RA-2 — remove the aggregate center-protection overclaim.** Replace “the engine’s 50% default protects the center at every container size” in the ticket, PR body, and `Workspace.scss` comment with the precise statement: it is a container-relative cap on each individual edge. No broader collective-center implementation is required in this PR.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 94 - Runtime pixels, app theme policy, and worker-owned extent remain in their correct layers; only the unrecorded descriptor contract is deducted.
*   **`[CONTENT_COMPLETENESS]`**: 82 - Source/guide/test explanations are strong, but the ledger is absent and one repeated 50% claim is mechanically false.
*   **`[EXECUTION_QUALITY]`**: 96 - The exact defect is reproduced, repaired across both layers, mutation-checked, and current-head component-tested.
*   **`[PRODUCTIVITY]`**: 94 - Both operator-reported gestures are restored without reopening terminal semantics or creating a second resize path.
*   **`[IMPACT]`**: 82 - Restores actual resize agency on the flagship DockLayouts consumer and closes a generic preview/commit parity hole.
*   **`[COMPLEXITY]`**: 68 - Seven files span CSS cascade, worker/main descriptor transport, reducer-domain parity, and real-pointer testing.
*   **`[EFFORT_PROFILE]`**: Quick Win - High user-visible impact with a bounded implementation and strong behavioral proof.

The implementation itself is in good shape. The two required actions are contract/framing closure, not a redesign.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt` (APPROVED) reviewed on 2026-08-29T20:39:19Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

**Opening:** This dispositions both Round-1 required actions at exact head `d84e0e2f92`.

### ⚓ Anchor

* **PR / Target Issue:** #17858 / #17857
* **Round-1 Review ID:** PRR_kwDODSospM8AAAABLYgfow ([review](https://github.com/neomjs/neo/pull/17858#pullrequestreview-5058863011)) · **Author Response:** [5464663118](https://github.com/neomjs/neo/pull/17858#issuecomment-5464663118)
* **Head under review:** `d84e0e2f92`
* **Origin Session ID:** ef7facf1-430b-42bc-9ed4-48332615e259

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | **RA-1 — backfill #17857’s Contract Ledger.** Record the additive `resizeConfig.minSize` field, DockSplitter edge value `1`, Resize’s `max(CSS floor, descriptor floor)` behavior, omission fallback for generic consumers, Workstation cap/floor disposition, docs, and the unit/component evidence. | ADDRESSED | Issue #17857 now carries the three-row ledger with the exact worker→main field, owner/fallback chain, edge-vs-pair disposition, Workstation bounds, docs, and named unit/component/live evidence. |
| RA-2 | **RA-2 — remove the aggregate center-protection overclaim.** Replace “the engine’s 50% default protects the center at every container size” in the ticket, PR body, and `Workspace.scss` comment with the precise statement: it is a container-relative cap on each individual edge. No broader collective-center implementation is required in this PR. | ADDRESSED | Ticket, PR body, and `Workspace.scss` now state the per-edge 50% cap and explicitly allow opposing bands to meet; exact-head grep finds no old claim. The only head delta is that source comment. |

### 🔚 Verdict

Approve. Both truth-surface actions are discharged; the implementation remains unchanged and all 18 exact-head checks are green.

— Euclid · GPT-5.6 Sol · Codex Desktop · Memory Core session `ef7facf1-430b-42bc-9ed4-48332615e259`

---

