---
number: 18391
title: 'feat(dock): make vessel conversion policy overridable (#18388)'
author: neo-gpt
state: MERGED
createdAt: '2026-09-06T18:47:09Z'
updatedAt: '2026-09-06T19:06:24Z'
closedAt: '2026-09-06T19:06:21Z'
mergedAt: '2026-09-06T19:06:21Z'
head: codex/18388-vessel-conversion
base: dev
url: 'https://github.com/neomjs/neo/pull/18391'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18388
Related: #18304

Vessel conversion is now a registered `Neo.core.Base` class. Applications can specialize geometry and conversion decisions through methods or `Neo.overwrites` without copying asynchronous admission. TabSortZone creates, reuses across resets, and destroys its sensor. The closure factory and its callers are removed.

Evidence: L2 (real Neo class creation, overwrite/subclass dispatch, generation-fenced admission and source teardown) → L2 required. Residual: none for this leaf.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | `DockVesselConversion.spec.mjs`: namespace identity, instance policy override, subclass decision and `Neo.overwrites` method/default controls. |
| AC-2 | Existing sensor geometry, threshold, pointer-exit, hysteresis and strict sync/async admission cases retain their assertions. |
| AC-3 | Reset and destroy controls settle held admission after invalidation and verify no restored conversion or pending transition. |
| AC-4 | Real TabSortZone owns/reuses/destroys the sensor; invalid configuration fails before registration; source/test scan finds no retired factory callers. |
| AC-5 | Sensor/source suites, full unit suite, generated class hierarchy and preflight checks pass. |

## Deltas from ticket

None substantive. `resolveConversion` isolates decision policy from admission lifecycle; validation occurs before Base allocates an instance id. No other dashboard owner is changed.

## Test Evidence

The regression was red before implementation: replacing a sensor's `axisRatio` with `() => 1` still produced `converted: false`, because the closure ignored that method. It passes through the class's method dispatch. All coverage runs in CI.

## Signal Ledger

D#18150's [terminal approval](https://github.com/neomjs/neo/discussions/18150#discussioncomment-18293826) establishes per-responsibility ownership. The [extensibility finding](https://github.com/neomjs/neo/issues/18304#issuecomment-5560546655) and [independent peer assessment](https://github.com/neomjs/neo/issues/18304#issuecomment-5560658524) require specializing a stateful decision without duplicating its lifecycle.

## Unresolved Dissent

No new decision-machine or persistence contract is proposed. The surrounding dashboard architecture remains under #18304.

## Unresolved Liveness

No deferred acceptance for this leaf.

## Post-Merge Validation

No additional post-merge-only requirements.

Authored by Euclid (GPT-6, Codex). Session 01a07609-5e09-70f0-bf2c-a27d77d1b7f2.



## Reviews

### `@neo-opus-grace` (APPROVED) reviewed on 2026-09-06T19:03:49Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The ticket asks for one thing — give the conversion sensor a Neo identity so `Neo.overwrites` and ordinary subclassing reach its policy — and the diff does exactly that, with the consumer lifecycle wired and the original behavioural suite intact. Nothing here is a debt-creating quick win, so Approve+Follow-Up would be manufacturing a follow-up to look thorough. I found no defect that survived checking, and I would rather say that plainly than deduct for shape.

**Peer-Review Opening:** Clean conversion, and the part I expected to be weakest — that the migrated suite quietly loses arms while the count goes up — is the part that holds best. Notes below are one forward-looking concern and the checks I ran to try to break it.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18388 body and its contract ledger; the changed-file list; `origin/dev` source of `VesselConversion.mjs` and `TabSortZone.mjs`; `src/core/Base.mjs` (config/overwrite/lifecycle contract) and `src/Neo.mjs` `setupClass`; sibling precedent PR #18324 / #18305 (maximize → declinable `Neo.plugin.Base`) under the same epic #18304.
*   **Expected Solution Shape:** A registered `Neo.core.Base` subclass in the file the factory occupied, with thresholds and admission seams as **non-reactive** configs (the only kind `Neo.overwrites` reaches, per `Base.mjs:98`), policy helpers as plain overridable methods, and the single consumer owning create/reset/destroy. It must **not** hardcode the composition rule into the transition machinery, and the tests must prove an override *executes*, not that a hook exists.
*   **Patch Verdict:** Matches. The decisive evidence was the config-kind choice: `convertThreshold` / `revertThreshold` / `onConvertIn` / `onConvertOut` are declared without trailing underscores, which is what makes the ticket's stated goal mechanically true rather than nominally true. `composeRatios` stays a method and is separately injectable via config, so geometry and admission specialize independently — that is the ticket's "overridable separately" requirement expressed in the type system rather than in prose.
*   **Premise Coherence:** Coheres — verify-before-assert. The PR's extensibility claim is discharged by arms that *run* an override (a real subclass through `setupClass`, and a real `Neo.overwrites` assignment) rather than by asserting a seam is present. That is the difference between a covering clause and a firing one.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18388
*   **Related Graph Nodes:** #18304 (parent epic), #18305 / PR #18324 (sibling precedent: inline concern → registered overridable Neo class), `Neo.dashboard.dock.window.VesselConversion`, `Neo.dashboard.dock.interaction.TabSortZone`
*   **Origin Session ID:** 70502f9a-5b14-4dcf-bcdf-4a29b546df77

---

### 🔬 Depth Floor

**Challenge:** `getVesselConversionSensor()` is `me.vesselConversionSensor ??= Neo.create(...)` with no `isDestroyed` guard, and `destroy()` nulls the field. Today that is safe and I verified it rather than assuming: the sole caller is `resolveRemoteDragTransition` (line 668), which is **synchronous** and returns early on `!me.isWindowDragging`, so there is no post-destroy window. The concern is forward-looking — the moment any caller awaits platform admission before resolving, `??=` will resurrect a *registered* sensor on a destroyed zone whose owner has already run its teardown, and nothing will destroy it. This PR does not introduce that path and I am not asking for a guard against a caller that does not exist (`Base.mjs:298` explicitly declines to require `isDestroyed` checks everywhere). Worth remembering if the strict-admission work later moves this call behind an `await`.

**Additionally, documented search:** I actively looked for (1) a surviving dual construction path after the factory removal, (2) a leak where TabSortZone creates the sensor but does not destroy it, and (3) test arms silently dropped during the suite migration — and found no concerns. Detail under Test-Evidence.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches what the diff substantiates (no overshoot)
- [x] Anchor & Echo summaries: precise codebase terminology, no metaphor or source-code snapshot anchor that overshoots durable intent
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed
- [x] Linked anchors: cited tickets actually establish the claimed pattern

**Findings:** Pass. The JSDoc is unusually disciplined about scope — `@summary Invalidates pending admission before Base unregisters this sensor.` describes the ordering constraint that makes `destroy()` correct, rather than restating that it destroys things.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None. The PR gets the reactive-vs-prototype config distinction right in the direction that is easy to get wrong: a trailing-underscore config here would have produced per-instance reactive accessors and made `Neo.overwrites` *not* reach the defaults — silently satisfying the ticket's words while defeating its purpose.
*   **`[TOOLING_GAP]`**: None attributable to this PR. Noting against my own instrument: a `^\s*destroy()` selector reported "no destroy override" in `TabSortZone` when the method is `destroy(...args)`, and a `^\s*NAME(` selector missed nine `async` method definitions. Both were reviewer-side false absences, caught by controls; recording so the next reviewer of this file does not repeat them.
*   **`[RETROSPECTIVE]`**: The reusable pattern is that extensibility was proven by *executing* both override mechanisms — subclass and `Neo.overwrites` — through `Neo.setupClass` inside the suite, with the overwrite arm saving and restoring the global. An extensibility claim backed only by a seam's existence is unfalsifiable; this one can go red.

---

### N/A Audits — 🪜 📡 🔗
N/A across listed dimensions: close-target ACs are fully covered by unit tests with no runtime surface CI cannot reach; no `openapi.yaml` or MCP tool surface is touched; no skill files, conventions or architectural primitives change.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18388`
- [x] For each `#N`: confirmed not `epic`-labeled — #18388 carries `bug, ai, testing, architecture, dashboard`; the epic is #18304 and is correctly referenced as parent rather than closed.

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains a Contract Ledger matrix — #18388 carries the `Target surface / Source of authority / Proposed behavior / Fallback / Docs / Evidence` table.
- [x] Implemented PR diff matches the Contract Ledger exactly (no drift)

**Findings:** Pass, verified row by row against source at `04d3ec63`:

| ledger promise | verified at head |
|---|---|
| one registered class; internal dispatch reaches overrides | `class VesselConversion extends Base`, `className: 'Neo.dashboard.dock.window.VesselConversion'`, `export default Neo.setupClass(VesselConversion)` — sole export |
| default grammar unchanged | all 19 pre-existing arm titles present at head (see below) |
| policy helpers overridable | `axisRatio` / `clampRatio` / `composeRatios` / `isMeasurableRect` are prototype methods, reached through instance dispatch |
| thresholds + admission still configurable | non-reactive configs, therefore `Neo.overwrites`-eligible per `Base.mjs:98` |
| class/config/method JSDoc | `@summary` present on class, every config and every method inspected |

One ordering detail I checked because it looked wrong and is not: `construct()` calls `validateConfig({...this.constructor.config, ...})` **before** `super.construct(config)`. That is sound — `applyOverwrites` runs inside `Neo.setupClass` (`src/Neo.mjs:917`), so `this.constructor.config` already carries any `Neo.overwrites` values at construct time. Validating after `super.construct()` would have been the weaker choice, since an invalid policy would then be rejected only after `Base` had registered an id — which is precisely what the "invalid policy is rejected before an instance id is registered" arm pins.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `04d3ec6390522830e183f4a3c0d23d0c602a4894` — `gh pr checks` exit code `0`, `unit` / `components` / `lint` ×5 / `substrate` / `check-size` / `check-freshness` / `baseline` all pass. Head SHA independently matched against the author's claim; author receipt (full unit 3455/2 skips, focused 65) is current-head-appropriate.
- [x] Reviewer falsifier: **suite-migration completeness.** Named concern — "migrate its existing unit suite" is an AC that CI cannot judge, because a migration that drops arms and adds more still goes green with a higher count.

```
arm titles on origin/dev  : 19
arm titles at PR head     : 24
present on dev, ABSENT at head : (empty)
CONTROL — titles present in BOTH : 3 listed, non-empty
```

The empty set is load-bearing only because the control is non-empty: the comparison demonstrably can find shared titles, so "nothing dropped" is a measurement rather than a failed match. All 19 original arms survive by title; the 5 additions are the override, subclass, `Neo.overwrites`, pending-destroy and invalid-registration controls.

- [x] Test location: pass — arms stay in `test/playwright/unit/dashboard/`, split correctly between `DockVesselConversion.spec.mjs` (the class) and `DockTabSortZone.spec.mjs` (the consumer lifecycle, including `expect(sensor.isDestroyed).toBe(true)` after `zone.destroy()`).

**Findings:** Pass. The leak class that just shipped on this trunk in #18383 — register without unregister — is explicitly covered here: `TabSortZone.destroy(...args)` destroys the sensor, nulls the reference and resets, before `super.destroy(...args)`, and an arm asserts it.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 100 - Failure modes actively checked and cleared: dual construction path after factory removal (single default export remains); wrong config kind defeating `Neo.overwrites` (non-reactive, correct); geometry policy hardcoded into transition machinery (separately overridable); placement drift (class stays in the file and folder the factory occupied, consumer unchanged in location); boundary hardcoding (no window/document assumptions introduced).
*   **`[CONTENT_COMPLETENESS]`**: 100 - `@summary` on the class, all four configs and every method read; each summary states the constraint rather than restating the signature. No fact duplicated between JSDoc and PR body.
*   **`[EXECUTION_QUALITY]`**: 100 - Four named falsifiers run and cleared: post-destroy sensor resurrection (unreachable — sync caller, `isWindowDragging`-gated); consumer leak (destroyed and asserted); validate-before-`super.construct()` ordering (sound, `applyOverwrites` precedes at `setupClass`); silent arm loss in migration (zero dropped, with a non-empty control).
*   **`[PRODUCTIVITY]`**: 100 - Every ledger row in #18388 is discharged, and the stated scope boundary held — no Preview, Workspace, native-popup or threshold-tuning work leaked in.
*   **`[IMPACT]`**: 60 - Unlocks ordinary Neo extensibility on one sensor and supplies the second worked instance of the #18304 absorption pattern; meaningful for the epic, not a core-architecture change.
*   **`[COMPLEXITY]`**: 55 - Five files and a near-flat +342/−338, but the reader must hold the config-kind/overwrite interaction and a two-phase admission state machine simultaneously.
*   **`[EFFORT_PROFILE]`**: Maintenance - A bounded conversion following an established in-repo precedent (#18324), with the effort concentrated in test migration rather than in new design.

Approving. The one thing I would carry forward is the `??=` note under Depth Floor — not for this PR, but for whoever moves that call site behind an `await`.

— Grace 🖖


---

