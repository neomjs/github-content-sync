---
number: 18351
title: >-
  feat(manager): logical topology Groups bind windows across generations; the
  dock, the Workstation and DemoB reserve their vessels in them (#18309)
author: neo-opus-vega
state: MERGED
createdAt: '2026-09-05T01:37:31Z'
updatedAt: '2026-09-05T13:03:37Z'
closedAt: '2026-09-05T13:03:34Z'
mergedAt: '2026-09-05T13:03:34Z'
head: agent/18309-transaction-group-binding
base: dev
url: 'https://github.com/neomjs/neo/pull/18351'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18309
Related: #18303 #18304 #18317

🌿 A window no longer proves who it is by reading its own URL; it carries what its Group told it, and the Group cannot be taken from it.

`Neo.manager.Transaction` is the worker-side authority for logical topology Groups: `appName` routes and never identifies, a window binds into a Group under one `workspaceKey`, and every binding records the replaceable runtime generation. A warm reload releases and rebinds its own slot on a lineage token; a late disconnect for a superseded generation finds no binding carrying its `windowId` and changes nothing; a copied identity presented while its binder lives forks a new Group; a released or reserved slot is held for its lineage for a bounded lease; an opener reserves a slot for a window it is about to create. Two boundaries the review executed and this head closes: a minted or forked identity is bound and published only once the window's carrier accepted it — the main thread's acknowledgement is awaited, and a pending, refused or rejected write, or a window that left meanwhile, admits nothing; and a lineage is dead once its lease ran out or its holder revoked it — a late child presenting it forks into its own Group instead of entering the live one, and only the holder's own token gives a slot back, so an older open's late failure cannot revoke its replacement. The boot carrier is a separately named persistent `sessionStorage` record, read by `worker/Manager` into every config registration and written by `Neo.Main#setTopologyIdentity` on the worker's word; `Main.windowOpen({topologyIdentity})` hands a staged same-origin child its reserved slot, or clears the inherited copy so a plain popup boots as a root of its own. The manager loads with the first multi-window participant — a single-window app's closure carries no Group machinery — and admits every window as its app registers.

The three consumers of the old vessel protocol change with the shared contract, per the author's disposition on the ticket: the dock `Workspace` reserves `popup:<itemId>` in its own Group for every tear-out vessel and observes its Group's bind, release and lease expiry on the manager; the Workstation adopts that lifecycle through the engine's hooks and keeps only what it alone knows (platform seams, theme bootstrap, embodiment staging, vessel workspaces, its journeys' receipts); the composition-style DemoB host reserves its stage targets, click pop-outs and tear-out vessels on the manager directly. Their owner grants, generation counters, connect-admission maps, `hostId`/`vesselFlow`/`vesselGrant`/`vesselGeneration`/`vesselAdmission` URLs, URL parses and per-instance worker `connect`/`disconnect` subscriptions are deleted; no compatibility branch remains and the `admissionToken` alias left with its last reader. A vessel's identity is its slot's lineage token, so a retirement presenting a superseded one is refused and the live vessel survives it. Every host remembers the Group its window bound into for its lifetime — the engine Workspace as `topologyGroupId`, DemoB in its own field — so releasing the window's slot, a reload's new generation or a lease running out never loses the documents it registered; a first boot's minted identity arrives with its accepted binding, and the host registers its participants then.

Evidence: L3 achieved (red-first unit arms on the manager, the carrier, the engine Workspace, the Workstation and DemoB; the real-browser pop-out component spec; under the Brain root, the two-root browser arm for AC-6 and the flagship journey reading the lineage off the vessel's carrier) → L3 required (AC-1 to AC-8). Residual: none.

## AC Evidence
| AC-1 | CI-covered: `test/playwright/unit/manager/Transaction.spec.mjs` › "a root without identity mints a Group; a second root of the same app mints another" — two Groups, distinct ids; exported through `src/manager/_export.mjs` |
| AC-2 | CI-covered: `Transaction.spec.mjs` › "the two-Workstation falsifier…" (the root reload rebinds its own slot at generation 2, same Group, the popup's slot untouched) and "a Group this worker never saw is created cold under the carried id"; `unit/main/MainNativeWindowRoute.spec.mjs` › "the topology identity rides the native route carrier…" (empty carrier reads `{}`, the write-back is what the next load presents, a staged child receives its reservation, a child opened without one has its inherited copy cleared); real browser: `component/dashboard/DockPopOut.spec.mjs` › "the host window is bound into a Group…" — a dedicated worker's host admitted at registration, the minted identity written into the page's `sessionStorage`. Acknowledged admission: `Transaction.spec.mjs` › "a minted or forked identity is published once its carrier accepted it…" — while the write is pending there is no binding, no Group and no event; `false`, a rejection and a non-`true` answer refuse with `admissionRefused`; a window released while its write is pending is refused (`window-gone`); the accepted root is published once and rebinds on reload without a write |
| AC-3 | The slot key is `popup:<itemId>` for every vessel (`Workspace#tearOutWorkspaceKey`, DemoB's openers) and the popup workspace id for DemoB's stage targets; the manager's binding is the one identity — the engine's `tearOutAdmissions` and DemoB's `vesselReservations` hold host context (sort zone, flow, window name) and no token or generation of their own, and the Workstation's `vesselOwnerGrants` + `tearOutConnectAdmissions` and DemoB's copies are deleted. Keyed participant membership is the Group's (`Neo.manager.Transaction#registerParticipant` / `getParticipant` / `participantKeys` / `unregisterParticipant`): `createDockWorkspaceSet` keeps no entry map — registration, lookup and count are the host's Group's, refused and fail-closed before the host's window has bound — and only the dock's both-or-neither adoption stays the adapter's until #18312 (the author's split, ticket comment 5548622668). The hosts remember their Group: the engine Workspace's `topologyGroupId` and DemoB's field are learned from the window's accepted binding — at construct when the carrier already held the identity, else from the `bind` event — and kept for the instance's lifetime, so the production resolver (`getGroupId: () => me.topologyGroupId`) reaches the participants after the root's release and lease expiry; a first boot registers them when its binding arrives (`afterSetTopologyGroupId`, DemoB's `adoptTopologyGroup`), a never-bound host refuses, a headless Workstation joins when a container supplies its window id. CI-covered: `unit/dashboard/DockWorkspaceSet.spec.mjs` › "before the host binds there is no membership…" and "membership is the Group's, and it outlives the binding…" (with the live-binding resolver as the control that loses it); `unit/manager/Transaction.spec.mjs` › "participants are the Group's and outlive its bindings…"; `unit/apps/workstation/Workspace.spec.mjs` › "the production resolver keeps the main participant through the root's release and lease expiry; a never-bound first boot joins when its binding is accepted"; `unit/examples/dashboard/crossWindow/DemoBWorkspace.spec.mjs` › "the production resolver keeps the three participants…"; `unit/dashboard/DockWorkspace.spec.mjs` › "the host keeps the Group it learned…"; the Workstation spec's headless arm registers `main` on the window id's arrival |
| AC-4 | CI-covered: `Transaction.spec.mjs` › the falsifier's tail — `release('a1')` after `a3` rebound returns `false`, `a3` stays bound at generation 2; at the consumers: `unit/dashboard/DockTearOut.spec.mjs` › "a retirement presenting a superseded lineage token clears nothing…" and `unit/apps/workstation/Workspace.spec.mjs` › "a retirement presenting a superseded lineage token is refused, and the live vessel survives it" — the control the author ran against `dev`. Revocation is fenced the same way: `Transaction.spec.mjs` › "a revocation is fenced by lineage…" and `unit/dashboard/DockWorkspace.spec.mjs` › "two overlapping opens for one item…" — `acquireTearOutVessel` twice with the first platform open pending: the older open's late failure revokes nothing, the replacement's child is admitted |
| AC-5 | CI-covered: `Transaction.spec.mjs` › "a copied identity presented while its binder is live forks a new Group" and "a released slot rebinds only for its lineage token; a stranger forks"; `unit/examples/dashboard/crossWindow/DemoBWorkspace.spec.mjs` › "the workspace stage mounts the one child that binds its reserved slot — never a forged or replayed identity"; a bookmarked popup URL names content only and carries no owner at all, so it boots as a fresh root (`MainNativeWindowRoute.spec` — a fresh window's carrier is empty). A dead lineage forks: `Transaction.spec.mjs` › "a dead lineage is not a door…" (expired, revoked and unreserved presentations fork, the live reservation binds, a cold root still returns); `unit/dashboard/DockWorkspace.spec.mjs` › "a late child presenting an expired reservation forks and never connects…"; `DemoBWorkspace.spec.mjs` › "a child presenting an expired or revoked reservation forks and reparents nothing — the pending record left with the slot" (DemoB subscribes `leaseExpired`; the engine's admission expiry already retired its vessel) |
| AC-6 | CI-covered, unit form: `Transaction.spec.mjs` › "the two-Workstation falsifier: a reload moves one generation, and a late disconnect cannot unbind the successor" — A + its popup one Group, B shares only the app name, reloading A creates no Group and touches neither B nor A's popup. Browser form, outside CI under the Brain root: `test/playwright/e2e/workstation/WorkstationTopologyGroupsNL.spec.mjs` — root A (the full Workstation) and root B (the Workstation's pane-host boot with no opener and no carrier: the same app in the same SharedWorker, its own window, admitted as a root of its own) carry two Groups; A's real pop-out binds a `popup:` slot of A's Group and its URL names content and theme only; reloading A rebinds A's slot at generation 2 under the same Group, the superseded generation holds nothing, B's binding and carrier and the vessel's binding are unchanged, and the Group count stays two (1/1 at head). B is the pane-host boot because two full roots crash the headless harness process on `dev` as on this branch — real Chrome runs both side by side; defect-noted to the swarm, not this PR's scope |
| AC-7 | Implementation code by the repository's Acorn counter (`check-file-sizes.mjs`'s `measureSource`), against `dev@ea54d5ff89` at this head: `src/` +307 (`manager/Transaction.mjs` is +261 of it), `apps/` −188, `examples/` −54 → `src/ + apps/ + examples/` **+65**. Raw diff lines beside it: `src/` +1008/−256, `apps/` +273/−505, `examples/` +339/−368 → +1620/−1129. At `5170aa0b1e` the same measure read **−20**: the four boundaries the first review executed — acknowledged admission, dead-lineage refusal, lineage-fenced revocation, the remembered Group — cost 85 implementation lines, 52 of them in the manager. **Met under the author's amended criterion** (ticket comment 5551938509: at most +65 net implementation lines for this first ownership cut — a bounded accounting amendment on the complete implementation, not a general growth exemption; any further growth needs a fresh disposition): +65 at this head is exactly the bound, the original named deletions are in, no compatibility owner remains, and no deletion was manufactured for the number. The replaced paths are gone in all three consumers — owner grants and generation counters, the URL admission and its parses, connect-admission maps, per-instance worker subscriptions (#18317's included), the `admissionToken` alias, and the WorkspaceSet's own entry map — with no compatibility branch |
| AC-8 | `learn/benefits/ArchitectureOverview.md` gains the `src/manager/` inventory row linking ADR 0029 |

size-guard-growth: src/dashboard/dock/Workspace.mjs — the host remembers the Group its window bound into (review round 1, RA-1): the resolver, its hook and the own-window adoption in onTopologyBind; 1284 → 1292 code lines, against the standing stop-adding directive as the review-demanded exception

## Deltas from ticket
**Admission rides app registration, not `connect`.** `connect` is a SharedWorker port event; `worker/Base.onRegisterApp` finds no port for a dedicated worker and never calls `onConnect`, so a manager subscribed to `connect` never hears a single-window app's host (measured on the pop-out component fixture: zero Groups). The identity therefore rides the config registration every window sends, and the manager admits a window as its app registers — the first moment a live app exists for the bind to reach. The manager subscribes to `disconnect` alone.

**The manager loads on demand.** The engine Workspace imports it only when the tear-out lifecycle is enabled or a slot is reserved, so a single-window app's closure stays without Group machinery (70 modules at `dev` and at head; a static import had made it 71) — the single-window load boundary the author named.

**AC-7's accounting and AC-3's boundary (the author's disposition, ticket comment 5548622668).** The criterion counts implementation code across `src/ + apps/ + examples/` with the repository's Acorn counter, raw totals beside it — DemoB is a named consumer of the same criterion. The Group owns keyed participant membership here; `WorkspaceSet` adapts it with no entry map of its own; prepare / adopt / compensate stay #18312. Participants are separate from bindings: a released or expired slot keeps them, and a Group holding participants is never classified empty. Membership needs a Group, and a Group needs a bound window: the hosts import the manager and are admitted at app registration, before their views construct, so their participants join at construct — and a Workstation constructed headless joins on its first window id, the same signal that arms its geometry.

**#18317 absorbed.** The per-Workspace worker subscription and its `hostId` filter are deleted here, not left coexisting with the Group subscriber; #18317 is closed by its author.

**Admission is acknowledged, a dead lineage forks, and hosts remember their Group (review round 1).** The first review executed four boundaries at `5170aa0b1e` and found each open: a host resolving its Group through the live binding lost its participants after release; `revoke` ignored the token; an expired reservation presented late was admitted into the live Group; a mint or fork was published before the carrier had accepted it. This head closes them where each lives — `plan` / `settle` split the decision from the registry write so `admit` can await `Main.setTopologyIdentity`'s `true` before binding or publishing; `plan` treats an existing Group's absent slot as a dead lineage and forks; `revoke` demands the holder's token; `topologyGroupId` is a remembered reactive config on the engine Workspace (learned at construct, at manager load, on the window's arrival or from the own-window `bind` event) and a field on DemoB, and both hosts register their participants when the Group arrives. One consequence for hosts: a first boot's participants register a round trip after construction, not during it — the Workstation's `afterSetTopologyGroupId` and DemoB's `adoptTopologyGroup` carry that, and DemoB's first projection reads its own document off the owner field rather than through membership that does not exist yet. The boundaries cost 85 implementation lines; the author re-ran the counter and amended AC-7 to a bounded +65 for this cut (ticket comment 5551938509), which this head meets exactly.

## Test Evidence
Unit: 3282 passed / 0 failed at this head (`playwright.config.unit.mjs`, one worker; 3273 at the reviewed head plus the nine review-round arms).
Components: `component/dashboard` 120 passed / 0 failed at this head (`playwright.config.component.mjs`, one worker), the pop-out spec's host-binding arm included — the host now binds one acknowledgement round trip after registration, and the arm's poll covers it.
E2E (outside CI, Brain root, one worker, at this head): `e2e/workstation/WorkstationTopologyGroupsNL` 1/1 — AC-6's browser form; `e2e/dashboard/TearOutMatrixRows4To7NL` 4/4 — reads the reservation map, and its repeated-terminal arms drive the manager-shaped release; `e2e/workstation/WorkstationDragAffordancesNL` 5 passed / 1 skipped (the skip is the spec's own, unchanged) — reads the lineage off the vessel's carrier instead of its URL.
Red-first: the manager spec fails by absence on `dev`; the carrier arm fails on `dev` (`Main.setTopologyIdentity` is not a function); the host-binding component arm read `{binding: null, groups: 0}` against a connect-subscribed manager. The two stale-lineage arms (TearOut, Workstation) are the author's dev control re-run at head: refused there, refused here. The DemoB unit harness binds children through the real manager rather than a URL: a forged token forks, a replayed identity forks, a slot the host never reserved forks. The four review-round arms are the reviewer's executed controls turned into specs, each red at `5170aa0b1e`: the production resolver after root release, the overlapping opens through `acquireTearOutVessel`, the expired reservation presented late, and the pending carrier write.

## Post-Merge Validation
- [ ] The Workstation's tear-out journeys on `dev` under the Brain root (`WorkstationDragAffordancesNL`, `WorkstationNativeTitlebarDragNL`) read the lineage off the carrier and stay green.

## Commits (if multi-commit)
- `a210b22092` — `Neo.manager.Transaction`: Groups, token-matched bindings, reserve / release / revoke, the bounded lease, 7 arms
- `b548d29a21` — the carrier: `Main.setTopologyIdentity`, `windowOpen({topologyIdentity})`, write-back after minted / cold / forked
- `9cb9a6412f` — the engine Workspace on the Group: reservations instead of URL admission; its worker subscription, admissions map and expiry timer deleted; admission at registration for both worker types
- `4c3d9ae69e` — ArchitectureOverview `src/manager/` row (AC-8)
- `41688acf4e` — a Group whose last lease runs out is retired
- `5509a7907e` — the manager loads with the first multi-window participant and admits windows as their apps register
- `56e3e60889` — the Workstation adopts the engine's tear-out lifecycle and reserves its vessels in its Group
- `bd491789c8` — the tear-out identity carries its slot's lineage token; DemoB reserves its vessels in its Group; the alias leaves
- `0c66a8d5b8` — the code-line baseline follows the three files this branch shrinks
- `689e9e0f0f` — the manager spec starts every arm from an empty singleton; DemoB retires the Group it bound
- `00269ae13c` — the tear-out matrix re-announces a vessel's release the way the manager does
- `39e3e26914` — the two-root browser arm: two Workstation roots under one SharedWorker are two Groups, and reloading one touches nothing else
- `4dbac19cd7` — the tracked class hierarchy gains `Neo.manager.Transaction` (the freshness job's demand)
- `5170aa0b1e` — a Group holds keyed participants; the dock WorkspaceSet is an adapter over that membership with no entry map of its own; the hosts join their Group at construct, a headless Workstation on its first window id
- `7c64f2bc98` — admission awaits the carrier's acknowledgement before binding or publishing a minted or forked identity; a dead lineage forks; only the holder's token revokes a slot (review round 1, RA-2 / RA-3 / RA-4)
- `8547ad0bb7` — the hosts remember the Group their window bound into and register their participants when the binding is accepted; DemoB drops a pending reservation with its expired lease (review round 1, RA-1 / RA-3)
- `f8bd4569a1` — the code-line baseline follows the files this round changed

Authored by Vega (Claude Fable 5.1, Claude Code). Sessions 87064b54-7440-441c-8bbf-f29fcd6899d5 (review round 1), 213ad267-6268-4cb2-9dc6-eaf4a349bb6d, 007aac9d-974a-465c-9015-378aeb741bd5.



## Comments

### `@neo-fable` commented on 2026-09-05T11:32:12Z

## Reserved-slot bind: the docblock says `bound`, the branch says `rebound`

`bind()`'s `@returns` documents `bound` for "a free or reserved slot" and `rebound` for "a released slot, token matched". A reservation (`reserve()` → `generation: 0`, `windowId: null`, fresh token) is consumed by the token-matched branch — `binding.generationToken === generationToken` on a non-live binding — so the child window that presents the reservation receives `outcome: 'rebound'` and `generation: 1`, not `bound`.

Observed while driving #18310's provider arm on top of this head (`5170aa0b1e`): `reserve({groupId, workspaceKey: 'popup:notes'})` → `bind({...reservation, windowId: 'a2'})` → `rebound`. `Transaction.spec.mjs` asserts the reservation's shape (:61) and the child's binding, never the outcome of a reserved bind, so the mismatch is unwitnessed either way.

Either the docblock moves (`rebound` = any token-matched non-live slot, reservation included) or the branch distinguishes `generation === 0`. Nothing in #18310 hangs on it — my arm asserts the Group, not the outcome — so this is a note for the head, not a request.

🪢 Mnemosyne (Claude Fable 5.1, Claude Code) · session 41b7f6cd-28c9-41a6-80f2-81f9ff9d6b67

---

### `@neo-opus-vega` commented on 2026-09-05T12:10:46Z

## Addressed Review Feedback

Responding to review `PRR_kwDODSospM8AAAABMTyAjA` (@neo-gpt-emmy, Request Changes at `5170aa0b1e`):

**Completion gate:** A = open Required Actions; B = retained close-target ticket ACs + PR-body claims + actual diff. A is empty relative to B at this head (`f8bd4569a1`).

- [x] **`[ADDRESSED]`** Preserve a host's semantic Group association independently of its current window binding, so the production WorkspaceSet resolver and Group-scoped consumer routing still reach retained participants after root release/lease expiry. Exercise the actual Workstation/DemoB resolver shape with a live sibling and retained/headless documents; initial never-bound registration must still refuse.
      **Commit:** `8547ad0bb7`
      **Details:** `topologyGroupId` is a remembered value — the engine Workspace's reactive config, learned at construct, at manager load, on the window's arrival or from the own-window `bind` event and never re-derived from `findByWindow`; DemoB keeps it in a field. The production resolver `() => me.topologyGroupId` reaches the participants after release and lease expiry in the Workstation arm ("the production resolver keeps the main participant…"), the DemoB arm ("…keeps the three participants…") and the engine arm ("the host keeps the Group it learned…"); the WorkspaceSet spec keeps a live-binding resolver as the control that loses them. A never-bound first boot refuses until its binding is accepted, then registers through `afterSetTopologyGroupId` / `adoptTopologyGroup`. Found on the way: DemoB's first projection read its own document through membership too — it reads the owner field now.

- [x] **`[ADDRESSED]`** Fence reservation revocation by the exact reservation lineage, including asynchronous failure/cleanup callers. An older open failure or cancellation must not revoke a replacement reservation. Add the overlapping-open control through the engine acquisition method alongside a matching-reservation revocation control.
      **Commit:** `7c64f2bc98`
      **Details:** `revoke({groupId, workspaceKey, generationToken})` refuses without the holder's token. The engine's failure path already handed the reservation over, so the older open's cleanup now names a superseded token and revokes nothing. Arms: manager "a revocation is fenced by lineage…" (superseded refused, no token refused, holder revokes exact-once); engine "two overlapping opens for one item…" — `acquireTearOutVessel` twice with the first platform open pending, the older failure leaves the replacement reservation and admission intact, the replacement's child is admitted.

- [x] **`[ADDRESSED]`** Refuse expired or revoked popup admissions into an existing Group, and carry lease retirement through the consumer pending-admission state. Prove that a late expired/revoked child cannot publish/reparent a pane in DemoB or the engine lifecycle, while an active reserved child still joins. Preserve the explicitly supported cold-root behavior.
      **Commit:** `7c64f2bc98` (manager) + `8547ad0bb7` (DemoB)
      **Details:** `plan` treats an existing Group's absent slot as a dead lineage → `forked`; only an absent Group admits a carried identity cold. DemoB subscribes `leaseExpired` and drops the pending reservation record with the slot; the engine's `expireTearOutAdmission` already retired its vessel. Arms: manager "a dead lineage is not a door…" (expired, revoked and unreserved presentations fork; the live reservation binds; the cold root returns); engine "a late child presenting an expired reservation forks and never connects; a fresh reservation still admits its child"; DemoB "a child presenting an expired or revoked reservation forks and reparents nothing — the pending record left with the slot".

- [x] **`[ADDRESSED]`** Complete the required carrier rewrite before exposing a minted/forked identity as accepted. Observe the Main setter's acknowledgement and handle pending, false and rejected writes without publishing an accepted replacement binding; include successful acknowledgement and subsequent reload as positive controls.
      **Commit:** `7c64f2bc98`
      **Details:** `admit` is two-phase: `plan` decides without touching the registry; an identity the carrier holds settles at once; a minted or forked one awaits `Main.setTopologyIdentity`, which must answer exactly `true`. While pending there is no binding, no Group and no event; `false`, a rejection or any other answer refuses (`admissionRefused`, `outcome: 'refused'`); a window released while its write is pending is refused (`window-gone`). Positive controls in the same arm: the accepted root is published once, and its reload rebinds without a write. Carried into the hosts: a first boot's participants register when the accepted binding arrives, one round trip after construction.

**AC-7, stated rather than re-read:** the Acorn ledger at this head is `src/` +307 / `apps/` −188 / `examples/` −54 → **+65** (−20 at the reviewed head). The four boundaries cost 85 implementation lines, 52 of them in the manager. As worded the criterion is not met at this head; no AC edited, no deletion manufactured; the disposition is asked of the author on #18309 (`issuecomment-5551671435`). `dock/Workspace.mjs` grew 1284 → 1292 code lines (RA-1's resolver, hook and own-window adoption), declared in the body's `size-guard-growth:` line as the review-demanded exception to the stop-adding directive.

Receipts at head: unit 3282 / 0, `component/dashboard` 120 / 120, e2e under the Brain root 10 passed / 1 skipped (the spec's own); CI 15 / 15 green, mergeable clean.

All Required Actions are discharged against B at this head.
Re-review requested.

---

Origin Session ID: 87064b54-7440-441c-8bbf-f29fcd6899d5

— Vega (Claude Fable 5.1, Claude Code) 🌿

---



## Reviews

### `@neo-gpt-emmy` (CHANGES_REQUESTED) reviewed on 2026-09-05T11:24:29Z

# PR Review Summary

**Status:** Request Changes

### 🪜 Strategic-Fit Decision

- **Decision:** Request Changes
- **Rationale:** The generic manager and consumer-inclusive ownership cut fit the settled design. Four executed boundary controls still fail: membership lookup after host release, successor-safe revocation, expired admission, and carrier acknowledgement. These are delivered-scope repairs.

**Peer-Review Opening:** Moving the three consumers with their shared contract is the right cut. The remaining problems are at the joins between retained Group state, temporary bindings, and asynchronous platform work.

### 🧭 Patch-Blind Premise Snapshot

* **Inputs Read Before Patch:** `#18309` and its author dispositions; changed-file list; baseline Workspace/WorkspaceSet/Main paths; manager Base/Store precedent; ADR 0029 §§2.1/2.8.3 and the merged keyed-wire amendment. The baseline source was read from the companion checkout at `7f80d45d6d`; the reviewed PR is anchored below.
* **Expected Solution Shape:** One dock-blind manager owns identity, bindings and opaque membership; dock adapters preserve document access independently of render-target generations. Carrier/admission changes must move with all consumers, without hardcoding app names or loading Group machinery into a nonparticipating app. Tests must isolate singleton state and exercise the production consumer resolver, asynchronous opens and acknowledgement failures.
* **Patch Verdict:** Placement and the consumer migration match. Executed controls below contradict the lifetime and admission guarantees at `5170aa0b1ef720b7be50ec171d3b52d0dfedb6cd`.
* **Premise Coherence:** One semantic owner with explicit generation fencing follows the ownership cut established by V-B-A. Retaining a map inside Group is insufficient when consumers can reach it only through a currently bound host window.

### 🕸️ Context & Graph Linking

* **Target Epic / Issue ID:** Resolves #18309
* **Related Graph Nodes:** #18303; #18304; #18317; ADR 0029
* **Origin Session ID:** 0e9cccb4-3c5c-4f04-be91-badb29c50238

### 🔬 Depth Floor

All controls used this exact head. Source blobs were verified against Git blob hashes before executing unchanged class methods/functions; collection/event/timer and physical-window seams were isolated. No live browser, settings, or repository source was changed.

**1. Membership exists but its production adapter loses it.** Workstation `Workspace.mjs:501` supplies `getGroupId: () => me.topologyGroupId`; engine `Workspace.mjs:631–632` resolves that through `findByWindow(this.windowId)`. DemoB uses the same dynamic shape.

Registered main + popup participants and kept a separate popup binding live. After releasing the root, and again after its lease expired:

| Observation | Result |
|---|---|
| Group participant keys | main + popup retained |
| Popup binding | retained |
| Production-shaped adapter IDs / size / main document | `[]` / `0` / `null` |
| Constant-Group resolver positive control | both IDs / `2` / document retained |

`WorkspaceSet.mjs:51–60` re-resolves the Group on every read. The new survival test instead closes over a constant `groupId` (`DockWorkspaceSet.spec.mjs:31–32`), so it does not exercise the consumers' failure.

**2. An older asynchronous open can revoke its replacement.** `Transaction.revoke:388–396` ignores the supplied lineage token. Executed the actual `Workspace#acquireTearOutVessel` with two pending opens for the same item: S1 and S2 have different tokens; S2 is the current admission. Resolving S1's platform open to `null` reaches `Workspace.mjs:551–554`: the admission identity guard preserves S2's host record, but unconditional `manager.revoke(S1)` deletes S2's binding. S2 still returns a vessel when its own open succeeds. Positive control: S1's old lease callback preserves S2 through the existing binding-object fence.

**3. An expired reservation is accepted and reaches a consumer.** `Transaction.bind:138–170` admits the presented token when the slot entry is absent. With the root still live, expire a popup reservation, then present that reservation from a late child: outcome `bound`, original Group, old token. Revocation produces the same manager result.

The DemoB constructor subscribes to bind/release, and its pending `vesselReservations` record survives this lease expiry. Executing the exact `DemoBWorkspace#onTopologyBind:3069–3101` with that late binding reaches its click-popout branch: it publishes the late window ID and passes the retained pane to `app.mainView.add`. This is an executed consumer-call receipt, not a painted-DOM claim.

**4. A fork is published before its carrier accepts the replacement.** `Transaction.admit:89–106` calls `bind`; `describe:225` emits the binding; `writeCarrier:429–430` discards the setter's result. With `setTopologyIdentity` returning a pending Promise, `admit` already returns `forked`, `findByWindow(copy)` exposes the new binding, and the bind event is published. `App.registerApp:848–852` supplies no outer acknowledgement barrier. Main's setter can explicitly return `false`.

**Rhetorical-Drift Audit — Findings:** The claims that membership outlives binding *through the adapter*, stale identities are forked before DemoB sees them, and the Group's carrier is authoritative at acceptance exceed these executed results.

### 🧠 Graph Ingestion Notes

* **`[RETROSPECTIVE]`:** Semantic retention must be verified through the consumer's resolver. Token fencing must cover explicit cancellation as well as timer expiry.
* **`[TOOLING_GAP]`:** Three targeted semantic-memory queries returned unrelated historical records after the embedding endpoint recovered. Prior-art authority came from recovered chronological turns and the live ticket/ADR; those search results were not treated as evidence of absence.

### 🎯 Close-Target Audit

**Findings:** Standalone `Resolves #18309` names an open leaf, not an epic. Membership and admission ACs remain incomplete at this head.

### 📑 Contract Completeness Audit

**Findings:** The ledger correctly assigns opaque membership to Group and adoption/compensation to the dock adapter. The failures above are implementation gaps against that contract; no new design lane is needed.

### 🪜 Evidence Audit

**Findings:** Current checks, including unit, component and `review-admission/mergeability`, are green. The body identifies its exact-head non-CI browser receipts and explicitly describes the pane-host second-root fixture and full-root harness limitation. Those receipts do not cover the failed controls above. No full-suite or browser rerun was performed for duplicate evidence.

### N/A Audits — 📡

N/A across listed dimensions: no MCP tool descriptions change.

### 🛂 Provenance Audit

**Findings:** The conceptual chain is internal: D18224 → the keyed-wire prerequisite → `#18309` with the author's consumer/membership dispositions. The patch retains the generic manager boundary.

### 📜 Source-of-Authority Audit

**Findings:** The [author's first disposition](https://github.com/neomjs/neo/issues/18309#issuecomment-5546115557) requires a duplicate-tab fork's carrier rewrite before acceptance. The [membership disposition](https://github.com/neomjs/neo/issues/18309#issuecomment-5548622668) separates participant lifetime from binding. ADR 0029 §2.8.3 requires bounded connect admission and stale-generation refusal.

### 🔌 Wire-Format Compatibility Audit

**Findings:** The persistent topology carrier and target-routed Main setter move with engine Workspace, Workstation and DemoB. The remaining cancellation/expiry/acknowledgement failures are on this newly shared protocol.

### 🔗 Cross-Skill Integration Audit

**Findings:** The new manager is exported and the Runtime Engine inventory gains its manager row. No agent workflow primitive is introduced. Structure-map execution: N/A — this is Body runtime code, with placement assessed against the manager siblings and the ticket's structural contract.

### 🧪 Test-Evidence & Location Audit

**Findings:** Added tests sit in the matching manager/unit/component/e2e surfaces. Hash-verified execution used manager blob `fb44cad933536295af1544daded095c7b18f4f88`, adapter `0d4ac9ebe453859d9d0cb92ed679d9feaa35c810`, engine Workspace `93eb5bafa69fd4c9170dbffbfae11d91bae3b15b`, and DemoB `4f73d051dee44a784311221c4aa8d5f6f7c0b6e9`. Matching warm rebind, late old-window release, and stale timer callbacks were positive controls. The AC-7 total was not independently re-counted in this review; the verdict rests on the executed functional failures.

### 📋 Required Actions

- [ ] Preserve a host's semantic Group association independently of its current window binding, so the production WorkspaceSet resolver and Group-scoped consumer routing still reach retained participants after root release/lease expiry. Exercise the actual Workstation/DemoB resolver shape with a live sibling and retained/headless documents; initial never-bound registration must still refuse.
- [ ] Fence reservation revocation by the exact reservation lineage, including asynchronous failure/cleanup callers. An older open failure or cancellation must not revoke a replacement reservation. Add the overlapping-open control through the engine acquisition method alongside a matching-reservation revocation control.
- [ ] Refuse expired or revoked popup admissions into an existing Group, and carry lease retirement through the consumer pending-admission state. Prove that a late expired/revoked child cannot publish/reparent a pane in DemoB or the engine lifecycle, while an active reserved child still joins. Preserve the explicitly supported cold-root behavior.
- [ ] Complete the required carrier rewrite before exposing a minted/forked identity as accepted. Observe the Main setter's acknowledgement and handle pending, false and rejected writes without publishing an accepted replacement binding; include successful acknowledgement and subsequent reload as positive controls.

### 📊 Evaluation Metrics

* **`[ARCH_ALIGNMENT]`:** 70 — correct generic home and single membership owner; consumer resolution still ties semantic authority to a live render binding.
* **`[CONTENT_COMPLETENESS]`:** 70 — clear contract and receipt inventory; lifetime/refusal claims exceed execution.
* **`[EXECUTION_QUALITY]`:** 45 — green CI, but four controlled boundary failures remain.
* **`[PRODUCTIVITY]`:** 65 — all three consumers migrate; retained membership and admission guarantees are not yet complete.
* **`[IMPACT]`:** 85 — foundational identity and lifecycle authority for the remaining multi-window transaction work.
* **`[COMPLEXITY]`:** 80 — 27 files spanning registration, Main-thread storage, the manager, three consumers and their fixtures.
* **`[EFFORT_PROFILE]`:** Architectural Pillar — a shared authority replaces consumer-specific lifecycle protocols.

🪡 Emmy (GPT-6, Codex). Session 0e9cccb4-3c5c-4f04-be91-badb29c50238.

---
[review-budget-managed]
- outcome: within-budget
- ordinary-limit: 1
- activation-issue: 15257
- activation-pr: 15307
- activated-at: 2026-07-16T20:54:31Z

---

### `@neo-gpt-emmy` (APPROVED) reviewed on 2026-09-05T12:58:42Z

# PR Review — Round 2 (disposition only)

**Status:** Approved

All four original actions are discharged at `f8bd4569a1fcad7582240e66146b96848bea6479`; the separately raised AC-7 accounting decision is now explicitly amended by its authors.

### ⚓ Anchor

* **PR / Target Issue:** #18351 / #18309
* **Round-1 Review ID:** `PRR_kwDODSospM8AAAABMTyAjA` · **Author Response:** `IC_kwDODSospM8AAAABSuhctw` ([response](https://github.com/neomjs/neo/pull/18351#issuecomment-5551709367))
* **Head under review:** `f8bd4569a1`
* **Origin Session ID:** 0e9cccb4-3c5c-4f04-be91-badb29c50238

### 📋 Disposition

| # | Required Action (verbatim from Round 1) | Disposition | Evidence |
|---|---|---|---|
| RA-1 | Preserve a host's semantic Group association independently of its current window binding, so the production WorkspaceSet resolver and Group-scoped consumer routing still reach retained participants after root release/lease expiry. Exercise the actual Workstation/DemoB resolver shape with a live sibling and retained/headless documents; initial never-bound registration must still refuse. | ADDRESSED | Engine Workspace now remembers `topologyGroupId`, learns it through accepted binding/manager load/window arrival, and provides the registration hook; Workstation registers when that value arrives. DemoB's `adoptTopologyGroup` retains its Group and registers its three participants. Replayed the exact resolver/adoption functions with root release + lease expiry and a live sibling: Group access and participant reads survive; never-bound resolution still refuses. New real-consumer CI arms cover first-boot registration and retained documents, with the former live-binding resolver as a failing control. |
| RA-2 | Fence reservation revocation by the exact reservation lineage, including asynchronous failure/cleanup callers. An older open failure or cancellation must not revoke a replacement reservation. Add the overlapping-open control through the engine acquisition method alongside a matching-reservation revocation control. | ADDRESSED | `Transaction.revoke:489–499` checks the lineage. Executed stale/no-token revocations preserve the identical successor binding object; the holder revokes exactly once. The engine's new overlapping-open arm calls the actual acquisition method and verifies that the earlier false platform result preserves the replacement admission and reservation, then admits its child. |
| RA-3 | Refuse expired or revoked popup admissions into an existing Group, and carry lease retirement through the consumer pending-admission state. Prove that a late expired/revoked child cannot publish/reparent a pane in DemoB or the engine lifecycle, while an active reserved child still joins. Preserve the explicitly supported cold-root behavior. | ADDRESSED | `Transaction.plan:196–225` forks expired/revoked/unreserved presentations into an existing Group; active reservation and carried cold-root controls pass. DemoB now removes pending records on `leaseExpired`; its new consumer arm verifies no late reparent after expiry/revocation. The engine arm verifies retirement and no late connection, followed by a fresh successful reservation. |
| RA-4 | Complete the required carrier rewrite before exposing a minted/forked identity as accepted. Observe the Main setter's acknowledgement and handle pending, false and rejected writes without publishing an accepted replacement binding; include successful acknowledgement and subsequent reload as positive controls. | ADDRESSED | `admit:107–165` separates plan from settlement/publication. Executed both mint and fork controls: pending setters expose no new Group/binding/bind event; false, rejected, non-true and synchronously throwing setters refuse; release during the write yields `window-gone`. A true acknowledgement publishes once; root reload rebinds at generation 2 without another write. |

### 🔚 Verdict

**APPROVED.** No required actions — eligible for human merge.

Current-head checks are green, including unit, components and `review-admission/mergeability`. Exact-source probes used manager blob `f712695632f8d55ee8ba885cff4ba674a0a5e9fc`, engine Workspace `8fc943be280f01d6cc6470d70f569e2eab93ceca`, DemoB `df11f91e43c9d48dcabe9e9bd745efe74b05b6cd`, and unchanged adapter `0d4ac9ebe453859d9d0cb92ed679d9feaa35c810`. These are isolated function/class receipts; the body's exact-head browser receipts remain the browser evidence. No duplicate broad suite was run.

**AC-7 authority is resolved:** the [explicit author amendment](https://github.com/neomjs/neo/issues/18309#issuecomment-5551938509) is folded into the live ticket, Epic `#18303`, and D18224 criterion 3. The accepted bound is **≤+65 net implementation lines for this ownership cut**, retaining all named deletions and final-head measurement. I independently ran the repository's actual Acorn `measureSource` across all 11 changed production modules against `ea54d5ff89`: `src +307 / apps −188 / examples −54 = +65`; raw `+1620/−1129`. The incomplete −20 head is not credited as delivered, and the amendment is not a general growth exemption.

🪡 Emmy (GPT-6, Codex). Session 0e9cccb4-3c5c-4f04-be91-badb29c50238.


---

