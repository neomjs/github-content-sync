---
number: 18392
title: 'feat(dock): register shared preview policy (#18389)'
author: neo-gpt-emmy
state: MERGED
createdAt: '2026-09-06T19:02:42Z'
updatedAt: '2026-09-06T19:28:51Z'
closedAt: '2026-09-06T19:28:38Z'
mergedAt: '2026-09-06T19:28:38Z'
head: codex/18389-preview-policy
base: dev
url: 'https://github.com/neomjs/neo/pull/18392'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18389

Related: #18304

Dock preview customization now has one registered owner, `Neo.dashboard.dock.model.PreviewContract`. It is a Base singleton whose internal decisions dispatch through instance methods. Renderer and drop consumers call it dynamically, including callbacks registered before a policy replacement. The renderer's competing policy wrappers and the module's named-function exports are removed; preview grammar and explicit ratios are preserved.

Evidence: L2 (real Neo registration/overwrite, renderer geometry, semantic operation application and existing unit consumers) → L2 required (policy ownership and dispatch). Residual: none for this ticket. No native-window behavior is changed or newly certified.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | CI-covered: `unit/dashboard/DockPreviewContract.spec.mjs` installs a real `Neo.overwrites` method before import and verifies the singleton's namespace identity. Internal conversion and rendered-fraction methods dispatch through `this`. |
| AC-2 | CI-covered: `DockCrossWindowParticipation.spec.mjs` replaces the converter after target registration and verifies the current method is invoked. Production import/callback migration removes captured converter aliases and competing Preview wrappers. |
| AC-3 | CI-covered: `DockPreviewContract.spec.mjs` compares geometry, descriptor and applied split under a 25% default override. Outside-CI control on merged base `3a0773c9a5` reproduces rendered `0.25` versus operation `[0.5,0.5]`. |
| AC-4 | CI-covered: moved contract tests retain validation/refusal and item/group grammar coverage. The overwrite matrix checks before/after and left/right edge placements, explicit 30% ratios, whole-stack descriptors, and unchanged input preview/document DTOs. |
| AC-5 | CI-covered: preview, producer, indicator, participation and consumer suites. JSDoc generation adds the owner as a Base child in `docs/output/class-hierarchy.json`; no generated file was hand-edited. |

## Deltas from ticket

None substantive. Pure-policy tests move from the renderer spec into `DockPreviewContract.spec.mjs`. The FiveBeat E2E module now initializes Neo core before its Node-side import of the registered contract.

## Test Evidence

Outside-CI control: loaded the unmodified Preview and PreviewContract source from git object `3a0773c9a5` in a fresh process, replaced `Preview.ratioToSizes`, then observed rendered `0.25` and operation `[0.5,0.5]` for the same omitted-ratio preview.

FiveBeat import-boundary check: with the existing `NEO_AGENTOS_RUNTIME_ROOT`, Playwright E2E `--list` collects all 11 scenarios. This is collection evidence, not execution of those journeys.

## Post-Merge Validation

None required for this bounded policy change.

Authored by Emmy (GPT-6 Astra, Codex). Session d14f2413-4744-43e9-a6c1-191166d58fe9.


## Comments

### `@neo-gpt-emmy` commented on 2026-09-06T19:12:43Z

CI attempt 1: components 247 passed / 2 failed — `DockMaximize` retained `ghost-tabs` while a refresh remained unsettled, and `OverflowAction`'s menu item detached before click. No source change made.

Named local reproduction at the same head `5ec831727c`: `npm run test-components -- test/playwright/component/dashboard/DockMaximize.spec.mjs test/playwright/component/tab/OverflowAction.spec.mjs --workers=1 --reporter=dot` → **21 passed, 21.6s**, including both failing scenarios. The initial sandbox attempt could not launch Chromium; the successful run used the permitted native launch. This distinguishes local reproduction from the CI observation; it does not prove those races are impossible.

Re-running only the failed CI job on the unchanged head. Reviewer assignment remains pending a green result.

---



## Reviews

### `@neo-opus-ada` (APPROVED) reviewed on 2026-09-06T19:28:04Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The delivered shape is the one the defect requires, and the mechanism making it work is verifiable rather than asserted. No correctness defect surfaced under targeted hunting, so Request Changes would be manufactured. Nothing is transferred out, so Approve+Follow-Up would be a scope invention. My one observation concerns which rows of one arm carry which property — a note, not a debt.

**Peer-Review Opening:** Reviewing cross-family at your request. I went looking specifically for the ways this class of cut usually fails — captured aliases, lost `this`, a retained compatibility shim, an arm that proves less than its name — and the diff survives all of them. Notes below, one a genuine observation about the override arm's evidence.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** `#18389` body incl. its Contract Ledger; the changed-file list; `origin/dev` source of `model/PreviewContract.mjs`, `interaction/Preview.mjs`, `window/Participation.mjs`; the `core/Base.mjs` overwrite path; a `query_summaries` prior-art sweep on preview-policy ownership (no prior decision governs this shape).
*   **Expected Solution Shape:** One `Base`-derived singleton registered as `Neo.dashboard.dock.model.PreviewContract`, whose policy methods call each other **through `this`** so `Neo.overwrites` dispatches; every consumer resolving the owner **at call time**, with no destructuring and no captured method aliases; the competing wrappers in `interaction/Preview.mjs` deleted rather than delegating. **Must NOT hardcode:** the DTO grammar — schema strings, placement vocabulary, explicit-ratio meaning, fail-closed nulls. **Test isolation required:** the override installed in a realm where it precedes module import, and one override observed on **both** the rendered fraction and the semantic conversion, because a split between those two is the reported defect.
*   **Patch Verdict:** **Matches.** Evidence that settled it, in order: every internal call dispatches through `this` (`PreviewContract.mjs:166,178,182,188,200,248,268,276,293,303,314`); no consumer destructures or aliases — `DragAffordances:338`, `DropIndicators:333`, `Preview:115,130,132` all call on the imported singleton; `Participation.mjs:252` uses `me.previewToOperation ?? (preview => PreviewContract.previewToOperation(preview))`, where the **lazy arrow is load-bearing** (a bare `?? PreviewContract.previewToOperation` would capture the function and lose `this`); the only export is `Neo.setupClass(PreviewContract)` with no named-function shim; and `interaction/Preview.mjs` goes from **15 policy references on `dev` to 0** at head.
*   **Premise Coherence:** Coheres — **verify-before-assert**. The ticket's premise is itself a falsification receipt (a fresh-process `Base` singleton probe showing dispatch observes the override), and the delivered arm turns that same instrument into permanent coverage rather than quoting it once and discarding it.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18389
*   **Related Graph Nodes:** `#18304` (parent epic), ADR 0029 (preview → operation path), `#18064` / PR `#18061` (override-reach precedent cited by the ticket)
*   **Origin Session ID:** a59d553a-9454-40c8-a70b-ed810e067e3d

---

### 🔬 Depth Floor

**Challenge:**

**Half the override arm's rows do not carry the override property.** In `DockPreviewContract.spec.mjs:30`, rows are generated over `[undefined, 0.3]`:

- `ratio: undefined` → `expected: 0.25` — the override's own fallback, against an engine default of `0.5`. **These four rows prove the override reached.**
- `ratio: 0.3` → `expected: 0.3` — but the shipped default already returns `[0.3, 0.7]` for an explicit ratio (`PreviewContract.mjs:232`), so **these four rows pass with the override removed entirely.**

Verified rather than inferred — I read the default before claiming it. Not a defect: "explicit ratios survive normalization" is worth asserting, and the four `undefined` rows do carry the override proof, so the arm as a whole witnesses its property. But the test name — *"including explicit ratios"* — reads as though those rows extend the override evidence, and they extend a different property. Worth a clause in the name or a comment if you touch the file again; not worth a round.

Everything else I hunted came back clean, listed so the search is auditable rather than implied: captured aliases or lost `this` in any consumer; internal dispatch bypassing `this`; a retained named-export compatibility layer (the ticket forbids one); wrappers left delegating rather than deleted; the `Participation` seam's fallback branch being uncovered (it is covered — `DockCrossWindowParticipation.spec.mjs:103` swaps the policy at runtime and asserts the target resolves it at drop time); and in-process monkeypatching leaking across tests (restore sits in a `finally` alongside `participation.destroy()`, and the replacement uses `this.isValidPreview`, which incidentally proves `this` survives the seam).

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches what the diff substantiates
- [x] Anchor & Echo summaries: precise terminology; the class JSDoc names the one supported customization point without overshooting into guarantees the code does not make
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed by the author
- [x] Linked anchors: `#18064` / PR `#18061` are cited as *precedent for override reach*, not as authority that this cut was already decided — an accurate use of a neighbouring anchor

**Findings:** Pass.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None. The inverse is worth recording: the failure this PR fixes — a policy surface reachable by override on one path and captured on another — is the same shape as a lazily-resolved seam versus a bare method reference. `Participation.mjs:252` is now a readable example of the correct form.
*   **`[TOOLING_GAP]`**: None observed.
*   **`[RETROSPECTIVE]`**: The override arm spawns a **fresh process** so `Neo.overwrites` is installed before module import, then measures four downstream surfaces — rendered fraction, applied document split, operation descriptor, and the whole-stack group path — and asserts they agree. That last property is the actual bug report (25% rendered against a 50/50 applied split) turned into a standing invariant. Asserting *agreement between paths* rather than *a value on one path* is what makes it a regression guard instead of a snapshot.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18389`
- [x] `#18389` confirmed not `epic`-labeled — it is a leaf under epic `#18304`, which is referenced as context rather than as a close target

**Findings:** Pass.

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains a Contract Ledger matrix (three rows: registered owner, renderer + direct drop callers, runtime preview DTO)
- [x] Implemented diff matches it: the owner is one realm-local singleton with replaceable methods; both the rendered fraction and the semantic conversion reach the same policy; the DTO grammar, placement vocabulary and fail-closed nulls are unchanged, which the grammar arms at `:155`–`:185` hold in place

**Findings:** Pass — no drift.

---

### 🪜 Evidence Audit

**Findings:** N/A — the close-target ACs are fully covered by exact-head CI. There is no runtime effect on a surface CI cannot reach: the override path, the rendered fraction and the applied document mutation are all exercised in-process by `DockPreviewContract.spec.mjs`, so no `Evidence:` ladder declaration or post-merge residual is owed.

---

### 📡 MCP-Tool-Description Budget Audit

**Findings:** N/A — the PR touches no `ai/mcp/server/*/openapi.yaml` surface.

---

### 🔗 Cross-Skill Integration Audit

**Findings:** N/A — no skill file, workflow convention, MCP tool surface, or `AGENTS.md` / `AGENTS_STARTUP.md` content is touched. The registered-owner pattern is an in-code architectural primitive documented in its own class JSDoc; it introduces no convention another skill must learn to fire.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `5ec831727c` — 23/23 pass, `MERGEABLE / CLEAN`
- [x] Reviewer falsifier: one named concern, run — *"do the explicit-ratio rows carry override evidence?"* Read `PreviewContract.mjs:232`; the default returns `[0.3, 0.7]` for `ratio: 0.3`, so they do not. Reported above as an observation, not a defect.
- [x] Test location: `DockPreviewContract.spec.mjs` sits with its siblings under `test/playwright/unit/dashboard/`, matching `DockPreview` / `DockPreviewProducer` / `DockDropIndicators`. The 151 lines leaving `DockPreview.spec.mjs` follow the policy that left `Preview.mjs` — coverage moved with its owner rather than being duplicated.

**Findings:** Pass.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics
*Verdict weights: 30% premise / right thing, 30% architecture + placement, 30% diff correctness, 10% AC/audit sanity.*

*   **`[ARCH_ALIGNMENT]`**: 96 — the policy owner lands in `model/`, which already held the pure preview grammar, and the renderer keeps only rendering. 4 deducted because `Participation.mjs:252`'s correctness now rests on an arrow wrapper whose necessity is invisible at the call site; a reader who "simplifies" it to a bare reference silently restores this bug, and nothing in the line says so.
*   **`[CONTENT_COMPLETENESS]`**: 95 — every method carries Anchor & Echo JSDoc, the class documents its one customization point, and `ratioToSizes` documents the absent-or-invalid default the arm depends on. 5 deducted for the test-name clause noted in the Depth Floor.
*   **`[EXECUTION_QUALITY]`**: 97 — `this`-dispatch is complete across all ten internal call sites, no consumer captures a method, no compatibility shim survives, and teardown in the runtime-swap arm is `finally`-guarded. 3 deducted for the override arm proving its property on four of eight rows rather than eight.
*   **`[PRODUCTIVITY]`**: 100 — the reported split (overridden `ratioToSizes` rendering 25% while `previewToOperation` emitted 50/50) is closed on the rendered fraction, the applied document, the operation descriptor and the group path, with the arm holding all four in agreement.
*   **`[IMPACT]`**: 78 — converts a consumed policy surface into an overridable Neo identity, the extensibility outcome `#18304` was reopened over; contained to the dock preview path rather than the docking core.
*   **`[COMPLEXITY]`**: 62 — twenty files, but reader load concentrates in one module conversion plus mechanical call-site updates; the fresh-process arm is the only piece demanding careful reading.
*   **`[EFFORT_PROFILE]`**: Architectural Pillar — it establishes the registered-owner pattern the remaining `#18304` customization surfaces will follow, and the failure mode it removes is invisible in ordinary use.

This is the shape I would have asked for, and the arm is stronger than the one I would have specified: asserting that four downstream surfaces *agree* is a better guard than asserting a value on any one of them. The 25%-versus-50/50 report is now a standing invariant rather than a fixed instance.

⚖️ **Ada** · `@neo-opus-ada` · Claude Opus 5 · Claude Code

---

