---
number: 18417
title: 'fix(dock): prevent duplicate pane DOM after popup adoption (#18409)'
author: neo-gpt
state: MERGED
createdAt: '2026-09-06T23:51:32Z'
updatedAt: '2026-09-07T00:19:59Z'
closedAt: '2026-09-07T00:19:56Z'
mergedAt: '2026-09-07T00:19:56Z'
head: codex/18409-popup-projection
base: dev
url: 'https://github.com/neomjs/neo/pull/18417'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18409
Related: #18303
Related: #18304

After a pointer tear-out creates destination-window DOM from the retained component/VDOM, committing the pane into PopupWorkspace no longer leaves a duplicate DOM copy in that popup's provisional viewport. The regression's DOM-node identity assertion covers this subsequent move within the popup; it does not claim that one DOM node survives the cross-window move. The reconciler collects the common ancestors of panes imported from outside its tab tree and awaits those moves before descendant commits. This prevents a destination `insertNode` from creating a second copy while the provisional viewport still owns the original DOM node. The change covers initial and retained-topology projection; no Workstation production code is added.

Evidence: L3 (actual-pointer headed Chrome, exact DOM-node identity and census, worker parent and native owner) → L3 required (AC-1/2). No residual for this leaf; the remaining popup-overlap journey stays on #18303.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Outside-CI: the new focused `WorkstationHumanPopupOverlapNL` test uses a real Metrics pointer tear-out and release, then requires one pane DOM node in its exact card body, usable geometry, unchanged live element/component identity and matching worker/native ownership. |
| AC-2 | CI-covered: deferred ancestor settlement blocks descendant publication for both initial and retained-topology projection. Both guards fail on the original source. Outside-CI: the final browser regression fails against unchanged dev and passes with the repair. |
| AC-3 | Outside-CI: normal native popup transfer, resize-before-hover, active-hover resize and popup-to-main retain their transfer/retirement and pane-identity assertions. |

## Deltas from ticket

The defect is duplicate DOM rendering: worker adoption was already correct. The repair belongs in the existing registered Reconciler, before its inner rendering commits. It adds no popup-specific repair callback, class or native-window authority. The original full overlap matrix is unchanged.

## Test Evidence

The final native command runs the new first-terminal test and the four existing native journeys:

```bash
NEO_AGENTOS_RUNTIME_ROOT=<tooling-checkout> npx playwright test -c test/playwright/playwright.config.e2e.mjs test/playwright/e2e/workstation/WorkstationHumanPopupOverlapNL.spec.mjs test/playwright/e2e/workstation/WorkstationNativePopupOverPopupNL.spec.mjs test/playwright/e2e/workstation/WorkstationNativeTitlebarDragNL.spec.mjs --grep-invert 'one local proxy plus readable target choices' --headed --workers=1
```

**5 passed, 27.0s**, on merged `d0f3b380` plus this change. The exact final pointer regression against unchanged `d0f3b380` fails in 12.6s: the DOM census contains both viewport and card-body copies while worker ownership is already correct. The repaired source was restored before the passing batch.

The diagnostic delta log on pre-fix source showed an `insertNode` into the card body with the existing pane id and no corresponding move/removal of its viewport copy. Diagnostic logging and corrective controls are absent from the permanent test.

The old full overlap matrix remains outside this narrow run: its pre-hover bare-popup observation still needs migration. This PR claims the first pointer terminal, not complete epic acceptance. Native titlebar journeys use on-screen CDP movement, not an actual OS titlebar grab.

## Post-Merge Validation

None for this leaf. Continue the remaining actual-pointer overlap and deployed-command acceptance on #18303.

Authored by Euclid (GPT-6, Codex). Session 01a07609-5e09-70f0-bf2c-a27d77d1b7f2.



## Comments

### `@neo-gpt` commented on 2026-09-07T00:06:34Z

The first component run at `d3b7f0c2` had one failure: `DockMaximize` left `maximizedNodeId = "ghost-tabs"`; its diagnostic said the refresh had not settled within 2 seconds. The same arm passes locally at this head (859ms; 2.5s total).

The **same ghost-id timeout predates this PR**, including [dev run 33996145753](https://github.com/neomjs/neo/actions/runs/33996145753/job/101386947613) at `1182a7cf11` and [run 33985218872](https://github.com/neomjs/neo/actions/runs/33985218872/job/101357380252) at `4be2d9306d`. Both expected null and received `ghost-tabs` in the same continuity arm. This establishes prior occurrence, not a complete diagnosis of the intermittent stall.

I have rerun only the failed job once. The PR source is unchanged; current-head CI must finish before formal approval.


---



## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-07T00:17:31Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The diagnosis names a transaction boundary rather than a symptom, the fix is 33 lines in the layer that owns projection ordering, and it is **inert on ordinary projections** by construction. The unit control has a negative phase that fails on the pre-fix source. Nothing survived source reading.

**Peer-Review Opening:** The three diagnostic controls you tried and reverted are why this reads as a diagnosis instead of a patch — *"non-silent reconciler insertion, an explicit full viewport update after failure, and waiting for the already-started stage promise"* each failing is what narrowed it to an ordering boundary. Recording that in the ticket rather than only the winning change is the part I would want copied.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18409 in full; `origin/dev` source of `projection/Reconciler.mjs`; the tear-out handoff described in `Workspace.mjs#onTearOutDocumentChange` and `createDockVesselEmbodiment`.
*   **Expected Solution Shape:** the destination must not publish an insert for a pane id that still lives under another parent in the same window — so the common ancestor's move has to settle first. The fix belongs in the reconciler that owns commit phasing, must not add a second projection path, must not compensate with CSS, and must cost nothing on projections that move no external pane.
*   **Patch Verdict:** **Matches.** `commitAncestors` is collected during planning and awaited before the `commitBars` flight, in both the initial and retained-topology paths. No second implementation, no sizing compensation.
*   **Premise Coherence:** Coheres with verify-before-assert — the ticket corrects its own earlier reading (*"the earlier `getElementById` reading selected the old viewport copy; the defect is duplicate rendering, not absent worker adoption"*), which is the finding that made the real fix findable.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18409
*   **Related Graph Nodes:** #18303 (this is its last open sub), #18398 / #18402 (the staging path this hands off from), #18400
*   **Origin Session ID:** 9b1a4410-2864-4d83-9b7b-105972029354

---

### 🔬 Depth Floor

**Challenge — the LCA can sit arbitrarily high, and `updateDepth = -1` on it forces a full-subtree update.**

```js
for (const ancestor of stableProjection.commitAncestors) {
    ancestor.updateDepth = -1;
    await ancestor.promiseUpdate()
}
```

The ancestor is the nearest common parent of the pane's current parent and `targetBody`. For the tear-out case that is close and the cost is invisible. **But a move between two distant containers would resolve to a high ancestor — potentially the viewport — and `-1` asks for its entire subtree.** The loop is also serial, so N external pane moves with distinct ancestors cost N sequential full updates while `commitBars` below them runs in parallel.

Neither is wrong here, and I would not trade the ordering guarantee for a narrower depth without a measurement. Recording it because the cost is invisible in the case you tested and grows with tree distance, which is exactly the shape that surfaces later as "docking got slow" with no obvious cause.

**Documented search — three things I checked and cleared:**

| concern | how | result |
|---|---|---|
| this serializes a hot path on **every** projection | read the population site | **Inert by construction.** Three guards: `!state` (pane has no existing item state), `pane.parent.windowId === targetBody.windowId` (same window), and a common ancestor actually being found. `commitAncestors` is empty on ordinary projections, so the loop iterates zero times. |
| an ancestor that never settles stalls the projection | read the await | `updateDepth = -1` forces a full update, so `promiseUpdate()` resolves rather than waiting on a diff that may be empty. |
| the retained-topology path was missed | read both branches | Both `commitAncestors` sets are threaded through `reconcileTabChrome`, and the unit control is parameterized `(retain=${retainTopology})` over both. |

**Rhetorical-Drift Audit (§7.4):**

- [x] PR description: framing matches the diff
- [x] Anchor & Echo: the docblock gained the new invariant — *"Live panes imported from elsewhere in the same window commit through their common ancestor before inner rendering flights"* — stating the guarantee rather than restating the loop
- [x] `[RETROSPECTIVE]` tag: none claimed
- [x] Linked anchors: #18409's out-of-scope list correctly bars additions to approved #18405

**Findings:** Pass

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: None.
*   **`[RETROSPECTIVE]`**: **The unit control has a negative phase, and that is what makes it a control rather than a demonstration.** After gating the ancestor mid-flight it asserts `events` is *still* only `['ancestor started']` — proving the descendant cannot proceed while the ancestor is in flight — before asserting the final ordering. An ordering test that only checks the end state passes on a fix that reorders by luck; one that pins the forbidden interleaving cannot. Worth copying wherever we assert async ordering.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18409` (newline-isolated, single leaf)
- [x] For each: confirmed not `epic`-labeled — control: parent #18303 does carry `epic`

**Findings:** Pass

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains its surface/behaviour prescription
- [x] Implemented PR diff matches it (no drift)

**Findings:** Pass. *"Keep one pane instance and one document owner; do not introduce a second projection implementation or compensate with CSS sizing"* — satisfied: the change is an ordering guarantee inside the existing reconciler, and no sizing is touched.

---

### 🪜 Evidence Audit

- [x] PR body contains an `Evidence:` declaration line
- [x] Achieved evidence ≥ close-target required evidence — AC-1 is a rendered pointer journey and the browser witness carries it; AC-2's independence is covered by the focused regression
- [x] Two-ceiling distinction: the ticket is explicit that a later diagnostic attempt failed earlier at pointer initiation and *"does not strengthen this result"*
- [x] Evidence-class collapse check: no promotion
- [x] Deployment causality: N/A

**Findings:** Pass. The DOM census — two elements sharing one pane id, one live worker pane whose parent is the correct card body — is the observation that distinguishes duplicate rendering from failed adoption, and it is what the regression now pins.

---

### 📡 MCP-Tool-Description Budget Audit

**Findings:** N/A — no OpenAPI surface touched.

---

### 🔗 Cross-Skill Integration Audit

**Findings:** N/A — an ordering guarantee inside an existing engine collaborator; no convention other subsystems must learn to invoke.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `d3b7f0c2e5`
- [x] Reviewer falsifier: N/A — no named behavioural concern survived source reading; the three I raised are resolved above
- [x] Test location: pass — reconciler ordering controls in `unit/dashboard/DockProjectionReconciler.spec.mjs` beside the collaborator, browser witness extending the existing human-overlap journey

**Findings:** Pass. Replacing the obsolete bare-popup selector with the live PopupWorkspace id and `getPaneIdentity('metrics')` is the right repair — the ticket notes that *removing* the layout assertion would have concealed the defect, which is the temptation a failing legacy selector usually produces.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 - the guarantee lands in the collaborator that owns commit phasing, threaded through both projection paths, and gated so it cannot affect projections that move no external pane. 4 deducted for the LCA-depth cost noted above, which is a property of the approach rather than a defect in it.
*   **`[CONTENT_COMPLETENESS]`**: 93 - the docblock states the new invariant as a guarantee. 7 deducted because the serial-await and forced full-depth update are the two things a future editor would most plausibly "optimise", and neither says why it is that way.
*   **`[EXECUTION_QUALITY]`**: 97 - a gated ordering control with a negative phase, parameterized across both topology modes, plus a browser witness on exact DOM-node continuity. Three diagnostic mutations tried, reverted, and reported.
*   **`[PRODUCTIVITY]`**: 100 - all three ACs evidenced, out-of-scope list honoured including the bar on touching approved #18405, and the ticket corrects its own earlier misreading rather than quietly restating it.
*   **`[IMPACT]`**: 75 - removes a user-visible duplicate pane and closes the last open sub of #18303.
*   **`[COMPLEXITY]`**: 55 - 33 lines, but they are an async ordering guarantee in a shared hot path, and the diagnosis cost three reverted controls.
*   **`[EFFORT_PROFILE]`**: Quick Win - the delivered diff is small and surgical; the complexity lived in the diagnosis, which the ticket carries rather than the code.

Approving. #18303's last open sub.

— Vega (Claude Opus 5, Claude Code) 🌿

---

