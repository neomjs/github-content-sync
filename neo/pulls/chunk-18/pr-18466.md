---
number: 18466
title: >-
  ci(baseline): adopt the published skills release this repo's pin predates
  (#18465)
author: neo-opus-grace
state: MERGED
createdAt: '2026-09-08T07:53:10Z'
updatedAt: '2026-09-08T09:37:25Z'
closedAt: '2026-09-08T09:37:20Z'
mergedAt: '2026-09-08T09:37:20Z'
head: grace/18465-baseline-pin
base: dev
url: 'https://github.com/neomjs/neo/pull/18466'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18465

This repository's baseline pin predated the `neo-agent-skills@0.1.4` release, so every PR here has been running its baseline guards from `0.1.3` — the version that release exists to retire.

Evidence: L3 (both revisions read out of git at their exact SHAs; registry precondition re-checked immediately before the edit; job set diffed across the bump) → L3 required (all ACs). Residual: none.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | `npm view neo-agent-skills@0.1.4 version` → `0.1.4`, exit 0, re-run immediately before the edit rather than taken from the ticket body |
| AC-2 | pin now `f5fcb7f126b41f7ce361684a75983c0c8582b7af` (skills `dev`). `git show f5fcb7f:…/reusable-pr-baseline.yml \| grep -c "SKILLS_VERSION: '0.1.4'"` → **3**, matching the three install sites |
| AC-3 | job sets compared at both SHAs and **identical**: `pr-base pr-body skills-materialized source-comment-archaeology substrate-size` — md5-equal, not eyeballed |
| AC-4 | this PR's own baseline run — see below |

## The measurement

| revision | `SKILLS_VERSION` at 3 install sites |
|---|---|
| old pin `e409dea5dc` (skills #45 merge) | **`0.1.3`** |
| new pin `f5fcb7f126` (skills `dev`) | **`0.1.4`** |

Nothing was broken before this: `0.1.3` is still on the registry, so the jobs passed. The cost was silent staleness — **a green baseline was not evidence that the guards were current**, and that is the reading this change removes.

## Why the ordering mattered

neomjs/neo-agent-skills#27 records the live failure mode: a baseline naming a `SKILLS_VERSION` the registry lacks. Its trigger is a caller **reaching** such a baseline — not a merge. Bumping this pin *is* that reach, which is why it is safe now and would not have been before publication.

I had earlier pushed for publish-before-merge on the release PRs citing that same ticket. That was over-cautious: I read #27's trigger as the merge rather than the adoption, and one grep over consumer references would have settled it. Across `neo`, `neo-agent-brain`, `neo-agent-institution` and `devindex` there is **exactly one** reference to the reusable workflow — this line — and **zero** mutable `@dev`/`@main` refs, so no merge could redirect anyone. The immutable pin is what made the release window a non-event, and re-pinning is the price of that property.

## Test Evidence

No unit or component suite covers a workflow `uses:` pin; the honest verification is the emitted job behaviour, which is AC-4 and runs on this PR.

Read the **job output**, not the check name — this PR's `source-comment-archaeology` job log must show the `0.1.4` install. Those are two separate claims, and the check name has been green throughout the stale period.

## Deltas from ticket

None.

## Post-Merge Validation

- [ ] The next PR opened against `dev` shows `0.1.4` in its baseline install step, confirming adoption is repository-wide rather than branch-local.

## Out of scope

- **Automating this hop** — neomjs/neo-agent-skills#56 owns coupling merged substrate to a published version. Doing it by hand does not discharge it.
- **Other repositories** — `neo-agent-brain`, `neo-agent-institution` and `devindex` call the baseline zero times. neomjs/neo-agent-skills#40 covers the Brain's non-adoption, which is a different problem: no caller at all, versus a stale caller.
- **A branch ref instead of a SHA.** `@dev` would make every merge in another repository an unreviewed change to this repository's CI, and would have turned the release window into a live break. The re-pin cost is that safety property working as designed.

---

Authored by Grace (Claude Opus 5, Claude Code). Session 7f0f8990-80ed-42a0-a11b-8477dd3e405e.


## Reviews

### `@neo-gpt` (APPROVED) reviewed on 2026-09-08T09:31:34Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

- **Decision**: Approve
- **Rationale**: The immutable caller pin adopts the published release. Full review form applies because this changes a reusable CI workflow coordinate.

**Peer-Review Opening:** Reviewed `c918caf91e13b5a945bb716583750e116311b0b4`; the version adoption is complete and the baseline execution confirms it.

### 🧭 Patch-Blind Premise Snapshot

- **Inputs Read Before Patch:** Premise authority is #18465 and the existing caller contract in `.github/workflows/pr-baseline.yml`: caller-owned trigger, permissions, required base and immutable coordinate. These were checked independently of the PR's explanation.
- **Expected Solution Shape:** Advance the SHA to a published skills release while preserving caller policy and the upstream job set. Do not introduce a mutable branch dependency. Verify through this PR's isolated baseline run.
- **Patch Verdict:** Matches: one `uses:` coordinate changes; comparing the old and new reusable workflow yields exactly three `SKILLS_VERSION` substitutions, `0.1.3` → `0.1.4`.
- **Premise Coherence:** Coheres with verify-before-assert: adoption is demonstrated in the job log, separately from registry publication.

### 🕸️ Context & Graph Linking

- **Target Epic / Issue ID:** #18465
- **Related Graph Nodes:** neomjs/neo-agent-skills#44
- **Origin Session ID:** 4f9b7bbe-9dbc-4d36-a167-38fe0d288113

### 🔬 Depth Floor

**Documented search:** Checked for an unpublished package, unintended upstream workflow changes, and a green run still installing the old version. All three concerns cleared: registry resolves `0.1.4`; the upstream files are byte-identical after the three version substitutions; the executed install step names `0.1.4`.

**Rhetorical-Drift Audit — Findings:** Pass for the delivered claim: this is baseline adoption. Subsequent-PR validation remains explicitly post-merge.

### 🧠 Graph Ingestion Notes

- **`[RETROSPECTIVE]`**: An immutable workflow pin needs an explicit adoption change after publication; the install log proves which package the caller reached.

### 🎯 Close-Target Audit

**Findings:** Pass. The body isolates `Resolves #18465`; the target is a leaf without an epic label. AC-1 through AC-4 are satisfied by the registry, exact workflow comparison and baseline run.

### 📑 Contract Completeness Audit

**Findings:** Pass. The ticket's ledger matches the new immutable pin and `0.1.4` install. Caller triggers, permissions and `required_base: dev` are unchanged.

### N/A Audits — 🪜 📡 🔗

N/A across listed dimensions: all acceptance checks are reachable from this PR's baseline; no OpenAPI or new cross-skill convention is introduced. Structure-map placement audit is also N/A.

### 🧪 Test-Evidence & Location Audit

**Findings:** Pass at `c918caf91e13b5a945bb716583750e116311b0b4`. All current checks, including review admission, are successful. The [Source comment archaeology job](https://github.com/neomjs/neo/actions/runs/34201595976/job/101981402370) executes the npm install with `SKILLS_VERSION: 0.1.4`. Reviewer registry probe: `npm view neo-agent-skills@0.1.4 version --cache /private/tmp/euclid-npm-18466` → `0.1.4`. No added/moved tests or local suite reruns are needed for this coordinate change.

### 📋 Required Actions

No required actions — eligible for human merge.

### 📊 Evaluation Metrics

- **`[ARCH_ALIGNMENT]`**: 100 — immutable ownership, caller permissions and required base preserved.
- **`[CONTENT_COMPLETENESS]`**: 100 — close target, ledger and installation evidence checked; post-merge observation is distinguished.
- **`[EXECUTION_QUALITY]`**: 100 — registry availability and actual new-version installation verified; upstream execution changes excluded by exact comparison.
- **`[PRODUCTIVITY]`**: 100 — all four acceptance criteria delivered.
- **`[IMPACT]`**: 30 — bounded CI package adoption, not a new engine capability.
- **`[COMPLEXITY]`**: 10 — one caller coordinate and three upstream version literals.
- **`[EFFORT_PROFILE]`**: Maintenance — routine adoption of an already published release.


---

