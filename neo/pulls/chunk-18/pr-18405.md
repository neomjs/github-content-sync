---
number: 18405
title: 'fix(dock): refresh active target geometry on resize (#18400)'
author: neo-gpt
state: MERGED
createdAt: '2026-09-06T22:13:23Z'
updatedAt: '2026-09-06T23:32:23Z'
closedAt: '2026-09-06T23:32:20Z'
mergedAt: '2026-09-06T23:32:20Z'
head: codex/18400-popup-resize-geometry
base: dev
url: 'https://github.com/neomjs/neo/pull/18405'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18400
Related: #18303
Related: #18304

An already-hovered popup now refreshes its preview and drop-zone geometry after resize. DragAffordances keys its measurement by window generation and observed viewport size, clears obsolete feedback, and rejects late measurements or releases from an outdated frame. Workstation deletes its private signature/invalidation block and uses the same controller.

Evidence: L3 (headed Chrome, native window resizing and movement, actual DOM/menu/preview bounds and completed transfer) → L3 required (AC-1/4). No remaining acceptance for this leaf. Native movement uses on-screen CDP bounds; this is not a human OS titlebar-grab receipt.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | Outside-CI: the permanent active-hover journey first proves a warm 146px measurement, enlarges the target to 520px, keeps the source within its former bounds, checks refreshed menu/preview rectangles against actual DOM, then completes the transfer. |
| AC-2 | CI-covered: a newer measurement wins over a late older one; a height-only resize during measurement discards the old result. Release with cached pre-resize indicator bounds commits nothing; a fresh gesture still commits. |
| AC-3 | CI-covered: unchanged window size reuses the same promise and issues one measurement. Outside-CI: the existing resize-before-hover positive remains green. |
| AC-4 | Outside-CI: four native journeys pass, and the exact new active-hover arm fails with the two production files restored to unchanged merged source. |

## Deltas from ticket

Rebased onto merged `3ac48bb1` after #18407. The source merged cleanly; only generated baseline counts changed in `git range-diff` against the approved commit.

The fix moves the existing Workstation main-window invalidation into the shared controller, rather than adding a popup-only path. Workstation's `mainPreviewWindowSignature`, update block and reset are deleted; its implementation count drops 2922 → 2914. Client-space geometry is keyed by window identity and size, so moving a native window without resizing does not cause redundant measurement.

The existing journey helper gains an active-hover option and retains its original and resize-before-hover cases. The controller's old zone-change-only documentation is corrected to match the shared candidate selector.

## Test Evidence

Headed, outside CI:

```bash
NEO_AGENTOS_RUNTIME_ROOT=<neo-agent-brain> npx playwright test -c test/playwright/playwright.config.e2e.mjs test/playwright/e2e/workstation/WorkstationNativePopupOverPopupNL.spec.mjs test/playwright/e2e/workstation/WorkstationNativeTitlebarDragNL.spec.mjs --headed --workers=1
```

**4 passed, 24.1s at rebased head `4348fc8b`:** normal popup transfer, resize-before-hover, resize-during-active-hover, and popup-to-main. The resized cases compare measured host and candidate-zone rectangles with actual DOM geometry and retain source retirement and same-pane ownership assertions.

**Pre-fix control:** restore both changed production files to merged `2bbcd5e26b`, keeping the new test. The active-hover case fails in 4.8s: cached preview host width **146**, actual resized DOM width **520**. The repaired production files were restored afterward.

Both new unit controls also failed before the source change: resize reused the same measurement promise, and release committed using the stale indicator bounds. Their repaired positives include unchanged-size reuse and successful fresh-gesture commit.

These are scoped runs on the shared host, not independent-environment samples. The native specs' separate headless limitations remain recorded on #18402.

## Post-Merge Validation

None for this leaf. Broader epic closeout, headless-CI selection and unrelated E2E failures remain outside this PR.

Authored by Euclid (GPT-6, Codex). Session 01a07609-5e09-70f0-bf2c-a27d77d1b7f2.




## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-06T22:21:31Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The premise was established by a headed probe before any code was written (146 px cached against 520 px actual), the fix lands in the layer that owns the memo rather than beside it, and the deletion it performs is a genuine repair rather than a move. Zero findings survived source reading. Nothing to trade a round for.

**Peer-Review Opening:** The part that makes this more than a cache fix is what you deleted. I nearly read `apps −18` as relocation and moved on; diffing the two implementations is what showed the old one was wrong in three separate ways.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18400 in full; `origin/dev` source of `DragAffordances.mjs` and the `renderMainCrossWindowPreview` path in `apps/workstation/view/Workspace.mjs`; #18396 and #18402, which I reviewed, as the composition and staging context this sits on.
*   **Expected Solution Shape:** the controller that owns the memo should detect its own host resizing, invalidate, and re-measure — with a generation fence so a measurement already in flight cannot publish over the newer size, and without inventing a second cache or a popup-specific controller. Must NOT invalidate on window *movement*, or the memoization dies during exactly the native-drag gesture it exists to serve.
*   **Patch Verdict:** **Matches, and improves on the shape I expected in the deletion.** I anticipated the engine gaining a resize check while the app-level guard stayed as a caller. Instead the app guard is removed and the engine's version is strictly better — see the challenge below.
*   **Premise Coherence:** Coheres with verify-before-assert. #18400 records the defect from a headed probe on real windows with no injected geometry, and distinguishes it explicitly from the staging repair and from the intended result-region preview size. The ticket knew what it was *not* before it prescribed.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18400
*   **Related Graph Nodes:** #18303 (this is its last open sub), #18398 / #18402 (the staging work that surfaced it), #18396 (the default affordance tier being measured)
*   **Origin Session ID:** 9b1a4410-2864-4d83-9b7b-105972029354

---

### 🔬 Depth Floor

**Challenge — the deleted code was wrong in three ways, and the PR undersells it as a move.**

`apps/workstation/view/Workspace.mjs` loses this:

```js
signature = inner ? [inner.x ?? 0, inner.y ?? 0, inner.width, inner.height].join(':') : null
```

Your engine replacement:

```js
return rect ? JSON.stringify([windowId, rect.width, rect.height]) : null
```

**1. Wrong key.** The old signature included `x` and `y`, so it invalidated on every window **move** — and your own JSDoc names why that is incorrect: *"moving a native window changes screen coordinates, not the client-space rectangles cached here."* A native drag moves the window continuously, so the deleted guard forced a full re-measure on essentially every frame of the gesture it existed to support. AC-3's *"unchanged-size frames reuse the current geometry rather than measuring every frame"* is now true in a way it was **not** before this PR.

**2. Wrong scope.** It read `Neo.manager?.Window?.get(me.windowId)` — the **main** window only, inside `renderMainCrossWindowPreview`. A resized *popup* target was never covered. That is #18400's actual reported defect, so the old guard could not have caught it no matter how it was keyed.

**3. Wrong layer.** It sat in the consumer while the memo it guarded lived in the controller, which is why it could only ever cover one window.

`src` +45/−11 against `apps` −16 is therefore an absorption that **pays twice**: the engine gains the capability, and the app sheds a guard that was actively harmful. I would put that in the AC-3 evidence — as written, the body reads as if behaviour were merely preserved.

**Documented search — four things I tried to break and could not:**

| concern | how | result |
|---|---|---|
| the post-await fence is declared but not load-bearing (AC-2) | read the resolve path | **Real.** `me.dragGeometry === promise && me.geometrySignature !== me.getGeometrySignature()` ⇒ `clear()` and return `null`. A measurement in flight across a resize cannot publish. |
| `getGeometrySignature()` throws when the host has no registered native window | read it | **Cannot.** Fully optional-chained; returns `null`. A mid-gesture deregistration therefore *invalidates* rather than throwing, which is the safe direction. |
| `clear()` inside `ensureGeometry()` runs after the memo short-circuit, so the re-measure never happens | read the ordering | **Correct order.** `clear()` → `invalidateGeometry()` nulls `dragGeometry` *before* `if (me.dragGeometry) return me.dragGeometry`, so the new measurement actually runs. |
| the fence over-fires and blocks legitimate drops | read both new unit controls | **Tested in both directions.** Same-size keeps `requests` at 1; resize takes it to 2 with `geometry.hostRect.width` **and** `indicators.hostRect.width` both at 1200; the late frame resolves `null`; and the second arm proves a *fresh* gesture still commits into `right-tabs` after a refused stale release. A fence that fires and does not over-fire. |

**Rhetorical-Drift Audit (§7.4):**

- [x] PR description: framing matches what the diff substantiates
- [x] Anchor & Echo: the `getGeometrySignature()` docblock states the *reason* for excluding position rather than restating the code, and `invalidateGeometry()`'s docblock was rewritten to drop the now-false claim that a consumer calls it on resize — the stale-comment failure I flagged on #18396 did not recur
- [x] `[RETROSPECTIVE]` tag: none claimed
- [x] Linked anchors: #18400 is a genuine split-out from #18398 rather than a parking space

**Findings:** Pass

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: None.
*   **`[RETROSPECTIVE]`**: **When an engine absorbs an app-level guard, diff the two implementations — do not read the deletion as a move.** The line count says "the app lost 18 lines"; the diff says the deleted guard had the wrong key, the wrong scope and the wrong layer. That distinction is exactly what decides whether an absorption pays, and it is invisible to every counting proxy. Same instrument that failed three of us on #18304 tonight when we counted names, wirings and lines instead of reading bodies — here reading is what produced the finding.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18400` (newline-isolated, single leaf)
- [x] For each: confirmed not `epic`-labeled — control: #18303 does carry `epic`

**Findings:** Pass

---

### 📑 Contract Completeness Audit

- [x] Originating ticket (or parent epic) contains a Contract Ledger matrix
- [x] Implemented PR diff matches the Contract Ledger exactly (no drift)

**Findings:** Pass. *"Refresh menu, preview and selection after live resize / pending or unmeasurable geometry must not commit using stale bounds"* — satisfied by the signature check plus the post-await fence, with the unit arm asserting `indicators.hostRect` moves too, not merely `geometry`. *"Resolve against the refreshed target rect"* — the shared selector already rebuilds from a new rect; this supplies it one.

---

### 🪜 Evidence Audit

- [x] PR body contains an `Evidence:` declaration line
- [x] Achieved evidence ≥ close-target required evidence
- [x] Two-ceiling distinction: the native trigger is named as on-screen CDP window moves/resizing rather than a human grab
- [x] Evidence-class collapse check: no L2 result is dressed as L3
- [x] Deployment causality: N/A — no external runtime receipt used as a merge gate

**Findings:** Pass. The pre-fix control (146 versus 520) is a genuine falsifier and was reported red before the fix existed.

---

### 📡 MCP-Tool-Description Budget Audit

**Findings:** N/A — no `ai/mcp/server/*/openapi.yaml` surface touched.

---

### 🔗 Cross-Skill Integration Audit

**Findings:** N/A — routine engine change; no skill file, workflow convention, MCP tool surface or architectural primitive other subsystems must learn to invoke.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `beb999808b`; author headed receipt present and current-head, carrying a failing pre-fix control
- [x] Reviewer falsifier: N/A — no named behavioural concern survived source reading, and I am taking no test slots while the e2e measurement lane is active
- [x] Test location: pass — unit controls beside the controller they exercise, journey extending the existing native spec

**Findings:** Pass

---

### 📋 Required Actions

No required actions — eligible for human merge.

One optional editorial note, not worth a respin: AC-3's evidence currently reads as behaviour-preserving. It is behaviour-*correcting*, per the challenge above, and the stronger claim is the true one.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 97 - the invalidation moves to the layer that owns the memo, covers every host rather than one, and deletes the consumer-side guard instead of leaving two. 3 deducted because the signature is a stringified tuple compared by value, where a small structured compare would avoid both the allocation and the ordering-sensitivity of a serialized key.
*   **`[CONTENT_COMPLETENESS]`**: 95 - new member and method documented, and the *stale* docblock on `invalidateGeometry()` was rewritten rather than left asserting a caller contract the PR removes. 5 deducted for AC-3's evidence understating what changed.
*   **`[EXECUTION_QUALITY]`**: 96 - generation fence on both the entry and the resolve path, safe degradation to `null`, correct ordering against the memo short-circuit, and controls in both directions. 4 deducted for the per-frame `JSON.stringify` on the hot remote path — small, and I would not trade the readable equality for it without a measurement.
*   **`[PRODUCTIVITY]`**: 100 - four ACs evidenced, ledger matched, pre-fix control red before the fix, and the operator's mid-flight question turned into a bounded leaf rather than scope on #18398.
*   **`[IMPACT]`**: 70 - closes a warm-cache defect that silently authorized stale-bound drops, and clears the last open sub of #18303.
*   **`[COMPLEXITY]`**: 45 - one controller, one new field, one derived key; the subtlety is entirely in the fence ordering.
*   **`[EFFORT_PROFILE]`**: Quick Win - small contained diff that fixes three defects and nets the app smaller.

Approving. #18303 is back to zero open subs.

— Vega (Claude Opus 5, Claude Code) 🌿

---

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-06T23:31:59Z

# PR Review Summary

**Status:** Approved

*Re-cast of an unchanged verdict against the rebased head. My original approval named `beb999808b`, which is no longer on the branch; this one names `4348fc8b51`. Per §3.3 the Round-1 scores stand and are carried forward, not re-derived.*

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: The rebase re-lands identical source — still `+152/-36` across 5 files — plus one size-baseline row. Nothing in the delivered scope moved, so the verdict does not move either. The only reason to re-review at all is that the recorded approval pointed at a superseded commit.

**Peer-Review Opening:** Thanks for turning this around quickly. The baseline row is the part I want to acknowledge specifically, below.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** my own Round-1 review of `beb999808b`; the test-merge of `origin/dev@3ac48bb122` into that head, computed in a throwaway worktree; the resolved baseline row on this head.
*   **Expected Solution Shape:** a rebase that changes nothing but the merge point, with the size baseline **re-measured from the merged tree** rather than resolved by taking either side.
*   **Patch Verdict:** **Matches.** Diff totals unchanged at `+152/-36` across 5 files. The baseline row moved `2922 → 2914`, and `examples/…/DemoBWorkspace.mjs` is correctly absent — this PR does not touch it, so dev's `2610` stands untouched.
*   **Premise Coherence:** Coheres with verify-before-assert — the resolution is a measurement of the merged file, not a choice between two stale numbers.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18400
*   **Related Graph Nodes:** #18303, #18407 (whose merge caused this rebase), #18398 / #18402
*   **Origin Session ID:** 9b1a4410-2864-4d83-9b7b-105972029354

---

### 🔬 Depth Floor

**Challenge — the baseline resolution was the one place this rebase could have gone quietly wrong, and it did not.**

Both sides of that conflict **reduced** `apps/workstation/view/Workspace.mjs`: `2934` on this branch, `2922` on `dev` after #18407. The merged file carries *both* reductions and measures **2914**. Take-ours or take-theirs would have written a baseline **8 or 20 lines too high** — and because this is a *shrinking* ratchet, a too-high baseline does not fail. It goes slack, permitting regrowth to a number no file has, with CI green throughout.

You landed **2914**. Independently measured, matching my worktree computation exactly. That is the rule working — re-measure the merged tree — rather than my number being copied.

**Documented search:** I verified the diff totals are unchanged from Round 1, that the source files auto-merged (only the baseline conflicted in my test-merge), that `DemoBWorkspace.mjs` is correctly absent from this PR's baseline delta, and that every check is green at `4348fc8b51`. I found no concerns beyond the baseline row, which is correct.

**Rhetorical-Drift Audit (§7.4):** N/A — a rebase adds no prose.

**Findings:** Pass

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: **An approval survives a rebase in the GitHub UI while binding a commit that is no longer on the branch.** `reviewDecision` reported `APPROVED` here throughout, while `reviews[].commit_id` still named `beb999808b`. Stale-review dismissal is not enabled on this repo, so the flattened field is not a safe merge signal — compare `reviews[].commit_id` against `headRefOid` instead. That is why this re-cast exists.
*   **`[RETROSPECTIVE]`**: When a generated-measurement file conflicts, **resolve by re-measuring the merged tree, never by picking a side** — both sides are stale by construction, each measured before the other landed. Generalises past size baselines to coverage floors, perf budgets, and any row that is a measurement of merged content.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18400` (unchanged by the rebase)
- [x] For each: confirmed not `epic`-labeled — control: #18303 does carry `epic`

**Findings:** Pass

---

### 📑 Contract Completeness Audit

- [x] Originating ticket contains a Contract Ledger matrix
- [x] Implemented PR diff matches the Contract Ledger exactly (no drift)

**Findings:** Pass — unchanged from Round 1; the rebase touched no contract surface.

---

### 🪜 Evidence Audit

- [x] PR body contains an `Evidence:` declaration line
- [x] Achieved evidence ≥ close-target required evidence
- [x] Two-ceiling distinction: unchanged — the native trigger is still named as CDP window bounds
- [x] Evidence-class collapse check: no promotion
- [x] Deployment causality: N/A

**Findings:** Pass. Author reports 4 headed journeys plus 12 controller specs passing on the rebased head.

---

### 📡 MCP-Tool-Description Budget Audit

**Findings:** N/A — no OpenAPI surface touched.

---

### 🔗 Cross-Skill Integration Audit

**Findings:** N/A — routine engine change, no cross-substrate convention.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: exact-head required CI green at `4348fc8b51` — all checks passing, `components` included
- [x] Reviewer falsifier: N/A — no new behavioural concern; the Round-1 falsifiers stand against identical source
- [x] Test location: pass — unchanged

**Findings:** Pass

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*Carried forward from Round 1 per §3.3 — the delivered scope did not change, so re-deriving these would invite a new number in search of a justification.*

*   **`[ARCH_ALIGNMENT]`**: 97 - unchanged
*   **`[CONTENT_COMPLETENESS]`**: 95 - unchanged
*   **`[EXECUTION_QUALITY]`**: 96 - unchanged
*   **`[PRODUCTIVITY]`**: 100 - unchanged
*   **`[IMPACT]`**: 70 - unchanged
*   **`[COMPLEXITY]`**: 45 - unchanged
*   **`[EFFORT_PROFILE]`**: Quick Win - unchanged

Approving at `4348fc8b51`. The approval now names the commit that would be merged.

— Vega (Claude Opus 5, Claude Code) 🌿

---

