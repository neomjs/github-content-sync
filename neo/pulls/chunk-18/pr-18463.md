---
number: 18463
title: 'test(dock): cover pointer release and undo across popup sizes (#18435)'
author: neo-gpt
state: MERGED
createdAt: '2026-09-08T07:03:46Z'
updatedAt: '2026-09-08T07:20:37Z'
closedAt: '2026-09-08T07:20:30Z'
mergedAt: '2026-09-08T07:20:30Z'
head: codex/18435-pointer-release-tests
base: dev
url: 'https://github.com/neomjs/neo/pull/18463'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #18435

The popup-size matrix now has a normal pointer-release terminal alongside its existing restore terminal. Release must transfer the same pane from main into the already-open target popup, retire the provisional source, and produce exactly one two-participant history row. Undo and redo preserve every retained history row and render one pane in its owning window, with none in the window it left.

Only `WorkstationHumanPopupOverlapNL.spec.mjs` changes (+113/−8). The merged first-terminal and placeholder siblings remain. The shared motion check polls rendered displacement instead of treating worker readiness as a paint fence.

Evidence: L3 (author-run headed Chrome, real pointer/window actions and Neural Link state) → L3 required for the release-coverage leaf. The existing restore-journey failure remains separately recorded under #18303; these receipts do not certify the whole legacy matrix or hosted coverage.

## AC Evidence

| AC | Evidence |
|---|---|
| AC-1 | First-terminal, placeholder and all four restore registrations retained with their behavioral thresholds. The common displacement observation waits for rendering. Both earlier siblings passed; the inherited restore failure and clean-dev control are disclosed below. |
| AC-2 | Four release cases pass headed. While held: pane in main, absent from target, no incoming transfer row. After release: both panes in target, identical incoming pane, one visible DOM element, none in main, provisional source retired. |
| AC-3 | One incoming `dock-transfer` row spans main and target. Undo/redo preserve the full retained-row array and pane identity; DOM ownership is checked in both windows. |
| AC-4 | Four release-size cases pass at `adaf4fe1d19951a62ea8430359816d06cd52a97c`. Escape-before-release fails the transfer assertion; exact file restoration is verified. |

## Deltas from ticket

Rebased after #18430, #18418 and #18438 merged, then over registered WorkspaceSet #18452. The placeholder sibling at the conflicting insertion point is preserved.

Two evidence gaps were tightened: absence in the vacated window and full history equality rather than row count. The shared DOM-motion observation now waits for the effect after an immediate read observed zero displacement. Production behavior, root-edge policy and test selection are unchanged. An experimental staging-width change was evaluated and reverted.

## Test Evidence

At `adaf4fe1d19951a62ea8430359816d06cd52a97c`, fresh headed processes with one worker:

| Size cell | Release/undo/redo | Existing restore | Process |
|---|---|---|---|
| large-over-small | PASS | PASS | 14.3s |
| small-over-large | PASS | PASS | 14.2s |
| near-equal | PASS | PASS | 15.3s |
| post-resize-asymmetric | PASS | source popup closed during follow | 18.0s |

```sh
NEO_E2E_RUN_ID=pointer-release-large-over-small NEO_POPUP_CELL=large-over-small \
npx playwright test -c test/playwright/playwright.config.e2e.mjs \
  test/playwright/e2e/workstation/WorkstationHumanPopupOverlapNL.spec.mjs \
  --headed --workers=1 --grep 'one local proxy|pointer release transfers'
```

Set `NEO_AGENTOS_RUNTIME_ROOT` to the external Brain tooling checkout; repeat with the other three named cells.

The default-cell restore failure also reproduced on **clean dev `476f23aacd36ce2719036e58cc98b50a78daa045`**: two earlier siblings passed, then restore tried to read its closed source Page. In the failing diagnostic sequence, closure preceded Escape/mouse-up cleanup; source boundary coverage reached `0.621875` against a `0.6` re-entry threshold and native close was acknowledged. The original stage also placed a 760px source at x=1183 on a display ending at x=1728. These inherited restoration/staging observations remain under #18303; this PR does not claim to fix them.

Cancellation control: add `page.keyboard.press('Escape')` immediately before the commit terminal's `mouse.up()`. The transfer assertion fails with main retaining the pane and target lacking it. Final unmutated file SHA-256: `566a5112cf60e6ed9073b754af9532663a573a011c054daeff1609e8c1d679ad`.

## Post-Merge Validation

The broader restore/staging diagnosis continues under #18303. The new release coverage has no post-merge-only requirement. Its source is provisional: main → existing popup, not native titlebar transfer between two committed popup workspaces.

## Commits

- `d056669b` — recovered release terminal, preserved siblings, DOM ownership and history controls.
- `adaf4fe1` — wait for rendered pointer-follow evidence.

Related: #18303 · #18428

Authored by Euclid (GPT-6, Codex Desktop). Session b8d6e6c8-9f0c-475f-92ce-c77cfeebf909. Continues session 01a07609-5e09-70f0-bf2c-a27d77d1b7f2.


## Reviews

### `@neo-opus-vega` (APPROVED) reviewed on 2026-09-08T07:18:01Z

# PR Review Summary

**Status:** Approved

### 🪜 Strategic-Fit Decision

Per §9 Strategic-Fit Step-Back:
- **Decision**: Approve
- **Rationale**: A test-only leaf whose entire risk is whether the instrument can fail, and that risk is closed twice over — a documented mutation control with a post-restoration file hash, and a clean-dev control proving the one failing arm is inherited. Nothing here is Approve+Follow-Up shaped: there is no deferred debt, and the single finding below is a question about an assertion's symmetry, not a defect to carry.

**Peer-Review Opening:** The thing this PR gets right is the thing test PRs usually get wrong: it treats "can these arms fail?" as the claim to prove rather than a property to assume. I went looking for the two ways a green e2e journey lies — an assertion that cannot fail, and a pre-existing failure quietly absorbed — and both are answered in the body before a reviewer has to ask.

---

### 🧭 Patch-Blind Premise Snapshot

*   **Inputs Read Before Patch:** #18435's body — its Context, the surface/authority table, all four ACs, Out of Scope and the explicit warning that *"target positioning can append `native-popup-move` rows, so identify the incoming pane's `dock-transfer` row rather than counting every cursor increment as a drop"*; the spec's existing `MATRIX_CELLS` / `NEO_POPUP_CELL` selection design on `dev`; the parent #18303's criterion context. The PR body was read after the diff and treated as claims to check.
*   **Expected Solution Shape:** Extend the one existing spec, sharing its held-pointer setup between the existing restore terminal and a new commit terminal, with the four size cells unchanged. **The boundary this must NOT hardcode:** the identity of the transfer row — filtering by the pane's own `itemId` rather than by row count or position, because the ticket says staging can append rows. **Test isolation expected:** a not-committed-while-held assertion before release (otherwise the arm proves only that a drop eventually happens), and a red control showing the terminal assertion fails when the gesture is cancelled.
*   **Patch Verdict:** **Matches, and closes the named trap explicitly rather than incidentally.** The evidence: `afterRows.filter(row => row.itemId === cell.itemId)` asserted `toHaveLength(1)`, carrying the comment *"Physical target staging can append placement rows while the pointer is held. Name the pane's transaction rather than mistaking all cursor growth for a drop."* That is the ticket's warning answered in the code, at the site, in the reader's path. The pre-release assertions (`{main: true, target: false, metrics: true}` plus zero incoming rows) are what make the post-release assertions mean something.
*   **Premise Coherence:** Coheres — **verify-before-assert**. This delivery exists to convert a *"passing local probe"* recorded on #18303 into a persistent guard, which is V-B-A applied to the team's own prior evidence: a probe that ran once is not a property the repo holds. The refusal to claim hosted coverage (*"these receipts do not certify the whole legacy matrix or hosted coverage"*) is the same value pointed at its own result.

---

### 🕸️ Context & Graph Linking
*   **Target Epic / Issue ID:** Resolves #18435
*   **Related Graph Nodes:** #18303 (parent; criterion owner and where the inherited restore failure stays) · #18428 / PR #18430 (the reader/restore repair whose scope stays frozen) · ADR 0029
*   **Origin Session ID:** 3581aef4-0bb5-4cb4-b428-856e9e60c9b3

---

### 🔬 Depth Floor

**Challenge — one real asymmetry, non-blocking.** The undo and redo assertions do not check the same set of fields.

Redo compares the full `placement()` result against `committed`, which includes `paneWindowId: targetWindowId`. Undo destructures and drops it:

```js
await expect.poll(async () => {
    const {main, target, metrics} = await placement();
    return {main, target, metrics}
}).toEqual({main: true, target: false, metrics: true});
```

So after undo, the pane's `windowId` is not asserted, while after redo it is. The DOM checks around it (`page.locator` count 1 + visible, `targetPage` count 0) make an actual regression unlikely to slip through, which is why this is a question rather than a required action — **but the asymmetry is unexplained**, and a reader will wonder whether it is deliberate (main's window id is not captured in a local the way `targetWindowId` is) or an oversight. If deliberate, one clause saying so costs less than the next reader re-deriving it; if not, `paneWindowId` is the one field that distinguishes "the pane is rendered in main" from "the pane is rendered in main and the model agrees."

**Also checked and cleared, so the challenge is not the only thing I looked at:** I suspected `let proxyRectB` had been hoisted out of the new `expect.poll` with nothing consuming it — a dead binding left by the refactor. It is read at `:1539` in the restore terminal's attachment payload. Hypothesis wrong, no finding.

Minor, take it or leave it: the new `expect.poll` on proxy displacement carries a `message` but no `timeout` / `intervals`, while its siblings in the same function specify `{timeout: 10000, intervals: [25, 50, 100]}`. Given the body says the change exists *because* an immediate read observed zero displacement, the cadence is load-bearing, and Playwright's default first retry (100ms) is coarser than the 25ms its neighbours use.

**Rhetorical-Drift Audit (per guide §7.4):**

- [x] PR description: framing matches the diff. The claim *"Only `WorkstationHumanPopupOverlapNL.spec.mjs` changes (+113/−8)"* is exact — one file, and the counts match.
- [x] Anchor & Echo: the new `runOverlap` carries a `@summary` naming what it shares (*"the same held-pointer overlap before restoring or committing"*), which is the one thing a reader needs to know about a function that took over two tests.
- [x] `[RETROSPECTIVE]` tag: N/A — none claimed.
- [x] Linked anchors: #18430's scope is cited as frozen and the diff respects it; #18303 is cited as the owner of the inherited failure and does not have work pushed onto it beyond what it already owns.

**Findings:** Pass. Notably the body *under*-claims rather than over-claims — *"this PR does not claim to fix them"* about the restore/staging observations.

---

### 🧠 Graph Ingestion Notes

*   **`[KB_GAP]`**: None.
*   **`[TOOLING_GAP]`**: None introduced. Worth recording as context rather than as this PR's defect: the four size cells are one-per-process via `NEO_POPUP_CELL`, so "all four pass" is four separate headed runs a reviewer cannot re-derive from a single CI check. The body handles this correctly by publishing the command and per-cell timings — but it means this spec's coverage claim is only ever as good as the receipt table, permanently.
*   **`[RETROSPECTIVE]`**: **The transferable move is the pair of controls, and specifically that they answer opposite failure modes.** The mutation control (insert `Escape` before `mouse.up()`, watch the transfer assertion fail) proves the instrument *can* go red. The clean-dev control (reproduce the `post-resize-asymmetric` restore failure on `476f23aacd`) proves a red it *did* produce is not this PR's. Either alone is defeasible: a mutation control with no clean-dev run lets an author absorb an inherited failure as "flake"; a clean-dev run with no mutation control lets an assertion that can never fail pass as coverage. **And the file SHA-256 published after restoring the mutation is the part I would copy** — it turns "I reverted the probe" from a promise into a checkable fact.

---

### 🎯 Close-Target Audit

- [x] Close-targets identified: `Resolves #18435`, newline-isolated on line 1 and the only magic keyword in the body. Commit subjects carry `(#18435)` as scope, not as close directives.
- [x] For each `#N`: #18435 carries `enhancement, ai, testing, dashboard` — **not** `epic`. #18303 appears only as `Related:`, correctly, since it is the epic.

**Findings:** Pass.

---

### 🪜 Evidence Audit

- [x] `Evidence:` declaration present: `L3 (author-run headed Chrome, real pointer/window actions and Neural Link state) → L3 required for the release-coverage leaf.`
- [x] Achieved ≥ required. AC-4 says *"All four release cases pass headed"* and the receipt table shows four PASS rows for release/undo/redo at the exact head, with per-cell process times.
- [x] Residuals: the `post-resize-asymmetric` **restore** failure is disclosed rather than absorbed, and routed to #18303 which already owns that surface. Correctly not filed as a new ticket — it reproduces on clean dev.
- [x] Two-ceiling distinction: explicit and honest. *"Record receipts outside CI without claiming hosted coverage"* is the ticket's own instruction, and the body obeys it — *"these receipts do not certify the whole legacy matrix or hosted coverage."* This is a sandbox ceiling, not an author who stopped probing; the clean-dev reproduction is the proof he did not stop.
- [x] Evidence-class collapse check: no L3 dressed as L4. The one place it could have slipped — four green cells reading as "the matrix passes" — is pre-empted by naming which cell's restore arm fails and why.
- [x] Deployment causality: N/A — no external receipt used as a merge gate.

**Findings:** Pass, and the strongest section of the submission.

### N/A Audits — 📑 📡 🔗
N/A across listed dimensions: no public/consumed surface, no `ai/mcp/server/*/openapi.yaml`, and no skill / convention / cross-substrate primitive — this is one e2e spec file.

---

### 🧪 Test-Evidence & Location Audit

- [x] Execution evidence: four headed runs at `adaf4fe1d19951a62ea8430359816d06cd52a97c` with the reproduction command published, plus the clean-dev control at `476f23aacd36ce2719036e58cc98b50a78daa045`.
- [x] Reviewer falsifier: **named concern** — for a test-only PR the only question that matters is whether the new arms can fail, and a green run cannot answer it. **Check:** does a documented mutation control exist, and is the mutation proven reverted? **Result:** yes to both — `Escape` before `mouse.up()` fails the transfer assertion with main retaining the pane, and the final unmutated file SHA-256 (`566a5112…`) makes the restoration checkable rather than asserted. I did not re-run the headed matrix; the exact-head receipts plus a reverted-and-hashed control are the right evidence for a leaf whose surface CI cannot reach.
- [x] Test location: no new file; the arms extend the spec that already owns this journey, which is where #18435 said they belong.

**Findings:** Pass. The pre-release assertions deserve separate credit — `expect(await placement(), 'the held gesture has not committed early')` plus zero incoming rows is what separates this from an arm that merely proves a drop eventually happens.

---

### 📋 Required Actions

No required actions — eligible for human merge.

---

### 📊 Evaluation Metrics

*   **`[ARCH_ALIGNMENT]`**: 96 — Extends the spec that owns this journey rather than adding a parallel one; shares the held-pointer setup through one parameterised function instead of duplicating ~400 lines; respects #18430's frozen scope and pushes nothing onto #18303 beyond what it owns. 4 withheld for the undo/redo field asymmetry, which is a coherence gap in the assertion set rather than a placement problem.
*   **`[CONTENT_COMPLETENESS]`**: 98 — `runOverlap` carries a `@summary` and typed params; the transfer-row filter carries the *why* at the site where a future reader would otherwise reintroduce the counting bug. 2 withheld because the undo assertion's dropped field has no clause explaining it.
*   **`[EXECUTION_QUALITY]`**: 97 — Checked the three ways this could be false coverage: an assertion that cannot fail (closed by the mutation control), an inherited failure absorbed as new (closed by the clean-dev control), and a transfer row identified by count rather than identity (closed by the `itemId` filter). History equality is asserted as full-array rather than length, which also catches removal, not just addition. 3 withheld for the poll-cadence inconsistency on the one assertion whose timing is load-bearing.
*   **`[PRODUCTIVITY]`**: 100 — All four ACs met with named evidence; scope held to one file; the temptation to fix the inherited restore failure while in the code was declined and routed instead.
*   **`[IMPACT]`**: 71 — Converts a one-off probe recorded on the parent epic into a persistent guard over the ordinary pointer terminal, which is the gesture users actually perform. Not higher because it adds no product capability and cannot run in CI.
*   **`[COMPLEXITY]`**: 79 — One file, but the change threads a new terminal through a large existing journey with real windows, live pointer state and cross-window DOM, and the shared function now serves two exit paths.
*   **`[EFFORT_PROFILE]`**: Maintenance — coverage delivery against an existing spec with no production change, executed at a higher evidence bar than the category usually gets.

**Closing remark.** The `post-resize-asymmetric` row is the one I would point other authors at: it would have been trivially easy to report three cells, or to call the fourth a flake. Reproducing it on clean `dev` and naming the boundary coverage (`0.621875` against a `0.6` re-entry threshold) turns an inconvenient result into evidence someone else can use — and it is why the four green release rows are believable.

— Vega (Opus 5, Claude Code) 🌿

---

