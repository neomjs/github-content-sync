---
number: 19200
title: 'fix(form): the clear trigger shows for a zero value (#19143)'
author: GautamSahu028
state: MERGED
createdAt: '2026-09-25T09:11:39Z'
updatedAt: '2026-09-25T10:33:58Z'
closedAt: '2026-09-25T10:08:38Z'
mergedAt: '2026-09-25T10:08:38Z'
head: fix/clear-trigger-zero
base: dev
url: 'https://github.com/neomjs/neo/pull/19200'
contentTrust:
  projected: true
  quarantined: 1
  signals: []
---
Please make sure to read the Contributing Guidelines:

[QUARANTINED_URL: github.com]

<!-- PULL REQUEST TEMPLATE -->
<!-- (Update "[ ]" to "[x]" to check a box) -->

**Does this PR resolve an issue?** (Required)

Resolves #19143

**What kind of change does this PR introduce?** (check at least one)

- [x] Bugfix
- [ ] Feature
- [ ] Code style update
- [ ] Refactor
- [ ] Build-related changes
- [ ] Other, please describe:

**Does this PR introduce a breaking change?** (check one)

- [ ] Yes
- [x] No

If yes, please describe the impact and migration path for existing applications:

**The PR fulfills this requirement:**

- [x] It's submitted to the `dev` branch, _not_ the `main` branch

If adding a **new feature**, the PR's description includes:
- [ ] A convincing reason for adding this feature (to avoid wasting your time, it's best to open a suggestion issue first and wait for approval before working on it)

**Other information:**

`Clear.getHiddenState()` turned `0` into `'0'` so that it counts as a value, but the return statement then checked `field.value` (still `0`, which is falsy), so a zero hid the clear button. It now checks the local `value`.

New spec: `test/playwright/unit/form/field/trigger/Clear.spec.mjs` covers NumberField `5`, `0`, `7`, `null` and TextField `''`, `'x'`. The `Neo.main.DomEvents` stub is installed in `beforeAll` only if missing and removed in `afterAll`.

Spec written first; failing output before the fix:

```
1) [unit-engine] › test\playwright\unit\form\field\trigger\Clear.spec.mjs:49:9 › Neo.form.field.trigger.Clear › NumberField value 0 => clear trigger hidden: false

   Error: expect(received).toBe(expected) // Object.is equality

   Expected: false
   Received: true

     53 |             field.value = value;
     54 |
   > 55 |             expect(trigger.getHiddenState()).toBe(hidden);
        |                                              ^

  1 failed
  5 passed (900ms)
```

After the fix:
- `npm run test-unit -- test/playwright/unit/form/field/trigger/Clear.spec.mjs`: 6 passed.
- `npm run test-unit` (Windows 11): 51 failed, 4506 passed. The same 51 also fail without this change (52 then, the extra one being the `0` row). None are in `form/`; they are in build scripts, git hooks, symlink and packaging specs. Relying on CI for the full-suite result.
- Checked in the browser: `examples/form/field/number/` with `clearToOriginalValue` unticked. A value of `0` now keeps the ✕ visible, and clicking it clears the field.


## Comments

### `@GautamSahu028` commented on 2026-09-25T10:10:03Z

> @GautamSahu028, thank you. This is a clean first PR, and the red-first output in your body is exactly the evidence we look for.
> 
> # PR Micro-Review
> **Class:** contained. It fixes one token in `Clear#getHiddenState()` and adds its regression spec, with no architectural concept.
> 
> **Verdict:** Approved
> 
> **Glance:** Verified at `203de91bc2`. `getHiddenState()` already turned `0` into `'0'` so it would count as a value, then tested `field.value`, which is still `0` and falsy. Testing the local `value` fixes that. `null`, `undefined` and `''` still hide the trigger through `!value`. `'0'` no longer does, and the `clearToOriginalValue` branch is untouched. The method's other readers pick up the fix too: `onFieldChange`, and `Text#afterSetReadOnly`, which recomputes the trigger when a field leaves read-only. CI at this head is green, 18/18. I re-ran your red-first claim with `dev`'s `Clear.mjs` restored: only the `NumberField value 0` row fails, and the other five pass. The `DomEvents` stub is installed only if missing and removed in `afterAll`, so it can't leak into other spec files in the same worker. I checked for a caller that relied on the old result for `0` and found none.
> 
> **Findings:** None.
> 
> One question, not blocking. Your body reports 51 unit failures on a clean Windows 11 clone, in build-script, git-hook, symlink and packaging specs, with or without your change. That's a gap in our contributor setup, not in your PR. Could you paste the list of failing spec files, here or on #18985? We want the next Windows contributor to start green.
> 
> * **Origin Session ID:** 0f80515e-7682-4313-8101-b926da48c55c
> 
> ⚖️ Ada, Claude Opus 5.5, Claude Code. Eligibility rules: pr-review-guide §6.4.

Thanks for the review!

Here are the failing spec files from `npm run test-unit` on a clean Windows 11 clone (Node, `npm install` + `npm run bundle-browser-deps`), at 203de91bc2. Each is followed by its number of failing tests. The same set fails at `dev` without this change.

| Failures | Spec |
|---:|---|
| 10 | `test/playwright/unit/buildScripts/docletCompactJsdoc.spec.mjs` |
| 6 | `test/playwright/unit/buildScripts/esmDistTransforms.spec.mjs` |
| 5 | `test/playwright/unit/main/MainWorkspaceAddon.spec.mjs` |
| 4 | `test/playwright/unit/buildScripts/buildDependencyResolution.spec.mjs` |
| 4 | `test/playwright/unit/buildScripts/distAppAssets.spec.mjs` |
| 3 | `test/playwright/unit/dashboard/DockImportDirection.spec.mjs` |
| 3 | `test/playwright/unit/util/HighlightJs.spec.mjs` |
| 2 | `test/playwright/unit/buildScripts/esmWorkspaceBuild.spec.mjs` |
| 2 | `test/playwright/unit/buildScripts/helpers/watchThemes.spec.mjs` |
| 1 | `test/playwright/unit/app/content/LearnCorpusLinks.spec.mjs` |
| 1 | `test/playwright/unit/apps/portal/view/news/discussions/Component.spec.mjs` |
| 1 | `test/playwright/unit/buildScripts/checkSpecRetirement.spec.mjs` |
| 1 | `test/playwright/unit/buildScripts/dockRailTabLayering.spec.mjs` |
| 1 | `test/playwright/unit/buildScripts/util/check-branch-discipline.spec.mjs` |
| 1 | `test/playwright/unit/buildScripts/util/check-whitespace.spec.mjs` |
| 1 | `test/playwright/unit/buildScripts/util/developmentThemeAssets.spec.mjs` |
| 1 | `test/playwright/unit/e2e/browserLifecycleReporter.spec.mjs` |
| 1 | `test/playwright/unit/e2e/glState.spec.mjs` |
| 1 | `test/playwright/unit/test/unitRunPolicy.spec.mjs` |
| 1 | `test/playwright/unit/worker/CanvasRemoteWindowId.spec.mjs` |

Total: 50 across 20 files.

One more, `test/playwright/unit/main/addon/ResizeObserver.spec.mjs` (1 test), failed in the full parallel run but passes on its own (11/11), so it looks like a timing flake rather than a Windows issue.

I haven't dug into the root causes. From the names, most involve paths, symlinks, git and the packed/dist tree. Happy to run any of them individually and share the full error output if that helps.


---

### `@neo-opus-ada` commented on 2026-09-25T10:13:23Z

@GautamSahu028, thank you, and congratulations on your first merged PR! This table is the first Windows evidence we've had, because none of our maintainer machines runs Windows.

The file names hint at a few shared causes, but we don't know them yet. Could you run the suite once more with the list reporter and attach the output file to a comment on #18985, our contributor-setup issue?

```bash
npm run test-unit -- --reporter=list > windows-unit.txt 2>&1
```

That includes the `ResizeObserver.spec.mjs` run. We've seen other specs under `test/playwright/unit/main/` fail only in the full run too, so its error helps as well.

With the errors in hand, I'll group the failures by cause and file each group as its own `help wanted` issue. Since your first PR is merged, `help wanted` is your next step in CONTRIBUTING, and your Windows machine makes you the best person to fix and verify these. If you want one, it's yours.

⚖️ Ada (a maintainer on this repo)


---

### `@GautamSahu028` commented on 2026-09-25T10:33:58Z

 

> @GautamSahu028, thank you, and congratulations on your first merged PR! This table is the first Windows evidence we've had, because none of our maintainer machines runs Windows.
> 
> The file names hint at a few shared causes, but we don't know them yet. Could you run the suite once more with the list reporter and attach the output file to a comment on #18985, our contributor-setup issue?
> 
> ```shell
> npm run test-unit -- --reporter=list > windows-unit.txt 2>&1
> ```
> 
> That includes the `ResizeObserver.spec.mjs` run. We've seen other specs under `test/playwright/unit/main/` fail only in the full run too, so its error helps as well.
> 
> With the errors in hand, I'll group the failures by cause and file each group as its own `help wanted` issue. Since your first PR is merged, `help wanted` is your next step in CONTRIBUTING, and your Windows machine makes you the best person to fix and verify these. If you want one, it's yours.
> 
> ⚖️ Ada (a maintainer on this repo)

Glad the fix landed and that the Windows run turned out useful.

I've run the suite again with the list reporter and posted the output on #18985. Result: 50 failed, 4507 passed, 3 skipped, the same 50 across 20 spec files as in my table above.

`main/addon/ResizeObserver.spec.mjs` passed in this run, so the file has no error for it. It has failed once in three runs (once in a full run; it passed on its own and in this run), so it looks intermittent under the full parallel run. If it fails again, I'll post that output too.

And yes, I'd like one of the help wanted issues once you've grouped them. Assign me whichever you think fits a first follow-up, and I'll fix and verify it on Windows.


---



## Reviews

### `@neo-opus-ada` (APPROVED) reviewed on 2026-09-25T10:05:52Z

@GautamSahu028, thank you. This is a clean first PR, and the red-first output in your body is exactly the evidence we look for.

# PR Micro-Review

**Class:** contained. It fixes one token in `Clear#getHiddenState()` and adds its regression spec, with no architectural concept.

**Verdict:** Approved

**Glance:** Verified at `203de91bc2`. `getHiddenState()` already turned `0` into `'0'` so it would count as a value, then tested `field.value`, which is still `0` and falsy. Testing the local `value` fixes that. `null`, `undefined` and `''` still hide the trigger through `!value`. `'0'` no longer does, and the `clearToOriginalValue` branch is untouched. The method's other readers pick up the fix too: `onFieldChange`, and `Text#afterSetReadOnly`, which recomputes the trigger when a field leaves read-only. CI at this head is green, 18/18. I re-ran your red-first claim with `dev`'s `Clear.mjs` restored: only the `NumberField value 0` row fails, and the other five pass. The `DomEvents` stub is installed only if missing and removed in `afterAll`, so it can't leak into other spec files in the same worker. I checked for a caller that relied on the old result for `0` and found none.

**Findings:** None.

One question, not blocking. Your body reports 51 unit failures on a clean Windows 11 clone, in build-script, git-hook, symlink and packaging specs, with or without your change. That's a gap in our contributor setup, not in your PR. Could you paste the list of failing spec files, here or on #18985? We want the next Windows contributor to start green.

- **Origin Session ID:** 0f80515e-7682-4313-8101-b926da48c55c

⚖️ Ada, Claude Opus 5.5, Claude Code. Eligibility rules: pr-review-guide §6.4.

---

