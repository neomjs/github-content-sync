---
number: 118
title: >-
  fix(baseline): a bot's pull request is reported, not judged, for its close
  target (#117)
author: neo-opus-grace
state: OPEN
createdAt: '2026-09-24T20:47:49Z'
updatedAt: '2026-09-24T20:49:03Z'
closedAt: null
mergedAt: null
head: grace/117-bot-close-target
base: dev
url: 'https://github.com/neomjs/neo-agent-skills/pull/118'
contentTrust:
  projected: true
  quarantined: 0
  signals: []
---
Resolves #117

A dependabot pull request stops failing `PR body`. Since #103, the close target has been judged for every author, and a bot's body never carries `Resolves #N`. As a result, every version bump in every consumer reds, including #114 AC-4's tag move.

**The scope:**
- `Validate the close target` runs for every author whose `user.type` is not `Bot`, draft included. A missing type is not `Bot`, so it is judged: the check fails closed.
- For a `Bot` author, a new step logs that the close target was not judged and why, naming the login. The green says what happened, so it is not the vacuous pass #103 removed.
- Human and agent PRs are judged exactly as before. The operator merges bot PRs without a ticket (neomjs/neo#19048, #19025, #18815 and #18756; neomjs/neo-agent-institution#188). The check now matches that practice.

**Release 0.1.19:** 0.1.18 is published, so this PR moves `package.json`, the lockfile and all seven `SKILLS_VERSION` pins.

**Load effect:** none. The reusable workflow is not loaded into any agent's context.

Evidence: L2 (the workflow contract suite requires the scope and the report step; three new mutations) → L2 required (AC-1…AC-3). Residual: a live dependabot PR going green at 0.1.19, Residual-Owner: #114 (AC-4, open).

## AC Evidence
| AC | Evidence |
|---|---|
| AC-1 | The close-target step carries `if: ${{ github.event.pull_request.user.type != 'Bot' }}`. `Report a bot's unjudged close target` carries `== 'Bot'` and echoes the login. |
| AC-2 | `test-reusable-pr-baseline` requires that exact gate and the report step. Three new mutations fail: the gate dropped (a bot is judged), the gate widened to exempt a `User`, and the report step removed. 71 mutations pass. |
| AC-3 | The job comment scopes #103's rule to "every pull request a person or agent opens" and names the bot report. |

## Deltas from ticket
- None.

## Test Evidence
- `npm test` exits 0; `test-reusable-pr-baseline`: canonical contract + 71 negative mutations passed.
- `node scripts/lint-skill-corpus.mjs --base origin/dev`: within budget.
- `grep -c 0.1.18` over `package.json`, `package-lock.json` and the workflow returns 0 in each.

## Post-Merge Validation
- [ ] After 0.1.19 is published and tagged, a consumer's dependabot PR shows `Report a bot's unjudged close target` run, `Validate the close target` skipped, and `PR body` green. This is #114 AC-4.

## Commits
- `9b9ce3d`: the close target is judged for every author but a Bot, and a Bot's exemption is reported.
- `1bf0519`: 0.1.19.

Authored by Grace (Claude Opus 5.5, Claude Code). Session 1f7129c9-c0f7-42e0-ba47-7a42e5ac57c2.
